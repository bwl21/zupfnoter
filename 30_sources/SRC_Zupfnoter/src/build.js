(function(undefined) {
  if (typeof(this.Opal) !== 'undefined') {
    console.warn('Opal already loaded. Loading twice can cause troubles, please fix your setup.');
    return this.Opal;
  }

  var nil;

  // The actual class for BasicObject
  var BasicObject;

  // The actual Object class.
  // The leading underscore is to avoid confusion with window.Object()
  var _Object;

  // The actual Module class
  var Module;

  // The actual Class class
  var Class;

  // Constructor for instances of BasicObject
  function BasicObject_alloc(){}

  // Constructor for instances of Object
  function Object_alloc(){}

  // Constructor for instances of Class
  function Class_alloc(){}

  // Constructor for instances of Module
  function Module_alloc(){}

  // Constructor for instances of NilClass (nil)
  function NilClass_alloc(){}

  // The Opal object that is exposed globally
  var Opal = this.Opal = {};

  // All bridged classes - keep track to donate methods from Object
  var bridges = {};

  // TopScope is used for inheriting constants from the top scope
  var TopScope = function(){};

  // Opal just acts as the top scope
  TopScope.prototype = Opal;

  // To inherit scopes
  Opal.constructor = TopScope;

  // List top scope constants
  Opal.constants = [];

  // This is a useful reference to global object inside ruby files
  Opal.global = this;

  // Minify common function calls
  var $hasOwn = Opal.hasOwnProperty;
  var $slice  = Opal.slice = Array.prototype.slice;

  // Nil object id is always 4
  var nil_id = 4;

  // Generates even sequential numbers greater than 4
  // (nil_id) to serve as unique ids for ruby objects
  var unique_id = nil_id;

  // Return next unique id
  Opal.uid = function() {
    unique_id += 2;
    return unique_id;
  };

  // Table holds all class variables
  Opal.cvars = {};

  // Globals table
  Opal.gvars = {};

  // Exit function, this should be replaced by platform specific implementation
  // (See nodejs and phantom for examples)
  Opal.exit = function(status) { if (Opal.gvars.DEBUG) console.log('Exited with status '+status); };

  // keeps track of exceptions for $!
  Opal.exceptions = [];

  // Get a constant on the given scope. Every class and module in Opal has a
  // scope used to store, and inherit, constants. For example, the top level
  // `Object` in ruby has a scope accessible as `Opal.Object.$$scope`.
  //
  // To get the `Array` class using this scope, you could use:
  //
  //     Opal.Object.$$scope.get("Array")
  //
  // If a constant with the given name cannot be found, then a dispatch to the
  // class/module's `#const_method` is called, which by default will raise an
  // error.
  //
  // @param [String] name the name of the constant to lookup
  // @return [RubyObject]
  //
  Opal.get = function(name) {
    var constant = this[name];

    if (constant == null) {
      return this.base.$const_get(name);
    }

    return constant;
  };

  // Create a new constants scope for the given class with the given
  // base. Constants are looked up through their parents, so the base
  // scope will be the outer scope of the new klass.
  //
  // @param base_scope [$$scope] the scope in which the new scope should be created
  // @param klass      [Class]
  // @param id         [String, null] the name of the newly created scope
  //
  Opal.create_scope = function(base_scope, klass, id) {
    var const_alloc = function() {};
    var const_scope = const_alloc.prototype = new base_scope.constructor();

    klass.$$scope       = const_scope;
    klass.$$base_module = base_scope.base;

    const_scope.base        = klass;
    const_scope.constructor = const_alloc;
    const_scope.constants   = [];

    if (id) {
      Opal.cdecl(base_scope, id, klass);
      const_alloc.displayName = id+"_scope_alloc";
    }
  }

  // A `class Foo; end` expression in ruby is compiled to call this runtime
  // method which either returns an existing class of the given name, or creates
  // a new class in the given `base` scope.
  //
  // If a constant with the given name exists, then we check to make sure that
  // it is a class and also that the superclasses match. If either of these
  // fail, then we raise a `TypeError`. Note, superklass may be null if one was
  // not specified in the ruby code.
  //
  // We pass a constructor to this method of the form `function ClassName() {}`
  // simply so that classes show up with nicely formatted names inside debuggers
  // in the web browser (or node/sprockets).
  //
  // The `base` is the current `self` value where the class is being created
  // from. We use this to get the scope for where the class should be created.
  // If `base` is an object (not a class/module), we simple get its class and
  // use that as the base instead.
  //
  // @param base        [Object] where the class is being created
  // @param superklass  [Class,null] superclass of the new class (may be null)
  // @param id          [String] the name of the class to be created
  // @param constructor [Function] function to use as constructor
  //
  // @return new [Class]  or existing ruby class
  //
  Opal.klass = function(base, superklass, id, constructor) {
    var klass, bridged, alloc;

    // If base is an object, use its class
    if (!base.$$is_class && !base.$$is_module) {
      base = base.$$class;
    }

    // If the superclass is a function then we're bridging a native JS class
    if (typeof(superklass) === 'function') {
      bridged = superklass;
      superklass = _Object;
    }

    // Try to find the class in the current scope
    klass = base.$$scope[id];

    // If the class exists in the scope, then we must use that
    if (klass && klass.$$orig_scope === base.$$scope) {
      // Make sure the existing constant is a class, or raise error
      if (!klass.$$is_class) {
        throw Opal.TypeError.$new(id + " is not a class");
      }

      // Make sure existing class has same superclass
      if (superklass && klass.$$super !== superklass) {
        throw Opal.TypeError.$new("superclass mismatch for class " + id);
      }

      return klass;
    }

    // Class doesnt exist, create a new one with given superclass...

    // Not specifying a superclass means we can assume it to be Object
    if (superklass == null) {
      superklass = _Object;
    }

    // If bridged the JS class will also be the alloc function
    alloc = bridged || boot_class_alloc(id, constructor, superklass);

    // Create the class object (instance of Class)
    klass = boot_class_object(id, superklass, alloc);

    // Name the class
    klass.$$name = id;
    klass.displayName = id;

    // Mark the object as a class
    klass.$$is_class = true;

    // Every class gets its own constant scope, inherited from current scope
    Opal.create_scope(base.$$scope, klass, id);

    // Name new class directly onto current scope (Opal.Foo.Baz = klass)
    base[id] = base.$$scope[id] = klass;

    if (bridged) {
      Opal.bridge(klass, alloc);
    }
    else {
      // Copy all parent constants to child, unless parent is Object
      if (superklass !== _Object && superklass !== BasicObject) {
        donate_constants(superklass, klass);
      }

      // Call .inherited() hook with new class on the superclass
      if (superklass.$inherited) {
        superklass.$inherited(klass);
      }
    }

    return klass;
  };

  // Create generic class with given superclass.
  Opal.boot_class = function(superklass, constructor) {
    var alloc = boot_class_alloc(null, constructor, superklass)

    return boot_class_object(null, superklass, alloc);
  }

  // The class object itself (as in `Class.new`)
  //
  // @param superklass [(Opal) Class] Another class object (as in `Class.new`)
  // @param alloc      [constructor]  The constructor that holds the prototype
  //                                  that will be used for instances of the
  //                                  newly constructed class.
  function boot_class_object(id, superklass, alloc) {
    // Grab the superclass prototype and use it to build an intermediary object
    // in the prototype chain.
    function Superclass_alloc_proxy() {};
    Superclass_alloc_proxy.prototype = superklass.constructor.prototype;
    function SingletonClass_alloc() {}
    SingletonClass_alloc.prototype = new Superclass_alloc_proxy();

    if (id) {
      SingletonClass_alloc.displayName = "SingletonClass_alloc("+id+")";
    }

    // The built class is the only instance of its singleton_class
    var klass = new SingletonClass_alloc();

    setup_module_or_class_object(klass, SingletonClass_alloc, superklass, alloc.prototype);

    // @property $$alloc This is the constructor of instances of the current
    //                   class. Its prototype will be used for method lookup
    klass.$$alloc = alloc;

    // @property $$proto.$$class Make available to instances a reference to the
    //                           class they belong to.
    klass.$$proto.$$class = klass;

    return klass;
  }

  // Adds common/required properties to a module or class object
  // (as in `Module.new` / `Class.new`)
  //
  // @param module      The module or class that needs to be prepared
  //
  // @param constructor The constructor of the module or class itself,
  //                    usually it's already assigned by using `new`. Some
  //                    ipothesis on why it's needed can be found below.
  //
  // @param superklass  The superclass of the class/module object, for modules
  //                    is `Module` (of `Module` in JS context)
  //
  // @param prototype   The prototype on which the class/module methods will
  //                    be stored.
  //
  function setup_module_or_class_object(module, constructor, superklass, prototype) {
    // @property $$id Each class is assigned a unique `id` that helps
    //                comparation and implementation of `#object_id`
    module.$$id = Opal.uid();

    // @property $$proto This is the prototype on which methods will be defined
    module.$$proto = prototype;

    // @property constructor keeps a ref to the constructor, but apparently the
    //                       constructor is already set on:
    //
    //                          `var module = new constructor` is called.
    //
    //                       Maybe there are some browsers not abiding (IE6?)
    module.constructor = constructor;

    if (superklass === Module) {
      // @property $$is_module Clearly mark this as a module
      module.$$is_module = true;
      module.$$class     = Module;
    }
    else {
      // @property $$is_class Clearly mark this as a class
      module.$$is_class = true;
      module.$$class    = Class;
    }

    // @property $$super the superclass, doesn't get changed by module inclusions
    module.$$super = superklass;

    // @property $$parent direct parent class or module
    //                    starts with the superclass, after module inclusion is
    //                    the last included module
    module.$$parent = superklass;

    // @property $$inc included modules
    module.$$inc = [];
  }

  // Define new module (or return existing module). The given `base` is basically
  // the current `self` value the `module` statement was defined in. If this is
  // a ruby module or class, then it is used, otherwise if the base is a ruby
  // object then that objects real ruby class is used (e.g. if the base is the
  // main object, then the top level `Object` class is used as the base).
  //
  // If a module of the given name is already defined in the base, then that
  // instance is just returned.
  //
  // If there is a class of the given name in the base, then an error is
  // generated instead (cannot have a class and module of same name in same base).
  //
  // Otherwise, a new module is created in the base with the given name, and that
  // new instance is returned back (to be referenced at runtime).
  //
  // @param  base [Module, Class] class or module this definition is inside
  // @param  id [String] the name of the new (or existing) module
  // @return [Module]
  //
  Opal.module = function(base, id) {
    var module;

    if (!base.$$is_class && !base.$$is_module) {
      base = base.$$class;
    }

    if ($hasOwn.call(base.$$scope, id)) {
      module = base.$$scope[id];

      if (!module.$$is_module && module !== _Object) {
        throw Opal.TypeError.$new(id + " is not a module");
      }
    }
    else {
      module = boot_module_object();

      // name module using base (e.g. Foo or Foo::Baz)
      module.$$name = id;

      // mark the object as a module
      module.$$is_module = true;

      // initialize dependency tracking
      module.$$dep = [];

      Opal.create_scope(base.$$scope, module, id);

      // Name new module directly onto current scope (Opal.Foo.Baz = module)
      base[id] = base.$$scope[id] = module;
    }

    return module;
  };

  // Internal function to create a new module instance. This simply sets up
  // the prototype hierarchy and method tables.
  //
  function boot_module_object() {
    var mtor = function() {};
    mtor.prototype = Module_alloc.prototype;

    function module_constructor() {}
    module_constructor.prototype = new mtor();

    var module = new module_constructor();
    var module_prototype = {};

    setup_module_or_class_object(module, module_constructor, Module, module_prototype);

    return module;
  }

  // Make `boot_module_object` available to the JS-API
  Opal.boot_module_object = boot_module_object;

  // Return the singleton class for the passed object.
  //
  // If the given object alredy has a singleton class, then it will be stored on
  // the object as the `$$meta` property. If this exists, then it is simply
  // returned back.
  //
  // Otherwise, a new singleton object for the class or object is created, set on
  // the object at `$$meta` for future use, and then returned.
  //
  // @param [RubyObject] object the ruby object
  // @return [RubyClass] the singleton class for object
  //
  Opal.get_singleton_class = function(object) {
    if (object.$$meta) {
      return object.$$meta;
    }

    if (object.$$is_class || object.$$is_module) {
      return build_class_singleton_class(object);
    }

    return build_object_singleton_class(object);
  };

  // Build the singleton class for an existing class.
  //
  // NOTE: Actually in MRI a class' singleton class inherits from its
  // superclass' singleton class which in turn inherits from Class.
  //
  // @param [RubyClass] klass
  // @return [RubyClass]
  //
  function build_class_singleton_class(klass) {
    var meta = new Opal.Class.$$alloc();

    meta.$$class = Opal.Class;
    meta.$$proto = klass.constructor.prototype;

    meta.$$is_singleton = true;
    meta.$$singleton_of = klass;
    meta.$$inc          = [];
    meta.$$scope        = klass.$$scope;

    return klass.$$meta = meta;
  }

  // Build the singleton class for a Ruby (non class) Object.
  //
  // @param [RubyObject] object
  // @return [RubyClass]
  //
  function build_object_singleton_class(object) {
    var orig_class = object.$$class,
        class_id   = "#<Class:#<" + orig_class.$$name + ":" + orig_class.$$id + ">>";

    var Singleton = function() {};
    var meta = Opal.boot_class(orig_class, Singleton);
    meta.$$name   = class_id;

    meta.$$proto  = object;
    meta.$$class  = orig_class.$$class;
    meta.$$scope  = orig_class.$$scope;
    meta.$$parent = orig_class;
    meta.$$is_singleton = true;
    meta.$$singleton_of = object;

    return object.$$meta = meta;
  }

  // Bridges a single method.
  function bridge_method(target, from, name, body) {
    var ancestors, i, ancestor, length;

    ancestors = target.$$bridge.$ancestors();

    // order important here, we have to check for method presence in
    // ancestors from the bridged class to the last ancestor
    for (i = 0, length = ancestors.length; i < length; i++) {
      ancestor = ancestors[i];

      if ($hasOwn.call(ancestor.$$proto, name) &&
          ancestor.$$proto[name] &&
          !ancestor.$$proto[name].$$donated &&
          !ancestor.$$proto[name].$$stub &&
          ancestor !== from) {
        break;
      }

      if (ancestor === from) {
        target.prototype[name] = body
        break;
      }
    }

  }

  // Bridges from *donator* to a *target*.
  function _bridge(target, donator) {
    var id, methods, method, i, bridged;

    if (typeof(target) === "function") {
      id      = donator.$__id__();
      methods = donator.$instance_methods();

      for (i = methods.length - 1; i >= 0; i--) {
        method = '$' + methods[i];

        bridge_method(target, donator, method, donator.$$proto[method]);
      }

      if (!bridges[id]) {
        bridges[id] = [];
      }

      bridges[id].push(target);
    }
    else {
      bridged = bridges[target.$__id__()];

      if (bridged) {
        for (i = bridged.length - 1; i >= 0; i--) {
          _bridge(bridged[i], donator);
        }

        bridges[donator.$__id__()] = bridged.slice();
      }
    }
  }

  // The actual inclusion of a module into a class.
  //
  // ## Class `$$parent` and `iclass`
  //
  // To handle `super` calls, every class has a `$$parent`. This parent is
  // used to resolve the next class for a super call. A normal class would
  // have this point to its superclass. However, if a class includes a module
  // then this would need to take into account the module. The module would
  // also have to then point its `$$parent` to the actual superclass. We
  // cannot modify modules like this, because it might be included in more
  // then one class. To fix this, we actually insert an `iclass` as the class'
  // `$$parent` which can then point to the superclass. The `iclass` acts as
  // a proxy to the actual module, so the `super` chain can then search it for
  // the required method.
  //
  // @param [RubyModule] module the module to include
  // @param [RubyClass] klass the target class to include module into
  // @return [null]
  //
  Opal.append_features = function(module, klass) {
    var iclass, donator, prototype, methods, id, i;

    // check if this module is already included in the class
    for (i = klass.$$inc.length - 1; i >= 0; i--) {
      if (klass.$$inc[i] === module) {
        return;
      }
    }

    klass.$$inc.push(module);
    module.$$dep.push(klass);
    _bridge(klass, module);

    // iclass
    iclass = {
      $$name:   module.$$name,
      $$proto:  module.$$proto,
      $$parent: klass.$$parent,
      $$module: module,
      $$iclass: true
    };

    klass.$$parent = iclass;

    donator   = module.$$proto;
    prototype = klass.$$proto;
    methods   = module.$instance_methods();

    for (i = methods.length - 1; i >= 0; i--) {
      id = '$' + methods[i];

      // if the target class already has a method of the same name defined
      // and that method was NOT donated, then it must be a method defined
      // by the class so we do not want to override it
      if ( prototype.hasOwnProperty(id) &&
          !prototype[id].$$donated &&
          !prototype[id].$$stub) {
        continue;
      }

      prototype[id] = donator[id];
      prototype[id].$$donated = module;
    }

    donate_constants(module, klass);
  };

  // Boot a base class (makes instances).
  function boot_class_alloc(id, constructor, superklass) {
    if (superklass) {
      var alloc_proxy = function() {};
      alloc_proxy.prototype  = superklass.$$proto || superklass.prototype;
      constructor.prototype = new alloc_proxy();
    }

    if (id) {
      constructor.displayName = id+'_alloc';
    }

    constructor.prototype.constructor = constructor;

    return constructor;
  }

  // Builds the class object for core classes:
  // - make the class object have a singleton class
  // - make the singleton class inherit from its parent singleton class
  //
  // @param id         [String]      the name of the class
  // @param alloc      [Function]    the constructor for the core class instances
  // @param superclass [Class alloc] the constructor of the superclass
  //
  function boot_core_class_object(id, alloc, superclass) {
    var superclass_constructor = function() {};
        superclass_constructor.prototype = superclass.prototype;

    var singleton_class = function() {};
        singleton_class.prototype = new superclass_constructor();

    singleton_class.displayName = "#<Class:"+id+">";

    // the singleton_class acts as the class object constructor
    var klass = new singleton_class();

    setup_module_or_class_object(klass, singleton_class, superclass, alloc.prototype);

    klass.$$alloc     = alloc;
    klass.$$name      = id;
    klass.displayName = id;

    // Give all instances a ref to their class
    alloc.prototype.$$class = klass;

    Opal[id] = klass;
    Opal.constants.push(id);

    return klass;
  }

  // For performance, some core Ruby classes are toll-free bridged to their
  // native JavaScript counterparts (e.g. a Ruby Array is a JavaScript Array).
  //
  // This method is used to setup a native constructor (e.g. Array), to have
  // its prototype act like a normal Ruby class. Firstly, a new Ruby class is
  // created using the native constructor so that its prototype is set as the
  // target for th new class. Note: all bridged classes are set to inherit
  // from Object.
  //
  // Example:
  //
  //    Opal.bridge(self, Function);
  //
  // @param [Class] klass the Ruby class to bridge
  // @param [Function] constructor native JavaScript constructor to use
  // @return [Class] returns the passed Ruby class
  //
  Opal.bridge = function(klass, constructor) {
    if (constructor.$$bridge) {
      throw Opal.ArgumentError.$new("already bridged");
    }

    Opal.stub_subscribers.push(constructor.prototype);

    constructor.prototype.$$class = klass;
    constructor.$$bridge          = klass;

    var ancestors = klass.$ancestors();

    // order important here, we have to bridge from the last ancestor to the
    // bridged class
    for (var i = ancestors.length - 1; i >= 0; i--) {
      _bridge(constructor, ancestors[i]);
    }

    for (var name in BasicObject_alloc.prototype) {
      var method = BasicObject_alloc.prototype[method];

      if (method && method.$$stub && !(name in constructor.prototype)) {
        constructor.prototype[name] = method;
      }
    }

    return klass;
  }


  // Constant assignment, see also `Opal.cdecl`
  //
  // @param base_module [Module, Class] the constant namespace
  // @param name        [String] the name of the constant
  // @param value       [Object] the value of the constant
  //
  // @example Assigning a namespaced constant
  //   self::FOO = 'bar'
  //
  // @example Assigning with Module#const_set
  //   Foo.const_set :BAR, 123
  //
  Opal.casgn = function(base_module, name, value) {
    function update(klass, name) {
      klass.$$name = name;

      for (name in klass.$$scope) {
        var value = klass.$$scope[name];

        if (value.$$name === nil && (value.$$is_class || value.$$is_module)) {
          update(value, name)
        }
      }
    }

    var scope = base_module.$$scope;

    if (value.$$is_class || value.$$is_module) {
      // Only checking _Object prevents setting a const on an anonymous class
      // that has a superclass that's not Object
      if (value.$$is_class || value.$$base_module === _Object) {
        value.$$base_module = base_module;
      }

      if (value.$$name === nil && value.$$base_module.$$name !== nil) {
        update(value, name);
      }
    }

    scope.constants.push(name);
    return scope[name] = value;
  };

  // constant decl
  Opal.cdecl = function(base_scope, name, value) {
    if ((value.$$is_class || value.$$is_module) && value.$$orig_scope == null) {
      value.$$name = name;
      value.$$orig_scope = base_scope;
      base_scope.constructor[name] = value;
    }

    base_scope.constants.push(name);
    return base_scope[name] = value;
  };

  // When a source module is included into the target module, we must also copy
  // its constants to the target.
  //
  function donate_constants(source_mod, target_mod) {
    var source_constants = source_mod.$$scope.constants,
        target_scope     = target_mod.$$scope,
        target_constants = target_scope.constants;

    for (var i = 0, length = source_constants.length; i < length; i++) {
      target_constants.push(source_constants[i]);
      target_scope[source_constants[i]] = source_mod.$$scope[source_constants[i]];
    }
  };

  // Donate methods for a module.
  function donate(module, jsid) {
    var included_in = module.$$dep,
        body = module.$$proto[jsid],
        i, length, includee, dest, current,
        klass_includees, j, jj, current_owner_index, module_index;

    if (!included_in) {
      return;
    }

    for (i = 0, length = included_in.length; i < length; i++) {
      includee = included_in[i];
      dest = includee.$$proto;
      current = dest[jsid];

      if (dest.hasOwnProperty(jsid) && !current.$$donated && !current.$$stub) {
        // target class has already defined the same method name - do nothing
      }
      else if (dest.hasOwnProperty(jsid) && !current.$$stub) {
        // target class includes another module that has defined this method
        klass_includees = includee.$$inc;

        for (j = 0, jj = klass_includees.length; j < jj; j++) {
          if (klass_includees[j] === current.$$donated) {
            current_owner_index = j;
          }
          if (klass_includees[j] === module) {
            module_index = j;
          }
        }

        // only redefine method on class if the module was included AFTER
        // the module which defined the current method body. Also make sure
        // a module can overwrite a method it defined before
        if (current_owner_index <= module_index) {
          dest[jsid] = body;
          dest[jsid].$$donated = module;
        }
      }
      else {
        // neither a class, or module included by class, has defined method
        dest[jsid] = body;
        dest[jsid].$$donated = module;
      }

      if (includee.$$dep) {
        donate(includee, jsid);
      }
    }
  };

  // Methods stubs are used to facilitate method_missing in opal. A stub is a
  // placeholder function which just calls `method_missing` on the receiver.
  // If no method with the given name is actually defined on an object, then it
  // is obvious to say that the stub will be called instead, and then in turn
  // method_missing will be called.
  //
  // When a file in ruby gets compiled to javascript, it includes a call to
  // this function which adds stubs for every method name in the compiled file.
  // It should then be safe to assume that method_missing will work for any
  // method call detected.
  //
  // Method stubs are added to the BasicObject prototype, which every other
  // ruby object inherits, so all objects should handle method missing. A stub
  // is only added if the given property name (method name) is not already
  // defined.
  //
  // Note: all ruby methods have a `$` prefix in javascript, so all stubs will
  // have this prefix as well (to make this method more performant).
  //
  //    Opal.add_stubs(["$foo", "$bar", "$baz="]);
  //
  // All stub functions will have a private `$$stub` property set to true so
  // that other internal methods can detect if a method is just a stub or not.
  // `Kernel#respond_to?` uses this property to detect a methods presence.
  //
  // @param [Array] stubs an array of method stubs to add
  //
  Opal.add_stubs = function(stubs) {
    var subscriber, subscribers = Opal.stub_subscribers,
        i, ilength = stubs.length,
        j, jlength = subscribers.length,
        method_name, stub;

    for (i = 0; i < ilength; i++) {
      method_name = stubs[i];
      stub = stub_for(method_name);

      for (j = 0; j < jlength; j++) {
        subscriber = subscribers[j];

        if (!(method_name in subscriber)) {
          subscriber[method_name] = stub;
        }
      }
    }
  };

  // Keep a list of prototypes that want method_missing stubs to be added.
  //
  // @default [Prototype List] BasicObject_alloc.prototype
  //
  Opal.stub_subscribers = [BasicObject_alloc.prototype];

  // Add a method_missing stub function to the given prototype for the
  // given name.
  //
  // @param [Prototype] prototype the target prototype
  // @param [String] stub stub name to add (e.g. "$foo")
  //
  Opal.add_stub_for = function(prototype, stub) {
    var method_missing_stub = stub_for(stub);
    prototype[stub] = method_missing_stub;
  }

  // Generate the method_missing stub for a given method name.
  //
  // @param [String] method_name The js-name of the method to stub (e.g. "$foo")
  //
  function stub_for(method_name) {
    function method_missing_stub() {
      // Copy any given block onto the method_missing dispatcher
      this.$method_missing.$$p = method_missing_stub.$$p;

      // Set block property to null ready for the next call (stop false-positives)
      method_missing_stub.$$p = null;

      // call method missing with correct args (remove '$' prefix on method name)
      return this.$method_missing.apply(this, [method_name.slice(1)].concat($slice.call(arguments)));
    }

    method_missing_stub.$$stub = true;

    return method_missing_stub;
  }

  // Arity count error dispatcher
  Opal.ac = function(actual, expected, object, meth) {
    var inspect = '';
    if (object.$$is_class || object.$$is_module) {
      inspect += object.$$name + '.';
    }
    else {
      inspect += object.$$class.$$name + '#';
    }
    inspect += meth;

    throw Opal.ArgumentError.$new('[' + inspect + '] wrong number of arguments(' + actual + ' for ' + expected + ')');
  };

  // The Array of ancestors for a given module/class
  Opal.ancestors = function(module_or_class) {
    var parent = module_or_class,
        result = [];

    while (parent) {
      result.push(parent);
      for (var i=0; i < parent.$$inc.length; i++) {
        result = result.concat(Opal.ancestors(parent.$$inc[i]));
      }

      parent = parent.$$is_class ? parent.$$super : null;
    }

    return result;
  }

  // Super dispatcher
  Opal.find_super_dispatcher = function(obj, jsid, current_func, iter, defs) {
    var dispatcher;

    if (defs) {
      if (obj.$$is_class || obj.$$is_module) {
        dispatcher = defs.$$super;
      }
      else {
        dispatcher = obj.$$class.$$proto;
      }
    }
    else {
      if (obj.$$is_class || obj.$$is_module) {
        dispatcher = obj.$$super;
      }
      else {
        dispatcher = find_obj_super_dispatcher(obj, jsid, current_func);
      }
    }

    dispatcher = dispatcher['$' + jsid];
    dispatcher.$$p = iter;

    return dispatcher;
  };

  // Iter dispatcher for super in a block
  Opal.find_iter_super_dispatcher = function(obj, jsid, current_func, iter, defs) {
    if (current_func.$$def) {
      return Opal.find_super_dispatcher(obj, current_func.$$jsid, current_func, iter, defs);
    }
    else {
      return Opal.find_super_dispatcher(obj, jsid, current_func, iter, defs);
    }
  };

  function find_obj_super_dispatcher(obj, jsid, current_func) {
    var klass = obj.$$meta || obj.$$class;
    jsid = '$' + jsid;

    while (klass) {
      if (klass.$$proto[jsid] === current_func) {
        // ok
        break;
      }

      klass = klass.$$parent;
    }

    // if we arent in a class, we couldnt find current?
    if (!klass) {
      throw new Error("could not find current class for super()");
    }

    klass = klass.$$parent;

    // else, let's find the next one
    while (klass) {
      var working = klass.$$proto[jsid];

      if (working && working !== current_func) {
        // ok
        break;
      }

      klass = klass.$$parent;
    }

    return klass.$$proto;
  };

  // Used to return as an expression. Sometimes, we can't simply return from
  // a javascript function as if we were a method, as the return is used as
  // an expression, or even inside a block which must "return" to the outer
  // method. This helper simply throws an error which is then caught by the
  // method. This approach is expensive, so it is only used when absolutely
  // needed.
  //
  Opal.ret = function(val) {
    Opal.returner.$v = val;
    throw Opal.returner;
  };

  // handles yield calls for 1 yielded arg
  Opal.yield1 = function(block, arg) {
    if (typeof(block) !== "function") {
      throw Opal.LocalJumpError.$new("no block given");
    }

    if (block.length > 1 && arg.$$is_array) {
      return block.apply(null, arg);
    }
    else {
      return block(arg);
    }
  };

  // handles yield for > 1 yielded arg
  Opal.yieldX = function(block, args) {
    if (typeof(block) !== "function") {
      throw Opal.LocalJumpError.$new("no block given");
    }

    if (block.length > 1 && args.length === 1) {
      if (args[0].$$is_array) {
        return block.apply(null, args[0]);
      }
    }

    if (!args.$$is_array) {
      args = $slice.call(args);
    }

    return block.apply(null, args);
  };

  // Finds the corresponding exception match in candidates.  Each candidate can
  // be a value, or an array of values.  Returns null if not found.
  Opal.rescue = function(exception, candidates) {
    for (var i = 0; i < candidates.length; i++) {
      var candidate = candidates[i];

      if (candidate.$$is_array) {
        var result = Opal.rescue(exception, candidate);

        if (result) {
          return result;
        }
      }
      else if (candidate['$==='](exception)) {
        return candidate;
      }
    }

    return null;
  };

  Opal.is_a = function(object, klass) {
    if (object.$$meta === klass) {
      return true;
    }

    var search = object.$$class;

    while (search) {
      if (search === klass) {
        return true;
      }

      for (var i = 0, length = search.$$inc.length; i < length; i++) {
        if (search.$$inc[i] === klass) {
          return true;
        }
      }

      search = search.$$super;
    }

    return false;
  };

  // Helpers for implementing multiple assignment
  // Our code for extracting the values and assigning them only works if the
  // return value is a JS array
  // So if we get an Array subclass, extract the wrapped JS array from it

  Opal.to_ary = function(value) {
    // Used for: a, b = something (no splat)
    if (value.$$is_array) {
      return (value.constructor === Array) ? value : value.literal;
    }
    else if (value['$respond_to?']('to_ary', true)) {
      var ary = value.$to_ary();
      if (ary === nil) {
        return [value];
      }
      else if (ary.$$is_array) {
        return (ary.constructor === Array) ? ary : ary.literal;
      }
      else {
        throw Opal.TypeError.$new("Can't convert " + value.$$class +
          " to Array (" + value.$$class + "#to_ary gives " + ary.$$class + ")");
      }
    }
    else {
      return [value];
    }
  };

  Opal.to_a = function(value) {
    // Used for: a, b = *something (with splat)
    if (value.$$is_array) {
      // A splatted array must be copied
      return (value.constructor === Array) ? value.slice() : value.literal.slice();
    }
    else if (value['$respond_to?']('to_a', true)) {
      var ary = value.$to_a();
      if (ary === nil) {
        return [value];
      }
      else if (ary.$$is_array) {
        return (ary.constructor === Array) ? ary : ary.literal;
      }
      else {
        throw Opal.TypeError.$new("Can't convert " + value.$$class +
          " to Array (" + value.$$class + "#to_a gives " + ary.$$class + ")");
      }
    }
    else {
      return [value];
    }
  };

  // Used to get a list of rest keyword arguments. Method takes the given
  // keyword args, i.e. the hash literal passed to the method containing all
  // keyword arguemnts passed to method, as well as the used args which are
  // the names of required and optional arguments defined. This method then
  // just returns all key/value pairs which have not been used, in a new
  // hash literal.
  //
  // @param given_args [Hash] all kwargs given to method
  // @param used_args [Object<String: true>] all keys used as named kwargs
  // @return [Hash]
  //
  Opal.kwrestargs = function(given_args, used_args) {
    var keys      = [],
        map       = {},
        key       = null,
        given_map = given_args.$$smap;

    for (key in given_map) {
      if (!used_args[key]) {
        keys.push(key);
        map[key] = given_map[key];
      }
    }

    return Opal.hash2(keys, map);
  };

  // Call a ruby method on a ruby object with some arguments:
  //
  //   var my_array = [1, 2, 3, 4]
  //   Opal.send(my_array, 'length')     # => 4
  //   Opal.send(my_array, 'reverse!')   # => [4, 3, 2, 1]
  //
  // A missing method will be forwarded to the object via
  // method_missing.
  //
  // The result of either call with be returned.
  //
  // @param [Object] recv the ruby object
  // @param [String] mid ruby method to call
  //
  Opal.send = function(recv, mid) {
    var args = $slice.call(arguments, 2),
        func = recv['$' + mid];

    if (func) {
      return func.apply(recv, args);
    }

    return recv.$method_missing.apply(recv, [mid].concat(args));
  };

  Opal.block_send = function(recv, mid, block) {
    var args = $slice.call(arguments, 3),
        func = recv['$' + mid];

    if (func) {
      func.$$p = block;
      return func.apply(recv, args);
    }

    return recv.$method_missing.apply(recv, [mid].concat(args));
  };

  // Used to define methods on an object. This is a helper method, used by the
  // compiled source to define methods on special case objects when the compiler
  // can not determine the destination object, or the object is a Module
  // instance. This can get called by `Module#define_method` as well.
  //
  // ## Modules
  //
  // Any method defined on a module will come through this runtime helper.
  // The method is added to the module body, and the owner of the method is
  // set to be the module itself. This is used later when choosing which
  // method should show on a class if more than 1 included modules define
  // the same method. Finally, if the module is in `module_function` mode,
  // then the method is also defined onto the module itself.
  //
  // ## Classes
  //
  // This helper will only be called for classes when a method is being
  // defined indirectly; either through `Module#define_method`, or by a
  // literal `def` method inside an `instance_eval` or `class_eval` body. In
  // either case, the method is simply added to the class' prototype. A special
  // exception exists for `BasicObject` and `Object`. These two classes are
  // special because they are used in toll-free bridged classes. In each of
  // these two cases, extra work is required to define the methods on toll-free
  // bridged class' prototypes as well.
  //
  // ## Objects
  //
  // If a simple ruby object is the object, then the method is simply just
  // defined on the object as a singleton method. This would be the case when
  // a method is defined inside an `instance_eval` block.
  //
  // @param [RubyObject or Class] obj the actual obj to define method for
  // @param [String] jsid the javascript friendly method name (e.g. '$foo')
  // @param [Function] body the literal javascript function used as method
  // @return [null]
  //
  Opal.defn = function(obj, jsid, body) {
    obj.$$proto[jsid] = body;

    if (obj.$$is_module) {
      donate(obj, jsid);

      if (obj.$$module_function) {
        Opal.defs(obj, jsid, body);
      }
    }

    if (obj.$__id__ && !obj.$__id__.$$stub) {
      var bridged = bridges[obj.$__id__()];

      if (bridged) {
        for (var i = bridged.length - 1; i >= 0; i--) {
          bridge_method(bridged[i], obj, jsid, body);
        }
      }
    }

    if (obj.$method_added && !obj.$method_added.$$stub) {
      obj.$method_added(jsid.substr(1));
    }

    var singleton_of = obj.$$singleton_of;
    if (singleton_of && singleton_of.$singleton_method_added && !singleton_of.$singleton_method_added.$$stub) {
      singleton_of.$singleton_method_added(jsid.substr(1));
    }

    return nil;
  };


  // Define a singleton method on the given object.
  Opal.defs = function(obj, jsid, body) {
    Opal.defn(Opal.get_singleton_class(obj), jsid, body)
  };

  Opal.def = function(obj, jsid, body) {
    // if instance_eval is invoked on a module/class, it sets inst_eval_mod
    if (!obj.$$eval && (obj.$$is_class || obj.$$is_module)) {
      Opal.defn(obj, jsid, body);
    }
    else {
      Opal.defs(obj, jsid, body);
    }
  };

  // Called from #remove_method.
  Opal.rdef = function(obj, jsid) {
    // TODO: remove from bridges as well

    if (!$hasOwn.call(obj.$$proto, jsid)) {
      throw Opal.NameError.$new("method '" + jsid.substr(1) + "' not defined in " + obj.$name());
    }

    delete obj.$$proto[jsid];

    if (obj.$$is_singleton) {
      if (obj.$$proto.$singleton_method_removed && !obj.$$proto.$singleton_method_removed.$$stub) {
        obj.$$proto.$singleton_method_removed(jsid.substr(1));
      }
    }
    else {
      if (obj.$method_removed && !obj.$method_removed.$$stub) {
        obj.$method_removed(jsid.substr(1));
      }
    }
  };

  // Called from #undef_method.
  Opal.udef = function(obj, jsid) {
    if (!obj.$$proto[jsid] || obj.$$proto[jsid].$$stub) {
      throw Opal.NameError.$new("method '" + jsid.substr(1) + "' not defined in " + obj.$name());
    }

    Opal.add_stub_for(obj.$$proto, jsid);

    if (obj.$$is_singleton) {
      if (obj.$$proto.$singleton_method_undefined && !obj.$$proto.$singleton_method_undefined.$$stub) {
        obj.$$proto.$singleton_method_undefined(jsid.substr(1));
      }
    }
    else {
      if (obj.$method_undefined && !obj.$method_undefined.$$stub) {
        obj.$method_undefined(jsid.substr(1));
      }
    }
  };

  Opal.alias = function(obj, name, old) {
    var id     = '$' + name,
        old_id = '$' + old,
        body   = obj.$$proto['$' + old];

    // instance_eval is being run on a class/module, so that need to alias class methods
    if (obj.$$eval) {
      return Opal.alias(Opal.get_singleton_class(obj), name, old);
    }

    if (typeof(body) !== "function" || body.$$stub) {
      var ancestor = obj.$$super;

      while (typeof(body) !== "function" && ancestor) {
        body     = ancestor[old_id];
        ancestor = ancestor.$$super;
      }

      if (typeof(body) !== "function" || body.$$stub) {
        throw Opal.NameError.$new("undefined method `" + old + "' for class `" + obj.$name() + "'")
      }
    }

    Opal.defn(obj, id, body);

    return obj;
  };

  Opal.alias_native = function(obj, name, native_name) {
    var id   = '$' + name,
        body = obj.$$proto[native_name];

    if (typeof(body) !== "function" || body.$$stub) {
      throw Opal.NameError.$new("undefined native method `" + native_name + "' for class `" + obj.$name() + "'")
    }

    Opal.defn(obj, id, body);

    return obj;
  };

  Opal.hash_init = function(hash) {
    hash.$$map  = {};
    hash.$$smap = {};
    hash.$$keys = [];
  };

  Opal.hash_clone = function(from_hash, to_hash) {
    to_hash.none = from_hash.none;
    to_hash.proc = from_hash.proc;

    for (var i = 0, keys = from_hash.$$keys, length = keys.length, key, value; i < length; i++) {
      key = from_hash.$$keys[i];

      if (key.$$is_string) {
        value = from_hash.$$smap[key];
      } else {
        value = key.value;
        key = key.key;
      }

      Opal.hash_put(to_hash, key, value);
    }
  };

  Opal.hash_put = function(hash, key, value) {
    if (key.$$is_string) {
      if (!hash.$$smap.hasOwnProperty(key)) {
        hash.$$keys.push(key);
      }
      hash.$$smap[key] = value;
      return;
    }

    var key_hash = key.$hash(), bucket, last_bucket;

    if (!hash.$$map.hasOwnProperty(key_hash)) {
      bucket = {key: key, key_hash: key_hash, value: value};
      hash.$$keys.push(bucket);
      hash.$$map[key_hash] = bucket;
      return;
    }

    bucket = hash.$$map[key_hash];

    while (bucket) {
      if (key === bucket.key || key['$eql?'](bucket.key)) {
        last_bucket = undefined;
        bucket.value = value;
        break;
      }
      last_bucket = bucket;
      bucket = bucket.next;
    }

    if (last_bucket) {
      bucket = {key: key, key_hash: key_hash, value: value};
      hash.$$keys.push(bucket);
      last_bucket.next = bucket;
    }
  };

  Opal.hash_get = function(hash, key) {
    if (key.$$is_string) {
      if (hash.$$smap.hasOwnProperty(key)) {
        return hash.$$smap[key];
      }
      return;
    }

    var key_hash = key.$hash(), bucket;

    if (hash.$$map.hasOwnProperty(key_hash)) {
      bucket = hash.$$map[key_hash];

      while (bucket) {
        if (key === bucket.key || key['$eql?'](bucket.key)) {
          return bucket.value;
        }
        bucket = bucket.next;
      }
    }
  };

  Opal.hash_delete = function(hash, key) {
    var i, keys = hash.$$keys, length = keys.length, value;

    if (key.$$is_string) {
      if (!hash.$$smap.hasOwnProperty(key)) {
        return;
      }

      for (i = 0; i < length; i++) {
        if (keys[i] === key) {
          keys.splice(i, 1);
          break;
        }
      }

      value = hash.$$smap[key];
      delete hash.$$smap[key];
      return value;
    }

    var key_hash = key.$hash();

    if (!hash.$$map.hasOwnProperty(key_hash)) {
      return;
    }

    var bucket = hash.$$map[key_hash], last_bucket;

    while (bucket) {
      if (key === bucket.key || key['$eql?'](bucket.key)) {
        value = bucket.value;

        for (i = 0; i < length; i++) {
          if (keys[i] === bucket) {
            keys.splice(i, 1);
            break;
          }
        }

        if (last_bucket && bucket.next) {
          last_bucket.next = bucket.next;
        }
        else if (last_bucket) {
          delete last_bucket.next;
        }
        else if (bucket.next) {
          hash.$$map[key_hash] = bucket.next;
        }
        else {
          delete hash.$$map[key_hash];
        }

        return value;
      }
      last_bucket = bucket;
      bucket = bucket.next;
    }
  };

  Opal.hash_rehash = function(hash) {
    for (var i = 0, length = hash.$$keys.length, key_hash, bucket, last_bucket; i < length; i++) {

      if (hash.$$keys[i].$$is_string) {
        continue;
      }

      key_hash = hash.$$keys[i].key.$hash();

      if (key_hash === hash.$$keys[i].key_hash) {
        continue;
      }

      bucket = hash.$$map[hash.$$keys[i].key_hash];
      last_bucket = undefined;

      while (bucket) {
        if (bucket === hash.$$keys[i]) {
          if (last_bucket && bucket.next) {
            last_bucket.next = bucket.next;
          }
          else if (last_bucket) {
            delete last_bucket.next;
          }
          else if (bucket.next) {
            hash.$$map[hash.$$keys[i].key_hash] = bucket.next;
          }
          else {
            delete hash.$$map[hash.$$keys[i].key_hash];
          }
          break;
        }
        last_bucket = bucket;
        bucket = bucket.next;
      }

      hash.$$keys[i].key_hash = key_hash;

      if (!hash.$$map.hasOwnProperty(key_hash)) {
        hash.$$map[key_hash] = hash.$$keys[i];
        continue;
      }

      bucket = hash.$$map[key_hash];
      last_bucket = undefined;

      while (bucket) {
        if (bucket === hash.$$keys[i]) {
          last_bucket = undefined;
          break;
        }
        last_bucket = bucket;
        bucket = bucket.next;
      }

      if (last_bucket) {
        last_bucket.next = hash.$$keys[i];
      }
    }
  };

  Opal.hash = function() {
    var arguments_length = arguments.length, args, hash, i, length, key, value;

    if (arguments_length === 1 && arguments[0].$$is_hash) {
      return arguments[0];
    }

    hash = new Opal.Hash.$$alloc();
    Opal.hash_init(hash);

    if (arguments_length === 1 && arguments[0].$$is_array) {
      args = arguments[0];
      length = args.length;

      for (i = 0; i < length; i++) {
        if (args[i].length !== 2) {
          throw Opal.ArgumentError.$new("value not of length 2: " + args[i].$inspect());
        }

        key = args[i][0];
        value = args[i][1];

        Opal.hash_put(hash, key, value);
      }

      return hash;
    }

    if (arguments_length === 1) {
      args = arguments[0];
      for (key in args) {
        if (args.hasOwnProperty(key)) {
          value = args[key];

          Opal.hash_put(hash, key, value);
        }
      }

      return hash;
    }

    if (arguments_length % 2 !== 0) {
      throw Opal.ArgumentError.$new("odd number of arguments for Hash");
    }

    for (i = 0; i < arguments_length; i += 2) {
      key = arguments[i];
      value = arguments[i + 1];

      Opal.hash_put(hash, key, value);
    }

    return hash;
  };

  // hash2 is a faster creator for hashes that just use symbols and
  // strings as keys. The map and keys array can be constructed at
  // compile time, so they are just added here by the constructor
  // function
  //
  Opal.hash2 = function(keys, smap) {
    var hash = new Opal.Hash.$$alloc();

    hash.$$map  = {};
    hash.$$keys = keys;
    hash.$$smap = smap;

    return hash;
  };

  // Create a new range instance with first and last values, and whether the
  // range excludes the last value.
  //
  Opal.range = function(first, last, exc) {
    var range         = new Opal.Range.$$alloc();
        range.begin   = first;
        range.end     = last;
        range.exclude = exc;

    return range;
  };

  Opal.ivar = function(name) {
    if (
        // properties
        name === "constructor" ||
        name === "displayName" ||
        name === "__count__" ||
        name === "__noSuchMethod__" ||
        name === "__parent__" ||
        name === "__proto__" ||

        // methods
        name === "hasOwnProperty" ||
        name === "valueOf"
       )
    {
      return name + "$";
    }

    return name;
  };

  // Require system
  // --------------

  Opal.modules         = {};
  Opal.loaded_features = ['corelib/runtime'];
  Opal.current_dir     = '.'
  Opal.require_table   = {'corelib/runtime': true};

  function normalize(path) {
    var parts, part, new_parts = [], SEPARATOR = '/';

    if (Opal.current_dir !== '.') {
      path = Opal.current_dir.replace(/\/*$/, '/') + path;
    }

    path = path.replace(/\.(rb|opal|js)$/, '');
    parts = path.split(SEPARATOR);

    for (var i = 0, ii = parts.length; i < ii; i++) {
      part = parts[i];
      if (part === '') continue;
      (part === '..') ? new_parts.pop() : new_parts.push(part)
    }

    return new_parts.join(SEPARATOR);
  }

  Opal.loaded = function(paths) {
    var i, l, path;

    for (i = 0, l = paths.length; i < l; i++) {
      path = normalize(paths[i]);

      if (Opal.require_table[path]) {
        return;
      }

      Opal.loaded_features.push(path);
      Opal.require_table[path] = true;
    }
  }

  Opal.load = function(path) {
    path = normalize(path);

    Opal.loaded([path]);

    var module = Opal.modules[path];

    if (module) {
      module(Opal);
    }
    else {
      var severity = Opal.dynamic_require_severity || 'warning';
      var message  = 'cannot load such file -- ' + path;

      if (severity === "error") {
        Opal.LoadError ? Opal.LoadError.$new(message) : function(){throw message}();
      }
      else if (severity === "warning") {
        console.warn('WARNING: LoadError: ' + message);
      }
    }

    return true;
  }

  Opal.require = function(path) {
    path = normalize(path);

    if (Opal.require_table[path]) {
      return false;
    }

    return Opal.load(path);
  }

  // Initialization
  // --------------

  // Constructors for *instances* of core objects
  boot_class_alloc('BasicObject', BasicObject_alloc);
  boot_class_alloc('Object',      Object_alloc,       BasicObject_alloc);
  boot_class_alloc('Module',      Module_alloc,       Object_alloc);
  boot_class_alloc('Class',       Class_alloc,        Module_alloc);

  // Constructors for *classes* of core objects
  BasicObject = boot_core_class_object('BasicObject', BasicObject_alloc, Class_alloc);
  _Object     = boot_core_class_object('Object',      Object_alloc,      BasicObject.constructor);
  Module      = boot_core_class_object('Module',      Module_alloc,      _Object.constructor);
  Class       = boot_core_class_object('Class',       Class_alloc,       Module.constructor);

  // Fix booted classes to use their metaclass
  BasicObject.$$class = Class;
  _Object.$$class     = Class;
  Module.$$class      = Class;
  Class.$$class       = Class;

  // Fix superclasses of booted classes
  BasicObject.$$super = null;
  _Object.$$super     = BasicObject;
  Module.$$super      = _Object;
  Class.$$super       = Module;

  BasicObject.$$parent = null;
  _Object.$$parent     = BasicObject;
  Module.$$parent      = _Object;
  Class.$$parent       = Module;

  Opal.base                = _Object;
  BasicObject.$$scope      = _Object.$$scope = Opal;
  BasicObject.$$orig_scope = _Object.$$orig_scope = Opal;

  Module.$$scope      = _Object.$$scope;
  Module.$$orig_scope = _Object.$$orig_scope;
  Class.$$scope       = _Object.$$scope;
  Class.$$orig_scope  = _Object.$$orig_scope;

  _Object.$$proto.toString = function() {
    return this.$to_s();
  };

  _Object.$$proto.$require = Opal.require;

  Opal.top = new _Object.$$alloc();

  // Nil
  Opal.klass(_Object, _Object, 'NilClass', NilClass_alloc);
  nil = Opal.nil = new NilClass_alloc();
  nil.$$id = nil_id;
  nil.call = nil.apply = function() { throw Opal.LocalJumpError.$new('no block given'); };

  Opal.breaker  = new Error('unexpected break');
  Opal.returner = new Error('unexpected return');

  TypeError.$$super = Error;
}).call(this);

if (typeof(global) !== 'undefined') {
  global.Opal = this.Opal;
  Opal.global = global;
}

if (typeof(window) !== 'undefined') {
  window.Opal = this.Opal;
  Opal.global = window;
}
Opal.loaded(["corelib/runtime"]);
/* Generated by Opal 0.9.0 */
Opal.modules["corelib/helpers"] = function(Opal) {
  Opal.dynamic_require_severity = "error";
  var OPAL_CONFIG = { method_missing: true, arity_check: false, freezing: true, tainting: true };
  var self = Opal.top, $scope = Opal, nil = Opal.nil, $breaker = Opal.breaker, $slice = Opal.slice, $module = Opal.module;

  Opal.add_stubs(['$new', '$class', '$===', '$respond_to?', '$raise', '$type_error', '$__send__', '$coerce_to', '$nil?', '$<=>', '$inspect', '$coerce_to!']);
  return (function($base) {
    var $Opal, self = $Opal = $module($base, 'Opal');

    var def = self.$$proto, $scope = self.$$scope;

    Opal.defs(self, '$bridge', function(klass, constructor) {
      var self = this;

      return Opal.bridge(klass, constructor);
    });

    Opal.defs(self, '$type_error', function(object, type, method, coerced) {
      var $a, $b, self = this;

      if (method == null) {
        method = nil
      }
      if (coerced == null) {
        coerced = nil
      }
      if ((($a = (($b = method !== false && method !== nil) ? coerced : method)) !== nil && (!$a.$$is_boolean || $a == true))) {
        return $scope.get('TypeError').$new("can't convert " + (object.$class()) + " into " + (type) + " (" + (object.$class()) + "#" + (method) + " gives " + (coerced.$class()))
        } else {
        return $scope.get('TypeError').$new("no implicit conversion of " + (object.$class()) + " into " + (type))
      };
    });

    Opal.defs(self, '$coerce_to', function(object, type, method) {
      var $a, self = this;

      if ((($a = type['$==='](object)) !== nil && (!$a.$$is_boolean || $a == true))) {
        return object};
      if ((($a = object['$respond_to?'](method)) !== nil && (!$a.$$is_boolean || $a == true))) {
        } else {
        self.$raise(self.$type_error(object, type))
      };
      return object.$__send__(method);
    });

    Opal.defs(self, '$coerce_to!', function(object, type, method) {
      var $a, self = this, coerced = nil;

      coerced = self.$coerce_to(object, type, method);
      if ((($a = type['$==='](coerced)) !== nil && (!$a.$$is_boolean || $a == true))) {
        } else {
        self.$raise(self.$type_error(object, type, method, coerced))
      };
      return coerced;
    });

    Opal.defs(self, '$coerce_to?', function(object, type, method) {
      var $a, self = this, coerced = nil;

      if ((($a = object['$respond_to?'](method)) !== nil && (!$a.$$is_boolean || $a == true))) {
        } else {
        return nil
      };
      coerced = self.$coerce_to(object, type, method);
      if ((($a = coerced['$nil?']()) !== nil && (!$a.$$is_boolean || $a == true))) {
        return nil};
      if ((($a = type['$==='](coerced)) !== nil && (!$a.$$is_boolean || $a == true))) {
        } else {
        self.$raise(self.$type_error(object, type, method, coerced))
      };
      return coerced;
    });

    Opal.defs(self, '$try_convert', function(object, type, method) {
      var $a, self = this;

      if ((($a = type['$==='](object)) !== nil && (!$a.$$is_boolean || $a == true))) {
        return object};
      if ((($a = object['$respond_to?'](method)) !== nil && (!$a.$$is_boolean || $a == true))) {
        return object.$__send__(method)
        } else {
        return nil
      };
    });

    Opal.defs(self, '$compare', function(a, b) {
      var $a, self = this, compare = nil;

      compare = a['$<=>'](b);
      if ((($a = compare === nil) !== nil && (!$a.$$is_boolean || $a == true))) {
        self.$raise($scope.get('ArgumentError'), "comparison of " + (a.$class()) + " with " + (b.$class()) + " failed")};
      return compare;
    });

    Opal.defs(self, '$destructure', function(args) {
      var self = this;

      
      if (args.length == 1) {
        return args[0];
      }
      else if (args.$$is_array) {
        return args;
      }
      else {
        return $slice.call(args);
      }
    
    });

    Opal.defs(self, '$respond_to?', function(obj, method) {
      var self = this;

      
      if (obj == null || !obj.$$class) {
        return false;
      }
    
      return obj['$respond_to?'](method);
    });

    Opal.defs(self, '$inspect', function(obj) {
      var self = this;

      
      if (obj === undefined) {
        return "undefined";
      }
      else if (obj === null) {
        return "null";
      }
      else if (!obj.$$class) {
        return obj.toString();
      }
      else {
        return obj.$inspect();
      }
    
    });

    Opal.defs(self, '$instance_variable_name!', function(name) {
      var $a, self = this;

      name = $scope.get('Opal')['$coerce_to!'](name, $scope.get('String'), "to_str");
      if ((($a = /^@[a-zA-Z_][a-zA-Z0-9_]*?$/.test(name)) !== nil && (!$a.$$is_boolean || $a == true))) {
        } else {
        self.$raise($scope.get('NameError').$new("'" + (name) + "' is not allowed as an instance variable name", name))
      };
      return name;
    });
  })($scope.base)
};

/* Generated by Opal 0.9.0 */
Opal.modules["corelib/module"] = function(Opal) {
  Opal.dynamic_require_severity = "error";
  var OPAL_CONFIG = { method_missing: true, arity_check: false, freezing: true, tainting: true };
  function $rb_lt(lhs, rhs) {
    return (typeof(lhs) === 'number' && typeof(rhs) === 'number') ? lhs < rhs : lhs['$<'](rhs);
  }
  function $rb_gt(lhs, rhs) {
    return (typeof(lhs) === 'number' && typeof(rhs) === 'number') ? lhs > rhs : lhs['$>'](rhs);
  }
  var self = Opal.top, $scope = Opal, nil = Opal.nil, $breaker = Opal.breaker, $slice = Opal.slice, $klass = Opal.klass;

  Opal.add_stubs(['$===', '$raise', '$equal?', '$<', '$>', '$nil?', '$attr_reader', '$attr_writer', '$coerce_to!', '$new', '$=~', '$inject', '$const_get', '$split', '$const_missing', '$to_str', '$to_proc', '$lambda', '$bind', '$call', '$class', '$append_features', '$included', '$name', '$to_s', '$__id__']);
  return (function($base, $super) {
    function $Module(){};
    var self = $Module = $klass($base, $super, 'Module', $Module);

    var def = self.$$proto, $scope = self.$$scope, TMP_1, TMP_3, TMP_5, TMP_6;

    Opal.defs(self, '$new', TMP_1 = function() {
      var self = this, $iter = TMP_1.$$p, block = $iter || nil;

      TMP_1.$$p = null;
      
      var klass         = Opal.boot_module_object();
      klass.$$name      = nil;
      klass.$$class     = Opal.Module;
      klass.$$dep       = []
      klass.$$is_module = true;
      klass.$$proto     = {};

      // inherit scope from parent
      Opal.create_scope(Opal.Module.$$scope, klass);

      if (block !== nil) {
        var block_self = block.$$s;
        block.$$s = null;
        block.call(klass);
        block.$$s = block_self;
      }

      return klass;
    
    });

    Opal.defn(self, '$===', function(object) {
      var $a, self = this;

      if ((($a = object == null) !== nil && (!$a.$$is_boolean || $a == true))) {
        return false};
      return Opal.is_a(object, self);
    });

    Opal.defn(self, '$<', function(other) {
      var $a, self = this;

      if ((($a = $scope.get('Module')['$==='](other)) !== nil && (!$a.$$is_boolean || $a == true))) {
        } else {
        self.$raise($scope.get('TypeError'), "compared with non class/module")
      };
      
      var working = self,
          ancestors,
          i, length;

      if (working === other) {
        return false;
      }

      for (i = 0, ancestors = Opal.ancestors(self), length = ancestors.length; i < length; i++) {
        if (ancestors[i] === other) {
          return true;
        }
      }

      for (i = 0, ancestors = Opal.ancestors(other), length = ancestors.length; i < length; i++) {
        if (ancestors[i] === self) {
          return false;
        }
      }

      return nil;
    
    });

    Opal.defn(self, '$<=', function(other) {
      var $a, self = this;

      return ((($a = self['$equal?'](other)) !== false && $a !== nil) ? $a : $rb_lt(self, other));
    });

    Opal.defn(self, '$>', function(other) {
      var $a, self = this;

      if ((($a = $scope.get('Module')['$==='](other)) !== nil && (!$a.$$is_boolean || $a == true))) {
        } else {
        self.$raise($scope.get('TypeError'), "compared with non class/module")
      };
      return $rb_lt(other, self);
    });

    Opal.defn(self, '$>=', function(other) {
      var $a, self = this;

      return ((($a = self['$equal?'](other)) !== false && $a !== nil) ? $a : $rb_gt(self, other));
    });

    Opal.defn(self, '$<=>', function(other) {
      var $a, self = this, lt = nil;

      
      if (self === other) {
        return 0;
      }
    
      if ((($a = $scope.get('Module')['$==='](other)) !== nil && (!$a.$$is_boolean || $a == true))) {
        } else {
        return nil
      };
      lt = $rb_lt(self, other);
      if ((($a = lt['$nil?']()) !== nil && (!$a.$$is_boolean || $a == true))) {
        return nil};
      if (lt !== false && lt !== nil) {
        return -1
        } else {
        return 1
      };
    });

    Opal.defn(self, '$alias_method', function(newname, oldname) {
      var self = this;

      Opal.alias(self, newname, oldname);
      return self;
    });

    Opal.defn(self, '$alias_native', function(mid, jsid) {
      var self = this;

      if (jsid == null) {
        jsid = mid
      }
      Opal.alias_native(self, mid, jsid);
      return self;
    });

    Opal.defn(self, '$ancestors', function() {
      var self = this;

      return Opal.ancestors(self);
    });

    Opal.defn(self, '$append_features', function(klass) {
      var self = this;

      Opal.append_features(self, klass);
      return self;
    });

    Opal.defn(self, '$attr_accessor', function() {
      var $a, $b, self = this, $splat_index = nil;

      var array_size = arguments.length - 0;
      if(array_size < 0) array_size = 0;
      var names = new Array(array_size);
      for($splat_index = 0; $splat_index < array_size; $splat_index++) {
        names[$splat_index] = arguments[$splat_index + 0];
      }
      ($a = self).$attr_reader.apply($a, Opal.to_a(names));
      return ($b = self).$attr_writer.apply($b, Opal.to_a(names));
    });

    Opal.alias(self, 'attr', 'attr_accessor');

    Opal.defn(self, '$attr_reader', function() {
      var self = this, $splat_index = nil;

      var array_size = arguments.length - 0;
      if(array_size < 0) array_size = 0;
      var names = new Array(array_size);
      for($splat_index = 0; $splat_index < array_size; $splat_index++) {
        names[$splat_index] = arguments[$splat_index + 0];
      }
      
      var proto = self.$$proto;

      for (var i = names.length - 1; i >= 0; i--) {
        var name = names[i],
            id   = '$' + name,
            ivar = Opal.ivar(name);

        // the closure here is needed because name will change at the next
        // cycle, I wish we could use let.
        var body = (function(ivar) {
          return function() {
            if (this[ivar] == null) {
              return nil;
            }
            else {
              return this[ivar];
            }
          };
        })(ivar);

        // initialize the instance variable as nil
        proto[ivar] = nil;

        if (self.$$is_singleton) {
          proto.constructor.prototype[id] = body;
        }
        else {
          Opal.defn(self, id, body);
        }
      }
    
      return nil;
    });

    Opal.defn(self, '$attr_writer', function() {
      var self = this, $splat_index = nil;

      var array_size = arguments.length - 0;
      if(array_size < 0) array_size = 0;
      var names = new Array(array_size);
      for($splat_index = 0; $splat_index < array_size; $splat_index++) {
        names[$splat_index] = arguments[$splat_index + 0];
      }
      
      var proto = self.$$proto;

      for (var i = names.length - 1; i >= 0; i--) {
        var name = names[i],
            id   = '$' + name + '=',
            ivar = Opal.ivar(name);

        // the closure here is needed because name will change at the next
        // cycle, I wish we could use let.
        var body = (function(ivar){
          return function(value) {
            return this[ivar] = value;
          }
        })(ivar);

        // initialize the instance variable as nil
        proto[ivar] = nil;

        if (self.$$is_singleton) {
          proto.constructor.prototype[id] = body;
        }
        else {
          Opal.defn(self, id, body);
        }
      }
    
      return nil;
    });

    Opal.defn(self, '$autoload', function(const$, path) {
      var self = this;

      
      var autoloaders;

      if (!(autoloaders = self.$$autoload)) {
        autoloaders = self.$$autoload = {};
      }

      autoloaders[const$] = path;
      return nil;
    ;
    });

    Opal.defn(self, '$class_variable_get', function(name) {
      var $a, self = this;

      name = $scope.get('Opal')['$coerce_to!'](name, $scope.get('String'), "to_str");
      if ((($a = name.length < 3 || name.slice(0,2) !== '@@') !== nil && (!$a.$$is_boolean || $a == true))) {
        self.$raise($scope.get('NameError').$new("class vars should start with @@", name))};
      
      var value = Opal.cvars[name.slice(2)];
      (function() {if ((($a = value == null) !== nil && (!$a.$$is_boolean || $a == true))) {
        return self.$raise($scope.get('NameError').$new("uninitialized class variable @@a in", name))
        } else {
        return nil
      }; return nil; })()
      return value;
    
    });

    Opal.defn(self, '$class_variable_set', function(name, value) {
      var $a, self = this;

      name = $scope.get('Opal')['$coerce_to!'](name, $scope.get('String'), "to_str");
      if ((($a = name.length < 3 || name.slice(0,2) !== '@@') !== nil && (!$a.$$is_boolean || $a == true))) {
        self.$raise($scope.get('NameError'))};
      
      Opal.cvars[name.slice(2)] = value;
      return value;
    
    });

    Opal.defn(self, '$constants', function() {
      var self = this;

      return self.$$scope.constants.slice(0);
    });

    Opal.defn(self, '$const_defined?', function(name, inherit) {
      var $a, self = this;

      if (inherit == null) {
        inherit = true
      }
      if ((($a = name['$=~'](/^[A-Z]\w*$/)) !== nil && (!$a.$$is_boolean || $a == true))) {
        } else {
        self.$raise($scope.get('NameError').$new("wrong constant name " + (name), name))
      };
      
      var scopes = [self.$$scope];

      if (inherit || self === Opal.Object) {
        var parent = self.$$super;

        while (parent !== Opal.BasicObject) {
          scopes.push(parent.$$scope);

          parent = parent.$$super;
        }
      }

      for (var i = 0, length = scopes.length; i < length; i++) {
        if (scopes[i].hasOwnProperty(name)) {
          return true;
        }
      }

      return false;
    
    });

    Opal.defn(self, '$const_get', function(name, inherit) {
      var $a, $b, TMP_2, self = this;

      if (inherit == null) {
        inherit = true
      }
      if ((($a = name.indexOf('::') != -1 && name != '::') !== nil && (!$a.$$is_boolean || $a == true))) {
        return ($a = ($b = name.$split("::")).$inject, $a.$$p = (TMP_2 = function(o, c){var self = TMP_2.$$s || this;
if (o == null) o = nil;if (c == null) c = nil;
        return o.$const_get(c)}, TMP_2.$$s = self, TMP_2), $a).call($b, self)};
      if ((($a = /^[A-Z]\w*$/.test(name)) !== nil && (!$a.$$is_boolean || $a == true))) {
        } else {
        self.$raise($scope.get('NameError').$new("wrong constant name " + (name), name))
      };
      
      var scopes = [self.$$scope];

      if (inherit || self == Opal.Object) {
        var parent = self.$$super;

        while (parent !== Opal.BasicObject) {
          scopes.push(parent.$$scope);

          parent = parent.$$super;
        }
      }

      for (var i = 0, length = scopes.length; i < length; i++) {
        if (scopes[i].hasOwnProperty(name)) {
          return scopes[i][name];
        }
      }

      return self.$const_missing(name);
    
    });

    Opal.defn(self, '$const_missing', function(name) {
      var self = this;

      
      if (self.$$autoload) {
        var file = self.$$autoload[name];

        if (file) {
          self.$require(file);

          return self.$const_get(name);
        }
      }
    
      return self.$raise($scope.get('NameError').$new("uninitialized constant " + (self) + "::" + (name), name));
    });

    Opal.defn(self, '$const_set', function(name, value) {
      var $a, self = this;

      if ((($a = name['$=~'](/^[A-Z]\w*$/)) !== nil && (!$a.$$is_boolean || $a == true))) {
        } else {
        self.$raise($scope.get('NameError').$new("wrong constant name " + (name), name))
      };
      try {
      name = name.$to_str()
      } catch ($err) {if (true) {
        try {
          self.$raise($scope.get('TypeError'), "conversion with #to_str failed")
        } finally {
          Opal.gvars["!"] = Opal.exceptions.pop() || Opal.nil;
        }
        }else { throw $err; }
      };
      Opal.casgn(self, name, value);
      return value;
    });

    Opal.defn(self, '$define_method', TMP_3 = function(name, method) {
      var $a, $b, $c, TMP_4, self = this, $iter = TMP_3.$$p, block = $iter || nil, $case = nil;

      TMP_3.$$p = null;
      if ((($a = method === undefined && block === nil) !== nil && (!$a.$$is_boolean || $a == true))) {
        self.$raise($scope.get('ArgumentError'), "tried to create a Proc object without a block")};
      ((($a = block) !== false && $a !== nil) ? $a : block = (function() {$case = method;if ($scope.get('Proc')['$===']($case)) {return method}else if ($scope.get('Method')['$===']($case)) {return method.$to_proc().$$unbound;}else if ($scope.get('UnboundMethod')['$===']($case)) {return ($b = ($c = self).$lambda, $b.$$p = (TMP_4 = function(args){var self = TMP_4.$$s || this, $a, bound = nil;
args = $slice.call(arguments, 0);
      bound = method.$bind(self);
        return ($a = bound).$call.apply($a, Opal.to_a(args));}, TMP_4.$$s = self, TMP_4), $b).call($c)}else {return self.$raise($scope.get('TypeError'), "wrong argument type " + (block.$class()) + " (expected Proc/Method)")}})());
      
      var id = '$' + name;

      block.$$jsid = name;
      block.$$s    = null;
      block.$$def  = block;

      Opal.defn(self, id, block);

      return name;
    
    });

    Opal.defn(self, '$remove_method', function() {
      var self = this, $splat_index = nil;

      var array_size = arguments.length - 0;
      if(array_size < 0) array_size = 0;
      var names = new Array(array_size);
      for($splat_index = 0; $splat_index < array_size; $splat_index++) {
        names[$splat_index] = arguments[$splat_index + 0];
      }
      
      for (var i = 0, length = names.length; i < length; i++) {
        Opal.rdef(self, "$" + names[i]);
      }
    
      return self;
    });

    Opal.defn(self, '$singleton_class?', function() {
      var self = this;

      return !!self.$$is_singleton;
    });

    Opal.defn(self, '$include', function() {
      var self = this, $splat_index = nil;

      var array_size = arguments.length - 0;
      if(array_size < 0) array_size = 0;
      var mods = new Array(array_size);
      for($splat_index = 0; $splat_index < array_size; $splat_index++) {
        mods[$splat_index] = arguments[$splat_index + 0];
      }
      
      for (var i = mods.length - 1; i >= 0; i--) {
        var mod = mods[i];

        if (mod === self) {
          continue;
        }

        if (!mod.$$is_module) {
          self.$raise($scope.get('TypeError'), "wrong argument type " + ((mod).$class()) + " (expected Module)");
        }

        (mod).$append_features(self);
        (mod).$included(self);
      }
    
      return self;
    });

    Opal.defn(self, '$include?', function(mod) {
      var self = this;

      
      for (var cls = self; cls; cls = cls.$$super) {
        for (var i = 0; i != cls.$$inc.length; i++) {
          var mod2 = cls.$$inc[i];
          if (mod === mod2) {
            return true;
          }
        }
      }
      return false;
    
    });

    Opal.defn(self, '$instance_method', function(name) {
      var self = this;

      
      var meth = self.$$proto['$' + name];

      if (!meth || meth.$$stub) {
        self.$raise($scope.get('NameError').$new("undefined method `" + (name) + "' for class `" + (self.$name()) + "'", name));
      }

      return $scope.get('UnboundMethod').$new(self, meth, name);
    
    });

    Opal.defn(self, '$instance_methods', function(include_super) {
      var self = this;

      if (include_super == null) {
        include_super = true
      }
      
      var methods = [],
          proto   = self.$$proto;

      for (var prop in proto) {
        if (prop.charAt(0) !== '$') {
          continue;
        }

        if (typeof(proto[prop]) !== "function") {
          continue;
        }

        if (proto[prop].$$stub) {
          continue;
        }

        if (!self.$$is_module) {
          if (self !== Opal.BasicObject && proto[prop] === Opal.BasicObject.$$proto[prop]) {
            continue;
          }

          if (!include_super && !proto.hasOwnProperty(prop)) {
            continue;
          }

          if (!include_super && proto[prop].$$donated) {
            continue;
          }
        }

        methods.push(prop.substr(1));
      }

      return methods;
    
    });

    Opal.defn(self, '$included', function(mod) {
      var self = this;

      return nil;
    });

    Opal.defn(self, '$extended', function(mod) {
      var self = this;

      return nil;
    });

    Opal.defn(self, '$method_added', function() {
      var self = this;

      return nil;
    });

    Opal.defn(self, '$method_removed', function() {
      var self = this;

      return nil;
    });

    Opal.defn(self, '$method_undefined', function() {
      var self = this;

      return nil;
    });

    Opal.defn(self, '$module_eval', TMP_5 = function() {
      var self = this, $iter = TMP_5.$$p, block = $iter || nil;

      TMP_5.$$p = null;
      if (block !== false && block !== nil) {
        } else {
        self.$raise($scope.get('ArgumentError'), "no block given")
      };
      
      var old = block.$$s,
          result;

      block.$$s = null;
      result = block.call(self);
      block.$$s = old;

      return result;
    
    });

    Opal.alias(self, 'class_eval', 'module_eval');

    Opal.defn(self, '$module_exec', TMP_6 = function() {
      var self = this, $iter = TMP_6.$$p, block = $iter || nil, $splat_index = nil;

      var array_size = arguments.length - 0;
      if(array_size < 0) array_size = 0;
      var args = new Array(array_size);
      for($splat_index = 0; $splat_index < array_size; $splat_index++) {
        args[$splat_index] = arguments[$splat_index + 0];
      }
      TMP_6.$$p = null;
      
      if (block === nil) {
        self.$raise($scope.get('LocalJumpError'), "no block given")
      }

      var block_self = block.$$s, result;

      block.$$s = null;
      result = block.apply(self, args);
      block.$$s = block_self;

      return result;
    ;
    });

    Opal.alias(self, 'class_exec', 'module_exec');

    Opal.defn(self, '$method_defined?', function(method) {
      var self = this;

      
      var body = self.$$proto['$' + method];
      return (!!body) && !body.$$stub;
    
    });

    Opal.defn(self, '$module_function', function() {
      var self = this, $splat_index = nil;

      var array_size = arguments.length - 0;
      if(array_size < 0) array_size = 0;
      var methods = new Array(array_size);
      for($splat_index = 0; $splat_index < array_size; $splat_index++) {
        methods[$splat_index] = arguments[$splat_index + 0];
      }
      
      if (methods.length === 0) {
        self.$$module_function = true;
      }
      else {
        for (var i = 0, length = methods.length; i < length; i++) {
          var meth = methods[i],
              id   = '$' + meth,
              func = self.$$proto[id];

          Opal.defs(self, id, func);
        }
      }

      return self;
    
    });

    Opal.defn(self, '$name', function() {
      var self = this;

      
      if (self.$$full_name) {
        return self.$$full_name;
      }

      var result = [], base = self;

      while (base) {
        if (base.$$name === nil) {
          return result.length === 0 ? nil : result.join('::');
        }

        result.unshift(base.$$name);

        base = base.$$base_module;

        if (base === Opal.Object) {
          break;
        }
      }

      if (result.length === 0) {
        return nil;
      }

      return self.$$full_name = result.join('::');
    
    });

    Opal.defn(self, '$remove_class_variable', function() {
      var self = this;

      return nil;
    });

    Opal.defn(self, '$remove_const', function(name) {
      var self = this;

      
      var old = self.$$scope[name];
      delete self.$$scope[name];
      return old;
    
    });

    Opal.defn(self, '$to_s', function() {
      var $a, self = this;

      return ((($a = Opal.Module.$name.call(self)) !== false && $a !== nil) ? $a : "#<" + (self.$$is_module ? 'Module' : 'Class') + ":0x" + (self.$__id__().$to_s(16)) + ">");
    });

    return (Opal.defn(self, '$undef_method', function() {
      var self = this, $splat_index = nil;

      var array_size = arguments.length - 0;
      if(array_size < 0) array_size = 0;
      var names = new Array(array_size);
      for($splat_index = 0; $splat_index < array_size; $splat_index++) {
        names[$splat_index] = arguments[$splat_index + 0];
      }
      
      for (var i = 0, length = names.length; i < length; i++) {
        Opal.udef(self, "$" + names[i]);
      }
    
      return self;
    }), nil) && 'undef_method';
  })($scope.base, null)
};

/* Generated by Opal 0.9.0 */
Opal.modules["corelib/class"] = function(Opal) {
  Opal.dynamic_require_severity = "error";
  var OPAL_CONFIG = { method_missing: true, arity_check: false, freezing: true, tainting: true };
  var self = Opal.top, $scope = Opal, nil = Opal.nil, $breaker = Opal.breaker, $slice = Opal.slice, $klass = Opal.klass;

  Opal.add_stubs(['$require', '$raise', '$allocate']);
  self.$require("corelib/module");
  return (function($base, $super) {
    function $Class(){};
    var self = $Class = $klass($base, $super, 'Class', $Class);

    var def = self.$$proto, $scope = self.$$scope, TMP_1, TMP_2;

    Opal.defs(self, '$new', TMP_1 = function(sup) {
      var self = this, $iter = TMP_1.$$p, block = $iter || nil;

      if (sup == null) {
        sup = $scope.get('Object')
      }
      TMP_1.$$p = null;
      
      if (!sup.$$is_class) {
        self.$raise($scope.get('TypeError'), "superclass must be a Class");
      }

      function AnonClass(){};
      var klass        = Opal.boot_class(sup, AnonClass)
      klass.$$name     = nil;
      klass.$$parent   = sup;
      klass.$$is_class = true;

      // inherit scope from parent
      Opal.create_scope(sup.$$scope, klass);

      sup.$inherited(klass);

      if (block !== nil) {
        var block_self = block.$$s;
        block.$$s = null;
        block.call(klass);
        block.$$s = block_self;
      }

      return klass;
    ;
    });

    Opal.defn(self, '$allocate', function() {
      var self = this;

      
      var obj = new self.$$alloc();
      obj.$$id = Opal.uid();
      return obj;
    
    });

    Opal.defn(self, '$inherited', function(cls) {
      var self = this;

      return nil;
    });

    Opal.defn(self, '$new', TMP_2 = function() {
      var self = this, $iter = TMP_2.$$p, block = $iter || nil, $splat_index = nil;

      var array_size = arguments.length - 0;
      if(array_size < 0) array_size = 0;
      var args = new Array(array_size);
      for($splat_index = 0; $splat_index < array_size; $splat_index++) {
        args[$splat_index] = arguments[$splat_index + 0];
      }
      TMP_2.$$p = null;
      
      var obj = self.$allocate();

      obj.$initialize.$$p = block;
      obj.$initialize.apply(obj, args);
      return obj;
    ;
    });

    return (Opal.defn(self, '$superclass', function() {
      var self = this;

      return self.$$super || nil;
    }), nil) && 'superclass';
  })($scope.base, null);
};

/* Generated by Opal 0.9.0 */
Opal.modules["corelib/basic_object"] = function(Opal) {
  Opal.dynamic_require_severity = "error";
  var OPAL_CONFIG = { method_missing: true, arity_check: false, freezing: true, tainting: true };
  function $rb_gt(lhs, rhs) {
    return (typeof(lhs) === 'number' && typeof(rhs) === 'number') ? lhs > rhs : lhs['$>'](rhs);
  }
  var self = Opal.top, $scope = Opal, nil = Opal.nil, $breaker = Opal.breaker, $slice = Opal.slice, $klass = Opal.klass, $range = Opal.range, $hash2 = Opal.hash2;

  Opal.add_stubs(['$==', '$!', '$nil?', '$cover?', '$size', '$raise', '$compile', '$lambda', '$>', '$new', '$inspect']);
  return (function($base, $super) {
    function $BasicObject(){};
    var self = $BasicObject = $klass($base, $super, 'BasicObject', $BasicObject);

    var def = self.$$proto, $scope = self.$$scope, TMP_1, TMP_2, TMP_4, TMP_5;

    Opal.defn(self, '$initialize', function() {
      var self = this;

      return nil;
    });

    Opal.defn(self, '$==', function(other) {
      var self = this;

      return self === other;
    });

    Opal.defn(self, '$eql?', function(other) {
      var self = this;

      return self['$=='](other);
    });

    Opal.alias(self, 'equal?', '==');

    Opal.defn(self, '$__id__', function() {
      var self = this;

      return self.$$id || (self.$$id = Opal.uid());
    });

    Opal.defn(self, '$__send__', TMP_1 = function(symbol) {
      var self = this, $iter = TMP_1.$$p, block = $iter || nil, $splat_index = nil;

      var array_size = arguments.length - 1;
      if(array_size < 0) array_size = 0;
      var args = new Array(array_size);
      for($splat_index = 0; $splat_index < array_size; $splat_index++) {
        args[$splat_index] = arguments[$splat_index + 1];
      }
      TMP_1.$$p = null;
      
      var func = self['$' + symbol]

      if (func) {
        if (block !== nil) {
          func.$$p = block;
        }

        return func.apply(self, args);
      }

      if (block !== nil) {
        self.$method_missing.$$p = block;
      }

      return self.$method_missing.apply(self, [symbol].concat(args));
    
    });

    Opal.defn(self, '$!', function() {
      var self = this;

      return false;
    });

    Opal.defn(self, '$!=', function(other) {
      var self = this;

      return (self['$=='](other))['$!']();
    });

    Opal.alias(self, 'equal?', '==');

    Opal.defn(self, '$instance_eval', TMP_2 = function() {
      var $a, $b, TMP_3, self = this, $iter = TMP_2.$$p, block = $iter || nil, string = nil, file = nil, _lineno = nil, compiled = nil, wrapper = nil, $splat_index = nil;

      var array_size = arguments.length - 0;
      if(array_size < 0) array_size = 0;
      var args = new Array(array_size);
      for($splat_index = 0; $splat_index < array_size; $splat_index++) {
        args[$splat_index] = arguments[$splat_index + 0];
      }
      TMP_2.$$p = null;
      if ((($a = ($b = block['$nil?'](), $b !== false && $b !== nil ?!!Opal.compile : $b)) !== nil && (!$a.$$is_boolean || $a == true))) {
        if ((($a = ($range(1, 3, false))['$cover?'](args.$size())) !== nil && (!$a.$$is_boolean || $a == true))) {
          } else {
          $scope.get('Kernel').$raise($scope.get('ArgumentError'), "wrong number of arguments (0 for 1..3)")
        };
        $a = Opal.to_a(args), string = ($a[0] == null ? nil : $a[0]), file = ($a[1] == null ? nil : $a[1]), _lineno = ($a[2] == null ? nil : $a[2]), $a;
        compiled = $scope.get('Opal').$compile(string, $hash2(["file", "eval"], {"file": (((($a = file) !== false && $a !== nil) ? $a : "(eval)")), "eval": true}));
        wrapper = function() {return eval(compiled)};
        block = ($a = ($b = $scope.get('Kernel')).$lambda, $a.$$p = (TMP_3 = function(){var self = TMP_3.$$s || this;

        return wrapper.call(self);}, TMP_3.$$s = self, TMP_3), $a).call($b);
      } else if ($rb_gt(args.$size(), 0)) {
        $scope.get('Kernel').$raise($scope.get('ArgumentError'), "wrong number of arguments (" + (args.$size()) + " for 0)")};
      
      var old = block.$$s,
          result;

      block.$$s = null;

      // Need to pass $$eval so that method definitions know if this is
      // being done on a class/module. Cannot be compiler driven since
      // send(:instance_eval) needs to work.
      if (self.$$is_class || self.$$is_module) {
        self.$$eval = true;
        try {
          result = block.call(self, self);
        }
        finally {
          self.$$eval = false;
        }
      }
      else {
        result = block.call(self, self);
      }

      block.$$s = old;

      return result;
    
    });

    Opal.defn(self, '$instance_exec', TMP_4 = function() {
      var self = this, $iter = TMP_4.$$p, block = $iter || nil, $splat_index = nil;

      var array_size = arguments.length - 0;
      if(array_size < 0) array_size = 0;
      var args = new Array(array_size);
      for($splat_index = 0; $splat_index < array_size; $splat_index++) {
        args[$splat_index] = arguments[$splat_index + 0];
      }
      TMP_4.$$p = null;
      if (block !== false && block !== nil) {
        } else {
        $scope.get('Kernel').$raise($scope.get('ArgumentError'), "no block given")
      };
      
      var block_self = block.$$s,
          result;

      block.$$s = null;

      if (self.$$is_class || self.$$is_module) {
        self.$$eval = true;
        try {
          result = block.apply(self, args);
        }
        finally {
          self.$$eval = false;
        }
      }
      else {
        result = block.apply(self, args);
      }

      block.$$s = block_self;

      return result;
    
    });

    Opal.defn(self, '$singleton_method_added', function() {
      var self = this;

      return nil;
    });

    Opal.defn(self, '$singleton_method_removed', function() {
      var self = this;

      return nil;
    });

    Opal.defn(self, '$singleton_method_undefined', function() {
      var self = this;

      return nil;
    });

    return (Opal.defn(self, '$method_missing', TMP_5 = function(symbol) {
      var $a, self = this, $iter = TMP_5.$$p, block = $iter || nil, $splat_index = nil;

      var array_size = arguments.length - 1;
      if(array_size < 0) array_size = 0;
      var args = new Array(array_size);
      for($splat_index = 0; $splat_index < array_size; $splat_index++) {
        args[$splat_index] = arguments[$splat_index + 1];
      }
      TMP_5.$$p = null;
      return $scope.get('Kernel').$raise($scope.get('NoMethodError').$new((function() {if ((($a = self.$inspect && !self.$inspect.$$stub) !== nil && (!$a.$$is_boolean || $a == true))) {
        return "undefined method `" + (symbol) + "' for " + (self.$inspect()) + ":" + (self.$$class)
        } else {
        return "undefined method `" + (symbol) + "' for " + (self.$$class)
      }; return nil; })(), symbol));
    }), nil) && 'method_missing';
  })($scope.base, null)
};

/* Generated by Opal 0.9.0 */
Opal.modules["corelib/kernel"] = function(Opal) {
  Opal.dynamic_require_severity = "error";
  var OPAL_CONFIG = { method_missing: true, arity_check: false, freezing: true, tainting: true };
  function $rb_gt(lhs, rhs) {
    return (typeof(lhs) === 'number' && typeof(rhs) === 'number') ? lhs > rhs : lhs['$>'](rhs);
  }
  function $rb_le(lhs, rhs) {
    return (typeof(lhs) === 'number' && typeof(rhs) === 'number') ? lhs <= rhs : lhs['$<='](rhs);
  }
  var self = Opal.top, $scope = Opal, nil = Opal.nil, $breaker = Opal.breaker, $slice = Opal.slice, $module = Opal.module, $gvars = Opal.gvars, $hash2 = Opal.hash2, $klass = Opal.klass;

  Opal.add_stubs(['$raise', '$new', '$inspect', '$!', '$=~', '$==', '$object_id', '$class', '$coerce_to?', '$<<', '$allocate', '$copy_instance_variables', '$copy_singleton_methods', '$initialize_clone', '$initialize_copy', '$define_method', '$to_proc', '$singleton_class', '$initialize_dup', '$for', '$loop', '$pop', '$call', '$append_features', '$extended', '$length', '$respond_to?', '$[]', '$nil?', '$to_a', '$to_int', '$fetch', '$Integer', '$Float', '$to_ary', '$to_str', '$coerce_to', '$to_s', '$__id__', '$instance_variable_name!', '$coerce_to!', '$===', '$>', '$print', '$format', '$puts', '$each', '$<=', '$empty?', '$exception', '$kind_of?', '$respond_to_missing?', '$try_convert!', '$expand_path', '$join', '$start_with?', '$sym', '$arg', '$include']);
  (function($base) {
    var $Kernel, self = $Kernel = $module($base, 'Kernel');

    var def = self.$$proto, $scope = self.$$scope, TMP_1, TMP_2, TMP_3, TMP_4, TMP_6, TMP_7, TMP_8, TMP_10, TMP_11;

    Opal.defn(self, '$method_missing', TMP_1 = function(symbol) {
      var self = this, $iter = TMP_1.$$p, block = $iter || nil, $splat_index = nil;

      var array_size = arguments.length - 1;
      if(array_size < 0) array_size = 0;
      var args = new Array(array_size);
      for($splat_index = 0; $splat_index < array_size; $splat_index++) {
        args[$splat_index] = arguments[$splat_index + 1];
      }
      TMP_1.$$p = null;
      return self.$raise($scope.get('NoMethodError').$new("undefined method `" + (symbol) + "' for " + (self.$inspect()), symbol, args));
    });

    Opal.defn(self, '$=~', function(obj) {
      var self = this;

      return false;
    });

    Opal.defn(self, '$!~', function(obj) {
      var self = this;

      return (self['$=~'](obj))['$!']();
    });

    Opal.defn(self, '$===', function(other) {
      var $a, self = this;

      return ((($a = self.$object_id()['$=='](other.$object_id())) !== false && $a !== nil) ? $a : self['$=='](other));
    });

    Opal.defn(self, '$<=>', function(other) {
      var self = this;

      
      // set guard for infinite recursion
      self.$$comparable = true;

      var x = self['$=='](other);

      if (x && x !== nil) {
        return 0;
      }

      return nil;
    
    });

    Opal.defn(self, '$method', function(name) {
      var self = this;

      
      var meth = self['$' + name];

      if (!meth || meth.$$stub) {
        self.$raise($scope.get('NameError').$new("undefined method `" + (name) + "' for class `" + (self.$class()) + "'", name));
      }

      return $scope.get('Method').$new(self, meth, name);
    
    });

    Opal.defn(self, '$methods', function(all) {
      var self = this;

      if (all == null) {
        all = true
      }
      
      var methods = [];

      for (var key in self) {
        if (key[0] == "$" && typeof(self[key]) === "function") {
          if (all == false || all === nil) {
            if (!Opal.hasOwnProperty.call(self, key)) {
              continue;
            }
          }
          if (self[key].$$stub === undefined) {
            methods.push(key.substr(1));
          }
        }
      }

      return methods;
    
    });

    Opal.alias(self, 'public_methods', 'methods');

    Opal.defn(self, '$Array', function(object) {
      var self = this;

      
      var coerced;

      if (object === nil) {
        return [];
      }

      if (object.$$is_array) {
        return object;
      }

      coerced = $scope.get('Opal')['$coerce_to?'](object, $scope.get('Array'), "to_ary");
      if (coerced !== nil) { return coerced; }

      coerced = $scope.get('Opal')['$coerce_to?'](object, $scope.get('Array'), "to_a");
      if (coerced !== nil) { return coerced; }

      return [object];
    
    });

    Opal.defn(self, '$at_exit', TMP_2 = function() {
      var $a, self = this, $iter = TMP_2.$$p, block = $iter || nil;
      if ($gvars.__at_exit__ == null) $gvars.__at_exit__ = nil;

      TMP_2.$$p = null;
      ((($a = $gvars.__at_exit__) !== false && $a !== nil) ? $a : $gvars.__at_exit__ = []);
      return $gvars.__at_exit__['$<<'](block);
    });

    Opal.defn(self, '$caller', function() {
      var self = this;

      return [];
    });

    Opal.defn(self, '$class', function() {
      var self = this;

      return self.$$class;
    });

    Opal.defn(self, '$copy_instance_variables', function(other) {
      var self = this;

      
      for (var name in other) {
        if (other.hasOwnProperty(name) && name.charAt(0) !== '$') {
          self[name] = other[name];
        }
      }
    
    });

    Opal.defn(self, '$copy_singleton_methods', function(other) {
      var self = this;

      
      var name;

      if (other.hasOwnProperty('$$meta')) {
        var other_singleton_class_proto = Opal.get_singleton_class(other).$$proto;
        var self_singleton_class_proto = Opal.get_singleton_class(self).$$proto;

        for (name in other_singleton_class_proto) {
          if (name.charAt(0) === '$' && other_singleton_class_proto.hasOwnProperty(name)) {
            self_singleton_class_proto[name] = other_singleton_class_proto[name];
          }
        }
      }

      for (name in other) {
        if (name.charAt(0) === '$' && name.charAt(1) !== '$' && other.hasOwnProperty(name)) {
          self[name] = other[name];
        }
      }
    
    });

    Opal.defn(self, '$clone', function() {
      var self = this, copy = nil;

      copy = self.$class().$allocate();
      copy.$copy_instance_variables(self);
      copy.$copy_singleton_methods(self);
      copy.$initialize_clone(self);
      return copy;
    });

    Opal.defn(self, '$initialize_clone', function(other) {
      var self = this;

      return self.$initialize_copy(other);
    });

    Opal.defn(self, '$define_singleton_method', TMP_3 = function(name, method) {
      var $a, $b, self = this, $iter = TMP_3.$$p, block = $iter || nil;

      TMP_3.$$p = null;
      return ($a = ($b = self.$singleton_class()).$define_method, $a.$$p = block.$to_proc(), $a).call($b, name, method);
    });

    Opal.defn(self, '$dup', function() {
      var self = this, copy = nil;

      copy = self.$class().$allocate();
      copy.$copy_instance_variables(self);
      copy.$initialize_dup(self);
      return copy;
    });

    Opal.defn(self, '$initialize_dup', function(other) {
      var self = this;

      return self.$initialize_copy(other);
    });

    Opal.defn(self, '$enum_for', TMP_4 = function(method) {
      var $a, $b, self = this, $iter = TMP_4.$$p, block = $iter || nil, $splat_index = nil;

      var array_size = arguments.length - 1;
      if(array_size < 0) array_size = 0;
      var args = new Array(array_size);
      for($splat_index = 0; $splat_index < array_size; $splat_index++) {
        args[$splat_index] = arguments[$splat_index + 1];
      }
      if (method == null) {
        method = "each"
      }
      TMP_4.$$p = null;
      return ($a = ($b = $scope.get('Enumerator')).$for, $a.$$p = block.$to_proc(), $a).apply($b, [self, method].concat(Opal.to_a(args)));
    });

    Opal.alias(self, 'to_enum', 'enum_for');

    Opal.defn(self, '$equal?', function(other) {
      var self = this;

      return self === other;
    });

    Opal.defn(self, '$exit', function(status) {
      var $a, $b, TMP_5, self = this;
      if ($gvars.__at_exit__ == null) $gvars.__at_exit__ = nil;

      if (status == null) {
        status = true
      }
      ((($a = $gvars.__at_exit__) !== false && $a !== nil) ? $a : $gvars.__at_exit__ = []);
      ($a = ($b = self).$loop, $a.$$p = (TMP_5 = function(){var self = TMP_5.$$s || this, block = nil;
        if ($gvars.__at_exit__ == null) $gvars.__at_exit__ = nil;

      block = $gvars.__at_exit__.$pop();
        if (block !== false && block !== nil) {
          return block.$call()
          } else {
          return ($breaker.$v = nil, $breaker)
        };}, TMP_5.$$s = self, TMP_5), $a).call($b);
      if ((($a = status === true) !== nil && (!$a.$$is_boolean || $a == true))) {
        status = 0};
      Opal.exit(status);
      return nil;
    });

    Opal.defn(self, '$extend', function() {
      var self = this, $splat_index = nil;

      var array_size = arguments.length - 0;
      if(array_size < 0) array_size = 0;
      var mods = new Array(array_size);
      for($splat_index = 0; $splat_index < array_size; $splat_index++) {
        mods[$splat_index] = arguments[$splat_index + 0];
      }
      
      var singleton = self.$singleton_class();

      for (var i = mods.length - 1; i >= 0; i--) {
        var mod = mods[i];

        if (!mod.$$is_module) {
          self.$raise($scope.get('TypeError'), "wrong argument type " + ((mod).$class()) + " (expected Module)");
        }

        (mod).$append_features(singleton);
        (mod).$extended(self);
      }
    ;
      return self;
    });

    Opal.defn(self, '$format', function(format_string) {
      var $a, $b, self = this, ary = nil, $splat_index = nil;
      if ($gvars.DEBUG == null) $gvars.DEBUG = nil;

      var array_size = arguments.length - 1;
      if(array_size < 0) array_size = 0;
      var args = new Array(array_size);
      for($splat_index = 0; $splat_index < array_size; $splat_index++) {
        args[$splat_index] = arguments[$splat_index + 1];
      }
      if ((($a = (($b = args.$length()['$=='](1)) ? args['$[]'](0)['$respond_to?']("to_ary") : args.$length()['$=='](1))) !== nil && (!$a.$$is_boolean || $a == true))) {
        ary = $scope.get('Opal')['$coerce_to?'](args['$[]'](0), $scope.get('Array'), "to_ary");
        if ((($a = ary['$nil?']()) !== nil && (!$a.$$is_boolean || $a == true))) {
          } else {
          args = ary.$to_a()
        };};
      
      var result = '',
          //used for slicing:
          begin_slice = 0,
          end_slice,
          //used for iterating over the format string:
          i,
          len = format_string.length,
          //used for processing field values:
          arg,
          str,
          //used for processing %g and %G fields:
          exponent,
          //used for keeping track of width and precision:
          width,
          precision,
          //used for holding temporary values:
          tmp_num,
          //used for processing %{} and %<> fileds:
          hash_parameter_key,
          closing_brace_char,
          //used for processing %b, %B, %o, %x, and %X fields:
          base_number,
          base_prefix,
          base_neg_zero_regex,
          base_neg_zero_digit,
          //used for processing arguments:
          next_arg,
          seq_arg_num = 1,
          pos_arg_num = 0,
          //used for keeping track of flags:
          flags,
          FNONE  = 0,
          FSHARP = 1,
          FMINUS = 2,
          FPLUS  = 4,
          FZERO  = 8,
          FSPACE = 16,
          FWIDTH = 32,
          FPREC  = 64,
          FPREC0 = 128;

      function CHECK_FOR_FLAGS() {
        if (flags&FWIDTH) { self.$raise($scope.get('ArgumentError'), "flag after width") }
        if (flags&FPREC0) { self.$raise($scope.get('ArgumentError'), "flag after precision") }
      }

      function CHECK_FOR_WIDTH() {
        if (flags&FWIDTH) { self.$raise($scope.get('ArgumentError'), "width given twice") }
        if (flags&FPREC0) { self.$raise($scope.get('ArgumentError'), "width after precision") }
      }

      function GET_NTH_ARG(num) {
        if (num >= args.length) { self.$raise($scope.get('ArgumentError'), "too few arguments") }
        return args[num];
      }

      function GET_NEXT_ARG() {
        switch (pos_arg_num) {
        case -1: self.$raise($scope.get('ArgumentError'), "unnumbered(" + (seq_arg_num) + ") mixed with numbered")
        case -2: self.$raise($scope.get('ArgumentError'), "unnumbered(" + (seq_arg_num) + ") mixed with named")
        }
        pos_arg_num = seq_arg_num++;
        return GET_NTH_ARG(pos_arg_num - 1);
      }

      function GET_POS_ARG(num) {
        if (pos_arg_num > 0) {
          self.$raise($scope.get('ArgumentError'), "numbered(" + (num) + ") after unnumbered(" + (pos_arg_num) + ")")
        }
        if (pos_arg_num === -2) {
          self.$raise($scope.get('ArgumentError'), "numbered(" + (num) + ") after named")
        }
        if (num < 1) {
          self.$raise($scope.get('ArgumentError'), "invalid index - " + (num) + "$")
        }
        pos_arg_num = -1;
        return GET_NTH_ARG(num - 1);
      }

      function GET_ARG() {
        return (next_arg === undefined ? GET_NEXT_ARG() : next_arg);
      }

      function READ_NUM(label) {
        var num, str = '';
        for (;; i++) {
          if (i === len) {
            self.$raise($scope.get('ArgumentError'), "malformed format string - %*[0-9]")
          }
          if (format_string.charCodeAt(i) < 48 || format_string.charCodeAt(i) > 57) {
            i--;
            num = parseInt(str, 10) || 0;
            if (num > 2147483647) {
              self.$raise($scope.get('ArgumentError'), "" + (label) + " too big")
            }
            return num;
          }
          str += format_string.charAt(i);
        }
      }

      function READ_NUM_AFTER_ASTER(label) {
        var arg, num = READ_NUM(label);
        if (format_string.charAt(i + 1) === '$') {
          i++;
          arg = GET_POS_ARG(num);
        } else {
          arg = GET_NEXT_ARG();
        }
        return (arg).$to_int();
      }

      for (i = format_string.indexOf('%'); i !== -1; i = format_string.indexOf('%', i)) {
        str = undefined;

        flags = FNONE;
        width = -1;
        precision = -1;
        next_arg = undefined;

        end_slice = i;

        i++;

        switch (format_string.charAt(i)) {
        case '%':
          begin_slice = i;
        case '':
        case '\n':
        case '\0':
          i++;
          continue;
        }

        format_sequence: for (; i < len; i++) {
          switch (format_string.charAt(i)) {

          case ' ':
            CHECK_FOR_FLAGS();
            flags |= FSPACE;
            continue format_sequence;

          case '#':
            CHECK_FOR_FLAGS();
            flags |= FSHARP;
            continue format_sequence;

          case '+':
            CHECK_FOR_FLAGS();
            flags |= FPLUS;
            continue format_sequence;

          case '-':
            CHECK_FOR_FLAGS();
            flags |= FMINUS;
            continue format_sequence;

          case '0':
            CHECK_FOR_FLAGS();
            flags |= FZERO;
            continue format_sequence;

          case '1':
          case '2':
          case '3':
          case '4':
          case '5':
          case '6':
          case '7':
          case '8':
          case '9':
            tmp_num = READ_NUM('width');
            if (format_string.charAt(i + 1) === '$') {
              if (i + 2 === len) {
                str = '%';
                i++;
                break format_sequence;
              }
              if (next_arg !== undefined) {
                self.$raise($scope.get('ArgumentError'), "value given twice - %" + (tmp_num) + "$")
              }
              next_arg = GET_POS_ARG(tmp_num);
              i++;
            } else {
              CHECK_FOR_WIDTH();
              flags |= FWIDTH;
              width = tmp_num;
            }
            continue format_sequence;

          case '<':
          case '\{':
            closing_brace_char = (format_string.charAt(i) === '<' ? '>' : '\}');
            hash_parameter_key = '';

            i++;

            for (;; i++) {
              if (i === len) {
                self.$raise($scope.get('ArgumentError'), "malformed name - unmatched parenthesis")
              }
              if (format_string.charAt(i) === closing_brace_char) {

                if (pos_arg_num > 0) {
                  self.$raise($scope.get('ArgumentError'), "named " + (hash_parameter_key) + " after unnumbered(" + (pos_arg_num) + ")")
                }
                if (pos_arg_num === -1) {
                  self.$raise($scope.get('ArgumentError'), "named " + (hash_parameter_key) + " after numbered")
                }
                pos_arg_num = -2;

                if (args[0] === undefined || !args[0].$$is_hash) {
                  self.$raise($scope.get('ArgumentError'), "one hash required")
                }

                next_arg = (args[0]).$fetch(hash_parameter_key);

                if (closing_brace_char === '>') {
                  continue format_sequence;
                } else {
                  str = next_arg.toString();
                  if (precision !== -1) { str = str.slice(0, precision); }
                  if (flags&FMINUS) {
                    while (str.length < width) { str = str + ' '; }
                  } else {
                    while (str.length < width) { str = ' ' + str; }
                  }
                  break format_sequence;
                }
              }
              hash_parameter_key += format_string.charAt(i);
            }

          case '*':
            i++;
            CHECK_FOR_WIDTH();
            flags |= FWIDTH;
            width = READ_NUM_AFTER_ASTER('width');
            if (width < 0) {
              flags |= FMINUS;
              width = -width;
            }
            continue format_sequence;

          case '.':
            if (flags&FPREC0) {
              self.$raise($scope.get('ArgumentError'), "precision given twice")
            }
            flags |= FPREC|FPREC0;
            precision = 0;
            i++;
            if (format_string.charAt(i) === '*') {
              i++;
              precision = READ_NUM_AFTER_ASTER('precision');
              if (precision < 0) {
                flags &= ~FPREC;
              }
              continue format_sequence;
            }
            precision = READ_NUM('precision');
            continue format_sequence;

          case 'd':
          case 'i':
          case 'u':
            arg = self.$Integer(GET_ARG());
            if (arg >= 0) {
              str = arg.toString();
              while (str.length < precision) { str = '0' + str; }
              if (flags&FMINUS) {
                if (flags&FPLUS || flags&FSPACE) { str = (flags&FPLUS ? '+' : ' ') + str; }
                while (str.length < width) { str = str + ' '; }
              } else {
                if (flags&FZERO && precision === -1) {
                  while (str.length < width - ((flags&FPLUS || flags&FSPACE) ? 1 : 0)) { str = '0' + str; }
                  if (flags&FPLUS || flags&FSPACE) { str = (flags&FPLUS ? '+' : ' ') + str; }
                } else {
                  if (flags&FPLUS || flags&FSPACE) { str = (flags&FPLUS ? '+' : ' ') + str; }
                  while (str.length < width) { str = ' ' + str; }
                }
              }
            } else {
              str = (-arg).toString();
              while (str.length < precision) { str = '0' + str; }
              if (flags&FMINUS) {
                str = '-' + str;
                while (str.length < width) { str = str + ' '; }
              } else {
                if (flags&FZERO && precision === -1) {
                  while (str.length < width - 1) { str = '0' + str; }
                  str = '-' + str;
                } else {
                  str = '-' + str;
                  while (str.length < width) { str = ' ' + str; }
                }
              }
            }
            break format_sequence;

          case 'b':
          case 'B':
          case 'o':
          case 'x':
          case 'X':
            switch (format_string.charAt(i)) {
            case 'b':
            case 'B':
              base_number = 2;
              base_prefix = '0b';
              base_neg_zero_regex = /^1+/;
              base_neg_zero_digit = '1';
              break;
            case 'o':
              base_number = 8;
              base_prefix = '0';
              base_neg_zero_regex = /^3?7+/;
              base_neg_zero_digit = '7';
              break;
            case 'x':
            case 'X':
              base_number = 16;
              base_prefix = '0x';
              base_neg_zero_regex = /^f+/;
              base_neg_zero_digit = 'f';
              break;
            }
            arg = self.$Integer(GET_ARG());
            if (arg >= 0) {
              str = arg.toString(base_number);
              while (str.length < precision) { str = '0' + str; }
              if (flags&FMINUS) {
                if (flags&FPLUS || flags&FSPACE) { str = (flags&FPLUS ? '+' : ' ') + str; }
                if (flags&FSHARP && arg !== 0) { str = base_prefix + str; }
                while (str.length < width) { str = str + ' '; }
              } else {
                if (flags&FZERO && precision === -1) {
                  while (str.length < width - ((flags&FPLUS || flags&FSPACE) ? 1 : 0) - ((flags&FSHARP && arg !== 0) ? base_prefix.length : 0)) { str = '0' + str; }
                  if (flags&FSHARP && arg !== 0) { str = base_prefix + str; }
                  if (flags&FPLUS || flags&FSPACE) { str = (flags&FPLUS ? '+' : ' ') + str; }
                } else {
                  if (flags&FSHARP && arg !== 0) { str = base_prefix + str; }
                  if (flags&FPLUS || flags&FSPACE) { str = (flags&FPLUS ? '+' : ' ') + str; }
                  while (str.length < width) { str = ' ' + str; }
                }
              }
            } else {
              if (flags&FPLUS || flags&FSPACE) {
                str = (-arg).toString(base_number);
                while (str.length < precision) { str = '0' + str; }
                if (flags&FMINUS) {
                  if (flags&FSHARP) { str = base_prefix + str; }
                  str = '-' + str;
                  while (str.length < width) { str = str + ' '; }
                } else {
                  if (flags&FZERO && precision === -1) {
                    while (str.length < width - 1 - (flags&FSHARP ? 2 : 0)) { str = '0' + str; }
                    if (flags&FSHARP) { str = base_prefix + str; }
                    str = '-' + str;
                  } else {
                    if (flags&FSHARP) { str = base_prefix + str; }
                    str = '-' + str;
                    while (str.length < width) { str = ' ' + str; }
                  }
                }
              } else {
                str = (arg >>> 0).toString(base_number).replace(base_neg_zero_regex, base_neg_zero_digit);
                while (str.length < precision - 2) { str = base_neg_zero_digit + str; }
                if (flags&FMINUS) {
                  str = '..' + str;
                  if (flags&FSHARP) { str = base_prefix + str; }
                  while (str.length < width) { str = str + ' '; }
                } else {
                  if (flags&FZERO && precision === -1) {
                    while (str.length < width - 2 - (flags&FSHARP ? base_prefix.length : 0)) { str = base_neg_zero_digit + str; }
                    str = '..' + str;
                    if (flags&FSHARP) { str = base_prefix + str; }
                  } else {
                    str = '..' + str;
                    if (flags&FSHARP) { str = base_prefix + str; }
                    while (str.length < width) { str = ' ' + str; }
                  }
                }
              }
            }
            if (format_string.charAt(i) === format_string.charAt(i).toUpperCase()) {
              str = str.toUpperCase();
            }
            break format_sequence;

          case 'f':
          case 'e':
          case 'E':
          case 'g':
          case 'G':
            arg = self.$Float(GET_ARG());
            if (arg >= 0 || isNaN(arg)) {
              if (arg === Infinity) {
                str = 'Inf';
              } else {
                switch (format_string.charAt(i)) {
                case 'f':
                  str = arg.toFixed(precision === -1 ? 6 : precision);
                  break;
                case 'e':
                case 'E':
                  str = arg.toExponential(precision === -1 ? 6 : precision);
                  break;
                case 'g':
                case 'G':
                  str = arg.toExponential();
                  exponent = parseInt(str.split('e')[1], 10);
                  if (!(exponent < -4 || exponent >= (precision === -1 ? 6 : precision))) {
                    str = arg.toPrecision(precision === -1 ? (flags&FSHARP ? 6 : undefined) : precision);
                  }
                  break;
                }
              }
              if (flags&FMINUS) {
                if (flags&FPLUS || flags&FSPACE) { str = (flags&FPLUS ? '+' : ' ') + str; }
                while (str.length < width) { str = str + ' '; }
              } else {
                if (flags&FZERO && arg !== Infinity && !isNaN(arg)) {
                  while (str.length < width - ((flags&FPLUS || flags&FSPACE) ? 1 : 0)) { str = '0' + str; }
                  if (flags&FPLUS || flags&FSPACE) { str = (flags&FPLUS ? '+' : ' ') + str; }
                } else {
                  if (flags&FPLUS || flags&FSPACE) { str = (flags&FPLUS ? '+' : ' ') + str; }
                  while (str.length < width) { str = ' ' + str; }
                }
              }
            } else {
              if (arg === -Infinity) {
                str = 'Inf';
              } else {
                switch (format_string.charAt(i)) {
                case 'f':
                  str = (-arg).toFixed(precision === -1 ? 6 : precision);
                  break;
                case 'e':
                case 'E':
                  str = (-arg).toExponential(precision === -1 ? 6 : precision);
                  break;
                case 'g':
                case 'G':
                  str = (-arg).toExponential();
                  exponent = parseInt(str.split('e')[1], 10);
                  if (!(exponent < -4 || exponent >= (precision === -1 ? 6 : precision))) {
                    str = (-arg).toPrecision(precision === -1 ? (flags&FSHARP ? 6 : undefined) : precision);
                  }
                  break;
                }
              }
              if (flags&FMINUS) {
                str = '-' + str;
                while (str.length < width) { str = str + ' '; }
              } else {
                if (flags&FZERO && arg !== -Infinity) {
                  while (str.length < width - 1) { str = '0' + str; }
                  str = '-' + str;
                } else {
                  str = '-' + str;
                  while (str.length < width) { str = ' ' + str; }
                }
              }
            }
            if (format_string.charAt(i) === format_string.charAt(i).toUpperCase() && arg !== Infinity && arg !== -Infinity && !isNaN(arg)) {
              str = str.toUpperCase();
            }
            str = str.replace(/([eE][-+]?)([0-9])$/, '$10$2');
            break format_sequence;

          case 'a':
          case 'A':
            // Not implemented because there are no specs for this field type.
            self.$raise($scope.get('NotImplementedError'), "`A` and `a` format field types are not implemented in Opal yet")

          case 'c':
            arg = GET_ARG();
            if ((arg)['$respond_to?']("to_ary")) { arg = (arg).$to_ary()[0]; }
            if ((arg)['$respond_to?']("to_str")) {
              str = (arg).$to_str();
            } else {
              str = String.fromCharCode($scope.get('Opal').$coerce_to(arg, $scope.get('Integer'), "to_int"));
            }
            if (str.length !== 1) {
              self.$raise($scope.get('ArgumentError'), "%c requires a character")
            }
            if (flags&FMINUS) {
              while (str.length < width) { str = str + ' '; }
            } else {
              while (str.length < width) { str = ' ' + str; }
            }
            break format_sequence;

          case 'p':
            str = (GET_ARG()).$inspect();
            if (precision !== -1) { str = str.slice(0, precision); }
            if (flags&FMINUS) {
              while (str.length < width) { str = str + ' '; }
            } else {
              while (str.length < width) { str = ' ' + str; }
            }
            break format_sequence;

          case 's':
            str = (GET_ARG()).$to_s();
            if (precision !== -1) { str = str.slice(0, precision); }
            if (flags&FMINUS) {
              while (str.length < width) { str = str + ' '; }
            } else {
              while (str.length < width) { str = ' ' + str; }
            }
            break format_sequence;

          default:
            self.$raise($scope.get('ArgumentError'), "malformed format string - %" + (format_string.charAt(i)))
          }
        }

        if (str === undefined) {
          self.$raise($scope.get('ArgumentError'), "malformed format string - %")
        }

        result += format_string.slice(begin_slice, end_slice) + str;
        begin_slice = i + 1;
      }

      if ($gvars.DEBUG && pos_arg_num >= 0 && seq_arg_num < args.length) {
        self.$raise($scope.get('ArgumentError'), "too many arguments for format string")
      }

      return result + format_string.slice(begin_slice);
    ;
    });

    Opal.defn(self, '$hash', function() {
      var self = this;

      return self.$__id__();
    });

    Opal.defn(self, '$initialize_copy', function(other) {
      var self = this;

      return nil;
    });

    Opal.defn(self, '$inspect', function() {
      var self = this;

      return self.$to_s();
    });

    Opal.defn(self, '$instance_of?', function(klass) {
      var self = this;

      
      if (!klass.$$is_class && !klass.$$is_module) {
        self.$raise($scope.get('TypeError'), "class or module required");
      }

      return self.$$class === klass;
    ;
    });

    Opal.defn(self, '$instance_variable_defined?', function(name) {
      var self = this;

      name = $scope.get('Opal')['$instance_variable_name!'](name);
      return Opal.hasOwnProperty.call(self, name.substr(1));
    });

    Opal.defn(self, '$instance_variable_get', function(name) {
      var self = this;

      name = $scope.get('Opal')['$instance_variable_name!'](name);
      
      var ivar = self[Opal.ivar(name.substr(1))];

      return ivar == null ? nil : ivar;
    
    });

    Opal.defn(self, '$instance_variable_set', function(name, value) {
      var self = this;

      name = $scope.get('Opal')['$instance_variable_name!'](name);
      return self[Opal.ivar(name.substr(1))] = value;
    });

    Opal.defn(self, '$remove_instance_variable', function(name) {
      var self = this;

      name = $scope.get('Opal')['$instance_variable_name!'](name);
      
      var key = Opal.ivar(name.substr(1)),
          val;
      if (self.hasOwnProperty(key)) {
        val = self[key];
        delete self[key];
        return val;
      }
    
      return self.$raise($scope.get('NameError'), "instance variable " + (name) + " not defined");
    });

    Opal.defn(self, '$instance_variables', function() {
      var self = this;

      
      var result = [], ivar;

      for (var name in self) {
        if (self.hasOwnProperty(name) && name.charAt(0) !== '$') {
          if (name.substr(-1) === '$') {
            ivar = name.slice(0, name.length - 1);
          } else {
            ivar = name;
          }
          result.push('@' + ivar);
        }
      }

      return result;
    
    });

    Opal.defn(self, '$Integer', function(value, base) {
      var self = this;

      
      var i, str, base_digits;

      if (!value.$$is_string) {
        if (base !== undefined) {
          self.$raise($scope.get('ArgumentError'), "base specified for non string value")
        }
        if (value === nil) {
          self.$raise($scope.get('TypeError'), "can't convert nil into Integer")
        }
        if (value.$$is_number) {
          if (value === Infinity || value === -Infinity || isNaN(value)) {
            self.$raise($scope.get('FloatDomainError'), value)
          }
          return Math.floor(value);
        }
        if (value['$respond_to?']("to_int")) {
          i = value.$to_int();
          if (i !== nil) {
            return i;
          }
        }
        return $scope.get('Opal')['$coerce_to!'](value, $scope.get('Integer'), "to_i");
      }

      if (base === undefined) {
        base = 0;
      } else {
        base = $scope.get('Opal').$coerce_to(base, $scope.get('Integer'), "to_int");
        if (base === 1 || base < 0 || base > 36) {
          self.$raise($scope.get('ArgumentError'), "invalid radix " + (base))
        }
      }

      str = value.toLowerCase();

      str = str.replace(/(\d)_(?=\d)/g, '$1');

      str = str.replace(/^(\s*[+-]?)(0[bodx]?)/, function (_, head, flag) {
        switch (flag) {
        case '0b':
          if (base === 0 || base === 2) {
            base = 2;
            return head;
          }
        case '0':
        case '0o':
          if (base === 0 || base === 8) {
            base = 8;
            return head;
          }
        case '0d':
          if (base === 0 || base === 10) {
            base = 10;
            return head;
          }
        case '0x':
          if (base === 0 || base === 16) {
            base = 16;
            return head;
          }
        }
        self.$raise($scope.get('ArgumentError'), "invalid value for Integer(): \"" + (value) + "\"")
      });

      base = (base === 0 ? 10 : base);

      base_digits = '0-' + (base <= 10 ? base - 1 : '9a-' + String.fromCharCode(97 + (base - 11)));

      if (!(new RegExp('^\\s*[+-]?[' + base_digits + ']+\\s*$')).test(str)) {
        self.$raise($scope.get('ArgumentError'), "invalid value for Integer(): \"" + (value) + "\"")
      }

      i = parseInt(str, base);

      if (isNaN(i)) {
        self.$raise($scope.get('ArgumentError'), "invalid value for Integer(): \"" + (value) + "\"")
      }

      return i;
    ;
    });

    Opal.defn(self, '$Float', function(value) {
      var self = this;

      
      var str;

      if (value === nil) {
        self.$raise($scope.get('TypeError'), "can't convert nil into Float")
      }

      if (value.$$is_string) {
        str = value.toString();

        str = str.replace(/(\d)_(?=\d)/g, '$1');

        //Special case for hex strings only:
        if (/^\s*[-+]?0[xX][0-9a-fA-F]+\s*$/.test(str)) {
          return self.$Integer(str);
        }

        if (!/^\s*[-+]?[0-9]*\.?[0-9]+([eE][-+]?[0-9]+)?\s*$/.test(str)) {
          self.$raise($scope.get('ArgumentError'), "invalid value for Float(): \"" + (value) + "\"")
        }

        return parseFloat(str);
      }

      return $scope.get('Opal')['$coerce_to!'](value, $scope.get('Float'), "to_f");
    
    });

    Opal.defn(self, '$Hash', function(arg) {
      var $a, $b, self = this;

      if ((($a = ((($b = arg['$nil?']()) !== false && $b !== nil) ? $b : arg['$==']([]))) !== nil && (!$a.$$is_boolean || $a == true))) {
        return $hash2([], {})};
      if ((($a = $scope.get('Hash')['$==='](arg)) !== nil && (!$a.$$is_boolean || $a == true))) {
        return arg};
      return $scope.get('Opal')['$coerce_to!'](arg, $scope.get('Hash'), "to_hash");
    });

    Opal.defn(self, '$is_a?', function(klass) {
      var self = this;

      
      if (!klass.$$is_class && !klass.$$is_module) {
        self.$raise($scope.get('TypeError'), "class or module required");
      }

      return Opal.is_a(self, klass);
    ;
    });

    Opal.alias(self, 'kind_of?', 'is_a?');

    Opal.defn(self, '$lambda', TMP_6 = function() {
      var self = this, $iter = TMP_6.$$p, block = $iter || nil;

      TMP_6.$$p = null;
      block.$$is_lambda = true;
      return block;
    });

    Opal.defn(self, '$load', function(file) {
      var self = this;

      file = $scope.get('Opal')['$coerce_to!'](file, $scope.get('String'), "to_str");
      return Opal.load(file);
    });

    Opal.defn(self, '$loop', TMP_7 = function() {
      var self = this, $iter = TMP_7.$$p, block = $iter || nil;

      TMP_7.$$p = null;
      
      while (true) {
        if (block() === $breaker) {
          return $breaker.$v;
        }
      }
    
      return self;
    });

    Opal.defn(self, '$nil?', function() {
      var self = this;

      return false;
    });

    Opal.alias(self, 'object_id', '__id__');

    Opal.defn(self, '$printf', function() {
      var $a, self = this, $splat_index = nil;

      var array_size = arguments.length - 0;
      if(array_size < 0) array_size = 0;
      var args = new Array(array_size);
      for($splat_index = 0; $splat_index < array_size; $splat_index++) {
        args[$splat_index] = arguments[$splat_index + 0];
      }
      if ($rb_gt(args.$length(), 0)) {
        self.$print(($a = self).$format.apply($a, Opal.to_a(args)))};
      return nil;
    });

    Opal.defn(self, '$proc', TMP_8 = function() {
      var self = this, $iter = TMP_8.$$p, block = $iter || nil;

      TMP_8.$$p = null;
      if (block !== false && block !== nil) {
        } else {
        self.$raise($scope.get('ArgumentError'), "tried to create Proc object without a block")
      };
      block.$$is_lambda = false;
      return block;
    });

    Opal.defn(self, '$puts', function() {
      var $a, self = this, $splat_index = nil;
      if ($gvars.stdout == null) $gvars.stdout = nil;

      var array_size = arguments.length - 0;
      if(array_size < 0) array_size = 0;
      var strs = new Array(array_size);
      for($splat_index = 0; $splat_index < array_size; $splat_index++) {
        strs[$splat_index] = arguments[$splat_index + 0];
      }
      return ($a = $gvars.stdout).$puts.apply($a, Opal.to_a(strs));
    });

    Opal.defn(self, '$p', function() {
      var $a, $b, TMP_9, self = this, $splat_index = nil;

      var array_size = arguments.length - 0;
      if(array_size < 0) array_size = 0;
      var args = new Array(array_size);
      for($splat_index = 0; $splat_index < array_size; $splat_index++) {
        args[$splat_index] = arguments[$splat_index + 0];
      }
      ($a = ($b = args).$each, $a.$$p = (TMP_9 = function(obj){var self = TMP_9.$$s || this;
        if ($gvars.stdout == null) $gvars.stdout = nil;
if (obj == null) obj = nil;
      return $gvars.stdout.$puts(obj.$inspect())}, TMP_9.$$s = self, TMP_9), $a).call($b);
      if ($rb_le(args.$length(), 1)) {
        return args['$[]'](0)
        } else {
        return args
      };
    });

    Opal.defn(self, '$print', function() {
      var $a, self = this, $splat_index = nil;
      if ($gvars.stdout == null) $gvars.stdout = nil;

      var array_size = arguments.length - 0;
      if(array_size < 0) array_size = 0;
      var strs = new Array(array_size);
      for($splat_index = 0; $splat_index < array_size; $splat_index++) {
        strs[$splat_index] = arguments[$splat_index + 0];
      }
      return ($a = $gvars.stdout).$print.apply($a, Opal.to_a(strs));
    });

    Opal.defn(self, '$warn', function() {
      var $a, $b, self = this, $splat_index = nil;
      if ($gvars.VERBOSE == null) $gvars.VERBOSE = nil;
      if ($gvars.stderr == null) $gvars.stderr = nil;

      var array_size = arguments.length - 0;
      if(array_size < 0) array_size = 0;
      var strs = new Array(array_size);
      for($splat_index = 0; $splat_index < array_size; $splat_index++) {
        strs[$splat_index] = arguments[$splat_index + 0];
      }
      if ((($a = ((($b = $gvars.VERBOSE['$nil?']()) !== false && $b !== nil) ? $b : strs['$empty?']())) !== nil && (!$a.$$is_boolean || $a == true))) {
        return nil
        } else {
        return ($a = $gvars.stderr).$puts.apply($a, Opal.to_a(strs))
      };
    });

    Opal.defn(self, '$raise', function(exception, string, _backtrace) {
      var self = this;
      if ($gvars["!"] == null) $gvars["!"] = nil;

      if (string == null) {
        string = nil
      }
      if (_backtrace == null) {
        _backtrace = nil
      }
      
      if (exception == null && $gvars["!"] !== nil) {
        throw $gvars["!"];
      }
      if (exception == null) {
        exception = $scope.get('RuntimeError').$new();
      }
      else if (exception.$$is_string) {
        exception = $scope.get('RuntimeError').$new(exception);
      }
      // using respond_to? and not an undefined check to avoid method_missing matching as true
      else if (exception.$$is_class && exception['$respond_to?']("exception")) {
        exception = exception.$exception(string);
      }
      else if (exception['$kind_of?']($scope.get('Exception'))) {
        // exception is fine
      }
      else {
        exception = $scope.get('TypeError').$new("exception class/object expected");
      }

      if ($gvars["!"] !== nil) {
        Opal.exceptions.push($gvars["!"]);
      }

      $gvars["!"] = exception;

      throw exception;
    ;
    });

    Opal.alias(self, 'fail', 'raise');

    Opal.defn(self, '$rand', function(max) {
      var self = this;

      
      if (max === undefined) {
        return Math.random();
      }
      else if (max.$$is_range) {
        var min = max.begin, range = max.end - min;
        if(!max.exclude) range++;

        return self.$rand(range) + min;
      }
      else {
        return Math.floor(Math.random() *
          Math.abs($scope.get('Opal').$coerce_to(max, $scope.get('Integer'), "to_int")));
      }
    
    });

    Opal.defn(self, '$respond_to?', function(name, include_all) {
      var $a, self = this;

      if (include_all == null) {
        include_all = false
      }
      if ((($a = self['$respond_to_missing?'](name, include_all)) !== nil && (!$a.$$is_boolean || $a == true))) {
        return true};
      
      var body = self['$' + name];

      if (typeof(body) === "function" && !body.$$stub) {
        return true;
      }
    
      return false;
    });

    Opal.defn(self, '$respond_to_missing?', function(method_name, include_all) {
      var self = this;

      if (include_all == null) {
        include_all = false
      }
      return false;
    });

    Opal.defn(self, '$require', function(file) {
      var self = this;

      file = $scope.get('Opal')['$coerce_to!'](file, $scope.get('String'), "to_str");
      return Opal.require(file);
    });

    Opal.defn(self, '$require_relative', function(file) {
      var self = this;

      $scope.get('Opal')['$try_convert!'](file, $scope.get('String'), "to_str");
      file = $scope.get('File').$expand_path($scope.get('File').$join(Opal.current_file, "..", file));
      return Opal.require(file);
    });

    Opal.defn(self, '$require_tree', function(path) {
      var self = this;

      path = $scope.get('File').$expand_path(path);
      if (path['$=='](".")) {
        path = ""};
      
      for (var name in Opal.modules) {
        if ((name)['$start_with?'](path)) {
          Opal.require(name);
        }
      }
    ;
      return nil;
    });

    Opal.alias(self, 'send', '__send__');

    Opal.alias(self, 'public_send', '__send__');

    Opal.defn(self, '$singleton_class', function() {
      var self = this;

      return Opal.get_singleton_class(self);
    });

    Opal.defn(self, '$sleep', function(seconds) {
      var self = this;

      if (seconds == null) {
        seconds = nil
      }
      
      if (seconds === nil) {
        self.$raise($scope.get('TypeError'), "can't convert NilClass into time interval")
      }
      if (!seconds.$$is_number) {
        self.$raise($scope.get('TypeError'), "can't convert " + (seconds.$class()) + " into time interval")
      }
      if (seconds < 0) {
        self.$raise($scope.get('ArgumentError'), "time interval must be positive")
      }
      var t = new Date();
      while (new Date() - t <= seconds * 1000);
      return seconds;
    ;
    });

    Opal.alias(self, 'sprintf', 'format');

    Opal.alias(self, 'srand', 'rand');

    Opal.defn(self, '$String', function(str) {
      var $a, self = this;

      return ((($a = $scope.get('Opal')['$coerce_to?'](str, $scope.get('String'), "to_str")) !== false && $a !== nil) ? $a : $scope.get('Opal')['$coerce_to!'](str, $scope.get('String'), "to_s"));
    });

    Opal.defn(self, '$tap', TMP_10 = function() {
      var self = this, $iter = TMP_10.$$p, block = $iter || nil;

      TMP_10.$$p = null;
      if (Opal.yield1(block, self) === $breaker) return $breaker.$v;
      return self;
    });

    Opal.defn(self, '$to_proc', function() {
      var self = this;

      return self;
    });

    Opal.defn(self, '$to_s', function() {
      var self = this;

      return "#<" + (self.$class()) + ":0x" + (self.$__id__().$to_s(16)) + ">";
    });

    Opal.defn(self, '$catch', TMP_11 = function(sym) {
      var $a, self = this, $iter = TMP_11.$$p, $yield = $iter || nil, e = nil;

      TMP_11.$$p = null;
      try {
      return $a = Opal.yieldX($yield, []), $a === $breaker ? $a : $a
      } catch ($err) {if (Opal.rescue($err, [$scope.get('UncaughtThrowError')])) {e = $err;
        try {
          if (e.$sym()['$=='](sym)) {
            return e.$arg()};
          return self.$raise();
        } finally {
          Opal.gvars["!"] = Opal.exceptions.pop() || Opal.nil;
        }
        }else { throw $err; }
      };
    });

    Opal.defn(self, '$throw', function() {
      var self = this, $splat_index = nil;

      var array_size = arguments.length - 0;
      if(array_size < 0) array_size = 0;
      var args = new Array(array_size);
      for($splat_index = 0; $splat_index < array_size; $splat_index++) {
        args[$splat_index] = arguments[$splat_index + 0];
      }
      return self.$raise($scope.get('UncaughtThrowError').$new(args));
    });
  })($scope.base);
  return (function($base, $super) {
    function $Object(){};
    var self = $Object = $klass($base, $super, 'Object', $Object);

    var def = self.$$proto, $scope = self.$$scope;

    return self.$include($scope.get('Kernel'))
  })($scope.base, null);
};

/* Generated by Opal 0.9.0 */
Opal.modules["corelib/error"] = function(Opal) {
  Opal.dynamic_require_severity = "error";
  var OPAL_CONFIG = { method_missing: true, arity_check: false, freezing: true, tainting: true };
  function $rb_gt(lhs, rhs) {
    return (typeof(lhs) === 'number' && typeof(rhs) === 'number') ? lhs > rhs : lhs['$>'](rhs);
  }
  var self = Opal.top, $scope = Opal, nil = Opal.nil, $breaker = Opal.breaker, $slice = Opal.slice, $klass = Opal.klass, $module = Opal.module;

  Opal.add_stubs(['$new', '$clone', '$to_s', '$empty?', '$class', '$attr_reader', '$[]', '$>', '$length', '$inspect']);
  (function($base, $super) {
    function $Exception(){};
    var self = $Exception = $klass($base, $super, 'Exception', $Exception);

    var def = self.$$proto, $scope = self.$$scope;

    def.message = nil;
    Opal.defs(self, '$new', function() {
      var self = this, $splat_index = nil;

      var array_size = arguments.length - 0;
      if(array_size < 0) array_size = 0;
      var args = new Array(array_size);
      for($splat_index = 0; $splat_index < array_size; $splat_index++) {
        args[$splat_index] = arguments[$splat_index + 0];
      }
      
      var message = (args.length > 0) ? args[0] : nil;
      var err = new self.$$alloc(message);

      if (Error.captureStackTrace) {
        Error.captureStackTrace(err);
      }

      err.name = self.$$name;
      err.$initialize.apply(err, args);
      return err;
    
    });

    Opal.defs(self, '$exception', function() {
      var $a, self = this, $splat_index = nil;

      var array_size = arguments.length - 0;
      if(array_size < 0) array_size = 0;
      var args = new Array(array_size);
      for($splat_index = 0; $splat_index < array_size; $splat_index++) {
        args[$splat_index] = arguments[$splat_index + 0];
      }
      return ($a = self).$new.apply($a, Opal.to_a(args));
    });

    Opal.defn(self, '$initialize', function() {
      var self = this, $splat_index = nil;

      var array_size = arguments.length - 0;
      if(array_size < 0) array_size = 0;
      var args = new Array(array_size);
      for($splat_index = 0; $splat_index < array_size; $splat_index++) {
        args[$splat_index] = arguments[$splat_index + 0];
      }
      return self.message = (args.length > 0) ? args[0] : nil;
    });

    Opal.defn(self, '$backtrace', function() {
      var self = this;

      
      var backtrace = self.stack;

      if (typeof(backtrace) === 'string') {
        return backtrace.split("\n").slice(0, 15);
      }
      else if (backtrace) {
        return backtrace.slice(0, 15);
      }

      return [];
    
    });

    Opal.defn(self, '$exception', function(str) {
      var self = this;

      if (str == null) {
        str = nil
      }
      
      if (str === nil || self === str) {
        return self;
      }
      
      var cloned = self.$clone();
      cloned.message = str;
      return cloned;
    
    });

    Opal.defn(self, '$message', function() {
      var self = this;

      return self.$to_s();
    });

    Opal.defn(self, '$inspect', function() {
      var $a, self = this, as_str = nil;

      as_str = self.$to_s();
      if ((($a = as_str['$empty?']()) !== nil && (!$a.$$is_boolean || $a == true))) {
        return self.$class().$to_s()
        } else {
        return "#<" + (self.$class().$to_s()) + ": " + (self.$to_s()) + ">"
      };
    });

    return (Opal.defn(self, '$to_s', function() {
      var $a, $b, self = this;

      return ((($a = (($b = self.message, $b !== false && $b !== nil ?self.message.$to_s() : $b))) !== false && $a !== nil) ? $a : self.$class().$to_s());
    }), nil) && 'to_s';
  })($scope.base, Error);
  (function($base, $super) {
    function $ScriptError(){};
    var self = $ScriptError = $klass($base, $super, 'ScriptError', $ScriptError);

    var def = self.$$proto, $scope = self.$$scope;

    return nil;
  })($scope.base, $scope.get('Exception'));
  (function($base, $super) {
    function $SyntaxError(){};
    var self = $SyntaxError = $klass($base, $super, 'SyntaxError', $SyntaxError);

    var def = self.$$proto, $scope = self.$$scope;

    return nil;
  })($scope.base, $scope.get('ScriptError'));
  (function($base, $super) {
    function $LoadError(){};
    var self = $LoadError = $klass($base, $super, 'LoadError', $LoadError);

    var def = self.$$proto, $scope = self.$$scope;

    return nil;
  })($scope.base, $scope.get('ScriptError'));
  (function($base, $super) {
    function $NotImplementedError(){};
    var self = $NotImplementedError = $klass($base, $super, 'NotImplementedError', $NotImplementedError);

    var def = self.$$proto, $scope = self.$$scope;

    return nil;
  })($scope.base, $scope.get('ScriptError'));
  (function($base, $super) {
    function $SystemExit(){};
    var self = $SystemExit = $klass($base, $super, 'SystemExit', $SystemExit);

    var def = self.$$proto, $scope = self.$$scope;

    return nil;
  })($scope.base, $scope.get('Exception'));
  (function($base, $super) {
    function $NoMemoryError(){};
    var self = $NoMemoryError = $klass($base, $super, 'NoMemoryError', $NoMemoryError);

    var def = self.$$proto, $scope = self.$$scope;

    return nil;
  })($scope.base, $scope.get('Exception'));
  (function($base, $super) {
    function $SignalException(){};
    var self = $SignalException = $klass($base, $super, 'SignalException', $SignalException);

    var def = self.$$proto, $scope = self.$$scope;

    return nil;
  })($scope.base, $scope.get('Exception'));
  (function($base, $super) {
    function $Interrupt(){};
    var self = $Interrupt = $klass($base, $super, 'Interrupt', $Interrupt);

    var def = self.$$proto, $scope = self.$$scope;

    return nil;
  })($scope.base, $scope.get('Exception'));
  (function($base, $super) {
    function $SecurityError(){};
    var self = $SecurityError = $klass($base, $super, 'SecurityError', $SecurityError);

    var def = self.$$proto, $scope = self.$$scope;

    return nil;
  })($scope.base, $scope.get('Exception'));
  (function($base, $super) {
    function $StandardError(){};
    var self = $StandardError = $klass($base, $super, 'StandardError', $StandardError);

    var def = self.$$proto, $scope = self.$$scope;

    return nil;
  })($scope.base, $scope.get('Exception'));
  (function($base, $super) {
    function $ZeroDivisionError(){};
    var self = $ZeroDivisionError = $klass($base, $super, 'ZeroDivisionError', $ZeroDivisionError);

    var def = self.$$proto, $scope = self.$$scope;

    return nil;
  })($scope.base, $scope.get('StandardError'));
  (function($base, $super) {
    function $NameError(){};
    var self = $NameError = $klass($base, $super, 'NameError', $NameError);

    var def = self.$$proto, $scope = self.$$scope;

    return nil;
  })($scope.base, $scope.get('StandardError'));
  (function($base, $super) {
    function $NoMethodError(){};
    var self = $NoMethodError = $klass($base, $super, 'NoMethodError', $NoMethodError);

    var def = self.$$proto, $scope = self.$$scope;

    return nil;
  })($scope.base, $scope.get('NameError'));
  (function($base, $super) {
    function $RuntimeError(){};
    var self = $RuntimeError = $klass($base, $super, 'RuntimeError', $RuntimeError);

    var def = self.$$proto, $scope = self.$$scope;

    return nil;
  })($scope.base, $scope.get('StandardError'));
  (function($base, $super) {
    function $LocalJumpError(){};
    var self = $LocalJumpError = $klass($base, $super, 'LocalJumpError', $LocalJumpError);

    var def = self.$$proto, $scope = self.$$scope;

    return nil;
  })($scope.base, $scope.get('StandardError'));
  (function($base, $super) {
    function $TypeError(){};
    var self = $TypeError = $klass($base, $super, 'TypeError', $TypeError);

    var def = self.$$proto, $scope = self.$$scope;

    return nil;
  })($scope.base, $scope.get('StandardError'));
  (function($base, $super) {
    function $ArgumentError(){};
    var self = $ArgumentError = $klass($base, $super, 'ArgumentError', $ArgumentError);

    var def = self.$$proto, $scope = self.$$scope;

    return nil;
  })($scope.base, $scope.get('StandardError'));
  (function($base, $super) {
    function $IndexError(){};
    var self = $IndexError = $klass($base, $super, 'IndexError', $IndexError);

    var def = self.$$proto, $scope = self.$$scope;

    return nil;
  })($scope.base, $scope.get('StandardError'));
  (function($base, $super) {
    function $StopIteration(){};
    var self = $StopIteration = $klass($base, $super, 'StopIteration', $StopIteration);

    var def = self.$$proto, $scope = self.$$scope;

    return nil;
  })($scope.base, $scope.get('IndexError'));
  (function($base, $super) {
    function $KeyError(){};
    var self = $KeyError = $klass($base, $super, 'KeyError', $KeyError);

    var def = self.$$proto, $scope = self.$$scope;

    return nil;
  })($scope.base, $scope.get('IndexError'));
  (function($base, $super) {
    function $RangeError(){};
    var self = $RangeError = $klass($base, $super, 'RangeError', $RangeError);

    var def = self.$$proto, $scope = self.$$scope;

    return nil;
  })($scope.base, $scope.get('StandardError'));
  (function($base, $super) {
    function $FloatDomainError(){};
    var self = $FloatDomainError = $klass($base, $super, 'FloatDomainError', $FloatDomainError);

    var def = self.$$proto, $scope = self.$$scope;

    return nil;
  })($scope.base, $scope.get('RangeError'));
  (function($base, $super) {
    function $IOError(){};
    var self = $IOError = $klass($base, $super, 'IOError', $IOError);

    var def = self.$$proto, $scope = self.$$scope;

    return nil;
  })($scope.base, $scope.get('StandardError'));
  (function($base, $super) {
    function $SystemCallError(){};
    var self = $SystemCallError = $klass($base, $super, 'SystemCallError', $SystemCallError);

    var def = self.$$proto, $scope = self.$$scope;

    return nil;
  })($scope.base, $scope.get('StandardError'));
  (function($base) {
    var $Errno, self = $Errno = $module($base, 'Errno');

    var def = self.$$proto, $scope = self.$$scope;

    (function($base, $super) {
      function $EINVAL(){};
      var self = $EINVAL = $klass($base, $super, 'EINVAL', $EINVAL);

      var def = self.$$proto, $scope = self.$$scope, TMP_1;

      return (Opal.defs(self, '$new', TMP_1 = function() {
        var self = this, $iter = TMP_1.$$p, $yield = $iter || nil;

        TMP_1.$$p = null;
        return Opal.find_super_dispatcher(self, 'new', TMP_1, null, $EINVAL).apply(self, ["Invalid argument"]);
      }), nil) && 'new'
    })($scope.base, $scope.get('SystemCallError'))
  })($scope.base);
  (function($base, $super) {
    function $UncaughtThrowError(){};
    var self = $UncaughtThrowError = $klass($base, $super, 'UncaughtThrowError', $UncaughtThrowError);

    var def = self.$$proto, $scope = self.$$scope, TMP_2;

    def.sym = nil;
    self.$attr_reader("sym", "arg");

    return (Opal.defn(self, '$initialize', TMP_2 = function(args) {
      var self = this, $iter = TMP_2.$$p, $yield = $iter || nil;

      TMP_2.$$p = null;
      self.sym = args['$[]'](0);
      if ($rb_gt(args.$length(), 1)) {
        self.arg = args['$[]'](1)};
      return Opal.find_super_dispatcher(self, 'initialize', TMP_2, null).apply(self, ["uncaught throw " + (self.sym.$inspect())]);
    }), nil) && 'initialize';
  })($scope.base, $scope.get('ArgumentError'));
  (function($base, $super) {
    function $NameError(){};
    var self = $NameError = $klass($base, $super, 'NameError', $NameError);

    var def = self.$$proto, $scope = self.$$scope, TMP_3;

    self.$attr_reader("name");

    return (Opal.defn(self, '$initialize', TMP_3 = function(message, name) {
      var self = this, $iter = TMP_3.$$p, $yield = $iter || nil;

      if (name == null) {
        name = nil
      }
      TMP_3.$$p = null;
      Opal.find_super_dispatcher(self, 'initialize', TMP_3, null).apply(self, [message]);
      return self.name = name;
    }), nil) && 'initialize';
  })($scope.base, null);
  return (function($base, $super) {
    function $NoMethodError(){};
    var self = $NoMethodError = $klass($base, $super, 'NoMethodError', $NoMethodError);

    var def = self.$$proto, $scope = self.$$scope, TMP_4;

    self.$attr_reader("args");

    return (Opal.defn(self, '$initialize', TMP_4 = function(message, name, args) {
      var self = this, $iter = TMP_4.$$p, $yield = $iter || nil;

      if (args == null) {
        args = []
      }
      TMP_4.$$p = null;
      Opal.find_super_dispatcher(self, 'initialize', TMP_4, null).apply(self, [message, name]);
      return self.args = args;
    }), nil) && 'initialize';
  })($scope.base, null);
};

/* Generated by Opal 0.9.0 */
Opal.modules["corelib/constants"] = function(Opal) {
  Opal.dynamic_require_severity = "error";
  var OPAL_CONFIG = { method_missing: true, arity_check: false, freezing: true, tainting: true };
  var self = Opal.top, $scope = Opal, nil = Opal.nil, $breaker = Opal.breaker, $slice = Opal.slice;

  Opal.cdecl($scope, 'RUBY_PLATFORM', "opal");
  Opal.cdecl($scope, 'RUBY_ENGINE', "opal");
  Opal.cdecl($scope, 'RUBY_VERSION', "2.2.3");
  Opal.cdecl($scope, 'RUBY_ENGINE_VERSION', "0.9.0");
  Opal.cdecl($scope, 'RUBY_RELEASE_DATE', "2015-12-20");
  Opal.cdecl($scope, 'RUBY_PATCHLEVEL', 0);
  Opal.cdecl($scope, 'RUBY_REVISION', 0);
  Opal.cdecl($scope, 'RUBY_COPYRIGHT', "opal - Copyright (C) 2013-2015 Adam Beynon");
  return Opal.cdecl($scope, 'RUBY_DESCRIPTION', "opal " + ($scope.get('RUBY_ENGINE_VERSION')) + " (" + ($scope.get('RUBY_RELEASE_DATE')) + " revision " + ($scope.get('RUBY_REVISION')) + ")");
};

/* Generated by Opal 0.9.0 */
Opal.modules["opal/base"] = function(Opal) {
  Opal.dynamic_require_severity = "error";
  var OPAL_CONFIG = { method_missing: true, arity_check: false, freezing: true, tainting: true };
  var self = Opal.top, $scope = Opal, nil = Opal.nil, $breaker = Opal.breaker, $slice = Opal.slice;

  Opal.add_stubs(['$require']);
  self.$require("corelib/runtime");
  self.$require("corelib/helpers");
  self.$require("corelib/module");
  self.$require("corelib/class");
  self.$require("corelib/basic_object");
  self.$require("corelib/kernel");
  self.$require("corelib/error");
  return self.$require("corelib/constants");
};

/* Generated by Opal 0.9.0 */
Opal.modules["corelib/nil"] = function(Opal) {
  Opal.dynamic_require_severity = "error";
  var OPAL_CONFIG = { method_missing: true, arity_check: false, freezing: true, tainting: true };
  function $rb_gt(lhs, rhs) {
    return (typeof(lhs) === 'number' && typeof(rhs) === 'number') ? lhs > rhs : lhs['$>'](rhs);
  }
  var self = Opal.top, $scope = Opal, nil = Opal.nil, $breaker = Opal.breaker, $slice = Opal.slice, $klass = Opal.klass;

  Opal.add_stubs(['$raise', '$class', '$new', '$>', '$length', '$Rational']);
  (function($base, $super) {
    function $NilClass(){};
    var self = $NilClass = $klass($base, $super, 'NilClass', $NilClass);

    var def = self.$$proto, $scope = self.$$scope;

    def.$$meta = self;

    Opal.defn(self, '$!', function() {
      var self = this;

      return true;
    });

    Opal.defn(self, '$&', function(other) {
      var self = this;

      return false;
    });

    Opal.defn(self, '$|', function(other) {
      var self = this;

      return other !== false && other !== nil;
    });

    Opal.defn(self, '$^', function(other) {
      var self = this;

      return other !== false && other !== nil;
    });

    Opal.defn(self, '$==', function(other) {
      var self = this;

      return other === nil;
    });

    Opal.defn(self, '$dup', function() {
      var self = this;

      return self.$raise($scope.get('TypeError'), "can't dup " + (self.$class()));
    });

    Opal.defn(self, '$clone', function() {
      var self = this;

      return self.$raise($scope.get('TypeError'), "can't clone " + (self.$class()));
    });

    Opal.defn(self, '$inspect', function() {
      var self = this;

      return "nil";
    });

    Opal.defn(self, '$nil?', function() {
      var self = this;

      return true;
    });

    Opal.defn(self, '$singleton_class', function() {
      var self = this;

      return $scope.get('NilClass');
    });

    Opal.defn(self, '$to_a', function() {
      var self = this;

      return [];
    });

    Opal.defn(self, '$to_h', function() {
      var self = this;

      return Opal.hash();
    });

    Opal.defn(self, '$to_i', function() {
      var self = this;

      return 0;
    });

    Opal.alias(self, 'to_f', 'to_i');

    Opal.defn(self, '$to_s', function() {
      var self = this;

      return "";
    });

    Opal.defn(self, '$to_c', function() {
      var self = this;

      return $scope.get('Complex').$new(0, 0);
    });

    Opal.defn(self, '$rationalize', function() {
      var self = this, $splat_index = nil;

      var array_size = arguments.length - 0;
      if(array_size < 0) array_size = 0;
      var args = new Array(array_size);
      for($splat_index = 0; $splat_index < array_size; $splat_index++) {
        args[$splat_index] = arguments[$splat_index + 0];
      }
      if ($rb_gt(args.$length(), 1)) {
        self.$raise($scope.get('ArgumentError'))};
      return self.$Rational(0, 1);
    });

    Opal.defn(self, '$to_r', function() {
      var self = this;

      return self.$Rational(0, 1);
    });

    return (Opal.defn(self, '$instance_variables', function() {
      var self = this;

      return [];
    }), nil) && 'instance_variables';
  })($scope.base, null);
  return Opal.cdecl($scope, 'NIL', nil);
};

/* Generated by Opal 0.9.0 */
Opal.modules["corelib/boolean"] = function(Opal) {
  Opal.dynamic_require_severity = "error";
  var OPAL_CONFIG = { method_missing: true, arity_check: false, freezing: true, tainting: true };
  var self = Opal.top, $scope = Opal, nil = Opal.nil, $breaker = Opal.breaker, $slice = Opal.slice, $klass = Opal.klass;

  Opal.add_stubs(['$raise', '$class']);
  (function($base, $super) {
    function $Boolean(){};
    var self = $Boolean = $klass($base, $super, 'Boolean', $Boolean);

    var def = self.$$proto, $scope = self.$$scope;

    def.$$is_boolean = true;

    def.$$meta = self;

    Opal.defn(self, '$__id__', function() {
      var self = this;

      return self.valueOf() ? 2 : 0;
    });

    Opal.alias(self, 'object_id', '__id__');

    Opal.defn(self, '$!', function() {
      var self = this;

      return self != true;
    });

    Opal.defn(self, '$&', function(other) {
      var self = this;

      return (self == true) ? (other !== false && other !== nil) : false;
    });

    Opal.defn(self, '$|', function(other) {
      var self = this;

      return (self == true) ? true : (other !== false && other !== nil);
    });

    Opal.defn(self, '$^', function(other) {
      var self = this;

      return (self == true) ? (other === false || other === nil) : (other !== false && other !== nil);
    });

    Opal.defn(self, '$==', function(other) {
      var self = this;

      return (self == true) === other.valueOf();
    });

    Opal.alias(self, 'equal?', '==');

    Opal.alias(self, 'eql?', '==');

    Opal.defn(self, '$singleton_class', function() {
      var self = this;

      return $scope.get('Boolean');
    });

    Opal.defn(self, '$to_s', function() {
      var self = this;

      return (self == true) ? 'true' : 'false';
    });

    Opal.defn(self, '$dup', function() {
      var self = this;

      return self.$raise($scope.get('TypeError'), "can't dup " + (self.$class()));
    });

    return (Opal.defn(self, '$clone', function() {
      var self = this;

      return self.$raise($scope.get('TypeError'), "can't clone " + (self.$class()));
    }), nil) && 'clone';
  })($scope.base, Boolean);
  Opal.cdecl($scope, 'TrueClass', $scope.get('Boolean'));
  Opal.cdecl($scope, 'FalseClass', $scope.get('Boolean'));
  Opal.cdecl($scope, 'TRUE', true);
  return Opal.cdecl($scope, 'FALSE', false);
};

/* Generated by Opal 0.9.0 */
Opal.modules["corelib/comparable"] = function(Opal) {
  Opal.dynamic_require_severity = "error";
  var OPAL_CONFIG = { method_missing: true, arity_check: false, freezing: true, tainting: true };
  function $rb_gt(lhs, rhs) {
    return (typeof(lhs) === 'number' && typeof(rhs) === 'number') ? lhs > rhs : lhs['$>'](rhs);
  }
  function $rb_lt(lhs, rhs) {
    return (typeof(lhs) === 'number' && typeof(rhs) === 'number') ? lhs < rhs : lhs['$<'](rhs);
  }
  var self = Opal.top, $scope = Opal, nil = Opal.nil, $breaker = Opal.breaker, $slice = Opal.slice, $module = Opal.module;

  Opal.add_stubs(['$===', '$>', '$<', '$equal?', '$<=>', '$normalize', '$raise', '$class']);
  return (function($base) {
    var $Comparable, self = $Comparable = $module($base, 'Comparable');

    var def = self.$$proto, $scope = self.$$scope;

    Opal.defs(self, '$normalize', function(what) {
      var $a, self = this;

      if ((($a = $scope.get('Integer')['$==='](what)) !== nil && (!$a.$$is_boolean || $a == true))) {
        return what};
      if ($rb_gt(what, 0)) {
        return 1};
      if ($rb_lt(what, 0)) {
        return -1};
      return 0;
    });

    Opal.defn(self, '$==', function(other) {
      var $a, self = this, cmp = nil;

      try {
      if ((($a = self['$equal?'](other)) !== nil && (!$a.$$is_boolean || $a == true))) {
          return true};
        
      if (self["$<=>"] == Opal.Kernel["$<=>"]) {
        return false;
      }

      // check for infinite recursion
      if (self.$$comparable) {
        delete self.$$comparable;
        return false;
      }
    
        if ((($a = cmp = (self['$<=>'](other))) !== nil && (!$a.$$is_boolean || $a == true))) {
          } else {
          return false
        };
        return $scope.get('Comparable').$normalize(cmp) == 0;
      } catch ($err) {if (Opal.rescue($err, [$scope.get('StandardError')])) {
        try {
          return false
        } finally {
          Opal.gvars["!"] = Opal.exceptions.pop() || Opal.nil;
        }
        }else { throw $err; }
      };
    });

    Opal.defn(self, '$>', function(other) {
      var $a, self = this, cmp = nil;

      if ((($a = cmp = (self['$<=>'](other))) !== nil && (!$a.$$is_boolean || $a == true))) {
        } else {
        self.$raise($scope.get('ArgumentError'), "comparison of " + (self.$class()) + " with " + (other.$class()) + " failed")
      };
      return $scope.get('Comparable').$normalize(cmp) > 0;
    });

    Opal.defn(self, '$>=', function(other) {
      var $a, self = this, cmp = nil;

      if ((($a = cmp = (self['$<=>'](other))) !== nil && (!$a.$$is_boolean || $a == true))) {
        } else {
        self.$raise($scope.get('ArgumentError'), "comparison of " + (self.$class()) + " with " + (other.$class()) + " failed")
      };
      return $scope.get('Comparable').$normalize(cmp) >= 0;
    });

    Opal.defn(self, '$<', function(other) {
      var $a, self = this, cmp = nil;

      if ((($a = cmp = (self['$<=>'](other))) !== nil && (!$a.$$is_boolean || $a == true))) {
        } else {
        self.$raise($scope.get('ArgumentError'), "comparison of " + (self.$class()) + " with " + (other.$class()) + " failed")
      };
      return $scope.get('Comparable').$normalize(cmp) < 0;
    });

    Opal.defn(self, '$<=', function(other) {
      var $a, self = this, cmp = nil;

      if ((($a = cmp = (self['$<=>'](other))) !== nil && (!$a.$$is_boolean || $a == true))) {
        } else {
        self.$raise($scope.get('ArgumentError'), "comparison of " + (self.$class()) + " with " + (other.$class()) + " failed")
      };
      return $scope.get('Comparable').$normalize(cmp) <= 0;
    });

    Opal.defn(self, '$between?', function(min, max) {
      var self = this;

      if ($rb_lt(self, min)) {
        return false};
      if ($rb_gt(self, max)) {
        return false};
      return true;
    });
  })($scope.base)
};

/* Generated by Opal 0.9.0 */
Opal.modules["corelib/regexp"] = function(Opal) {
  Opal.dynamic_require_severity = "error";
  var OPAL_CONFIG = { method_missing: true, arity_check: false, freezing: true, tainting: true };
  var self = Opal.top, $scope = Opal, nil = Opal.nil, $breaker = Opal.breaker, $slice = Opal.slice, $klass = Opal.klass, $gvars = Opal.gvars;

  Opal.add_stubs(['$nil?', '$[]', '$raise', '$escape', '$options', '$to_str', '$new', '$join', '$coerce_to!', '$!', '$match', '$coerce_to?', '$begin', '$coerce_to', '$call', '$=~', '$attr_reader', '$===', '$inspect', '$to_a']);
  (function($base, $super) {
    function $RegexpError(){};
    var self = $RegexpError = $klass($base, $super, 'RegexpError', $RegexpError);

    var def = self.$$proto, $scope = self.$$scope;

    return nil;
  })($scope.base, $scope.get('StandardError'));
  (function($base, $super) {
    function $Regexp(){};
    var self = $Regexp = $klass($base, $super, 'Regexp', $Regexp);

    var def = self.$$proto, $scope = self.$$scope, TMP_2;

    Opal.cdecl($scope, 'IGNORECASE', 1);

    Opal.cdecl($scope, 'MULTILINE', 4);

    def.$$is_regexp = true;

    (function(self) {
      var $scope = self.$$scope, def = self.$$proto, TMP_1;

      Opal.defn(self, '$allocate', TMP_1 = function() {
        var self = this, $iter = TMP_1.$$p, $yield = $iter || nil, allocated = nil, $zuper = nil, $zuper_index = nil;

        TMP_1.$$p = null;
        $zuper = [];
        for($zuper_index = 0; $zuper_index < arguments.length; $zuper_index++) {
          $zuper[$zuper_index] = arguments[$zuper_index];
        }
        allocated = Opal.find_super_dispatcher(self, 'allocate', TMP_1, $iter).apply(self, $zuper);
        allocated.uninitialized = true;
        return allocated;
      });
      Opal.defn(self, '$escape', function(string) {
        var self = this;

        
        return string.replace(/([-[\]\/{}()*+?.^$\\| ])/g, '\\$1')
                     .replace(/[\n]/g, '\\n')
                     .replace(/[\r]/g, '\\r')
                     .replace(/[\f]/g, '\\f')
                     .replace(/[\t]/g, '\\t');
      
      });
      Opal.defn(self, '$last_match', function(n) {
        var $a, self = this;
        if ($gvars["~"] == null) $gvars["~"] = nil;

        if (n == null) {
          n = nil
        }
        if ((($a = n['$nil?']()) !== nil && (!$a.$$is_boolean || $a == true))) {
          return $gvars["~"]
          } else {
          return $gvars["~"]['$[]'](n)
        };
      });
      Opal.alias(self, 'quote', 'escape');
      Opal.defn(self, '$union', function() {
        var self = this, $splat_index = nil;

        var array_size = arguments.length - 0;
        if(array_size < 0) array_size = 0;
        var parts = new Array(array_size);
        for($splat_index = 0; $splat_index < array_size; $splat_index++) {
          parts[$splat_index] = arguments[$splat_index + 0];
        }
        
        var is_first_part_array, quoted_validated, part, options, each_part_options;
        if (parts.length == 0) {
          return /(?!)/;
        }
        // cover the 2 arrays passed as arguments case
        is_first_part_array = parts[0].$$is_array;
        if (parts.length > 1 && is_first_part_array) {
          self.$raise($scope.get('TypeError'), "no implicit conversion of Array into String")
        }        
        // deal with splat issues (related to https://github.com/opal/opal/issues/858)
        if (is_first_part_array) {
          parts = parts[0];
        }
        options = undefined;
        quoted_validated = [];
        for (var i=0; i < parts.length; i++) {
          part = parts[i];
          if (part.$$is_string) {
            quoted_validated.push(self.$escape(part));
          }
          else if (part.$$is_regexp) {
            each_part_options = (part).$options();
            if (options != undefined && options != each_part_options) {
              self.$raise($scope.get('TypeError'), "All expressions must use the same options")
            }
            options = each_part_options;
            quoted_validated.push('('+part.source+')');
          }
          else {
            quoted_validated.push(self.$escape((part).$to_str()));
          }
        }
      
        return self.$new((quoted_validated).$join("|"), options);
      });
      return (Opal.defn(self, '$new', function(regexp, options) {
        var self = this;

        
        if (regexp.$$is_regexp) {
          return new RegExp(regexp);
        }

        regexp = $scope.get('Opal')['$coerce_to!'](regexp, $scope.get('String'), "to_str");

        if (regexp.charAt(regexp.length - 1) === '\\') {
          self.$raise($scope.get('RegexpError'), "too short escape sequence: /" + (regexp) + "/")
        }

        if (options === undefined || options['$!']()) {
          return new RegExp(regexp);
        }

        if (options.$$is_number) {
          var temp = '';
          if ($scope.get('IGNORECASE') & options) { temp += 'i'; }
          if ($scope.get('MULTILINE')  & options) { temp += 'm'; }
          options = temp;
        }
        else {
          options = 'i';
        }

        return new RegExp(regexp, options);
      ;
      }), nil) && 'new';
    })(Opal.get_singleton_class(self));

    Opal.defn(self, '$==', function(other) {
      var self = this;

      return other.constructor == RegExp && self.toString() === other.toString();
    });

    Opal.defn(self, '$===', function(string) {
      var self = this;

      return self.$match($scope.get('Opal')['$coerce_to?'](string, $scope.get('String'), "to_str")) !== nil;
    });

    Opal.defn(self, '$=~', function(string) {
      var $a, self = this;
      if ($gvars["~"] == null) $gvars["~"] = nil;

      return ($a = self.$match(string), $a !== false && $a !== nil ?$gvars["~"].$begin(0) : $a);
    });

    Opal.alias(self, 'eql?', '==');

    Opal.defn(self, '$inspect', function() {
      var self = this;

      return self.toString();
    });

    Opal.defn(self, '$match', TMP_2 = function(string, pos) {
      var self = this, $iter = TMP_2.$$p, block = $iter || nil;
      if ($gvars["~"] == null) $gvars["~"] = nil;

      TMP_2.$$p = null;
      
      if (self.uninitialized) {
        self.$raise($scope.get('TypeError'), "uninitialized Regexp")
      }

      if (pos === undefined) {
        pos = 0;
      } else {
        pos = $scope.get('Opal').$coerce_to(pos, $scope.get('Integer'), "to_int");
      }

      if (string === nil) {
        return $gvars["~"] = nil;
      }

      string = $scope.get('Opal').$coerce_to(string, $scope.get('String'), "to_str");

      if (pos < 0) {
        pos += string.length;
        if (pos < 0) {
          return $gvars["~"] = nil;
        }
      }

      var source = self.source;
      var flags = 'g';
      // m flag + a . in Ruby will match white space, but in JS, it only matches beginning/ending of lines, so we get the equivalent here
      if (self.multiline) {
        source = source.replace('.', "[\\s\\S]");
        flags += 'm';
      }

      // global RegExp maintains state, so not using self/this
      var md, re = new RegExp(source, flags + (self.ignoreCase ? 'i' : ''));

      while (true) {
        md = re.exec(string);
        if (md === null) {
          return $gvars["~"] = nil;
        }
        if (md.index >= pos) {
          $gvars["~"] = $scope.get('MatchData').$new(re, md)
          return block === nil ? $gvars["~"] : block.$call($gvars["~"]);
        }
        re.lastIndex = md.index + 1;
      }
    ;
    });

    Opal.defn(self, '$~', function() {
      var self = this;
      if ($gvars._ == null) $gvars._ = nil;

      return self['$=~']($gvars._);
    });

    Opal.defn(self, '$source', function() {
      var self = this;

      return self.source;
    });

    Opal.defn(self, '$options', function() {
      var self = this;

      
      if (self.uninitialized) {
        self.$raise($scope.get('TypeError'), "uninitialized Regexp")
      }
      var result = 0;
      // should be supported in IE6 according to https://msdn.microsoft.com/en-us/library/7f5z26w4(v=vs.94).aspx
      if (self.multiline) {
        result |= $scope.get('MULTILINE');
      }
      if (self.ignoreCase) {
        result |= $scope.get('IGNORECASE');
      }
      return result;
    ;
    });

    Opal.defn(self, '$casefold?', function() {
      var self = this;

      return self.ignoreCase;
    });

    return Opal.alias(self, 'to_s', 'source');
  })($scope.base, RegExp);
  return (function($base, $super) {
    function $MatchData(){};
    var self = $MatchData = $klass($base, $super, 'MatchData', $MatchData);

    var def = self.$$proto, $scope = self.$$scope;

    def.matches = nil;
    self.$attr_reader("post_match", "pre_match", "regexp", "string");

    Opal.defn(self, '$initialize', function(regexp, match_groups) {
      var self = this;

      $gvars["~"] = self;
      self.regexp = regexp;
      self.begin = match_groups.index;
      self.string = match_groups.input;
      self.pre_match = match_groups.input.slice(0, match_groups.index);
      self.post_match = match_groups.input.slice(match_groups.index + match_groups[0].length);
      self.matches = [];
      
      for (var i = 0, length = match_groups.length; i < length; i++) {
        var group = match_groups[i];

        if (group == null) {
          self.matches.push(nil);
        }
        else {
          self.matches.push(group);
        }
      }
    
    });

    Opal.defn(self, '$[]', function() {
      var $a, self = this, $splat_index = nil;

      var array_size = arguments.length - 0;
      if(array_size < 0) array_size = 0;
      var args = new Array(array_size);
      for($splat_index = 0; $splat_index < array_size; $splat_index++) {
        args[$splat_index] = arguments[$splat_index + 0];
      }
      return ($a = self.matches)['$[]'].apply($a, Opal.to_a(args));
    });

    Opal.defn(self, '$offset', function(n) {
      var self = this;

      
      if (n !== 0) {
        self.$raise($scope.get('ArgumentError'), "MatchData#offset only supports 0th element")
      }
      return [self.begin, self.begin + self.matches[n].length];
    ;
    });

    Opal.defn(self, '$==', function(other) {
      var $a, $b, $c, $d, self = this;

      if ((($a = $scope.get('MatchData')['$==='](other)) !== nil && (!$a.$$is_boolean || $a == true))) {
        } else {
        return false
      };
      return ($a = ($b = ($c = ($d = self.string == other.string, $d !== false && $d !== nil ?self.regexp.toString() == other.regexp.toString() : $d), $c !== false && $c !== nil ?self.pre_match == other.pre_match : $c), $b !== false && $b !== nil ?self.post_match == other.post_match : $b), $a !== false && $a !== nil ?self.begin == other.begin : $a);
    });

    Opal.alias(self, 'eql?', '==');

    Opal.defn(self, '$begin', function(n) {
      var self = this;

      
      if (n !== 0) {
        self.$raise($scope.get('ArgumentError'), "MatchData#begin only supports 0th element")
      }
      return self.begin;
    ;
    });

    Opal.defn(self, '$end', function(n) {
      var self = this;

      
      if (n !== 0) {
        self.$raise($scope.get('ArgumentError'), "MatchData#end only supports 0th element")
      }
      return self.begin + self.matches[n].length;
    ;
    });

    Opal.defn(self, '$captures', function() {
      var self = this;

      return self.matches.slice(1);
    });

    Opal.defn(self, '$inspect', function() {
      var self = this;

      
      var str = "#<MatchData " + (self.matches[0]).$inspect();

      for (var i = 1, length = self.matches.length; i < length; i++) {
        str += " " + i + ":" + (self.matches[i]).$inspect();
      }

      return str + ">";
    ;
    });

    Opal.defn(self, '$length', function() {
      var self = this;

      return self.matches.length;
    });

    Opal.alias(self, 'size', 'length');

    Opal.defn(self, '$to_a', function() {
      var self = this;

      return self.matches;
    });

    Opal.defn(self, '$to_s', function() {
      var self = this;

      return self.matches[0];
    });

    return (Opal.defn(self, '$values_at', function() {
      var self = this, $splat_index = nil;

      var array_size = arguments.length - 0;
      if(array_size < 0) array_size = 0;
      var args = new Array(array_size);
      for($splat_index = 0; $splat_index < array_size; $splat_index++) {
        args[$splat_index] = arguments[$splat_index + 0];
      }
      
      var i, a, index, values = [];

      for (i = 0; i < args.length; i++) {

        if (args[i].$$is_range) {
          a = (args[i]).$to_a();
          a.unshift(i, 1);
          Array.prototype.splice.apply(args, a);
        }

        index = $scope.get('Opal')['$coerce_to!'](args[i], $scope.get('Integer'), "to_int");

        if (index < 0) {
          index += self.matches.length;
          if (index < 0) {
            values.push(nil);
            continue;
          }
        }

        values.push(self.matches[index]);
      }

      return values;
    
    }), nil) && 'values_at';
  })($scope.base, null);
};

/* Generated by Opal 0.9.0 */
Opal.modules["corelib/string"] = function(Opal) {
  Opal.dynamic_require_severity = "error";
  var OPAL_CONFIG = { method_missing: true, arity_check: false, freezing: true, tainting: true };
  function $rb_divide(lhs, rhs) {
    return (typeof(lhs) === 'number' && typeof(rhs) === 'number') ? lhs / rhs : lhs['$/'](rhs);
  }
  function $rb_plus(lhs, rhs) {
    return (typeof(lhs) === 'number' && typeof(rhs) === 'number') ? lhs + rhs : lhs['$+'](rhs);
  }
  var self = Opal.top, $scope = Opal, nil = Opal.nil, $breaker = Opal.breaker, $slice = Opal.slice, $klass = Opal.klass, $gvars = Opal.gvars;

  Opal.add_stubs(['$require', '$include', '$coerce_to?', '$coerce_to', '$raise', '$===', '$format', '$to_s', '$respond_to?', '$to_str', '$<=>', '$==', '$=~', '$new', '$empty?', '$ljust', '$ceil', '$/', '$+', '$rjust', '$floor', '$to_a', '$each_char', '$to_proc', '$coerce_to!', '$copy_singleton_methods', '$initialize_clone', '$initialize_dup', '$enum_for', '$size', '$chomp', '$[]', '$to_i', '$class', '$each_line', '$match', '$captures', '$proc', '$shift', '$__send__', '$succ', '$escape']);
  self.$require("corelib/comparable");
  self.$require("corelib/regexp");
  (function($base, $super) {
    function $String(){};
    var self = $String = $klass($base, $super, 'String', $String);

    var def = self.$$proto, $scope = self.$$scope, TMP_1, TMP_2, TMP_4, TMP_5, TMP_6, TMP_7, TMP_8, TMP_9, TMP_11;

    def.length = nil;
    self.$include($scope.get('Comparable'));

    def.$$is_string = true;

    Opal.defn(self, '$__id__', function() {
      var self = this;

      return self.toString();
    });

    Opal.alias(self, 'object_id', '__id__');

    Opal.defs(self, '$try_convert', function(what) {
      var self = this;

      return $scope.get('Opal')['$coerce_to?'](what, $scope.get('String'), "to_str");
    });

    Opal.defs(self, '$new', function(str) {
      var self = this;

      if (str == null) {
        str = ""
      }
      str = $scope.get('Opal').$coerce_to(str, $scope.get('String'), "to_str");
      return new String(str);
    });

    Opal.defn(self, '$initialize', function(str) {
      var self = this;

      
      if (str === undefined) {
        return self;
      }
    
      return self.$raise($scope.get('NotImplementedError'), "Mutable strings are not supported in Opal.");
    });

    Opal.defn(self, '$%', function(data) {
      var $a, self = this;

      if ((($a = $scope.get('Array')['$==='](data)) !== nil && (!$a.$$is_boolean || $a == true))) {
        return ($a = self).$format.apply($a, [self].concat(Opal.to_a(data)))
        } else {
        return self.$format(self, data)
      };
    });

    Opal.defn(self, '$*', function(count) {
      var self = this;

      
      count = $scope.get('Opal').$coerce_to(count, $scope.get('Integer'), "to_int");

      if (count < 0) {
        self.$raise($scope.get('ArgumentError'), "negative argument")
      }

      if (count === 0) {
        return '';
      }

      var result = '',
          string = self.toString();

      // All credit for the bit-twiddling magic code below goes to Mozilla
      // polyfill implementation of String.prototype.repeat() posted here:
      // https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/String/repeat

      if (string.length * count >= 1 << 28) {
        self.$raise($scope.get('RangeError'), "multiply count must not overflow maximum string size")
      }

      for (;;) {
        if ((count & 1) === 1) {
          result += string;
        }
        count >>>= 1;
        if (count === 0) {
          break;
        }
        string += string;
      }

      return result;
    ;
    });

    Opal.defn(self, '$+', function(other) {
      var self = this;

      other = $scope.get('Opal').$coerce_to(other, $scope.get('String'), "to_str");
      return self + other.$to_s();
    });

    Opal.defn(self, '$<=>', function(other) {
      var $a, self = this;

      if ((($a = other['$respond_to?']("to_str")) !== nil && (!$a.$$is_boolean || $a == true))) {
        other = other.$to_str().$to_s();
        return self > other ? 1 : (self < other ? -1 : 0);
        } else {
        
        var cmp = other['$<=>'](self);

        if (cmp === nil) {
          return nil;
        }
        else {
          return cmp > 0 ? -1 : (cmp < 0 ? 1 : 0);
        }
      ;
      };
    });

    Opal.defn(self, '$==', function(other) {
      var self = this;

      
      if (other.$$is_string) {
        return self.toString() === other.toString();
      }
      if ($scope.get('Opal')['$respond_to?'](other, "to_str")) {
        return other['$=='](self);
      }
      return false;
    ;
    });

    Opal.alias(self, 'eql?', '==');

    Opal.alias(self, '===', '==');

    Opal.defn(self, '$=~', function(other) {
      var self = this;

      
      if (other.$$is_string) {
        self.$raise($scope.get('TypeError'), "type mismatch: String given");
      }

      return other['$=~'](self);
    ;
    });

    Opal.defn(self, '$[]', function(index, length) {
      var self = this;

      
      var size = self.length, exclude;

      if (index.$$is_range) {
        exclude = index.exclude;
        length  = $scope.get('Opal').$coerce_to(index.end, $scope.get('Integer'), "to_int");
        index   = $scope.get('Opal').$coerce_to(index.begin, $scope.get('Integer'), "to_int");

        if (Math.abs(index) > size) {
          return nil;
        }

        if (index < 0) {
          index += size;
        }

        if (length < 0) {
          length += size;
        }

        if (!exclude) {
          length += 1;
        }

        length = length - index;

        if (length < 0) {
          length = 0;
        }

        return self.substr(index, length);
      }


      if (index.$$is_string) {
        if (length != null) {
          self.$raise($scope.get('TypeError'))
        }
        return self.indexOf(index) !== -1 ? index : nil;
      }


      if (index.$$is_regexp) {
        var match = self.match(index);

        if (match === null) {
          $gvars["~"] = nil
          return nil;
        }

        $gvars["~"] = $scope.get('MatchData').$new(index, match)

        if (length == null) {
          return match[0];
        }

        length = $scope.get('Opal').$coerce_to(length, $scope.get('Integer'), "to_int");

        if (length < 0 && -length < match.length) {
          return match[length += match.length];
        }

        if (length >= 0 && length < match.length) {
          return match[length];
        }

        return nil;
      }


      index = $scope.get('Opal').$coerce_to(index, $scope.get('Integer'), "to_int");

      if (index < 0) {
        index += size;
      }

      if (length == null) {
        if (index >= size || index < 0) {
          return nil;
        }
        return self.substr(index, 1);
      }

      length = $scope.get('Opal').$coerce_to(length, $scope.get('Integer'), "to_int");

      if (length < 0) {
        return nil;
      }

      if (index > size || index < 0) {
        return nil;
      }

      return self.substr(index, length);
    
    });

    Opal.alias(self, 'byteslice', '[]');

    Opal.defn(self, '$capitalize', function() {
      var self = this;

      return self.charAt(0).toUpperCase() + self.substr(1).toLowerCase();
    });

    Opal.defn(self, '$casecmp', function(other) {
      var self = this;

      other = $scope.get('Opal').$coerce_to(other, $scope.get('String'), "to_str").$to_s();
      
      var ascii_only = /^[\x00-\x7F]*$/;
      if (ascii_only.test(self) && ascii_only.test(other)) {
        self = self.toLowerCase();
        other = other.toLowerCase();
      }
    
      return self['$<=>'](other);
    });

    Opal.defn(self, '$center', function(width, padstr) {
      var $a, self = this;

      if (padstr == null) {
        padstr = " "
      }
      width = $scope.get('Opal').$coerce_to(width, $scope.get('Integer'), "to_int");
      padstr = $scope.get('Opal').$coerce_to(padstr, $scope.get('String'), "to_str").$to_s();
      if ((($a = padstr['$empty?']()) !== nil && (!$a.$$is_boolean || $a == true))) {
        self.$raise($scope.get('ArgumentError'), "zero width padding")};
      if ((($a = width <= self.length) !== nil && (!$a.$$is_boolean || $a == true))) {
        return self};
      
      var ljustified = self.$ljust($rb_divide(($rb_plus(width, self.length)), 2).$ceil(), padstr),
          rjustified = self.$rjust($rb_divide(($rb_plus(width, self.length)), 2).$floor(), padstr);

      return rjustified + ljustified.slice(self.length);
    ;
    });

    Opal.defn(self, '$chars', TMP_1 = function() {
      var $a, $b, self = this, $iter = TMP_1.$$p, block = $iter || nil;

      TMP_1.$$p = null;
      if (block !== false && block !== nil) {
        } else {
        return self.$each_char().$to_a()
      };
      return ($a = ($b = self).$each_char, $a.$$p = block.$to_proc(), $a).call($b);
    });

    Opal.defn(self, '$chomp', function(separator) {
      var $a, self = this;
      if ($gvars["/"] == null) $gvars["/"] = nil;

      if (separator == null) {
        separator = $gvars["/"]
      }
      if ((($a = separator === nil || self.length === 0) !== nil && (!$a.$$is_boolean || $a == true))) {
        return self};
      separator = $scope.get('Opal')['$coerce_to!'](separator, $scope.get('String'), "to_str").$to_s();
      
      if (separator === "\n") {
        return self.replace(/\r?\n?$/, '');
      }
      else if (separator === "") {
        return self.replace(/(\r?\n)+$/, '');
      }
      else if (self.length > separator.length) {
        var tail = self.substr(self.length - separator.length, separator.length);

        if (tail === separator) {
          return self.substr(0, self.length - separator.length);
        }
      }
    
      return self;
    });

    Opal.defn(self, '$chop', function() {
      var self = this;

      
      var length = self.length;

      if (length <= 1) {
        return "";
      }

      if (self.charAt(length - 1) === "\n" && self.charAt(length - 2) === "\r") {
        return self.substr(0, length - 2);
      }
      else {
        return self.substr(0, length - 1);
      }
    
    });

    Opal.defn(self, '$chr', function() {
      var self = this;

      return self.charAt(0);
    });

    Opal.defn(self, '$clone', function() {
      var self = this, copy = nil;

      copy = self.slice();
      copy.$copy_singleton_methods(self);
      copy.$initialize_clone(self);
      return copy;
    });

    Opal.defn(self, '$dup', function() {
      var self = this, copy = nil;

      copy = self.slice();
      copy.$initialize_dup(self);
      return copy;
    });

    Opal.defn(self, '$count', function() {
      var self = this, $splat_index = nil;

      var array_size = arguments.length - 0;
      if(array_size < 0) array_size = 0;
      var sets = new Array(array_size);
      for($splat_index = 0; $splat_index < array_size; $splat_index++) {
        sets[$splat_index] = arguments[$splat_index + 0];
      }
      
      if (sets.length === 0) {
        self.$raise($scope.get('ArgumentError'), "ArgumentError: wrong number of arguments (0 for 1+)")
      }
      var char_class = char_class_from_char_sets(sets);
      if (char_class === null) {
        return 0;
      }
      return self.length - self.replace(new RegExp(char_class, 'g'), '').length;
    ;
    });

    Opal.defn(self, '$delete', function() {
      var self = this, $splat_index = nil;

      var array_size = arguments.length - 0;
      if(array_size < 0) array_size = 0;
      var sets = new Array(array_size);
      for($splat_index = 0; $splat_index < array_size; $splat_index++) {
        sets[$splat_index] = arguments[$splat_index + 0];
      }
      
      if (sets.length === 0) {
        self.$raise($scope.get('ArgumentError'), "ArgumentError: wrong number of arguments (0 for 1+)")
      }
      var char_class = char_class_from_char_sets(sets);
      if (char_class === null) {
        return self;
      }
      return self.replace(new RegExp(char_class, 'g'), '');
    ;
    });

    Opal.defn(self, '$downcase', function() {
      var self = this;

      return self.toLowerCase();
    });

    Opal.defn(self, '$each_char', TMP_2 = function() {
      var $a, $b, TMP_3, self = this, $iter = TMP_2.$$p, block = $iter || nil;

      TMP_2.$$p = null;
      if ((block !== nil)) {
        } else {
        return ($a = ($b = self).$enum_for, $a.$$p = (TMP_3 = function(){var self = TMP_3.$$s || this;

        return self.$size()}, TMP_3.$$s = self, TMP_3), $a).call($b, "each_char")
      };
      
      for (var i = 0, length = self.length; i < length; i++) {
        var value = Opal.yield1(block, self.charAt(i));

        if (value === $breaker) {
          return $breaker.$v;
        }
      }
    
      return self;
    });

    Opal.defn(self, '$each_line', TMP_4 = function(separator) {
      var self = this, $iter = TMP_4.$$p, block = $iter || nil;
      if ($gvars["/"] == null) $gvars["/"] = nil;

      if (separator == null) {
        separator = $gvars["/"]
      }
      TMP_4.$$p = null;
      if ((block !== nil)) {
        } else {
        return self.$enum_for("each_line", separator)
      };
      
      var value;

      if (separator === nil) {
        value = Opal.yield1(block, self);

        if (value === $breaker) {
          return value.$v;
        }
        else {
          return self;
        }
      }

      separator = $scope.get('Opal').$coerce_to(separator, $scope.get('String'), "to_str")

      var a, i, n, length, chomped, trailing, splitted;

      if (separator.length === 0) {
        for (a = self.split(/(\n{2,})/), i = 0, n = a.length; i < n; i += 2) {
          if (a[i] || a[i + 1]) {
            value = Opal.yield1(block, (a[i] || "") + (a[i + 1] || ""));

            if (value === $breaker) {
              return value.$v;
            }
          }
        }

        return self;
      }

      chomped  = self.$chomp(separator);
      trailing = self.length != chomped.length;
      splitted = chomped.split(separator);

      for (i = 0, length = splitted.length; i < length; i++) {
        if (i < length - 1 || trailing) {
          value = Opal.yield1(block, splitted[i] + separator);

          if (value === $breaker) {
            return value.$v;
          }
        }
        else {
          value = Opal.yield1(block, splitted[i]);

          if (value === $breaker) {
            return value.$v;
          }
        }
      }
    
      return self;
    });

    Opal.defn(self, '$empty?', function() {
      var self = this;

      return self.length === 0;
    });

    Opal.defn(self, '$end_with?', function() {
      var self = this, $splat_index = nil;

      var array_size = arguments.length - 0;
      if(array_size < 0) array_size = 0;
      var suffixes = new Array(array_size);
      for($splat_index = 0; $splat_index < array_size; $splat_index++) {
        suffixes[$splat_index] = arguments[$splat_index + 0];
      }
      
      for (var i = 0, length = suffixes.length; i < length; i++) {
        var suffix = $scope.get('Opal').$coerce_to(suffixes[i], $scope.get('String'), "to_str").$to_s();

        if (self.length >= suffix.length &&
            self.substr(self.length - suffix.length, suffix.length) == suffix) {
          return true;
        }
      }
    
      return false;
    });

    Opal.alias(self, 'eql?', '==');

    Opal.alias(self, 'equal?', '===');

    Opal.defn(self, '$gsub', TMP_5 = function(pattern, replacement) {
      var self = this, $iter = TMP_5.$$p, block = $iter || nil;

      TMP_5.$$p = null;
      
      if (replacement === undefined && block === nil) {
        return self.$enum_for("gsub", pattern);
      }

      var result = '', match_data = nil, index = 0, match, _replacement;

      if (pattern.$$is_regexp) {
        pattern = new RegExp(pattern.source, 'gm' + (pattern.ignoreCase ? 'i' : ''));
      } else {
        pattern = $scope.get('Opal').$coerce_to(pattern, $scope.get('String'), "to_str");
        pattern = new RegExp(pattern.replace(/[.*+?^${}()|[\]\\]/g, '\\$&'), 'gm');
      }

      while (true) {
        match = pattern.exec(self);

        if (match === null) {
          $gvars["~"] = nil
          result += self.slice(index);
          break;
        }

        match_data = $scope.get('MatchData').$new(pattern, match);

        if (replacement === undefined) {
          _replacement = block(match[0]);
        }
        else if (replacement.$$is_hash) {
          _replacement = (replacement)['$[]'](match[0]).$to_s();
        }
        else {
          if (!replacement.$$is_string) {
            replacement = $scope.get('Opal').$coerce_to(replacement, $scope.get('String'), "to_str");
          }
          _replacement = replacement.replace(/([\\]+)([0-9+&`'])/g, function (original, slashes, command) {
            if (slashes.length % 2 === 0) {
              return original;
            }
            switch (command) {
            case "+":
              for (var i = match.length - 1; i > 0; i--) {
                if (match[i] !== undefined) {
                  return slashes.slice(1) + match[i];
                }
              }
              return '';
            case "&": return slashes.slice(1) + match[0];
            case "`": return slashes.slice(1) + self.slice(0, match.index);
            case "'": return slashes.slice(1) + self.slice(match.index + match[0].length);
            default:  return slashes.slice(1) + (match[command] || '');
            }
          }).replace(/\\\\/g, '\\');
        }

        if (pattern.lastIndex === match.index) {
          result += (_replacement + self.slice(index, match.index + 1))
          pattern.lastIndex += 1;
        }
        else {
          result += (self.slice(index, match.index) + _replacement)
        }
        index = pattern.lastIndex;
      }

      $gvars["~"] = match_data
      return result;
    ;
    });

    Opal.defn(self, '$hash', function() {
      var self = this;

      return self.toString();
    });

    Opal.defn(self, '$hex', function() {
      var self = this;

      return self.$to_i(16);
    });

    Opal.defn(self, '$include?', function(other) {
      var $a, self = this;

      
      if (other.$$is_string) {
        return self.indexOf(other) !== -1;
      }
    
      if ((($a = other['$respond_to?']("to_str")) !== nil && (!$a.$$is_boolean || $a == true))) {
        } else {
        self.$raise($scope.get('TypeError'), "no implicit conversion of " + (other.$class()) + " into String")
      };
      return self.indexOf(other.$to_str()) !== -1;
    });

    Opal.defn(self, '$index', function(search, offset) {
      var self = this;

      
      var index,
          match,
          regex;

      if (offset === undefined) {
        offset = 0;
      } else {
        offset = $scope.get('Opal').$coerce_to(offset, $scope.get('Integer'), "to_int");
        if (offset < 0) {
          offset += self.length;
          if (offset < 0) {
            return nil;
          }
        }
      }

      if (search.$$is_regexp) {
        regex = new RegExp(search.source, 'gm' + (search.ignoreCase ? 'i' : ''));
        while (true) {
          match = regex.exec(self);
          if (match === null) {
            $gvars["~"] = nil;
            index = -1;
            break;
          }
          if (match.index >= offset) {
            $gvars["~"] = $scope.get('MatchData').$new(regex, match)
            index = match.index;
            break;
          }
          regex.lastIndex = match.index + 1;
        }
      } else {
        search = $scope.get('Opal').$coerce_to(search, $scope.get('String'), "to_str");
        if (search.length === 0 && offset > self.length) {
          index = -1;
        } else {
          index = self.indexOf(search, offset);
        }
      }

      return index === -1 ? nil : index;
    
    });

    Opal.defn(self, '$inspect', function() {
      var self = this;

      
      var escapable = /[\\\"\x00-\x1f\x7f-\x9f\u0600-\u0604\u070f\u17b4\u17b5\u200c-\u200f\u2028-\u202f\u2060-\u206f\ufeff\ufff0-\uffff]/g,
          meta = {
            '\u0007': '\\a',
            '\u001b': '\\e',
            '\b': '\\b',
            '\t': '\\t',
            '\n': '\\n',
            '\f': '\\f',
            '\r': '\\r',
            '\v': '\\v',
            '"' : '\\"',
            '\\': '\\\\'
          },
          escaped = self.replace(escapable, function (chr) {
            return meta[chr] || '\\u' + ('0000' + chr.charCodeAt(0).toString(16).toUpperCase()).slice(-4);
          });
      return '"' + escaped.replace(/\#[\$\@\{]/g, '\\$&') + '"';
    
    });

    Opal.defn(self, '$intern', function() {
      var self = this;

      return self;
    });

    Opal.defn(self, '$lines', TMP_6 = function(separator) {
      var $a, $b, self = this, $iter = TMP_6.$$p, block = $iter || nil, e = nil;
      if ($gvars["/"] == null) $gvars["/"] = nil;

      if (separator == null) {
        separator = $gvars["/"]
      }
      TMP_6.$$p = null;
      e = ($a = ($b = self).$each_line, $a.$$p = block.$to_proc(), $a).call($b, separator);
      if (block !== false && block !== nil) {
        return self
        } else {
        return e.$to_a()
      };
    });

    Opal.defn(self, '$length', function() {
      var self = this;

      return self.length;
    });

    Opal.defn(self, '$ljust', function(width, padstr) {
      var $a, self = this;

      if (padstr == null) {
        padstr = " "
      }
      width = $scope.get('Opal').$coerce_to(width, $scope.get('Integer'), "to_int");
      padstr = $scope.get('Opal').$coerce_to(padstr, $scope.get('String'), "to_str").$to_s();
      if ((($a = padstr['$empty?']()) !== nil && (!$a.$$is_boolean || $a == true))) {
        self.$raise($scope.get('ArgumentError'), "zero width padding")};
      if ((($a = width <= self.length) !== nil && (!$a.$$is_boolean || $a == true))) {
        return self};
      
      var index  = -1,
          result = "";

      width -= self.length;

      while (++index < width) {
        result += padstr;
      }

      return self + result.slice(0, width);
    
    });

    Opal.defn(self, '$lstrip', function() {
      var self = this;

      return self.replace(/^\s*/, '');
    });

    Opal.defn(self, '$match', TMP_7 = function(pattern, pos) {
      var $a, $b, self = this, $iter = TMP_7.$$p, block = $iter || nil;

      TMP_7.$$p = null;
      if ((($a = ((($b = $scope.get('String')['$==='](pattern)) !== false && $b !== nil) ? $b : pattern['$respond_to?']("to_str"))) !== nil && (!$a.$$is_boolean || $a == true))) {
        pattern = $scope.get('Regexp').$new(pattern.$to_str())};
      if ((($a = $scope.get('Regexp')['$==='](pattern)) !== nil && (!$a.$$is_boolean || $a == true))) {
        } else {
        self.$raise($scope.get('TypeError'), "wrong argument type " + (pattern.$class()) + " (expected Regexp)")
      };
      return ($a = ($b = pattern).$match, $a.$$p = block.$to_proc(), $a).call($b, self, pos);
    });

    Opal.defn(self, '$next', function() {
      var self = this;

      
      var i = self.length;
      if (i === 0) {
        return '';
      }
      var result = self;
      var first_alphanum_char_index = self.search(/[a-zA-Z0-9]/);
      var carry = false;
      var code;
      while (i--) {
        code = self.charCodeAt(i);
        if ((code >= 48 && code <= 57) ||
          (code >= 65 && code <= 90) ||
          (code >= 97 && code <= 122)) {
          switch (code) {
          case 57:
            carry = true;
            code = 48;
            break;
          case 90:
            carry = true;
            code = 65;
            break;
          case 122:
            carry = true;
            code = 97;
            break;
          default:
            carry = false;
            code += 1;
          }
        } else {
          if (first_alphanum_char_index === -1) {
            if (code === 255) {
              carry = true;
              code = 0;
            } else {
              carry = false;
              code += 1;
            }
          } else {
            carry = true;
          }
        }
        result = result.slice(0, i) + String.fromCharCode(code) + result.slice(i + 1);
        if (carry && (i === 0 || i === first_alphanum_char_index)) {
          switch (code) {
          case 65:
            break;
          case 97:
            break;
          default:
            code += 1;
          }
          if (i === 0) {
            result = String.fromCharCode(code) + result;
          } else {
            result = result.slice(0, i) + String.fromCharCode(code) + result.slice(i);
          }
          carry = false;
        }
        if (!carry) {
          break;
        }
      }
      return result;
    
    });

    Opal.defn(self, '$oct', function() {
      var self = this;

      
      var result,
          string = self,
          radix = 8;

      if (/^\s*_/.test(string)) {
        return 0;
      }

      string = string.replace(/^(\s*[+-]?)(0[bodx]?)(.+)$/i, function (original, head, flag, tail) {
        switch (tail.charAt(0)) {
        case '+':
        case '-':
          return original;
        case '0':
          if (tail.charAt(1) === 'x' && flag === '0x') {
            return original;
          }
        }
        switch (flag) {
        case '0b':
          radix = 2;
          break;
        case '0':
        case '0o':
          radix = 8;
          break;
        case '0d':
          radix = 10;
          break;
        case '0x':
          radix = 16;
          break;
        }
        return head + tail;
      });

      result = parseInt(string.replace(/_(?!_)/g, ''), radix);
      return isNaN(result) ? 0 : result;
    
    });

    Opal.defn(self, '$ord', function() {
      var self = this;

      return self.charCodeAt(0);
    });

    Opal.defn(self, '$partition', function(sep) {
      var self = this;

      
      var i, m;

      if (sep.$$is_regexp) {
        m = sep.exec(self);
        if (m === null) {
          i = -1;
        } else {
          $scope.get('MatchData').$new(sep, m);
          sep = m[0];
          i = m.index;
        }
      } else {
        sep = $scope.get('Opal').$coerce_to(sep, $scope.get('String'), "to_str");
        i = self.indexOf(sep);
      }

      if (i === -1) {
        return [self, '', ''];
      }

      return [
        self.slice(0, i),
        self.slice(i, i + sep.length),
        self.slice(i + sep.length)
      ];
    
    });

    Opal.defn(self, '$reverse', function() {
      var self = this;

      return self.split('').reverse().join('');
    });

    Opal.defn(self, '$rindex', function(search, offset) {
      var self = this;

      
      var i, m, r, _m;

      if (offset === undefined) {
        offset = self.length;
      } else {
        offset = $scope.get('Opal').$coerce_to(offset, $scope.get('Integer'), "to_int");
        if (offset < 0) {
          offset += self.length;
          if (offset < 0) {
            return nil;
          }
        }
      }

      if (search.$$is_regexp) {
        m = null;
        r = new RegExp(search.source, 'gm' + (search.ignoreCase ? 'i' : ''));
        while (true) {
          _m = r.exec(self);
          if (_m === null || _m.index > offset) {
            break;
          }
          m = _m;
          r.lastIndex = m.index + 1;
        }
        if (m === null) {
          $gvars["~"] = nil
          i = -1;
        } else {
          $scope.get('MatchData').$new(r, m);
          i = m.index;
        }
      } else {
        search = $scope.get('Opal').$coerce_to(search, $scope.get('String'), "to_str");
        i = self.lastIndexOf(search, offset);
      }

      return i === -1 ? nil : i;
    
    });

    Opal.defn(self, '$rjust', function(width, padstr) {
      var $a, self = this;

      if (padstr == null) {
        padstr = " "
      }
      width = $scope.get('Opal').$coerce_to(width, $scope.get('Integer'), "to_int");
      padstr = $scope.get('Opal').$coerce_to(padstr, $scope.get('String'), "to_str").$to_s();
      if ((($a = padstr['$empty?']()) !== nil && (!$a.$$is_boolean || $a == true))) {
        self.$raise($scope.get('ArgumentError'), "zero width padding")};
      if ((($a = width <= self.length) !== nil && (!$a.$$is_boolean || $a == true))) {
        return self};
      
      var chars     = Math.floor(width - self.length),
          patterns  = Math.floor(chars / padstr.length),
          result    = Array(patterns + 1).join(padstr),
          remaining = chars - result.length;

      return result + padstr.slice(0, remaining) + self;
    
    });

    Opal.defn(self, '$rpartition', function(sep) {
      var self = this;

      
      var i, m, r, _m;

      if (sep.$$is_regexp) {
        m = null;
        r = new RegExp(sep.source, 'gm' + (sep.ignoreCase ? 'i' : ''));

        while (true) {
          _m = r.exec(self);
          if (_m === null) {
            break;
          }
          m = _m;
          r.lastIndex = m.index + 1;
        }

        if (m === null) {
          i = -1;
        } else {
          $scope.get('MatchData').$new(r, m);
          sep = m[0];
          i = m.index;
        }

      } else {
        sep = $scope.get('Opal').$coerce_to(sep, $scope.get('String'), "to_str");
        i = self.lastIndexOf(sep);
      }

      if (i === -1) {
        return ['', '', self];
      }

      return [
        self.slice(0, i),
        self.slice(i, i + sep.length),
        self.slice(i + sep.length)
      ];
    
    });

    Opal.defn(self, '$rstrip', function() {
      var self = this;

      return self.replace(/[\s\u0000]*$/, '');
    });

    Opal.defn(self, '$scan', TMP_8 = function(pattern) {
      var self = this, $iter = TMP_8.$$p, block = $iter || nil;

      TMP_8.$$p = null;
      
      var result = [],
          match_data = nil,
          match;

      if (pattern.$$is_regexp) {
        pattern = new RegExp(pattern.source, 'gm' + (pattern.ignoreCase ? 'i' : ''));
      } else {
        pattern = $scope.get('Opal').$coerce_to(pattern, $scope.get('String'), "to_str");
        pattern = new RegExp(pattern.replace(/[.*+?^${}()|[\]\\]/g, '\\$&'), 'gm');
      }

      while ((match = pattern.exec(self)) != null) {
        match_data = $scope.get('MatchData').$new(pattern, match);
        if (block === nil) {
          match.length == 1 ? result.push(match[0]) : result.push((match_data).$captures());
        } else {
          match.length == 1 ? block(match[0]) : block.call(self, (match_data).$captures());
        }
        if (pattern.lastIndex === match.index) {
          pattern.lastIndex += 1;
        }
      }

      $gvars["~"] = match_data

      return (block !== nil ? self : result);
    
    });

    Opal.alias(self, 'size', 'length');

    Opal.alias(self, 'slice', '[]');

    Opal.defn(self, '$split', function(pattern, limit) {
      var $a, self = this;
      if ($gvars[";"] == null) $gvars[";"] = nil;

      
      if (self.length === 0) {
        return [];
      }

      if (limit === undefined) {
        limit = 0;
      } else {
        limit = $scope.get('Opal')['$coerce_to!'](limit, $scope.get('Integer'), "to_int");
        if (limit === 1) {
          return [self];
        }
      }

      if (pattern === undefined || pattern === nil) {
        pattern = ((($a = $gvars[";"]) !== false && $a !== nil) ? $a : " ");
      }

      var result = [],
          string = self.toString(),
          index = 0,
          match,
          i;

      if (pattern.$$is_regexp) {
        pattern = new RegExp(pattern.source, 'gm' + (pattern.ignoreCase ? 'i' : ''));
      } else {
        pattern = $scope.get('Opal').$coerce_to(pattern, $scope.get('String'), "to_str").$to_s();
        if (pattern === ' ') {
          pattern = /\s+/gm;
          string = string.replace(/^\s+/, '');
        } else {
          pattern = new RegExp(pattern.replace(/[.*+?^${}()|[\]\\]/g, '\\$&'), 'gm');
        }
      }

      result = string.split(pattern);

      if (result.length === 1 && result[0] === string) {
        return result;
      }

      while ((i = result.indexOf(undefined)) !== -1) {
        result.splice(i, 1);
      }

      if (limit === 0) {
        while (result[result.length - 1] === '') {
          result.length -= 1;
        }
        return result;
      }

      match = pattern.exec(string);

      if (limit < 0) {
        if (match !== null && match[0] === '' && pattern.source.indexOf('(?=') === -1) {
          for (i = 0; i < match.length; i++) {
            result.push('');
          }
        }
        return result;
      }

      if (match !== null && match[0] === '') {
        result.splice(limit - 1, result.length - 1, result.slice(limit - 1).join(''));
        return result;
      }

      i = 0;
      while (match !== null) {
        i++;
        index = pattern.lastIndex;
        if (i + 1 === limit) {
          break;
        }
        match = pattern.exec(string);
      }

      result.splice(limit - 1, result.length - 1, string.slice(index));
      return result;
    
    });

    Opal.defn(self, '$squeeze', function() {
      var self = this, $splat_index = nil;

      var array_size = arguments.length - 0;
      if(array_size < 0) array_size = 0;
      var sets = new Array(array_size);
      for($splat_index = 0; $splat_index < array_size; $splat_index++) {
        sets[$splat_index] = arguments[$splat_index + 0];
      }
      
      if (sets.length === 0) {
        return self.replace(/(.)\1+/g, '$1');
      }
      var char_class = char_class_from_char_sets(sets);
      if (char_class === null) {
        return self;
      }
      return self.replace(new RegExp('(' + char_class + ')\\1+', 'g'), '$1');
    
    });

    Opal.defn(self, '$start_with?', function() {
      var self = this, $splat_index = nil;

      var array_size = arguments.length - 0;
      if(array_size < 0) array_size = 0;
      var prefixes = new Array(array_size);
      for($splat_index = 0; $splat_index < array_size; $splat_index++) {
        prefixes[$splat_index] = arguments[$splat_index + 0];
      }
      
      for (var i = 0, length = prefixes.length; i < length; i++) {
        var prefix = $scope.get('Opal').$coerce_to(prefixes[i], $scope.get('String'), "to_str").$to_s();

        if (self.indexOf(prefix) === 0) {
          return true;
        }
      }

      return false;
    
    });

    Opal.defn(self, '$strip', function() {
      var self = this;

      return self.replace(/^\s*/, '').replace(/[\s\u0000]*$/, '');
    });

    Opal.defn(self, '$sub', TMP_9 = function(pattern, replacement) {
      var self = this, $iter = TMP_9.$$p, block = $iter || nil;

      TMP_9.$$p = null;
      
      if (!pattern.$$is_regexp) {
        pattern = $scope.get('Opal').$coerce_to(pattern, $scope.get('String'), "to_str");
        pattern = new RegExp(pattern.replace(/[.*+?^${}()|[\]\\]/g, '\\$&'));
      }

      var result = pattern.exec(self);

      if (result === null) {
        $gvars["~"] = nil
        return self.toString();
      }

      $scope.get('MatchData').$new(pattern, result)

      if (replacement === undefined) {
        if (block === nil) {
          self.$raise($scope.get('ArgumentError'), "wrong number of arguments (1 for 2)")
        }
        return self.slice(0, result.index) + block(result[0]) + self.slice(result.index + result[0].length);
      }

      if (replacement.$$is_hash) {
        return self.slice(0, result.index) + (replacement)['$[]'](result[0]).$to_s() + self.slice(result.index + result[0].length);
      }

      replacement = $scope.get('Opal').$coerce_to(replacement, $scope.get('String'), "to_str");

      replacement = replacement.replace(/([\\]+)([0-9+&`'])/g, function (original, slashes, command) {
        if (slashes.length % 2 === 0) {
          return original;
        }
        switch (command) {
        case "+":
          for (var i = result.length - 1; i > 0; i--) {
            if (result[i] !== undefined) {
              return slashes.slice(1) + result[i];
            }
          }
          return '';
        case "&": return slashes.slice(1) + result[0];
        case "`": return slashes.slice(1) + self.slice(0, result.index);
        case "'": return slashes.slice(1) + self.slice(result.index + result[0].length);
        default:  return slashes.slice(1) + (result[command] || '');
        }
      }).replace(/\\\\/g, '\\');

      return self.slice(0, result.index) + replacement + self.slice(result.index + result[0].length);
    ;
    });

    Opal.alias(self, 'succ', 'next');

    Opal.defn(self, '$sum', function(n) {
      var self = this;

      if (n == null) {
        n = 16
      }
      
      n = $scope.get('Opal').$coerce_to(n, $scope.get('Integer'), "to_int");

      var result = 0,
          length = self.length,
          i = 0;

      for (; i < length; i++) {
        result += self.charCodeAt(i);
      }

      if (n <= 0) {
        return result;
      }

      return result & (Math.pow(2, n) - 1);
    ;
    });

    Opal.defn(self, '$swapcase', function() {
      var self = this;

      
      var str = self.replace(/([a-z]+)|([A-Z]+)/g, function($0,$1,$2) {
        return $1 ? $0.toUpperCase() : $0.toLowerCase();
      });

      if (self.constructor === String) {
        return str;
      }

      return self.$class().$new(str);
    
    });

    Opal.defn(self, '$to_f', function() {
      var self = this;

      
      if (self.charAt(0) === '_') {
        return 0;
      }

      var result = parseFloat(self.replace(/_/g, ''));

      if (isNaN(result) || result == Infinity || result == -Infinity) {
        return 0;
      }
      else {
        return result;
      }
    
    });

    Opal.defn(self, '$to_i', function(base) {
      var self = this;

      if (base == null) {
        base = 10
      }
      
      var result,
          string = self.toLowerCase(),
          radix = $scope.get('Opal').$coerce_to(base, $scope.get('Integer'), "to_int");

      if (radix === 1 || radix < 0 || radix > 36) {
        self.$raise($scope.get('ArgumentError'), "invalid radix " + (radix))
      }

      if (/^\s*_/.test(string)) {
        return 0;
      }

      string = string.replace(/^(\s*[+-]?)(0[bodx]?)(.+)$/, function (original, head, flag, tail) {
        switch (tail.charAt(0)) {
        case '+':
        case '-':
          return original;
        case '0':
          if (tail.charAt(1) === 'x' && flag === '0x' && (radix === 0 || radix === 16)) {
            return original;
          }
        }
        switch (flag) {
        case '0b':
          if (radix === 0 || radix === 2) {
            radix = 2;
            return head + tail;
          }
          break;
        case '0':
        case '0o':
          if (radix === 0 || radix === 8) {
            radix = 8;
            return head + tail;
          }
          break;
        case '0d':
          if (radix === 0 || radix === 10) {
            radix = 10;
            return head + tail;
          }
          break;
        case '0x':
          if (radix === 0 || radix === 16) {
            radix = 16;
            return head + tail;
          }
          break;
        }
        return original
      });

      result = parseInt(string.replace(/_(?!_)/g, ''), radix);
      return isNaN(result) ? 0 : result;
    ;
    });

    Opal.defn(self, '$to_proc', function() {
      var $a, $b, TMP_10, self = this, sym = nil;

      sym = self;
      return ($a = ($b = self).$proc, $a.$$p = (TMP_10 = function(args){var self = TMP_10.$$s || this, block, $a, $b, obj = nil;
args = $slice.call(arguments, 0);
        block = TMP_10.$$p || nil, TMP_10.$$p = null;
      if ((($a = args['$empty?']()) !== nil && (!$a.$$is_boolean || $a == true))) {
          self.$raise($scope.get('ArgumentError'), "no receiver given")};
        obj = args.$shift();
        return ($a = ($b = obj).$__send__, $a.$$p = block.$to_proc(), $a).apply($b, [sym].concat(Opal.to_a(args)));}, TMP_10.$$s = self, TMP_10), $a).call($b);
    });

    Opal.defn(self, '$to_s', function() {
      var self = this;

      return self.toString();
    });

    Opal.alias(self, 'to_str', 'to_s');

    Opal.alias(self, 'to_sym', 'intern');

    Opal.defn(self, '$tr', function(from, to) {
      var self = this;

      from = $scope.get('Opal').$coerce_to(from, $scope.get('String'), "to_str").$to_s();
      to = $scope.get('Opal').$coerce_to(to, $scope.get('String'), "to_str").$to_s();
      
      if (from.length == 0 || from === to) {
        return self;
      }

      var i, in_range, c, ch, start, end, length;
      var subs = {};
      var from_chars = from.split('');
      var from_length = from_chars.length;
      var to_chars = to.split('');
      var to_length = to_chars.length;

      var inverse = false;
      var global_sub = null;
      if (from_chars[0] === '^' && from_chars.length > 1) {
        inverse = true;
        from_chars.shift();
        global_sub = to_chars[to_length - 1]
        from_length -= 1;
      }

      var from_chars_expanded = [];
      var last_from = null;
      in_range = false;
      for (i = 0; i < from_length; i++) {
        ch = from_chars[i];
        if (last_from == null) {
          last_from = ch;
          from_chars_expanded.push(ch);
        }
        else if (ch === '-') {
          if (last_from === '-') {
            from_chars_expanded.push('-');
            from_chars_expanded.push('-');
          }
          else if (i == from_length - 1) {
            from_chars_expanded.push('-');
          }
          else {
            in_range = true;
          }
        }
        else if (in_range) {
          start = last_from.charCodeAt(0);
          end = ch.charCodeAt(0);
          if (start > end) {
            self.$raise($scope.get('ArgumentError'), "invalid range \"" + (String.fromCharCode(start)) + "-" + (String.fromCharCode(end)) + "\" in string transliteration")
          }
          for (c = start + 1; c < end; c++) {
            from_chars_expanded.push(String.fromCharCode(c));
          }
          from_chars_expanded.push(ch);
          in_range = null;
          last_from = null;
        }
        else {
          from_chars_expanded.push(ch);
        }
      }

      from_chars = from_chars_expanded;
      from_length = from_chars.length;

      if (inverse) {
        for (i = 0; i < from_length; i++) {
          subs[from_chars[i]] = true;
        }
      }
      else {
        if (to_length > 0) {
          var to_chars_expanded = [];
          var last_to = null;
          in_range = false;
          for (i = 0; i < to_length; i++) {
            ch = to_chars[i];
            if (last_from == null) {
              last_from = ch;
              to_chars_expanded.push(ch);
            }
            else if (ch === '-') {
              if (last_to === '-') {
                to_chars_expanded.push('-');
                to_chars_expanded.push('-');
              }
              else if (i == to_length - 1) {
                to_chars_expanded.push('-');
              }
              else {
                in_range = true;
              }
            }
            else if (in_range) {
              start = last_from.charCodeAt(0);
              end = ch.charCodeAt(0);
              if (start > end) {
                self.$raise($scope.get('ArgumentError'), "invalid range \"" + (String.fromCharCode(start)) + "-" + (String.fromCharCode(end)) + "\" in string transliteration")
              }
              for (c = start + 1; c < end; c++) {
                to_chars_expanded.push(String.fromCharCode(c));
              }
              to_chars_expanded.push(ch);
              in_range = null;
              last_from = null;
            }
            else {
              to_chars_expanded.push(ch);
            }
          }

          to_chars = to_chars_expanded;
          to_length = to_chars.length;
        }

        var length_diff = from_length - to_length;
        if (length_diff > 0) {
          var pad_char = (to_length > 0 ? to_chars[to_length - 1] : '');
          for (i = 0; i < length_diff; i++) {
            to_chars.push(pad_char);
          }
        }

        for (i = 0; i < from_length; i++) {
          subs[from_chars[i]] = to_chars[i];
        }
      }

      var new_str = ''
      for (i = 0, length = self.length; i < length; i++) {
        ch = self.charAt(i);
        var sub = subs[ch];
        if (inverse) {
          new_str += (sub == null ? global_sub : ch);
        }
        else {
          new_str += (sub != null ? sub : ch);
        }
      }
      return new_str;
    
    });

    Opal.defn(self, '$tr_s', function(from, to) {
      var self = this;

      from = $scope.get('Opal').$coerce_to(from, $scope.get('String'), "to_str").$to_s();
      to = $scope.get('Opal').$coerce_to(to, $scope.get('String'), "to_str").$to_s();
      
      if (from.length == 0) {
        return self;
      }

      var i, in_range, c, ch, start, end, length;
      var subs = {};
      var from_chars = from.split('');
      var from_length = from_chars.length;
      var to_chars = to.split('');
      var to_length = to_chars.length;

      var inverse = false;
      var global_sub = null;
      if (from_chars[0] === '^' && from_chars.length > 1) {
        inverse = true;
        from_chars.shift();
        global_sub = to_chars[to_length - 1]
        from_length -= 1;
      }

      var from_chars_expanded = [];
      var last_from = null;
      in_range = false;
      for (i = 0; i < from_length; i++) {
        ch = from_chars[i];
        if (last_from == null) {
          last_from = ch;
          from_chars_expanded.push(ch);
        }
        else if (ch === '-') {
          if (last_from === '-') {
            from_chars_expanded.push('-');
            from_chars_expanded.push('-');
          }
          else if (i == from_length - 1) {
            from_chars_expanded.push('-');
          }
          else {
            in_range = true;
          }
        }
        else if (in_range) {
          start = last_from.charCodeAt(0);
          end = ch.charCodeAt(0);
          if (start > end) {
            self.$raise($scope.get('ArgumentError'), "invalid range \"" + (String.fromCharCode(start)) + "-" + (String.fromCharCode(end)) + "\" in string transliteration")
          }
          for (c = start + 1; c < end; c++) {
            from_chars_expanded.push(String.fromCharCode(c));
          }
          from_chars_expanded.push(ch);
          in_range = null;
          last_from = null;
        }
        else {
          from_chars_expanded.push(ch);
        }
      }

      from_chars = from_chars_expanded;
      from_length = from_chars.length;

      if (inverse) {
        for (i = 0; i < from_length; i++) {
          subs[from_chars[i]] = true;
        }
      }
      else {
        if (to_length > 0) {
          var to_chars_expanded = [];
          var last_to = null;
          in_range = false;
          for (i = 0; i < to_length; i++) {
            ch = to_chars[i];
            if (last_from == null) {
              last_from = ch;
              to_chars_expanded.push(ch);
            }
            else if (ch === '-') {
              if (last_to === '-') {
                to_chars_expanded.push('-');
                to_chars_expanded.push('-');
              }
              else if (i == to_length - 1) {
                to_chars_expanded.push('-');
              }
              else {
                in_range = true;
              }
            }
            else if (in_range) {
              start = last_from.charCodeAt(0);
              end = ch.charCodeAt(0);
              if (start > end) {
                self.$raise($scope.get('ArgumentError'), "invalid range \"" + (String.fromCharCode(start)) + "-" + (String.fromCharCode(end)) + "\" in string transliteration")
              }
              for (c = start + 1; c < end; c++) {
                to_chars_expanded.push(String.fromCharCode(c));
              }
              to_chars_expanded.push(ch);
              in_range = null;
              last_from = null;
            }
            else {
              to_chars_expanded.push(ch);
            }
          }

          to_chars = to_chars_expanded;
          to_length = to_chars.length;
        }

        var length_diff = from_length - to_length;
        if (length_diff > 0) {
          var pad_char = (to_length > 0 ? to_chars[to_length - 1] : '');
          for (i = 0; i < length_diff; i++) {
            to_chars.push(pad_char);
          }
        }

        for (i = 0; i < from_length; i++) {
          subs[from_chars[i]] = to_chars[i];
        }
      }
      var new_str = ''
      var last_substitute = null
      for (i = 0, length = self.length; i < length; i++) {
        ch = self.charAt(i);
        var sub = subs[ch]
        if (inverse) {
          if (sub == null) {
            if (last_substitute == null) {
              new_str += global_sub;
              last_substitute = true;
            }
          }
          else {
            new_str += ch;
            last_substitute = null;
          }
        }
        else {
          if (sub != null) {
            if (last_substitute == null || last_substitute !== sub) {
              new_str += sub;
              last_substitute = sub;
            }
          }
          else {
            new_str += ch;
            last_substitute = null;
          }
        }
      }
      return new_str;
    
    });

    Opal.defn(self, '$upcase', function() {
      var self = this;

      return self.toUpperCase();
    });

    Opal.defn(self, '$upto', TMP_11 = function(stop, excl) {
      var self = this, $iter = TMP_11.$$p, block = $iter || nil;

      if (excl == null) {
        excl = false
      }
      TMP_11.$$p = null;
      if ((block !== nil)) {
        } else {
        return self.$enum_for("upto", stop, excl)
      };
      stop = $scope.get('Opal').$coerce_to(stop, $scope.get('String'), "to_str");
      
      var a, b, s = self.toString(), value;

      if (s.length === 1 && stop.length === 1) {

        a = s.charCodeAt(0);
        b = stop.charCodeAt(0);

        while (a <= b) {
          if (excl && a === b) {
            break;
          }

          value = block(String.fromCharCode(a));
          if (value === $breaker) { return $breaker.$v; }

          a += 1;
        }

      } else if (parseInt(s, 10).toString() === s && parseInt(stop, 10).toString() === stop) {

        a = parseInt(s, 10);
        b = parseInt(stop, 10);

        while (a <= b) {
          if (excl && a === b) {
            break;
          }

          value = block(a.toString());
          if (value === $breaker) { return $breaker.$v; }

          a += 1;
        }

      } else {

        while (s.length <= stop.length && s <= stop) {
          if (excl && s === stop) {
            break;
          }

          value = block(s);
          if (value === $breaker) { return $breaker.$v; }

          s = (s).$succ();
        }

      }
      return self;
    
    });

    
    function char_class_from_char_sets(sets) {
      function explode_sequences_in_character_set(set) {
        var result = '',
            i, len = set.length,
            curr_char,
            skip_next_dash,
            char_code_from,
            char_code_upto,
            char_code;
        for (i = 0; i < len; i++) {
          curr_char = set.charAt(i);
          if (curr_char === '-' && i > 0 && i < (len - 1) && !skip_next_dash) {
            char_code_from = set.charCodeAt(i - 1);
            char_code_upto = set.charCodeAt(i + 1);
            if (char_code_from > char_code_upto) {
              self.$raise($scope.get('ArgumentError'), "invalid range \"" + (char_code_from) + "-" + (char_code_upto) + "\" in string transliteration")
            }
            for (char_code = char_code_from + 1; char_code < char_code_upto + 1; char_code++) {
              result += String.fromCharCode(char_code);
            }
            skip_next_dash = true;
            i++;
          } else {
            skip_next_dash = (curr_char === '\\');
            result += curr_char;
          }
        }
        return result;
      }

      function intersection(setA, setB) {
        if (setA.length === 0) {
          return setB;
        }
        var result = '',
            i, len = setA.length,
            chr;
        for (i = 0; i < len; i++) {
          chr = setA.charAt(i);
          if (setB.indexOf(chr) !== -1) {
            result += chr;
          }
        }
        return result;
      }

      var i, len, set, neg, chr, tmp,
          pos_intersection = '',
          neg_intersection = '';

      for (i = 0, len = sets.length; i < len; i++) {
        set = $scope.get('Opal').$coerce_to(sets[i], $scope.get('String'), "to_str");
        neg = (set.charAt(0) === '^' && set.length > 1);
        set = explode_sequences_in_character_set(neg ? set.slice(1) : set);
        if (neg) {
          neg_intersection = intersection(neg_intersection, set);
        } else {
          pos_intersection = intersection(pos_intersection, set);
        }
      }

      if (pos_intersection.length > 0 && neg_intersection.length > 0) {
        tmp = '';
        for (i = 0, len = pos_intersection.length; i < len; i++) {
          chr = pos_intersection.charAt(i);
          if (neg_intersection.indexOf(chr) === -1) {
            tmp += chr;
          }
        }
        pos_intersection = tmp;
        neg_intersection = '';
      }

      if (pos_intersection.length > 0) {
        return '[' + $scope.get('Regexp').$escape(pos_intersection) + ']';
      }

      if (neg_intersection.length > 0) {
        return '[^' + $scope.get('Regexp').$escape(neg_intersection) + ']';
      }

      return null;
    }
  
  })($scope.base, String);
  return Opal.cdecl($scope, 'Symbol', $scope.get('String'));
};

/* Generated by Opal 0.9.0 */
Opal.modules["corelib/enumerable"] = function(Opal) {
  Opal.dynamic_require_severity = "error";
  var OPAL_CONFIG = { method_missing: true, arity_check: false, freezing: true, tainting: true };
  function $rb_gt(lhs, rhs) {
    return (typeof(lhs) === 'number' && typeof(rhs) === 'number') ? lhs > rhs : lhs['$>'](rhs);
  }
  function $rb_times(lhs, rhs) {
    return (typeof(lhs) === 'number' && typeof(rhs) === 'number') ? lhs * rhs : lhs['$*'](rhs);
  }
  function $rb_lt(lhs, rhs) {
    return (typeof(lhs) === 'number' && typeof(rhs) === 'number') ? lhs < rhs : lhs['$<'](rhs);
  }
  function $rb_plus(lhs, rhs) {
    return (typeof(lhs) === 'number' && typeof(rhs) === 'number') ? lhs + rhs : lhs['$+'](rhs);
  }
  function $rb_minus(lhs, rhs) {
    return (typeof(lhs) === 'number' && typeof(rhs) === 'number') ? lhs - rhs : lhs['$-'](rhs);
  }
  function $rb_divide(lhs, rhs) {
    return (typeof(lhs) === 'number' && typeof(rhs) === 'number') ? lhs / rhs : lhs['$/'](rhs);
  }
  var self = Opal.top, $scope = Opal, nil = Opal.nil, $breaker = Opal.breaker, $slice = Opal.slice, $module = Opal.module;

  Opal.add_stubs(['$raise', '$new', '$yield', '$dup', '$enum_for', '$enumerator_size', '$flatten', '$map', '$==', '$destructure', '$respond_to?', '$coerce_to!', '$>', '$*', '$nil?', '$coerce_to', '$try_convert', '$<', '$+', '$-', '$ceil', '$/', '$size', '$===', '$<<', '$[]', '$[]=', '$inspect', '$__send__', '$compare', '$<=>', '$proc', '$call', '$to_a', '$lambda', '$sort!', '$map!', '$first', '$zip']);
  return (function($base) {
    var $Enumerable, self = $Enumerable = $module($base, 'Enumerable');

    var def = self.$$proto, $scope = self.$$scope, TMP_1, TMP_2, TMP_3, TMP_6, TMP_8, TMP_11, TMP_12, TMP_14, TMP_15, TMP_16, TMP_18, TMP_19, TMP_21, TMP_23, TMP_25, TMP_27, TMP_28, TMP_29, TMP_31, TMP_33, TMP_34, TMP_36, TMP_37, TMP_39, TMP_41, TMP_42, TMP_43, TMP_44, TMP_46, TMP_48, TMP_50, TMP_52, TMP_54, TMP_59, TMP_60;

    Opal.defn(self, '$all?', TMP_1 = function() {
      var $a, self = this, $iter = TMP_1.$$p, block = $iter || nil;

      TMP_1.$$p = null;
      
      var result = true;

      if (block !== nil) {
        self.$each.$$p = function() {
          var value = Opal.yieldX(block, arguments);

          if (value === $breaker) {
            result = $breaker.$v;
            return $breaker;
          }

          if ((($a = value) === nil || ($a.$$is_boolean && $a == false))) {
            result = false;
            return $breaker;
          }
        };
      }
      else {
        self.$each.$$p = function(obj) {
          if (arguments.length == 1 && (($a = obj) === nil || ($a.$$is_boolean && $a == false))) {
            result = false;
            return $breaker;
          }
        };
      }

      self.$each();

      return result;
    
    });

    Opal.defn(self, '$any?', TMP_2 = function() {
      var $a, self = this, $iter = TMP_2.$$p, block = $iter || nil;

      TMP_2.$$p = null;
      
      var result = false;

      if (block !== nil) {
        self.$each.$$p = function() {
          var value = Opal.yieldX(block, arguments);

          if (value === $breaker) {
            result = $breaker.$v;
            return $breaker;
          }

          if ((($a = value) !== nil && (!$a.$$is_boolean || $a == true))) {
            result = true;
            return $breaker;
          }
        };
      }
      else {
        self.$each.$$p = function(obj) {
          if (arguments.length != 1 || (($a = obj) !== nil && (!$a.$$is_boolean || $a == true))) {
            result = true;
            return $breaker;
          }
        }
      }

      self.$each();

      return result;
    
    });

    Opal.defn(self, '$chunk', TMP_3 = function(state) {
      var $a, $b, TMP_4, self = this, $iter = TMP_3.$$p, original_block = $iter || nil;

      TMP_3.$$p = null;
      if (original_block !== false && original_block !== nil) {
        } else {
        $scope.get('Kernel').$raise($scope.get('ArgumentError'), "no block given")
      };
      return ($a = ($b = Opal.get('Enumerator')).$new, $a.$$p = (TMP_4 = function(yielder){var self = TMP_4.$$s || this, $a, $b, TMP_5;
if (yielder == null) yielder = nil;
      
        var block, previous = nil, accumulate = [];

        if (state == undefined || state === nil) {
          block = original_block;
        } else {
          block = ($a = ($b = $scope.get('Proc')).$new, $a.$$p = (TMP_5 = function(val){var self = TMP_5.$$s || this;
if (val == null) val = nil;
        return original_block.$yield(val, state.$dup())}, TMP_5.$$s = self, TMP_5), $a).call($b)
        }

        function releaseAccumulate() {
          if (accumulate.length > 0) {
            yielder.$yield(previous, accumulate)
          }
        }

        self.$each.$$p = function(value) {
          var key = Opal.yield1(block, value);

          if (key === $breaker) {
            return $breaker;
          }

          if (key === nil) {
            releaseAccumulate();
            accumulate = [];
            previous = nil;
          } else {
            if (previous === nil || previous === key) {
              accumulate.push(value);
            } else {
              releaseAccumulate();
              accumulate = [value];
            }

            previous = key;
          }
        }

        self.$each();

        releaseAccumulate();
      ;}, TMP_4.$$s = self, TMP_4), $a).call($b);
    });

    Opal.defn(self, '$collect', TMP_6 = function() {
      var $a, $b, TMP_7, self = this, $iter = TMP_6.$$p, block = $iter || nil;

      TMP_6.$$p = null;
      if ((block !== nil)) {
        } else {
        return ($a = ($b = self).$enum_for, $a.$$p = (TMP_7 = function(){var self = TMP_7.$$s || this;

        return self.$enumerator_size()}, TMP_7.$$s = self, TMP_7), $a).call($b, "collect")
      };
      
      var result = [];

      self.$each.$$p = function() {
        var value = Opal.yieldX(block, arguments);

        if (value === $breaker) {
          result = $breaker.$v;
          return $breaker;
        }

        result.push(value);
      };

      self.$each();

      return result;
    
    });

    Opal.defn(self, '$collect_concat', TMP_8 = function() {
      var $a, $b, TMP_9, $c, TMP_10, self = this, $iter = TMP_8.$$p, block = $iter || nil;

      TMP_8.$$p = null;
      if ((block !== nil)) {
        } else {
        return ($a = ($b = self).$enum_for, $a.$$p = (TMP_9 = function(){var self = TMP_9.$$s || this;

        return self.$enumerator_size()}, TMP_9.$$s = self, TMP_9), $a).call($b, "collect_concat")
      };
      return ($a = ($c = self).$map, $a.$$p = (TMP_10 = function(item){var self = TMP_10.$$s || this, $a;
if (item == null) item = nil;
      return $a = Opal.yield1(block, item), $a === $breaker ? $a : $a}, TMP_10.$$s = self, TMP_10), $a).call($c).$flatten(1);
    });

    Opal.defn(self, '$count', TMP_11 = function(object) {
      var $a, self = this, $iter = TMP_11.$$p, block = $iter || nil;

      TMP_11.$$p = null;
      
      var result = 0;

      if (object != null) {
        block = function() {
          return $scope.get('Opal').$destructure(arguments)['$=='](object);
        };
      }
      else if (block === nil) {
        block = function() { return true; };
      }

      self.$each.$$p = function() {
        var value = Opal.yieldX(block, arguments);

        if (value === $breaker) {
          result = $breaker.$v;
          return $breaker;
        }

        if ((($a = value) !== nil && (!$a.$$is_boolean || $a == true))) {
          result++;
        }
      }

      self.$each();

      return result;
    
    });

    Opal.defn(self, '$cycle', TMP_12 = function(n) {
      var $a, $b, TMP_13, self = this, $iter = TMP_12.$$p, block = $iter || nil;

      if (n == null) {
        n = nil
      }
      TMP_12.$$p = null;
      if ((block !== nil)) {
        } else {
        return ($a = ($b = self).$enum_for, $a.$$p = (TMP_13 = function(){var self = TMP_13.$$s || this, $a;

        if (n['$=='](nil)) {
            if ((($a = self['$respond_to?']("size")) !== nil && (!$a.$$is_boolean || $a == true))) {
              return (($scope.get('Float')).$$scope.get('INFINITY'))
              } else {
              return nil
            }
            } else {
            n = $scope.get('Opal')['$coerce_to!'](n, $scope.get('Integer'), "to_int");
            if ($rb_gt(n, 0)) {
              return $rb_times(self.$enumerator_size(), n)
              } else {
              return 0
            };
          }}, TMP_13.$$s = self, TMP_13), $a).call($b, "cycle", n)
      };
      if ((($a = n['$nil?']()) !== nil && (!$a.$$is_boolean || $a == true))) {
        } else {
        n = $scope.get('Opal')['$coerce_to!'](n, $scope.get('Integer'), "to_int");
        if ((($a = n <= 0) !== nil && (!$a.$$is_boolean || $a == true))) {
          return nil};
      };
      
      var result,
          all = [], i, length, value;

      self.$each.$$p = function() {
        var param = $scope.get('Opal').$destructure(arguments),
            value = Opal.yield1(block, param);

        if (value === $breaker) {
          result = $breaker.$v;
          return $breaker;
        }

        all.push(param);
      }

      self.$each();

      if (result !== undefined) {
        return result;
      }

      if (all.length === 0) {
        return nil;
      }

      if (n === nil) {
        while (true) {
          for (i = 0, length = all.length; i < length; i++) {
            value = Opal.yield1(block, all[i]);

            if (value === $breaker) {
              return $breaker.$v;
            }
          }
        }
      }
      else {
        while (n > 1) {
          for (i = 0, length = all.length; i < length; i++) {
            value = Opal.yield1(block, all[i]);

            if (value === $breaker) {
              return $breaker.$v;
            }
          }

          n--;
        }
      }
    
    });

    Opal.defn(self, '$detect', TMP_14 = function(ifnone) {
      var $a, self = this, $iter = TMP_14.$$p, block = $iter || nil;

      TMP_14.$$p = null;
      if ((block !== nil)) {
        } else {
        return self.$enum_for("detect", ifnone)
      };
      
      var result;

      self.$each.$$p = function() {
        var params = $scope.get('Opal').$destructure(arguments),
            value  = Opal.yield1(block, params);

        if (value === $breaker) {
          result = $breaker.$v;
          return $breaker;
        }

        if ((($a = value) !== nil && (!$a.$$is_boolean || $a == true))) {
          result = params;
          return $breaker;
        }
      };

      self.$each();

      if (result === undefined && ifnone !== undefined) {
        if (typeof(ifnone) === 'function') {
          result = ifnone();
        }
        else {
          result = ifnone;
        }
      }

      return result === undefined ? nil : result;
    
    });

    Opal.defn(self, '$drop', function(number) {
      var $a, self = this;

      number = $scope.get('Opal').$coerce_to(number, $scope.get('Integer'), "to_int");
      if ((($a = number < 0) !== nil && (!$a.$$is_boolean || $a == true))) {
        self.$raise($scope.get('ArgumentError'), "attempt to drop negative size")};
      
      var result  = [],
          current = 0;

      self.$each.$$p = function() {
        if (number <= current) {
          result.push($scope.get('Opal').$destructure(arguments));
        }

        current++;
      };

      self.$each()

      return result;
    
    });

    Opal.defn(self, '$drop_while', TMP_15 = function() {
      var $a, self = this, $iter = TMP_15.$$p, block = $iter || nil;

      TMP_15.$$p = null;
      if ((block !== nil)) {
        } else {
        return self.$enum_for("drop_while")
      };
      
      var result   = [],
          dropping = true;

      self.$each.$$p = function() {
        var param = $scope.get('Opal').$destructure(arguments);

        if (dropping) {
          var value = Opal.yield1(block, param);

          if (value === $breaker) {
            result = $breaker.$v;
            return $breaker;
          }

          if ((($a = value) === nil || ($a.$$is_boolean && $a == false))) {
            dropping = false;
            result.push(param);
          }
        }
        else {
          result.push(param);
        }
      };

      self.$each();

      return result;
    
    });

    Opal.defn(self, '$each_cons', TMP_16 = function(n) {
      var $a, $b, TMP_17, self = this, $iter = TMP_16.$$p, block = $iter || nil;

      TMP_16.$$p = null;
      if ((($a = arguments.length != 1) !== nil && (!$a.$$is_boolean || $a == true))) {
        self.$raise($scope.get('ArgumentError'), "wrong number of arguments (" + (arguments.length) + " for 1)")};
      n = $scope.get('Opal').$try_convert(n, $scope.get('Integer'), "to_int");
      if ((($a = n <= 0) !== nil && (!$a.$$is_boolean || $a == true))) {
        self.$raise($scope.get('ArgumentError'), "invalid size")};
      if ((block !== nil)) {
        } else {
        return ($a = ($b = self).$enum_for, $a.$$p = (TMP_17 = function(){var self = TMP_17.$$s || this, $a, $b, enum_size = nil;

        enum_size = self.$enumerator_size();
          if ((($a = enum_size['$nil?']()) !== nil && (!$a.$$is_boolean || $a == true))) {
            return nil
          } else if ((($a = ((($b = enum_size['$=='](0)) !== false && $b !== nil) ? $b : $rb_lt(enum_size, n))) !== nil && (!$a.$$is_boolean || $a == true))) {
            return 0
            } else {
            return $rb_plus($rb_minus(enum_size, n), 1)
          };}, TMP_17.$$s = self, TMP_17), $a).call($b, "each_cons", n)
      };
      
      var buffer = [], result = nil;

      self.$each.$$p = function() {
        var element = $scope.get('Opal').$destructure(arguments);
        buffer.push(element);
        if (buffer.length > n) {
          buffer.shift();
        }
        if (buffer.length == n) {
          var value = Opal.yield1(block, buffer.slice(0, n));

          if (value == $breaker) {
            result = $breaker.$v;
            return $breaker;
          }
        }
      }

      self.$each();

      return result;
    
    });

    Opal.defn(self, '$each_entry', TMP_18 = function() {
      var self = this, $iter = TMP_18.$$p, block = $iter || nil;

      TMP_18.$$p = null;
      return self.$raise($scope.get('NotImplementedError'));
    });

    Opal.defn(self, '$each_slice', TMP_19 = function(n) {
      var $a, $b, TMP_20, self = this, $iter = TMP_19.$$p, block = $iter || nil;

      TMP_19.$$p = null;
      n = $scope.get('Opal').$coerce_to(n, $scope.get('Integer'), "to_int");
      if ((($a = n <= 0) !== nil && (!$a.$$is_boolean || $a == true))) {
        self.$raise($scope.get('ArgumentError'), "invalid slice size")};
      if ((block !== nil)) {
        } else {
        return ($a = ($b = self).$enum_for, $a.$$p = (TMP_20 = function(){var self = TMP_20.$$s || this, $a;

        if ((($a = self['$respond_to?']("size")) !== nil && (!$a.$$is_boolean || $a == true))) {
            return ($rb_divide(self.$size(), n)).$ceil()
            } else {
            return nil
          }}, TMP_20.$$s = self, TMP_20), $a).call($b, "each_slice", n)
      };
      
      var result,
          slice = []

      self.$each.$$p = function() {
        var param = $scope.get('Opal').$destructure(arguments);

        slice.push(param);

        if (slice.length === n) {
          if (Opal.yield1(block, slice) === $breaker) {
            result = $breaker.$v;
            return $breaker;
          }

          slice = [];
        }
      };

      self.$each();

      if (result !== undefined) {
        return result;
      }

      // our "last" group, if smaller than n then won't have been yielded
      if (slice.length > 0) {
        if (Opal.yield1(block, slice) === $breaker) {
          return $breaker.$v;
        }
      }
    ;
      return nil;
    });

    Opal.defn(self, '$each_with_index', TMP_21 = function() {
      var $a, $b, TMP_22, self = this, $iter = TMP_21.$$p, block = $iter || nil, $splat_index = nil;

      var array_size = arguments.length - 0;
      if(array_size < 0) array_size = 0;
      var args = new Array(array_size);
      for($splat_index = 0; $splat_index < array_size; $splat_index++) {
        args[$splat_index] = arguments[$splat_index + 0];
      }
      TMP_21.$$p = null;
      if ((block !== nil)) {
        } else {
        return ($a = ($b = self).$enum_for, $a.$$p = (TMP_22 = function(){var self = TMP_22.$$s || this;

        return self.$enumerator_size()}, TMP_22.$$s = self, TMP_22), $a).apply($b, ["each_with_index"].concat(Opal.to_a(args)))
      };
      
      var result,
          index = 0;

      self.$each.$$p = function() {
        var param = $scope.get('Opal').$destructure(arguments),
            value = block(param, index);

        if (value === $breaker) {
          result = $breaker.$v;
          return $breaker;
        }

        index++;
      };

      self.$each.apply(self, args);

      if (result !== undefined) {
        return result;
      }
    
      return self;
    });

    Opal.defn(self, '$each_with_object', TMP_23 = function(object) {
      var $a, $b, TMP_24, self = this, $iter = TMP_23.$$p, block = $iter || nil;

      TMP_23.$$p = null;
      if ((block !== nil)) {
        } else {
        return ($a = ($b = self).$enum_for, $a.$$p = (TMP_24 = function(){var self = TMP_24.$$s || this;

        return self.$enumerator_size()}, TMP_24.$$s = self, TMP_24), $a).call($b, "each_with_object", object)
      };
      
      var result;

      self.$each.$$p = function() {
        var param = $scope.get('Opal').$destructure(arguments),
            value = block(param, object);

        if (value === $breaker) {
          result = $breaker.$v;
          return $breaker;
        }
      };

      self.$each();

      if (result !== undefined) {
        return result;
      }
    
      return object;
    });

    Opal.defn(self, '$entries', function() {
      var self = this, $splat_index = nil;

      var array_size = arguments.length - 0;
      if(array_size < 0) array_size = 0;
      var args = new Array(array_size);
      for($splat_index = 0; $splat_index < array_size; $splat_index++) {
        args[$splat_index] = arguments[$splat_index + 0];
      }
      
      var result = [];

      self.$each.$$p = function() {
        result.push($scope.get('Opal').$destructure(arguments));
      };

      self.$each.apply(self, args);

      return result;
    
    });

    Opal.alias(self, 'find', 'detect');

    Opal.defn(self, '$find_all', TMP_25 = function() {
      var $a, $b, TMP_26, self = this, $iter = TMP_25.$$p, block = $iter || nil;

      TMP_25.$$p = null;
      if ((block !== nil)) {
        } else {
        return ($a = ($b = self).$enum_for, $a.$$p = (TMP_26 = function(){var self = TMP_26.$$s || this;

        return self.$enumerator_size()}, TMP_26.$$s = self, TMP_26), $a).call($b, "find_all")
      };
      
      var result = [];

      self.$each.$$p = function() {
        var param = $scope.get('Opal').$destructure(arguments),
            value = Opal.yield1(block, param);

        if (value === $breaker) {
          result = $breaker.$v;
          return $breaker;
        }

        if ((($a = value) !== nil && (!$a.$$is_boolean || $a == true))) {
          result.push(param);
        }
      };

      self.$each();

      return result;
    
    });

    Opal.defn(self, '$find_index', TMP_27 = function(object) {
      var $a, self = this, $iter = TMP_27.$$p, block = $iter || nil;

      TMP_27.$$p = null;
      if ((($a = object === undefined && block === nil) !== nil && (!$a.$$is_boolean || $a == true))) {
        return self.$enum_for("find_index")};
      
      var result = nil,
          index  = 0;

      if (object != null) {
        self.$each.$$p = function() {
          var param = $scope.get('Opal').$destructure(arguments);

          if ((param)['$=='](object)) {
            result = index;
            return $breaker;
          }

          index += 1;
        };
      }
      else if (block !== nil) {
        self.$each.$$p = function() {
          var value = Opal.yieldX(block, arguments);

          if (value === $breaker) {
            result = $breaker.$v;
            return $breaker;
          }

          if ((($a = value) !== nil && (!$a.$$is_boolean || $a == true))) {
            result = index;
            return $breaker;
          }

          index += 1;
        };
      }

      self.$each();

      return result;
    
    });

    Opal.defn(self, '$first', function(number) {
      var $a, self = this, result = nil;

      if ((($a = number === undefined) !== nil && (!$a.$$is_boolean || $a == true))) {
        result = nil;
        
        self.$each.$$p = function() {
          result = $scope.get('Opal').$destructure(arguments);

          return $breaker;
        };

        self.$each();
      ;
        } else {
        result = [];
        number = $scope.get('Opal').$coerce_to(number, $scope.get('Integer'), "to_int");
        if ((($a = number < 0) !== nil && (!$a.$$is_boolean || $a == true))) {
          self.$raise($scope.get('ArgumentError'), "attempt to take negative size")};
        if ((($a = number == 0) !== nil && (!$a.$$is_boolean || $a == true))) {
          return []};
        
        var current = 0;
        number = $scope.get('Opal').$coerce_to(number, $scope.get('Integer'), "to_int");

        self.$each.$$p = function() {
          result.push($scope.get('Opal').$destructure(arguments));

          if (number <= ++current) {
            return $breaker;
          }
        };

        self.$each();
      
      };
      return result;
    });

    Opal.alias(self, 'flat_map', 'collect_concat');

    Opal.defn(self, '$grep', TMP_28 = function(pattern) {
      var $a, self = this, $iter = TMP_28.$$p, block = $iter || nil;

      TMP_28.$$p = null;
      
      var result = [];

      if (block !== nil) {
        self.$each.$$p = function() {
          var param = $scope.get('Opal').$destructure(arguments),
              value = pattern['$==='](param);

          if ((($a = value) !== nil && (!$a.$$is_boolean || $a == true))) {
            value = Opal.yield1(block, param);

            if (value === $breaker) {
              result = $breaker.$v;
              return $breaker;
            }

            result.push(value);
          }
        };
      }
      else {
        self.$each.$$p = function() {
          var param = $scope.get('Opal').$destructure(arguments),
              value = pattern['$==='](param);

          if ((($a = value) !== nil && (!$a.$$is_boolean || $a == true))) {
            result.push(param);
          }
        };
      }

      self.$each();

      return result;
    ;
    });

    Opal.defn(self, '$group_by', TMP_29 = function() {
      var $a, $b, TMP_30, $c, $d, self = this, $iter = TMP_29.$$p, block = $iter || nil, hash = nil;

      TMP_29.$$p = null;
      if ((block !== nil)) {
        } else {
        return ($a = ($b = self).$enum_for, $a.$$p = (TMP_30 = function(){var self = TMP_30.$$s || this;

        return self.$enumerator_size()}, TMP_30.$$s = self, TMP_30), $a).call($b, "group_by")
      };
      hash = $scope.get('Hash').$new();
      
      var result;

      self.$each.$$p = function() {
        var param = $scope.get('Opal').$destructure(arguments),
            value = Opal.yield1(block, param);

        if (value === $breaker) {
          result = $breaker.$v;
          return $breaker;
        }

        (($a = value, $c = hash, ((($d = $c['$[]']($a)) !== false && $d !== nil) ? $d : $c['$[]=']($a, []))))['$<<'](param);
      }

      self.$each();

      if (result !== undefined) {
        return result;
      }
    
      return hash;
    });

    Opal.defn(self, '$include?', function(obj) {
      var self = this;

      
      var result = false;

      self.$each.$$p = function() {
        var param = $scope.get('Opal').$destructure(arguments);

        if ((param)['$=='](obj)) {
          result = true;
          return $breaker;
        }
      }

      self.$each();

      return result;
    
    });

    Opal.defn(self, '$inject', TMP_31 = function(object, sym) {
      var self = this, $iter = TMP_31.$$p, block = $iter || nil;

      TMP_31.$$p = null;
      
      var result = object;

      if (block !== nil && sym === undefined) {
        self.$each.$$p = function() {
          var value = $scope.get('Opal').$destructure(arguments);

          if (result === undefined) {
            result = value;
            return;
          }

          value = Opal.yieldX(block, [result, value]);

          if (value === $breaker) {
            result = $breaker.$v;
            return $breaker;
          }

          result = value;
        };
      }
      else {
        if (sym === undefined) {
          if (!$scope.get('Symbol')['$==='](object)) {
            self.$raise($scope.get('TypeError'), "" + (object.$inspect()) + " is not a Symbol");
          }

          sym    = object;
          result = undefined;
        }

        self.$each.$$p = function() {
          var value = $scope.get('Opal').$destructure(arguments);

          if (result === undefined) {
            result = value;
            return;
          }

          result = (result).$__send__(sym, value);
        };
      }

      self.$each();

      return result == undefined ? nil : result;
    ;
    });

    Opal.defn(self, '$lazy', function() {
      var $a, $b, TMP_32, self = this;

      return ($a = ($b = (($scope.get('Enumerator')).$$scope.get('Lazy'))).$new, $a.$$p = (TMP_32 = function(enum$, args){var self = TMP_32.$$s || this, $a;
if (enum$ == null) enum$ = nil;args = $slice.call(arguments, 1);
      return ($a = enum$).$yield.apply($a, Opal.to_a(args))}, TMP_32.$$s = self, TMP_32), $a).call($b, self, self.$enumerator_size());
    });

    Opal.defn(self, '$enumerator_size', function() {
      var $a, self = this;

      if ((($a = self['$respond_to?']("size")) !== nil && (!$a.$$is_boolean || $a == true))) {
        return self.$size()
        } else {
        return nil
      };
    });

    Opal.alias(self, 'map', 'collect');

    Opal.defn(self, '$max', TMP_33 = function() {
      var self = this, $iter = TMP_33.$$p, block = $iter || nil;

      TMP_33.$$p = null;
      
      var result;

      if (block !== nil) {
        self.$each.$$p = function() {
          var param = $scope.get('Opal').$destructure(arguments);

          if (result === undefined) {
            result = param;
            return;
          }

          var value = block(param, result);

          if (value === $breaker) {
            result = $breaker.$v;
            return $breaker;
          }

          if (value === nil) {
            self.$raise($scope.get('ArgumentError'), "comparison failed");
          }

          if (value > 0) {
            result = param;
          }
        };
      }
      else {
        self.$each.$$p = function() {
          var param = $scope.get('Opal').$destructure(arguments);

          if (result === undefined) {
            result = param;
            return;
          }

          if ($scope.get('Opal').$compare(param, result) > 0) {
            result = param;
          }
        };
      }

      self.$each();

      return result === undefined ? nil : result;
    
    });

    Opal.defn(self, '$max_by', TMP_34 = function() {
      var $a, $b, TMP_35, self = this, $iter = TMP_34.$$p, block = $iter || nil;

      TMP_34.$$p = null;
      if (block !== false && block !== nil) {
        } else {
        return ($a = ($b = self).$enum_for, $a.$$p = (TMP_35 = function(){var self = TMP_35.$$s || this;

        return self.$enumerator_size()}, TMP_35.$$s = self, TMP_35), $a).call($b, "max_by")
      };
      
      var result,
          by;

      self.$each.$$p = function() {
        var param = $scope.get('Opal').$destructure(arguments),
            value = Opal.yield1(block, param);

        if (result === undefined) {
          result = param;
          by     = value;
          return;
        }

        if (value === $breaker) {
          result = $breaker.$v;
          return $breaker;
        }

        if ((value)['$<=>'](by) > 0) {
          result = param
          by     = value;
        }
      };

      self.$each();

      return result === undefined ? nil : result;
    
    });

    Opal.alias(self, 'member?', 'include?');

    Opal.defn(self, '$min', TMP_36 = function() {
      var self = this, $iter = TMP_36.$$p, block = $iter || nil;

      TMP_36.$$p = null;
      
      var result;

      if (block !== nil) {
        self.$each.$$p = function() {
          var param = $scope.get('Opal').$destructure(arguments);

          if (result === undefined) {
            result = param;
            return;
          }

          var value = block(param, result);

          if (value === $breaker) {
            result = $breaker.$v;
            return $breaker;
          }

          if (value === nil) {
            self.$raise($scope.get('ArgumentError'), "comparison failed");
          }

          if (value < 0) {
            result = param;
          }
        };
      }
      else {
        self.$each.$$p = function() {
          var param = $scope.get('Opal').$destructure(arguments);

          if (result === undefined) {
            result = param;
            return;
          }

          if ($scope.get('Opal').$compare(param, result) < 0) {
            result = param;
          }
        };
      }

      self.$each();

      return result === undefined ? nil : result;
    
    });

    Opal.defn(self, '$min_by', TMP_37 = function() {
      var $a, $b, TMP_38, self = this, $iter = TMP_37.$$p, block = $iter || nil;

      TMP_37.$$p = null;
      if (block !== false && block !== nil) {
        } else {
        return ($a = ($b = self).$enum_for, $a.$$p = (TMP_38 = function(){var self = TMP_38.$$s || this;

        return self.$enumerator_size()}, TMP_38.$$s = self, TMP_38), $a).call($b, "min_by")
      };
      
      var result,
          by;

      self.$each.$$p = function() {
        var param = $scope.get('Opal').$destructure(arguments),
            value = Opal.yield1(block, param);

        if (result === undefined) {
          result = param;
          by     = value;
          return;
        }

        if (value === $breaker) {
          result = $breaker.$v;
          return $breaker;
        }

        if ((value)['$<=>'](by) < 0) {
          result = param
          by     = value;
        }
      };

      self.$each();

      return result === undefined ? nil : result;
    
    });

    Opal.defn(self, '$minmax', TMP_39 = function() {
      var $a, $b, $c, TMP_40, self = this, $iter = TMP_39.$$p, block = $iter || nil;

      TMP_39.$$p = null;
      ((($a = block) !== false && $a !== nil) ? $a : block = ($b = ($c = self).$proc, $b.$$p = (TMP_40 = function(a, b){var self = TMP_40.$$s || this;
if (a == null) a = nil;if (b == null) b = nil;
      return a['$<=>'](b)}, TMP_40.$$s = self, TMP_40), $b).call($c));
      
      var min = nil, max = nil, first_time = true;

      self.$each.$$p = function() {
        var element = $scope.get('Opal').$destructure(arguments);
        if (first_time) {
          min = max = element;
          first_time = false;
        } else {
          var min_cmp = block.$call(min, element);

          if (min_cmp === nil) {
            self.$raise($scope.get('ArgumentError'), "comparison failed")
          } else if (min_cmp > 0) {
            min = element;
          }

          var max_cmp = block.$call(max, element);

          if (max_cmp === nil) {
            self.$raise($scope.get('ArgumentError'), "comparison failed")
          } else if (max_cmp < 0) {
            max = element;
          }
        }
      }

      self.$each();

      return [min, max];
    
    });

    Opal.defn(self, '$minmax_by', TMP_41 = function() {
      var self = this, $iter = TMP_41.$$p, block = $iter || nil;

      TMP_41.$$p = null;
      return self.$raise($scope.get('NotImplementedError'));
    });

    Opal.defn(self, '$none?', TMP_42 = function() {
      var $a, self = this, $iter = TMP_42.$$p, block = $iter || nil;

      TMP_42.$$p = null;
      
      var result = true;

      if (block !== nil) {
        self.$each.$$p = function() {
          var value = Opal.yieldX(block, arguments);

          if (value === $breaker) {
            result = $breaker.$v;
            return $breaker;
          }

          if ((($a = value) !== nil && (!$a.$$is_boolean || $a == true))) {
            result = false;
            return $breaker;
          }
        }
      }
      else {
        self.$each.$$p = function() {
          var value = $scope.get('Opal').$destructure(arguments);

          if ((($a = value) !== nil && (!$a.$$is_boolean || $a == true))) {
            result = false;
            return $breaker;
          }
        };
      }

      self.$each();

      return result;
    
    });

    Opal.defn(self, '$one?', TMP_43 = function() {
      var $a, self = this, $iter = TMP_43.$$p, block = $iter || nil;

      TMP_43.$$p = null;
      
      var result = false;

      if (block !== nil) {
        self.$each.$$p = function() {
          var value = Opal.yieldX(block, arguments);

          if (value === $breaker) {
            result = $breaker.$v;
            return $breaker;
          }

          if ((($a = value) !== nil && (!$a.$$is_boolean || $a == true))) {
            if (result === true) {
              result = false;
              return $breaker;
            }

            result = true;
          }
        }
      }
      else {
        self.$each.$$p = function() {
          var value = $scope.get('Opal').$destructure(arguments);

          if ((($a = value) !== nil && (!$a.$$is_boolean || $a == true))) {
            if (result === true) {
              result = false;
              return $breaker;
            }

            result = true;
          }
        }
      }

      self.$each();

      return result;
    
    });

    Opal.defn(self, '$partition', TMP_44 = function() {
      var $a, $b, TMP_45, self = this, $iter = TMP_44.$$p, block = $iter || nil;

      TMP_44.$$p = null;
      if ((block !== nil)) {
        } else {
        return ($a = ($b = self).$enum_for, $a.$$p = (TMP_45 = function(){var self = TMP_45.$$s || this;

        return self.$enumerator_size()}, TMP_45.$$s = self, TMP_45), $a).call($b, "partition")
      };
      
      var truthy = [], falsy = [], result;

      self.$each.$$p = function() {
        var param = $scope.get('Opal').$destructure(arguments),
            value = Opal.yield1(block, param);

        if (value === $breaker) {
          result = $breaker.$v;
          return $breaker;
        }

        if ((($a = value) !== nil && (!$a.$$is_boolean || $a == true))) {
          truthy.push(param);
        }
        else {
          falsy.push(param);
        }
      };

      self.$each();

      return [truthy, falsy];
    
    });

    Opal.alias(self, 'reduce', 'inject');

    Opal.defn(self, '$reject', TMP_46 = function() {
      var $a, $b, TMP_47, self = this, $iter = TMP_46.$$p, block = $iter || nil;

      TMP_46.$$p = null;
      if ((block !== nil)) {
        } else {
        return ($a = ($b = self).$enum_for, $a.$$p = (TMP_47 = function(){var self = TMP_47.$$s || this;

        return self.$enumerator_size()}, TMP_47.$$s = self, TMP_47), $a).call($b, "reject")
      };
      
      var result = [];

      self.$each.$$p = function() {
        var param = $scope.get('Opal').$destructure(arguments),
            value = Opal.yield1(block, param);

        if (value === $breaker) {
          result = $breaker.$v;
          return $breaker;
        }

        if ((($a = value) === nil || ($a.$$is_boolean && $a == false))) {
          result.push(param);
        }
      };

      self.$each();

      return result;
    
    });

    Opal.defn(self, '$reverse_each', TMP_48 = function() {
      var $a, $b, TMP_49, self = this, $iter = TMP_48.$$p, block = $iter || nil;

      TMP_48.$$p = null;
      if ((block !== nil)) {
        } else {
        return ($a = ($b = self).$enum_for, $a.$$p = (TMP_49 = function(){var self = TMP_49.$$s || this;

        return self.$enumerator_size()}, TMP_49.$$s = self, TMP_49), $a).call($b, "reverse_each")
      };
      
      var result = [];

      self.$each.$$p = function() {
        result.push(arguments);
      };

      self.$each();

      for (var i = result.length - 1; i >= 0; i--) {
        Opal.yieldX(block, result[i]);
      }

      return result;
    
    });

    Opal.alias(self, 'select', 'find_all');

    Opal.defn(self, '$slice_before', TMP_50 = function(pattern) {
      var $a, $b, TMP_51, self = this, $iter = TMP_50.$$p, block = $iter || nil;

      TMP_50.$$p = null;
      if ((($a = pattern === undefined && block === nil || arguments.length > 1) !== nil && (!$a.$$is_boolean || $a == true))) {
        self.$raise($scope.get('ArgumentError'), "wrong number of arguments (" + (arguments.length) + " for 1)")};
      return ($a = ($b = $scope.get('Enumerator')).$new, $a.$$p = (TMP_51 = function(e){var self = TMP_51.$$s || this, $a;
if (e == null) e = nil;
      
        var slice = [];

        if (block !== nil) {
          if (pattern === undefined) {
            self.$each.$$p = function() {
              var param = $scope.get('Opal').$destructure(arguments),
                  value = Opal.yield1(block, param);

              if ((($a = value) !== nil && (!$a.$$is_boolean || $a == true)) && slice.length > 0) {
                e['$<<'](slice);
                slice = [];
              }

              slice.push(param);
            };
          }
          else {
            self.$each.$$p = function() {
              var param = $scope.get('Opal').$destructure(arguments),
                  value = block(param, pattern.$dup());

              if ((($a = value) !== nil && (!$a.$$is_boolean || $a == true)) && slice.length > 0) {
                e['$<<'](slice);
                slice = [];
              }

              slice.push(param);
            };
          }
        }
        else {
          self.$each.$$p = function() {
            var param = $scope.get('Opal').$destructure(arguments),
                value = pattern['$==='](param);

            if ((($a = value) !== nil && (!$a.$$is_boolean || $a == true)) && slice.length > 0) {
              e['$<<'](slice);
              slice = [];
            }

            slice.push(param);
          };
        }

        self.$each();

        if (slice.length > 0) {
          e['$<<'](slice);
        }
      ;}, TMP_51.$$s = self, TMP_51), $a).call($b);
    });

    Opal.defn(self, '$sort', TMP_52 = function() {
      var $a, $b, TMP_53, self = this, $iter = TMP_52.$$p, block = $iter || nil, ary = nil;

      TMP_52.$$p = null;
      ary = self.$to_a();
      if ((block !== nil)) {
        } else {
        block = ($a = ($b = self).$lambda, $a.$$p = (TMP_53 = function(a, b){var self = TMP_53.$$s || this;
if (a == null) a = nil;if (b == null) b = nil;
        return a['$<=>'](b)}, TMP_53.$$s = self, TMP_53), $a).call($b)
      };
      return ary.sort(block);
    });

    Opal.defn(self, '$sort_by', TMP_54 = function() {
      var $a, $b, TMP_55, $c, TMP_56, $d, TMP_57, $e, TMP_58, self = this, $iter = TMP_54.$$p, block = $iter || nil, dup = nil;

      TMP_54.$$p = null;
      if ((block !== nil)) {
        } else {
        return ($a = ($b = self).$enum_for, $a.$$p = (TMP_55 = function(){var self = TMP_55.$$s || this;

        return self.$enumerator_size()}, TMP_55.$$s = self, TMP_55), $a).call($b, "sort_by")
      };
      dup = ($a = ($c = self).$map, $a.$$p = (TMP_56 = function(){var self = TMP_56.$$s || this, arg = nil;

      arg = $scope.get('Opal').$destructure(arguments);
        return [block.$call(arg), arg];}, TMP_56.$$s = self, TMP_56), $a).call($c);
      ($a = ($d = dup)['$sort!'], $a.$$p = (TMP_57 = function(a, b){var self = TMP_57.$$s || this;
if (a == null) a = nil;if (b == null) b = nil;
      return (a[0])['$<=>'](b[0])}, TMP_57.$$s = self, TMP_57), $a).call($d);
      return ($a = ($e = dup)['$map!'], $a.$$p = (TMP_58 = function(i){var self = TMP_58.$$s || this;
if (i == null) i = nil;
      return i[1];}, TMP_58.$$s = self, TMP_58), $a).call($e);
    });

    Opal.defn(self, '$take', function(num) {
      var self = this;

      return self.$first(num);
    });

    Opal.defn(self, '$take_while', TMP_59 = function() {
      var $a, self = this, $iter = TMP_59.$$p, block = $iter || nil;

      TMP_59.$$p = null;
      if (block !== false && block !== nil) {
        } else {
        return self.$enum_for("take_while")
      };
      
      var result = [];

      self.$each.$$p = function() {
        var param = $scope.get('Opal').$destructure(arguments),
            value = Opal.yield1(block, param);

        if (value === $breaker) {
          result = $breaker.$v;
          return $breaker;
        }

        if ((($a = value) === nil || ($a.$$is_boolean && $a == false))) {
          return $breaker;
        }

        result.push(param);
      };

      self.$each();

      return result;
    
    });

    Opal.alias(self, 'to_a', 'entries');

    Opal.defn(self, '$zip', TMP_60 = function() {
      var $a, self = this, $iter = TMP_60.$$p, block = $iter || nil, $splat_index = nil;

      var array_size = arguments.length - 0;
      if(array_size < 0) array_size = 0;
      var others = new Array(array_size);
      for($splat_index = 0; $splat_index < array_size; $splat_index++) {
        others[$splat_index] = arguments[$splat_index + 0];
      }
      TMP_60.$$p = null;
      return ($a = self.$to_a()).$zip.apply($a, Opal.to_a(others));
    });
  })($scope.base)
};

/* Generated by Opal 0.9.0 */
Opal.modules["corelib/enumerator"] = function(Opal) {
  Opal.dynamic_require_severity = "error";
  var OPAL_CONFIG = { method_missing: true, arity_check: false, freezing: true, tainting: true };
  function $rb_plus(lhs, rhs) {
    return (typeof(lhs) === 'number' && typeof(rhs) === 'number') ? lhs + rhs : lhs['$+'](rhs);
  }
  function $rb_lt(lhs, rhs) {
    return (typeof(lhs) === 'number' && typeof(rhs) === 'number') ? lhs < rhs : lhs['$<'](rhs);
  }
  var self = Opal.top, $scope = Opal, nil = Opal.nil, $breaker = Opal.breaker, $slice = Opal.slice, $klass = Opal.klass;

  Opal.add_stubs(['$require', '$include', '$allocate', '$new', '$to_proc', '$coerce_to', '$nil?', '$empty?', '$+', '$class', '$__send__', '$===', '$call', '$enum_for', '$size', '$destructure', '$inspect', '$[]', '$raise', '$yield', '$each', '$enumerator_size', '$respond_to?', '$try_convert', '$<', '$for']);
  self.$require("corelib/enumerable");
  return (function($base, $super) {
    function $Enumerator(){};
    var self = $Enumerator = $klass($base, $super, 'Enumerator', $Enumerator);

    var def = self.$$proto, $scope = self.$$scope, TMP_1, TMP_2, TMP_3, TMP_4;

    def.size = def.args = def.object = def.method = nil;
    self.$include($scope.get('Enumerable'));

    def.$$is_enumerator = true;

    Opal.defs(self, '$for', TMP_1 = function(object, method) {
      var self = this, $iter = TMP_1.$$p, block = $iter || nil, $splat_index = nil;

      var array_size = arguments.length - 2;
      if(array_size < 0) array_size = 0;
      var args = new Array(array_size);
      for($splat_index = 0; $splat_index < array_size; $splat_index++) {
        args[$splat_index] = arguments[$splat_index + 2];
      }
      if (method == null) {
        method = "each"
      }
      TMP_1.$$p = null;
      
      var obj = self.$allocate();

      obj.object = object;
      obj.size   = block;
      obj.method = method;
      obj.args   = args;

      return obj;
    ;
    });

    Opal.defn(self, '$initialize', TMP_2 = function() {
      var $a, $b, self = this, $iter = TMP_2.$$p, block = $iter || nil;

      TMP_2.$$p = null;
      if (block !== false && block !== nil) {
        self.object = ($a = ($b = $scope.get('Generator')).$new, $a.$$p = block.$to_proc(), $a).call($b);
        self.method = "each";
        self.args = [];
        self.size = arguments[0] || nil;
        if ((($a = self.size) !== nil && (!$a.$$is_boolean || $a == true))) {
          return self.size = $scope.get('Opal').$coerce_to(self.size, $scope.get('Integer'), "to_int")
          } else {
          return nil
        };
        } else {
        self.object = arguments[0];
        self.method = arguments[1] || "each";
        self.args = $slice.call(arguments, 2);
        return self.size = nil;
      };
    });

    Opal.defn(self, '$each', TMP_3 = function() {
      var $a, $b, $c, self = this, $iter = TMP_3.$$p, block = $iter || nil, $splat_index = nil;

      var array_size = arguments.length - 0;
      if(array_size < 0) array_size = 0;
      var args = new Array(array_size);
      for($splat_index = 0; $splat_index < array_size; $splat_index++) {
        args[$splat_index] = arguments[$splat_index + 0];
      }
      TMP_3.$$p = null;
      if ((($a = ($b = block['$nil?'](), $b !== false && $b !== nil ?args['$empty?']() : $b)) !== nil && (!$a.$$is_boolean || $a == true))) {
        return self};
      args = $rb_plus(self.args, args);
      if ((($a = block['$nil?']()) !== nil && (!$a.$$is_boolean || $a == true))) {
        return ($a = self.$class()).$new.apply($a, [self.object, self.method].concat(Opal.to_a(args)))};
      return ($b = ($c = self.object).$__send__, $b.$$p = block.$to_proc(), $b).apply($c, [self.method].concat(Opal.to_a(args)));
    });

    Opal.defn(self, '$size', function() {
      var $a, self = this;

      if ((($a = $scope.get('Proc')['$==='](self.size)) !== nil && (!$a.$$is_boolean || $a == true))) {
        return ($a = self.size).$call.apply($a, Opal.to_a(self.args))
        } else {
        return self.size
      };
    });

    Opal.defn(self, '$with_index', TMP_4 = function(offset) {
      var $a, $b, TMP_5, self = this, $iter = TMP_4.$$p, block = $iter || nil;

      if (offset == null) {
        offset = 0
      }
      TMP_4.$$p = null;
      if (offset !== false && offset !== nil) {
        offset = $scope.get('Opal').$coerce_to(offset, $scope.get('Integer'), "to_int")
        } else {
        offset = 0
      };
      if (block !== false && block !== nil) {
        } else {
        return ($a = ($b = self).$enum_for, $a.$$p = (TMP_5 = function(){var self = TMP_5.$$s || this;

        return self.$size()}, TMP_5.$$s = self, TMP_5), $a).call($b, "with_index", offset)
      };
      
      var result, index = offset;

      self.$each.$$p = function() {
        var param = $scope.get('Opal').$destructure(arguments),
            value = block(param, index);

        if (value === $breaker) {
          result = $breaker.$v;
          return $breaker;
        }

        index++;

        return value;
      }

      self.$each();

      if (result !== undefined) {
        return result;
      }

      return self.object;
    
    });

    Opal.alias(self, 'with_object', 'each_with_object');

    Opal.defn(self, '$inspect', function() {
      var $a, self = this, result = nil;

      result = "#<" + (self.$class()) + ": " + (self.object.$inspect()) + ":" + (self.method);
      if ((($a = self.args['$empty?']()) !== nil && (!$a.$$is_boolean || $a == true))) {
        } else {
        result = $rb_plus(result, "(" + (self.args.$inspect()['$[]']($scope.get('Range').$new(1, -2))) + ")")
      };
      return $rb_plus(result, ">");
    });

    (function($base, $super) {
      function $Generator(){};
      var self = $Generator = $klass($base, $super, 'Generator', $Generator);

      var def = self.$$proto, $scope = self.$$scope, TMP_6, TMP_7;

      def.block = nil;
      self.$include($scope.get('Enumerable'));

      Opal.defn(self, '$initialize', TMP_6 = function() {
        var self = this, $iter = TMP_6.$$p, block = $iter || nil;

        TMP_6.$$p = null;
        if (block !== false && block !== nil) {
          } else {
          self.$raise($scope.get('LocalJumpError'), "no block given")
        };
        return self.block = block;
      });

      return (Opal.defn(self, '$each', TMP_7 = function() {
        var $a, $b, self = this, $iter = TMP_7.$$p, block = $iter || nil, yielder = nil, $splat_index = nil;

        var array_size = arguments.length - 0;
        if(array_size < 0) array_size = 0;
        var args = new Array(array_size);
        for($splat_index = 0; $splat_index < array_size; $splat_index++) {
          args[$splat_index] = arguments[$splat_index + 0];
        }
        TMP_7.$$p = null;
        yielder = ($a = ($b = $scope.get('Yielder')).$new, $a.$$p = block.$to_proc(), $a).call($b);
        
        try {
          args.unshift(yielder);

          if (Opal.yieldX(self.block, args) === $breaker) {
            return $breaker.$v;
          }
        }
        catch (e) {
          if (e === $breaker) {
            return $breaker.$v;
          }
          else {
            throw e;
          }
        }
      ;
        return self;
      }), nil) && 'each';
    })($scope.base, null);

    (function($base, $super) {
      function $Yielder(){};
      var self = $Yielder = $klass($base, $super, 'Yielder', $Yielder);

      var def = self.$$proto, $scope = self.$$scope, TMP_8;

      def.block = nil;
      Opal.defn(self, '$initialize', TMP_8 = function() {
        var self = this, $iter = TMP_8.$$p, block = $iter || nil;

        TMP_8.$$p = null;
        return self.block = block;
      });

      Opal.defn(self, '$yield', function() {
        var self = this, $splat_index = nil;

        var array_size = arguments.length - 0;
        if(array_size < 0) array_size = 0;
        var values = new Array(array_size);
        for($splat_index = 0; $splat_index < array_size; $splat_index++) {
          values[$splat_index] = arguments[$splat_index + 0];
        }
        
        var value = Opal.yieldX(self.block, values);

        if (value === $breaker) {
          throw $breaker;
        }

        return value;
      ;
      });

      return (Opal.defn(self, '$<<', function() {
        var $a, self = this, $splat_index = nil;

        var array_size = arguments.length - 0;
        if(array_size < 0) array_size = 0;
        var values = new Array(array_size);
        for($splat_index = 0; $splat_index < array_size; $splat_index++) {
          values[$splat_index] = arguments[$splat_index + 0];
        }
        ($a = self).$yield.apply($a, Opal.to_a(values));
        return self;
      }), nil) && '<<';
    })($scope.base, null);

    return (function($base, $super) {
      function $Lazy(){};
      var self = $Lazy = $klass($base, $super, 'Lazy', $Lazy);

      var def = self.$$proto, $scope = self.$$scope, TMP_9, TMP_12, TMP_14, TMP_19, TMP_21, TMP_22, TMP_24, TMP_27, TMP_30;

      def.enumerator = nil;
      (function($base, $super) {
        function $StopLazyError(){};
        var self = $StopLazyError = $klass($base, $super, 'StopLazyError', $StopLazyError);

        var def = self.$$proto, $scope = self.$$scope;

        return nil;
      })($scope.base, $scope.get('Exception'));

      Opal.defn(self, '$initialize', TMP_9 = function(object, size) {
        var TMP_10, self = this, $iter = TMP_9.$$p, block = $iter || nil;

        if (size == null) {
          size = nil
        }
        TMP_9.$$p = null;
        if ((block !== nil)) {
          } else {
          self.$raise($scope.get('ArgumentError'), "tried to call lazy new without a block")
        };
        self.enumerator = object;
        return Opal.find_super_dispatcher(self, 'initialize', TMP_9, (TMP_10 = function(yielder, each_args){var self = TMP_10.$$s || this, $a, $b, TMP_11;
if (yielder == null) yielder = nil;each_args = $slice.call(arguments, 1);
        try {
          return ($a = ($b = object).$each, $a.$$p = (TMP_11 = function(args){var self = TMP_11.$$s || this;
args = $slice.call(arguments, 0);
            
              args.unshift(yielder);

              if (Opal.yieldX(block, args) === $breaker) {
                return $breaker;
              }
            ;}, TMP_11.$$s = self, TMP_11), $a).apply($b, Opal.to_a(each_args))
          } catch ($err) {if (Opal.rescue($err, [$scope.get('Exception')])) {
            try {
              return nil
            } finally {
              Opal.gvars["!"] = Opal.exceptions.pop() || Opal.nil;
            }
            }else { throw $err; }
          }}, TMP_10.$$s = self, TMP_10)).apply(self, [size]);
      });

      Opal.alias(self, 'force', 'to_a');

      Opal.defn(self, '$lazy', function() {
        var self = this;

        return self;
      });

      Opal.defn(self, '$collect', TMP_12 = function() {
        var $a, $b, TMP_13, self = this, $iter = TMP_12.$$p, block = $iter || nil;

        TMP_12.$$p = null;
        if (block !== false && block !== nil) {
          } else {
          self.$raise($scope.get('ArgumentError'), "tried to call lazy map without a block")
        };
        return ($a = ($b = $scope.get('Lazy')).$new, $a.$$p = (TMP_13 = function(enum$, args){var self = TMP_13.$$s || this;
if (enum$ == null) enum$ = nil;args = $slice.call(arguments, 1);
        
          var value = Opal.yieldX(block, args);

          if (value === $breaker) {
            return $breaker;
          }

          enum$.$yield(value);
        }, TMP_13.$$s = self, TMP_13), $a).call($b, self, self.$enumerator_size());
      });

      Opal.defn(self, '$collect_concat', TMP_14 = function() {
        var $a, $b, TMP_15, self = this, $iter = TMP_14.$$p, block = $iter || nil;

        TMP_14.$$p = null;
        if (block !== false && block !== nil) {
          } else {
          self.$raise($scope.get('ArgumentError'), "tried to call lazy map without a block")
        };
        return ($a = ($b = $scope.get('Lazy')).$new, $a.$$p = (TMP_15 = function(enum$, args){var self = TMP_15.$$s || this, $a, $b, TMP_16, $c, TMP_17;
if (enum$ == null) enum$ = nil;args = $slice.call(arguments, 1);
        
          var value = Opal.yieldX(block, args);

          if (value === $breaker) {
            return $breaker;
          }

          if ((value)['$respond_to?']("force") && (value)['$respond_to?']("each")) {
            ($a = ($b = (value)).$each, $a.$$p = (TMP_16 = function(v){var self = TMP_16.$$s || this;
if (v == null) v = nil;
          return enum$.$yield(v)}, TMP_16.$$s = self, TMP_16), $a).call($b)
          }
          else {
            var array = $scope.get('Opal').$try_convert(value, $scope.get('Array'), "to_ary");

            if (array === nil) {
              enum$.$yield(value);
            }
            else {
              ($a = ($c = (value)).$each, $a.$$p = (TMP_17 = function(v){var self = TMP_17.$$s || this;
if (v == null) v = nil;
          return enum$.$yield(v)}, TMP_17.$$s = self, TMP_17), $a).call($c);
            }
          }
        ;}, TMP_15.$$s = self, TMP_15), $a).call($b, self, nil);
      });

      Opal.defn(self, '$drop', function(n) {
        var $a, $b, TMP_18, self = this, current_size = nil, set_size = nil, dropped = nil;

        n = $scope.get('Opal').$coerce_to(n, $scope.get('Integer'), "to_int");
        if ($rb_lt(n, 0)) {
          self.$raise($scope.get('ArgumentError'), "attempt to drop negative size")};
        current_size = self.$enumerator_size();
        set_size = (function() {if ((($a = $scope.get('Integer')['$==='](current_size)) !== nil && (!$a.$$is_boolean || $a == true))) {
          if ($rb_lt(n, current_size)) {
            return n
            } else {
            return current_size
          }
          } else {
          return current_size
        }; return nil; })();
        dropped = 0;
        return ($a = ($b = $scope.get('Lazy')).$new, $a.$$p = (TMP_18 = function(enum$, args){var self = TMP_18.$$s || this, $a;
if (enum$ == null) enum$ = nil;args = $slice.call(arguments, 1);
        if ($rb_lt(dropped, n)) {
            return dropped = $rb_plus(dropped, 1)
            } else {
            return ($a = enum$).$yield.apply($a, Opal.to_a(args))
          }}, TMP_18.$$s = self, TMP_18), $a).call($b, self, set_size);
      });

      Opal.defn(self, '$drop_while', TMP_19 = function() {
        var $a, $b, TMP_20, self = this, $iter = TMP_19.$$p, block = $iter || nil, succeeding = nil;

        TMP_19.$$p = null;
        if (block !== false && block !== nil) {
          } else {
          self.$raise($scope.get('ArgumentError'), "tried to call lazy drop_while without a block")
        };
        succeeding = true;
        return ($a = ($b = $scope.get('Lazy')).$new, $a.$$p = (TMP_20 = function(enum$, args){var self = TMP_20.$$s || this, $a, $b;
if (enum$ == null) enum$ = nil;args = $slice.call(arguments, 1);
        if (succeeding !== false && succeeding !== nil) {
            
            var value = Opal.yieldX(block, args);

            if (value === $breaker) {
              return $breaker;
            }

            if ((($a = value) === nil || ($a.$$is_boolean && $a == false))) {
              succeeding = false;

              ($a = enum$).$yield.apply($a, Opal.to_a(args));
            }
          
            } else {
            return ($b = enum$).$yield.apply($b, Opal.to_a(args))
          }}, TMP_20.$$s = self, TMP_20), $a).call($b, self, nil);
      });

      Opal.defn(self, '$enum_for', TMP_21 = function(method) {
        var $a, $b, self = this, $iter = TMP_21.$$p, block = $iter || nil, $splat_index = nil;

        var array_size = arguments.length - 1;
        if(array_size < 0) array_size = 0;
        var args = new Array(array_size);
        for($splat_index = 0; $splat_index < array_size; $splat_index++) {
          args[$splat_index] = arguments[$splat_index + 1];
        }
        if (method == null) {
          method = "each"
        }
        TMP_21.$$p = null;
        return ($a = ($b = self.$class()).$for, $a.$$p = block.$to_proc(), $a).apply($b, [self, method].concat(Opal.to_a(args)));
      });

      Opal.defn(self, '$find_all', TMP_22 = function() {
        var $a, $b, TMP_23, self = this, $iter = TMP_22.$$p, block = $iter || nil;

        TMP_22.$$p = null;
        if (block !== false && block !== nil) {
          } else {
          self.$raise($scope.get('ArgumentError'), "tried to call lazy select without a block")
        };
        return ($a = ($b = $scope.get('Lazy')).$new, $a.$$p = (TMP_23 = function(enum$, args){var self = TMP_23.$$s || this, $a;
if (enum$ == null) enum$ = nil;args = $slice.call(arguments, 1);
        
          var value = Opal.yieldX(block, args);

          if (value === $breaker) {
            return $breaker;
          }

          if ((($a = value) !== nil && (!$a.$$is_boolean || $a == true))) {
            ($a = enum$).$yield.apply($a, Opal.to_a(args));
          }
        ;}, TMP_23.$$s = self, TMP_23), $a).call($b, self, nil);
      });

      Opal.alias(self, 'flat_map', 'collect_concat');

      Opal.defn(self, '$grep', TMP_24 = function(pattern) {
        var $a, $b, TMP_25, $c, TMP_26, self = this, $iter = TMP_24.$$p, block = $iter || nil;

        TMP_24.$$p = null;
        if (block !== false && block !== nil) {
          return ($a = ($b = $scope.get('Lazy')).$new, $a.$$p = (TMP_25 = function(enum$, args){var self = TMP_25.$$s || this, $a;
if (enum$ == null) enum$ = nil;args = $slice.call(arguments, 1);
          
            var param = $scope.get('Opal').$destructure(args),
                value = pattern['$==='](param);

            if ((($a = value) !== nil && (!$a.$$is_boolean || $a == true))) {
              value = Opal.yield1(block, param);

              if (value === $breaker) {
                return $breaker;
              }

              enum$.$yield(Opal.yield1(block, param));
            }
          ;}, TMP_25.$$s = self, TMP_25), $a).call($b, self, nil)
          } else {
          return ($a = ($c = $scope.get('Lazy')).$new, $a.$$p = (TMP_26 = function(enum$, args){var self = TMP_26.$$s || this, $a;
if (enum$ == null) enum$ = nil;args = $slice.call(arguments, 1);
          
            var param = $scope.get('Opal').$destructure(args),
                value = pattern['$==='](param);

            if ((($a = value) !== nil && (!$a.$$is_boolean || $a == true))) {
              enum$.$yield(param);
            }
          ;}, TMP_26.$$s = self, TMP_26), $a).call($c, self, nil)
        };
      });

      Opal.alias(self, 'map', 'collect');

      Opal.alias(self, 'select', 'find_all');

      Opal.defn(self, '$reject', TMP_27 = function() {
        var $a, $b, TMP_28, self = this, $iter = TMP_27.$$p, block = $iter || nil;

        TMP_27.$$p = null;
        if (block !== false && block !== nil) {
          } else {
          self.$raise($scope.get('ArgumentError'), "tried to call lazy reject without a block")
        };
        return ($a = ($b = $scope.get('Lazy')).$new, $a.$$p = (TMP_28 = function(enum$, args){var self = TMP_28.$$s || this, $a;
if (enum$ == null) enum$ = nil;args = $slice.call(arguments, 1);
        
          var value = Opal.yieldX(block, args);

          if (value === $breaker) {
            return $breaker;
          }

          if ((($a = value) === nil || ($a.$$is_boolean && $a == false))) {
            ($a = enum$).$yield.apply($a, Opal.to_a(args));
          }
        ;}, TMP_28.$$s = self, TMP_28), $a).call($b, self, nil);
      });

      Opal.defn(self, '$take', function(n) {
        var $a, $b, TMP_29, self = this, current_size = nil, set_size = nil, taken = nil;

        n = $scope.get('Opal').$coerce_to(n, $scope.get('Integer'), "to_int");
        if ($rb_lt(n, 0)) {
          self.$raise($scope.get('ArgumentError'), "attempt to take negative size")};
        current_size = self.$enumerator_size();
        set_size = (function() {if ((($a = $scope.get('Integer')['$==='](current_size)) !== nil && (!$a.$$is_boolean || $a == true))) {
          if ($rb_lt(n, current_size)) {
            return n
            } else {
            return current_size
          }
          } else {
          return current_size
        }; return nil; })();
        taken = 0;
        return ($a = ($b = $scope.get('Lazy')).$new, $a.$$p = (TMP_29 = function(enum$, args){var self = TMP_29.$$s || this, $a;
if (enum$ == null) enum$ = nil;args = $slice.call(arguments, 1);
        if ($rb_lt(taken, n)) {
            ($a = enum$).$yield.apply($a, Opal.to_a(args));
            return taken = $rb_plus(taken, 1);
            } else {
            return self.$raise($scope.get('StopLazyError'))
          }}, TMP_29.$$s = self, TMP_29), $a).call($b, self, set_size);
      });

      Opal.defn(self, '$take_while', TMP_30 = function() {
        var $a, $b, TMP_31, self = this, $iter = TMP_30.$$p, block = $iter || nil;

        TMP_30.$$p = null;
        if (block !== false && block !== nil) {
          } else {
          self.$raise($scope.get('ArgumentError'), "tried to call lazy take_while without a block")
        };
        return ($a = ($b = $scope.get('Lazy')).$new, $a.$$p = (TMP_31 = function(enum$, args){var self = TMP_31.$$s || this, $a;
if (enum$ == null) enum$ = nil;args = $slice.call(arguments, 1);
        
          var value = Opal.yieldX(block, args);

          if (value === $breaker) {
            return $breaker;
          }

          if ((($a = value) !== nil && (!$a.$$is_boolean || $a == true))) {
            ($a = enum$).$yield.apply($a, Opal.to_a(args));
          }
          else {
            self.$raise($scope.get('StopLazyError'));
          }
        ;}, TMP_31.$$s = self, TMP_31), $a).call($b, self, nil);
      });

      Opal.alias(self, 'to_enum', 'enum_for');

      return (Opal.defn(self, '$inspect', function() {
        var self = this;

        return "#<" + (self.$class()) + ": " + (self.enumerator.$inspect()) + ">";
      }), nil) && 'inspect';
    })($scope.base, self);
  })($scope.base, null);
};

/* Generated by Opal 0.9.0 */
Opal.modules["corelib/numeric"] = function(Opal) {
  Opal.dynamic_require_severity = "error";
  var OPAL_CONFIG = { method_missing: true, arity_check: false, freezing: true, tainting: true };
  function $rb_minus(lhs, rhs) {
    return (typeof(lhs) === 'number' && typeof(rhs) === 'number') ? lhs - rhs : lhs['$-'](rhs);
  }
  function $rb_times(lhs, rhs) {
    return (typeof(lhs) === 'number' && typeof(rhs) === 'number') ? lhs * rhs : lhs['$*'](rhs);
  }
  function $rb_lt(lhs, rhs) {
    return (typeof(lhs) === 'number' && typeof(rhs) === 'number') ? lhs < rhs : lhs['$<'](rhs);
  }
  function $rb_divide(lhs, rhs) {
    return (typeof(lhs) === 'number' && typeof(rhs) === 'number') ? lhs / rhs : lhs['$/'](rhs);
  }
  function $rb_gt(lhs, rhs) {
    return (typeof(lhs) === 'number' && typeof(rhs) === 'number') ? lhs > rhs : lhs['$>'](rhs);
  }
  var self = Opal.top, $scope = Opal, nil = Opal.nil, $breaker = Opal.breaker, $slice = Opal.slice, $klass = Opal.klass;

  Opal.add_stubs(['$require', '$include', '$instance_of?', '$class', '$Float', '$coerce', '$===', '$raise', '$__send__', '$equal?', '$coerce_to!', '$-@', '$**', '$-', '$*', '$div', '$<', '$ceil', '$to_f', '$denominator', '$to_r', '$==', '$floor', '$/', '$%', '$Complex', '$zero?', '$numerator', '$abs', '$arg', '$round', '$to_i', '$truncate', '$>']);
  self.$require("corelib/comparable");
  return (function($base, $super) {
    function $Numeric(){};
    var self = $Numeric = $klass($base, $super, 'Numeric', $Numeric);

    var def = self.$$proto, $scope = self.$$scope;

    self.$include($scope.get('Comparable'));

    Opal.defn(self, '$coerce', function(other) {
      var $a, self = this;

      if ((($a = other['$instance_of?'](self.$class())) !== nil && (!$a.$$is_boolean || $a == true))) {
        return [other, self]};
      return [self.$Float(other), self.$Float(self)];
    });

    Opal.defn(self, '$__coerced__', function(method, other) {
      var $a, $b, self = this, a = nil, b = nil, $case = nil;

      try {
      $b = other.$coerce(self), $a = Opal.to_ary($b), a = ($a[0] == null ? nil : $a[0]), b = ($a[1] == null ? nil : $a[1]), $b
      } catch ($err) {if (true) {
        try {
          $case = method;if ("+"['$===']($case) || "-"['$===']($case) || "*"['$===']($case) || "/"['$===']($case) || "%"['$===']($case) || "&"['$===']($case) || "|"['$===']($case) || "^"['$===']($case) || "**"['$===']($case)) {self.$raise($scope.get('TypeError'), "" + (other.$class()) + " can't be coerce into Numeric")}else if (">"['$===']($case) || ">="['$===']($case) || "<"['$===']($case) || "<="['$===']($case) || "<=>"['$===']($case)) {self.$raise($scope.get('ArgumentError'), "comparison of " + (self.$class()) + " with " + (other.$class()) + " failed")}
        } finally {
          Opal.gvars["!"] = Opal.exceptions.pop() || Opal.nil;
        }
        }else { throw $err; }
      };
      return a.$__send__(method, b);
    });

    Opal.defn(self, '$<=>', function(other) {
      var $a, self = this;

      if ((($a = self['$equal?'](other)) !== nil && (!$a.$$is_boolean || $a == true))) {
        return 0};
      return nil;
    });

    Opal.defn(self, '$[]', function(bit) {
      var self = this, min = nil, max = nil;

      bit = $scope.get('Opal')['$coerce_to!'](bit, $scope.get('Integer'), "to_int");
      min = ((2)['$**'](30))['$-@']();
      max = $rb_minus(((2)['$**'](30)), 1);
      return (bit < min || bit > max) ? 0 : (self >> bit) % 2;
    });

    Opal.defn(self, '$+@', function() {
      var self = this;

      return self;
    });

    Opal.defn(self, '$-@', function() {
      var self = this;

      return $rb_minus(0, self);
    });

    Opal.defn(self, '$%', function(other) {
      var self = this;

      return $rb_minus(self, $rb_times(other, self.$div(other)));
    });

    Opal.defn(self, '$abs', function() {
      var self = this;

      if ($rb_lt(self, 0)) {
        return self['$-@']()
        } else {
        return self
      };
    });

    Opal.defn(self, '$abs2', function() {
      var self = this;

      return $rb_times(self, self);
    });

    Opal.defn(self, '$angle', function() {
      var self = this;

      if ($rb_lt(self, 0)) {
        return (($scope.get('Math')).$$scope.get('PI'))
        } else {
        return 0
      };
    });

    Opal.alias(self, 'arg', 'angle');

    Opal.defn(self, '$ceil', function() {
      var self = this;

      return self.$to_f().$ceil();
    });

    Opal.defn(self, '$conj', function() {
      var self = this;

      return self;
    });

    Opal.alias(self, 'conjugate', 'conj');

    Opal.defn(self, '$denominator', function() {
      var self = this;

      return self.$to_r().$denominator();
    });

    Opal.defn(self, '$div', function(other) {
      var self = this;

      if (other['$=='](0)) {
        self.$raise($scope.get('ZeroDivisionError'), "divided by o")};
      return ($rb_divide(self, other)).$floor();
    });

    Opal.defn(self, '$divmod', function(other) {
      var self = this;

      return [self.$div(other), self['$%'](other)];
    });

    Opal.defn(self, '$fdiv', function(other) {
      var self = this;

      return $rb_divide(self.$to_f(), other);
    });

    Opal.defn(self, '$floor', function() {
      var self = this;

      return self.$to_f().$floor();
    });

    Opal.defn(self, '$i', function() {
      var self = this;

      return self.$Complex(0, self);
    });

    Opal.defn(self, '$imag', function() {
      var self = this;

      return 0;
    });

    Opal.alias(self, 'imaginary', 'imag');

    Opal.defn(self, '$integer?', function() {
      var self = this;

      return false;
    });

    Opal.alias(self, 'magnitude', 'abs');

    Opal.alias(self, 'modulo', '%');

    Opal.defn(self, '$nonzero?', function() {
      var $a, self = this;

      if ((($a = self['$zero?']()) !== nil && (!$a.$$is_boolean || $a == true))) {
        return nil
        } else {
        return self
      };
    });

    Opal.defn(self, '$numerator', function() {
      var self = this;

      return self.$to_r().$numerator();
    });

    Opal.alias(self, 'phase', 'arg');

    Opal.defn(self, '$polar', function() {
      var self = this;

      return [self.$abs(), self.$arg()];
    });

    Opal.defn(self, '$quo', function(other) {
      var self = this;

      return $rb_divide($scope.get('Opal')['$coerce_to!'](self, $scope.get('Rational'), "to_r"), other);
    });

    Opal.defn(self, '$real', function() {
      var self = this;

      return self;
    });

    Opal.defn(self, '$real?', function() {
      var self = this;

      return true;
    });

    Opal.defn(self, '$rect', function() {
      var self = this;

      return [self, 0];
    });

    Opal.alias(self, 'rectangular', 'rect');

    Opal.defn(self, '$round', function(digits) {
      var self = this;

      return self.$to_f().$round(digits);
    });

    Opal.defn(self, '$to_c', function() {
      var self = this;

      return self.$Complex(self, 0);
    });

    Opal.defn(self, '$to_int', function() {
      var self = this;

      return self.$to_i();
    });

    Opal.defn(self, '$truncate', function() {
      var self = this;

      return self.$to_f().$truncate();
    });

    Opal.defn(self, '$zero?', function() {
      var self = this;

      return self['$=='](0);
    });

    Opal.defn(self, '$positive?', function() {
      var self = this;

      return $rb_gt(self, 0);
    });

    Opal.defn(self, '$negative?', function() {
      var self = this;

      return $rb_lt(self, 0);
    });

    Opal.defn(self, '$dup', function() {
      var self = this;

      return self.$raise($scope.get('TypeError'), "can't dup " + (self.$class()));
    });

    return (Opal.defn(self, '$clone', function() {
      var self = this;

      return self.$raise($scope.get('TypeError'), "can't clone " + (self.$class()));
    }), nil) && 'clone';
  })($scope.base, null);
};

/* Generated by Opal 0.9.0 */
Opal.modules["corelib/array"] = function(Opal) {
  Opal.dynamic_require_severity = "error";
  var OPAL_CONFIG = { method_missing: true, arity_check: false, freezing: true, tainting: true };
  function $rb_gt(lhs, rhs) {
    return (typeof(lhs) === 'number' && typeof(rhs) === 'number') ? lhs > rhs : lhs['$>'](rhs);
  }
  function $rb_times(lhs, rhs) {
    return (typeof(lhs) === 'number' && typeof(rhs) === 'number') ? lhs * rhs : lhs['$*'](rhs);
  }
  function $rb_lt(lhs, rhs) {
    return (typeof(lhs) === 'number' && typeof(rhs) === 'number') ? lhs < rhs : lhs['$<'](rhs);
  }
  var self = Opal.top, $scope = Opal, nil = Opal.nil, $breaker = Opal.breaker, $slice = Opal.slice, $klass = Opal.klass, $hash2 = Opal.hash2, $gvars = Opal.gvars;

  Opal.add_stubs(['$require', '$include', '$raise', '$===', '$to_a', '$respond_to?', '$to_ary', '$coerce_to', '$initialize', '$to_proc', '$coerce_to?', '$join', '$to_str', '$class', '$clone', '$hash', '$<=>', '$==', '$object_id', '$inspect', '$enum_for', '$coerce_to!', '$>', '$*', '$enumerator_size', '$empty?', '$copy_singleton_methods', '$initialize_clone', '$initialize_dup', '$replace', '$size', '$eql?', '$length', '$begin', '$end', '$exclude_end?', '$flatten', '$__id__', '$[]', '$to_s', '$new', '$!', '$delete_if', '$each', '$reverse', '$rotate', '$rand', '$at', '$keep_if', '$shuffle!', '$dup', '$<', '$sort', '$!=', '$times', '$[]=', '$<<', '$kind_of?', '$last', '$first', '$upto']);
  self.$require("corelib/enumerable");
  self.$require("corelib/numeric");
  return (function($base, $super) {
    function $Array(){};
    var self = $Array = $klass($base, $super, 'Array', $Array);

    var def = self.$$proto, $scope = self.$$scope, TMP_1, TMP_2, TMP_3, TMP_4, TMP_6, TMP_8, TMP_10, TMP_12, TMP_13, TMP_15, TMP_17, TMP_19, TMP_20, TMP_21, TMP_22, TMP_24, TMP_26, TMP_27, TMP_29, TMP_31, TMP_33, TMP_34, TMP_36, TMP_38, TMP_39, TMP_40, TMP_43, TMP_44, TMP_47;

    def.length = nil;
    self.$include($scope.get('Enumerable'));

    def.$$is_array = true;

    Opal.defs(self, '$[]', function() {
      var self = this, $splat_index = nil;

      var array_size = arguments.length - 0;
      if(array_size < 0) array_size = 0;
      var objects = new Array(array_size);
      for($splat_index = 0; $splat_index < array_size; $splat_index++) {
        objects[$splat_index] = arguments[$splat_index + 0];
      }
      return objects;
    });

    Opal.defn(self, '$initialize', TMP_1 = function(size, obj) {
      var $a, self = this, $iter = TMP_1.$$p, block = $iter || nil;

      if (size == null) {
        size = nil
      }
      if (obj == null) {
        obj = nil
      }
      TMP_1.$$p = null;
      if ((($a = arguments.length > 2) !== nil && (!$a.$$is_boolean || $a == true))) {
        self.$raise($scope.get('ArgumentError'), "wrong number of arguments (" + (arguments.length) + " for 0..2)")};
      
      if (arguments.length === 0) {
        self.splice(0, self.length);
        return self;
      }
    
      if ((($a = arguments.length === 1) !== nil && (!$a.$$is_boolean || $a == true))) {
        if ((($a = $scope.get('Array')['$==='](size)) !== nil && (!$a.$$is_boolean || $a == true))) {
          return size.$to_a()
        } else if ((($a = size['$respond_to?']("to_ary")) !== nil && (!$a.$$is_boolean || $a == true))) {
          return size.$to_ary()}};
      size = $scope.get('Opal').$coerce_to(size, $scope.get('Integer'), "to_int");
      if ((($a = size < 0) !== nil && (!$a.$$is_boolean || $a == true))) {
        self.$raise($scope.get('ArgumentError'), "negative array size")};
      
      self.splice(0, self.length);
      var i, value;

      if (block === nil) {
        for (i = 0; i < size; i++) {
          self.push(obj);
        }
      }
      else {
        for (i = 0, value; i < size; i++) {
          value = block(i);

          if (value === $breaker) {
            return $breaker.$v;
          }

          self[i] = value;
        }
      }

      return self;
    
    });

    Opal.defs(self, '$new', TMP_2 = function() {
      var $a, $b, self = this, $iter = TMP_2.$$p, block = $iter || nil, $splat_index = nil;

      var array_size = arguments.length - 0;
      if(array_size < 0) array_size = 0;
      var args = new Array(array_size);
      for($splat_index = 0; $splat_index < array_size; $splat_index++) {
        args[$splat_index] = arguments[$splat_index + 0];
      }
      TMP_2.$$p = null;
      return ($a = ($b = []).$initialize, $a.$$p = block.$to_proc(), $a).apply($b, Opal.to_a(args));
    });

    Opal.defs(self, '$try_convert', function(obj) {
      var self = this;

      return $scope.get('Opal')['$coerce_to?'](obj, $scope.get('Array'), "to_ary");
    });

    Opal.defn(self, '$&', function(other) {
      var $a, self = this;

      if ((($a = $scope.get('Array')['$==='](other)) !== nil && (!$a.$$is_boolean || $a == true))) {
        other = other.$to_a()
        } else {
        other = $scope.get('Opal').$coerce_to(other, $scope.get('Array'), "to_ary").$to_a()
      };
      
      var result = [], hash = $hash2([], {}), i, length, item;

      for (i = 0, length = other.length; i < length; i++) {
        Opal.hash_put(hash, other[i], true);
      }

      for (i = 0, length = self.length; i < length; i++) {
        item = self[i];
        if (Opal.hash_delete(hash, item) !== undefined) {
          result.push(item);
        }
      }

      return result;
    ;
    });

    Opal.defn(self, '$|', function(other) {
      var $a, self = this;

      if ((($a = $scope.get('Array')['$==='](other)) !== nil && (!$a.$$is_boolean || $a == true))) {
        other = other.$to_a()
        } else {
        other = $scope.get('Opal').$coerce_to(other, $scope.get('Array'), "to_ary").$to_a()
      };
      
      var hash = $hash2([], {}), i, length, item;

      for (i = 0, length = self.length; i < length; i++) {
        Opal.hash_put(hash, self[i], true);
      }

      for (i = 0, length = other.length; i < length; i++) {
        Opal.hash_put(hash, other[i], true);
      }

      return hash.$keys();
    ;
    });

    Opal.defn(self, '$*', function(other) {
      var $a, self = this;

      if ((($a = other['$respond_to?']("to_str")) !== nil && (!$a.$$is_boolean || $a == true))) {
        return self.$join(other.$to_str())};
      if ((($a = other['$respond_to?']("to_int")) !== nil && (!$a.$$is_boolean || $a == true))) {
        } else {
        self.$raise($scope.get('TypeError'), "no implicit conversion of " + (other.$class()) + " into Integer")
      };
      other = $scope.get('Opal').$coerce_to(other, $scope.get('Integer'), "to_int");
      if ((($a = other < 0) !== nil && (!$a.$$is_boolean || $a == true))) {
        self.$raise($scope.get('ArgumentError'), "negative argument")};
      
      var result = [];

      for (var i = 0; i < other; i++) {
        result = result.concat(self);
      }

      return result;
    
    });

    Opal.defn(self, '$+', function(other) {
      var $a, self = this;

      if ((($a = $scope.get('Array')['$==='](other)) !== nil && (!$a.$$is_boolean || $a == true))) {
        other = other.$to_a()
        } else {
        other = $scope.get('Opal').$coerce_to(other, $scope.get('Array'), "to_ary").$to_a()
      };
      return self.concat(other);
    });

    Opal.defn(self, '$-', function(other) {
      var $a, self = this;

      if ((($a = $scope.get('Array')['$==='](other)) !== nil && (!$a.$$is_boolean || $a == true))) {
        other = other.$to_a()
        } else {
        other = $scope.get('Opal').$coerce_to(other, $scope.get('Array'), "to_ary").$to_a()
      };
      if ((($a = self.length === 0) !== nil && (!$a.$$is_boolean || $a == true))) {
        return []};
      if ((($a = other.length === 0) !== nil && (!$a.$$is_boolean || $a == true))) {
        return self.$clone()};
      
      var result = [], hash = $hash2([], {}), i, length, item;

      for (i = 0, length = other.length; i < length; i++) {
        Opal.hash_put(hash, other[i], true);
      }

      for (i = 0, length = self.length; i < length; i++) {
        item = self[i];
        if (Opal.hash_get(hash, item) === undefined) {
          result.push(item);
        }
      }

      return result;
    ;
    });

    Opal.defn(self, '$<<', function(object) {
      var self = this;

      self.push(object);
      return self;
    });

    Opal.defn(self, '$<=>', function(other) {
      var $a, self = this;

      if ((($a = $scope.get('Array')['$==='](other)) !== nil && (!$a.$$is_boolean || $a == true))) {
        other = other.$to_a()
      } else if ((($a = other['$respond_to?']("to_ary")) !== nil && (!$a.$$is_boolean || $a == true))) {
        other = other.$to_ary().$to_a()
        } else {
        return nil
      };
      
      if (self.$hash() === other.$hash()) {
        return 0;
      }

      var count = Math.min(self.length, other.length);

      for (var i = 0; i < count; i++) {
        var tmp = (self[i])['$<=>'](other[i]);

        if (tmp !== 0) {
          return tmp;
        }
      }

      return (self.length)['$<=>'](other.length);
    ;
    });

    Opal.defn(self, '$==', function(other) {
      var self = this;

      
      var recursed = {};

      function _eqeq(array, other) {
        var i, length, a, b;

        if (array === other)
          return true;

        if (!other.$$is_array) {
          if ($scope.get('Opal')['$respond_to?'](other, "to_ary")) {
            return (other)['$=='](array);
          } else {
            return false;
          }
        }

        if (array.constructor !== Array)
          array = array.literal;
        if (other.constructor !== Array)
          other = other.literal;

        if (array.length !== other.length) {
          return false;
        }

        recursed[(array).$object_id()] = true;

        for (i = 0, length = array.length; i < length; i++) {
          a = array[i];
          b = other[i];
          if (a.$$is_array) {
            if (b.$$is_array && b.length !== a.length) {
              return false;
            }
            if (!recursed.hasOwnProperty((a).$object_id())) {
              if (!_eqeq(a, b)) {
                return false;
              }
            }
          } else {
            if (!(a)['$=='](b)) {
              return false;
            }
          }
        }

        return true;
      }

      return _eqeq(self, other);
    ;
    });

    Opal.defn(self, '$[]', function(index, length) {
      var self = this;

      
      var size = self.length,
          exclude, from, to;

      if (index.$$is_range) {
        exclude = index.exclude;
        from    = $scope.get('Opal').$coerce_to(index.begin, $scope.get('Integer'), "to_int");
        to      = $scope.get('Opal').$coerce_to(index.end, $scope.get('Integer'), "to_int");

        if (from < 0) {
          from += size;

          if (from < 0) {
            return nil;
          }
        }

        if (from > size) {
          return nil;
        }

        if (to < 0) {
          to += size;

          if (to < 0) {
            return [];
          }
        }

        if (!exclude) {
          to += 1;
        }

        return self.slice(from, to);
      }
      else {
        index = $scope.get('Opal').$coerce_to(index, $scope.get('Integer'), "to_int");

        if (index < 0) {
          index += size;

          if (index < 0) {
            return nil;
          }
        }

        if (length === undefined) {
          if (index >= size || index < 0) {
            return nil;
          }

          return self[index];
        }
        else {
          length = $scope.get('Opal').$coerce_to(length, $scope.get('Integer'), "to_int");

          if (length < 0 || index > size || index < 0) {
            return nil;
          }

          return self.slice(index, index + length);
        }
      }
    
    });

    Opal.defn(self, '$[]=', function(index, value, extra) {
      var $a, self = this, data = nil, length = nil;

      
      var i, size = self.length;
    
      if ((($a = $scope.get('Range')['$==='](index)) !== nil && (!$a.$$is_boolean || $a == true))) {
        if ((($a = $scope.get('Array')['$==='](value)) !== nil && (!$a.$$is_boolean || $a == true))) {
          data = value.$to_a()
        } else if ((($a = value['$respond_to?']("to_ary")) !== nil && (!$a.$$is_boolean || $a == true))) {
          data = value.$to_ary().$to_a()
          } else {
          data = [value]
        };
        
        var exclude = index.exclude,
            from    = $scope.get('Opal').$coerce_to(index.begin, $scope.get('Integer'), "to_int"),
            to      = $scope.get('Opal').$coerce_to(index.end, $scope.get('Integer'), "to_int");

        if (from < 0) {
          from += size;

          if (from < 0) {
            self.$raise($scope.get('RangeError'), "" + (index.$inspect()) + " out of range");
          }
        }

        if (to < 0) {
          to += size;
        }

        if (!exclude) {
          to += 1;
        }

        if (from > size) {
          for (i = size; i < from; i++) {
            self[i] = nil;
          }
        }

        if (to < 0) {
          self.splice.apply(self, [from, 0].concat(data));
        }
        else {
          self.splice.apply(self, [from, to - from].concat(data));
        }

        return value;
      ;
        } else {
        if ((($a = extra === undefined) !== nil && (!$a.$$is_boolean || $a == true))) {
          length = 1
          } else {
          length = value;
          value = extra;
          if ((($a = $scope.get('Array')['$==='](value)) !== nil && (!$a.$$is_boolean || $a == true))) {
            data = value.$to_a()
          } else if ((($a = value['$respond_to?']("to_ary")) !== nil && (!$a.$$is_boolean || $a == true))) {
            data = value.$to_ary().$to_a()
            } else {
            data = [value]
          };
        };
        
        var old;

        index  = $scope.get('Opal').$coerce_to(index, $scope.get('Integer'), "to_int");
        length = $scope.get('Opal').$coerce_to(length, $scope.get('Integer'), "to_int");

        if (index < 0) {
          old    = index;
          index += size;

          if (index < 0) {
            self.$raise($scope.get('IndexError'), "index " + (old) + " too small for array; minimum " + (-self.length));
          }
        }

        if (length < 0) {
          self.$raise($scope.get('IndexError'), "negative length (" + (length) + ")")
        }

        if (index > size) {
          for (i = size; i < index; i++) {
            self[i] = nil;
          }
        }

        if (extra === undefined) {
          self[index] = value;
        }
        else {
          self.splice.apply(self, [index, length].concat(data));
        }

        return value;
      
      };
    });

    Opal.defn(self, '$assoc', function(object) {
      var self = this;

      
      for (var i = 0, length = self.length, item; i < length; i++) {
        if (item = self[i], item.length && (item[0])['$=='](object)) {
          return item;
        }
      }

      return nil;
    
    });

    Opal.defn(self, '$at', function(index) {
      var self = this;

      index = $scope.get('Opal').$coerce_to(index, $scope.get('Integer'), "to_int");
      
      if (index < 0) {
        index += self.length;
      }

      if (index < 0 || index >= self.length) {
        return nil;
      }

      return self[index];
    
    });

    Opal.defn(self, '$bsearch', TMP_3 = function() {
      var self = this, $iter = TMP_3.$$p, block = $iter || nil;

      TMP_3.$$p = null;
      if ((block !== nil)) {
        } else {
        return self.$enum_for("bsearch")
      };
      
      var min = 0,
          max = self.length,
          mid,
          val,
          ret,
          smaller = false,
          satisfied = nil;

      while (min < max) {
        mid = min + Math.floor((max - min) / 2);
        val = self[mid];
        ret = block(val);

        if (ret === $breaker) {
          return $breaker.$v;
        }
        else if (ret === true) {
          satisfied = val;
          smaller = true;
        }
        else if (ret === false || ret === nil) {
          smaller = false;
        }
        else if (ret.$$is_number) {
          if (ret === 0) { return val; }
          smaller = (ret < 0);
        }
        else {
          self.$raise($scope.get('TypeError'), "wrong argument type " + ((ret).$class()) + " (must be numeric, true, false or nil)")
        }

        if (smaller) { max = mid; } else { min = mid + 1; }
      }

      return satisfied;
    
    });

    Opal.defn(self, '$cycle', TMP_4 = function(n) {
      var $a, $b, TMP_5, $c, self = this, $iter = TMP_4.$$p, block = $iter || nil;

      if (n == null) {
        n = nil
      }
      TMP_4.$$p = null;
      if ((block !== nil)) {
        } else {
        return ($a = ($b = self).$enum_for, $a.$$p = (TMP_5 = function(){var self = TMP_5.$$s || this;

        if (n['$=='](nil)) {
            return (($scope.get('Float')).$$scope.get('INFINITY'))
            } else {
            n = $scope.get('Opal')['$coerce_to!'](n, $scope.get('Integer'), "to_int");
            if ($rb_gt(n, 0)) {
              return $rb_times(self.$enumerator_size(), n)
              } else {
              return 0
            };
          }}, TMP_5.$$s = self, TMP_5), $a).call($b, "cycle", n)
      };
      if ((($a = ((($c = self['$empty?']()) !== false && $c !== nil) ? $c : n['$=='](0))) !== nil && (!$a.$$is_boolean || $a == true))) {
        return nil};
      
      var i, length, value;

      if (n === nil) {
        while (true) {
          for (i = 0, length = self.length; i < length; i++) {
            value = Opal.yield1(block, self[i]);

            if (value === $breaker) {
              return $breaker.$v;
            }
          }
        }
      }
      else {
        n = $scope.get('Opal')['$coerce_to!'](n, $scope.get('Integer'), "to_int");
        if (n <= 0) {
          return self;
        }

        while (n > 0) {
          for (i = 0, length = self.length; i < length; i++) {
            value = Opal.yield1(block, self[i]);

            if (value === $breaker) {
              return $breaker.$v;
            }
          }

          n--;
        }
      }
    
      return self;
    });

    Opal.defn(self, '$clear', function() {
      var self = this;

      self.splice(0, self.length);
      return self;
    });

    Opal.defn(self, '$clone', function() {
      var self = this, copy = nil;

      copy = [];
      copy.$copy_singleton_methods(self);
      copy.$initialize_clone(self);
      return copy;
    });

    Opal.defn(self, '$dup', function() {
      var self = this, copy = nil;

      copy = [];
      copy.$initialize_dup(self);
      return copy;
    });

    Opal.defn(self, '$initialize_copy', function(other) {
      var self = this;

      return self.$replace(other);
    });

    Opal.defn(self, '$collect', TMP_6 = function() {
      var $a, $b, TMP_7, self = this, $iter = TMP_6.$$p, block = $iter || nil;

      TMP_6.$$p = null;
      if ((block !== nil)) {
        } else {
        return ($a = ($b = self).$enum_for, $a.$$p = (TMP_7 = function(){var self = TMP_7.$$s || this;

        return self.$size()}, TMP_7.$$s = self, TMP_7), $a).call($b, "collect")
      };
      
      var result = [];

      for (var i = 0, length = self.length; i < length; i++) {
        var value = Opal.yield1(block, self[i]);

        if (value === $breaker) {
          return $breaker.$v;
        }

        result.push(value);
      }

      return result;
    
    });

    Opal.defn(self, '$collect!', TMP_8 = function() {
      var $a, $b, TMP_9, self = this, $iter = TMP_8.$$p, block = $iter || nil;

      TMP_8.$$p = null;
      if ((block !== nil)) {
        } else {
        return ($a = ($b = self).$enum_for, $a.$$p = (TMP_9 = function(){var self = TMP_9.$$s || this;

        return self.$size()}, TMP_9.$$s = self, TMP_9), $a).call($b, "collect!")
      };
      
      for (var i = 0, length = self.length; i < length; i++) {
        var value = Opal.yield1(block, self[i]);

        if (value === $breaker) {
          return $breaker.$v;
        }

        self[i] = value;
      }
    
      return self;
    });

    
    function binomial_coefficient(n, k) {
      if (n === k || k === 0) {
        return 1;
      }

      if (k > 0 && n > k) {
        return binomial_coefficient(n - 1, k - 1) + binomial_coefficient(n - 1, k);
      }

      return 0;
    }
  

    Opal.defn(self, '$combination', TMP_10 = function(n) {
      var $a, $b, TMP_11, self = this, $iter = TMP_10.$$p, $yield = $iter || nil, num = nil;

      TMP_10.$$p = null;
      num = $scope.get('Opal')['$coerce_to!'](n, $scope.get('Integer'), "to_int");
      if (($yield !== nil)) {
        } else {
        return ($a = ($b = self).$enum_for, $a.$$p = (TMP_11 = function(){var self = TMP_11.$$s || this;

        return binomial_coefficient(self.length, num);}, TMP_11.$$s = self, TMP_11), $a).call($b, "combination", num)
      };
      
      var i, length, stack, chosen, lev, done, next;

      if (num === 0) {
        ((($a = Opal.yield1($yield, [])) === $breaker) ? $breaker.$v : $a)
      } else if (num === 1) {
        for (i = 0, length = self.length; i < length; i++) {
          ((($a = Opal.yield1($yield, [self[i]])) === $breaker) ? $breaker.$v : $a)
        }
      }
      else if (num === self.length) {
        ((($a = Opal.yield1($yield, self.slice())) === $breaker) ? $breaker.$v : $a)
      }
      else if (num >= 0 && num < self.length) {
        stack = [];
        for (i = 0; i <= num + 1; i++) {
          stack.push(0);
        }

        chosen = [];
        lev = 0;
        done = false;
        stack[0] = -1;

        while (!done) {
          chosen[lev] = self[stack[lev+1]];
          while (lev < num - 1) {
            lev++;
            next = stack[lev+1] = stack[lev] + 1;
            chosen[lev] = self[next];
          }
          ((($a = Opal.yield1($yield, chosen.slice())) === $breaker) ? $breaker.$v : $a)
          lev++;
          do {
            done = (lev === 0);
            stack[lev]++;
            lev--;
          } while ( stack[lev+1] + num === self.length + lev + 1 );
        }
      }
    ;
      return self;
    });

    Opal.defn(self, '$compact', function() {
      var self = this;

      
      var result = [];

      for (var i = 0, length = self.length, item; i < length; i++) {
        if ((item = self[i]) !== nil) {
          result.push(item);
        }
      }

      return result;
    
    });

    Opal.defn(self, '$compact!', function() {
      var self = this;

      
      var original = self.length;

      for (var i = 0, length = self.length; i < length; i++) {
        if (self[i] === nil) {
          self.splice(i, 1);

          length--;
          i--;
        }
      }

      return self.length === original ? nil : self;
    
    });

    Opal.defn(self, '$concat', function(other) {
      var $a, self = this;

      if ((($a = $scope.get('Array')['$==='](other)) !== nil && (!$a.$$is_boolean || $a == true))) {
        other = other.$to_a()
        } else {
        other = $scope.get('Opal').$coerce_to(other, $scope.get('Array'), "to_ary").$to_a()
      };
      
      for (var i = 0, length = other.length; i < length; i++) {
        self.push(other[i]);
      }
    
      return self;
    });

    Opal.defn(self, '$delete', TMP_12 = function(object) {
      var $a, self = this, $iter = TMP_12.$$p, $yield = $iter || nil;

      TMP_12.$$p = null;
      
      var original = self.length;

      for (var i = 0, length = original; i < length; i++) {
        if ((self[i])['$=='](object)) {
          self.splice(i, 1);

          length--;
          i--;
        }
      }

      if (self.length === original) {
        if (($yield !== nil)) {
          return ((($a = Opal.yieldX($yield, [])) === $breaker) ? $breaker.$v : $a);
        }
        return nil;
      }
      return object;
    ;
    });

    Opal.defn(self, '$delete_at', function(index) {
      var self = this;

      
      index = $scope.get('Opal').$coerce_to(index, $scope.get('Integer'), "to_int");

      if (index < 0) {
        index += self.length;
      }

      if (index < 0 || index >= self.length) {
        return nil;
      }

      var result = self[index];

      self.splice(index, 1);

      return result;
    ;
    });

    Opal.defn(self, '$delete_if', TMP_13 = function() {
      var $a, $b, TMP_14, self = this, $iter = TMP_13.$$p, block = $iter || nil;

      TMP_13.$$p = null;
      if ((block !== nil)) {
        } else {
        return ($a = ($b = self).$enum_for, $a.$$p = (TMP_14 = function(){var self = TMP_14.$$s || this;

        return self.$size()}, TMP_14.$$s = self, TMP_14), $a).call($b, "delete_if")
      };
      
      for (var i = 0, length = self.length, value; i < length; i++) {
        if ((value = block(self[i])) === $breaker) {
          return $breaker.$v;
        }

        if (value !== false && value !== nil) {
          self.splice(i, 1);

          length--;
          i--;
        }
      }
    
      return self;
    });

    Opal.defn(self, '$drop', function(number) {
      var self = this;

      
      if (number < 0) {
        self.$raise($scope.get('ArgumentError'))
      }

      return self.slice(number);
    ;
    });

    Opal.defn(self, '$each', TMP_15 = function() {
      var $a, $b, TMP_16, self = this, $iter = TMP_15.$$p, block = $iter || nil;

      TMP_15.$$p = null;
      if ((block !== nil)) {
        } else {
        return ($a = ($b = self).$enum_for, $a.$$p = (TMP_16 = function(){var self = TMP_16.$$s || this;

        return self.$size()}, TMP_16.$$s = self, TMP_16), $a).call($b, "each")
      };
      
      for (var i = 0, length = self.length; i < length; i++) {
        var value = Opal.yield1(block, self[i]);

        if (value == $breaker) {
          return $breaker.$v;
        }
      }
    
      return self;
    });

    Opal.defn(self, '$each_index', TMP_17 = function() {
      var $a, $b, TMP_18, self = this, $iter = TMP_17.$$p, block = $iter || nil;

      TMP_17.$$p = null;
      if ((block !== nil)) {
        } else {
        return ($a = ($b = self).$enum_for, $a.$$p = (TMP_18 = function(){var self = TMP_18.$$s || this;

        return self.$size()}, TMP_18.$$s = self, TMP_18), $a).call($b, "each_index")
      };
      
      for (var i = 0, length = self.length; i < length; i++) {
        var value = Opal.yield1(block, i);

        if (value === $breaker) {
          return $breaker.$v;
        }
      }
    
      return self;
    });

    Opal.defn(self, '$empty?', function() {
      var self = this;

      return self.length === 0;
    });

    Opal.defn(self, '$eql?', function(other) {
      var self = this;

      
      var recursed = {};

      function _eql(array, other) {
        var i, length, a, b;

        if (!other.$$is_array) {
          return false;
        }

        other = other.$to_a();

        if (array.length !== other.length) {
          return false;
        }

        recursed[(array).$object_id()] = true;

        for (i = 0, length = array.length; i < length; i++) {
          a = array[i];
          b = other[i];
          if (a.$$is_array) {
            if (b.$$is_array && b.length !== a.length) {
              return false;
            }
            if (!recursed.hasOwnProperty((a).$object_id())) {
              if (!_eql(a, b)) {
                return false;
              }
            }
          } else {
            if (!(a)['$eql?'](b)) {
              return false;
            }
          }
        }

        return true;
      }

      return _eql(self, other);
    
    });

    Opal.defn(self, '$fetch', TMP_19 = function(index, defaults) {
      var self = this, $iter = TMP_19.$$p, block = $iter || nil;

      TMP_19.$$p = null;
      
      var original = index;

      index = $scope.get('Opal').$coerce_to(index, $scope.get('Integer'), "to_int");

      if (index < 0) {
        index += self.length;
      }

      if (index >= 0 && index < self.length) {
        return self[index];
      }

      if (block !== nil) {
        return block(original);
      }

      if (defaults != null) {
        return defaults;
      }

      if (self.length === 0) {
        self.$raise($scope.get('IndexError'), "index " + (original) + " outside of array bounds: 0...0")
      }
      else {
        self.$raise($scope.get('IndexError'), "index " + (original) + " outside of array bounds: -" + (self.length) + "..." + (self.length));
      }
    ;
    });

    Opal.defn(self, '$fill', TMP_20 = function() {
      var $a, $b, self = this, $iter = TMP_20.$$p, block = $iter || nil, one = nil, two = nil, obj = nil, left = nil, right = nil, $splat_index = nil;

      var array_size = arguments.length - 0;
      if(array_size < 0) array_size = 0;
      var args = new Array(array_size);
      for($splat_index = 0; $splat_index < array_size; $splat_index++) {
        args[$splat_index] = arguments[$splat_index + 0];
      }
      TMP_20.$$p = null;
      
      var i, length, value;
    
      if (block !== false && block !== nil) {
        if ((($a = args.length > 2) !== nil && (!$a.$$is_boolean || $a == true))) {
          self.$raise($scope.get('ArgumentError'), "wrong number of arguments (" + (args.$length()) + " for 0..2)")};
        $b = args, $a = Opal.to_ary($b), one = ($a[0] == null ? nil : $a[0]), two = ($a[1] == null ? nil : $a[1]), $b;
        } else {
        if ((($a = args.length == 0) !== nil && (!$a.$$is_boolean || $a == true))) {
          self.$raise($scope.get('ArgumentError'), "wrong number of arguments (0 for 1..3)")
        } else if ((($a = args.length > 3) !== nil && (!$a.$$is_boolean || $a == true))) {
          self.$raise($scope.get('ArgumentError'), "wrong number of arguments (" + (args.$length()) + " for 1..3)")};
        $b = args, $a = Opal.to_ary($b), obj = ($a[0] == null ? nil : $a[0]), one = ($a[1] == null ? nil : $a[1]), two = ($a[2] == null ? nil : $a[2]), $b;
      };
      if ((($a = $scope.get('Range')['$==='](one)) !== nil && (!$a.$$is_boolean || $a == true))) {
        if (two !== false && two !== nil) {
          self.$raise($scope.get('TypeError'), "length invalid with range")};
        left = $scope.get('Opal').$coerce_to(one.$begin(), $scope.get('Integer'), "to_int");
        if ((($a = left < 0) !== nil && (!$a.$$is_boolean || $a == true))) {
          left += self.length;};
        if ((($a = left < 0) !== nil && (!$a.$$is_boolean || $a == true))) {
          self.$raise($scope.get('RangeError'), "" + (one.$inspect()) + " out of range")};
        right = $scope.get('Opal').$coerce_to(one.$end(), $scope.get('Integer'), "to_int");
        if ((($a = right < 0) !== nil && (!$a.$$is_boolean || $a == true))) {
          right += self.length;};
        if ((($a = one['$exclude_end?']()) !== nil && (!$a.$$is_boolean || $a == true))) {
          } else {
          right += 1;
        };
        if ((($a = right <= left) !== nil && (!$a.$$is_boolean || $a == true))) {
          return self};
      } else if (one !== false && one !== nil) {
        left = $scope.get('Opal').$coerce_to(one, $scope.get('Integer'), "to_int");
        if ((($a = left < 0) !== nil && (!$a.$$is_boolean || $a == true))) {
          left += self.length;};
        if ((($a = left < 0) !== nil && (!$a.$$is_boolean || $a == true))) {
          left = 0};
        if (two !== false && two !== nil) {
          right = $scope.get('Opal').$coerce_to(two, $scope.get('Integer'), "to_int");
          if ((($a = right == 0) !== nil && (!$a.$$is_boolean || $a == true))) {
            return self};
          right += left;
          } else {
          right = self.length
        };
        } else {
        left = 0;
        right = self.length;
      };
      if ((($a = left > self.length) !== nil && (!$a.$$is_boolean || $a == true))) {
        
        for (i = self.length; i < right; i++) {
          self[i] = nil;
        }
      ;};
      if ((($a = right > self.length) !== nil && (!$a.$$is_boolean || $a == true))) {
        self.length = right};
      if (block !== false && block !== nil) {
        
        for (length = self.length; left < right; left++) {
          value = block(left);

          if (value === $breaker) {
            return $breaker.$v;
          }

          self[left] = value;
        }
      ;
        } else {
        
        for (length = self.length; left < right; left++) {
          self[left] = obj;
        }
      ;
      };
      return self;
    });

    Opal.defn(self, '$first', function(count) {
      var self = this;

      
      if (count == null) {
        return self.length === 0 ? nil : self[0];
      }

      count = $scope.get('Opal').$coerce_to(count, $scope.get('Integer'), "to_int");

      if (count < 0) {
        self.$raise($scope.get('ArgumentError'), "negative array size");
      }

      return self.slice(0, count);
    
    });

    Opal.defn(self, '$flatten', function(level) {
      var self = this;

      
      function _flatten(array, level) {
        var result = [],
            i, length,
            item, ary;

        array = (array).$to_a();

        for (i = 0, length = array.length; i < length; i++) {
          item = array[i];

          if (!$scope.get('Opal')['$respond_to?'](item, "to_ary")) {
            result.push(item);
            continue;
          }

          ary = (item).$to_ary();

          if (ary === nil) {
            result.push(item);
            continue;
          }

          if (!ary.$$is_array) {
            self.$raise($scope.get('TypeError'));
          }

          if (ary === self) {
            self.$raise($scope.get('ArgumentError'));
          }

          switch (level) {
          case undefined:
            result.push.apply(result, _flatten(ary));
            break;
          case 0:
            result.push(ary);
            break;
          default:
            result.push.apply(result, _flatten(ary, level - 1));
          }
        }
        return result;
      }

      if (level !== undefined) {
        level = $scope.get('Opal').$coerce_to(level, $scope.get('Integer'), "to_int");
      }

      return _flatten(self, level);
    
    });

    Opal.defn(self, '$flatten!', function(level) {
      var self = this;

      
      var flattened = self.$flatten(level);

      if (self.length == flattened.length) {
        for (var i = 0, length = self.length; i < length; i++) {
          if (self[i] !== flattened[i]) {
            break;
          }
        }

        if (i == length) {
          return nil;
        }
      }

      self.$replace(flattened);
    ;
      return self;
    });

    Opal.defn(self, '$hash', function() {
      var self = this;

      
      var top = (Opal.hash_ids == undefined),
          result = ['A'],
          hash_id = self.$object_id(),
          item, i, key;

      try {
        if (top) {
          Opal.hash_ids = {};
        }

        if (Opal.hash_ids.hasOwnProperty(hash_id)) {
          return 'self';
        }

        for (key in Opal.hash_ids) {
          if (Opal.hash_ids.hasOwnProperty(key)) {
            item = Opal.hash_ids[key];
            if (self['$eql?'](item)) {
              return 'self';
            }
          }
        }

        Opal.hash_ids[hash_id] = self;

        for (i = 0; i < self.length; i++) {
          item = self[i];
          result.push(item.$hash());
        }

        return result.join(',');
      } finally {
        if (top) {
          delete Opal.hash_ids;
        }
      }
    
    });

    Opal.defn(self, '$include?', function(member) {
      var self = this;

      
      for (var i = 0, length = self.length; i < length; i++) {
        if ((self[i])['$=='](member)) {
          return true;
        }
      }

      return false;
    
    });

    Opal.defn(self, '$index', TMP_21 = function(object) {
      var self = this, $iter = TMP_21.$$p, block = $iter || nil;

      TMP_21.$$p = null;
      
      var i, length, value;

      if (object != null) {
        for (i = 0, length = self.length; i < length; i++) {
          if ((self[i])['$=='](object)) {
            return i;
          }
        }
      }
      else if (block !== nil) {
        for (i = 0, length = self.length; i < length; i++) {
          if ((value = block(self[i])) === $breaker) {
            return $breaker.$v;
          }

          if (value !== false && value !== nil) {
            return i;
          }
        }
      }
      else {
        return self.$enum_for("index");
      }

      return nil;
    
    });

    Opal.defn(self, '$insert', function(index) {
      var self = this, $splat_index = nil;

      var array_size = arguments.length - 1;
      if(array_size < 0) array_size = 0;
      var objects = new Array(array_size);
      for($splat_index = 0; $splat_index < array_size; $splat_index++) {
        objects[$splat_index] = arguments[$splat_index + 1];
      }
      
      index = $scope.get('Opal').$coerce_to(index, $scope.get('Integer'), "to_int");

      if (objects.length > 0) {
        if (index < 0) {
          index += self.length + 1;

          if (index < 0) {
            self.$raise($scope.get('IndexError'), "" + (index) + " is out of bounds");
          }
        }
        if (index > self.length) {
          for (var i = self.length; i < index; i++) {
            self.push(nil);
          }
        }

        self.splice.apply(self, [index, 0].concat(objects));
      }
    ;
      return self;
    });

    Opal.defn(self, '$inspect', function() {
      var self = this;

      
      var result = [],
          id     = self.$__id__();

      for (var i = 0, length = self.length; i < length; i++) {
        var item = self['$[]'](i);

        if ((item).$__id__() === id) {
          result.push('[...]');
        }
        else {
          result.push((item).$inspect());
        }
      }

      return '[' + result.join(', ') + ']';
    ;
    });

    Opal.defn(self, '$join', function(sep) {
      var $a, self = this;
      if ($gvars[","] == null) $gvars[","] = nil;

      if (sep == null) {
        sep = nil
      }
      if ((($a = self.length === 0) !== nil && (!$a.$$is_boolean || $a == true))) {
        return ""};
      if ((($a = sep === nil) !== nil && (!$a.$$is_boolean || $a == true))) {
        sep = $gvars[","]};
      
      var result = [];
      var i, length, item, tmp;

      for (i = 0, length = self.length; i < length; i++) {
        item = self[i];

        if ($scope.get('Opal')['$respond_to?'](item, "to_str")) {
          tmp = (item).$to_str();

          if (tmp !== nil) {
            result.push((tmp).$to_s());

            continue;
          }
        }

        if ($scope.get('Opal')['$respond_to?'](item, "to_ary")) {
          tmp = (item).$to_ary();

          if (tmp === self) {
            self.$raise($scope.get('ArgumentError'));
          }

          if (tmp !== nil) {
            result.push((tmp).$join(sep));

            continue;
          }
        }

        if ($scope.get('Opal')['$respond_to?'](item, "to_s")) {
          tmp = (item).$to_s();

          if (tmp !== nil) {
            result.push(tmp);

            continue;
          }
        }

        self.$raise($scope.get('NoMethodError').$new("" + ($scope.get('Opal').$inspect(item)) + " doesn't respond to #to_str, #to_ary or #to_s", "to_str"));
      }

      if (sep === nil) {
        return result.join('');
      }
      else {
        return result.join($scope.get('Opal')['$coerce_to!'](sep, $scope.get('String'), "to_str").$to_s());
      }
    ;
    });

    Opal.defn(self, '$keep_if', TMP_22 = function() {
      var $a, $b, TMP_23, self = this, $iter = TMP_22.$$p, block = $iter || nil;

      TMP_22.$$p = null;
      if ((block !== nil)) {
        } else {
        return ($a = ($b = self).$enum_for, $a.$$p = (TMP_23 = function(){var self = TMP_23.$$s || this;

        return self.$size()}, TMP_23.$$s = self, TMP_23), $a).call($b, "keep_if")
      };
      
      for (var i = 0, length = self.length, value; i < length; i++) {
        if ((value = block(self[i])) === $breaker) {
          return $breaker.$v;
        }

        if (value === false || value === nil) {
          self.splice(i, 1);

          length--;
          i--;
        }
      }
    
      return self;
    });

    Opal.defn(self, '$last', function(count) {
      var self = this;

      
      if (count == null) {
        return self.length === 0 ? nil : self[self.length - 1];
      }

      count = $scope.get('Opal').$coerce_to(count, $scope.get('Integer'), "to_int");

      if (count < 0) {
        self.$raise($scope.get('ArgumentError'), "negative array size");
      }

      if (count > self.length) {
        count = self.length;
      }

      return self.slice(self.length - count, self.length);
    
    });

    Opal.defn(self, '$length', function() {
      var self = this;

      return self.length;
    });

    Opal.alias(self, 'map', 'collect');

    Opal.alias(self, 'map!', 'collect!');

    Opal.defn(self, '$permutation', TMP_24 = function(num) {
      var $a, $b, TMP_25, self = this, $iter = TMP_24.$$p, block = $iter || nil, perm = nil, used = nil;

      TMP_24.$$p = null;
      if ((block !== nil)) {
        } else {
        return ($a = ($b = self).$enum_for, $a.$$p = (TMP_25 = function(){var self = TMP_25.$$s || this;

        return self.$size()}, TMP_25.$$s = self, TMP_25), $a).call($b, "permutation", num)
      };
      
      var permute, offensive, output;

      if (num === undefined) {
        num = self.length;
      }
      else {
        num = $scope.get('Opal').$coerce_to(num, $scope.get('Integer'), "to_int")
      }

      if (num < 0 || self.length < num) {
        // no permutations, yield nothing
      }
      else if (num === 0) {
        // exactly one permutation: the zero-length array
        ((($a = Opal.yield1(block, [])) === $breaker) ? $breaker.$v : $a)
      }
      else if (num === 1) {
        // this is a special, easy case
        for (var i = 0; i < self.length; i++) {
          ((($a = Opal.yield1(block, [self[i]])) === $breaker) ? $breaker.$v : $a)
        }
      }
      else {
        // this is the general case
        perm = $scope.get('Array').$new(num)
        used = $scope.get('Array').$new(self.length, false)

        permute = function(num, perm, index, used, blk) {
          self = this;
          for(var i = 0; i < self.length; i++){
            if(used['$[]'](i)['$!']()) {
              perm[index] = i;
              if(index < num - 1) {
                used[i] = true;
                permute.call(self, num, perm, index + 1, used, blk);
                used[i] = false;
              }
              else {
                output = [];
                for (var j = 0; j < perm.length; j++) {
                  output.push(self[perm[j]]);
                }
                Opal.yield1(blk, output);
              }
            }
          }
        }

        if ((block !== nil)) {
          // offensive (both definitions) copy.
          offensive = self.slice();
          permute.call(offensive, num, perm, 0, used, block);
        }
        else {
          permute.call(self, num, perm, 0, used, block);
        }
      }
    ;
      return self;
    });

    Opal.defn(self, '$pop', function(count) {
      var $a, self = this;

      if ((($a = count === undefined) !== nil && (!$a.$$is_boolean || $a == true))) {
        if ((($a = self.length === 0) !== nil && (!$a.$$is_boolean || $a == true))) {
          return nil};
        return self.pop();};
      count = $scope.get('Opal').$coerce_to(count, $scope.get('Integer'), "to_int");
      if ((($a = count < 0) !== nil && (!$a.$$is_boolean || $a == true))) {
        self.$raise($scope.get('ArgumentError'), "negative array size")};
      if ((($a = self.length === 0) !== nil && (!$a.$$is_boolean || $a == true))) {
        return []};
      if ((($a = count > self.length) !== nil && (!$a.$$is_boolean || $a == true))) {
        return self.splice(0, self.length);
        } else {
        return self.splice(self.length - count, self.length);
      };
    });

    Opal.defn(self, '$product', TMP_26 = function() {
      var $a, self = this, $iter = TMP_26.$$p, block = $iter || nil, $splat_index = nil;

      var array_size = arguments.length - 0;
      if(array_size < 0) array_size = 0;
      var args = new Array(array_size);
      for($splat_index = 0; $splat_index < array_size; $splat_index++) {
        args[$splat_index] = arguments[$splat_index + 0];
      }
      TMP_26.$$p = null;
      
      var result = (block !== nil) ? null : [],
          n = args.length + 1,
          counters = new Array(n),
          lengths  = new Array(n),
          arrays   = new Array(n),
          i, m, subarray, len, resultlen = 1;

      arrays[0] = self;
      for (i = 1; i < n; i++) {
        arrays[i] = $scope.get('Opal').$coerce_to(args[i - 1], $scope.get('Array'), "to_ary");
      }

      for (i = 0; i < n; i++) {
        len = arrays[i].length;
        if (len === 0) {
          return result || self;
        }
        resultlen *= len;
        if (resultlen > 2147483647) {
          self.$raise($scope.get('RangeError'), "too big to product")
        }
        lengths[i] = len;
        counters[i] = 0;
      }

      outer_loop: for (;;) {
        subarray = [];
        for (i = 0; i < n; i++) {
          subarray.push(arrays[i][counters[i]]);
        }
        if (result) {
          result.push(subarray);
        } else {
          ((($a = Opal.yield1(block, subarray)) === $breaker) ? $breaker.$v : $a)
        }
        m = n - 1;
        counters[m]++;
        while (counters[m] === lengths[m]) {
          counters[m] = 0;
          if (--m < 0) break outer_loop;
          counters[m]++;
        }
      }

      return result || self;
    ;
    });

    Opal.defn(self, '$push', function() {
      var self = this, $splat_index = nil;

      var array_size = arguments.length - 0;
      if(array_size < 0) array_size = 0;
      var objects = new Array(array_size);
      for($splat_index = 0; $splat_index < array_size; $splat_index++) {
        objects[$splat_index] = arguments[$splat_index + 0];
      }
      
      for (var i = 0, length = objects.length; i < length; i++) {
        self.push(objects[i]);
      }
    
      return self;
    });

    Opal.defn(self, '$rassoc', function(object) {
      var self = this;

      
      for (var i = 0, length = self.length, item; i < length; i++) {
        item = self[i];

        if (item.length && item[1] !== undefined) {
          if ((item[1])['$=='](object)) {
            return item;
          }
        }
      }

      return nil;
    
    });

    Opal.defn(self, '$reject', TMP_27 = function() {
      var $a, $b, TMP_28, self = this, $iter = TMP_27.$$p, block = $iter || nil;

      TMP_27.$$p = null;
      if ((block !== nil)) {
        } else {
        return ($a = ($b = self).$enum_for, $a.$$p = (TMP_28 = function(){var self = TMP_28.$$s || this;

        return self.$size()}, TMP_28.$$s = self, TMP_28), $a).call($b, "reject")
      };
      
      var result = [];

      for (var i = 0, length = self.length, value; i < length; i++) {
        if ((value = block(self[i])) === $breaker) {
          return $breaker.$v;
        }

        if (value === false || value === nil) {
          result.push(self[i]);
        }
      }
      return result;
    
    });

    Opal.defn(self, '$reject!', TMP_29 = function() {
      var $a, $b, TMP_30, $c, self = this, $iter = TMP_29.$$p, block = $iter || nil, original = nil;

      TMP_29.$$p = null;
      if ((block !== nil)) {
        } else {
        return ($a = ($b = self).$enum_for, $a.$$p = (TMP_30 = function(){var self = TMP_30.$$s || this;

        return self.$size()}, TMP_30.$$s = self, TMP_30), $a).call($b, "reject!")
      };
      original = self.$length();
      ($a = ($c = self).$delete_if, $a.$$p = block.$to_proc(), $a).call($c);
      if (self.$length()['$=='](original)) {
        return nil
        } else {
        return self
      };
    });

    Opal.defn(self, '$replace', function(other) {
      var $a, self = this;

      if ((($a = $scope.get('Array')['$==='](other)) !== nil && (!$a.$$is_boolean || $a == true))) {
        other = other.$to_a()
        } else {
        other = $scope.get('Opal').$coerce_to(other, $scope.get('Array'), "to_ary").$to_a()
      };
      
      self.splice(0, self.length);
      self.push.apply(self, other);
    
      return self;
    });

    Opal.defn(self, '$reverse', function() {
      var self = this;

      return self.slice(0).reverse();
    });

    Opal.defn(self, '$reverse!', function() {
      var self = this;

      return self.reverse();
    });

    Opal.defn(self, '$reverse_each', TMP_31 = function() {
      var $a, $b, TMP_32, $c, self = this, $iter = TMP_31.$$p, block = $iter || nil;

      TMP_31.$$p = null;
      if ((block !== nil)) {
        } else {
        return ($a = ($b = self).$enum_for, $a.$$p = (TMP_32 = function(){var self = TMP_32.$$s || this;

        return self.$size()}, TMP_32.$$s = self, TMP_32), $a).call($b, "reverse_each")
      };
      ($a = ($c = self.$reverse()).$each, $a.$$p = block.$to_proc(), $a).call($c);
      return self;
    });

    Opal.defn(self, '$rindex', TMP_33 = function(object) {
      var self = this, $iter = TMP_33.$$p, block = $iter || nil;

      TMP_33.$$p = null;
      
      var i, value;

      if (object != null) {
        for (i = self.length - 1; i >= 0; i--) {
          if (i >= self.length) {
            break;
          }
          if ((self[i])['$=='](object)) {
            return i;
          }
        }
      }
      else if (block !== nil) {
        for (i = self.length - 1; i >= 0; i--) {
          if (i >= self.length) {
            break;
          }
          if ((value = block(self[i])) === $breaker) {
            return $breaker.$v;
          }
          if (value !== false && value !== nil) {
            return i;
          }
        }
      }
      else if (object == null) {
        return self.$enum_for("rindex");
      }

      return nil;
    
    });

    Opal.defn(self, '$rotate', function(n) {
      var self = this;

      if (n == null) {
        n = 1
      }
      n = $scope.get('Opal').$coerce_to(n, $scope.get('Integer'), "to_int");
      
      var ary, idx, firstPart, lastPart;

      if (self.length === 1) {
        return self.slice();
      }
      if (self.length === 0) {
        return [];
      }

      ary = self.slice();
      idx = n % ary.length;

      firstPart = ary.slice(idx);
      lastPart = ary.slice(0, idx);
      return firstPart.concat(lastPart);
    
    });

    Opal.defn(self, '$rotate!', function(cnt) {
      var self = this, ary = nil;

      if (cnt == null) {
        cnt = 1
      }
      
      if (self.length === 0 || self.length === 1) {
        return self;
      }
    
      cnt = $scope.get('Opal').$coerce_to(cnt, $scope.get('Integer'), "to_int");
      ary = self.$rotate(cnt);
      return self.$replace(ary);
    });

    (function($base, $super) {
      function $SampleRandom(){};
      var self = $SampleRandom = $klass($base, $super, 'SampleRandom', $SampleRandom);

      var def = self.$$proto, $scope = self.$$scope;

      def.rng = nil;
      Opal.defn(self, '$initialize', function(rng) {
        var self = this;

        return self.rng = rng;
      });

      return (Opal.defn(self, '$rand', function(size) {
        var $a, self = this, random = nil;

        random = $scope.get('Opal').$coerce_to(self.rng.$rand(size), $scope.get('Integer'), "to_int");
        if ((($a = random < 0) !== nil && (!$a.$$is_boolean || $a == true))) {
          self.$raise($scope.get('RangeError'), "random value must be >= 0")};
        if ((($a = random < size) !== nil && (!$a.$$is_boolean || $a == true))) {
          } else {
          self.$raise($scope.get('RangeError'), "random value must be less than Array size")
        };
        return random;
      }), nil) && 'rand';
    })($scope.base, null);

    Opal.defn(self, '$sample', function(count, options) {
      var $a, $b, self = this, o = nil, rng = nil;

      if ((($a = count === undefined) !== nil && (!$a.$$is_boolean || $a == true))) {
        return self.$at($scope.get('Kernel').$rand(self.length))};
      if ((($a = options === undefined) !== nil && (!$a.$$is_boolean || $a == true))) {
        if ((($a = (o = $scope.get('Opal')['$coerce_to?'](count, $scope.get('Hash'), "to_hash"))) !== nil && (!$a.$$is_boolean || $a == true))) {
          options = o;
          count = nil;
          } else {
          options = nil;
          count = $scope.get('Opal').$coerce_to(count, $scope.get('Integer'), "to_int");
        }
        } else {
        count = $scope.get('Opal').$coerce_to(count, $scope.get('Integer'), "to_int");
        options = $scope.get('Opal').$coerce_to(options, $scope.get('Hash'), "to_hash");
      };
      if ((($a = (($b = count !== false && count !== nil) ? count < 0 : count)) !== nil && (!$a.$$is_boolean || $a == true))) {
        self.$raise($scope.get('ArgumentError'), "count must be greater than 0")};
      if (options !== false && options !== nil) {
        rng = options['$[]']("random")};
      if ((($a = (($b = rng !== false && rng !== nil) ? rng['$respond_to?']("rand") : rng)) !== nil && (!$a.$$is_boolean || $a == true))) {
        rng = $scope.get('SampleRandom').$new(rng)
        } else {
        rng = $scope.get('Kernel')
      };
      if (count !== false && count !== nil) {
        } else {
        return self[rng.$rand(self.length)]
      };
      

      var abandon, spin, result, i, j, k, targetIndex, oldValue;

      if (count > self.length) {
        count = self.length;
      }

      switch (count) {
        case 0:
          return [];
          break;
        case 1:
          return [self[rng.$rand(self.length)]];
          break;
        case 2:
          i = rng.$rand(self.length);
          j = rng.$rand(self.length);
          if (i === j) {
            j = i === 0 ? i + 1 : i - 1;
          }
          return [self[i], self[j]];
          break;
        default:
          if (self.length / count > 3) {
            abandon = false;
            spin = 0;

            result = $scope.get('Array').$new(count);
            i = 1;

            result[0] = rng.$rand(self.length);
            while (i < count) {
              k = rng.$rand(self.length);
              j = 0;

              while (j < i) {
                while (k === result[j]) {
                  spin++;
                  if (spin > 100) {
                    abandon = true;
                    break;
                  }
                  k = rng.$rand(self.length);
                }
                if (abandon) { break; }

                j++;
              }

              if (abandon) { break; }

              result[i] = k;

              i++;
            }

            if (!abandon) {
              i = 0;
              while (i < count) {
                result[i] = self[result[i]];
                i++;
              }

              return result;
            }
          }

          result = self.slice();

          for (var c = 0; c < count; c++) {
            targetIndex = rng.$rand(self.length);
            oldValue = result[c];
            result[c] = result[targetIndex];
            result[targetIndex] = oldValue;
          }

          return count === self.length ? result : (result)['$[]'](0, count);
      }
    
    });

    Opal.defn(self, '$select', TMP_34 = function() {
      var $a, $b, TMP_35, self = this, $iter = TMP_34.$$p, block = $iter || nil;

      TMP_34.$$p = null;
      if ((block !== nil)) {
        } else {
        return ($a = ($b = self).$enum_for, $a.$$p = (TMP_35 = function(){var self = TMP_35.$$s || this;

        return self.$size()}, TMP_35.$$s = self, TMP_35), $a).call($b, "select")
      };
      
      var result = [];

      for (var i = 0, length = self.length, item, value; i < length; i++) {
        item = self[i];

        if ((value = Opal.yield1(block, item)) === $breaker) {
          return $breaker.$v;
        }

        if (value !== false && value !== nil) {
          result.push(item);
        }
      }

      return result;
    
    });

    Opal.defn(self, '$select!', TMP_36 = function() {
      var $a, $b, TMP_37, $c, self = this, $iter = TMP_36.$$p, block = $iter || nil;

      TMP_36.$$p = null;
      if ((block !== nil)) {
        } else {
        return ($a = ($b = self).$enum_for, $a.$$p = (TMP_37 = function(){var self = TMP_37.$$s || this;

        return self.$size()}, TMP_37.$$s = self, TMP_37), $a).call($b, "select!")
      };
      
      var original = self.length;
      ($a = ($c = self).$keep_if, $a.$$p = block.$to_proc(), $a).call($c);
      return self.length === original ? nil : self;
    
    });

    Opal.defn(self, '$shift', function(count) {
      var $a, self = this;

      if ((($a = count === undefined) !== nil && (!$a.$$is_boolean || $a == true))) {
        if ((($a = self.length === 0) !== nil && (!$a.$$is_boolean || $a == true))) {
          return nil};
        return self.shift();};
      count = $scope.get('Opal').$coerce_to(count, $scope.get('Integer'), "to_int");
      if ((($a = count < 0) !== nil && (!$a.$$is_boolean || $a == true))) {
        self.$raise($scope.get('ArgumentError'), "negative array size")};
      if ((($a = self.length === 0) !== nil && (!$a.$$is_boolean || $a == true))) {
        return []};
      return self.splice(0, count);
    });

    Opal.alias(self, 'size', 'length');

    Opal.defn(self, '$shuffle', function(rng) {
      var self = this;

      return self.$dup()['$shuffle!'](rng);
    });

    Opal.defn(self, '$shuffle!', function(rng) {
      var self = this;

      
      var randgen, i = self.length, j, tmp;

      if (rng !== undefined) {
        rng = $scope.get('Opal')['$coerce_to?'](rng, $scope.get('Hash'), "to_hash");

        if (rng !== nil) {
          rng = rng['$[]']("random");

          if (rng !== nil && rng['$respond_to?']("rand")) {
            randgen = rng;
          }
        }
      }

      while (i) {
        if (randgen) {
          j = randgen.$rand(i).$to_int();

          if (j < 0) {
            self.$raise($scope.get('RangeError'), "random number too small " + (j))
          }

          if (j >= i) {
            self.$raise($scope.get('RangeError'), "random number too big " + (j))
          }
        }
        else {
          j = Math.floor(Math.random() * i);
        }

        tmp = self[--i];
        self[i] = self[j];
        self[j] = tmp;
      }

      return self;
    ;
    });

    Opal.alias(self, 'slice', '[]');

    Opal.defn(self, '$slice!', function(index, length) {
      var self = this;

      
      if (index < 0) {
        index += self.length;
      }

      if (length != null) {
        return self.splice(index, length);
      }

      if (index < 0 || index >= self.length) {
        return nil;
      }

      return self.splice(index, 1)[0];
    
    });

    Opal.defn(self, '$sort', TMP_38 = function() {
      var $a, self = this, $iter = TMP_38.$$p, block = $iter || nil;

      TMP_38.$$p = null;
      if ((($a = self.length > 1) !== nil && (!$a.$$is_boolean || $a == true))) {
        } else {
        return self
      };
      
      if (block === nil) {
        block = function(a, b) {
          return (a)['$<=>'](b);
        };
      }

      try {
        return self.slice().sort(function(x, y) {
          var ret = block(x, y);

          if (ret === $breaker) {
            throw $breaker;
          }
          else if (ret === nil) {
            self.$raise($scope.get('ArgumentError'), "comparison of " + ((x).$inspect()) + " with " + ((y).$inspect()) + " failed");
          }

          return $rb_gt(ret, 0) ? 1 : ($rb_lt(ret, 0) ? -1 : 0);
        });
      }
      catch (e) {
        if (e === $breaker) {
          return $breaker.$v;
        }
        else {
          throw e;
        }
      }
    ;
    });

    Opal.defn(self, '$sort!', TMP_39 = function() {
      var $a, $b, self = this, $iter = TMP_39.$$p, block = $iter || nil;

      TMP_39.$$p = null;
      
      var result;

      if ((block !== nil)) {
        result = ($a = ($b = (self.slice())).$sort, $a.$$p = block.$to_proc(), $a).call($b);
      }
      else {
        result = (self.slice()).$sort();
      }

      self.length = 0;
      for(var i = 0, length = result.length; i < length; i++) {
        self.push(result[i]);
      }

      return self;
    ;
    });

    Opal.defn(self, '$take', function(count) {
      var self = this;

      
      if (count < 0) {
        self.$raise($scope.get('ArgumentError'));
      }

      return self.slice(0, count);
    ;
    });

    Opal.defn(self, '$take_while', TMP_40 = function() {
      var self = this, $iter = TMP_40.$$p, block = $iter || nil;

      TMP_40.$$p = null;
      
      var result = [];

      for (var i = 0, length = self.length, item, value; i < length; i++) {
        item = self[i];

        if ((value = block(item)) === $breaker) {
          return $breaker.$v;
        }

        if (value === false || value === nil) {
          return result;
        }

        result.push(item);
      }

      return result;
    
    });

    Opal.defn(self, '$to_a', function() {
      var self = this;

      return self;
    });

    Opal.alias(self, 'to_ary', 'to_a');

    Opal.defn(self, '$to_h', function() {
      var self = this;

      
      var i, len = self.length, ary, key, val, hash = $hash2([], {});

      for (i = 0; i < len; i++) {
        ary = $scope.get('Opal')['$coerce_to?'](self[i], $scope.get('Array'), "to_ary");
        if (!ary.$$is_array) {
          self.$raise($scope.get('TypeError'), "wrong element type " + ((ary).$class()) + " at " + (i) + " (expected array)")
        }
        if (ary.length !== 2) {
          self.$raise($scope.get('ArgumentError'), "wrong array length at " + (i) + " (expected 2, was " + ((ary).$length()) + ")")
        }
        key = ary[0];
        val = ary[1];
        Opal.hash_put(hash, key, val);
      }

      return hash;
    ;
    });

    Opal.alias(self, 'to_s', 'inspect');

    Opal.defn(self, '$transpose', function() {
      var $a, $b, TMP_41, self = this, result = nil, max = nil;

      if ((($a = self['$empty?']()) !== nil && (!$a.$$is_boolean || $a == true))) {
        return []};
      result = [];
      max = nil;
      ($a = ($b = self).$each, $a.$$p = (TMP_41 = function(row){var self = TMP_41.$$s || this, $a, $b, TMP_42;
if (row == null) row = nil;
      if ((($a = $scope.get('Array')['$==='](row)) !== nil && (!$a.$$is_boolean || $a == true))) {
          row = row.$to_a()
          } else {
          row = $scope.get('Opal').$coerce_to(row, $scope.get('Array'), "to_ary").$to_a()
        };
        ((($a = max) !== false && $a !== nil) ? $a : max = row.length);
        if ((($a = (row.length)['$!='](max)) !== nil && (!$a.$$is_boolean || $a == true))) {
          self.$raise($scope.get('IndexError'), "element size differs (" + (row.length) + " should be " + (max))};
        return ($a = ($b = (row.length)).$times, $a.$$p = (TMP_42 = function(i){var self = TMP_42.$$s || this, $a, $b, $c, entry = nil;
if (i == null) i = nil;
        entry = (($a = i, $b = result, ((($c = $b['$[]']($a)) !== false && $c !== nil) ? $c : $b['$[]=']($a, []))));
          return entry['$<<'](row.$at(i));}, TMP_42.$$s = self, TMP_42), $a).call($b);}, TMP_41.$$s = self, TMP_41), $a).call($b);
      return result;
    });

    Opal.defn(self, '$uniq', TMP_43 = function() {
      var self = this, $iter = TMP_43.$$p, block = $iter || nil;

      TMP_43.$$p = null;
      
      var hash = $hash2([], {}), i, length, item, key;

      if (block === nil) {
        for (i = 0, length = self.length; i < length; i++) {
          item = self[i];
          if (Opal.hash_get(hash, item) === undefined) {
            Opal.hash_put(hash, item, item);
          }
        }
      }
      else {
        for (i = 0, length = self.length; i < length; i++) {
          item = self[i];
          key = Opal.yield1(block, item);
          if (Opal.hash_get(hash, key) === undefined) {
            Opal.hash_put(hash, key, item);
          }
        }
      }

      return hash.$values();
    ;
    });

    Opal.defn(self, '$uniq!', TMP_44 = function() {
      var self = this, $iter = TMP_44.$$p, block = $iter || nil;

      TMP_44.$$p = null;
      
      var original_length = self.length, hash = $hash2([], {}), i, length, item, key;

      for (i = 0, length = original_length; i < length; i++) {
        item = self[i];
        key = (block === nil ? item : Opal.yield1(block, item));

        if (Opal.hash_get(hash, key) === undefined) {
          Opal.hash_put(hash, key, item);
          continue;
        }

        self.splice(i, 1);
        length--;
        i--;
      }

      return self.length === original_length ? nil : self;
    ;
    });

    Opal.defn(self, '$unshift', function() {
      var self = this, $splat_index = nil;

      var array_size = arguments.length - 0;
      if(array_size < 0) array_size = 0;
      var objects = new Array(array_size);
      for($splat_index = 0; $splat_index < array_size; $splat_index++) {
        objects[$splat_index] = arguments[$splat_index + 0];
      }
      
      for (var i = objects.length - 1; i >= 0; i--) {
        self.unshift(objects[i]);
      }
    
      return self;
    });

    Opal.defn(self, '$values_at', function() {
      var $a, $b, TMP_45, self = this, out = nil, $splat_index = nil;

      var array_size = arguments.length - 0;
      if(array_size < 0) array_size = 0;
      var args = new Array(array_size);
      for($splat_index = 0; $splat_index < array_size; $splat_index++) {
        args[$splat_index] = arguments[$splat_index + 0];
      }
      out = [];
      ($a = ($b = args).$each, $a.$$p = (TMP_45 = function(elem){var self = TMP_45.$$s || this, $a, $b, TMP_46, finish = nil, start = nil, i = nil;
if (elem == null) elem = nil;
      if ((($a = elem['$kind_of?']($scope.get('Range'))) !== nil && (!$a.$$is_boolean || $a == true))) {
          finish = $scope.get('Opal').$coerce_to(elem.$last(), $scope.get('Integer'), "to_int");
          start = $scope.get('Opal').$coerce_to(elem.$first(), $scope.get('Integer'), "to_int");
          
          if (start < 0) {
            start = start + self.length;
            return nil;;
          }
        
          
          if (finish < 0) {
            finish = finish + self.length;
          }
          if (elem['$exclude_end?']()) {
            finish--;
          }
          if (finish < start) {
            return nil;;
          }
        
          return ($a = ($b = start).$upto, $a.$$p = (TMP_46 = function(i){var self = TMP_46.$$s || this;
if (i == null) i = nil;
          return out['$<<'](self.$at(i))}, TMP_46.$$s = self, TMP_46), $a).call($b, finish);
          } else {
          i = $scope.get('Opal').$coerce_to(elem, $scope.get('Integer'), "to_int");
          return out['$<<'](self.$at(i));
        }}, TMP_45.$$s = self, TMP_45), $a).call($b);
      return out;
    });

    return (Opal.defn(self, '$zip', TMP_47 = function() {
      var $a, self = this, $iter = TMP_47.$$p, block = $iter || nil, $splat_index = nil;

      var array_size = arguments.length - 0;
      if(array_size < 0) array_size = 0;
      var others = new Array(array_size);
      for($splat_index = 0; $splat_index < array_size; $splat_index++) {
        others[$splat_index] = arguments[$splat_index + 0];
      }
      TMP_47.$$p = null;
      
      var result = [], size = self.length, part, o, i, j, jj;

      for (j = 0, jj = others.length; j < jj; j++) {
        o = others[j];
        if (o.$$is_array) {
          continue;
        }
        if (o.$$is_enumerator) {
          if (o.$size() === Infinity) {
            others[j] = o.$take(size);
          } else {
            others[j] = o.$to_a();
          }
          continue;
        }
        others[j] = (((($a = $scope.get('Opal')['$coerce_to?'](o, $scope.get('Array'), "to_ary")) !== false && $a !== nil) ? $a : $scope.get('Opal')['$coerce_to!'](o, $scope.get('Enumerator'), "each"))).$to_a();
      }

      for (i = 0; i < size; i++) {
        part = [self[i]];

        for (j = 0, jj = others.length; j < jj; j++) {
          o = others[j][i];

          if (o == null) {
            o = nil;
          }

          part[j + 1] = o;
        }

        result[i] = part;
      }

      if (block !== nil) {
        for (i = 0; i < size; i++) {
          block(result[i]);
        }

        return nil;
      }

      return result;
    
    }), nil) && 'zip';
  })($scope.base, Array);
};

/* Generated by Opal 0.9.0 */
Opal.modules["corelib/hash"] = function(Opal) {
  Opal.dynamic_require_severity = "error";
  var OPAL_CONFIG = { method_missing: true, arity_check: false, freezing: true, tainting: true };
  var self = Opal.top, $scope = Opal, nil = Opal.nil, $breaker = Opal.breaker, $slice = Opal.slice, $klass = Opal.klass;

  Opal.add_stubs(['$require', '$include', '$coerce_to?', '$[]', '$merge!', '$allocate', '$raise', '$==', '$coerce_to!', '$lambda?', '$abs', '$arity', '$call', '$enum_for', '$size', '$inspect', '$flatten', '$eql?', '$default', '$to_proc', '$dup', '$===', '$default_proc', '$default_proc=', '$default=', '$alias_method']);
  self.$require("corelib/enumerable");
  return (function($base, $super) {
    function $Hash(){};
    var self = $Hash = $klass($base, $super, 'Hash', $Hash);

    var def = self.$$proto, $scope = self.$$scope, TMP_1, TMP_2, TMP_3, TMP_5, TMP_7, TMP_9, TMP_11, TMP_12, TMP_14, TMP_15, TMP_16, TMP_18, TMP_20, TMP_22;

    def.proc = def.none = nil;
    self.$include($scope.get('Enumerable'));

    def.$$is_hash = true;

    Opal.defs(self, '$[]', function() {
      var self = this, $splat_index = nil;

      var array_size = arguments.length - 0;
      if(array_size < 0) array_size = 0;
      var argv = new Array(array_size);
      for($splat_index = 0; $splat_index < array_size; $splat_index++) {
        argv[$splat_index] = arguments[$splat_index + 0];
      }
      
      var hash, argc = argv.length, i;

      if (argc === 1) {
        hash = $scope.get('Opal')['$coerce_to?'](argv['$[]'](0), $scope.get('Hash'), "to_hash");
        if (hash !== nil) {
          return self.$allocate()['$merge!'](hash);
        }

        argv = $scope.get('Opal')['$coerce_to?'](argv['$[]'](0), $scope.get('Array'), "to_ary");
        if (argv === nil) {
          self.$raise($scope.get('ArgumentError'), "odd number of arguments for Hash")
        }

        argc = argv.length;
        hash = self.$allocate();

        for (i = 0; i < argc; i++) {
          if (!argv[i].$$is_array) continue;
          switch(argv[i].length) {
          case 1:
            hash.$store(argv[i][0], nil);
            break;
          case 2:
            hash.$store(argv[i][0], argv[i][1]);
            break;
          default:
            self.$raise($scope.get('ArgumentError'), "invalid number of elements (" + (argv[i].length) + " for 1..2)")
          }
        }

        return hash;
      }

      if (argc % 2 !== 0) {
        self.$raise($scope.get('ArgumentError'), "odd number of arguments for Hash")
      }

      hash = self.$allocate();

      for (i = 0; i < argc; i += 2) {
        hash.$store(argv[i], argv[i + 1]);
      }

      return hash;
    ;
    });

    Opal.defs(self, '$allocate', function() {
      var self = this;

      
      var hash = new self.$$alloc();

      Opal.hash_init(hash);

      hash.none = nil;
      hash.proc = nil;

      return hash;
    
    });

    Opal.defs(self, '$try_convert', function(obj) {
      var self = this;

      return $scope.get('Opal')['$coerce_to?'](obj, $scope.get('Hash'), "to_hash");
    });

    Opal.defn(self, '$initialize', TMP_1 = function(defaults) {
      var self = this, $iter = TMP_1.$$p, block = $iter || nil;

      TMP_1.$$p = null;
      
      if (defaults !== undefined && block !== nil) {
        self.$raise($scope.get('ArgumentError'), "wrong number of arguments (1 for 0)")
      }
      self.none = (defaults === undefined ? nil : defaults);
      self.proc = block;
    ;
      return self;
    });

    Opal.defn(self, '$==', function(other) {
      var self = this;

      
      if (self === other) {
        return true;
      }

      if (!other.$$is_hash) {
        return false;
      }

      if (self.$$keys.length !== other.$$keys.length) {
        return false;
      }

      for (var i = 0, keys = self.$$keys, length = keys.length, key, value, other_value; i < length; i++) {
        key = keys[i];

        if (key.$$is_string) {
          value = self.$$smap[key];
          other_value = other.$$smap[key];
        } else {
          value = key.value;
          other_value = Opal.hash_get(other, key.key);
        }

        if (other_value === undefined || !value['$eql?'](other_value)) {
          return false;
        }
      }

      return true;
    
    });

    Opal.defn(self, '$[]', function(key) {
      var self = this;

      
      var value = Opal.hash_get(self, key);

      if (value !== undefined) {
        return value;
      }

      return self.$default(key);
    
    });

    Opal.defn(self, '$[]=', function(key, value) {
      var self = this;

      
      Opal.hash_put(self, key, value);
      return value;
    
    });

    Opal.defn(self, '$assoc', function(object) {
      var self = this;

      
      for (var i = 0, keys = self.$$keys, length = keys.length, key; i < length; i++) {
        key = keys[i];

        if (key.$$is_string) {
          if ((key)['$=='](object)) {
            return [key, self.$$smap[key]];
          }
        } else {
          if ((key.key)['$=='](object)) {
            return [key.key, key.value];
          }
        }
      }

      return nil;
    
    });

    Opal.defn(self, '$clear', function() {
      var self = this;

      
      Opal.hash_init(self);
      return self;
    
    });

    Opal.defn(self, '$clone', function() {
      var self = this;

      
      var hash = new self.$$class.$$alloc();

      Opal.hash_init(hash);
      Opal.hash_clone(self, hash);

      return hash;
    
    });

    Opal.defn(self, '$default', function(key) {
      var self = this;

      
      if (key !== undefined && self.proc !== nil) {
        return self.proc.$call(self, key);
      }
      return self.none;
    ;
    });

    Opal.defn(self, '$default=', function(object) {
      var self = this;

      
      self.proc = nil;
      self.none = object;

      return object;
    
    });

    Opal.defn(self, '$default_proc', function() {
      var self = this;

      return self.proc;
    });

    Opal.defn(self, '$default_proc=', function(proc) {
      var self = this;

      
      if (proc !== nil) {
        proc = $scope.get('Opal')['$coerce_to!'](proc, $scope.get('Proc'), "to_proc");

        if (proc['$lambda?']() && proc.$arity().$abs() !== 2) {
          self.$raise($scope.get('TypeError'), "default_proc takes two arguments");
        }
      }

      self.none = nil;
      self.proc = proc;

      return proc;
    ;
    });

    Opal.defn(self, '$delete', TMP_2 = function(key) {
      var self = this, $iter = TMP_2.$$p, block = $iter || nil;

      TMP_2.$$p = null;
      
      var value = Opal.hash_delete(self, key);

      if (value !== undefined) {
        return value;
      }

      if (block !== nil) {
        return block.$call(key);
      }

      return nil;
    
    });

    Opal.defn(self, '$delete_if', TMP_3 = function() {
      var $a, $b, TMP_4, self = this, $iter = TMP_3.$$p, block = $iter || nil;

      TMP_3.$$p = null;
      if (block !== false && block !== nil) {
        } else {
        return ($a = ($b = self).$enum_for, $a.$$p = (TMP_4 = function(){var self = TMP_4.$$s || this;

        return self.$size()}, TMP_4.$$s = self, TMP_4), $a).call($b, "delete_if")
      };
      
      for (var i = 0, keys = self.$$keys, length = keys.length, key, value, obj; i < length; i++) {
        key = keys[i];

        if (key.$$is_string) {
          value = self.$$smap[key];
        } else {
          value = key.value;
          key = key.key;
        }

        obj = block(key, value);

        if (obj === $breaker) {
          return $breaker.$v;
        }

        if (obj !== false && obj !== nil) {
          if (Opal.hash_delete(self, key) !== undefined) {
            length--;
            i--;
          }
        }
      }

      return self;
    
    });

    Opal.alias(self, 'dup', 'clone');

    Opal.defn(self, '$each', TMP_5 = function() {
      var $a, $b, TMP_6, self = this, $iter = TMP_5.$$p, block = $iter || nil;

      TMP_5.$$p = null;
      if (block !== false && block !== nil) {
        } else {
        return ($a = ($b = self).$enum_for, $a.$$p = (TMP_6 = function(){var self = TMP_6.$$s || this;

        return self.$size()}, TMP_6.$$s = self, TMP_6), $a).call($b, "each")
      };
      
      for (var i = 0, keys = self.$$keys, length = keys.length, key, value, obj; i < length; i++) {
        key = keys[i];

        if (key.$$is_string) {
          value = self.$$smap[key];
        } else {
          value = key.value;
          key = key.key;
        }

        obj = Opal.yield1(block, [key, value]);

        if (obj === $breaker) {
          return $breaker.$v;
        }
      }

      return self;
    
    });

    Opal.defn(self, '$each_key', TMP_7 = function() {
      var $a, $b, TMP_8, self = this, $iter = TMP_7.$$p, block = $iter || nil;

      TMP_7.$$p = null;
      if (block !== false && block !== nil) {
        } else {
        return ($a = ($b = self).$enum_for, $a.$$p = (TMP_8 = function(){var self = TMP_8.$$s || this;

        return self.$size()}, TMP_8.$$s = self, TMP_8), $a).call($b, "each_key")
      };
      
      for (var i = 0, keys = self.$$keys, length = keys.length, key; i < length; i++) {
        key = keys[i];

        if (block(key.$$is_string ? key : key.key) === $breaker) {
          return $breaker.$v;
        }
      }

      return self;
    
    });

    Opal.alias(self, 'each_pair', 'each');

    Opal.defn(self, '$each_value', TMP_9 = function() {
      var $a, $b, TMP_10, self = this, $iter = TMP_9.$$p, block = $iter || nil;

      TMP_9.$$p = null;
      if (block !== false && block !== nil) {
        } else {
        return ($a = ($b = self).$enum_for, $a.$$p = (TMP_10 = function(){var self = TMP_10.$$s || this;

        return self.$size()}, TMP_10.$$s = self, TMP_10), $a).call($b, "each_value")
      };
      
      for (var i = 0, keys = self.$$keys, length = keys.length, key; i < length; i++) {
        key = keys[i];

        if (block(key.$$is_string ? self.$$smap[key] : key.value) === $breaker) {
          return $breaker.$v;
        }
      }

      return self;
    
    });

    Opal.defn(self, '$empty?', function() {
      var self = this;

      return self.$$keys.length === 0;
    });

    Opal.alias(self, 'eql?', '==');

    Opal.defn(self, '$fetch', TMP_11 = function(key, defaults) {
      var self = this, $iter = TMP_11.$$p, block = $iter || nil;

      TMP_11.$$p = null;
      
      var value = Opal.hash_get(self, key);

      if (value !== undefined) {
        return value;
      }

      if (block !== nil) {
        value = block(key);

        if (value === $breaker) {
          return $breaker.$v;
        }

        return value;
      }

      if (defaults !== undefined) {
        return defaults;
      }
    
      return self.$raise($scope.get('KeyError'), "key not found: " + (key.$inspect()));
    });

    Opal.defn(self, '$flatten', function(level) {
      var self = this;

      if (level == null) {
        level = 1
      }
      level = $scope.get('Opal')['$coerce_to!'](level, $scope.get('Integer'), "to_int");
      
      var result = [];

      for (var i = 0, keys = self.$$keys, length = keys.length, key, value; i < length; i++) {
        key = keys[i];

        if (key.$$is_string) {
          value = self.$$smap[key];
        } else {
          value = key.value;
          key = key.key;
        }

        result.push(key);

        if (value.$$is_array) {
          if (level === 1) {
            result.push(value);
            continue;
          }

          result = result.concat((value).$flatten(level - 2));
          continue;
        }

        result.push(value);
      }

      return result;
    
    });

    Opal.defn(self, '$has_key?', function(key) {
      var self = this;

      return Opal.hash_get(self, key) !== undefined;
    });

    Opal.defn(self, '$has_value?', function(value) {
      var self = this;

      
      for (var i = 0, keys = self.$$keys, length = keys.length, key; i < length; i++) {
        key = keys[i];

        if (((key.$$is_string ? self.$$smap[key] : key.value))['$=='](value)) {
          return true;
        }
      }

      return false;
    
    });

    Opal.defn(self, '$hash', function() {
      var self = this;

      
      var top = (Opal.hash_ids === undefined),
          hash_id = self.$object_id(),
          result = ['Hash'],
          key, item;

      try {
        if (top) {
          Opal.hash_ids = {};
        }

        if (Opal.hash_ids.hasOwnProperty(hash_id)) {
          return 'self';
        }

        for (key in Opal.hash_ids) {
          if (Opal.hash_ids.hasOwnProperty(key)) {
            item = Opal.hash_ids[key];
            if (self['$eql?'](item)) {
              return 'self';
            }
          }
        }

        Opal.hash_ids[hash_id] = self;

        for (var i = 0, keys = self.$$keys, length = keys.length; i < length; i++) {
          key = keys[i];

          if (key.$$is_string) {
            result.push([key, self.$$smap[key].$hash()]);
          } else {
            result.push([key.key_hash, key.value.$hash()]);
          }
        }

        return result.sort().join();

      } finally {
        if (top) {
          delete Opal.hash_ids;
        }
      }
    
    });

    Opal.alias(self, 'include?', 'has_key?');

    Opal.defn(self, '$index', function(object) {
      var self = this;

      
      for (var i = 0, keys = self.$$keys, length = keys.length, key, value; i < length; i++) {
        key = keys[i];

        if (key.$$is_string) {
          value = self.$$smap[key];
        } else {
          value = key.value;
          key = key.key;
        }

        if ((value)['$=='](object)) {
          return key;
        }
      }

      return nil;
    
    });

    Opal.defn(self, '$indexes', function() {
      var self = this, $splat_index = nil;

      var array_size = arguments.length - 0;
      if(array_size < 0) array_size = 0;
      var args = new Array(array_size);
      for($splat_index = 0; $splat_index < array_size; $splat_index++) {
        args[$splat_index] = arguments[$splat_index + 0];
      }
      
      var result = [];

      for (var i = 0, length = args.length, key, value; i < length; i++) {
        key = args[i];
        value = Opal.hash_get(self, key);

        if (value === undefined) {
          result.push(self.$default());
          continue;
        }

        result.push(value);
      }

      return result;
    
    });

    Opal.alias(self, 'indices', 'indexes');

    var inspect_ids;

    Opal.defn(self, '$inspect', function() {
      var self = this;

      
      var top = (inspect_ids === undefined),
          hash_id = self.$object_id(),
          result = [];

      try {
        if (top) {
          inspect_ids = {};
        }

        if (inspect_ids.hasOwnProperty(hash_id)) {
          return '{...}';
        }

        inspect_ids[hash_id] = true;

        for (var i = 0, keys = self.$$keys, length = keys.length, key, value; i < length; i++) {
          key = keys[i];

          if (key.$$is_string) {
            value = self.$$smap[key];
          } else {
            value = key.value;
            key = key.key;
          }

          result.push(key.$inspect() + '=>' + value.$inspect());
        }

        return '{' + result.join(', ') + '}';

      } finally {
        if (top) {
          inspect_ids = undefined;
        }
      }
    
    });

    Opal.defn(self, '$invert', function() {
      var self = this;

      
      var hash = Opal.hash();

      for (var i = 0, keys = self.$$keys, length = keys.length, key, value; i < length; i++) {
        key = keys[i];

        if (key.$$is_string) {
          value = self.$$smap[key];
        } else {
          value = key.value;
          key = key.key;
        }

        Opal.hash_put(hash, value, key);
      }

      return hash;
    
    });

    Opal.defn(self, '$keep_if', TMP_12 = function() {
      var $a, $b, TMP_13, self = this, $iter = TMP_12.$$p, block = $iter || nil;

      TMP_12.$$p = null;
      if (block !== false && block !== nil) {
        } else {
        return ($a = ($b = self).$enum_for, $a.$$p = (TMP_13 = function(){var self = TMP_13.$$s || this;

        return self.$size()}, TMP_13.$$s = self, TMP_13), $a).call($b, "keep_if")
      };
      
      for (var i = 0, keys = self.$$keys, length = keys.length, key, value, obj; i < length; i++) {
        key = keys[i];

        if (key.$$is_string) {
          value = self.$$smap[key];
        } else {
          value = key.value;
          key = key.key;
        }

        obj = block(key, value);

        if (obj === $breaker) {
          return $breaker.$v;
        }

        if (obj === false || obj === nil) {
          if (Opal.hash_delete(self, key) !== undefined) {
            length--;
            i--;
          }
        }
      }

      return self;
    
    });

    Opal.alias(self, 'key', 'index');

    Opal.alias(self, 'key?', 'has_key?');

    Opal.defn(self, '$keys', function() {
      var self = this;

      
      var result = [];

      for (var i = 0, keys = self.$$keys, length = keys.length, key; i < length; i++) {
        key = keys[i];

        if (key.$$is_string) {
          result.push(key);
        } else {
          result.push(key.key);
        }
      }

      return result;
    
    });

    Opal.defn(self, '$length', function() {
      var self = this;

      return self.$$keys.length;
    });

    Opal.alias(self, 'member?', 'has_key?');

    Opal.defn(self, '$merge', TMP_14 = function(other) {
      var $a, $b, self = this, $iter = TMP_14.$$p, block = $iter || nil;

      TMP_14.$$p = null;
      return ($a = ($b = self.$dup())['$merge!'], $a.$$p = block.$to_proc(), $a).call($b, other);
    });

    Opal.defn(self, '$merge!', TMP_15 = function(other) {
      var self = this, $iter = TMP_15.$$p, block = $iter || nil;

      TMP_15.$$p = null;
      
      if (!$scope.get('Hash')['$==='](other)) {
        other = $scope.get('Opal')['$coerce_to!'](other, $scope.get('Hash'), "to_hash");
      }

      var i, other_keys = other.$$keys, length = other_keys.length, key, value, other_value;

      if (block === nil) {
        for (i = 0; i < length; i++) {
          key = other_keys[i];

          if (key.$$is_string) {
            other_value = other.$$smap[key];
          } else {
            other_value = key.value;
            key = key.key;
          }

          Opal.hash_put(self, key, other_value);
        }

        return self;
      }

      for (i = 0; i < length; i++) {
        key = other_keys[i];

        if (key.$$is_string) {
          other_value = other.$$smap[key];
        } else {
          other_value = key.value;
          key = key.key;
        }

        value = Opal.hash_get(self, key);

        if (value === undefined) {
          Opal.hash_put(self, key, other_value);
          continue;
        }

        Opal.hash_put(self, key, block(key, value, other_value));
      }

      return self;
    ;
    });

    Opal.defn(self, '$rassoc', function(object) {
      var self = this;

      
      for (var i = 0, keys = self.$$keys, length = keys.length, key, value; i < length; i++) {
        key = keys[i];

        if (key.$$is_string) {
          value = self.$$smap[key];
        } else {
          value = key.value;
          key = key.key;
        }

        if ((value)['$=='](object)) {
          return [key, value];
        }
      }

      return nil;
    
    });

    Opal.defn(self, '$rehash', function() {
      var self = this;

      
      Opal.hash_rehash(self);
      return self;
    
    });

    Opal.defn(self, '$reject', TMP_16 = function() {
      var $a, $b, TMP_17, self = this, $iter = TMP_16.$$p, block = $iter || nil;

      TMP_16.$$p = null;
      if (block !== false && block !== nil) {
        } else {
        return ($a = ($b = self).$enum_for, $a.$$p = (TMP_17 = function(){var self = TMP_17.$$s || this;

        return self.$size()}, TMP_17.$$s = self, TMP_17), $a).call($b, "reject")
      };
      
      var hash = Opal.hash();

      for (var i = 0, keys = self.$$keys, length = keys.length, key, value, obj; i < length; i++) {
        key = keys[i];

        if (key.$$is_string) {
          value = self.$$smap[key];
        } else {
          value = key.value;
          key = key.key;
        }

        obj = block(key, value);

        if (obj === $breaker) {
          return $breaker.$v;
        }

        if (obj === false || obj === nil) {
          Opal.hash_put(hash, key, value);
        }
      }

      return hash;
    
    });

    Opal.defn(self, '$reject!', TMP_18 = function() {
      var $a, $b, TMP_19, self = this, $iter = TMP_18.$$p, block = $iter || nil;

      TMP_18.$$p = null;
      if (block !== false && block !== nil) {
        } else {
        return ($a = ($b = self).$enum_for, $a.$$p = (TMP_19 = function(){var self = TMP_19.$$s || this;

        return self.$size()}, TMP_19.$$s = self, TMP_19), $a).call($b, "reject!")
      };
      
      var changes_were_made = false;

      for (var i = 0, keys = self.$$keys, length = keys.length, key, value, obj; i < length; i++) {
        key = keys[i];

        if (key.$$is_string) {
          value = self.$$smap[key];
        } else {
          value = key.value;
          key = key.key;
        }

        obj = block(key, value);

        if (obj === $breaker) {
          return $breaker.$v;
        }

        if (obj !== false && obj !== nil) {
          if (Opal.hash_delete(self, key) !== undefined) {
            changes_were_made = true;
            length--;
            i--;
          }
        }
      }

      return changes_were_made ? self : nil;
    
    });

    Opal.defn(self, '$replace', function(other) {
      var $a, $b, self = this;

      other = $scope.get('Opal')['$coerce_to!'](other, $scope.get('Hash'), "to_hash");
      
      Opal.hash_init(self);

      for (var i = 0, other_keys = other.$$keys, length = other_keys.length, key, value, other_value; i < length; i++) {
        key = other_keys[i];

        if (key.$$is_string) {
          other_value = other.$$smap[key];
        } else {
          other_value = key.value;
          key = key.key;
        }

        Opal.hash_put(self, key, other_value);
      }
    
      if ((($a = other.$default_proc()) !== nil && (!$a.$$is_boolean || $a == true))) {
        (($a = [other.$default_proc()]), $b = self, $b['$default_proc='].apply($b, $a), $a[$a.length-1])
        } else {
        (($a = [other.$default()]), $b = self, $b['$default='].apply($b, $a), $a[$a.length-1])
      };
      return self;
    });

    Opal.defn(self, '$select', TMP_20 = function() {
      var $a, $b, TMP_21, self = this, $iter = TMP_20.$$p, block = $iter || nil;

      TMP_20.$$p = null;
      if (block !== false && block !== nil) {
        } else {
        return ($a = ($b = self).$enum_for, $a.$$p = (TMP_21 = function(){var self = TMP_21.$$s || this;

        return self.$size()}, TMP_21.$$s = self, TMP_21), $a).call($b, "select")
      };
      
      var hash = Opal.hash();

      for (var i = 0, keys = self.$$keys, length = keys.length, key, value, obj; i < length; i++) {
        key = keys[i];

        if (key.$$is_string) {
          value = self.$$smap[key];
        } else {
          value = key.value;
          key = key.key;
        }

        obj = block(key, value);

        if (obj === $breaker) {
          return $breaker.$v;
        }

        if (obj !== false && obj !== nil) {
          Opal.hash_put(hash, key, value);
        }
      }

      return hash;
    
    });

    Opal.defn(self, '$select!', TMP_22 = function() {
      var $a, $b, TMP_23, self = this, $iter = TMP_22.$$p, block = $iter || nil;

      TMP_22.$$p = null;
      if (block !== false && block !== nil) {
        } else {
        return ($a = ($b = self).$enum_for, $a.$$p = (TMP_23 = function(){var self = TMP_23.$$s || this;

        return self.$size()}, TMP_23.$$s = self, TMP_23), $a).call($b, "select!")
      };
      
      var result = nil;

      for (var i = 0, keys = self.$$keys, length = keys.length, key, value, obj; i < length; i++) {
        key = keys[i];

        if (key.$$is_string) {
          value = self.$$smap[key];
        } else {
          value = key.value;
          key = key.key;
        }

        obj = block(key, value);

        if (obj === $breaker) {
          return $breaker.$v;
        }

        if (obj === false || obj === nil) {
          if (Opal.hash_delete(self, key) !== undefined) {
            length--;
            i--;
          }
          result = self;
        }
      }

      return result;
    
    });

    Opal.defn(self, '$shift', function() {
      var self = this;

      
      var keys = self.$$keys,
          key;

      if (keys.length > 0) {
        key = keys[0];

        key = key.$$is_string ? key : key.key;

        return [key, Opal.hash_delete(self, key)];
      }

      return self.$default(nil);
    
    });

    Opal.alias(self, 'size', 'length');

    self.$alias_method("store", "[]=");

    Opal.defn(self, '$to_a', function() {
      var self = this;

      
      var result = [];

      for (var i = 0, keys = self.$$keys, length = keys.length, key, value; i < length; i++) {
        key = keys[i];

        if (key.$$is_string) {
          value = self.$$smap[key];
        } else {
          value = key.value;
          key = key.key;
        }

        result.push([key, value]);
      }

      return result;
    
    });

    Opal.defn(self, '$to_h', function() {
      var self = this;

      
      if (self.$$class === Opal.Hash) {
        return self;
      }

      var hash = new Opal.Hash.$$alloc();

      Opal.hash_init(hash);
      Opal.hash_clone(self, hash);

      return hash;
    
    });

    Opal.defn(self, '$to_hash', function() {
      var self = this;

      return self;
    });

    Opal.alias(self, 'to_s', 'inspect');

    Opal.alias(self, 'update', 'merge!');

    Opal.alias(self, 'value?', 'has_value?');

    Opal.alias(self, 'values_at', 'indexes');

    return (Opal.defn(self, '$values', function() {
      var self = this;

      
      var result = [];

      for (var i = 0, keys = self.$$keys, length = keys.length, key; i < length; i++) {
        key = keys[i];

        if (key.$$is_string) {
          result.push(self.$$smap[key]);
        } else {
          result.push(key.value);
        }
      }

      return result;
    
    }), nil) && 'values';
  })($scope.base, null);
};

/* Generated by Opal 0.9.0 */
Opal.modules["corelib/number"] = function(Opal) {
  Opal.dynamic_require_severity = "error";
  var OPAL_CONFIG = { method_missing: true, arity_check: false, freezing: true, tainting: true };
  function $rb_gt(lhs, rhs) {
    return (typeof(lhs) === 'number' && typeof(rhs) === 'number') ? lhs > rhs : lhs['$>'](rhs);
  }
  function $rb_lt(lhs, rhs) {
    return (typeof(lhs) === 'number' && typeof(rhs) === 'number') ? lhs < rhs : lhs['$<'](rhs);
  }
  function $rb_plus(lhs, rhs) {
    return (typeof(lhs) === 'number' && typeof(rhs) === 'number') ? lhs + rhs : lhs['$+'](rhs);
  }
  function $rb_minus(lhs, rhs) {
    return (typeof(lhs) === 'number' && typeof(rhs) === 'number') ? lhs - rhs : lhs['$-'](rhs);
  }
  function $rb_divide(lhs, rhs) {
    return (typeof(lhs) === 'number' && typeof(rhs) === 'number') ? lhs / rhs : lhs['$/'](rhs);
  }
  function $rb_times(lhs, rhs) {
    return (typeof(lhs) === 'number' && typeof(rhs) === 'number') ? lhs * rhs : lhs['$*'](rhs);
  }
  function $rb_le(lhs, rhs) {
    return (typeof(lhs) === 'number' && typeof(rhs) === 'number') ? lhs <= rhs : lhs['$<='](rhs);
  }
  function $rb_ge(lhs, rhs) {
    return (typeof(lhs) === 'number' && typeof(rhs) === 'number') ? lhs >= rhs : lhs['$>='](rhs);
  }
  var self = Opal.top, $scope = Opal, nil = Opal.nil, $breaker = Opal.breaker, $slice = Opal.slice, $klass = Opal.klass;

  Opal.add_stubs(['$require', '$bridge', '$raise', '$class', '$Float', '$respond_to?', '$coerce_to!', '$__coerced__', '$===', '$!', '$>', '$**', '$new', '$<', '$to_f', '$==', '$nan?', '$infinite?', '$enum_for', '$+', '$-', '$gcd', '$lcm', '$/', '$frexp', '$to_i', '$ldexp', '$rationalize', '$*', '$<<', '$to_r', '$-@', '$size', '$<=', '$>=']);
  self.$require("corelib/numeric");
  (function($base, $super) {
    function $Number(){};
    var self = $Number = $klass($base, $super, 'Number', $Number);

    var def = self.$$proto, $scope = self.$$scope, TMP_1, TMP_2, TMP_4, TMP_5, TMP_6, TMP_7, TMP_8, TMP_9, TMP_10, TMP_11;

    $scope.get('Opal').$bridge(self, Number);

    Number.prototype.$$is_number = true;

    Opal.defn(self, '$coerce', function(other) {
      var self = this;

      
      if (other === nil) {
        self.$raise($scope.get('TypeError'), "can't convert " + (other.$class()) + " into Float");
      }
      else if (other.$$is_string) {
        return [self.$Float(other), self];
      }
      else if (other['$respond_to?']("to_f")) {
        return [$scope.get('Opal')['$coerce_to!'](other, $scope.get('Float'), "to_f"), self];
      }
      else if (other.$$is_number) {
        return [other, self];
      }
      else {
        self.$raise($scope.get('TypeError'), "can't convert " + (other.$class()) + " into Float");
      }
    ;
    });

    Opal.defn(self, '$__id__', function() {
      var self = this;

      return (self * 2) + 1;
    });

    Opal.alias(self, 'object_id', '__id__');

    Opal.defn(self, '$+', function(other) {
      var self = this;

      
      if (other.$$is_number) {
        return self + other;
      }
      else {
        return self.$__coerced__("+", other);
      }
    
    });

    Opal.defn(self, '$-', function(other) {
      var self = this;

      
      if (other.$$is_number) {
        return self - other;
      }
      else {
        return self.$__coerced__("-", other);
      }
    
    });

    Opal.defn(self, '$*', function(other) {
      var self = this;

      
      if (other.$$is_number) {
        return self * other;
      }
      else {
        return self.$__coerced__("*", other);
      }
    
    });

    Opal.defn(self, '$/', function(other) {
      var self = this;

      
      if (other.$$is_number) {
        return self / other;
      }
      else {
        return self.$__coerced__("/", other);
      }
    
    });

    Opal.alias(self, 'fdiv', '/');

    Opal.defn(self, '$%', function(other) {
      var self = this;

      
      if (other.$$is_number) {
        if (other == -Infinity) {
          return other;
        }
        else if (other == 0) {
          self.$raise($scope.get('ZeroDivisionError'), "divided by 0");
        }
        else if (other < 0 || self < 0) {
          return (self % other + other) % other;
        }
        else {
          return self % other;
        }
      }
      else {
        return self.$__coerced__("%", other);
      }
    
    });

    Opal.defn(self, '$&', function(other) {
      var self = this;

      
      if (other.$$is_number) {
        return self & other;
      }
      else {
        return self.$__coerced__("&", other);
      }
    
    });

    Opal.defn(self, '$|', function(other) {
      var self = this;

      
      if (other.$$is_number) {
        return self | other;
      }
      else {
        return self.$__coerced__("|", other);
      }
    
    });

    Opal.defn(self, '$^', function(other) {
      var self = this;

      
      if (other.$$is_number) {
        return self ^ other;
      }
      else {
        return self.$__coerced__("^", other);
      }
    
    });

    Opal.defn(self, '$<', function(other) {
      var self = this;

      
      if (other.$$is_number) {
        return self < other;
      }
      else {
        return self.$__coerced__("<", other);
      }
    
    });

    Opal.defn(self, '$<=', function(other) {
      var self = this;

      
      if (other.$$is_number) {
        return self <= other;
      }
      else {
        return self.$__coerced__("<=", other);
      }
    
    });

    Opal.defn(self, '$>', function(other) {
      var self = this;

      
      if (other.$$is_number) {
        return self > other;
      }
      else {
        return self.$__coerced__(">", other);
      }
    
    });

    Opal.defn(self, '$>=', function(other) {
      var self = this;

      
      if (other.$$is_number) {
        return self >= other;
      }
      else {
        return self.$__coerced__(">=", other);
      }
    
    });

    Opal.defn(self, '$<=>', function(other) {
      var self = this;

      try {
      
      if (other.$$is_number) {
        if (isNaN(self) || isNaN(other)) {
          return nil;
        }

        return self > other ? 1 : (self < other ? -1 : 0);
      }
      else {
        return self.$__coerced__("<=>", other);
      }
    
      } catch ($err) {if (Opal.rescue($err, [$scope.get('ArgumentError')])) {
        try {
          return nil
        } finally {
          Opal.gvars["!"] = Opal.exceptions.pop() || Opal.nil;
        }
        }else { throw $err; }
      };
    });

    Opal.defn(self, '$<<', function(count) {
      var self = this;

      count = $scope.get('Opal')['$coerce_to!'](count, $scope.get('Integer'), "to_int");
      return count > 0 ? self << count : self >> -count;
    });

    Opal.defn(self, '$>>', function(count) {
      var self = this;

      count = $scope.get('Opal')['$coerce_to!'](count, $scope.get('Integer'), "to_int");
      return count > 0 ? self >> count : self << -count;
    });

    Opal.defn(self, '$[]', function(bit) {
      var self = this;

      bit = $scope.get('Opal')['$coerce_to!'](bit, $scope.get('Integer'), "to_int");
      
      if (bit < (($scope.get('Integer')).$$scope.get('MIN')) || bit > (($scope.get('Integer')).$$scope.get('MAX'))) {
        return 0;
      }

      if (self < 0) {
        return (((~self) + 1) >> bit) % 2;
      }
      else {
        return (self >> bit) % 2;
      }
    ;
    });

    Opal.defn(self, '$+@', function() {
      var self = this;

      return +self;
    });

    Opal.defn(self, '$-@', function() {
      var self = this;

      return -self;
    });

    Opal.defn(self, '$~', function() {
      var self = this;

      return ~self;
    });

    Opal.defn(self, '$**', function(other) {
      var $a, $b, $c, self = this;

      if ((($a = $scope.get('Integer')['$==='](other)) !== nil && (!$a.$$is_boolean || $a == true))) {
        if ((($a = ((($b = ($scope.get('Integer')['$==='](self))['$!']()) !== false && $b !== nil) ? $b : $rb_gt(other, 0))) !== nil && (!$a.$$is_boolean || $a == true))) {
          return Math.pow(self, other);
          } else {
          return $scope.get('Rational').$new(self, 1)['$**'](other)
        }
      } else if ((($a = (($b = $rb_lt(self, 0)) ? (((($c = $scope.get('Float')['$==='](other)) !== false && $c !== nil) ? $c : $scope.get('Rational')['$==='](other))) : $rb_lt(self, 0))) !== nil && (!$a.$$is_boolean || $a == true))) {
        return $scope.get('Complex').$new(self, 0)['$**'](other.$to_f())
      } else if ((($a = other.$$is_number != null) !== nil && (!$a.$$is_boolean || $a == true))) {
        return Math.pow(self, other);
        } else {
        return self.$__coerced__("**", other)
      };
    });

    Opal.defn(self, '$==', function(other) {
      var self = this;

      
      if (other.$$is_number) {
        return self == Number(other);
      }
      else if (other['$respond_to?']("==")) {
        return other['$=='](self);
      }
      else {
        return false;
      }
    ;
    });

    Opal.defn(self, '$abs', function() {
      var self = this;

      return Math.abs(self);
    });

    Opal.defn(self, '$abs2', function() {
      var self = this;

      return Math.abs(self * self);
    });

    Opal.defn(self, '$angle', function() {
      var $a, self = this;

      if ((($a = self['$nan?']()) !== nil && (!$a.$$is_boolean || $a == true))) {
        return self};
      
      if (self == 0) {
        if (1 / self > 0) {
          return 0;
        }
        else {
          return Math.PI;
        }
      }
      else if (self < 0) {
        return Math.PI;
      }
      else {
        return 0;
      }
    
    });

    Opal.alias(self, 'arg', 'angle');

    Opal.alias(self, 'phase', 'angle');

    Opal.defn(self, '$bit_length', function() {
      var $a, self = this;

      if ((($a = $scope.get('Integer')['$==='](self)) !== nil && (!$a.$$is_boolean || $a == true))) {
        } else {
        self.$raise($scope.get('NoMethodError').$new("undefined method `bit_length` for " + (self) + ":Float", "bit_length"))
      };
      
      if (self === 0 || self === -1) {
        return 0;
      }

      var result = 0,
          value  = self < 0 ? ~self : self;

      while (value != 0) {
        result   += 1;
        value  >>>= 1;
      }

      return result;
    
    });

    Opal.defn(self, '$ceil', function() {
      var self = this;

      return Math.ceil(self);
    });

    Opal.defn(self, '$chr', function(encoding) {
      var self = this;

      return String.fromCharCode(self);
    });

    Opal.defn(self, '$denominator', TMP_1 = function() {
      var $a, $b, self = this, $iter = TMP_1.$$p, $yield = $iter || nil, $zuper = nil, $zuper_index = nil;

      TMP_1.$$p = null;
      $zuper = [];
      for($zuper_index = 0; $zuper_index < arguments.length; $zuper_index++) {
        $zuper[$zuper_index] = arguments[$zuper_index];
      }
      if ((($a = ((($b = self['$nan?']()) !== false && $b !== nil) ? $b : self['$infinite?']())) !== nil && (!$a.$$is_boolean || $a == true))) {
        return 1
        } else {
        return Opal.find_super_dispatcher(self, 'denominator', TMP_1, $iter).apply(self, $zuper)
      };
    });

    Opal.defn(self, '$downto', TMP_2 = function(stop) {
      var $a, $b, TMP_3, self = this, $iter = TMP_2.$$p, block = $iter || nil;

      TMP_2.$$p = null;
      if ((block !== nil)) {
        } else {
        return ($a = ($b = self).$enum_for, $a.$$p = (TMP_3 = function(){var self = TMP_3.$$s || this, $a;

        if ((($a = $scope.get('Numeric')['$==='](stop)) !== nil && (!$a.$$is_boolean || $a == true))) {
            } else {
            self.$raise($scope.get('ArgumentError'), "comparison of " + (self.$class()) + " with " + (stop.$class()) + " failed")
          };
          if ($rb_gt(stop, self)) {
            return 0
            } else {
            return $rb_plus($rb_minus(self, stop), 1)
          };}, TMP_3.$$s = self, TMP_3), $a).call($b, "downto", stop)
      };
      
      if (!stop.$$is_number) {
        self.$raise($scope.get('ArgumentError'), "comparison of " + (self.$class()) + " with " + (stop.$class()) + " failed")
      }
      for (var i = self; i >= stop; i--) {
        if (block(i) === $breaker) {
          return $breaker.$v;
        }
      }
    ;
      return self;
    });

    Opal.alias(self, 'eql?', '==');

    Opal.defn(self, '$equal?', function(other) {
      var $a, self = this;

      return ((($a = self['$=='](other)) !== false && $a !== nil) ? $a : isNaN(self) && isNaN(other));
    });

    Opal.defn(self, '$even?', function() {
      var self = this;

      return self % 2 === 0;
    });

    Opal.defn(self, '$floor', function() {
      var self = this;

      return Math.floor(self);
    });

    Opal.defn(self, '$gcd', function(other) {
      var $a, self = this;

      if ((($a = $scope.get('Integer')['$==='](other)) !== nil && (!$a.$$is_boolean || $a == true))) {
        } else {
        self.$raise($scope.get('TypeError'), "not an integer")
      };
      
      var min = Math.abs(self),
          max = Math.abs(other);

      while (min > 0) {
        var tmp = min;

        min = max % min;
        max = tmp;
      }

      return max;
    
    });

    Opal.defn(self, '$gcdlcm', function(other) {
      var self = this;

      return [self.$gcd(), self.$lcm()];
    });

    Opal.defn(self, '$integer?', function() {
      var self = this;

      return self % 1 === 0;
    });

    Opal.defn(self, '$is_a?', TMP_4 = function(klass) {
      var $a, $b, self = this, $iter = TMP_4.$$p, $yield = $iter || nil, $zuper = nil, $zuper_index = nil;

      TMP_4.$$p = null;
      $zuper = [];
      for($zuper_index = 0; $zuper_index < arguments.length; $zuper_index++) {
        $zuper[$zuper_index] = arguments[$zuper_index];
      }
      if ((($a = (($b = klass['$==']($scope.get('Fixnum'))) ? $scope.get('Integer')['$==='](self) : klass['$==']($scope.get('Fixnum')))) !== nil && (!$a.$$is_boolean || $a == true))) {
        return true};
      if ((($a = (($b = klass['$==']($scope.get('Integer'))) ? $scope.get('Integer')['$==='](self) : klass['$==']($scope.get('Integer')))) !== nil && (!$a.$$is_boolean || $a == true))) {
        return true};
      if ((($a = (($b = klass['$==']($scope.get('Float'))) ? $scope.get('Float')['$==='](self) : klass['$==']($scope.get('Float')))) !== nil && (!$a.$$is_boolean || $a == true))) {
        return true};
      return Opal.find_super_dispatcher(self, 'is_a?', TMP_4, $iter).apply(self, $zuper);
    });

    Opal.alias(self, 'kind_of?', 'is_a?');

    Opal.defn(self, '$instance_of?', TMP_5 = function(klass) {
      var $a, $b, self = this, $iter = TMP_5.$$p, $yield = $iter || nil, $zuper = nil, $zuper_index = nil;

      TMP_5.$$p = null;
      $zuper = [];
      for($zuper_index = 0; $zuper_index < arguments.length; $zuper_index++) {
        $zuper[$zuper_index] = arguments[$zuper_index];
      }
      if ((($a = (($b = klass['$==']($scope.get('Fixnum'))) ? $scope.get('Integer')['$==='](self) : klass['$==']($scope.get('Fixnum')))) !== nil && (!$a.$$is_boolean || $a == true))) {
        return true};
      if ((($a = (($b = klass['$==']($scope.get('Integer'))) ? $scope.get('Integer')['$==='](self) : klass['$==']($scope.get('Integer')))) !== nil && (!$a.$$is_boolean || $a == true))) {
        return true};
      if ((($a = (($b = klass['$==']($scope.get('Float'))) ? $scope.get('Float')['$==='](self) : klass['$==']($scope.get('Float')))) !== nil && (!$a.$$is_boolean || $a == true))) {
        return true};
      return Opal.find_super_dispatcher(self, 'instance_of?', TMP_5, $iter).apply(self, $zuper);
    });

    Opal.defn(self, '$lcm', function(other) {
      var $a, self = this;

      if ((($a = $scope.get('Integer')['$==='](other)) !== nil && (!$a.$$is_boolean || $a == true))) {
        } else {
        self.$raise($scope.get('TypeError'), "not an integer")
      };
      
      if (self == 0 || other == 0) {
        return 0;
      }
      else {
        return Math.abs(self * other / self.$gcd(other));
      }
    
    });

    Opal.alias(self, 'magnitude', 'abs');

    Opal.alias(self, 'modulo', '%');

    Opal.defn(self, '$next', function() {
      var self = this;

      return self + 1;
    });

    Opal.defn(self, '$nonzero?', function() {
      var self = this;

      return self == 0 ? nil : self;
    });

    Opal.defn(self, '$numerator', TMP_6 = function() {
      var $a, $b, self = this, $iter = TMP_6.$$p, $yield = $iter || nil, $zuper = nil, $zuper_index = nil;

      TMP_6.$$p = null;
      $zuper = [];
      for($zuper_index = 0; $zuper_index < arguments.length; $zuper_index++) {
        $zuper[$zuper_index] = arguments[$zuper_index];
      }
      if ((($a = ((($b = self['$nan?']()) !== false && $b !== nil) ? $b : self['$infinite?']())) !== nil && (!$a.$$is_boolean || $a == true))) {
        return self
        } else {
        return Opal.find_super_dispatcher(self, 'numerator', TMP_6, $iter).apply(self, $zuper)
      };
    });

    Opal.defn(self, '$odd?', function() {
      var self = this;

      return self % 2 !== 0;
    });

    Opal.defn(self, '$ord', function() {
      var self = this;

      return self;
    });

    Opal.defn(self, '$pred', function() {
      var self = this;

      return self - 1;
    });

    Opal.defn(self, '$quo', TMP_7 = function(other) {
      var $a, self = this, $iter = TMP_7.$$p, $yield = $iter || nil, $zuper = nil, $zuper_index = nil;

      TMP_7.$$p = null;
      $zuper = [];
      for($zuper_index = 0; $zuper_index < arguments.length; $zuper_index++) {
        $zuper[$zuper_index] = arguments[$zuper_index];
      }
      if ((($a = $scope.get('Integer')['$==='](self)) !== nil && (!$a.$$is_boolean || $a == true))) {
        return Opal.find_super_dispatcher(self, 'quo', TMP_7, $iter).apply(self, $zuper)
        } else {
        return $rb_divide(self, other)
      };
    });

    Opal.defn(self, '$rationalize', function(eps) {
      var $a, $b, self = this, f = nil, n = nil;

      
      if (arguments.length > 1) {
        self.$raise($scope.get('ArgumentError'), "wrong number of arguments (" + (arguments.length) + " for 0..1)");
      }
    ;
      if ((($a = $scope.get('Integer')['$==='](self)) !== nil && (!$a.$$is_boolean || $a == true))) {
        return $scope.get('Rational').$new(self, 1)
      } else if ((($a = self['$infinite?']()) !== nil && (!$a.$$is_boolean || $a == true))) {
        return self.$raise($scope.get('FloatDomainError'), "Infinity")
      } else if ((($a = self['$nan?']()) !== nil && (!$a.$$is_boolean || $a == true))) {
        return self.$raise($scope.get('FloatDomainError'), "NaN")
      } else if ((($a = eps == null) !== nil && (!$a.$$is_boolean || $a == true))) {
        $b = $scope.get('Math').$frexp(self), $a = Opal.to_ary($b), f = ($a[0] == null ? nil : $a[0]), n = ($a[1] == null ? nil : $a[1]), $b;
        f = $scope.get('Math').$ldexp(f, (($scope.get('Float')).$$scope.get('MANT_DIG'))).$to_i();
        n = $rb_minus(n, (($scope.get('Float')).$$scope.get('MANT_DIG')));
        return $scope.get('Rational').$new($rb_times(2, f), (1)['$<<'](($rb_minus(1, n)))).$rationalize($scope.get('Rational').$new(1, (1)['$<<'](($rb_minus(1, n)))));
        } else {
        return self.$to_r().$rationalize(eps)
      };
    });

    Opal.defn(self, '$round', function(ndigits) {
      var $a, $b, self = this, _ = nil, exp = nil;

      if ((($a = $scope.get('Integer')['$==='](self)) !== nil && (!$a.$$is_boolean || $a == true))) {
        if ((($a = ndigits == null) !== nil && (!$a.$$is_boolean || $a == true))) {
          return self};
        if ((($a = ($b = $scope.get('Float')['$==='](ndigits), $b !== false && $b !== nil ?ndigits['$infinite?']() : $b)) !== nil && (!$a.$$is_boolean || $a == true))) {
          self.$raise($scope.get('RangeError'), "Infinity")};
        ndigits = $scope.get('Opal')['$coerce_to!'](ndigits, $scope.get('Integer'), "to_int");
        if ($rb_lt(ndigits, (($scope.get('Integer')).$$scope.get('MIN')))) {
          self.$raise($scope.get('RangeError'), "out of bounds")};
        if ((($a = ndigits >= 0) !== nil && (!$a.$$is_boolean || $a == true))) {
          return self};
        ndigits = ndigits['$-@']();
        
        if (0.415241 * ndigits - 0.125 > self.$size()) {
          return 0;
        }

        var f = Math.pow(10, ndigits),
            x = Math.floor((Math.abs(x) + f / 2) / f) * f;

        return self < 0 ? -x : x;
      ;
        } else {
        if ((($a = ($b = self['$nan?'](), $b !== false && $b !== nil ?ndigits == null : $b)) !== nil && (!$a.$$is_boolean || $a == true))) {
          self.$raise($scope.get('FloatDomainError'), "NaN")};
        ndigits = $scope.get('Opal')['$coerce_to!'](ndigits || 0, $scope.get('Integer'), "to_int");
        if ($rb_le(ndigits, 0)) {
          if ((($a = self['$nan?']()) !== nil && (!$a.$$is_boolean || $a == true))) {
            self.$raise($scope.get('RangeError'), "NaN")
          } else if ((($a = self['$infinite?']()) !== nil && (!$a.$$is_boolean || $a == true))) {
            self.$raise($scope.get('FloatDomainError'), "Infinity")}
        } else if (ndigits['$=='](0)) {
          return Math.round(self)
        } else if ((($a = ((($b = self['$nan?']()) !== false && $b !== nil) ? $b : self['$infinite?']())) !== nil && (!$a.$$is_boolean || $a == true))) {
          return self};
        $b = $scope.get('Math').$frexp(self), $a = Opal.to_ary($b), _ = ($a[0] == null ? nil : $a[0]), exp = ($a[1] == null ? nil : $a[1]), $b;
        if ($rb_ge(ndigits, $rb_minus(($rb_plus((($scope.get('Float')).$$scope.get('DIG')), 2)), ((function() {if ($rb_gt(exp, 0)) {
          return $rb_divide(exp, 4)
          } else {
          return $rb_minus($rb_divide(exp, 3), 1)
        }; return nil; })())))) {
          return self};
        if ($rb_lt(ndigits, ((function() {if ($rb_gt(exp, 0)) {
          return $rb_plus($rb_divide(exp, 3), 1)
          } else {
          return $rb_divide(exp, 4)
        }; return nil; })())['$-@']())) {
          return 0};
        return Math.round(self * Math.pow(10, ndigits)) / Math.pow(10, ndigits);
      };
    });

    Opal.defn(self, '$step', TMP_8 = function(limit, step) {
      var $a, self = this, $iter = TMP_8.$$p, block = $iter || nil;

      if (step == null) {
        step = 1
      }
      TMP_8.$$p = null;
      if (block !== false && block !== nil) {
        } else {
        return self.$enum_for("step", limit, step)
      };
      if ((($a = step == 0) !== nil && (!$a.$$is_boolean || $a == true))) {
        self.$raise($scope.get('ArgumentError'), "step cannot be 0")};
      
      var value = self;

      if (limit === Infinity || limit === -Infinity) {
        block(value);
        return self;
      }

      if (step > 0) {
        while (value <= limit) {
          block(value);
          value += step;
        }
      }
      else {
        while (value >= limit) {
          block(value);
          value += step;
        }
      }
    
      return self;
    });

    Opal.alias(self, 'succ', 'next');

    Opal.defn(self, '$times', TMP_9 = function() {
      var self = this, $iter = TMP_9.$$p, block = $iter || nil;

      TMP_9.$$p = null;
      if (block !== false && block !== nil) {
        } else {
        return self.$enum_for("times")
      };
      
      for (var i = 0; i < self; i++) {
        if (block(i) === $breaker) {
          return $breaker.$v;
        }
      }
    
      return self;
    });

    Opal.defn(self, '$to_f', function() {
      var self = this;

      return self;
    });

    Opal.defn(self, '$to_i', function() {
      var self = this;

      return parseInt(self, 10);
    });

    Opal.alias(self, 'to_int', 'to_i');

    Opal.defn(self, '$to_r', function() {
      var $a, $b, self = this, f = nil, e = nil;

      if ((($a = $scope.get('Integer')['$==='](self)) !== nil && (!$a.$$is_boolean || $a == true))) {
        return $scope.get('Rational').$new(self, 1)
        } else {
        $b = $scope.get('Math').$frexp(self), $a = Opal.to_ary($b), f = ($a[0] == null ? nil : $a[0]), e = ($a[1] == null ? nil : $a[1]), $b;
        f = $scope.get('Math').$ldexp(f, (($scope.get('Float')).$$scope.get('MANT_DIG'))).$to_i();
        e = $rb_minus(e, (($scope.get('Float')).$$scope.get('MANT_DIG')));
        return ($rb_times(f, ((($scope.get('Float')).$$scope.get('RADIX'))['$**'](e)))).$to_r();
      };
    });

    Opal.defn(self, '$to_s', function(base) {
      var $a, $b, self = this;

      if (base == null) {
        base = 10
      }
      if ((($a = ((($b = $rb_lt(base, 2)) !== false && $b !== nil) ? $b : $rb_gt(base, 36))) !== nil && (!$a.$$is_boolean || $a == true))) {
        self.$raise($scope.get('ArgumentError'), "base must be between 2 and 36")};
      return self.toString(base);
    });

    Opal.alias(self, 'truncate', 'to_i');

    Opal.alias(self, 'inspect', 'to_s');

    Opal.defn(self, '$divmod', TMP_10 = function(other) {
      var $a, $b, self = this, $iter = TMP_10.$$p, $yield = $iter || nil, $zuper = nil, $zuper_index = nil;

      TMP_10.$$p = null;
      $zuper = [];
      for($zuper_index = 0; $zuper_index < arguments.length; $zuper_index++) {
        $zuper[$zuper_index] = arguments[$zuper_index];
      }
      if ((($a = ((($b = self['$nan?']()) !== false && $b !== nil) ? $b : other['$nan?']())) !== nil && (!$a.$$is_boolean || $a == true))) {
        return self.$raise($scope.get('FloatDomainError'), "NaN")
      } else if ((($a = self['$infinite?']()) !== nil && (!$a.$$is_boolean || $a == true))) {
        return self.$raise($scope.get('FloatDomainError'), "Infinity")
        } else {
        return Opal.find_super_dispatcher(self, 'divmod', TMP_10, $iter).apply(self, $zuper)
      };
    });

    Opal.defn(self, '$upto', TMP_11 = function(stop) {
      var $a, $b, TMP_12, self = this, $iter = TMP_11.$$p, block = $iter || nil;

      TMP_11.$$p = null;
      if ((block !== nil)) {
        } else {
        return ($a = ($b = self).$enum_for, $a.$$p = (TMP_12 = function(){var self = TMP_12.$$s || this, $a;

        if ((($a = $scope.get('Numeric')['$==='](stop)) !== nil && (!$a.$$is_boolean || $a == true))) {
            } else {
            self.$raise($scope.get('ArgumentError'), "comparison of " + (self.$class()) + " with " + (stop.$class()) + " failed")
          };
          if ($rb_lt(stop, self)) {
            return 0
            } else {
            return $rb_plus($rb_minus(stop, self), 1)
          };}, TMP_12.$$s = self, TMP_12), $a).call($b, "upto", stop)
      };
      
      if (!stop.$$is_number) {
        self.$raise($scope.get('ArgumentError'), "comparison of " + (self.$class()) + " with " + (stop.$class()) + " failed")
      }
      for (var i = self; i <= stop; i++) {
        if (block(i) === $breaker) {
          return $breaker.$v;
        }
      }
    ;
      return self;
    });

    Opal.defn(self, '$zero?', function() {
      var self = this;

      return self == 0;
    });

    Opal.defn(self, '$size', function() {
      var self = this;

      return 4;
    });

    Opal.defn(self, '$nan?', function() {
      var self = this;

      return isNaN(self);
    });

    Opal.defn(self, '$finite?', function() {
      var self = this;

      return self != Infinity && self != -Infinity && !isNaN(self);
    });

    Opal.defn(self, '$infinite?', function() {
      var self = this;

      
      if (self == Infinity) {
        return +1;
      }
      else if (self == -Infinity) {
        return -1;
      }
      else {
        return nil;
      }
    
    });

    Opal.defn(self, '$positive?', function() {
      var self = this;

      return self == Infinity || 1 / self > 0;
    });

    return (Opal.defn(self, '$negative?', function() {
      var self = this;

      return self == -Infinity || 1 / self < 0;
    }), nil) && 'negative?';
  })($scope.base, $scope.get('Numeric'));
  Opal.cdecl($scope, 'Fixnum', $scope.get('Number'));
  (function($base, $super) {
    function $Integer(){};
    var self = $Integer = $klass($base, $super, 'Integer', $Integer);

    var def = self.$$proto, $scope = self.$$scope;

    Opal.defs(self, '$===', function(other) {
      var self = this;

      
      if (!other.$$is_number) {
        return false;
      }

      return (other % 1) === 0;
    
    });

    Opal.cdecl($scope, 'MAX', Math.pow(2, 30) - 1);

    return Opal.cdecl($scope, 'MIN', -Math.pow(2, 30));
  })($scope.base, $scope.get('Numeric'));
  return (function($base, $super) {
    function $Float(){};
    var self = $Float = $klass($base, $super, 'Float', $Float);

    var def = self.$$proto, $scope = self.$$scope, $a;

    Opal.defs(self, '$===', function(other) {
      var self = this;

      return !!other.$$is_number;
    });

    Opal.cdecl($scope, 'INFINITY', Infinity);

    Opal.cdecl($scope, 'MAX', Number.MAX_VALUE);

    Opal.cdecl($scope, 'MIN', Number.MIN_VALUE);

    Opal.cdecl($scope, 'NAN', NaN);

    Opal.cdecl($scope, 'DIG', 15);

    Opal.cdecl($scope, 'MANT_DIG', 53);

    Opal.cdecl($scope, 'RADIX', 2);

    if ((($a = (typeof(Number.EPSILON) !== "undefined")) !== nil && (!$a.$$is_boolean || $a == true))) {
      return Opal.cdecl($scope, 'EPSILON', Number.EPSILON)
      } else {
      return Opal.cdecl($scope, 'EPSILON', 2.2204460492503130808472633361816E-16)
    };
  })($scope.base, $scope.get('Numeric'));
};

/* Generated by Opal 0.9.0 */
Opal.modules["corelib/range"] = function(Opal) {
  Opal.dynamic_require_severity = "error";
  var OPAL_CONFIG = { method_missing: true, arity_check: false, freezing: true, tainting: true };
  function $rb_le(lhs, rhs) {
    return (typeof(lhs) === 'number' && typeof(rhs) === 'number') ? lhs <= rhs : lhs['$<='](rhs);
  }
  function $rb_lt(lhs, rhs) {
    return (typeof(lhs) === 'number' && typeof(rhs) === 'number') ? lhs < rhs : lhs['$<'](rhs);
  }
  function $rb_minus(lhs, rhs) {
    return (typeof(lhs) === 'number' && typeof(rhs) === 'number') ? lhs - rhs : lhs['$-'](rhs);
  }
  var self = Opal.top, $scope = Opal, nil = Opal.nil, $breaker = Opal.breaker, $slice = Opal.slice, $klass = Opal.klass;

  Opal.add_stubs(['$require', '$include', '$attr_reader', '$<=>', '$raise', '$include?', '$<=', '$<', '$enum_for', '$upto', '$to_proc', '$succ', '$!', '$==', '$===', '$exclude_end?', '$eql?', '$begin', '$end', '$-', '$abs', '$to_i', '$inspect']);
  self.$require("corelib/enumerable");
  return (function($base, $super) {
    function $Range(){};
    var self = $Range = $klass($base, $super, 'Range', $Range);

    var def = self.$$proto, $scope = self.$$scope, TMP_1, TMP_2, TMP_3;

    def.begin = def.exclude = def.end = nil;
    self.$include($scope.get('Enumerable'));

    def.$$is_range = true;

    self.$attr_reader("begin", "end");

    Opal.defn(self, '$initialize', function(first, last, exclude) {
      var $a, self = this;

      if (exclude == null) {
        exclude = false
      }
      if ((($a = first['$<=>'](last)) !== nil && (!$a.$$is_boolean || $a == true))) {
        } else {
        self.$raise($scope.get('ArgumentError'))
      };
      self.begin = first;
      self.end = last;
      return self.exclude = exclude;
    });

    Opal.defn(self, '$==', function(other) {
      var self = this;

      
      if (!other.$$is_range) {
        return false;
      }

      return self.exclude === other.exclude &&
             self.begin   ==  other.begin &&
             self.end     ==  other.end;
    
    });

    Opal.defn(self, '$===', function(value) {
      var self = this;

      return self['$include?'](value);
    });

    Opal.defn(self, '$cover?', function(value) {
      var $a, $b, self = this;

      return (($a = $rb_le(self.begin, value)) ? ((function() {if ((($b = self.exclude) !== nil && (!$b.$$is_boolean || $b == true))) {
        return $rb_lt(value, self.end)
        } else {
        return $rb_le(value, self.end)
      }; return nil; })()) : $rb_le(self.begin, value));
    });

    Opal.defn(self, '$each', TMP_1 = function() {
      var $a, $b, $c, self = this, $iter = TMP_1.$$p, block = $iter || nil, current = nil, last = nil;

      TMP_1.$$p = null;
      if ((block !== nil)) {
        } else {
        return self.$enum_for("each")
      };
      
      var i, limit, value;

      if (self.begin.$$is_number && self.end.$$is_number) {
        if (self.begin % 1 !== 0 || self.end % 1 !== 0) {
          self.$raise($scope.get('TypeError'), "can't iterate from Float")
        }

        for (i = self.begin, limit = self.end + (function() {if ((($a = self.exclude) !== nil && (!$a.$$is_boolean || $a == true))) {
        return 0
        } else {
        return 1
      }; return nil; })(); i < limit; i++) {
          value = block(i);
          if (value === $breaker) { return $breaker.$v; }
        }

        return self;
      }

      if (self.begin.$$is_string && self.end.$$is_string) {
        value = ($a = ($b = self.begin).$upto, $a.$$p = block.$to_proc(), $a).call($b, self.end, self.exclude);

        // The following is a bit hackish: we know that
        // String#upto normally returns self, but may
        // return a different value if there's a `break`
        // statement in the supplied block. We need to
        // propagate this `break` value here, so we
        // test for equality with `@begin` string to
        // determine the return value:
        return value === self.begin ? self : value;
      }
    ;
      current = self.begin;
      last = self.end;
      while ($rb_lt(current, last)) {
      if (Opal.yield1(block, current) === $breaker) return $breaker.$v;
      current = current.$succ();};
      if ((($a = ($c = self.exclude['$!'](), $c !== false && $c !== nil ?current['$=='](last) : $c)) !== nil && (!$a.$$is_boolean || $a == true))) {
        if (Opal.yield1(block, current) === $breaker) return $breaker.$v};
      return self;
    });

    Opal.defn(self, '$eql?', function(other) {
      var $a, $b, self = this;

      if ((($a = $scope.get('Range')['$==='](other)) !== nil && (!$a.$$is_boolean || $a == true))) {
        } else {
        return false
      };
      return ($a = ($b = self.exclude['$==='](other['$exclude_end?']()), $b !== false && $b !== nil ?self.begin['$eql?'](other.$begin()) : $b), $a !== false && $a !== nil ?self.end['$eql?'](other.$end()) : $a);
    });

    Opal.defn(self, '$exclude_end?', function() {
      var self = this;

      return self.exclude;
    });

    Opal.alias(self, 'first', 'begin');

    Opal.alias(self, 'include?', 'cover?');

    Opal.alias(self, 'last', 'end');

    Opal.defn(self, '$max', TMP_2 = function() {
      var self = this, $iter = TMP_2.$$p, $yield = $iter || nil, $zuper = nil, $zuper_index = nil;

      TMP_2.$$p = null;
      $zuper = [];
      for($zuper_index = 0; $zuper_index < arguments.length; $zuper_index++) {
        $zuper[$zuper_index] = arguments[$zuper_index];
      }
      if (($yield !== nil)) {
        return Opal.find_super_dispatcher(self, 'max', TMP_2, $iter).apply(self, $zuper)
        } else {
        return self.exclude ? self.end - 1 : self.end;
      };
    });

    Opal.alias(self, 'member?', 'cover?');

    Opal.defn(self, '$min', TMP_3 = function() {
      var self = this, $iter = TMP_3.$$p, $yield = $iter || nil, $zuper = nil, $zuper_index = nil;

      TMP_3.$$p = null;
      $zuper = [];
      for($zuper_index = 0; $zuper_index < arguments.length; $zuper_index++) {
        $zuper[$zuper_index] = arguments[$zuper_index];
      }
      if (($yield !== nil)) {
        return Opal.find_super_dispatcher(self, 'min', TMP_3, $iter).apply(self, $zuper)
        } else {
        return self.begin
      };
    });

    Opal.alias(self, 'member?', 'include?');

    Opal.defn(self, '$size', function() {
      var $a, $b, self = this, _begin = nil, _end = nil, infinity = nil;

      _begin = self.begin;
      _end = self.end;
      if ((($a = self.exclude) !== nil && (!$a.$$is_boolean || $a == true))) {
        _end = $rb_minus(_end, 1)};
      if ((($a = ($b = $scope.get('Numeric')['$==='](_begin), $b !== false && $b !== nil ?$scope.get('Numeric')['$==='](_end) : $b)) !== nil && (!$a.$$is_boolean || $a == true))) {
        } else {
        return nil
      };
      if ($rb_lt(_end, _begin)) {
        return 0};
      infinity = (($scope.get('Float')).$$scope.get('INFINITY'));
      if ((($a = ((($b = infinity['$=='](_begin.$abs())) !== false && $b !== nil) ? $b : _end.$abs()['$=='](infinity))) !== nil && (!$a.$$is_boolean || $a == true))) {
        return infinity};
      return ((Math.abs(_end - _begin) + 1)).$to_i();
    });

    Opal.defn(self, '$step', function(n) {
      var self = this;

      if (n == null) {
        n = 1
      }
      return self.$raise($scope.get('NotImplementedError'));
    });

    Opal.defn(self, '$to_s', function() {
      var self = this;

      return self.begin.$inspect() + (self.exclude ? '...' : '..') + self.end.$inspect();
    });

    return Opal.alias(self, 'inspect', 'to_s');
  })($scope.base, null);
};

/* Generated by Opal 0.9.0 */
Opal.modules["corelib/proc"] = function(Opal) {
  Opal.dynamic_require_severity = "error";
  var OPAL_CONFIG = { method_missing: true, arity_check: false, freezing: true, tainting: true };
  var self = Opal.top, $scope = Opal, nil = Opal.nil, $breaker = Opal.breaker, $slice = Opal.slice, $klass = Opal.klass;

  Opal.add_stubs(['$raise', '$coerce_to!']);
  return (function($base, $super) {
    function $Proc(){};
    var self = $Proc = $klass($base, $super, 'Proc', $Proc);

    var def = self.$$proto, $scope = self.$$scope, TMP_1, TMP_2;

    def.$$is_proc = true;

    def.$$is_lambda = false;

    Opal.defs(self, '$new', TMP_1 = function() {
      var self = this, $iter = TMP_1.$$p, block = $iter || nil;

      TMP_1.$$p = null;
      if (block !== false && block !== nil) {
        } else {
        self.$raise($scope.get('ArgumentError'), "tried to create a Proc object without a block")
      };
      return block;
    });

    Opal.defn(self, '$call', TMP_2 = function() {
      var self = this, $iter = TMP_2.$$p, block = $iter || nil, $splat_index = nil;

      var array_size = arguments.length - 0;
      if(array_size < 0) array_size = 0;
      var args = new Array(array_size);
      for($splat_index = 0; $splat_index < array_size; $splat_index++) {
        args[$splat_index] = arguments[$splat_index + 0];
      }
      TMP_2.$$p = null;
      
      if (block !== nil) {
        self.$$p = block;
      }

      var result;

      if (self.$$is_lambda) {
        result = self.apply(null, args);
      }
      else {
        result = Opal.yieldX(self, args);
      }

      if (result === $breaker) {
        return $breaker.$v;
      }

      return result;
    
    });

    Opal.alias(self, '[]', 'call');

    Opal.alias(self, '===', 'call');

    Opal.alias(self, 'yield', 'call');

    Opal.defn(self, '$to_proc', function() {
      var self = this;

      return self;
    });

    Opal.defn(self, '$lambda?', function() {
      var self = this;

      return !!self.$$is_lambda;
    });

    Opal.defn(self, '$arity', function() {
      var self = this;

      if (self.$$is_curried) { return -1; }
      if (self.$$arity) { return self.$$arity };
      return self.length;
    });

    Opal.defn(self, '$source_location', function() {
      var self = this;

      if (self.$$is_curried) { return nil; }
      return nil;
    });

    Opal.defn(self, '$binding', function() {
      var self = this;

      if (self.$$is_curried) { self.$raise($scope.get('ArgumentError'), "Can't create Binding") };
      return nil;
    });

    Opal.defn(self, '$parameters', function() {
      var self = this;

      if (self.$$is_curried) { return [["rest"]]; };
      return nil;
    });

    Opal.defn(self, '$curry', function(arity) {
      var self = this;

      
      if (arity === undefined) {
        arity = self.length;
      }
      else {
        arity = $scope.get('Opal')['$coerce_to!'](arity, $scope.get('Integer'), "to_int");
        if (self.$$is_lambda && arity !== self.length) {
          self.$raise($scope.get('ArgumentError'), "wrong number of arguments (" + (arity) + " for " + (self.length) + ")")
        }
      }

      function curried () {
        var args = $slice.call(arguments),
            length = args.length,
            result;

        if (length > arity && self.$$is_lambda && !self.$$is_curried) {
          self.$raise($scope.get('ArgumentError'), "wrong number of arguments (" + (length) + " for " + (arity) + ")")
        }

        if (length >= arity) {
          return self.$call.apply(self, args);
        }

        result = function () {
          return curried.apply(null,
            args.concat($slice.call(arguments)));
        }
        result.$$is_lambda = self.$$is_lambda;
        result.$$is_curried = true;

        return result;
      };

      curried.$$is_lambda = self.$$is_lambda;
      curried.$$is_curried = true;
      return curried;
    
    });

    Opal.defn(self, '$dup', function() {
      var self = this;

      
      var original_proc = self.$$original_proc || self,
          proc = function () {
            return original_proc.apply(this, arguments);
          };

      for (var prop in self) {
        if (self.hasOwnProperty(prop)) {
          proc[prop] = self[prop];
        }
      }

      return proc;
    
    });

    return Opal.alias(self, 'clone', 'dup');
  })($scope.base, Function)
};

/* Generated by Opal 0.9.0 */
Opal.modules["corelib/method"] = function(Opal) {
  Opal.dynamic_require_severity = "error";
  var OPAL_CONFIG = { method_missing: true, arity_check: false, freezing: true, tainting: true };
  var self = Opal.top, $scope = Opal, nil = Opal.nil, $breaker = Opal.breaker, $slice = Opal.slice, $klass = Opal.klass;

  Opal.add_stubs(['$attr_reader', '$class', '$arity', '$new', '$name']);
  (function($base, $super) {
    function $Method(){};
    var self = $Method = $klass($base, $super, 'Method', $Method);

    var def = self.$$proto, $scope = self.$$scope, TMP_1;

    def.method = def.receiver = def.owner = def.name = nil;
    self.$attr_reader("owner", "receiver", "name");

    Opal.defn(self, '$initialize', function(receiver, method, name) {
      var self = this;

      self.receiver = receiver;
      self.owner = receiver.$class();
      self.name = name;
      return self.method = method;
    });

    Opal.defn(self, '$arity', function() {
      var self = this;

      return self.method.$arity();
    });

    Opal.defn(self, '$call', TMP_1 = function() {
      var self = this, $iter = TMP_1.$$p, block = $iter || nil, $splat_index = nil;

      var array_size = arguments.length - 0;
      if(array_size < 0) array_size = 0;
      var args = new Array(array_size);
      for($splat_index = 0; $splat_index < array_size; $splat_index++) {
        args[$splat_index] = arguments[$splat_index + 0];
      }
      TMP_1.$$p = null;
      
      self.method.$$p = block;

      return self.method.apply(self.receiver, args);
    ;
    });

    Opal.alias(self, '[]', 'call');

    Opal.defn(self, '$unbind', function() {
      var self = this;

      return $scope.get('UnboundMethod').$new(self.owner, self.method, self.name);
    });

    Opal.defn(self, '$to_proc', function() {
      var self = this;

      
      var proc = function () { return self.$call.apply(self, $slice.call(arguments)); };
      proc.$$unbound = self.method;
      proc.$$is_lambda = true;
      return proc;
    
    });

    return (Opal.defn(self, '$inspect', function() {
      var self = this;

      return "#<Method: " + (self.receiver.$class()) + "#" + (self.name) + ">";
    }), nil) && 'inspect';
  })($scope.base, null);
  return (function($base, $super) {
    function $UnboundMethod(){};
    var self = $UnboundMethod = $klass($base, $super, 'UnboundMethod', $UnboundMethod);

    var def = self.$$proto, $scope = self.$$scope;

    def.method = def.name = def.owner = nil;
    self.$attr_reader("owner", "name");

    Opal.defn(self, '$initialize', function(owner, method, name) {
      var self = this;

      self.owner = owner;
      self.method = method;
      return self.name = name;
    });

    Opal.defn(self, '$arity', function() {
      var self = this;

      return self.method.$arity();
    });

    Opal.defn(self, '$bind', function(object) {
      var self = this;

      return $scope.get('Method').$new(object, self.method, self.name);
    });

    return (Opal.defn(self, '$inspect', function() {
      var self = this;

      return "#<UnboundMethod: " + (self.owner.$name()) + "#" + (self.name) + ">";
    }), nil) && 'inspect';
  })($scope.base, null);
};

/* Generated by Opal 0.9.0 */
Opal.modules["corelib/variables"] = function(Opal) {
  Opal.dynamic_require_severity = "error";
  var OPAL_CONFIG = { method_missing: true, arity_check: false, freezing: true, tainting: true };
  var self = Opal.top, $scope = Opal, nil = Opal.nil, $breaker = Opal.breaker, $slice = Opal.slice, $gvars = Opal.gvars, $hash2 = Opal.hash2;

  Opal.add_stubs(['$new']);
  $gvars["&"] = $gvars["~"] = $gvars["`"] = $gvars["'"] = nil;
  $gvars.LOADED_FEATURES = $gvars["\""] = Opal.loaded_features;
  $gvars.LOAD_PATH = $gvars[":"] = [];
  $gvars["/"] = "\n";
  $gvars[","] = nil;
  Opal.cdecl($scope, 'ARGV', []);
  Opal.cdecl($scope, 'ARGF', $scope.get('Object').$new());
  Opal.cdecl($scope, 'ENV', $hash2([], {}));
  $gvars.VERBOSE = false;
  $gvars.DEBUG = false;
  return $gvars.SAFE = 0;
};

/* Generated by Opal 0.9.0 */
Opal.modules["opal/mini"] = function(Opal) {
  Opal.dynamic_require_severity = "error";
  var OPAL_CONFIG = { method_missing: true, arity_check: false, freezing: true, tainting: true };
  var self = Opal.top, $scope = Opal, nil = Opal.nil, $breaker = Opal.breaker, $slice = Opal.slice;

  Opal.add_stubs(['$require']);
  self.$require("opal/base");
  self.$require("corelib/nil");
  self.$require("corelib/boolean");
  self.$require("corelib/string");
  self.$require("corelib/comparable");
  self.$require("corelib/enumerable");
  self.$require("corelib/enumerator");
  self.$require("corelib/array");
  self.$require("corelib/hash");
  self.$require("corelib/number");
  self.$require("corelib/range");
  self.$require("corelib/proc");
  self.$require("corelib/method");
  self.$require("corelib/regexp");
  return self.$require("corelib/variables");
};

/* Generated by Opal 0.9.0 */
Opal.modules["corelib/array/inheritance"] = function(Opal) {
  Opal.dynamic_require_severity = "error";
  var OPAL_CONFIG = { method_missing: true, arity_check: false, freezing: true, tainting: true };
  function $rb_times(lhs, rhs) {
    return (typeof(lhs) === 'number' && typeof(rhs) === 'number') ? lhs * rhs : lhs['$*'](rhs);
  }
  function $rb_minus(lhs, rhs) {
    return (typeof(lhs) === 'number' && typeof(rhs) === 'number') ? lhs - rhs : lhs['$-'](rhs);
  }
  function $rb_plus(lhs, rhs) {
    return (typeof(lhs) === 'number' && typeof(rhs) === 'number') ? lhs + rhs : lhs['$+'](rhs);
  }
  var self = Opal.top, $scope = Opal, nil = Opal.nil, $breaker = Opal.breaker, $slice = Opal.slice, $klass = Opal.klass;

  Opal.add_stubs(['$new', '$allocate', '$initialize', '$to_proc', '$__send__', '$clone', '$respond_to?', '$==', '$eql?', '$inspect', '$hash', '$*', '$class', '$slice', '$uniq', '$flatten', '$-', '$+']);
  (function($base, $super) {
    function $Array(){};
    var self = $Array = $klass($base, $super, 'Array', $Array);

    var def = self.$$proto, $scope = self.$$scope;

    return (Opal.defs(self, '$inherited', function(klass) {
      var self = this, replace = nil;

      replace = $scope.get('Class').$new((($scope.get('Array')).$$scope.get('Wrapper')));
      
      klass.$$proto         = replace.$$proto;
      klass.$$proto.$$class = klass;
      klass.$$alloc         = replace.$$alloc;
      klass.$$parent        = (($scope.get('Array')).$$scope.get('Wrapper'));

      klass.$allocate = replace.$allocate;
      klass.$new      = replace.$new;
      klass["$[]"]    = replace["$[]"];
    
    }), nil) && 'inherited'
  })($scope.base, null);
  return (function($base, $super) {
    function $Wrapper(){};
    var self = $Wrapper = $klass($base, $super, 'Wrapper', $Wrapper);

    var def = self.$$proto, $scope = self.$$scope, TMP_1, TMP_2, TMP_3, TMP_4, TMP_5;

    def.literal = nil;
    def.$$is_array = true;

    Opal.defs(self, '$allocate', TMP_1 = function(array) {
      var self = this, $iter = TMP_1.$$p, $yield = $iter || nil, obj = nil;

      if (array == null) {
        array = []
      }
      TMP_1.$$p = null;
      obj = Opal.find_super_dispatcher(self, 'allocate', TMP_1, null, $Wrapper).apply(self, []);
      obj.literal = array;
      return obj;
    });

    Opal.defs(self, '$new', TMP_2 = function() {
      var $a, $b, self = this, $iter = TMP_2.$$p, block = $iter || nil, obj = nil, $splat_index = nil;

      var array_size = arguments.length - 0;
      if(array_size < 0) array_size = 0;
      var args = new Array(array_size);
      for($splat_index = 0; $splat_index < array_size; $splat_index++) {
        args[$splat_index] = arguments[$splat_index + 0];
      }
      TMP_2.$$p = null;
      obj = self.$allocate();
      ($a = ($b = obj).$initialize, $a.$$p = block.$to_proc(), $a).apply($b, Opal.to_a(args));
      return obj;
    });

    Opal.defs(self, '$[]', function() {
      var self = this, $splat_index = nil;

      var array_size = arguments.length - 0;
      if(array_size < 0) array_size = 0;
      var objects = new Array(array_size);
      for($splat_index = 0; $splat_index < array_size; $splat_index++) {
        objects[$splat_index] = arguments[$splat_index + 0];
      }
      return self.$allocate(objects);
    });

    Opal.defn(self, '$initialize', TMP_3 = function() {
      var $a, $b, self = this, $iter = TMP_3.$$p, block = $iter || nil, $splat_index = nil;

      var array_size = arguments.length - 0;
      if(array_size < 0) array_size = 0;
      var args = new Array(array_size);
      for($splat_index = 0; $splat_index < array_size; $splat_index++) {
        args[$splat_index] = arguments[$splat_index + 0];
      }
      TMP_3.$$p = null;
      return self.literal = ($a = ($b = $scope.get('Array')).$new, $a.$$p = block.$to_proc(), $a).apply($b, Opal.to_a(args));
    });

    Opal.defn(self, '$method_missing', TMP_4 = function() {
      var $a, $b, self = this, $iter = TMP_4.$$p, block = $iter || nil, result = nil, $splat_index = nil;

      var array_size = arguments.length - 0;
      if(array_size < 0) array_size = 0;
      var args = new Array(array_size);
      for($splat_index = 0; $splat_index < array_size; $splat_index++) {
        args[$splat_index] = arguments[$splat_index + 0];
      }
      TMP_4.$$p = null;
      result = ($a = ($b = self.literal).$__send__, $a.$$p = block.$to_proc(), $a).apply($b, Opal.to_a(args));
      if ((($a = result === self.literal) !== nil && (!$a.$$is_boolean || $a == true))) {
        return self
        } else {
        return result
      };
    });

    Opal.defn(self, '$initialize_copy', function(other) {
      var self = this;

      return self.literal = (other.literal).$clone();
    });

    Opal.defn(self, '$respond_to?', TMP_5 = function(name) {
      var $a, self = this, $iter = TMP_5.$$p, $yield = $iter || nil, $zuper = nil, $zuper_index = nil;

      TMP_5.$$p = null;
      $zuper = [];
      for($zuper_index = 0; $zuper_index < arguments.length; $zuper_index++) {
        $zuper[$zuper_index] = arguments[$zuper_index];
      }
      return ((($a = Opal.find_super_dispatcher(self, 'respond_to?', TMP_5, $iter).apply(self, $zuper)) !== false && $a !== nil) ? $a : self.literal['$respond_to?'](name));
    });

    Opal.defn(self, '$==', function(other) {
      var self = this;

      return self.literal['$=='](other);
    });

    Opal.defn(self, '$eql?', function(other) {
      var self = this;

      return self.literal['$eql?'](other);
    });

    Opal.defn(self, '$to_a', function() {
      var self = this;

      return self.literal;
    });

    Opal.defn(self, '$to_ary', function() {
      var self = this;

      return self;
    });

    Opal.defn(self, '$inspect', function() {
      var self = this;

      return self.literal.$inspect();
    });

    Opal.defn(self, '$hash', function() {
      var self = this;

      return self.literal.$hash();
    });

    Opal.defn(self, '$*', function(other) {
      var self = this;

      
      var result = $rb_times(self.literal, other);

      if (result.$$is_array) {
        return self.$class().$allocate(result)
      }
      else {
        return result;
      }
    ;
    });

    Opal.defn(self, '$[]', function(index, length) {
      var self = this;

      
      var result = self.literal.$slice(index, length);

      if (result.$$is_array && (index.$$is_range || length !== undefined)) {
        return self.$class().$allocate(result)
      }
      else {
        return result;
      }
    ;
    });

    Opal.alias(self, 'slice', '[]');

    Opal.defn(self, '$uniq', function() {
      var self = this;

      return self.$class().$allocate(self.literal.$uniq());
    });

    Opal.defn(self, '$flatten', function(level) {
      var self = this;

      return self.$class().$allocate(self.literal.$flatten(level));
    });

    Opal.defn(self, '$-', function(other) {
      var self = this;

      return $rb_minus(self.literal, other);
    });

    return (Opal.defn(self, '$+', function(other) {
      var self = this;

      return $rb_plus(self.literal, other);
    }), nil) && '+';
  })($scope.get('Array'), null);
};

/* Generated by Opal 0.9.0 */
Opal.modules["corelib/string/inheritance"] = function(Opal) {
  Opal.dynamic_require_severity = "error";
  var OPAL_CONFIG = { method_missing: true, arity_check: false, freezing: true, tainting: true };
  function $rb_plus(lhs, rhs) {
    return (typeof(lhs) === 'number' && typeof(rhs) === 'number') ? lhs + rhs : lhs['$+'](rhs);
  }
  function $rb_times(lhs, rhs) {
    return (typeof(lhs) === 'number' && typeof(rhs) === 'number') ? lhs * rhs : lhs['$*'](rhs);
  }
  var self = Opal.top, $scope = Opal, nil = Opal.nil, $breaker = Opal.breaker, $slice = Opal.slice, $klass = Opal.klass, $gvars = Opal.gvars;

  Opal.add_stubs(['$require', '$new', '$allocate', '$initialize', '$to_proc', '$__send__', '$class', '$clone', '$respond_to?', '$==', '$inspect', '$+', '$*', '$map', '$split', '$enum_for', '$each_line', '$to_a', '$%']);
  self.$require("corelib/string");
  (function($base, $super) {
    function $String(){};
    var self = $String = $klass($base, $super, 'String', $String);

    var def = self.$$proto, $scope = self.$$scope;

    return (Opal.defs(self, '$inherited', function(klass) {
      var self = this, replace = nil;

      replace = $scope.get('Class').$new((($scope.get('String')).$$scope.get('Wrapper')));
      
      klass.$$proto         = replace.$$proto;
      klass.$$proto.$$class = klass;
      klass.$$alloc         = replace.$$alloc;
      klass.$$parent        = (($scope.get('String')).$$scope.get('Wrapper'));

      klass.$allocate = replace.$allocate;
      klass.$new      = replace.$new;
    
    }), nil) && 'inherited'
  })($scope.base, null);
  return (function($base, $super) {
    function $Wrapper(){};
    var self = $Wrapper = $klass($base, $super, 'Wrapper', $Wrapper);

    var def = self.$$proto, $scope = self.$$scope, TMP_1, TMP_2, TMP_3, TMP_4, TMP_6, TMP_8;

    def.literal = nil;
    def.$$is_string = true;

    Opal.defs(self, '$allocate', TMP_1 = function(string) {
      var self = this, $iter = TMP_1.$$p, $yield = $iter || nil, obj = nil;

      if (string == null) {
        string = ""
      }
      TMP_1.$$p = null;
      obj = Opal.find_super_dispatcher(self, 'allocate', TMP_1, null, $Wrapper).apply(self, []);
      obj.literal = string;
      return obj;
    });

    Opal.defs(self, '$new', TMP_2 = function() {
      var $a, $b, self = this, $iter = TMP_2.$$p, block = $iter || nil, obj = nil, $splat_index = nil;

      var array_size = arguments.length - 0;
      if(array_size < 0) array_size = 0;
      var args = new Array(array_size);
      for($splat_index = 0; $splat_index < array_size; $splat_index++) {
        args[$splat_index] = arguments[$splat_index + 0];
      }
      TMP_2.$$p = null;
      obj = self.$allocate();
      ($a = ($b = obj).$initialize, $a.$$p = block.$to_proc(), $a).apply($b, Opal.to_a(args));
      return obj;
    });

    Opal.defs(self, '$[]', function() {
      var self = this, $splat_index = nil;

      var array_size = arguments.length - 0;
      if(array_size < 0) array_size = 0;
      var objects = new Array(array_size);
      for($splat_index = 0; $splat_index < array_size; $splat_index++) {
        objects[$splat_index] = arguments[$splat_index + 0];
      }
      return self.$allocate(objects);
    });

    Opal.defn(self, '$initialize', function(string) {
      var self = this;

      if (string == null) {
        string = ""
      }
      return self.literal = string;
    });

    Opal.defn(self, '$method_missing', TMP_3 = function() {
      var $a, $b, self = this, $iter = TMP_3.$$p, block = $iter || nil, result = nil, $splat_index = nil;

      var array_size = arguments.length - 0;
      if(array_size < 0) array_size = 0;
      var args = new Array(array_size);
      for($splat_index = 0; $splat_index < array_size; $splat_index++) {
        args[$splat_index] = arguments[$splat_index + 0];
      }
      TMP_3.$$p = null;
      result = ($a = ($b = self.literal).$__send__, $a.$$p = block.$to_proc(), $a).apply($b, Opal.to_a(args));
      if ((($a = result.$$is_string != null) !== nil && (!$a.$$is_boolean || $a == true))) {
        if ((($a = result == self.literal) !== nil && (!$a.$$is_boolean || $a == true))) {
          return self
          } else {
          return self.$class().$allocate(result)
        }
        } else {
        return result
      };
    });

    Opal.defn(self, '$initialize_copy', function(other) {
      var self = this;

      return self.literal = (other.literal).$clone();
    });

    Opal.defn(self, '$respond_to?', TMP_4 = function(name) {
      var $a, self = this, $iter = TMP_4.$$p, $yield = $iter || nil, $zuper = nil, $zuper_index = nil;

      TMP_4.$$p = null;
      $zuper = [];
      for($zuper_index = 0; $zuper_index < arguments.length; $zuper_index++) {
        $zuper[$zuper_index] = arguments[$zuper_index];
      }
      return ((($a = Opal.find_super_dispatcher(self, 'respond_to?', TMP_4, $iter).apply(self, $zuper)) !== false && $a !== nil) ? $a : self.literal['$respond_to?'](name));
    });

    Opal.defn(self, '$==', function(other) {
      var self = this;

      return self.literal['$=='](other);
    });

    Opal.alias(self, 'eql?', '==');

    Opal.alias(self, '===', '==');

    Opal.defn(self, '$to_s', function() {
      var self = this;

      return self.literal;
    });

    Opal.alias(self, 'to_str', 'to_s');

    Opal.defn(self, '$inspect', function() {
      var self = this;

      return self.literal.$inspect();
    });

    Opal.defn(self, '$+', function(other) {
      var self = this;

      return $rb_plus(self.literal, other);
    });

    Opal.defn(self, '$*', function(other) {
      var self = this;

      
      var result = $rb_times(self.literal, other);

      if (result.$$is_string) {
        return self.$class().$allocate(result)
      }
      else {
        return result;
      }
    ;
    });

    Opal.defn(self, '$split', function(pattern, limit) {
      var $a, $b, TMP_5, self = this;

      return ($a = ($b = self.literal.$split(pattern, limit)).$map, $a.$$p = (TMP_5 = function(str){var self = TMP_5.$$s || this;
if (str == null) str = nil;
      return self.$class().$allocate(str)}, TMP_5.$$s = self, TMP_5), $a).call($b);
    });

    Opal.defn(self, '$replace', function(string) {
      var self = this;

      return self.literal = string;
    });

    Opal.defn(self, '$each_line', TMP_6 = function(separator) {
      var $a, $b, TMP_7, self = this, $iter = TMP_6.$$p, $yield = $iter || nil;
      if ($gvars["/"] == null) $gvars["/"] = nil;

      if (separator == null) {
        separator = $gvars["/"]
      }
      TMP_6.$$p = null;
      if (($yield !== nil)) {
        } else {
        return self.$enum_for("each_line", separator)
      };
      return ($a = ($b = self.literal).$each_line, $a.$$p = (TMP_7 = function(str){var self = TMP_7.$$s || this, $a;
if (str == null) str = nil;
      return $a = Opal.yield1($yield, self.$class().$allocate(str)), $a === $breaker ? $a : $a}, TMP_7.$$s = self, TMP_7), $a).call($b, separator);
    });

    Opal.defn(self, '$lines', TMP_8 = function(separator) {
      var $a, $b, self = this, $iter = TMP_8.$$p, block = $iter || nil, e = nil;
      if ($gvars["/"] == null) $gvars["/"] = nil;

      if (separator == null) {
        separator = $gvars["/"]
      }
      TMP_8.$$p = null;
      e = ($a = ($b = self).$each_line, $a.$$p = block.$to_proc(), $a).call($b, separator);
      if (block !== false && block !== nil) {
        return self
        } else {
        return e.$to_a()
      };
    });

    return (Opal.defn(self, '$%', function(data) {
      var self = this;

      return self.literal['$%'](data);
    }), nil) && '%';
  })($scope.get('String'), null);
};

/* Generated by Opal 0.9.0 */
Opal.modules["corelib/string/encoding"] = function(Opal) {
  Opal.dynamic_require_severity = "error";
  var OPAL_CONFIG = { method_missing: true, arity_check: false, freezing: true, tainting: true };
  function $rb_plus(lhs, rhs) {
    return (typeof(lhs) === 'number' && typeof(rhs) === 'number') ? lhs + rhs : lhs['$+'](rhs);
  }
  var $a, $b, TMP_4, $c, TMP_6, $d, TMP_8, self = Opal.top, $scope = Opal, nil = Opal.nil, $breaker = Opal.breaker, $slice = Opal.slice, $klass = Opal.klass, $hash2 = Opal.hash2;

  Opal.add_stubs(['$require', '$+', '$[]', '$new', '$to_proc', '$each', '$const_set', '$sub', '$upcase', '$const_get', '$===', '$==', '$name', '$include?', '$names', '$constants', '$raise', '$attr_accessor', '$attr_reader', '$register', '$length', '$bytes', '$to_a', '$each_byte', '$bytesize', '$enum_for', '$force_encoding', '$dup', '$coerce_to!', '$find', '$nil?', '$getbyte']);
  self.$require("corelib/string");
  (function($base, $super) {
    function $Encoding(){};
    var self = $Encoding = $klass($base, $super, 'Encoding', $Encoding);

    var def = self.$$proto, $scope = self.$$scope, TMP_1;

    def.ascii = def.dummy = def.name = nil;
    Opal.defs(self, '$register', TMP_1 = function(name, options) {
      var $a, $b, $c, TMP_2, self = this, $iter = TMP_1.$$p, block = $iter || nil, names = nil, encoding = nil;

      if (options == null) {
        options = $hash2([], {})
      }
      TMP_1.$$p = null;
      names = $rb_plus([name], (((($a = options['$[]']("aliases")) !== false && $a !== nil) ? $a : [])));
      encoding = ($a = ($b = $scope.get('Class')).$new, $a.$$p = block.$to_proc(), $a).call($b, self).$new(name, names, ((($a = options['$[]']("ascii")) !== false && $a !== nil) ? $a : false), ((($a = options['$[]']("dummy")) !== false && $a !== nil) ? $a : false));
      return ($a = ($c = names).$each, $a.$$p = (TMP_2 = function(name){var self = TMP_2.$$s || this;
if (name == null) name = nil;
      return self.$const_set(name.$sub("-", "_"), encoding)}, TMP_2.$$s = self, TMP_2), $a).call($c);
    });

    Opal.defs(self, '$find', function(name) {try {

      var $a, $b, TMP_3, self = this, upcase = nil;

      upcase = name.$upcase();
      ($a = ($b = self.$constants()).$each, $a.$$p = (TMP_3 = function(const$){var self = TMP_3.$$s || this, $a, $b, encoding = nil;
if (const$ == null) const$ = nil;
      encoding = self.$const_get(const$);
        if ((($a = $scope.get('Encoding')['$==='](encoding)) !== nil && (!$a.$$is_boolean || $a == true))) {
          } else {
          return nil;
        };
        if ((($a = ((($b = encoding.$name()['$=='](upcase)) !== false && $b !== nil) ? $b : encoding.$names()['$include?'](upcase))) !== nil && (!$a.$$is_boolean || $a == true))) {
          Opal.ret(encoding)
          } else {
          return nil
        };}, TMP_3.$$s = self, TMP_3), $a).call($b);
      return self.$raise($scope.get('ArgumentError'), "unknown encoding name - " + (name));
      } catch ($returner) { if ($returner === Opal.returner) { return $returner.$v } throw $returner; }
    });

    (function(self) {
      var $scope = self.$$scope, def = self.$$proto;

      return self.$attr_accessor("default_external")
    })(Opal.get_singleton_class(self));

    self.$attr_reader("name", "names");

    Opal.defn(self, '$initialize', function(name, names, ascii, dummy) {
      var self = this;

      self.name = name;
      self.names = names;
      self.ascii = ascii;
      return self.dummy = dummy;
    });

    Opal.defn(self, '$ascii_compatible?', function() {
      var self = this;

      return self.ascii;
    });

    Opal.defn(self, '$dummy?', function() {
      var self = this;

      return self.dummy;
    });

    Opal.defn(self, '$to_s', function() {
      var self = this;

      return self.name;
    });

    Opal.defn(self, '$inspect', function() {
      var $a, self = this;

      return "#<Encoding:" + (self.name) + ((function() {if ((($a = self.dummy) !== nil && (!$a.$$is_boolean || $a == true))) {
        return " (dummy)"
        } else {
        return nil
      }; return nil; })()) + ">";
    });

    Opal.defn(self, '$each_byte', function() {
      var self = this;

      return self.$raise($scope.get('NotImplementedError'));
    });

    Opal.defn(self, '$getbyte', function() {
      var self = this;

      return self.$raise($scope.get('NotImplementedError'));
    });

    Opal.defn(self, '$bytesize', function() {
      var self = this;

      return self.$raise($scope.get('NotImplementedError'));
    });

    (function($base, $super) {
      function $EncodingError(){};
      var self = $EncodingError = $klass($base, $super, 'EncodingError', $EncodingError);

      var def = self.$$proto, $scope = self.$$scope;

      return nil;
    })($scope.base, $scope.get('StandardError'));

    return (function($base, $super) {
      function $CompatibilityError(){};
      var self = $CompatibilityError = $klass($base, $super, 'CompatibilityError', $CompatibilityError);

      var def = self.$$proto, $scope = self.$$scope;

      return nil;
    })($scope.base, $scope.get('EncodingError'));
  })($scope.base, null);
  ($a = ($b = $scope.get('Encoding')).$register, $a.$$p = (TMP_4 = function(){var self = TMP_4.$$s || this, TMP_5;

  Opal.def(self, '$each_byte', TMP_5 = function(string) {
      var $a, self = this, $iter = TMP_5.$$p, block = $iter || nil;

      TMP_5.$$p = null;
      
      for (var i = 0, length = string.length; i < length; i++) {
        var code = string.charCodeAt(i);

        if (code <= 0x7f) {
          ((($a = Opal.yield1(block, code)) === $breaker) ? $breaker.$v : $a);
        }
        else {
          var encoded = encodeURIComponent(string.charAt(i)).substr(1).split('%');

          for (var j = 0, encoded_length = encoded.length; j < encoded_length; j++) {
            ((($a = Opal.yield1(block, parseInt(encoded[j], 16))) === $breaker) ? $breaker.$v : $a);
          }
        }
      }
    
    });
    return (Opal.def(self, '$bytesize', function() {
      var self = this;

      return self.$bytes().$length();
    }), nil) && 'bytesize';}, TMP_4.$$s = self, TMP_4), $a).call($b, "UTF-8", $hash2(["aliases", "ascii"], {"aliases": ["CP65001"], "ascii": true}));
  ($a = ($c = $scope.get('Encoding')).$register, $a.$$p = (TMP_6 = function(){var self = TMP_6.$$s || this, TMP_7;

  Opal.def(self, '$each_byte', TMP_7 = function(string) {
      var $a, self = this, $iter = TMP_7.$$p, block = $iter || nil;

      TMP_7.$$p = null;
      
      for (var i = 0, length = string.length; i < length; i++) {
        var code = string.charCodeAt(i);

        ((($a = Opal.yield1(block, code & 0xff)) === $breaker) ? $breaker.$v : $a);
        ((($a = Opal.yield1(block, code >> 8)) === $breaker) ? $breaker.$v : $a);
      }
    
    });
    return (Opal.def(self, '$bytesize', function() {
      var self = this;

      return self.$bytes().$length();
    }), nil) && 'bytesize';}, TMP_6.$$s = self, TMP_6), $a).call($c, "UTF-16LE");
  ($a = ($d = $scope.get('Encoding')).$register, $a.$$p = (TMP_8 = function(){var self = TMP_8.$$s || this, TMP_9;

  Opal.def(self, '$each_byte', TMP_9 = function(string) {
      var $a, self = this, $iter = TMP_9.$$p, block = $iter || nil;

      TMP_9.$$p = null;
      
      for (var i = 0, length = string.length; i < length; i++) {
        ((($a = Opal.yield1(block, string.charCodeAt(i) & 0xff)) === $breaker) ? $breaker.$v : $a);
      }
    
    });
    return (Opal.def(self, '$bytesize', function() {
      var self = this;

      return self.$bytes().$length();
    }), nil) && 'bytesize';}, TMP_8.$$s = self, TMP_8), $a).call($d, "ASCII-8BIT", $hash2(["aliases", "ascii"], {"aliases": ["BINARY"], "ascii": true}));
  return (function($base, $super) {
    function $String(){};
    var self = $String = $klass($base, $super, 'String', $String);

    var def = self.$$proto, $scope = self.$$scope, TMP_10;

    def.encoding = nil;
    String.prototype.encoding = (($scope.get('Encoding')).$$scope.get('UTF_16LE'));

    Opal.defn(self, '$bytes', function() {
      var self = this;

      return self.$each_byte().$to_a();
    });

    Opal.defn(self, '$bytesize', function() {
      var self = this;

      return self.encoding.$bytesize(self);
    });

    Opal.defn(self, '$each_byte', TMP_10 = function() {
      var $a, $b, self = this, $iter = TMP_10.$$p, block = $iter || nil;

      TMP_10.$$p = null;
      if ((block !== nil)) {
        } else {
        return self.$enum_for("each_byte")
      };
      ($a = ($b = self.encoding).$each_byte, $a.$$p = block.$to_proc(), $a).call($b, self);
      return self;
    });

    Opal.defn(self, '$encode', function(encoding) {
      var self = this;

      return self.$dup().$force_encoding(encoding);
    });

    Opal.defn(self, '$encoding', function() {
      var self = this;

      return self.encoding;
    });

    Opal.defn(self, '$force_encoding', function(encoding) {
      var $a, self = this;

      encoding = $scope.get('Opal')['$coerce_to!'](encoding, $scope.get('String'), "to_str");
      encoding = $scope.get('Encoding').$find(encoding);
      if (encoding['$=='](self.encoding)) {
        return self};
      if ((($a = encoding['$nil?']()) !== nil && (!$a.$$is_boolean || $a == true))) {
        self.$raise($scope.get('ArgumentError'), "unknown encoding name - " + (encoding))};
      
      var result = new String(self);
      result.encoding = encoding;

      return result;
    
    });

    return (Opal.defn(self, '$getbyte', function(idx) {
      var self = this;

      return self.encoding.$getbyte(self, idx);
    }), nil) && 'getbyte';
  })($scope.base, null);
};

/* Generated by Opal 0.9.0 */
Opal.modules["corelib/math"] = function(Opal) {
  Opal.dynamic_require_severity = "error";
  var OPAL_CONFIG = { method_missing: true, arity_check: false, freezing: true, tainting: true };
  function $rb_minus(lhs, rhs) {
    return (typeof(lhs) === 'number' && typeof(rhs) === 'number') ? lhs - rhs : lhs['$-'](rhs);
  }
  function $rb_divide(lhs, rhs) {
    return (typeof(lhs) === 'number' && typeof(rhs) === 'number') ? lhs / rhs : lhs['$/'](rhs);
  }
  var self = Opal.top, $scope = Opal, nil = Opal.nil, $breaker = Opal.breaker, $slice = Opal.slice, $module = Opal.module;

  Opal.add_stubs(['$new', '$raise', '$Float', '$type_error', '$Integer', '$module_function', '$checked', '$float!', '$===', '$gamma', '$-', '$integer!', '$/', '$infinite?']);
  return (function($base) {
    var $Math, self = $Math = $module($base, 'Math');

    var def = self.$$proto, $scope = self.$$scope, $a;

    Opal.cdecl($scope, 'E', Math.E);

    Opal.cdecl($scope, 'PI', Math.PI);

    Opal.cdecl($scope, 'DomainError', $scope.get('Class').$new($scope.get('StandardError')));

    Opal.defs(self, '$checked', function(method) {
      var self = this, $splat_index = nil;

      var array_size = arguments.length - 1;
      if(array_size < 0) array_size = 0;
      var args = new Array(array_size);
      for($splat_index = 0; $splat_index < array_size; $splat_index++) {
        args[$splat_index] = arguments[$splat_index + 1];
      }
      
      if (isNaN(args[0]) || (args.length == 2 && isNaN(args[1]))) {
        return NaN;
      }

      var result = Math[method].apply(null, args);

      if (isNaN(result)) {
        self.$raise($scope.get('DomainError'), "Numerical argument is out of domain - \"" + (method) + "\"");
      }

      return result;
    
    });

    Opal.defs(self, '$float!', function(value) {
      var self = this;

      try {
      return self.$Float(value)
      } catch ($err) {if (Opal.rescue($err, [$scope.get('ArgumentError')])) {
        try {
          return self.$raise($scope.get('Opal').$type_error(value, $scope.get('Float')))
        } finally {
          Opal.gvars["!"] = Opal.exceptions.pop() || Opal.nil;
        }
        }else { throw $err; }
      };
    });

    Opal.defs(self, '$integer!', function(value) {
      var self = this;

      try {
      return self.$Integer(value)
      } catch ($err) {if (Opal.rescue($err, [$scope.get('ArgumentError')])) {
        try {
          return self.$raise($scope.get('Opal').$type_error(value, $scope.get('Integer')))
        } finally {
          Opal.gvars["!"] = Opal.exceptions.pop() || Opal.nil;
        }
        }else { throw $err; }
      };
    });

    self.$module_function();

    Opal.defn(self, '$acos', function(x) {
      var self = this;

      return $scope.get('Math').$checked("acos", $scope.get('Math')['$float!'](x));
    });

    if ((($a = (typeof(Math.acosh) !== "undefined")) !== nil && (!$a.$$is_boolean || $a == true))) {
      } else {
      
      Math.acosh = function(x) {
        return Math.log(x + Math.sqrt(x * x - 1));
      }
    
    };

    Opal.defn(self, '$acosh', function(x) {
      var self = this;

      return $scope.get('Math').$checked("acosh", $scope.get('Math')['$float!'](x));
    });

    Opal.defn(self, '$asin', function(x) {
      var self = this;

      return $scope.get('Math').$checked("asin", $scope.get('Math')['$float!'](x));
    });

    if ((($a = (typeof(Math.asinh) !== "undefined")) !== nil && (!$a.$$is_boolean || $a == true))) {
      } else {
      
      Math.asinh = function(x) {
        return Math.log(x + Math.sqrt(x * x + 1))
      }
    ;
    };

    Opal.defn(self, '$asinh', function(x) {
      var self = this;

      return $scope.get('Math').$checked("asinh", $scope.get('Math')['$float!'](x));
    });

    Opal.defn(self, '$atan', function(x) {
      var self = this;

      return $scope.get('Math').$checked("atan", $scope.get('Math')['$float!'](x));
    });

    Opal.defn(self, '$atan2', function(y, x) {
      var self = this;

      return $scope.get('Math').$checked("atan2", $scope.get('Math')['$float!'](y), $scope.get('Math')['$float!'](x));
    });

    if ((($a = (typeof(Math.atanh) !== "undefined")) !== nil && (!$a.$$is_boolean || $a == true))) {
      } else {
      
      Math.atanh = function(x) {
        return 0.5 * Math.log((1 + x) / (1 - x));
      }
    
    };

    Opal.defn(self, '$atanh', function(x) {
      var self = this;

      return $scope.get('Math').$checked("atanh", $scope.get('Math')['$float!'](x));
    });

    if ((($a = (typeof(Math.cbrt) !== "undefined")) !== nil && (!$a.$$is_boolean || $a == true))) {
      } else {
      
      Math.cbrt = function(x) {
        if (x == 0) {
          return 0;
        }

        if (x < 0) {
          return -Math.cbrt(-x);
        }

        var r  = x,
            ex = 0;

        while (r < 0.125) {
          r *= 8;
          ex--;
        }

        while (r > 1.0) {
          r *= 0.125;
          ex++;
        }

        r = (-0.46946116 * r + 1.072302) * r + 0.3812513;

        while (ex < 0) {
          r *= 0.5;
          ex++;
        }

        while (ex > 0) {
          r *= 2;
          ex--;
        }

        r = (2.0 / 3.0) * r + (1.0 / 3.0) * x / (r * r);
        r = (2.0 / 3.0) * r + (1.0 / 3.0) * x / (r * r);
        r = (2.0 / 3.0) * r + (1.0 / 3.0) * x / (r * r);
        r = (2.0 / 3.0) * r + (1.0 / 3.0) * x / (r * r);

        return r;
      }
    
    };

    Opal.defn(self, '$cbrt', function(x) {
      var self = this;

      return $scope.get('Math').$checked("cbrt", $scope.get('Math')['$float!'](x));
    });

    Opal.defn(self, '$cos', function(x) {
      var self = this;

      return $scope.get('Math').$checked("cos", $scope.get('Math')['$float!'](x));
    });

    if ((($a = (typeof(Math.cosh) !== "undefined")) !== nil && (!$a.$$is_boolean || $a == true))) {
      } else {
      
      Math.cosh = function(x) {
        return (Math.exp(x) + Math.exp(-x)) / 2;
      }
    
    };

    Opal.defn(self, '$cosh', function(x) {
      var self = this;

      return $scope.get('Math').$checked("cosh", $scope.get('Math')['$float!'](x));
    });

    if ((($a = (typeof(Math.erf) !== "undefined")) !== nil && (!$a.$$is_boolean || $a == true))) {
      } else {
      
      Math.erf = function(x) {
        var A1 =  0.254829592,
            A2 = -0.284496736,
            A3 =  1.421413741,
            A4 = -1.453152027,
            A5 =  1.061405429,
            P  =  0.3275911;

        var sign = 1;

        if (x < 0) {
            sign = -1;
        }

        x = Math.abs(x);

        var t = 1.0 / (1.0 + P * x);
        var y = 1.0 - (((((A5 * t + A4) * t) + A3) * t + A2) * t + A1) * t * Math.exp(-x * x);

        return sign * y;
      }
    
    };

    Opal.defn(self, '$erf', function(x) {
      var self = this;

      return $scope.get('Math').$checked("erf", $scope.get('Math')['$float!'](x));
    });

    if ((($a = (typeof(Math.erfc) !== "undefined")) !== nil && (!$a.$$is_boolean || $a == true))) {
      } else {
      
      Math.erfc = function(x) {
        var z = Math.abs(x),
            t = 1.0 / (0.5 * z + 1.0);

        var A1 = t * 0.17087277 + -0.82215223,
            A2 = t * A1 + 1.48851587,
            A3 = t * A2 + -1.13520398,
            A4 = t * A3 + 0.27886807,
            A5 = t * A4 + -0.18628806,
            A6 = t * A5 + 0.09678418,
            A7 = t * A6 + 0.37409196,
            A8 = t * A7 + 1.00002368,
            A9 = t * A8,
            A10 = -z * z - 1.26551223 + A9;

        var a = t * Math.exp(A10);

        if (x < 0.0) {
          return 2.0 - a;
        }
        else {
          return a;
        }
      }
    
    };

    Opal.defn(self, '$erfc', function(x) {
      var self = this;

      return $scope.get('Math').$checked("erfc", $scope.get('Math')['$float!'](x));
    });

    Opal.defn(self, '$exp', function(x) {
      var self = this;

      return $scope.get('Math').$checked("exp", $scope.get('Math')['$float!'](x));
    });

    Opal.defn(self, '$frexp', function(x) {
      var self = this;

      x = $scope.get('Math')['$float!'](x);
      
      if (isNaN(x)) {
        return [NaN, 0];
      }

      var ex   = Math.floor(Math.log(Math.abs(x)) / Math.log(2)) + 1,
          frac = x / Math.pow(2, ex);

      return [frac, ex];
    
    });

    Opal.defn(self, '$gamma', function(n) {
      var self = this;

      n = $scope.get('Math')['$float!'](n);
      
      var i, t, x, value, result, twoN, threeN, fourN, fiveN;

      var G = 4.7421875;

      var P = [
         0.99999999999999709182,
         57.156235665862923517,
        -59.597960355475491248,
         14.136097974741747174,
        -0.49191381609762019978,
         0.33994649984811888699e-4,
         0.46523628927048575665e-4,
        -0.98374475304879564677e-4,
         0.15808870322491248884e-3,
        -0.21026444172410488319e-3,
         0.21743961811521264320e-3,
        -0.16431810653676389022e-3,
         0.84418223983852743293e-4,
        -0.26190838401581408670e-4,
         0.36899182659531622704e-5
      ];


      if (isNaN(n)) {
        return NaN;
      }

      if (n === 0 && 1 / n < 0) {
        return -Infinity;
      }

      if (n === -1 || n === -Infinity) {
        self.$raise($scope.get('DomainError'), "Numerical argument is out of domain - \"gamma\"");
      }

      if ($scope.get('Integer')['$==='](n)) {
        if (n <= 0) {
          return isFinite(n) ? Infinity : NaN;
        }

        if (n > 171) {
          return Infinity;
        }

        value  = n - 2;
        result = n - 1;

        while (value > 1) {
          result *= value;
          value--;
        }

        if (result == 0) {
          result = 1;
        }

        return result;
      }

      if (n < 0.5) {
        return Math.PI / (Math.sin(Math.PI * n) * $scope.get('Math').$gamma($rb_minus(1, n)));
      }

      if (n >= 171.35) {
        return Infinity;
      }

      if (n > 85.0) {
        twoN   = n * n;
        threeN = twoN * n;
        fourN  = threeN * n;
        fiveN  = fourN * n;

        return Math.sqrt(2 * Math.PI / n) * Math.pow((n / Math.E), n) *
          (1 + 1 / (12 * n) + 1 / (288 * twoN) - 139 / (51840 * threeN) -
          571 / (2488320 * fourN) + 163879 / (209018880 * fiveN) +
          5246819 / (75246796800 * fiveN * n));
      }

      n -= 1;
      x  = P[0];

      for (i = 1; i < P.length; ++i) {
        x += P[i] / (n + i);
      }

      t = n + G + 0.5;

      return Math.sqrt(2 * Math.PI) * Math.pow(t, n + 0.5) * Math.exp(-t) * x;
    
    });

    if ((($a = (typeof(Math.hypot) !== "undefined")) !== nil && (!$a.$$is_boolean || $a == true))) {
      } else {
      
      Math.hypot = function(x, y) {
        return Math.sqrt(x * x + y * y)
      }
    ;
    };

    Opal.defn(self, '$hypot', function(x, y) {
      var self = this;

      return $scope.get('Math').$checked("hypot", $scope.get('Math')['$float!'](x), $scope.get('Math')['$float!'](y));
    });

    Opal.defn(self, '$ldexp', function(mantissa, exponent) {
      var self = this;

      mantissa = $scope.get('Math')['$float!'](mantissa);
      exponent = $scope.get('Math')['$integer!'](exponent);
      
      if (isNaN(exponent)) {
        self.$raise($scope.get('RangeError'), "float NaN out of range of integer");
      }

      return mantissa * Math.pow(2, exponent);
    ;
    });

    Opal.defn(self, '$lgamma', function(n) {
      var self = this;

      
      if (n == -1) {
        return [Infinity, 1];
      }
      else {
        return [Math.log(Math.abs($scope.get('Math').$gamma(n))), $scope.get('Math').$gamma(n) < 0 ? -1 : 1];
      }
    ;
    });

    Opal.defn(self, '$log', function(x, base) {
      var $a, self = this;

      if ((($a = $scope.get('String')['$==='](x)) !== nil && (!$a.$$is_boolean || $a == true))) {
        self.$raise($scope.get('Opal').$type_error(x, $scope.get('Float')))};
      if ((($a = base == null) !== nil && (!$a.$$is_boolean || $a == true))) {
        return $scope.get('Math').$checked("log", $scope.get('Math')['$float!'](x))
        } else {
        if ((($a = $scope.get('String')['$==='](base)) !== nil && (!$a.$$is_boolean || $a == true))) {
          self.$raise($scope.get('Opal').$type_error(base, $scope.get('Float')))};
        return $rb_divide($scope.get('Math').$checked("log", $scope.get('Math')['$float!'](x)), $scope.get('Math').$checked("log", $scope.get('Math')['$float!'](base)));
      };
    });

    if ((($a = (typeof(Math.log10) !== "undefined")) !== nil && (!$a.$$is_boolean || $a == true))) {
      } else {
      
      Math.log10 = function(x) {
        return Math.log(x) / Math.LN10;
      }
    
    };

    Opal.defn(self, '$log10', function(x) {
      var $a, self = this;

      if ((($a = $scope.get('String')['$==='](x)) !== nil && (!$a.$$is_boolean || $a == true))) {
        self.$raise($scope.get('Opal').$type_error(x, $scope.get('Float')))};
      return $scope.get('Math').$checked("log10", $scope.get('Math')['$float!'](x));
    });

    if ((($a = (typeof(Math.log2) !== "undefined")) !== nil && (!$a.$$is_boolean || $a == true))) {
      } else {
      
      Math.log2 = function(x) {
        return Math.log(x) / Math.LN2;
      }
    
    };

    Opal.defn(self, '$log2', function(x) {
      var $a, self = this;

      if ((($a = $scope.get('String')['$==='](x)) !== nil && (!$a.$$is_boolean || $a == true))) {
        self.$raise($scope.get('Opal').$type_error(x, $scope.get('Float')))};
      return $scope.get('Math').$checked("log2", $scope.get('Math')['$float!'](x));
    });

    Opal.defn(self, '$sin', function(x) {
      var self = this;

      return $scope.get('Math').$checked("sin", $scope.get('Math')['$float!'](x));
    });

    if ((($a = (typeof(Math.sinh) !== "undefined")) !== nil && (!$a.$$is_boolean || $a == true))) {
      } else {
      
      Math.sinh = function(x) {
        return (Math.exp(x) - Math.exp(-x)) / 2;
      }
    
    };

    Opal.defn(self, '$sinh', function(x) {
      var self = this;

      return $scope.get('Math').$checked("sinh", $scope.get('Math')['$float!'](x));
    });

    Opal.defn(self, '$sqrt', function(x) {
      var self = this;

      return $scope.get('Math').$checked("sqrt", $scope.get('Math')['$float!'](x));
    });

    Opal.defn(self, '$tan', function(x) {
      var $a, self = this;

      x = $scope.get('Math')['$float!'](x);
      if ((($a = x['$infinite?']()) !== nil && (!$a.$$is_boolean || $a == true))) {
        return (($scope.get('Float')).$$scope.get('NAN'))};
      return $scope.get('Math').$checked("tan", $scope.get('Math')['$float!'](x));
    });

    if ((($a = (typeof(Math.tanh) !== "undefined")) !== nil && (!$a.$$is_boolean || $a == true))) {
      } else {
      
      Math.tanh = function(x) {
        if (x == Infinity) {
          return 1;
        }
        else if (x == -Infinity) {
          return -1;
        }
        else {
          return (Math.exp(x) - Math.exp(-x)) / (Math.exp(x) + Math.exp(-x));
        }
      }
    
    };

    Opal.defn(self, '$tanh', function(x) {
      var self = this;

      return $scope.get('Math').$checked("tanh", $scope.get('Math')['$float!'](x));
    });
  })($scope.base)
};

/* Generated by Opal 0.9.0 */
Opal.modules["corelib/complex"] = function(Opal) {
  Opal.dynamic_require_severity = "error";
  var OPAL_CONFIG = { method_missing: true, arity_check: false, freezing: true, tainting: true };
  function $rb_times(lhs, rhs) {
    return (typeof(lhs) === 'number' && typeof(rhs) === 'number') ? lhs * rhs : lhs['$*'](rhs);
  }
  function $rb_plus(lhs, rhs) {
    return (typeof(lhs) === 'number' && typeof(rhs) === 'number') ? lhs + rhs : lhs['$+'](rhs);
  }
  function $rb_minus(lhs, rhs) {
    return (typeof(lhs) === 'number' && typeof(rhs) === 'number') ? lhs - rhs : lhs['$-'](rhs);
  }
  function $rb_divide(lhs, rhs) {
    return (typeof(lhs) === 'number' && typeof(rhs) === 'number') ? lhs / rhs : lhs['$/'](rhs);
  }
  function $rb_gt(lhs, rhs) {
    return (typeof(lhs) === 'number' && typeof(rhs) === 'number') ? lhs > rhs : lhs['$>'](rhs);
  }
  var self = Opal.top, $scope = Opal, nil = Opal.nil, $breaker = Opal.breaker, $slice = Opal.slice, $klass = Opal.klass, $module = Opal.module;

  Opal.add_stubs(['$require', '$===', '$real?', '$raise', '$new', '$*', '$cos', '$sin', '$attr_reader', '$class', '$==', '$real', '$imag', '$Complex', '$-@', '$+', '$__coerced__', '$-', '$nan?', '$/', '$conj', '$abs2', '$quo', '$polar', '$exp', '$log', '$>', '$!=', '$divmod', '$**', '$hypot', '$atan2', '$lcm', '$denominator', '$to_s', '$numerator', '$abs', '$arg', '$rationalize', '$to_f', '$to_i', '$to_r', '$inspect', '$positive?', '$infinite?']);
  self.$require("corelib/numeric");
  (function($base, $super) {
    function $Complex(){};
    var self = $Complex = $klass($base, $super, 'Complex', $Complex);

    var def = self.$$proto, $scope = self.$$scope;

    def.real = def.imag = nil;
    Opal.defs(self, '$rect', function(real, imag) {
      var $a, $b, $c, $d, self = this;

      if (imag == null) {
        imag = 0
      }
      if ((($a = ($b = ($c = ($d = $scope.get('Numeric')['$==='](real), $d !== false && $d !== nil ?real['$real?']() : $d), $c !== false && $c !== nil ?$scope.get('Numeric')['$==='](imag) : $c), $b !== false && $b !== nil ?imag['$real?']() : $b)) !== nil && (!$a.$$is_boolean || $a == true))) {
        } else {
        self.$raise($scope.get('TypeError'), "not a real")
      };
      return self.$new(real, imag);
    });

    (function(self) {
      var $scope = self.$$scope, def = self.$$proto;

      return Opal.alias(self, 'rectangular', 'rect')
    })(Opal.get_singleton_class(self));

    Opal.defs(self, '$polar', function(r, theta) {
      var $a, $b, $c, $d, self = this;

      if (theta == null) {
        theta = 0
      }
      if ((($a = ($b = ($c = ($d = $scope.get('Numeric')['$==='](r), $d !== false && $d !== nil ?r['$real?']() : $d), $c !== false && $c !== nil ?$scope.get('Numeric')['$==='](theta) : $c), $b !== false && $b !== nil ?theta['$real?']() : $b)) !== nil && (!$a.$$is_boolean || $a == true))) {
        } else {
        self.$raise($scope.get('TypeError'), "not a real")
      };
      return self.$new($rb_times(r, $scope.get('Math').$cos(theta)), $rb_times(r, $scope.get('Math').$sin(theta)));
    });

    self.$attr_reader("real", "imag");

    Opal.defn(self, '$initialize', function(real, imag) {
      var self = this;

      if (imag == null) {
        imag = 0
      }
      self.real = real;
      return self.imag = imag;
    });

    Opal.defn(self, '$coerce', function(other) {
      var $a, $b, self = this;

      if ((($a = $scope.get('Complex')['$==='](other)) !== nil && (!$a.$$is_boolean || $a == true))) {
        return [other, self]
      } else if ((($a = ($b = $scope.get('Numeric')['$==='](other), $b !== false && $b !== nil ?other['$real?']() : $b)) !== nil && (!$a.$$is_boolean || $a == true))) {
        return [$scope.get('Complex').$new(other, 0), self]
        } else {
        return self.$raise($scope.get('TypeError'), "" + (other.$class()) + " can't be coerced into Complex")
      };
    });

    Opal.defn(self, '$==', function(other) {
      var $a, $b, self = this;

      if ((($a = $scope.get('Complex')['$==='](other)) !== nil && (!$a.$$is_boolean || $a == true))) {
        return (($a = self.real['$=='](other.$real())) ? self.imag['$=='](other.$imag()) : self.real['$=='](other.$real()))
      } else if ((($a = ($b = $scope.get('Numeric')['$==='](other), $b !== false && $b !== nil ?other['$real?']() : $b)) !== nil && (!$a.$$is_boolean || $a == true))) {
        return (($a = self.real['$=='](other)) ? self.imag['$=='](0) : self.real['$=='](other))
        } else {
        return other['$=='](self)
      };
    });

    Opal.defn(self, '$-@', function() {
      var self = this;

      return self.$Complex(self.real['$-@'](), self.imag['$-@']());
    });

    Opal.defn(self, '$+', function(other) {
      var $a, $b, self = this;

      if ((($a = $scope.get('Complex')['$==='](other)) !== nil && (!$a.$$is_boolean || $a == true))) {
        return self.$Complex($rb_plus(self.real, other.$real()), $rb_plus(self.imag, other.$imag()))
      } else if ((($a = ($b = $scope.get('Numeric')['$==='](other), $b !== false && $b !== nil ?other['$real?']() : $b)) !== nil && (!$a.$$is_boolean || $a == true))) {
        return self.$Complex($rb_plus(self.real, other), self.imag)
        } else {
        return self.$__coerced__("+", other)
      };
    });

    Opal.defn(self, '$-', function(other) {
      var $a, $b, self = this;

      if ((($a = $scope.get('Complex')['$==='](other)) !== nil && (!$a.$$is_boolean || $a == true))) {
        return self.$Complex($rb_minus(self.real, other.$real()), $rb_minus(self.imag, other.$imag()))
      } else if ((($a = ($b = $scope.get('Numeric')['$==='](other), $b !== false && $b !== nil ?other['$real?']() : $b)) !== nil && (!$a.$$is_boolean || $a == true))) {
        return self.$Complex($rb_minus(self.real, other), self.imag)
        } else {
        return self.$__coerced__("-", other)
      };
    });

    Opal.defn(self, '$*', function(other) {
      var $a, $b, self = this;

      if ((($a = $scope.get('Complex')['$==='](other)) !== nil && (!$a.$$is_boolean || $a == true))) {
        return self.$Complex($rb_minus($rb_times(self.real, other.$real()), $rb_times(self.imag, other.$imag())), $rb_plus($rb_times(self.real, other.$imag()), $rb_times(self.imag, other.$real())))
      } else if ((($a = ($b = $scope.get('Numeric')['$==='](other), $b !== false && $b !== nil ?other['$real?']() : $b)) !== nil && (!$a.$$is_boolean || $a == true))) {
        return self.$Complex($rb_times(self.real, other), $rb_times(self.imag, other))
        } else {
        return self.$__coerced__("*", other)
      };
    });

    Opal.defn(self, '$/', function(other) {
      var $a, $b, $c, $d, $e, self = this;

      if ((($a = $scope.get('Complex')['$==='](other)) !== nil && (!$a.$$is_boolean || $a == true))) {
        if ((($a = ((($b = ((($c = ((($d = (($e = $scope.get('Number')['$==='](self.real), $e !== false && $e !== nil ?self.real['$nan?']() : $e))) !== false && $d !== nil) ? $d : (($e = $scope.get('Number')['$==='](self.imag), $e !== false && $e !== nil ?self.imag['$nan?']() : $e)))) !== false && $c !== nil) ? $c : (($d = $scope.get('Number')['$==='](other.$real()), $d !== false && $d !== nil ?other.$real()['$nan?']() : $d)))) !== false && $b !== nil) ? $b : (($c = $scope.get('Number')['$==='](other.$imag()), $c !== false && $c !== nil ?other.$imag()['$nan?']() : $c)))) !== nil && (!$a.$$is_boolean || $a == true))) {
          return $scope.get('Complex').$new((($scope.get('Float')).$$scope.get('NAN')), (($scope.get('Float')).$$scope.get('NAN')))
          } else {
          return $rb_divide($rb_times(self, other.$conj()), other.$abs2())
        }
      } else if ((($a = ($b = $scope.get('Numeric')['$==='](other), $b !== false && $b !== nil ?other['$real?']() : $b)) !== nil && (!$a.$$is_boolean || $a == true))) {
        return self.$Complex(self.real.$quo(other), self.imag.$quo(other))
        } else {
        return self.$__coerced__("/", other)
      };
    });

    Opal.defn(self, '$**', function(other) {
      var $a, $b, $c, $d, $e, self = this, r = nil, theta = nil, ore = nil, oim = nil, nr = nil, ntheta = nil, x = nil, z = nil, n = nil, div = nil, mod = nil;

      if (other['$=='](0)) {
        return $scope.get('Complex').$new(1, 0)};
      if ((($a = $scope.get('Complex')['$==='](other)) !== nil && (!$a.$$is_boolean || $a == true))) {
        $b = self.$polar(), $a = Opal.to_ary($b), r = ($a[0] == null ? nil : $a[0]), theta = ($a[1] == null ? nil : $a[1]), $b;
        ore = other.$real();
        oim = other.$imag();
        nr = $scope.get('Math').$exp($rb_minus($rb_times(ore, $scope.get('Math').$log(r)), $rb_times(oim, theta)));
        ntheta = $rb_plus($rb_times(theta, ore), $rb_times(oim, $scope.get('Math').$log(r)));
        return $scope.get('Complex').$polar(nr, ntheta);
      } else if ((($a = $scope.get('Integer')['$==='](other)) !== nil && (!$a.$$is_boolean || $a == true))) {
        if ($rb_gt(other, 0)) {
          x = self;
          z = x;
          n = $rb_minus(other, 1);
          while ((($b = n['$!='](0)) !== nil && (!$b.$$is_boolean || $b == true))) {
          while ((($c = ($e = n.$divmod(2), $d = Opal.to_ary($e), div = ($d[0] == null ? nil : $d[0]), mod = ($d[1] == null ? nil : $d[1]), $e, mod['$=='](0))) !== nil && (!$c.$$is_boolean || $c == true))) {
          x = self.$Complex($rb_minus($rb_times(x.$real(), x.$real()), $rb_times(x.$imag(), x.$imag())), $rb_times($rb_times(2, x.$real()), x.$imag()));
          n = div;};
          z = $rb_times(z, x);
          n = $rb_minus(n, 1);};
          return z;
          } else {
          return ($rb_divide($scope.get('Rational').$new(1, 1), self))['$**'](other['$-@']())
        }
      } else if ((($a = ((($b = $scope.get('Float')['$==='](other)) !== false && $b !== nil) ? $b : $scope.get('Rational')['$==='](other))) !== nil && (!$a.$$is_boolean || $a == true))) {
        $b = self.$polar(), $a = Opal.to_ary($b), r = ($a[0] == null ? nil : $a[0]), theta = ($a[1] == null ? nil : $a[1]), $b;
        return $scope.get('Complex').$polar(r['$**'](other), $rb_times(theta, other));
        } else {
        return self.$__coerced__("**", other)
      };
    });

    Opal.defn(self, '$abs', function() {
      var self = this;

      return $scope.get('Math').$hypot(self.real, self.imag);
    });

    Opal.defn(self, '$abs2', function() {
      var self = this;

      return $rb_plus($rb_times(self.real, self.real), $rb_times(self.imag, self.imag));
    });

    Opal.defn(self, '$angle', function() {
      var self = this;

      return $scope.get('Math').$atan2(self.imag, self.real);
    });

    Opal.alias(self, 'arg', 'angle');

    Opal.defn(self, '$conj', function() {
      var self = this;

      return self.$Complex(self.real, self.imag['$-@']());
    });

    Opal.alias(self, 'conjugate', 'conj');

    Opal.defn(self, '$denominator', function() {
      var self = this;

      return self.real.$denominator().$lcm(self.imag.$denominator());
    });

    Opal.alias(self, 'divide', '/');

    Opal.defn(self, '$eql?', function(other) {
      var $a, $b, self = this;

      return ($a = ($b = $scope.get('Complex')['$==='](other), $b !== false && $b !== nil ?self.real.$class()['$=='](self.imag.$class()) : $b), $a !== false && $a !== nil ?self['$=='](other) : $a);
    });

    Opal.defn(self, '$fdiv', function(other) {
      var $a, self = this;

      if ((($a = $scope.get('Numeric')['$==='](other)) !== nil && (!$a.$$is_boolean || $a == true))) {
        } else {
        self.$raise($scope.get('TypeError'), "" + (other.$class()) + " can't be coerced into Complex")
      };
      return $rb_divide(self, other);
    });

    Opal.defn(self, '$hash', function() {
      var self = this;

      return "Complex:" + (self.real) + ":" + (self.imag);
    });

    Opal.alias(self, 'imaginary', 'imag');

    Opal.defn(self, '$inspect', function() {
      var self = this;

      return "(" + (self.$to_s()) + ")";
    });

    Opal.alias(self, 'magnitude', 'abs');

    Opal.defn(self, '$numerator', function() {
      var self = this, d = nil;

      d = self.$denominator();
      return self.$Complex($rb_times(self.real.$numerator(), ($rb_divide(d, self.real.$denominator()))), $rb_times(self.imag.$numerator(), ($rb_divide(d, self.imag.$denominator()))));
    });

    Opal.alias(self, 'phase', 'arg');

    Opal.defn(self, '$polar', function() {
      var self = this;

      return [self.$abs(), self.$arg()];
    });

    Opal.alias(self, 'quo', '/');

    Opal.defn(self, '$rationalize', function(eps) {
      var $a, self = this;

      
      if (arguments.length > 1) {
        self.$raise($scope.get('ArgumentError'), "wrong number of arguments (" + (arguments.length) + " for 0..1)");
      }
    ;
      if ((($a = self.imag['$!='](0)) !== nil && (!$a.$$is_boolean || $a == true))) {
        self.$raise($scope.get('RangeError'), "can't' convert " + (self) + " into Rational")};
      return self.$real().$rationalize(eps);
    });

    Opal.defn(self, '$real?', function() {
      var self = this;

      return false;
    });

    Opal.defn(self, '$rect', function() {
      var self = this;

      return [self.real, self.imag];
    });

    Opal.alias(self, 'rectangular', 'rect');

    Opal.defn(self, '$to_f', function() {
      var self = this;

      if (self.imag['$=='](0)) {
        } else {
        self.$raise($scope.get('RangeError'), "can't convert " + (self) + " into Float")
      };
      return self.real.$to_f();
    });

    Opal.defn(self, '$to_i', function() {
      var self = this;

      if (self.imag['$=='](0)) {
        } else {
        self.$raise($scope.get('RangeError'), "can't convert " + (self) + " into Integer")
      };
      return self.real.$to_i();
    });

    Opal.defn(self, '$to_r', function() {
      var self = this;

      if (self.imag['$=='](0)) {
        } else {
        self.$raise($scope.get('RangeError'), "can't convert " + (self) + " into Rational")
      };
      return self.real.$to_r();
    });

    Opal.defn(self, '$to_s', function() {
      var $a, $b, $c, self = this, result = nil;

      result = self.real.$inspect();
      if ((($a = ((($b = (($c = $scope.get('Number')['$==='](self.imag), $c !== false && $c !== nil ?self.imag['$nan?']() : $c))) !== false && $b !== nil) ? $b : self.imag['$positive?']())) !== nil && (!$a.$$is_boolean || $a == true))) {
        result = $rb_plus(result, "+")
        } else {
        result = $rb_plus(result, "-")
      };
      result = $rb_plus(result, self.imag.$abs().$inspect());
      if ((($a = ($b = $scope.get('Number')['$==='](self.imag), $b !== false && $b !== nil ?(((($c = self.imag['$nan?']()) !== false && $c !== nil) ? $c : self.imag['$infinite?']())) : $b)) !== nil && (!$a.$$is_boolean || $a == true))) {
        result = $rb_plus(result, "*")};
      return $rb_plus(result, "i");
    });

    return Opal.cdecl($scope, 'I', self.$new(0, 1));
  })($scope.base, $scope.get('Numeric'));
  return (function($base) {
    var $Kernel, self = $Kernel = $module($base, 'Kernel');

    var def = self.$$proto, $scope = self.$$scope;

    Opal.defn(self, '$Complex', function(real, imag) {
      var self = this;

      if (imag == null) {
        imag = nil
      }
      if (imag !== false && imag !== nil) {
        return $scope.get('Complex').$new(real, imag)
        } else {
        return $scope.get('Complex').$new(real, 0)
      };
    })
  })($scope.base);
};

/* Generated by Opal 0.9.0 */
Opal.modules["corelib/rational"] = function(Opal) {
  Opal.dynamic_require_severity = "error";
  var OPAL_CONFIG = { method_missing: true, arity_check: false, freezing: true, tainting: true };
  function $rb_lt(lhs, rhs) {
    return (typeof(lhs) === 'number' && typeof(rhs) === 'number') ? lhs < rhs : lhs['$<'](rhs);
  }
  function $rb_divide(lhs, rhs) {
    return (typeof(lhs) === 'number' && typeof(rhs) === 'number') ? lhs / rhs : lhs['$/'](rhs);
  }
  function $rb_minus(lhs, rhs) {
    return (typeof(lhs) === 'number' && typeof(rhs) === 'number') ? lhs - rhs : lhs['$-'](rhs);
  }
  function $rb_times(lhs, rhs) {
    return (typeof(lhs) === 'number' && typeof(rhs) === 'number') ? lhs * rhs : lhs['$*'](rhs);
  }
  function $rb_plus(lhs, rhs) {
    return (typeof(lhs) === 'number' && typeof(rhs) === 'number') ? lhs + rhs : lhs['$+'](rhs);
  }
  function $rb_gt(lhs, rhs) {
    return (typeof(lhs) === 'number' && typeof(rhs) === 'number') ? lhs > rhs : lhs['$>'](rhs);
  }
  function $rb_le(lhs, rhs) {
    return (typeof(lhs) === 'number' && typeof(rhs) === 'number') ? lhs <= rhs : lhs['$<='](rhs);
  }
  var self = Opal.top, $scope = Opal, nil = Opal.nil, $breaker = Opal.breaker, $slice = Opal.slice, $klass = Opal.klass, $module = Opal.module;

  Opal.add_stubs(['$require', '$to_i', '$==', '$raise', '$<', '$-@', '$new', '$gcd', '$/', '$nil?', '$===', '$reduce', '$to_r', '$equal?', '$!', '$coerce_to!', '$attr_reader', '$to_f', '$numerator', '$denominator', '$<=>', '$-', '$*', '$__coerced__', '$+', '$Rational', '$>', '$**', '$abs', '$ceil', '$with_precision', '$floor', '$to_s', '$<=', '$truncate', '$send', '$convert']);
  self.$require("corelib/numeric");
  (function($base, $super) {
    function $Rational(){};
    var self = $Rational = $klass($base, $super, 'Rational', $Rational);

    var def = self.$$proto, $scope = self.$$scope;

    def.num = def.den = nil;
    Opal.defs(self, '$reduce', function(num, den) {
      var self = this, gcd = nil;

      num = num.$to_i();
      den = den.$to_i();
      if (den['$=='](0)) {
        self.$raise($scope.get('ZeroDivisionError'), "divided by 0")
      } else if ($rb_lt(den, 0)) {
        num = num['$-@']();
        den = den['$-@']();
      } else if (den['$=='](1)) {
        return self.$new(num, den)};
      gcd = num.$gcd(den);
      return self.$new($rb_divide(num, gcd), $rb_divide(den, gcd));
    });

    Opal.defs(self, '$convert', function(num, den) {
      var $a, $b, $c, self = this;

      if ((($a = ((($b = num['$nil?']()) !== false && $b !== nil) ? $b : den['$nil?']())) !== nil && (!$a.$$is_boolean || $a == true))) {
        self.$raise($scope.get('TypeError'), "cannot convert nil into Rational")};
      if ((($a = ($b = $scope.get('Integer')['$==='](num), $b !== false && $b !== nil ?$scope.get('Integer')['$==='](den) : $b)) !== nil && (!$a.$$is_boolean || $a == true))) {
        return self.$reduce(num, den)};
      if ((($a = ((($b = ((($c = $scope.get('Float')['$==='](num)) !== false && $c !== nil) ? $c : $scope.get('String')['$==='](num))) !== false && $b !== nil) ? $b : $scope.get('Complex')['$==='](num))) !== nil && (!$a.$$is_boolean || $a == true))) {
        num = num.$to_r()};
      if ((($a = ((($b = ((($c = $scope.get('Float')['$==='](den)) !== false && $c !== nil) ? $c : $scope.get('String')['$==='](den))) !== false && $b !== nil) ? $b : $scope.get('Complex')['$==='](den))) !== nil && (!$a.$$is_boolean || $a == true))) {
        den = den.$to_r()};
      if ((($a = ($b = den['$equal?'](1), $b !== false && $b !== nil ?($scope.get('Integer')['$==='](num))['$!']() : $b)) !== nil && (!$a.$$is_boolean || $a == true))) {
        return $scope.get('Opal')['$coerce_to!'](num, $scope.get('Rational'), "to_r")
      } else if ((($a = ($b = $scope.get('Numeric')['$==='](num), $b !== false && $b !== nil ?$scope.get('Numeric')['$==='](den) : $b)) !== nil && (!$a.$$is_boolean || $a == true))) {
        return $rb_divide(num, den)
        } else {
        return self.$reduce(num, den)
      };
    });

    self.$attr_reader("numerator", "denominator");

    Opal.defn(self, '$initialize', function(num, den) {
      var self = this;

      self.num = num;
      return self.den = den;
    });

    Opal.defn(self, '$numerator', function() {
      var self = this;

      return self.num;
    });

    Opal.defn(self, '$denominator', function() {
      var self = this;

      return self.den;
    });

    Opal.defn(self, '$coerce', function(other) {
      var self = this, $case = nil;

      return (function() {$case = other;if ($scope.get('Rational')['$===']($case)) {return [other, self]}else if ($scope.get('Integer')['$===']($case)) {return [other.$to_r(), self]}else if ($scope.get('Float')['$===']($case)) {return [other, self.$to_f()]}else { return nil }})();
    });

    Opal.defn(self, '$==', function(other) {
      var $a, self = this, $case = nil;

      return (function() {$case = other;if ($scope.get('Rational')['$===']($case)) {return (($a = self.num['$=='](other.$numerator())) ? self.den['$=='](other.$denominator()) : self.num['$=='](other.$numerator()))}else if ($scope.get('Integer')['$===']($case)) {return (($a = self.num['$=='](other)) ? self.den['$=='](1) : self.num['$=='](other))}else if ($scope.get('Float')['$===']($case)) {return self.$to_f()['$=='](other)}else {return other['$=='](self)}})();
    });

    Opal.defn(self, '$<=>', function(other) {
      var self = this, $case = nil;

      return (function() {$case = other;if ($scope.get('Rational')['$===']($case)) {return $rb_minus($rb_times(self.num, other.$denominator()), $rb_times(self.den, other.$numerator()))['$<=>'](0)}else if ($scope.get('Integer')['$===']($case)) {return $rb_minus(self.num, $rb_times(self.den, other))['$<=>'](0)}else if ($scope.get('Float')['$===']($case)) {return self.$to_f()['$<=>'](other)}else {return self.$__coerced__("<=>", other)}})();
    });

    Opal.defn(self, '$+', function(other) {
      var self = this, $case = nil, num = nil, den = nil;

      return (function() {$case = other;if ($scope.get('Rational')['$===']($case)) {num = $rb_plus($rb_times(self.num, other.$denominator()), $rb_times(self.den, other.$numerator()));
      den = $rb_times(self.den, other.$denominator());
      return self.$Rational(num, den);}else if ($scope.get('Integer')['$===']($case)) {return self.$Rational($rb_plus(self.num, $rb_times(other, self.den)), self.den)}else if ($scope.get('Float')['$===']($case)) {return $rb_plus(self.$to_f(), other)}else {return self.$__coerced__("+", other)}})();
    });

    Opal.defn(self, '$-', function(other) {
      var self = this, $case = nil, num = nil, den = nil;

      return (function() {$case = other;if ($scope.get('Rational')['$===']($case)) {num = $rb_minus($rb_times(self.num, other.$denominator()), $rb_times(self.den, other.$numerator()));
      den = $rb_times(self.den, other.$denominator());
      return self.$Rational(num, den);}else if ($scope.get('Integer')['$===']($case)) {return self.$Rational($rb_minus(self.num, $rb_times(other, self.den)), self.den)}else if ($scope.get('Float')['$===']($case)) {return $rb_minus(self.$to_f(), other)}else {return self.$__coerced__("-", other)}})();
    });

    Opal.defn(self, '$*', function(other) {
      var self = this, $case = nil, num = nil, den = nil;

      return (function() {$case = other;if ($scope.get('Rational')['$===']($case)) {num = $rb_times(self.num, other.$numerator());
      den = $rb_times(self.den, other.$denominator());
      return self.$Rational(num, den);}else if ($scope.get('Integer')['$===']($case)) {return self.$Rational($rb_times(self.num, other), self.den)}else if ($scope.get('Float')['$===']($case)) {return $rb_times(self.$to_f(), other)}else {return self.$__coerced__("*", other)}})();
    });

    Opal.defn(self, '$/', function(other) {
      var self = this, $case = nil, num = nil, den = nil;

      return (function() {$case = other;if ($scope.get('Rational')['$===']($case)) {num = $rb_times(self.num, other.$denominator());
      den = $rb_times(self.den, other.$numerator());
      return self.$Rational(num, den);}else if ($scope.get('Integer')['$===']($case)) {if (other['$=='](0)) {
        return $rb_divide(self.$to_f(), 0.0)
        } else {
        return self.$Rational(self.num, $rb_times(self.den, other))
      }}else if ($scope.get('Float')['$===']($case)) {return $rb_divide(self.$to_f(), other)}else {return self.$__coerced__("/", other)}})();
    });

    Opal.defn(self, '$**', function(other) {
      var $a, $b, self = this, $case = nil;

      return (function() {$case = other;if ($scope.get('Integer')['$===']($case)) {if ((($a = (($b = self['$=='](0)) ? $rb_lt(other, 0) : self['$=='](0))) !== nil && (!$a.$$is_boolean || $a == true))) {
        return (($scope.get('Float')).$$scope.get('INFINITY'))
      } else if ($rb_gt(other, 0)) {
        return self.$Rational(self.num['$**'](other), self.den['$**'](other))
      } else if ($rb_lt(other, 0)) {
        return self.$Rational(self.den['$**'](other['$-@']()), self.num['$**'](other['$-@']()))
        } else {
        return self.$Rational(1, 1)
      }}else if ($scope.get('Float')['$===']($case)) {return self.$to_f()['$**'](other)}else if ($scope.get('Rational')['$===']($case)) {if (other['$=='](0)) {
        return self.$Rational(1, 1)
      } else if (other.$denominator()['$=='](1)) {
        if ($rb_lt(other, 0)) {
          return self.$Rational(self.den['$**'](other.$numerator().$abs()), self.num['$**'](other.$numerator().$abs()))
          } else {
          return self.$Rational(self.num['$**'](other.$numerator()), self.den['$**'](other.$numerator()))
        }
      } else if ((($a = (($b = self['$=='](0)) ? $rb_lt(other, 0) : self['$=='](0))) !== nil && (!$a.$$is_boolean || $a == true))) {
        return self.$raise($scope.get('ZeroDivisionError'), "divided by 0")
        } else {
        return self.$to_f()['$**'](other)
      }}else {return self.$__coerced__("**", other)}})();
    });

    Opal.defn(self, '$abs', function() {
      var self = this;

      return self.$Rational(self.num.$abs(), self.den.$abs());
    });

    Opal.defn(self, '$ceil', function(precision) {
      var self = this;

      if (precision == null) {
        precision = 0
      }
      if (precision['$=='](0)) {
        return (($rb_divide(self.num['$-@'](), self.den))['$-@']()).$ceil()
        } else {
        return self.$with_precision("ceil", precision)
      };
    });

    Opal.alias(self, 'divide', '/');

    Opal.defn(self, '$floor', function(precision) {
      var self = this;

      if (precision == null) {
        precision = 0
      }
      if (precision['$=='](0)) {
        return (($rb_divide(self.num['$-@'](), self.den))['$-@']()).$floor()
        } else {
        return self.$with_precision("floor", precision)
      };
    });

    Opal.defn(self, '$hash', function() {
      var self = this;

      return "Rational:" + (self.num) + ":" + (self.den);
    });

    Opal.defn(self, '$inspect', function() {
      var self = this;

      return "(" + (self.$to_s()) + ")";
    });

    Opal.alias(self, 'quo', '/');

    Opal.defn(self, '$rationalize', function(eps) {
      var self = this;

      
      if (arguments.length > 1) {
        self.$raise($scope.get('ArgumentError'), "wrong number of arguments (" + (arguments.length) + " for 0..1)");
      }

      if (eps == null) {
        return self;
      }

      var e = eps.$abs(),
          a = $rb_minus(self, e),
          b = $rb_plus(self, e);

      var p0 = 0,
          p1 = 1,
          q0 = 1,
          q1 = 0,
          p2, q2;

      var c, k, t;

      while (true) {
        c = (a).$ceil();

        if ($rb_le(c, b)) {
          break;
        }

        k  = c - 1;
        p2 = k * p1 + p0;
        q2 = k * q1 + q0;
        t  = $rb_divide(1, ($rb_minus(b, k)));
        b  = $rb_divide(1, ($rb_minus(a, k)));
        a  = t;

        p0 = p1;
        q0 = q1;
        p1 = p2;
        q1 = q2;
      }

      return self.$Rational(c * p1 + p0, c * q1 + q0);
    ;
    });

    Opal.defn(self, '$round', function(precision) {
      var self = this, num = nil, den = nil, approx = nil;

      if (precision == null) {
        precision = 0
      }
      if (precision['$=='](0)) {
        } else {
        return self.$with_precision("round", precision)
      };
      if (self.num['$=='](0)) {
        return 0};
      if (self.den['$=='](1)) {
        return self.num};
      num = $rb_plus($rb_times(self.num.$abs(), 2), self.den);
      den = $rb_times(self.den, 2);
      approx = ($rb_divide(num, den)).$truncate();
      if ($rb_lt(self.num, 0)) {
        return approx['$-@']()
        } else {
        return approx
      };
    });

    Opal.defn(self, '$to_f', function() {
      var self = this;

      return $rb_divide(self.num, self.den);
    });

    Opal.defn(self, '$to_i', function() {
      var self = this;

      return self.$truncate();
    });

    Opal.defn(self, '$to_r', function() {
      var self = this;

      return self;
    });

    Opal.defn(self, '$to_s', function() {
      var self = this;

      return "" + (self.num) + "/" + (self.den);
    });

    Opal.defn(self, '$truncate', function(precision) {
      var self = this;

      if (precision == null) {
        precision = 0
      }
      if (precision['$=='](0)) {
        if ($rb_lt(self.num, 0)) {
          return self.$ceil()
          } else {
          return self.$floor()
        }
        } else {
        return self.$with_precision("truncate", precision)
      };
    });

    return (Opal.defn(self, '$with_precision', function(method, precision) {
      var $a, self = this, p = nil, s = nil;

      if ((($a = $scope.get('Integer')['$==='](precision)) !== nil && (!$a.$$is_boolean || $a == true))) {
        } else {
        self.$raise($scope.get('TypeError'), "not an Integer")
      };
      p = (10)['$**'](precision);
      s = $rb_times(self, p);
      if ($rb_lt(precision, 1)) {
        return ($rb_divide(s.$send(method), p)).$to_i()
        } else {
        return self.$Rational(s.$send(method), p)
      };
    }), nil) && 'with_precision';
  })($scope.base, $scope.get('Numeric'));
  return (function($base) {
    var $Kernel, self = $Kernel = $module($base, 'Kernel');

    var def = self.$$proto, $scope = self.$$scope;

    Opal.defn(self, '$Rational', function(numerator, denominator) {
      var self = this;

      if (denominator == null) {
        denominator = 1
      }
      return $scope.get('Rational').$convert(numerator, denominator);
    })
  })($scope.base);
};

/* Generated by Opal 0.9.0 */
Opal.modules["corelib/time"] = function(Opal) {
  Opal.dynamic_require_severity = "error";
  var OPAL_CONFIG = { method_missing: true, arity_check: false, freezing: true, tainting: true };
  function $rb_gt(lhs, rhs) {
    return (typeof(lhs) === 'number' && typeof(rhs) === 'number') ? lhs > rhs : lhs['$>'](rhs);
  }
  function $rb_lt(lhs, rhs) {
    return (typeof(lhs) === 'number' && typeof(rhs) === 'number') ? lhs < rhs : lhs['$<'](rhs);
  }
  function $rb_plus(lhs, rhs) {
    return (typeof(lhs) === 'number' && typeof(rhs) === 'number') ? lhs + rhs : lhs['$+'](rhs);
  }
  function $rb_divide(lhs, rhs) {
    return (typeof(lhs) === 'number' && typeof(rhs) === 'number') ? lhs / rhs : lhs['$/'](rhs);
  }
  function $rb_minus(lhs, rhs) {
    return (typeof(lhs) === 'number' && typeof(rhs) === 'number') ? lhs - rhs : lhs['$-'](rhs);
  }
  function $rb_le(lhs, rhs) {
    return (typeof(lhs) === 'number' && typeof(rhs) === 'number') ? lhs <= rhs : lhs['$<='](rhs);
  }
  var self = Opal.top, $scope = Opal, nil = Opal.nil, $breaker = Opal.breaker, $slice = Opal.slice, $klass = Opal.klass, $range = Opal.range;

  Opal.add_stubs(['$require', '$include', '$===', '$raise', '$coerce_to!', '$respond_to?', '$to_str', '$to_i', '$new', '$<=>', '$to_f', '$nil?', '$>', '$<', '$strftime', '$year', '$month', '$day', '$+', '$round', '$/', '$-', '$copy_instance_variables', '$initialize_dup', '$is_a?', '$zero?', '$wday', '$utc?', '$mon', '$yday', '$hour', '$min', '$sec', '$rjust', '$ljust', '$zone', '$to_s', '$[]', '$cweek_cyear', '$isdst', '$<=', '$!=', '$==', '$ceil']);
  self.$require("corelib/comparable");
  return (function($base, $super) {
    function $Time(){};
    var self = $Time = $klass($base, $super, 'Time', $Time);

    var def = self.$$proto, $scope = self.$$scope;

    self.$include($scope.get('Comparable'));

    
    var days_of_week = ["Sunday", "Monday", "Tuesday", "Wednesday", "Thursday", "Friday", "Saturday", "Sunday"],
        short_days   = ["Sun", "Mon", "Tue", "Wed", "Thu", "Fri", "Sat"],
        short_months = ["Jan", "Feb", "Mar", "Apr", "May", "Jun", "Jul", "Aug", "Sep", "Oct", "Nov", "Dec"],
        long_months  = ["January", "February", "March", "April", "May", "June", "July", "August", "September", "October", "November", "December"];
  ;

    Opal.defs(self, '$at', function(seconds, frac) {
      var self = this;

      
      var result;

      if ($scope.get('Time')['$==='](seconds)) {
        if (frac !== undefined) {
          self.$raise($scope.get('TypeError'), "can't convert Time into an exact number")
        }
        result = new Date(seconds.getTime());
        result.is_utc = seconds.is_utc;
        return result;
      }

      if (!seconds.$$is_number) {
        seconds = $scope.get('Opal')['$coerce_to!'](seconds, $scope.get('Integer'), "to_int");
      }

      if (frac === undefined) {
        return new Date(seconds * 1000);
      }

      if (!frac.$$is_number) {
        frac = $scope.get('Opal')['$coerce_to!'](frac, $scope.get('Integer'), "to_int");
      }

      return new Date(seconds * 1000 + (frac / 1000));
    ;
    });

    
    function time_params(year, month, day, hour, min, sec) {
      if (year.$$is_string) {
        year = parseInt(year, 10);
      } else {
        year = $scope.get('Opal')['$coerce_to!'](year, $scope.get('Integer'), "to_int");
      }

      if (month === nil) {
        month = 1;
      } else if (!month.$$is_number) {
        if ((month)['$respond_to?']("to_str")) {
          month = (month).$to_str();
          switch (month.toLowerCase()) {
          case 'jan': month =  1; break;
          case 'feb': month =  2; break;
          case 'mar': month =  3; break;
          case 'apr': month =  4; break;
          case 'may': month =  5; break;
          case 'jun': month =  6; break;
          case 'jul': month =  7; break;
          case 'aug': month =  8; break;
          case 'sep': month =  9; break;
          case 'oct': month = 10; break;
          case 'nov': month = 11; break;
          case 'dec': month = 12; break;
          default: month = (month).$to_i();
          }
        } else {
          month = $scope.get('Opal')['$coerce_to!'](month, $scope.get('Integer'), "to_int");
        }
      }

      if (month < 1 || month > 12) {
        self.$raise($scope.get('ArgumentError'), "month out of range: " + (month))
      }
      month = month - 1;

      if (day === nil) {
        day = 1;
      } else if (day.$$is_string) {
        day = parseInt(day, 10);
      } else {
        day = $scope.get('Opal')['$coerce_to!'](day, $scope.get('Integer'), "to_int");
      }

      if (day < 1 || day > 31) {
        self.$raise($scope.get('ArgumentError'), "day out of range: " + (day))
      }

      if (hour === nil) {
        hour = 0;
      } else if (hour.$$is_string) {
        hour = parseInt(hour, 10);
      } else {
        hour = $scope.get('Opal')['$coerce_to!'](hour, $scope.get('Integer'), "to_int");
      }

      if (hour < 0 || hour > 24) {
        self.$raise($scope.get('ArgumentError'), "hour out of range: " + (hour))
      }

      if (min === nil) {
        min = 0;
      } else if (min.$$is_string) {
        min = parseInt(min, 10);
      } else {
        min = $scope.get('Opal')['$coerce_to!'](min, $scope.get('Integer'), "to_int");
      }

      if (min < 0 || min > 59) {
        self.$raise($scope.get('ArgumentError'), "min out of range: " + (min))
      }

      if (sec === nil) {
        sec = 0;
      } else if (!sec.$$is_number) {
        if (sec.$$is_string) {
          sec = parseInt(sec, 10);
        } else {
          sec = $scope.get('Opal')['$coerce_to!'](sec, $scope.get('Integer'), "to_int");
        }
      }

      if (sec < 0 || sec > 60) {
        self.$raise($scope.get('ArgumentError'), "sec out of range: " + (sec))
      }

      return [year, month, day, hour, min, sec];
    }
  ;

    Opal.defs(self, '$new', function(year, month, day, hour, min, sec, utc_offset) {
      var self = this;

      if (month == null) {
        month = nil
      }
      if (day == null) {
        day = nil
      }
      if (hour == null) {
        hour = nil
      }
      if (min == null) {
        min = nil
      }
      if (sec == null) {
        sec = nil
      }
      if (utc_offset == null) {
        utc_offset = nil
      }
      
      var args, result;

      if (year === undefined) {
        return new Date();
      }

      if (utc_offset !== nil) {
        self.$raise($scope.get('ArgumentError'), "Opal does not support explicitly specifying UTC offset for Time")
      }

      args  = time_params(year, month, day, hour, min, sec);
      year  = args[0];
      month = args[1];
      day   = args[2];
      hour  = args[3];
      min   = args[4];
      sec   = args[5];

      result = new Date(year, month, day, hour, min, 0, sec * 1000);
      if (year < 100) {
        result.setFullYear(year);
      }
      return result;
    
    });

    Opal.defs(self, '$local', function(year, month, day, hour, min, sec, millisecond, _dummy1, _dummy2, _dummy3) {
      var self = this;

      if (month == null) {
        month = nil
      }
      if (day == null) {
        day = nil
      }
      if (hour == null) {
        hour = nil
      }
      if (min == null) {
        min = nil
      }
      if (sec == null) {
        sec = nil
      }
      if (millisecond == null) {
        millisecond = nil
      }
      if (_dummy1 == null) {
        _dummy1 = nil
      }
      if (_dummy2 == null) {
        _dummy2 = nil
      }
      if (_dummy3 == null) {
        _dummy3 = nil
      }
      
      var args, result;

      if (arguments.length === 10) {
        args  = $slice.call(arguments);
        year  = args[5];
        month = args[4];
        day   = args[3];
        hour  = args[2];
        min   = args[1];
        sec   = args[0];
      }

      args  = time_params(year, month, day, hour, min, sec);
      year  = args[0];
      month = args[1];
      day   = args[2];
      hour  = args[3];
      min   = args[4];
      sec   = args[5];

      result = new Date(year, month, day, hour, min, 0, sec * 1000);
      if (year < 100) {
        result.setFullYear(year);
      }
      return result;
    
    });

    Opal.defs(self, '$gm', function(year, month, day, hour, min, sec, millisecond, _dummy1, _dummy2, _dummy3) {
      var self = this;

      if (month == null) {
        month = nil
      }
      if (day == null) {
        day = nil
      }
      if (hour == null) {
        hour = nil
      }
      if (min == null) {
        min = nil
      }
      if (sec == null) {
        sec = nil
      }
      if (millisecond == null) {
        millisecond = nil
      }
      if (_dummy1 == null) {
        _dummy1 = nil
      }
      if (_dummy2 == null) {
        _dummy2 = nil
      }
      if (_dummy3 == null) {
        _dummy3 = nil
      }
      
      var args, result;

      if (arguments.length === 10) {
        args  = $slice.call(arguments);
        year  = args[5];
        month = args[4];
        day   = args[3];
        hour  = args[2];
        min   = args[1];
        sec   = args[0];
      }

      args  = time_params(year, month, day, hour, min, sec);
      year  = args[0];
      month = args[1];
      day   = args[2];
      hour  = args[3];
      min   = args[4];
      sec   = args[5];

      result = new Date(Date.UTC(year, month, day, hour, min, 0, sec * 1000));
      if (year < 100) {
        result.setUTCFullYear(year);
      }
      result.is_utc = true;
      return result;
    
    });

    (function(self) {
      var $scope = self.$$scope, def = self.$$proto;

      Opal.alias(self, 'mktime', 'local');
      return Opal.alias(self, 'utc', 'gm');
    })(Opal.get_singleton_class(self));

    Opal.defs(self, '$now', function() {
      var self = this;

      return self.$new();
    });

    Opal.defn(self, '$+', function(other) {
      var $a, self = this;

      if ((($a = $scope.get('Time')['$==='](other)) !== nil && (!$a.$$is_boolean || $a == true))) {
        self.$raise($scope.get('TypeError'), "time + time?")};
      
      if (!other.$$is_number) {
        other = $scope.get('Opal')['$coerce_to!'](other, $scope.get('Integer'), "to_int");
      }
      var result = new Date(self.getTime() + (other * 1000));
      result.is_utc = self.is_utc;
      return result;
    ;
    });

    Opal.defn(self, '$-', function(other) {
      var $a, self = this;

      if ((($a = $scope.get('Time')['$==='](other)) !== nil && (!$a.$$is_boolean || $a == true))) {
        return (self.getTime() - other.getTime()) / 1000};
      
      if (!other.$$is_number) {
        other = $scope.get('Opal')['$coerce_to!'](other, $scope.get('Integer'), "to_int");
      }
      var result = new Date(self.getTime() - (other * 1000));
      result.is_utc = self.is_utc;
      return result;
    ;
    });

    Opal.defn(self, '$<=>', function(other) {
      var $a, self = this, r = nil;

      if ((($a = $scope.get('Time')['$==='](other)) !== nil && (!$a.$$is_boolean || $a == true))) {
        return self.$to_f()['$<=>'](other.$to_f())
        } else {
        r = other['$<=>'](self);
        if ((($a = r['$nil?']()) !== nil && (!$a.$$is_boolean || $a == true))) {
          return nil
        } else if ($rb_gt(r, 0)) {
          return -1
        } else if ($rb_lt(r, 0)) {
          return 1
          } else {
          return 0
        };
      };
    });

    Opal.defn(self, '$==', function(other) {
      var self = this;

      return self.$to_f() === other.$to_f();
    });

    Opal.defn(self, '$asctime', function() {
      var self = this;

      return self.$strftime("%a %b %e %H:%M:%S %Y");
    });

    Opal.alias(self, 'ctime', 'asctime');

    Opal.defn(self, '$day', function() {
      var self = this;

      return self.is_utc ? self.getUTCDate() : self.getDate();
    });

    Opal.defn(self, '$yday', function() {
      var self = this, start_of_year = nil, start_of_day = nil, one_day = nil;

      start_of_year = $scope.get('Time').$new(self.$year()).$to_i();
      start_of_day = $scope.get('Time').$new(self.$year(), self.$month(), self.$day()).$to_i();
      one_day = 86400;
      return $rb_plus(($rb_divide(($rb_minus(start_of_day, start_of_year)), one_day)).$round(), 1);
    });

    Opal.defn(self, '$isdst', function() {
      var self = this;

      
      var jan = new Date(self.getFullYear(), 0, 1),
          jul = new Date(self.getFullYear(), 6, 1);
      return self.getTimezoneOffset() < Math.max(jan.getTimezoneOffset(), jul.getTimezoneOffset());
    
    });

    Opal.alias(self, 'dst?', 'isdst');

    Opal.defn(self, '$dup', function() {
      var self = this, copy = nil;

      copy = new Date(self.getTime());
      copy.$copy_instance_variables(self);
      copy.$initialize_dup(self);
      return copy;
    });

    Opal.defn(self, '$eql?', function(other) {
      var $a, self = this;

      return ($a = other['$is_a?']($scope.get('Time')), $a !== false && $a !== nil ?(self['$<=>'](other))['$zero?']() : $a);
    });

    Opal.defn(self, '$friday?', function() {
      var self = this;

      return self.$wday() == 5;
    });

    Opal.defn(self, '$hash', function() {
      var self = this;

      return 'Time:' + self.getTime();
    });

    Opal.defn(self, '$hour', function() {
      var self = this;

      return self.is_utc ? self.getUTCHours() : self.getHours();
    });

    Opal.defn(self, '$inspect', function() {
      var $a, self = this;

      if ((($a = self['$utc?']()) !== nil && (!$a.$$is_boolean || $a == true))) {
        return self.$strftime("%Y-%m-%d %H:%M:%S UTC")
        } else {
        return self.$strftime("%Y-%m-%d %H:%M:%S %z")
      };
    });

    Opal.alias(self, 'mday', 'day');

    Opal.defn(self, '$min', function() {
      var self = this;

      return self.is_utc ? self.getUTCMinutes() : self.getMinutes();
    });

    Opal.defn(self, '$mon', function() {
      var self = this;

      return (self.is_utc ? self.getUTCMonth() : self.getMonth()) + 1;
    });

    Opal.defn(self, '$monday?', function() {
      var self = this;

      return self.$wday() == 1;
    });

    Opal.alias(self, 'month', 'mon');

    Opal.defn(self, '$saturday?', function() {
      var self = this;

      return self.$wday() == 6;
    });

    Opal.defn(self, '$sec', function() {
      var self = this;

      return self.is_utc ? self.getUTCSeconds() : self.getSeconds();
    });

    Opal.defn(self, '$succ', function() {
      var self = this;

      
      var result = new Date(self.getTime() + 1000);
      result.is_utc = self.is_utc;
      return result;
    
    });

    Opal.defn(self, '$usec', function() {
      var self = this;

      return self.getMilliseconds() * 1000;
    });

    Opal.defn(self, '$zone', function() {
      var self = this;

      
      var string = self.toString(),
          result;

      if (string.indexOf('(') == -1) {
        result = string.match(/[A-Z]{3,4}/)[0];
      }
      else {
        result = string.match(/\([^)]+\)/)[0].match(/[A-Z]/g).join('');
      }

      if (result == "GMT" && /(GMT\W*\d{4})/.test(string)) {
        return RegExp.$1;
      }
      else {
        return result;
      }
    
    });

    Opal.defn(self, '$getgm', function() {
      var self = this;

      
      var result = new Date(self.getTime());
      result.is_utc = true;
      return result;
    
    });

    Opal.alias(self, 'getutc', 'getgm');

    Opal.defn(self, '$gmtime', function() {
      var self = this;

      
      self.is_utc = true;
      return self;
    
    });

    Opal.alias(self, 'utc', 'gmtime');

    Opal.defn(self, '$gmt?', function() {
      var self = this;

      return self.is_utc === true;
    });

    Opal.defn(self, '$gmt_offset', function() {
      var self = this;

      return -self.getTimezoneOffset() * 60;
    });

    Opal.defn(self, '$strftime', function(format) {
      var self = this;

      
      return format.replace(/%([\-_#^0]*:{0,2})(\d+)?([EO]*)(.)/g, function(full, flags, width, _, conv) {
        var result = "",
            zero   = flags.indexOf('0') !== -1,
            pad    = flags.indexOf('-') === -1,
            blank  = flags.indexOf('_') !== -1,
            upcase = flags.indexOf('^') !== -1,
            invert = flags.indexOf('#') !== -1,
            colons = (flags.match(':') || []).length;

        width = parseInt(width, 10);

        if (zero && blank) {
          if (flags.indexOf('0') < flags.indexOf('_')) {
            zero = false;
          }
          else {
            blank = false;
          }
        }

        switch (conv) {
          case 'Y':
            result += self.$year();
            break;

          case 'C':
            zero    = !blank;
            result += Math.round(self.$year() / 100);
            break;

          case 'y':
            zero    = !blank;
            result += (self.$year() % 100);
            break;

          case 'm':
            zero    = !blank;
            result += self.$mon();
            break;

          case 'B':
            result += long_months[self.$mon() - 1];
            break;

          case 'b':
          case 'h':
            blank   = !zero;
            result += short_months[self.$mon() - 1];
            break;

          case 'd':
            zero    = !blank
            result += self.$day();
            break;

          case 'e':
            blank   = !zero
            result += self.$day();
            break;

          case 'j':
            result += self.$yday();
            break;

          case 'H':
            zero    = !blank;
            result += self.$hour();
            break;

          case 'k':
            blank   = !zero;
            result += self.$hour();
            break;

          case 'I':
            zero    = !blank;
            result += (self.$hour() % 12 || 12);
            break;

          case 'l':
            blank   = !zero;
            result += (self.$hour() % 12 || 12);
            break;

          case 'P':
            result += (self.$hour() >= 12 ? "pm" : "am");
            break;

          case 'p':
            result += (self.$hour() >= 12 ? "PM" : "AM");
            break;

          case 'M':
            zero    = !blank;
            result += self.$min();
            break;

          case 'S':
            zero    = !blank;
            result += self.$sec()
            break;

          case 'L':
            zero    = !blank;
            width   = isNaN(width) ? 3 : width;
            result += self.getMilliseconds();
            break;

          case 'N':
            width   = isNaN(width) ? 9 : width;
            result += (self.getMilliseconds().toString()).$rjust(3, "0");
            result  = (result).$ljust(width, "0");
            break;

          case 'z':
            var offset  = self.getTimezoneOffset(),
                hours   = Math.floor(Math.abs(offset) / 60),
                minutes = Math.abs(offset) % 60;

            result += offset < 0 ? "+" : "-";
            result += hours < 10 ? "0" : "";
            result += hours;

            if (colons > 0) {
              result += ":";
            }

            result += minutes < 10 ? "0" : "";
            result += minutes;

            if (colons > 1) {
              result += ":00";
            }

            break;

          case 'Z':
            result += self.$zone();
            break;

          case 'A':
            result += days_of_week[self.$wday()];
            break;

          case 'a':
            result += short_days[self.$wday()];
            break;

          case 'u':
            result += (self.$wday() + 1);
            break;

          case 'w':
            result += self.$wday();
            break;

          case 'V':
            result += self.$cweek_cyear()['$[]'](0).$to_s().$rjust(2, "0");
            break;

          case 'G':
            result += self.$cweek_cyear()['$[]'](1);
            break;

          case 'g':
            result += self.$cweek_cyear()['$[]'](1)['$[]']($range(-2, -1, false));
            break;

          case 's':
            result += self.$to_i();
            break;

          case 'n':
            result += "\n";
            break;

          case 't':
            result += "\t";
            break;

          case '%':
            result += "%";
            break;

          case 'c':
            result += self.$strftime("%a %b %e %T %Y");
            break;

          case 'D':
          case 'x':
            result += self.$strftime("%m/%d/%y");
            break;

          case 'F':
            result += self.$strftime("%Y-%m-%d");
            break;

          case 'v':
            result += self.$strftime("%e-%^b-%4Y");
            break;

          case 'r':
            result += self.$strftime("%I:%M:%S %p");
            break;

          case 'R':
            result += self.$strftime("%H:%M");
            break;

          case 'T':
          case 'X':
            result += self.$strftime("%H:%M:%S");
            break;

          default:
            return full;
        }

        if (upcase) {
          result = result.toUpperCase();
        }

        if (invert) {
          result = result.replace(/[A-Z]/, function(c) { c.toLowerCase() }).
                          replace(/[a-z]/, function(c) { c.toUpperCase() });
        }

        if (pad && (zero || blank)) {
          result = (result).$rjust(isNaN(width) ? 2 : width, blank ? " " : "0");
        }

        return result;
      });
    
    });

    Opal.defn(self, '$sunday?', function() {
      var self = this;

      return self.$wday() == 0;
    });

    Opal.defn(self, '$thursday?', function() {
      var self = this;

      return self.$wday() == 4;
    });

    Opal.defn(self, '$to_a', function() {
      var self = this;

      return [self.$sec(), self.$min(), self.$hour(), self.$day(), self.$month(), self.$year(), self.$wday(), self.$yday(), self.$isdst(), self.$zone()];
    });

    Opal.defn(self, '$to_f', function() {
      var self = this;

      return self.getTime() / 1000;
    });

    Opal.defn(self, '$to_i', function() {
      var self = this;

      return parseInt(self.getTime() / 1000, 10);
    });

    Opal.alias(self, 'to_s', 'inspect');

    Opal.defn(self, '$tuesday?', function() {
      var self = this;

      return self.$wday() == 2;
    });

    Opal.alias(self, 'tv_sec', 'sec');

    Opal.alias(self, 'tv_usec', 'usec');

    Opal.alias(self, 'utc?', 'gmt?');

    Opal.alias(self, 'gmtoff', 'gmt_offset');

    Opal.alias(self, 'utc_offset', 'gmt_offset');

    Opal.defn(self, '$wday', function() {
      var self = this;

      return self.is_utc ? self.getUTCDay() : self.getDay();
    });

    Opal.defn(self, '$wednesday?', function() {
      var self = this;

      return self.$wday() == 3;
    });

    Opal.defn(self, '$year', function() {
      var self = this;

      return self.is_utc ? self.getUTCFullYear() : self.getFullYear();
    });

    return (Opal.defn(self, '$cweek_cyear', function() {
      var $a, $b, self = this, jan01 = nil, jan01_wday = nil, first_monday = nil, year = nil, offset = nil, week = nil, dec31 = nil, dec31_wday = nil;

      jan01 = $scope.get('Time').$new(self.$year(), 1, 1);
      jan01_wday = jan01.$wday();
      first_monday = 0;
      year = self.$year();
      if ((($a = (($b = $rb_le(jan01_wday, 4)) ? jan01_wday['$!='](0) : $rb_le(jan01_wday, 4))) !== nil && (!$a.$$is_boolean || $a == true))) {
        offset = $rb_minus(jan01_wday, 1)
        } else {
        offset = $rb_minus($rb_minus(jan01_wday, 7), 1);
        if (offset['$=='](-8)) {
          offset = -1};
      };
      week = ($rb_divide(($rb_plus(self.$yday(), offset)), 7.0)).$ceil();
      if ($rb_le(week, 0)) {
        return $scope.get('Time').$new($rb_minus(self.$year(), 1), 12, 31).$cweek_cyear()
      } else if (week['$=='](53)) {
        dec31 = $scope.get('Time').$new(self.$year(), 12, 31);
        dec31_wday = dec31.$wday();
        if ((($a = (($b = $rb_le(dec31_wday, 3)) ? dec31_wday['$!='](0) : $rb_le(dec31_wday, 3))) !== nil && (!$a.$$is_boolean || $a == true))) {
          week = 1;
          year = $rb_plus(year, 1);};};
      return [week, year];
    }), nil) && 'cweek_cyear';
  })($scope.base, Date);
};

/* Generated by Opal 0.9.0 */
Opal.modules["corelib/struct"] = function(Opal) {
  Opal.dynamic_require_severity = "error";
  var OPAL_CONFIG = { method_missing: true, arity_check: false, freezing: true, tainting: true };
  function $rb_lt(lhs, rhs) {
    return (typeof(lhs) === 'number' && typeof(rhs) === 'number') ? lhs < rhs : lhs['$<'](rhs);
  }
  function $rb_ge(lhs, rhs) {
    return (typeof(lhs) === 'number' && typeof(rhs) === 'number') ? lhs >= rhs : lhs['$>='](rhs);
  }
  function $rb_plus(lhs, rhs) {
    return (typeof(lhs) === 'number' && typeof(rhs) === 'number') ? lhs + rhs : lhs['$+'](rhs);
  }
  var self = Opal.top, $scope = Opal, nil = Opal.nil, $breaker = Opal.breaker, $slice = Opal.slice, $klass = Opal.klass, $hash2 = Opal.hash2;

  Opal.add_stubs(['$require', '$include', '$==', '$[]', '$upcase', '$const_set', '$new', '$unshift', '$each', '$define_struct_attribute', '$class_eval', '$to_proc', '$allocate', '$initialize', '$raise', '$<<', '$members', '$define_method', '$instance_eval', '$each_with_index', '$[]=', '$class', '$hash', '$===', '$<', '$-@', '$size', '$>=', '$coerce_to!', '$include?', '$to_sym', '$instance_of?', '$__id__', '$eql?', '$enum_for', '$length', '$map', '$+', '$join', '$inspect', '$each_pair', '$inject', '$flatten', '$to_a']);
  self.$require("corelib/enumerable");
  return (function($base, $super) {
    function $Struct(){};
    var self = $Struct = $klass($base, $super, 'Struct', $Struct);

    var def = self.$$proto, $scope = self.$$scope, TMP_1, TMP_8, TMP_11;

    self.$include($scope.get('Enumerable'));

    Opal.defs(self, '$new', TMP_1 = function(name) {
      var $a, $b, $c, TMP_2, self = this, $iter = TMP_1.$$p, block = $iter || nil, $splat_index = nil, $zuper = nil, $zuper_index = nil;

      var array_size = arguments.length - 1;
      if(array_size < 0) array_size = 0;
      var args = new Array(array_size);
      for($splat_index = 0; $splat_index < array_size; $splat_index++) {
        args[$splat_index] = arguments[$splat_index + 1];
      }
      TMP_1.$$p = null;
      $zuper = [];
      for($zuper_index = 0; $zuper_index < arguments.length; $zuper_index++) {
        $zuper[$zuper_index] = arguments[$zuper_index];
      }
      if (self['$==']($scope.get('Struct'))) {
        } else {
        return Opal.find_super_dispatcher(self, 'new', TMP_1, $iter, $Struct).apply(self, $zuper)
      };
      if (name['$[]'](0)['$=='](name['$[]'](0).$upcase())) {
        return $scope.get('Struct').$const_set(name, ($a = self).$new.apply($a, Opal.to_a(args)))
        } else {
        args.$unshift(name);
        return ($b = ($c = $scope.get('Class')).$new, $b.$$p = (TMP_2 = function(){var self = TMP_2.$$s || this, $a, $b, TMP_3, $c;

        ($a = ($b = args).$each, $a.$$p = (TMP_3 = function(arg){var self = TMP_3.$$s || this;
if (arg == null) arg = nil;
          return self.$define_struct_attribute(arg)}, TMP_3.$$s = self, TMP_3), $a).call($b);
          if (block !== false && block !== nil) {
            ($a = ($c = self).$class_eval, $a.$$p = block.$to_proc(), $a).call($c)};
          return (function(self) {
            var $scope = self.$$scope, def = self.$$proto;

            Opal.defn(self, '$new', function() {
              var $a, self = this, instance = nil, $splat_index = nil;

              var array_size = arguments.length - 0;
              if(array_size < 0) array_size = 0;
              var args = new Array(array_size);
              for($splat_index = 0; $splat_index < array_size; $splat_index++) {
                args[$splat_index] = arguments[$splat_index + 0];
              }
              instance = self.$allocate();
              instance.$$data = {};;
              ($a = instance).$initialize.apply($a, Opal.to_a(args));
              return instance;
            });
            return Opal.alias(self, '[]', 'new');
          })(Opal.get_singleton_class(self));}, TMP_2.$$s = self, TMP_2), $b).call($c, self);
      };
    });

    Opal.defs(self, '$define_struct_attribute', function(name) {
      var $a, $b, TMP_4, $c, TMP_5, self = this;

      if (self['$==']($scope.get('Struct'))) {
        self.$raise($scope.get('ArgumentError'), "you cannot define attributes to the Struct class")};
      self.$members()['$<<'](name);
      ($a = ($b = self).$define_method, $a.$$p = (TMP_4 = function(){var self = TMP_4.$$s || this;

      return self.$$data[name];}, TMP_4.$$s = self, TMP_4), $a).call($b, name);
      return ($a = ($c = self).$define_method, $a.$$p = (TMP_5 = function(value){var self = TMP_5.$$s || this;
if (value == null) value = nil;
      return self.$$data[name] = value;}, TMP_5.$$s = self, TMP_5), $a).call($c, "" + (name) + "=");
    });

    Opal.defs(self, '$members', function() {
      var $a, self = this;
      if (self.members == null) self.members = nil;

      if (self['$==']($scope.get('Struct'))) {
        self.$raise($scope.get('ArgumentError'), "the Struct class has no members")};
      return ((($a = self.members) !== false && $a !== nil) ? $a : self.members = []);
    });

    Opal.defs(self, '$inherited', function(klass) {
      var $a, $b, TMP_6, self = this, members = nil;
      if (self.members == null) self.members = nil;

      members = self.members;
      return ($a = ($b = klass).$instance_eval, $a.$$p = (TMP_6 = function(){var self = TMP_6.$$s || this;

      return self.members = members}, TMP_6.$$s = self, TMP_6), $a).call($b);
    });

    Opal.defn(self, '$initialize', function() {
      var $a, $b, TMP_7, self = this, $splat_index = nil;

      var array_size = arguments.length - 0;
      if(array_size < 0) array_size = 0;
      var args = new Array(array_size);
      for($splat_index = 0; $splat_index < array_size; $splat_index++) {
        args[$splat_index] = arguments[$splat_index + 0];
      }
      return ($a = ($b = self.$members()).$each_with_index, $a.$$p = (TMP_7 = function(name, index){var self = TMP_7.$$s || this;
if (name == null) name = nil;if (index == null) index = nil;
      return self['$[]='](name, args['$[]'](index))}, TMP_7.$$s = self, TMP_7), $a).call($b);
    });

    Opal.defn(self, '$members', function() {
      var self = this;

      return self.$class().$members();
    });

    Opal.defn(self, '$hash', function() {
      var self = this;

      return $scope.get('Hash').$new(self.$$data).$hash();
    });

    Opal.defn(self, '$[]', function(name) {
      var $a, self = this;

      if ((($a = $scope.get('Integer')['$==='](name)) !== nil && (!$a.$$is_boolean || $a == true))) {
        if ($rb_lt(name, self.$members().$size()['$-@']())) {
          self.$raise($scope.get('IndexError'), "offset " + (name) + " too small for struct(size:" + (self.$members().$size()) + ")")};
        if ($rb_ge(name, self.$members().$size())) {
          self.$raise($scope.get('IndexError'), "offset " + (name) + " too large for struct(size:" + (self.$members().$size()) + ")")};
        name = self.$members()['$[]'](name);
      } else if ((($a = $scope.get('String')['$==='](name)) !== nil && (!$a.$$is_boolean || $a == true))) {
        
        if(!self.$$data.hasOwnProperty(name)) {
          self.$raise($scope.get('NameError').$new("no member '" + (name) + "' in struct", name))
        }
      ;
        } else {
        self.$raise($scope.get('TypeError'), "no implicit conversion of " + (name.$class()) + " into Integer")
      };
      name = $scope.get('Opal')['$coerce_to!'](name, $scope.get('String'), "to_str");
      return self.$$data[name];
    });

    Opal.defn(self, '$[]=', function(name, value) {
      var $a, self = this;

      if ((($a = $scope.get('Integer')['$==='](name)) !== nil && (!$a.$$is_boolean || $a == true))) {
        if ($rb_lt(name, self.$members().$size()['$-@']())) {
          self.$raise($scope.get('IndexError'), "offset " + (name) + " too small for struct(size:" + (self.$members().$size()) + ")")};
        if ($rb_ge(name, self.$members().$size())) {
          self.$raise($scope.get('IndexError'), "offset " + (name) + " too large for struct(size:" + (self.$members().$size()) + ")")};
        name = self.$members()['$[]'](name);
      } else if ((($a = $scope.get('String')['$==='](name)) !== nil && (!$a.$$is_boolean || $a == true))) {
        if ((($a = self.$members()['$include?'](name.$to_sym())) !== nil && (!$a.$$is_boolean || $a == true))) {
          } else {
          self.$raise($scope.get('NameError').$new("no member '" + (name) + "' in struct", name))
        }
        } else {
        self.$raise($scope.get('TypeError'), "no implicit conversion of " + (name.$class()) + " into Integer")
      };
      name = $scope.get('Opal')['$coerce_to!'](name, $scope.get('String'), "to_str");
      return self.$$data[name] = value;
    });

    Opal.defn(self, '$==', function(other) {
      var $a, self = this;

      if ((($a = other['$instance_of?'](self.$class())) !== nil && (!$a.$$is_boolean || $a == true))) {
        } else {
        return false
      };
      
      var recursed1 = {}, recursed2 = {};

      function _eqeq(struct, other) {
        var key, a, b;

        recursed1[(struct).$__id__()] = true;
        recursed2[(other).$__id__()] = true;

        for (key in struct.$$data) {
          a = struct.$$data[key];
          b = other.$$data[key];

          if ($scope.get('Struct')['$==='](a)) {
            if (!recursed1.hasOwnProperty((a).$__id__()) || !recursed2.hasOwnProperty((b).$__id__())) {
              if (!_eqeq(a, b)) {
                return false;
              }
            }
          } else {
            if (!(a)['$=='](b)) {
              return false;
            }
          }
        }

        return true;
      }

      return _eqeq(self, other);
    ;
    });

    Opal.defn(self, '$eql?', function(other) {
      var $a, self = this;

      if ((($a = other['$instance_of?'](self.$class())) !== nil && (!$a.$$is_boolean || $a == true))) {
        } else {
        return false
      };
      
      var recursed1 = {}, recursed2 = {};

      function _eqeq(struct, other) {
        var key, a, b;

        recursed1[(struct).$__id__()] = true;
        recursed2[(other).$__id__()] = true;

        for (key in struct.$$data) {
          a = struct.$$data[key];
          b = other.$$data[key];

          if ($scope.get('Struct')['$==='](a)) {
            if (!recursed1.hasOwnProperty((a).$__id__()) || !recursed2.hasOwnProperty((b).$__id__())) {
              if (!_eqeq(a, b)) {
                return false;
              }
            }
          } else {
            if (!(a)['$eql?'](b)) {
              return false;
            }
          }
        }

        return true;
      }

      return _eqeq(self, other);
    ;
    });

    Opal.defn(self, '$each', TMP_8 = function() {
      var $a, $b, TMP_9, $c, TMP_10, self = this, $iter = TMP_8.$$p, $yield = $iter || nil;

      TMP_8.$$p = null;
      if (($yield !== nil)) {
        } else {
        return ($a = ($b = self).$enum_for, $a.$$p = (TMP_9 = function(){var self = TMP_9.$$s || this;

        return self.$size()}, TMP_9.$$s = self, TMP_9), $a).call($b, "each")
      };
      ($a = ($c = self.$members()).$each, $a.$$p = (TMP_10 = function(name){var self = TMP_10.$$s || this, $a;
if (name == null) name = nil;
      return $a = Opal.yield1($yield, self['$[]'](name)), $a === $breaker ? $a : $a}, TMP_10.$$s = self, TMP_10), $a).call($c);
      return self;
    });

    Opal.defn(self, '$each_pair', TMP_11 = function() {
      var $a, $b, TMP_12, $c, TMP_13, self = this, $iter = TMP_11.$$p, $yield = $iter || nil;

      TMP_11.$$p = null;
      if (($yield !== nil)) {
        } else {
        return ($a = ($b = self).$enum_for, $a.$$p = (TMP_12 = function(){var self = TMP_12.$$s || this;

        return self.$size()}, TMP_12.$$s = self, TMP_12), $a).call($b, "each_pair")
      };
      ($a = ($c = self.$members()).$each, $a.$$p = (TMP_13 = function(name){var self = TMP_13.$$s || this, $a;
if (name == null) name = nil;
      return $a = Opal.yield1($yield, [name, self['$[]'](name)]), $a === $breaker ? $a : $a}, TMP_13.$$s = self, TMP_13), $a).call($c);
      return self;
    });

    Opal.defn(self, '$length', function() {
      var self = this;

      return self.$members().$length();
    });

    Opal.alias(self, 'size', 'length');

    Opal.defn(self, '$to_a', function() {
      var $a, $b, TMP_14, self = this;

      return ($a = ($b = self.$members()).$map, $a.$$p = (TMP_14 = function(name){var self = TMP_14.$$s || this;
if (name == null) name = nil;
      return self['$[]'](name)}, TMP_14.$$s = self, TMP_14), $a).call($b);
    });

    Opal.alias(self, 'values', 'to_a');

    Opal.defn(self, '$inspect', function() {
      var $a, $b, TMP_15, self = this, result = nil;

      result = "#<struct ";
      if (self.$class()['$==']($scope.get('Struct'))) {
        result = $rb_plus(result, "" + (self.$class()) + " ")};
      result = $rb_plus(result, ($a = ($b = self.$each_pair()).$map, $a.$$p = (TMP_15 = function(name, value){var self = TMP_15.$$s || this;
if (name == null) name = nil;if (value == null) value = nil;
      return "" + (name) + "=" + (value.$inspect())}, TMP_15.$$s = self, TMP_15), $a).call($b).$join(", "));
      result = $rb_plus(result, ">");
      return result;
    });

    Opal.alias(self, 'to_s', 'inspect');

    Opal.defn(self, '$to_h', function() {
      var $a, $b, TMP_16, self = this;

      return ($a = ($b = self.$members()).$inject, $a.$$p = (TMP_16 = function(h, name){var self = TMP_16.$$s || this;
if (h == null) h = nil;if (name == null) name = nil;
      h['$[]='](name, self['$[]'](name));
        return h;}, TMP_16.$$s = self, TMP_16), $a).call($b, $hash2([], {}));
    });

    return (Opal.defn(self, '$values_at', function() {
      var $a, $b, TMP_17, self = this, $splat_index = nil;

      var array_size = arguments.length - 0;
      if(array_size < 0) array_size = 0;
      var args = new Array(array_size);
      for($splat_index = 0; $splat_index < array_size; $splat_index++) {
        args[$splat_index] = arguments[$splat_index + 0];
      }
      args = ($a = ($b = args).$map, $a.$$p = (TMP_17 = function(arg){var self = TMP_17.$$s || this;
if (arg == null) arg = nil;
      return arg.$$is_range ? arg.$to_a() : arg;}, TMP_17.$$s = self, TMP_17), $a).call($b).$flatten();
      
      var result = [];
      for (var i = 0, len = args.length; i < len; i++) {
        if (!args[i].$$is_number) {
          self.$raise($scope.get('TypeError'), "no implicit conversion of " + ((args[i]).$class()) + " into Integer")
        }
        result.push(self['$[]'](args[i]));
      }
      return result;
    ;
    }), nil) && 'values_at';
  })($scope.base, null);
};

/* Generated by Opal 0.9.0 */
Opal.modules["corelib/io"] = function(Opal) {
  Opal.dynamic_require_severity = "error";
  var OPAL_CONFIG = { method_missing: true, arity_check: false, freezing: true, tainting: true };
  var $a, $b, self = Opal.top, $scope = Opal, nil = Opal.nil, $breaker = Opal.breaker, $slice = Opal.slice, $klass = Opal.klass, $module = Opal.module, $gvars = Opal.gvars;

  Opal.add_stubs(['$attr_accessor', '$size', '$write', '$join', '$map', '$String', '$empty?', '$concat', '$chomp', '$getbyte', '$getc', '$raise', '$new', '$write_proc=', '$extend']);
  (function($base, $super) {
    function $IO(){};
    var self = $IO = $klass($base, $super, 'IO', $IO);

    var def = self.$$proto, $scope = self.$$scope;

    def.tty = def.closed = nil;
    Opal.cdecl($scope, 'SEEK_SET', 0);

    Opal.cdecl($scope, 'SEEK_CUR', 1);

    Opal.cdecl($scope, 'SEEK_END', 2);

    Opal.defn(self, '$tty?', function() {
      var self = this;

      return self.tty;
    });

    Opal.defn(self, '$closed?', function() {
      var self = this;

      return self.closed;
    });

    self.$attr_accessor("write_proc");

    Opal.defn(self, '$write', function(string) {
      var self = this;

      self.write_proc(string);
      return string.$size();
    });

    self.$attr_accessor("sync", "tty");

    Opal.defn(self, '$flush', function() {
      var self = this;

      return nil;
    });

    (function($base) {
      var $Writable, self = $Writable = $module($base, 'Writable');

      var def = self.$$proto, $scope = self.$$scope;

      Opal.defn(self, '$<<', function(string) {
        var self = this;

        self.$write(string);
        return self;
      });

      Opal.defn(self, '$print', function() {
        var $a, $b, TMP_1, self = this, $splat_index = nil;
        if ($gvars[","] == null) $gvars[","] = nil;

        var array_size = arguments.length - 0;
        if(array_size < 0) array_size = 0;
        var args = new Array(array_size);
        for($splat_index = 0; $splat_index < array_size; $splat_index++) {
          args[$splat_index] = arguments[$splat_index + 0];
        }
        self.$write(($a = ($b = args).$map, $a.$$p = (TMP_1 = function(arg){var self = TMP_1.$$s || this;
if (arg == null) arg = nil;
        return self.$String(arg)}, TMP_1.$$s = self, TMP_1), $a).call($b).$join($gvars[","]));
        return nil;
      });

      Opal.defn(self, '$puts', function() {
        var $a, $b, TMP_2, self = this, newline = nil, $splat_index = nil;
        if ($gvars["/"] == null) $gvars["/"] = nil;

        var array_size = arguments.length - 0;
        if(array_size < 0) array_size = 0;
        var args = new Array(array_size);
        for($splat_index = 0; $splat_index < array_size; $splat_index++) {
          args[$splat_index] = arguments[$splat_index + 0];
        }
        newline = $gvars["/"];
        if ((($a = args['$empty?']()) !== nil && (!$a.$$is_boolean || $a == true))) {
          self.$write($gvars["/"])
          } else {
          self.$write(($a = ($b = args).$map, $a.$$p = (TMP_2 = function(arg){var self = TMP_2.$$s || this;
if (arg == null) arg = nil;
          return self.$String(arg).$chomp()}, TMP_2.$$s = self, TMP_2), $a).call($b).$concat([nil]).$join(newline))
        };
        return nil;
      });
    })($scope.base);

    return (function($base) {
      var $Readable, self = $Readable = $module($base, 'Readable');

      var def = self.$$proto, $scope = self.$$scope;

      Opal.defn(self, '$readbyte', function() {
        var self = this;

        return self.$getbyte();
      });

      Opal.defn(self, '$readchar', function() {
        var self = this;

        return self.$getc();
      });

      Opal.defn(self, '$readline', function(sep) {
        var self = this;
        if ($gvars["/"] == null) $gvars["/"] = nil;

        if (sep == null) {
          sep = $gvars["/"]
        }
        return self.$raise($scope.get('NotImplementedError'));
      });

      Opal.defn(self, '$readpartial', function(integer, outbuf) {
        var self = this;

        if (outbuf == null) {
          outbuf = nil
        }
        return self.$raise($scope.get('NotImplementedError'));
      });
    })($scope.base);
  })($scope.base, null);
  Opal.cdecl($scope, 'STDERR', $gvars.stderr = $scope.get('IO').$new());
  Opal.cdecl($scope, 'STDIN', $gvars.stdin = $scope.get('IO').$new());
  Opal.cdecl($scope, 'STDOUT', $gvars.stdout = $scope.get('IO').$new());
  (($a = [typeof(process) === 'object' ? function(s){process.stdout.write(s)} : function(s){console.log(s)}]), $b = $scope.get('STDOUT'), $b['$write_proc='].apply($b, $a), $a[$a.length-1]);
  (($a = [typeof(process) === 'object' ? function(s){process.stderr.write(s)} : function(s){console.warn(s)}]), $b = $scope.get('STDERR'), $b['$write_proc='].apply($b, $a), $a[$a.length-1]);
  $scope.get('STDOUT').$extend((($scope.get('IO')).$$scope.get('Writable')));
  return $scope.get('STDERR').$extend((($scope.get('IO')).$$scope.get('Writable')));
};

/* Generated by Opal 0.9.0 */
Opal.modules["corelib/main"] = function(Opal) {
  Opal.dynamic_require_severity = "error";
  var OPAL_CONFIG = { method_missing: true, arity_check: false, freezing: true, tainting: true };
  var self = Opal.top, $scope = Opal, nil = Opal.nil, $breaker = Opal.breaker, $slice = Opal.slice;

  Opal.add_stubs(['$include']);
  Opal.defs(self, '$to_s', function() {
    var self = this;

    return "main";
  });
  return (Opal.defs(self, '$include', function(mod) {
    var self = this;

    return $scope.get('Object').$include(mod);
  }), nil) && 'include';
};

/* Generated by Opal 0.9.0 */
Opal.modules["corelib/dir"] = function(Opal) {
  Opal.dynamic_require_severity = "error";
  var OPAL_CONFIG = { method_missing: true, arity_check: false, freezing: true, tainting: true };
  var self = Opal.top, $scope = Opal, nil = Opal.nil, $breaker = Opal.breaker, $slice = Opal.slice, $klass = Opal.klass;

  Opal.add_stubs(['$[]']);
  return (function($base, $super) {
    function $Dir(){};
    var self = $Dir = $klass($base, $super, 'Dir', $Dir);

    var def = self.$$proto, $scope = self.$$scope;

    return (function(self) {
      var $scope = self.$$scope, def = self.$$proto, TMP_1;

      Opal.defn(self, '$chdir', TMP_1 = function(dir) {
        var $a, self = this, $iter = TMP_1.$$p, $yield = $iter || nil, prev_cwd = nil;

        TMP_1.$$p = null;
        try {
        prev_cwd = Opal.current_dir;
        Opal.current_dir = dir;
        return $a = Opal.yieldX($yield, []), $a === $breaker ? $a : $a;
        } finally {
        Opal.current_dir = prev_cwd;
        };
      });
      Opal.defn(self, '$pwd', function() {
        var self = this;

        return Opal.current_dir || '.';
      });
      Opal.alias(self, 'getwd', 'pwd');
      return (Opal.defn(self, '$home', function() {
        var $a, self = this;

        return ((($a = $scope.get('ENV')['$[]']("HOME")) !== false && $a !== nil) ? $a : ".");
      }), nil) && 'home';
    })(Opal.get_singleton_class(self))
  })($scope.base, null)
};

/* Generated by Opal 0.9.0 */
Opal.modules["corelib/file"] = function(Opal) {
  Opal.dynamic_require_severity = "error";
  var OPAL_CONFIG = { method_missing: true, arity_check: false, freezing: true, tainting: true };
  var self = Opal.top, $scope = Opal, nil = Opal.nil, $breaker = Opal.breaker, $slice = Opal.slice, $klass = Opal.klass, $range = Opal.range;

  Opal.add_stubs(['$join', '$compact', '$split', '$==', '$first', '$[]=', '$home', '$each', '$pop', '$<<', '$[]', '$gsub', '$find', '$=~']);
  return (function($base, $super) {
    function $File(){};
    var self = $File = $klass($base, $super, 'File', $File);

    var def = self.$$proto, $scope = self.$$scope;

    Opal.cdecl($scope, 'Separator', Opal.cdecl($scope, 'SEPARATOR', "/"));

    Opal.cdecl($scope, 'ALT_SEPARATOR', nil);

    Opal.cdecl($scope, 'PATH_SEPARATOR', ":");

    return (function(self) {
      var $scope = self.$$scope, def = self.$$proto;

      Opal.defn(self, '$expand_path', function(path, basedir) {
        var $a, $b, TMP_1, self = this, parts = nil, new_parts = nil;

        if (basedir == null) {
          basedir = nil
        }
        path = [basedir, path].$compact().$join($scope.get('SEPARATOR'));
        parts = path.$split($scope.get('SEPARATOR'));
        new_parts = [];
        if (parts.$first()['$==']("~")) {
          parts['$[]='](0, $scope.get('Dir').$home())};
        ($a = ($b = parts).$each, $a.$$p = (TMP_1 = function(part){var self = TMP_1.$$s || this;
if (part == null) part = nil;
        if (part['$==']("..")) {
            return new_parts.$pop()
            } else {
            return new_parts['$<<'](part)
          }}, TMP_1.$$s = self, TMP_1), $a).call($b);
        return new_parts.$join($scope.get('SEPARATOR'));
      });
      Opal.alias(self, 'realpath', 'expand_path');
      Opal.defn(self, '$dirname', function(path) {
        var self = this;

        return self.$split(path)['$[]']($range(0, -2, false));
      });
      Opal.defn(self, '$basename', function(path) {
        var self = this;

        return self.$split(path)['$[]'](-1);
      });
      Opal.defn(self, '$exist?', function(path) {
        var self = this;

        return Opal.modules[path] != null;
      });
      Opal.alias(self, 'exists?', 'exist?');
      Opal.defn(self, '$directory?', function(path) {
        var $a, $b, TMP_2, self = this, files = nil, file = nil;

        files = [];
        
        for (var key in Opal.modules) {
          files.push(key)
        }
      ;
        path = path.$gsub((new RegExp("(^." + $scope.get('SEPARATOR') + "+|" + $scope.get('SEPARATOR') + "+$)")));
        file = ($a = ($b = files).$find, $a.$$p = (TMP_2 = function(file){var self = TMP_2.$$s || this;
if (file == null) file = nil;
        return file['$=~']((new RegExp("^" + path)))}, TMP_2.$$s = self, TMP_2), $a).call($b);
        return file;
      });
      Opal.defn(self, '$join', function() {
        var self = this, $splat_index = nil;

        var array_size = arguments.length - 0;
        if(array_size < 0) array_size = 0;
        var paths = new Array(array_size);
        for($splat_index = 0; $splat_index < array_size; $splat_index++) {
          paths[$splat_index] = arguments[$splat_index + 0];
        }
        return paths.$join($scope.get('SEPARATOR')).$gsub((new RegExp("" + $scope.get('SEPARATOR') + "+")), $scope.get('SEPARATOR'));
      });
      return (Opal.defn(self, '$split', function(path) {
        var self = this;

        return path.$split($scope.get('SEPARATOR'));
      }), nil) && 'split';
    })(Opal.get_singleton_class(self));
  })($scope.base, $scope.get('IO'))
};

/* Generated by Opal 0.9.0 */
Opal.modules["corelib/process"] = function(Opal) {
  Opal.dynamic_require_severity = "error";
  var OPAL_CONFIG = { method_missing: true, arity_check: false, freezing: true, tainting: true };
  var self = Opal.top, $scope = Opal, nil = Opal.nil, $breaker = Opal.breaker, $slice = Opal.slice, $klass = Opal.klass;

  Opal.add_stubs(['$to_f', '$now', '$new']);
  (function($base, $super) {
    function $Process(){};
    var self = $Process = $klass($base, $super, 'Process', $Process);

    var def = self.$$proto, $scope = self.$$scope;

    Opal.cdecl($scope, 'CLOCK_REALTIME', 0);

    Opal.cdecl($scope, 'CLOCK_MONOTONIC', 1);

    Opal.defs(self, '$pid', function() {
      var self = this;

      return 0;
    });

    Opal.defs(self, '$times', function() {
      var self = this, t = nil;

      t = $scope.get('Time').$now().$to_f();
      return (($scope.get('Benchmark')).$$scope.get('Tms')).$new(t, t, t, t, t);
    });

    return (Opal.defs(self, '$clock_gettime', function(clock_id, unit) {
      var self = this;

      if (unit == null) {
        unit = nil
      }
      return $scope.get('Time').$now().$to_f();
    }), nil) && 'clock_gettime';
  })($scope.base, null);
  (function($base, $super) {
    function $Signal(){};
    var self = $Signal = $klass($base, $super, 'Signal', $Signal);

    var def = self.$$proto, $scope = self.$$scope;

    return (Opal.defs(self, '$trap', function() {
      var self = this;

      return nil;
    }), nil) && 'trap'
  })($scope.base, null);
  return (function($base, $super) {
    function $GC(){};
    var self = $GC = $klass($base, $super, 'GC', $GC);

    var def = self.$$proto, $scope = self.$$scope;

    return (Opal.defs(self, '$start', function() {
      var self = this;

      return nil;
    }), nil) && 'start'
  })($scope.base, null);
};

/* Generated by Opal 0.9.0 */
Opal.modules["corelib/unsupported"] = function(Opal) {
  Opal.dynamic_require_severity = "error";
  var OPAL_CONFIG = { method_missing: true, arity_check: false, freezing: true, tainting: true };
  var self = Opal.top, $scope = Opal, nil = Opal.nil, $breaker = Opal.breaker, $slice = Opal.slice, $klass = Opal.klass, $module = Opal.module;

  Opal.add_stubs(['$warn', '$raise', '$%', '$module_function']);
  
  var warnings = {};

  function warn(string) {
    if (warnings[string]) {
      return;
    }

    warnings[string] = true;
    self.$warn(string);
  }

  (function($base, $super) {
    function $String(){};
    var self = $String = $klass($base, $super, 'String', $String);

    var def = self.$$proto, $scope = self.$$scope;

    var ERROR = "String#%s not supported. Mutable String methods are not supported in Opal.";

    Opal.defn(self, '$<<', function() {
      var self = this;

      return self.$raise($scope.get('NotImplementedError'), (ERROR)['$%']("<<"));
    });

    Opal.defn(self, '$capitalize!', function() {
      var self = this;

      return self.$raise($scope.get('NotImplementedError'), (ERROR)['$%']("capitalize!"));
    });

    Opal.defn(self, '$chomp!', function() {
      var self = this;

      return self.$raise($scope.get('NotImplementedError'), (ERROR)['$%']("chomp!"));
    });

    Opal.defn(self, '$chop!', function() {
      var self = this;

      return self.$raise($scope.get('NotImplementedError'), (ERROR)['$%']("chop!"));
    });

    Opal.defn(self, '$downcase!', function() {
      var self = this;

      return self.$raise($scope.get('NotImplementedError'), (ERROR)['$%']("downcase!"));
    });

    Opal.defn(self, '$gsub!', function() {
      var self = this;

      return self.$raise($scope.get('NotImplementedError'), (ERROR)['$%']("gsub!"));
    });

    Opal.defn(self, '$lstrip!', function() {
      var self = this;

      return self.$raise($scope.get('NotImplementedError'), (ERROR)['$%']("lstrip!"));
    });

    Opal.defn(self, '$next!', function() {
      var self = this;

      return self.$raise($scope.get('NotImplementedError'), (ERROR)['$%']("next!"));
    });

    Opal.defn(self, '$reverse!', function() {
      var self = this;

      return self.$raise($scope.get('NotImplementedError'), (ERROR)['$%']("reverse!"));
    });

    Opal.defn(self, '$slice!', function() {
      var self = this;

      return self.$raise($scope.get('NotImplementedError'), (ERROR)['$%']("slice!"));
    });

    Opal.defn(self, '$squeeze!', function() {
      var self = this;

      return self.$raise($scope.get('NotImplementedError'), (ERROR)['$%']("squeeze!"));
    });

    Opal.defn(self, '$strip!', function() {
      var self = this;

      return self.$raise($scope.get('NotImplementedError'), (ERROR)['$%']("strip!"));
    });

    Opal.defn(self, '$sub!', function() {
      var self = this;

      return self.$raise($scope.get('NotImplementedError'), (ERROR)['$%']("sub!"));
    });

    Opal.defn(self, '$succ!', function() {
      var self = this;

      return self.$raise($scope.get('NotImplementedError'), (ERROR)['$%']("succ!"));
    });

    Opal.defn(self, '$swapcase!', function() {
      var self = this;

      return self.$raise($scope.get('NotImplementedError'), (ERROR)['$%']("swapcase!"));
    });

    Opal.defn(self, '$tr!', function() {
      var self = this;

      return self.$raise($scope.get('NotImplementedError'), (ERROR)['$%']("tr!"));
    });

    Opal.defn(self, '$tr_s!', function() {
      var self = this;

      return self.$raise($scope.get('NotImplementedError'), (ERROR)['$%']("tr_s!"));
    });

    return (Opal.defn(self, '$upcase!', function() {
      var self = this;

      return self.$raise($scope.get('NotImplementedError'), (ERROR)['$%']("upcase!"));
    }), nil) && 'upcase!';
  })($scope.base, null);
  (function($base) {
    var $Kernel, self = $Kernel = $module($base, 'Kernel');

    var def = self.$$proto, $scope = self.$$scope;

    var ERROR = "Object freezing is not supported by Opal";

    Opal.defn(self, '$freeze', function() {
      var $a, self = this;

      if ((($a = OPAL_CONFIG.freezing) !== nil && (!$a.$$is_boolean || $a == true))) {
        warn(ERROR);
        } else {
        self.$raise($scope.get('NotImplementedError'), ERROR)
      };
      return self;
    });

    Opal.defn(self, '$frozen?', function() {
      var $a, self = this;

      if ((($a = OPAL_CONFIG.freezing) !== nil && (!$a.$$is_boolean || $a == true))) {
        warn(ERROR);
        } else {
        self.$raise($scope.get('NotImplementedError'), ERROR)
      };
      return false;
    });
  })($scope.base);
  (function($base) {
    var $Kernel, self = $Kernel = $module($base, 'Kernel');

    var def = self.$$proto, $scope = self.$$scope;

    var ERROR = "Object tainting is not supported by Opal";

    Opal.defn(self, '$taint', function() {
      var $a, self = this;

      if ((($a = OPAL_CONFIG.tainting) !== nil && (!$a.$$is_boolean || $a == true))) {
        warn(ERROR);
        } else {
        self.$raise($scope.get('NotImplementedError'), ERROR)
      };
      return self;
    });

    Opal.defn(self, '$untaint', function() {
      var $a, self = this;

      if ((($a = OPAL_CONFIG.tainting) !== nil && (!$a.$$is_boolean || $a == true))) {
        warn(ERROR);
        } else {
        self.$raise($scope.get('NotImplementedError'), ERROR)
      };
      return self;
    });

    Opal.defn(self, '$tainted?', function() {
      var $a, self = this;

      if ((($a = OPAL_CONFIG.tainting) !== nil && (!$a.$$is_boolean || $a == true))) {
        warn(ERROR);
        } else {
        self.$raise($scope.get('NotImplementedError'), ERROR)
      };
      return false;
    });
  })($scope.base);
  (function($base) {
    var $Marshal, self = $Marshal = $module($base, 'Marshal');

    var def = self.$$proto, $scope = self.$$scope;

    var ERROR = "Marshalling is not supported by Opal";

    self.$module_function();

    Opal.defn(self, '$dump', function() {
      var self = this;

      return self.$raise($scope.get('NotImplementedError'), ERROR);
    });

    Opal.defn(self, '$load', function() {
      var self = this;

      return self.$raise($scope.get('NotImplementedError'), ERROR);
    });

    Opal.defn(self, '$restore', function() {
      var self = this;

      return self.$raise($scope.get('NotImplementedError'), ERROR);
    });
  })($scope.base);
  (function($base, $super) {
    function $Module(){};
    var self = $Module = $klass($base, $super, 'Module', $Module);

    var def = self.$$proto, $scope = self.$$scope;

    Opal.defn(self, '$public', function() {
      var self = this, $splat_index = nil;

      var array_size = arguments.length - 0;
      if(array_size < 0) array_size = 0;
      var methods = new Array(array_size);
      for($splat_index = 0; $splat_index < array_size; $splat_index++) {
        methods[$splat_index] = arguments[$splat_index + 0];
      }
      
      if (methods.length === 0) {
        self.$$module_function = false;
      }

      return nil;
    
    });

    Opal.alias(self, 'private', 'public');

    Opal.alias(self, 'protected', 'public');

    Opal.alias(self, 'nesting', 'public');

    Opal.defn(self, '$private_class_method', function() {
      var self = this;

      return self;
    });

    Opal.alias(self, 'public_class_method', 'private_class_method');

    Opal.defn(self, '$private_method_defined?', function(obj) {
      var self = this;

      return false;
    });

    Opal.defn(self, '$private_constant', function() {
      var self = this;

      return nil;
    });

    Opal.alias(self, 'protected_method_defined?', 'private_method_defined?');

    Opal.alias(self, 'public_instance_methods', 'instance_methods');

    return Opal.alias(self, 'public_method_defined?', 'method_defined?');
  })($scope.base, null);
  (function($base) {
    var $Kernel, self = $Kernel = $module($base, 'Kernel');

    var def = self.$$proto, $scope = self.$$scope;

    Opal.defn(self, '$private_methods', function() {
      var self = this;

      return [];
    });

    Opal.alias(self, 'private_instance_methods', 'private_methods');
  })($scope.base);
  return (function($base) {
    var $Kernel, self = $Kernel = $module($base, 'Kernel');

    var def = self.$$proto, $scope = self.$$scope;

    Opal.defn(self, '$eval', function() {
      var self = this;

      return self.$raise($scope.get('NotImplementedError'), "To use Kernel#eval, you must first require 'opal-parser'. " + ("See https://github.com/opal/opal/blob/" + ($scope.get('RUBY_ENGINE_VERSION')) + "/docs/opal_parser.md for details."));
    })
  })($scope.base);
};

/* Generated by Opal 0.9.0 */
Opal.modules["opal"] = function(Opal) {
  Opal.dynamic_require_severity = "error";
  var OPAL_CONFIG = { method_missing: true, arity_check: false, freezing: true, tainting: true };
  var self = Opal.top, $scope = Opal, nil = Opal.nil, $breaker = Opal.breaker, $slice = Opal.slice;

  Opal.add_stubs(['$require']);
  self.$require("opal/base");
  self.$require("opal/mini");
  self.$require("corelib/array/inheritance");
  self.$require("corelib/string/inheritance");
  self.$require("corelib/string/encoding");
  self.$require("corelib/math");
  self.$require("corelib/complex");
  self.$require("corelib/rational");
  self.$require("corelib/time");
  self.$require("corelib/struct");
  self.$require("corelib/io");
  self.$require("corelib/main");
  self.$require("corelib/dir");
  self.$require("corelib/file");
  self.$require("corelib/process");
  return self.$require("corelib/unsupported");
};

/* Generated by Opal 0.9.0 */
Opal.modules["native"] = function(Opal) {
  Opal.dynamic_require_severity = "error";
  var OPAL_CONFIG = { method_missing: true, arity_check: false, freezing: true, tainting: true };
  function $rb_minus(lhs, rhs) {
    return (typeof(lhs) === 'number' && typeof(rhs) === 'number') ? lhs - rhs : lhs['$-'](rhs);
  }
  function $rb_ge(lhs, rhs) {
    return (typeof(lhs) === 'number' && typeof(rhs) === 'number') ? lhs >= rhs : lhs['$>='](rhs);
  }
  var self = Opal.top, $scope = Opal, nil = Opal.nil, $breaker = Opal.breaker, $slice = Opal.slice, $module = Opal.module, $range = Opal.range, $hash2 = Opal.hash2, $klass = Opal.klass, $gvars = Opal.gvars;

  Opal.add_stubs(['$try_convert', '$native?', '$respond_to?', '$to_n', '$raise', '$inspect', '$Native', '$proc', '$map!', '$end_with?', '$define_method', '$[]', '$convert', '$call', '$to_proc', '$new', '$each', '$native_reader', '$native_writer', '$extend', '$is_a?', '$map', '$alias_method', '$to_a', '$_Array', '$include', '$method_missing', '$bind', '$instance_method', '$[]=', '$slice', '$-', '$length', '$enum_for', '$===', '$>=', '$<<', '$each_pair', '$_initialize', '$name', '$exiting_mid', '$native_module']);
  (function($base) {
    var $Native, self = $Native = $module($base, 'Native');

    var def = self.$$proto, $scope = self.$$scope, TMP_1, TMP_2;

    Opal.defs(self, '$is_a?', function(object, klass) {
      var self = this;

      
      try {
        return object instanceof self.$try_convert(klass);
      }
      catch (e) {
        return false;
      }
    ;
    });

    Opal.defs(self, '$try_convert', function(value) {
      var self = this;

      
      if (self['$native?'](value)) {
        return value;
      }
      else if (value['$respond_to?']("to_n")) {
        return value.$to_n();
      }
      else {
        return nil;
      }
    ;
    });

    Opal.defs(self, '$convert', function(value) {
      var self = this;

      
      if (self['$native?'](value)) {
        return value;
      }
      else if (value['$respond_to?']("to_n")) {
        return value.$to_n();
      }
      else {
        self.$raise($scope.get('ArgumentError'), "" + (value.$inspect()) + " isn't native");
      }
    ;
    });

    Opal.defs(self, '$call', TMP_1 = function(obj, key) {
      var self = this, $iter = TMP_1.$$p, block = $iter || nil, $splat_index = nil;

      var array_size = arguments.length - 2;
      if(array_size < 0) array_size = 0;
      var args = new Array(array_size);
      for($splat_index = 0; $splat_index < array_size; $splat_index++) {
        args[$splat_index] = arguments[$splat_index + 2];
      }
      TMP_1.$$p = null;
      
      var prop = obj[key];

      if (prop instanceof Function) {
        var converted = new Array(args.length);

        for (var i = 0, length = args.length; i < length; i++) {
          var item = args[i],
              conv = self.$try_convert(item);

          converted[i] = conv === nil ? item : conv;
        }

        if (block !== nil) {
          converted.push(block);
        }

        return self.$Native(prop.apply(obj, converted));
      }
      else {
        return self.$Native(prop);
      }
    ;
    });

    Opal.defs(self, '$proc', TMP_2 = function() {
      var $a, $b, TMP_3, self = this, $iter = TMP_2.$$p, block = $iter || nil;

      TMP_2.$$p = null;
      if (block !== false && block !== nil) {
        } else {
        self.$raise($scope.get('LocalJumpError'), "no block given")
      };
      return ($a = ($b = $scope.get('Kernel')).$proc, $a.$$p = (TMP_3 = function(args){var self = TMP_3.$$s || this, $a, $b, TMP_4, instance = nil;
args = $slice.call(arguments, 0);
      ($a = ($b = args)['$map!'], $a.$$p = (TMP_4 = function(arg){var self = TMP_4.$$s || this;
if (arg == null) arg = nil;
        return self.$Native(arg)}, TMP_4.$$s = self, TMP_4), $a).call($b);
        instance = self.$Native(this);
        
        // if global is current scope, run the block in the scope it was defined
        if (this === Opal.global) {
          return block.apply(self, args);
        }

        var self_ = block.$$s;
        block.$$s = null;

        try {
          return block.apply(instance, args);
        }
        finally {
          block.$$s = self_;
        }
      ;}, TMP_3.$$s = self, TMP_3), $a).call($b);
    });

    (function($base) {
      var $Helpers, self = $Helpers = $module($base, 'Helpers');

      var def = self.$$proto, $scope = self.$$scope;

      Opal.defn(self, '$alias_native', function(new$, old, $kwargs) {
        var $a, $b, TMP_5, $c, TMP_6, $d, TMP_7, self = this, as = nil;

        if (old == null) {
          old = new$
        }
        if (old == null) {
          $kwargs = $hash2([], {});
        }
        else if (old.$$is_hash) {
          $kwargs = old;
          old = new$;
        }
        else if ($kwargs == null) {
          $kwargs = $hash2([], {});
        }
        if (!$kwargs.$$is_hash) {
          throw Opal.ArgumentError.$new('expecting keyword args');
        }
        if ((as = $kwargs.$$smap['as']) == null) {
          as = nil
        }
        if ((($a = old['$end_with?']("=")) !== nil && (!$a.$$is_boolean || $a == true))) {
          return ($a = ($b = self).$define_method, $a.$$p = (TMP_5 = function(value){var self = TMP_5.$$s || this;
            if (self["native"] == null) self["native"] = nil;
if (value == null) value = nil;
          self["native"][old['$[]']($range(0, -2, false))] = $scope.get('Native').$convert(value);
            return value;}, TMP_5.$$s = self, TMP_5), $a).call($b, new$)
        } else if (as !== false && as !== nil) {
          return ($a = ($c = self).$define_method, $a.$$p = (TMP_6 = function(args){var self = TMP_6.$$s || this, block, $a, $b, $c, value = nil;
            if (self["native"] == null) self["native"] = nil;
args = $slice.call(arguments, 0);
            block = TMP_6.$$p || nil, TMP_6.$$p = null;
          if ((($a = value = ($b = ($c = $scope.get('Native')).$call, $b.$$p = block.$to_proc(), $b).apply($c, [self["native"], old].concat(Opal.to_a(args)))) !== nil && (!$a.$$is_boolean || $a == true))) {
              return as.$new(value.$to_n())
              } else {
              return nil
            }}, TMP_6.$$s = self, TMP_6), $a).call($c, new$)
          } else {
          return ($a = ($d = self).$define_method, $a.$$p = (TMP_7 = function(args){var self = TMP_7.$$s || this, block, $a, $b;
            if (self["native"] == null) self["native"] = nil;
args = $slice.call(arguments, 0);
            block = TMP_7.$$p || nil, TMP_7.$$p = null;
          return ($a = ($b = $scope.get('Native')).$call, $a.$$p = block.$to_proc(), $a).apply($b, [self["native"], old].concat(Opal.to_a(args)))}, TMP_7.$$s = self, TMP_7), $a).call($d, new$)
        };
      });

      Opal.defn(self, '$native_reader', function() {
        var $a, $b, TMP_8, self = this, $splat_index = nil;

        var array_size = arguments.length - 0;
        if(array_size < 0) array_size = 0;
        var names = new Array(array_size);
        for($splat_index = 0; $splat_index < array_size; $splat_index++) {
          names[$splat_index] = arguments[$splat_index + 0];
        }
        return ($a = ($b = names).$each, $a.$$p = (TMP_8 = function(name){var self = TMP_8.$$s || this, $a, $b, TMP_9;
if (name == null) name = nil;
        return ($a = ($b = self).$define_method, $a.$$p = (TMP_9 = function(){var self = TMP_9.$$s || this;
            if (self["native"] == null) self["native"] = nil;

          return self.$Native(self["native"][name])}, TMP_9.$$s = self, TMP_9), $a).call($b, name)}, TMP_8.$$s = self, TMP_8), $a).call($b);
      });

      Opal.defn(self, '$native_writer', function() {
        var $a, $b, TMP_10, self = this, $splat_index = nil;

        var array_size = arguments.length - 0;
        if(array_size < 0) array_size = 0;
        var names = new Array(array_size);
        for($splat_index = 0; $splat_index < array_size; $splat_index++) {
          names[$splat_index] = arguments[$splat_index + 0];
        }
        return ($a = ($b = names).$each, $a.$$p = (TMP_10 = function(name){var self = TMP_10.$$s || this, $a, $b, TMP_11;
if (name == null) name = nil;
        return ($a = ($b = self).$define_method, $a.$$p = (TMP_11 = function(value){var self = TMP_11.$$s || this;
            if (self["native"] == null) self["native"] = nil;
if (value == null) value = nil;
          return self.$Native(self["native"][name] = value)}, TMP_11.$$s = self, TMP_11), $a).call($b, "" + (name) + "=")}, TMP_10.$$s = self, TMP_10), $a).call($b);
      });

      Opal.defn(self, '$native_accessor', function() {
        var $a, $b, self = this, $splat_index = nil;

        var array_size = arguments.length - 0;
        if(array_size < 0) array_size = 0;
        var names = new Array(array_size);
        for($splat_index = 0; $splat_index < array_size; $splat_index++) {
          names[$splat_index] = arguments[$splat_index + 0];
        }
        ($a = self).$native_reader.apply($a, Opal.to_a(names));
        return ($b = self).$native_writer.apply($b, Opal.to_a(names));
      });
    })($scope.base);

    Opal.defs(self, '$included', function(klass) {
      var self = this;

      return klass.$extend($scope.get('Helpers'));
    });

    Opal.defn(self, '$initialize', function(native$) {
      var $a, self = this;

      if ((($a = $scope.get('Kernel')['$native?'](native$)) !== nil && (!$a.$$is_boolean || $a == true))) {
        } else {
        $scope.get('Kernel').$raise($scope.get('ArgumentError'), "" + (native$.$inspect()) + " isn't native")
      };
      return self["native"] = native$;
    });

    Opal.defn(self, '$to_n', function() {
      var self = this;
      if (self["native"] == null) self["native"] = nil;

      return self["native"];
    });
  })($scope.base);
  (function($base) {
    var $Kernel, self = $Kernel = $module($base, 'Kernel');

    var def = self.$$proto, $scope = self.$$scope, TMP_14;

    Opal.defn(self, '$native?', function(value) {
      var self = this;

      return value == null || !value.$$class;
    });

    Opal.defn(self, '$Native', function(obj) {
      var $a, $b, TMP_12, $c, TMP_13, self = this;

      if ((($a = obj == null) !== nil && (!$a.$$is_boolean || $a == true))) {
        return nil
      } else if ((($a = self['$native?'](obj)) !== nil && (!$a.$$is_boolean || $a == true))) {
        return (($scope.get('Native')).$$scope.get('Object')).$new(obj)
      } else if ((($a = obj['$is_a?']($scope.get('Array'))) !== nil && (!$a.$$is_boolean || $a == true))) {
        return ($a = ($b = obj).$map, $a.$$p = (TMP_12 = function(o){var self = TMP_12.$$s || this;
if (o == null) o = nil;
        return self.$Native(o)}, TMP_12.$$s = self, TMP_12), $a).call($b)
      } else if ((($a = obj['$is_a?']($scope.get('Proc'))) !== nil && (!$a.$$is_boolean || $a == true))) {
        return ($a = ($c = self).$proc, $a.$$p = (TMP_13 = function(args){var self = TMP_13.$$s || this, block, $a, $b;
args = $slice.call(arguments, 0);
          block = TMP_13.$$p || nil, TMP_13.$$p = null;
        return self.$Native(($a = ($b = obj).$call, $a.$$p = block.$to_proc(), $a).apply($b, Opal.to_a(args)))}, TMP_13.$$s = self, TMP_13), $a).call($c)
        } else {
        return obj
      };
    });

    self.$alias_method("_Array", "Array");

    Opal.defn(self, '$Array', TMP_14 = function(object) {
      var $a, $b, self = this, $iter = TMP_14.$$p, block = $iter || nil, $splat_index = nil;

      var array_size = arguments.length - 1;
      if(array_size < 0) array_size = 0;
      var args = new Array(array_size);
      for($splat_index = 0; $splat_index < array_size; $splat_index++) {
        args[$splat_index] = arguments[$splat_index + 1];
      }
      TMP_14.$$p = null;
      if ((($a = self['$native?'](object)) !== nil && (!$a.$$is_boolean || $a == true))) {
        return ($a = ($b = (($scope.get('Native')).$$scope.get('Array'))).$new, $a.$$p = block.$to_proc(), $a).apply($b, [object].concat(Opal.to_a(args))).$to_a()};
      return self.$_Array(object);
    });
  })($scope.base);
  (function($base, $super) {
    function $Object(){};
    var self = $Object = $klass($base, $super, 'Object', $Object);

    var def = self.$$proto, $scope = self.$$scope, TMP_15, TMP_16, TMP_17;

    def["native"] = nil;
    self.$include(Opal.get('Native'));

    Opal.defn(self, '$==', function(other) {
      var self = this;

      return self["native"] === $scope.get('Native').$try_convert(other);
    });

    Opal.defn(self, '$has_key?', function(name) {
      var self = this;

      return Opal.hasOwnProperty.call(self["native"], name);
    });

    Opal.alias(self, 'key?', 'has_key?');

    Opal.alias(self, 'include?', 'has_key?');

    Opal.alias(self, 'member?', 'has_key?');

    Opal.defn(self, '$each', TMP_15 = function() {
      var $a, self = this, $iter = TMP_15.$$p, $yield = $iter || nil, $splat_index = nil;

      var array_size = arguments.length - 0;
      if(array_size < 0) array_size = 0;
      var args = new Array(array_size);
      for($splat_index = 0; $splat_index < array_size; $splat_index++) {
        args[$splat_index] = arguments[$splat_index + 0];
      }
      TMP_15.$$p = null;
      if (($yield !== nil)) {
        
        for (var key in self["native"]) {
          ((($a = Opal.yieldX($yield, [key, self["native"][key]])) === $breaker) ? $breaker.$v : $a)
        }
      ;
        return self;
        } else {
        return ($a = self).$method_missing.apply($a, ["each"].concat(Opal.to_a(args)))
      };
    });

    Opal.defn(self, '$[]', function(key) {
      var self = this;

      
      var prop = self["native"][key];

      if (prop instanceof Function) {
        return prop;
      }
      else {
        return Opal.get('Native').$call(self["native"], key)
      }
    ;
    });

    Opal.defn(self, '$[]=', function(key, value) {
      var $a, self = this, native$ = nil;

      native$ = $scope.get('Native').$try_convert(value);
      if ((($a = native$ === nil) !== nil && (!$a.$$is_boolean || $a == true))) {
        return self["native"][key] = value;
        } else {
        return self["native"][key] = native$;
      };
    });

    Opal.defn(self, '$merge!', function(other) {
      var self = this;

      
      other = $scope.get('Native').$convert(other);

      for (var prop in other) {
        self["native"][prop] = other[prop];
      }
    ;
      return self;
    });

    Opal.defn(self, '$respond_to?', function(name, include_all) {
      var self = this;

      if (include_all == null) {
        include_all = false
      }
      return $scope.get('Kernel').$instance_method("respond_to?").$bind(self).$call(name, include_all);
    });

    Opal.defn(self, '$respond_to_missing?', function(name, include_all) {
      var self = this;

      if (include_all == null) {
        include_all = false
      }
      return Opal.hasOwnProperty.call(self["native"], name);
    });

    Opal.defn(self, '$method_missing', TMP_16 = function(mid) {
      var $a, $b, self = this, $iter = TMP_16.$$p, block = $iter || nil, $splat_index = nil;

      var array_size = arguments.length - 1;
      if(array_size < 0) array_size = 0;
      var args = new Array(array_size);
      for($splat_index = 0; $splat_index < array_size; $splat_index++) {
        args[$splat_index] = arguments[$splat_index + 1];
      }
      TMP_16.$$p = null;
      
      if (mid.charAt(mid.length - 1) === '=') {
        return self['$[]='](mid.$slice(0, $rb_minus(mid.$length(), 1)), args['$[]'](0));
      }
      else {
        return ($a = ($b = Opal.get('Native')).$call, $a.$$p = block.$to_proc(), $a).apply($b, [self["native"], mid].concat(Opal.to_a(args)));
      }
    ;
    });

    Opal.defn(self, '$nil?', function() {
      var self = this;

      return false;
    });

    Opal.defn(self, '$is_a?', function(klass) {
      var self = this;

      return Opal.is_a(self, klass);
    });

    Opal.alias(self, 'kind_of?', 'is_a?');

    Opal.defn(self, '$instance_of?', function(klass) {
      var self = this;

      return self.$$class === klass;
    });

    Opal.defn(self, '$class', function() {
      var self = this;

      return self.$$class;
    });

    Opal.defn(self, '$to_a', TMP_17 = function(options) {
      var $a, $b, self = this, $iter = TMP_17.$$p, block = $iter || nil;

      if (options == null) {
        options = $hash2([], {})
      }
      TMP_17.$$p = null;
      return ($a = ($b = (($scope.get('Native')).$$scope.get('Array'))).$new, $a.$$p = block.$to_proc(), $a).call($b, self["native"], options).$to_a();
    });

    return (Opal.defn(self, '$inspect', function() {
      var self = this;

      return "#<Native:" + (String(self["native"])) + ">";
    }), nil) && 'inspect';
  })($scope.get('Native'), $scope.get('BasicObject'));
  (function($base, $super) {
    function $Array(){};
    var self = $Array = $klass($base, $super, 'Array', $Array);

    var def = self.$$proto, $scope = self.$$scope, TMP_18, TMP_19;

    def.named = def["native"] = def.get = def.block = def.set = def.length = nil;
    self.$include($scope.get('Native'));

    self.$include($scope.get('Enumerable'));

    Opal.defn(self, '$initialize', TMP_18 = function(native$, options) {
      var $a, self = this, $iter = TMP_18.$$p, block = $iter || nil;

      if (options == null) {
        options = $hash2([], {})
      }
      TMP_18.$$p = null;
      Opal.find_super_dispatcher(self, 'initialize', TMP_18, null).apply(self, [native$]);
      self.get = ((($a = options['$[]']("get")) !== false && $a !== nil) ? $a : options['$[]']("access"));
      self.named = options['$[]']("named");
      self.set = ((($a = options['$[]']("set")) !== false && $a !== nil) ? $a : options['$[]']("access"));
      self.length = ((($a = options['$[]']("length")) !== false && $a !== nil) ? $a : "length");
      self.block = block;
      if ((($a = self.$length() == null) !== nil && (!$a.$$is_boolean || $a == true))) {
        return self.$raise($scope.get('ArgumentError'), "no length found on the array-like object")
        } else {
        return nil
      };
    });

    Opal.defn(self, '$each', TMP_19 = function() {
      var self = this, $iter = TMP_19.$$p, block = $iter || nil;

      TMP_19.$$p = null;
      if (block !== false && block !== nil) {
        } else {
        return self.$enum_for("each")
      };
      
      for (var i = 0, length = self.$length(); i < length; i++) {
        var value = Opal.yield1(block, self['$[]'](i));

        if (value === $breaker) {
          return $breaker.$v;
        }
      }
    ;
      return self;
    });

    Opal.defn(self, '$[]', function(index) {
      var $a, self = this, result = nil, $case = nil;

      result = (function() {$case = index;if ($scope.get('String')['$===']($case) || $scope.get('Symbol')['$===']($case)) {if ((($a = self.named) !== nil && (!$a.$$is_boolean || $a == true))) {
        return self["native"][self.named](index);
        } else {
        return self["native"][index];
      }}else if ($scope.get('Integer')['$===']($case)) {if ((($a = self.get) !== nil && (!$a.$$is_boolean || $a == true))) {
        return self["native"][self.get](index);
        } else {
        return self["native"][index];
      }}else { return nil }})();
      if (result !== false && result !== nil) {
        if ((($a = self.block) !== nil && (!$a.$$is_boolean || $a == true))) {
          return self.block.$call(result)
          } else {
          return self.$Native(result)
        }
        } else {
        return nil
      };
    });

    Opal.defn(self, '$[]=', function(index, value) {
      var $a, self = this;

      if ((($a = self.set) !== nil && (!$a.$$is_boolean || $a == true))) {
        return self["native"][self.set](index, $scope.get('Native').$convert(value));
        } else {
        return self["native"][index] = $scope.get('Native').$convert(value);
      };
    });

    Opal.defn(self, '$last', function(count) {
      var $a, self = this, index = nil, result = nil;

      if (count == null) {
        count = nil
      }
      if (count !== false && count !== nil) {
        index = $rb_minus(self.$length(), 1);
        result = [];
        while ($rb_ge(index, 0)) {
        result['$<<'](self['$[]'](index));
        index = $rb_minus(index, 1);};
        return result;
        } else {
        return self['$[]']($rb_minus(self.$length(), 1))
      };
    });

    Opal.defn(self, '$length', function() {
      var self = this;

      return self["native"][self.length];
    });

    Opal.alias(self, 'to_ary', 'to_a');

    return (Opal.defn(self, '$inspect', function() {
      var self = this;

      return self.$to_a().$inspect();
    }), nil) && 'inspect';
  })($scope.get('Native'), null);
  (function($base, $super) {
    function $Numeric(){};
    var self = $Numeric = $klass($base, $super, 'Numeric', $Numeric);

    var def = self.$$proto, $scope = self.$$scope;

    return (Opal.defn(self, '$to_n', function() {
      var self = this;

      return self.valueOf();
    }), nil) && 'to_n'
  })($scope.base, null);
  (function($base, $super) {
    function $Proc(){};
    var self = $Proc = $klass($base, $super, 'Proc', $Proc);

    var def = self.$$proto, $scope = self.$$scope;

    return (Opal.defn(self, '$to_n', function() {
      var self = this;

      return self;
    }), nil) && 'to_n'
  })($scope.base, null);
  (function($base, $super) {
    function $String(){};
    var self = $String = $klass($base, $super, 'String', $String);

    var def = self.$$proto, $scope = self.$$scope;

    return (Opal.defn(self, '$to_n', function() {
      var self = this;

      return self.valueOf();
    }), nil) && 'to_n'
  })($scope.base, null);
  (function($base, $super) {
    function $Regexp(){};
    var self = $Regexp = $klass($base, $super, 'Regexp', $Regexp);

    var def = self.$$proto, $scope = self.$$scope;

    return (Opal.defn(self, '$to_n', function() {
      var self = this;

      return self.valueOf();
    }), nil) && 'to_n'
  })($scope.base, null);
  (function($base, $super) {
    function $MatchData(){};
    var self = $MatchData = $klass($base, $super, 'MatchData', $MatchData);

    var def = self.$$proto, $scope = self.$$scope;

    def.matches = nil;
    return (Opal.defn(self, '$to_n', function() {
      var self = this;

      return self.matches;
    }), nil) && 'to_n'
  })($scope.base, null);
  (function($base, $super) {
    function $Struct(){};
    var self = $Struct = $klass($base, $super, 'Struct', $Struct);

    var def = self.$$proto, $scope = self.$$scope;

    return (Opal.defn(self, '$to_n', function() {
      var $a, $b, TMP_20, self = this, result = nil;

      result = {};
      ($a = ($b = self).$each_pair, $a.$$p = (TMP_20 = function(name, value){var self = TMP_20.$$s || this;
if (name == null) name = nil;if (value == null) value = nil;
      return result[name] = $scope.get('Native').$try_convert(value);}, TMP_20.$$s = self, TMP_20), $a).call($b);
      return result;
    }), nil) && 'to_n'
  })($scope.base, null);
  (function($base, $super) {
    function $Array(){};
    var self = $Array = $klass($base, $super, 'Array', $Array);

    var def = self.$$proto, $scope = self.$$scope;

    return (Opal.defn(self, '$to_n', function() {
      var self = this;

      
      var result = [];

      for (var i = 0, length = self.length; i < length; i++) {
        var obj = self[i];

        result.push($scope.get('Native').$try_convert(obj));
      }

      return result;
    
    }), nil) && 'to_n'
  })($scope.base, null);
  (function($base, $super) {
    function $Boolean(){};
    var self = $Boolean = $klass($base, $super, 'Boolean', $Boolean);

    var def = self.$$proto, $scope = self.$$scope;

    return (Opal.defn(self, '$to_n', function() {
      var self = this;

      return self.valueOf();
    }), nil) && 'to_n'
  })($scope.base, null);
  (function($base, $super) {
    function $Time(){};
    var self = $Time = $klass($base, $super, 'Time', $Time);

    var def = self.$$proto, $scope = self.$$scope;

    return (Opal.defn(self, '$to_n', function() {
      var self = this;

      return self;
    }), nil) && 'to_n'
  })($scope.base, null);
  (function($base, $super) {
    function $NilClass(){};
    var self = $NilClass = $klass($base, $super, 'NilClass', $NilClass);

    var def = self.$$proto, $scope = self.$$scope;

    return (Opal.defn(self, '$to_n', function() {
      var self = this;

      return null;
    }), nil) && 'to_n'
  })($scope.base, null);
  (function($base, $super) {
    function $Hash(){};
    var self = $Hash = $klass($base, $super, 'Hash', $Hash);

    var def = self.$$proto, $scope = self.$$scope, TMP_21;

    self.$alias_method("_initialize", "initialize");

    Opal.defn(self, '$initialize', TMP_21 = function(defaults) {
      var $a, $b, self = this, $iter = TMP_21.$$p, block = $iter || nil;

      TMP_21.$$p = null;
      
      if (defaults !== undefined && defaults.constructor === Object) {
        var smap = self.$$smap,
            keys = self.$$keys,
            key, value;

        for (key in defaults) {
          value = defaults[key];

          if (value && value.constructor === Object) {
            smap[key] = $scope.get('Hash').$new(value);
          } else if (value && value.$$is_array) {
            value = value.map(function(item) {
              if (item && item.constructor === Object) {
                return $scope.get('Hash').$new(item);
              }

              return item;
            });
            smap[key] = value
          } else {
            smap[key] = self.$Native(value);
          }

          keys.push(key);
        }

        return self;
      }

      return ($a = ($b = self).$_initialize, $a.$$p = block.$to_proc(), $a).call($b, defaults);
    
    });

    return (Opal.defn(self, '$to_n', function() {
      var self = this;

      
      var result = {},
          keys = self.$$keys,
          smap = self.$$smap,
          key, value;

      for (var i = 0, length = keys.length; i < length; i++) {
        key = keys[i];

        if (key.$$is_string) {
          value = smap[key];
        } else {
          key = key.key;
          value = key.value;
        }

        result[key] = $scope.get('Native').$try_convert(value);
      }

      return result;
    
    }), nil) && 'to_n';
  })($scope.base, null);
  (function($base, $super) {
    function $Module(){};
    var self = $Module = $klass($base, $super, 'Module', $Module);

    var def = self.$$proto, $scope = self.$$scope;

    return (Opal.defn(self, '$native_module', function() {
      var self = this;

      return Opal.global[self.$name()] = self;
    }), nil) && 'native_module'
  })($scope.base, null);
  (function($base, $super) {
    function $Class(){};
    var self = $Class = $klass($base, $super, 'Class', $Class);

    var def = self.$$proto, $scope = self.$$scope;

    Opal.defn(self, '$native_alias', function(new_jsid, existing_mid) {
      var self = this;

      
      var aliased = self.$$proto['$' + existing_mid];
      if (!aliased) {
        self.$raise($scope.get('NameError').$new("undefined method `" + (existing_mid) + "' for class `" + (self.$inspect()) + "'", self.$exiting_mid()));
      }
      self.$$proto[new_jsid] = aliased;
    ;
    });

    return (Opal.defn(self, '$native_class', function() {
      var self = this;

      self.$native_module();
      self["new"] = self.$new;
    }), nil) && 'native_class';
  })($scope.base, null);
  return $gvars.$ = $gvars.global = self.$Native(Opal.global);
};

/* Generated by Opal 0.9.0 */
Opal.modules["opal/jquery/constants"] = function(Opal) {
  Opal.dynamic_require_severity = "error";
  var OPAL_CONFIG = { method_missing: true, arity_check: false, freezing: true, tainting: true };
  var $a, self = Opal.top, $scope = Opal, nil = Opal.nil, $breaker = Opal.breaker, $slice = Opal.slice;

  Opal.add_stubs(['$require', '$raise']);
  self.$require("native");
  if ((($a = ($scope.JQUERY_CLASS != null)) !== nil && (!$a.$$is_boolean || $a == true))) {
    return nil
    } else {
    return (function() {if ((($a = !!Opal.global.jQuery) !== nil && (!$a.$$is_boolean || $a == true))) {return Opal.cdecl($scope, 'JQUERY_CLASS', Opal.cdecl($scope, 'JQUERY_SELECTOR', Opal.global.jQuery))}else if ((($a = !!Opal.global.Zepto) !== nil && (!$a.$$is_boolean || $a == true))) {Opal.cdecl($scope, 'JQUERY_SELECTOR', Opal.global.Zepto);
    return Opal.cdecl($scope, 'JQUERY_CLASS', Opal.global.Zepto.zepto.Z);}else {return self.$raise($scope.get('NameError'), "Can't find jQuery or Zepto. jQuery must be included before opal-jquery")}})()
  };
};

/* Generated by Opal 0.9.0 */
Opal.modules["opal/jquery/element"] = function(Opal) {
  Opal.dynamic_require_severity = "error";
  var OPAL_CONFIG = { method_missing: true, arity_check: false, freezing: true, tainting: true };
  var self = Opal.top, $scope = Opal, nil = Opal.nil, $breaker = Opal.breaker, $slice = Opal.slice, $klass = Opal.klass;

  Opal.add_stubs(['$require', '$to_n', '$include', '$each', '$alias_native', '$attr_reader', '$nil?', '$[]', '$[]=', '$raise', '$is_a?', '$has_key?', '$delete', '$call', '$gsub', '$upcase', '$compact', '$map', '$respond_to?', '$<<', '$Native', '$new']);
  self.$require("native");
  self.$require("opal/jquery/constants");
  return (function($base, $super) {
    function $Element(){};
    var self = $Element = $klass($base, $super, 'Element', $Element);

    var def = self.$$proto, $scope = self.$$scope, TMP_2, TMP_3, TMP_6, TMP_7, TMP_8;

    var $ = $scope.get('JQUERY_SELECTOR').$to_n();

    self.$include($scope.get('Enumerable'));

    Opal.defs(self, '$find', function(selector) {
      var self = this;

      return $(selector);
    });

    Opal.defs(self, '$[]', function(selector) {
      var self = this;

      return $(selector);
    });

    Opal.defs(self, '$id', function(id) {
      var self = this;

      
      var el = document.getElementById(id);

      if (!el) {
        return nil;
      }

      return $(el);
    
    });

    Opal.defs(self, '$new', function(tag) {
      var self = this;

      if (tag == null) {
        tag = "div"
      }
      return $(document.createElement(tag));
    });

    Opal.defs(self, '$parse', function(str) {
      var self = this;

      return $.parseHTML ? $($.parseHTML(str)) : $(str);
    });

    Opal.defs(self, '$expose', function() {
      var $a, $b, TMP_1, self = this, $splat_index = nil;

      var array_size = arguments.length - 0;
      if(array_size < 0) array_size = 0;
      var methods = new Array(array_size);
      for($splat_index = 0; $splat_index < array_size; $splat_index++) {
        methods[$splat_index] = arguments[$splat_index + 0];
      }
      return ($a = ($b = methods).$each, $a.$$p = (TMP_1 = function(method){var self = TMP_1.$$s || this;
if (method == null) method = nil;
      return self.$alias_native(method)}, TMP_1.$$s = self, TMP_1), $a).call($b);
    });

    self.$attr_reader("selector");

    self.$alias_native("after");

    self.$alias_native("before");

    self.$alias_native("parent");

    self.$alias_native("parents");

    self.$alias_native("prev");

    self.$alias_native("remove");

    self.$alias_native("hide");

    self.$alias_native("show");

    self.$alias_native("toggle");

    self.$alias_native("children");

    self.$alias_native("blur");

    self.$alias_native("closest");

    self.$alias_native("detach");

    self.$alias_native("focus");

    self.$alias_native("find");

    self.$alias_native("next");

    self.$alias_native("siblings");

    self.$alias_native("text");

    self.$alias_native("trigger");

    self.$alias_native("append");

    self.$alias_native("prepend");

    self.$alias_native("serialize");

    self.$alias_native("is");

    self.$alias_native("filter");

    self.$alias_native("last");

    self.$alias_native("wrap");

    self.$alias_native("stop");

    self.$alias_native("clone");

    self.$alias_native("empty");

    self.$alias_native("get");

    self.$alias_native("prop");

    Opal.alias(self, 'succ', 'next');

    Opal.alias(self, '<<', 'append');

    self.$alias_native("add_class", "addClass");

    self.$alias_native("append_to", "appendTo");

    self.$alias_native("has_class?", "hasClass");

    self.$alias_native("html=", "html");

    self.$alias_native("index");

    self.$alias_native("is?", "is");

    self.$alias_native("remove_attr", "removeAttr");

    self.$alias_native("remove_class", "removeClass");

    self.$alias_native("submit");

    self.$alias_native("text=", "text");

    self.$alias_native("toggle_class", "toggleClass");

    self.$alias_native("value=", "val");

    self.$alias_native("scroll_top=", "scrollTop");

    self.$alias_native("scroll_top", "scrollTop");

    self.$alias_native("scroll_left=", "scrollLeft");

    self.$alias_native("scroll_left", "scrollLeft");

    self.$alias_native("remove_attribute", "removeAttr");

    self.$alias_native("slide_down", "slideDown");

    self.$alias_native("slide_up", "slideUp");

    self.$alias_native("slide_toggle", "slideToggle");

    self.$alias_native("fade_toggle", "fadeToggle");

    self.$alias_native("height=", "height");

    self.$alias_native("width=", "width");

    self.$alias_native("outer_width", "outerWidth");

    self.$alias_native("outer_height", "outerHeight");

    Opal.defn(self, '$to_n', function() {
      var self = this;

      return self;
    });

    Opal.defn(self, '$[]', function(name) {
      var self = this;

      
      var value = self.attr(name);
      if(value === undefined) return nil;
      return value;
    
    });

    Opal.defn(self, '$[]=', function(name, value) {
      var $a, self = this;

      if ((($a = value['$nil?']()) !== nil && (!$a.$$is_boolean || $a == true))) {
        return self.removeAttr(name);};
      return self.attr(name, value);
    });

    Opal.defn(self, '$attr', function() {
      var self = this, $splat_index = nil;

      var array_size = arguments.length - 0;
      if(array_size < 0) array_size = 0;
      var args = new Array(array_size);
      for($splat_index = 0; $splat_index < array_size; $splat_index++) {
        args[$splat_index] = arguments[$splat_index + 0];
      }
      
      var size = args.length;
      switch (size) {
      case 1:
        return self['$[]'](args[0]);
        break;
      case 2:
        return self['$[]='](args[0], args[1]);
        break;
      default:
        self.$raise($scope.get('ArgumentError'), "#attr only accepts 1 or 2 arguments")
      }
    ;
    });

    Opal.defn(self, '$has_attribute?', function(name) {
      var self = this;

      return self.attr(name) !== undefined;
    });

    Opal.defn(self, '$append_to_body', function() {
      var self = this;

      return self.appendTo(document.body);
    });

    Opal.defn(self, '$append_to_head', function() {
      var self = this;

      return self.appendTo(document.head);
    });

    Opal.defn(self, '$at', function(index) {
      var self = this;

      
      var length = self.length;

      if (index < 0) {
        index += length;
      }

      if (index < 0 || index >= length) {
        return nil;
      }

      return $(self[index]);
    
    });

    Opal.defn(self, '$class_name', function() {
      var self = this;

      
      var first = self[0];
      return (first && first.className) || "";
    
    });

    Opal.defn(self, '$class_name=', function(name) {
      var self = this;

      
      for (var i = 0, length = self.length; i < length; i++) {
        self[i].className = name;
      }
    
      return self;
    });

    Opal.defn(self, '$css', function(name, value) {
      var $a, $b, self = this;

      if (value == null) {
        value = nil
      }
      if ((($a = ($b = value['$nil?'](), $b !== false && $b !== nil ?name['$is_a?']($scope.get('String')) : $b)) !== nil && (!$a.$$is_boolean || $a == true))) {
        return self.css(name)
      } else if ((($a = name['$is_a?']($scope.get('Hash'))) !== nil && (!$a.$$is_boolean || $a == true))) {
        self.css(name.$to_n());
        } else {
        self.css(name, value);
      };
      return self;
    });

    Opal.defn(self, '$animate', TMP_2 = function(params) {
      var $a, self = this, $iter = TMP_2.$$p, block = $iter || nil, speed = nil;

      TMP_2.$$p = null;
      speed = (function() {if ((($a = params['$has_key?']("speed")) !== nil && (!$a.$$is_boolean || $a == true))) {
        return params.$delete("speed")
        } else {
        return 400
      }; return nil; })();
      
      self.animate(params.$to_n(), speed, function() {
        (function() {if ((block !== nil)) {
        return block.$call()
        } else {
        return nil
      }; return nil; })()
      })
    ;
    });

    Opal.defn(self, '$data', function() {
      var self = this, $splat_index = nil;

      var array_size = arguments.length - 0;
      if(array_size < 0) array_size = 0;
      var args = new Array(array_size);
      for($splat_index = 0; $splat_index < array_size; $splat_index++) {
        args[$splat_index] = arguments[$splat_index + 0];
      }
      
      var result = self.data.apply(self, args);
      return result == null ? nil : result;
    
    });

    Opal.defn(self, '$effect', TMP_3 = function(name) {
      var $a, $b, TMP_4, $c, TMP_5, self = this, $iter = TMP_3.$$p, block = $iter || nil, $splat_index = nil;

      var array_size = arguments.length - 1;
      if(array_size < 0) array_size = 0;
      var args = new Array(array_size);
      for($splat_index = 0; $splat_index < array_size; $splat_index++) {
        args[$splat_index] = arguments[$splat_index + 1];
      }
      TMP_3.$$p = null;
      name = ($a = ($b = name).$gsub, $a.$$p = (TMP_4 = function(match){var self = TMP_4.$$s || this;
if (match == null) match = nil;
      return match['$[]'](1).$upcase()}, TMP_4.$$s = self, TMP_4), $a).call($b, /_\w/);
      args = ($a = ($c = args).$map, $a.$$p = (TMP_5 = function(a){var self = TMP_5.$$s || this, $a;
if (a == null) a = nil;
      if ((($a = a['$respond_to?']("to_n")) !== nil && (!$a.$$is_boolean || $a == true))) {
          return a.$to_n()
          } else {
          return nil
        }}, TMP_5.$$s = self, TMP_5), $a).call($c).$compact();
      args['$<<'](function() { (function() {if ((block !== nil)) {
        return block.$call()
        } else {
        return nil
      }; return nil; })() });
      return self[name].apply(self, args);
    });

    Opal.defn(self, '$visible?', function() {
      var self = this;

      return self.is(':visible');
    });

    Opal.defn(self, '$offset', function() {
      var self = this;

      return self.$Native(self.offset());
    });

    Opal.defn(self, '$each', TMP_6 = function() {
      var self = this, $iter = TMP_6.$$p, $yield = $iter || nil;

      TMP_6.$$p = null;
      for (var i = 0, length = self.length; i < length; i++) {
      if (Opal.yield1($yield, $(self[i])) === $breaker) return $breaker.$v;
      };
      return self;
    });

    Opal.defn(self, '$first', function() {
      var self = this;

      return self.length ? self.first() : nil;
    });

    Opal.defn(self, '$html', function(content) {
      var self = this;

      
      if (content != null) {
        return self.html(content);
      }

      return self.html() || '';
    
    });

    Opal.defn(self, '$id', function() {
      var self = this;

      
      var first = self[0];
      return (first && first.id) || "";
    
    });

    Opal.defn(self, '$id=', function(id) {
      var self = this;

      
      var first = self[0];

      if (first) {
        first.id = id;
      }

      return self;
    
    });

    Opal.defn(self, '$tag_name', function() {
      var self = this;

      return self.length > 0 ? self[0].tagName.toLowerCase() : nil;
    });

    Opal.defn(self, '$inspect', function() {
      var self = this;

      
      if      (self[0] === document) return '#<Element [document]>'
      else if (self[0] === window  ) return '#<Element [window]>'

      var val, el, str, result = [];

      for (var i = 0, length = self.length; i < length; i++) {
        el  = self[i];
        if (!el.tagName) { return '#<Element ['+el.toString()+']'; }

        str = "<" + el.tagName.toLowerCase();

        if (val = el.id) str += (' id="' + val + '"');
        if (val = el.className) str += (' class="' + val + '"');

        result.push(str + '>');
      }

      return '#<Element [' + result.join(', ') + ']>';
    
    });

    Opal.defn(self, '$to_s', function() {
      var self = this;

      
      var val, el, result = [];

      for (var i = 0, length = self.length; i < length; i++) {
        el  = self[i];

        result.push(el.outerHTML)
      }

      return result.join(', ');
    
    });

    Opal.defn(self, '$length', function() {
      var self = this;

      return self.length;
    });

    Opal.defn(self, '$any?', function() {
      var self = this;

      return self.length > 0;
    });

    Opal.defn(self, '$empty?', function() {
      var self = this;

      return self.length === 0;
    });

    Opal.alias(self, 'empty?', 'none?');

    Opal.defn(self, '$on', TMP_7 = function(name, sel) {
      var self = this, $iter = TMP_7.$$p, block = $iter || nil;

      if (sel == null) {
        sel = nil
      }
      TMP_7.$$p = null;
      
      var wrapper = function(evt) {
        if (evt.preventDefault) {
          evt = $scope.get('Event').$new(evt);
        }

        return block.apply(null, arguments);
      };

      block._jq_wrap = wrapper;

      if (sel == nil) {
        self.on(name, wrapper);
      }
      else {
        self.on(name, sel, wrapper);
      }
    ;
      return block;
    });

    Opal.defn(self, '$one', TMP_8 = function(name, sel) {
      var self = this, $iter = TMP_8.$$p, block = $iter || nil;

      if (sel == null) {
        sel = nil
      }
      TMP_8.$$p = null;
      
      var wrapper = function(evt) {
        if (evt.preventDefault) {
          evt = $scope.get('Event').$new(evt);
        }

        return block.apply(null, arguments);
      };

      block._jq_wrap = wrapper;

      if (sel == nil) {
        self.one(name, wrapper);
      }
      else {
        self.one(name, sel, wrapper);
      }
    ;
      return block;
    });

    Opal.defn(self, '$off', function(name, sel, block) {
      var self = this;

      if (block == null) {
        block = nil
      }
      
      if (sel == null) {
        return self.off(name);
      }
      else if (block === nil) {
        return self.off(name, sel._jq_wrap);
      }
      else {
        return self.off(name, sel, block._jq_wrap);
      }
    
    });

    Opal.defn(self, '$serialize_array', function() {
      var $a, $b, TMP_9, self = this;

      return ($a = ($b = (self.serializeArray())).$map, $a.$$p = (TMP_9 = function(e){var self = TMP_9.$$s || this;
if (e == null) e = nil;
      return $scope.get('Hash').$new(e)}, TMP_9.$$s = self, TMP_9), $a).call($b);
    });

    Opal.alias(self, 'size', 'length');

    Opal.defn(self, '$value', function() {
      var self = this;

      return self.val() || "";
    });

    Opal.defn(self, '$height', function() {
      var self = this;

      return self.height() || nil;
    });

    Opal.defn(self, '$width', function() {
      var self = this;

      return self.width() || nil;
    });

    return (Opal.defn(self, '$position', function() {
      var self = this;

      return self.$Native(self.position());
    }), nil) && 'position';
  })($scope.base, $scope.get('JQUERY_CLASS').$to_n());
};

/* Generated by Opal 0.9.0 */
Opal.modules["opal/jquery/window"] = function(Opal) {
  Opal.dynamic_require_severity = "error";
  var OPAL_CONFIG = { method_missing: true, arity_check: false, freezing: true, tainting: true };
  var self = Opal.top, $scope = Opal, nil = Opal.nil, $breaker = Opal.breaker, $slice = Opal.slice, $module = Opal.module, $klass = Opal.klass, $gvars = Opal.gvars;

  Opal.add_stubs(['$require', '$include', '$find', '$on', '$to_proc', '$element', '$off', '$trigger', '$new']);
  self.$require("opal/jquery/element");
  (function($base) {
    var $Browser, self = $Browser = $module($base, 'Browser');

    var def = self.$$proto, $scope = self.$$scope;

    (function($base, $super) {
      function $Window(){};
      var self = $Window = $klass($base, $super, 'Window', $Window);

      var def = self.$$proto, $scope = self.$$scope, TMP_1, TMP_2;

      def.element = nil;
      self.$include($scope.get('Native'));

      Opal.defn(self, '$element', function() {
        var $a, self = this;

        return ((($a = self.element) !== false && $a !== nil) ? $a : self.element = $scope.get('Element').$find(window));
      });

      Opal.defn(self, '$on', TMP_1 = function() {
        var $a, $b, self = this, $iter = TMP_1.$$p, block = $iter || nil, $splat_index = nil;

        var array_size = arguments.length - 0;
        if(array_size < 0) array_size = 0;
        var args = new Array(array_size);
        for($splat_index = 0; $splat_index < array_size; $splat_index++) {
          args[$splat_index] = arguments[$splat_index + 0];
        }
        TMP_1.$$p = null;
        return ($a = ($b = self.$element()).$on, $a.$$p = block.$to_proc(), $a).apply($b, Opal.to_a(args));
      });

      Opal.defn(self, '$off', TMP_2 = function() {
        var $a, $b, self = this, $iter = TMP_2.$$p, block = $iter || nil, $splat_index = nil;

        var array_size = arguments.length - 0;
        if(array_size < 0) array_size = 0;
        var args = new Array(array_size);
        for($splat_index = 0; $splat_index < array_size; $splat_index++) {
          args[$splat_index] = arguments[$splat_index + 0];
        }
        TMP_2.$$p = null;
        return ($a = ($b = self.$element()).$off, $a.$$p = block.$to_proc(), $a).apply($b, Opal.to_a(args));
      });

      return (Opal.defn(self, '$trigger', function() {
        var $a, self = this, $splat_index = nil;

        var array_size = arguments.length - 0;
        if(array_size < 0) array_size = 0;
        var args = new Array(array_size);
        for($splat_index = 0; $splat_index < array_size; $splat_index++) {
          args[$splat_index] = arguments[$splat_index + 0];
        }
        return ($a = self.$element()).$trigger.apply($a, Opal.to_a(args));
      }), nil) && 'trigger';
    })($scope.base, null)
  })($scope.base);
  Opal.cdecl($scope, 'Window', (($scope.get('Browser')).$$scope.get('Window')).$new(window));
  return $gvars.window = $scope.get('Window');
};

/* Generated by Opal 0.9.0 */
Opal.modules["opal/jquery/document"] = function(Opal) {
  Opal.dynamic_require_severity = "error";
  var OPAL_CONFIG = { method_missing: true, arity_check: false, freezing: true, tainting: true };
  var self = Opal.top, $scope = Opal, nil = Opal.nil, $breaker = Opal.breaker, $slice = Opal.slice, $module = Opal.module, $gvars = Opal.gvars;

  Opal.add_stubs(['$require', '$to_n', '$call', '$new', '$ready?', '$resolve', '$module_function', '$find', '$extend']);
  self.$require("opal/jquery/constants");
  self.$require("opal/jquery/element");
  (function($base) {
    var $Browser, self = $Browser = $module($base, 'Browser');

    var def = self.$$proto, $scope = self.$$scope;

    (function($base) {
      var $DocumentMethods, self = $DocumentMethods = $module($base, 'DocumentMethods');

      var def = self.$$proto, $scope = self.$$scope, TMP_1, $a, $b, TMP_3;

      var $ = $scope.get('JQUERY_SELECTOR').$to_n();

      Opal.defn(self, '$ready?', TMP_1 = function() {
        var $a, $b, self = this, $iter = TMP_1.$$p, block = $iter || nil;

        TMP_1.$$p = null;
        if ((block !== nil)) {
          if ((($a = (($b = Opal.cvars['@@__isReady']) == null ? nil : $b)) !== nil && (!$a.$$is_boolean || $a == true))) {
            return block.$call()
            } else {
            return $(block);
          }
          } else {
          return nil
        };
      });

      Opal.defn(self, '$ready', function() {
        var $a, $b, TMP_2, self = this, promise = nil;

        promise = $scope.get('Promise').$new();
        ($a = ($b = $scope.get('Document'))['$ready?'], $a.$$p = (TMP_2 = function(){var self = TMP_2.$$s || this;

        return promise.$resolve()}, TMP_2.$$s = self, TMP_2), $a).call($b);
        return promise;
      });

      self.$module_function("ready?");

      ($a = ($b = self)['$ready?'], $a.$$p = (TMP_3 = function(){var self = TMP_3.$$s || this;

      return (Opal.cvars['@@__isReady'] = true)}, TMP_3.$$s = self, TMP_3), $a).call($b);

      Opal.defn(self, '$title', function() {
        var self = this;

        return document.title;
      });

      Opal.defn(self, '$title=', function(title) {
        var self = this;

        return document.title = title;
      });

      Opal.defn(self, '$head', function() {
        var self = this;

        return $scope.get('Element').$find(document.head);
      });

      Opal.defn(self, '$body', function() {
        var self = this;

        return $scope.get('Element').$find(document.body);
      });
    })($scope.base)
  })($scope.base);
  Opal.cdecl($scope, 'Document', $scope.get('Element').$find(document));
  $scope.get('Document').$extend((($scope.get('Browser')).$$scope.get('DocumentMethods')));
  return $gvars.document = $scope.get('Document');
};

/* Generated by Opal 0.9.0 */
Opal.modules["opal/jquery/event"] = function(Opal) {
  Opal.dynamic_require_severity = "error";
  var OPAL_CONFIG = { method_missing: true, arity_check: false, freezing: true, tainting: true };
  var self = Opal.top, $scope = Opal, nil = Opal.nil, $breaker = Opal.breaker, $slice = Opal.slice, $klass = Opal.klass;

  Opal.add_stubs(['$require', '$to_n', '$stop', '$prevent']);
  self.$require("opal/jquery/constants");
  return (function($base, $super) {
    function $Event(){};
    var self = $Event = $klass($base, $super, 'Event', $Event);

    var def = self.$$proto, $scope = self.$$scope;

    def["native"] = nil;
    var $ = $scope.get('JQUERY_SELECTOR').$to_n();

    Opal.defn(self, '$initialize', function(native$) {
      var self = this;

      return self["native"] = native$;
    });

    Opal.defn(self, '$to_n', function() {
      var self = this;

      return self["native"];
    });

    Opal.defn(self, '$[]', function(name) {
      var self = this;

      return self["native"][name];
    });

    Opal.defn(self, '$type', function() {
      var self = this;

      return self["native"].type;
    });

    Opal.defn(self, '$element', function() {
      var self = this;

      return $(self["native"].currentTarget);
    });

    Opal.alias(self, 'current_target', 'element');

    Opal.defn(self, '$target', function() {
      var self = this;

      return $(self["native"].target);
    });

    Opal.defn(self, '$prevented?', function() {
      var self = this;

      return self["native"].isDefaultPrevented();
    });

    Opal.defn(self, '$prevent', function() {
      var self = this;

      return self["native"].preventDefault();
    });

    Opal.defn(self, '$stopped?', function() {
      var self = this;

      return self["native"].isPropagationStopped();
    });

    Opal.defn(self, '$stop', function() {
      var self = this;

      return self["native"].stopPropagation();
    });

    Opal.defn(self, '$stop_immediate', function() {
      var self = this;

      return self["native"].stopImmediatePropagation();
    });

    Opal.defn(self, '$kill', function() {
      var self = this;

      self.$stop();
      return self.$prevent();
    });

    Opal.defn(self, '$page_x', function() {
      var self = this;

      return self["native"].pageX;
    });

    Opal.defn(self, '$page_y', function() {
      var self = this;

      return self["native"].pageY;
    });

    Opal.defn(self, '$touch_x', function() {
      var self = this;

      return self["native"].originalEvent.touches[0].pageX;
    });

    Opal.defn(self, '$touch_y', function() {
      var self = this;

      return self["native"].originalEvent.touches[0].pageY;
    });

    Opal.defn(self, '$ctrl_key', function() {
      var self = this;

      return self["native"].ctrlKey;
    });

    Opal.defn(self, '$meta_key', function() {
      var self = this;

      return self["native"].metaKey;
    });

    Opal.defn(self, '$alt_key', function() {
      var self = this;

      return self["native"].altKey;
    });

    Opal.defn(self, '$shift_key', function() {
      var self = this;

      return self["native"].shiftKey;
    });

    Opal.defn(self, '$key_code', function() {
      var self = this;

      return self["native"].keyCode;
    });

    Opal.defn(self, '$which', function() {
      var self = this;

      return self["native"].which;
    });

    Opal.alias(self, 'default_prevented?', 'prevented?');

    Opal.alias(self, 'prevent_default', 'prevent');

    Opal.alias(self, 'propagation_stopped?', 'stopped?');

    Opal.alias(self, 'stop_propagation', 'stop');

    return Opal.alias(self, 'stop_immediate_propagation', 'stop_immediate');
  })($scope.base, null);
};

/* Generated by Opal 0.9.0 */
Opal.modules["json"] = function(Opal) {
  Opal.dynamic_require_severity = "error";
  var OPAL_CONFIG = { method_missing: true, arity_check: false, freezing: true, tainting: true };
  var self = Opal.top, $scope = Opal, nil = Opal.nil, $breaker = Opal.breaker, $slice = Opal.slice, $module = Opal.module, $hash2 = Opal.hash2, $klass = Opal.klass;

  Opal.add_stubs(['$new', '$push', '$[]=', '$[]', '$create_id', '$json_create', '$attr_accessor', '$create_id=', '$===', '$parse', '$generate', '$from_object', '$merge', '$to_json', '$responds_to?', '$to_io', '$write', '$to_s', '$to_a', '$strftime']);
  (function($base) {
    var $JSON, self = $JSON = $module($base, 'JSON');

    var def = self.$$proto, $scope = self.$$scope, $a, $b;

    
    var $parse  = JSON.parse,
        $hasOwn = Opal.hasOwnProperty;

    function to_opal(value, options) {
      var klass, arr, hash, i, ii, k;

      switch (typeof value) {
        case 'string':
          return value;

        case 'number':
          return value;

        case 'boolean':
          return !!value;

        case 'null':
          return nil;

        case 'object':
          if (!value) return nil;

          if (value.$$is_array) {
            arr = (options.array_class).$new();

            for (i = 0, ii = value.length; i < ii; i++) {
              (arr).$push(to_opal(value[i], options));
            }

            return arr;
          }
          else {
            hash = (options.object_class).$new();

            for (k in value) {
              if ($hasOwn.call(value, k)) {
                (hash)['$[]='](k, to_opal(value[k], options));
              }
            }

            if (!options.parse && (klass = (hash)['$[]']($scope.get('JSON').$create_id())) != nil) {
              klass = Opal.get(klass);
              return (klass).$json_create(hash);
            }
            else {
              return hash;
            }
          }
        }
    };
  

    (function(self) {
      var $scope = self.$$scope, def = self.$$proto;

      return self.$attr_accessor("create_id")
    })(Opal.get_singleton_class(self));

    (($a = ["json_class"]), $b = self, $b['$create_id='].apply($b, $a), $a[$a.length-1]);

    Opal.defs(self, '$[]', function(value, options) {
      var $a, self = this;

      if (options == null) {
        options = $hash2([], {})
      }
      if ((($a = $scope.get('String')['$==='](value)) !== nil && (!$a.$$is_boolean || $a == true))) {
        return self.$parse(value, options)
        } else {
        return self.$generate(value, options)
      };
    });

    Opal.defs(self, '$parse', function(source, options) {
      var self = this;

      if (options == null) {
        options = $hash2([], {})
      }
      return self.$from_object($parse(source), options.$merge($hash2(["parse"], {"parse": true})));
    });

    Opal.defs(self, '$parse!', function(source, options) {
      var self = this;

      if (options == null) {
        options = $hash2([], {})
      }
      return self.$parse(source, options);
    });

    Opal.defs(self, '$load', function(source, options) {
      var self = this;

      if (options == null) {
        options = $hash2([], {})
      }
      return self.$from_object($parse(source), options);
    });

    Opal.defs(self, '$from_object', function(js_object, options) {
      var $a, $b, $c, self = this;

      if (options == null) {
        options = $hash2([], {})
      }
      ($a = "object_class", $b = options, ((($c = $b['$[]']($a)) !== false && $c !== nil) ? $c : $b['$[]=']($a, $scope.get('Hash'))));
      ($a = "array_class", $b = options, ((($c = $b['$[]']($a)) !== false && $c !== nil) ? $c : $b['$[]=']($a, $scope.get('Array'))));
      return to_opal(js_object, options.$$smap);
    });

    Opal.defs(self, '$generate', function(obj, options) {
      var self = this;

      if (options == null) {
        options = $hash2([], {})
      }
      return obj.$to_json(options);
    });

    Opal.defs(self, '$dump', function(obj, io, limit) {
      var $a, self = this, string = nil;

      if (io == null) {
        io = nil
      }
      if (limit == null) {
        limit = nil
      }
      string = self.$generate(obj);
      if (io !== false && io !== nil) {
        if ((($a = io['$responds_to?']("to_io")) !== nil && (!$a.$$is_boolean || $a == true))) {
          io = io.$to_io()};
        io.$write(string);
        return io;
        } else {
        return string
      };
    });
  })($scope.base);
  (function($base, $super) {
    function $Object(){};
    var self = $Object = $klass($base, $super, 'Object', $Object);

    var def = self.$$proto, $scope = self.$$scope;

    return (Opal.defn(self, '$to_json', function() {
      var self = this;

      return self.$to_s().$to_json();
    }), nil) && 'to_json'
  })($scope.base, null);
  (function($base) {
    var $Enumerable, self = $Enumerable = $module($base, 'Enumerable');

    var def = self.$$proto, $scope = self.$$scope;

    Opal.defn(self, '$to_json', function() {
      var self = this;

      return self.$to_a().$to_json();
    })
  })($scope.base);
  (function($base, $super) {
    function $Array(){};
    var self = $Array = $klass($base, $super, 'Array', $Array);

    var def = self.$$proto, $scope = self.$$scope;

    return (Opal.defn(self, '$to_json', function() {
      var self = this;

      
      var result = [];

      for (var i = 0, length = self.length; i < length; i++) {
        result.push((self[i]).$to_json());
      }

      return '[' + result.join(', ') + ']';
    
    }), nil) && 'to_json'
  })($scope.base, null);
  (function($base, $super) {
    function $Boolean(){};
    var self = $Boolean = $klass($base, $super, 'Boolean', $Boolean);

    var def = self.$$proto, $scope = self.$$scope;

    return (Opal.defn(self, '$to_json', function() {
      var self = this;

      return (self == true) ? 'true' : 'false';
    }), nil) && 'to_json'
  })($scope.base, null);
  (function($base, $super) {
    function $Hash(){};
    var self = $Hash = $klass($base, $super, 'Hash', $Hash);

    var def = self.$$proto, $scope = self.$$scope;

    return (Opal.defn(self, '$to_json', function() {
      var self = this;

      
      var result = [];

      for (var i = 0, keys = self.$$keys, length = keys.length, key, value; i < length; i++) {
        key = keys[i];

        if (key.$$is_string) {
          value = self.$$smap[key];
        } else {
          value = key.value;
          key = key.key;
        }

        result.push((key).$to_s().$to_json() + ':' + (value).$to_json());
      }

      return '{' + result.join(', ') + '}';
    ;
    }), nil) && 'to_json'
  })($scope.base, null);
  (function($base, $super) {
    function $NilClass(){};
    var self = $NilClass = $klass($base, $super, 'NilClass', $NilClass);

    var def = self.$$proto, $scope = self.$$scope;

    return (Opal.defn(self, '$to_json', function() {
      var self = this;

      return "null";
    }), nil) && 'to_json'
  })($scope.base, null);
  (function($base, $super) {
    function $Numeric(){};
    var self = $Numeric = $klass($base, $super, 'Numeric', $Numeric);

    var def = self.$$proto, $scope = self.$$scope;

    return (Opal.defn(self, '$to_json', function() {
      var self = this;

      return self.toString();
    }), nil) && 'to_json'
  })($scope.base, null);
  (function($base, $super) {
    function $String(){};
    var self = $String = $klass($base, $super, 'String', $String);

    var def = self.$$proto, $scope = self.$$scope;

    return Opal.alias(self, 'to_json', 'inspect')
  })($scope.base, null);
  (function($base, $super) {
    function $Time(){};
    var self = $Time = $klass($base, $super, 'Time', $Time);

    var def = self.$$proto, $scope = self.$$scope;

    return (Opal.defn(self, '$to_json', function() {
      var self = this;

      return self.$strftime("%FT%T%z").$to_json();
    }), nil) && 'to_json'
  })($scope.base, null);
  return (function($base, $super) {
    function $Date(){};
    var self = $Date = $klass($base, $super, 'Date', $Date);

    var def = self.$$proto, $scope = self.$$scope;

    Opal.defn(self, '$to_json', function() {
      var self = this;

      return self.$to_s().$to_json();
    });

    return (Opal.defn(self, '$as_json', function() {
      var self = this;

      return self.$to_s();
    }), nil) && 'as_json';
  })($scope.base, null);
};

/* Generated by Opal 0.9.0 */
Opal.modules["promise"] = function(Opal) {
  Opal.dynamic_require_severity = "error";
  var OPAL_CONFIG = { method_missing: true, arity_check: false, freezing: true, tainting: true };
  function $rb_plus(lhs, rhs) {
    return (typeof(lhs) === 'number' && typeof(rhs) === 'number') ? lhs + rhs : lhs['$+'](rhs);
  }
  function $rb_le(lhs, rhs) {
    return (typeof(lhs) === 'number' && typeof(rhs) === 'number') ? lhs <= rhs : lhs['$<='](rhs);
  }
  function $rb_minus(lhs, rhs) {
    return (typeof(lhs) === 'number' && typeof(rhs) === 'number') ? lhs - rhs : lhs['$-'](rhs);
  }
  var self = Opal.top, $scope = Opal, nil = Opal.nil, $breaker = Opal.breaker, $slice = Opal.slice, $klass = Opal.klass, $hash2 = Opal.hash2;

  Opal.add_stubs(['$resolve', '$new', '$reject', '$attr_reader', '$===', '$value', '$has_key?', '$keys', '$!', '$==', '$<<', '$>>', '$exception?', '$[]', '$resolved?', '$rejected?', '$error', '$include?', '$action', '$realized?', '$raise', '$^', '$call', '$resolve!', '$exception!', '$reject!', '$class', '$object_id', '$+', '$inspect', '$act?', '$nil?', '$prev', '$push', '$concat', '$it', '$lambda', '$reverse', '$pop', '$<=', '$length', '$shift', '$-', '$each', '$wait', '$then', '$to_proc', '$map', '$reduce', '$always', '$try', '$tap', '$all?', '$find']);
  return (function($base, $super) {
    function $Promise(){};
    var self = $Promise = $klass($base, $super, 'Promise', $Promise);

    var def = self.$$proto, $scope = self.$$scope, TMP_1, TMP_2, TMP_3, TMP_4;

    def.value = def.action = def.exception = def.realized = def.delayed = def.error = def.prev = def.next = nil;
    Opal.defs(self, '$value', function(value) {
      var self = this;

      return self.$new().$resolve(value);
    });

    Opal.defs(self, '$error', function(value) {
      var self = this;

      return self.$new().$reject(value);
    });

    Opal.defs(self, '$when', function() {
      var self = this, $splat_index = nil;

      var array_size = arguments.length - 0;
      if(array_size < 0) array_size = 0;
      var promises = new Array(array_size);
      for($splat_index = 0; $splat_index < array_size; $splat_index++) {
        promises[$splat_index] = arguments[$splat_index + 0];
      }
      return $scope.get('When').$new(promises);
    });

    self.$attr_reader("error", "prev", "next");

    Opal.defn(self, '$initialize', function(action) {
      var self = this;

      if (action == null) {
        action = $hash2([], {})
      }
      self.action = action;
      self.realized = false;
      self.exception = false;
      self.value = nil;
      self.error = nil;
      self.delayed = false;
      self.prev = nil;
      return self.next = nil;
    });

    Opal.defn(self, '$value', function() {
      var $a, self = this;

      if ((($a = $scope.get('Promise')['$==='](self.value)) !== nil && (!$a.$$is_boolean || $a == true))) {
        return self.value.$value()
        } else {
        return self.value
      };
    });

    Opal.defn(self, '$act?', function() {
      var $a, self = this;

      return ((($a = self.action['$has_key?']("success")) !== false && $a !== nil) ? $a : self.action['$has_key?']("always"));
    });

    Opal.defn(self, '$action', function() {
      var self = this;

      return self.action.$keys();
    });

    Opal.defn(self, '$exception?', function() {
      var self = this;

      return self.exception;
    });

    Opal.defn(self, '$realized?', function() {
      var self = this;

      return self.realized['$!']()['$!']();
    });

    Opal.defn(self, '$resolved?', function() {
      var self = this;

      return self.realized['$==']("resolve");
    });

    Opal.defn(self, '$rejected?', function() {
      var self = this;

      return self.realized['$==']("reject");
    });

    Opal.defn(self, '$^', function(promise) {
      var self = this;

      promise['$<<'](self);
      self['$>>'](promise);
      return promise;
    });

    Opal.defn(self, '$<<', function(promise) {
      var self = this;

      self.prev = promise;
      return self;
    });

    Opal.defn(self, '$>>', function(promise) {
      var $a, $b, $c, self = this;

      self.next = promise;
      if ((($a = self['$exception?']()) !== nil && (!$a.$$is_boolean || $a == true))) {
        promise.$reject(self.delayed['$[]'](0))
      } else if ((($a = self['$resolved?']()) !== nil && (!$a.$$is_boolean || $a == true))) {
        promise.$resolve((function() {if ((($a = self.delayed) !== nil && (!$a.$$is_boolean || $a == true))) {
          return self.delayed['$[]'](0)
          } else {
          return self.$value()
        }; return nil; })())
      } else if ((($a = self['$rejected?']()) !== nil && (!$a.$$is_boolean || $a == true))) {
        if ((($a = ((($b = self.action['$has_key?']("failure")['$!']()) !== false && $b !== nil) ? $b : $scope.get('Promise')['$==='](((function() {if ((($c = self.delayed) !== nil && (!$c.$$is_boolean || $c == true))) {
          return self.delayed['$[]'](0)
          } else {
          return self.error
        }; return nil; })())))) !== nil && (!$a.$$is_boolean || $a == true))) {
          promise.$reject((function() {if ((($a = self.delayed) !== nil && (!$a.$$is_boolean || $a == true))) {
            return self.delayed['$[]'](0)
            } else {
            return self.$error()
          }; return nil; })())
        } else if ((($a = promise.$action()['$include?']("always")) !== nil && (!$a.$$is_boolean || $a == true))) {
          promise.$reject((function() {if ((($a = self.delayed) !== nil && (!$a.$$is_boolean || $a == true))) {
            return self.delayed['$[]'](0)
            } else {
            return self.$error()
          }; return nil; })())}};
      return self;
    });

    Opal.defn(self, '$resolve', function(value) {
      var $a, $b, self = this, block = nil, e = nil;

      if (value == null) {
        value = nil
      }
      if ((($a = self['$realized?']()) !== nil && (!$a.$$is_boolean || $a == true))) {
        self.$raise($scope.get('ArgumentError'), "the promise has already been realized")};
      if ((($a = $scope.get('Promise')['$==='](value)) !== nil && (!$a.$$is_boolean || $a == true))) {
        return (value['$<<'](self.prev))['$^'](self)};
      try {
      if ((($a = block = ((($b = self.action['$[]']("success")) !== false && $b !== nil) ? $b : self.action['$[]']("always"))) !== nil && (!$a.$$is_boolean || $a == true))) {
          value = block.$call(value)};
        self['$resolve!'](value);
      } catch ($err) {if (Opal.rescue($err, [$scope.get('Exception')])) {e = $err;
        try {
          self['$exception!'](e)
        } finally {
          Opal.gvars["!"] = Opal.exceptions.pop() || Opal.nil;
        }
        }else { throw $err; }
      };
      return self;
    });

    Opal.defn(self, '$resolve!', function(value) {
      var $a, self = this;

      self.realized = "resolve";
      self.value = value;
      if ((($a = self.next) !== nil && (!$a.$$is_boolean || $a == true))) {
        return self.next.$resolve(value)
        } else {
        return self.delayed = [value]
      };
    });

    Opal.defn(self, '$reject', function(value) {
      var $a, $b, self = this, block = nil, e = nil;

      if (value == null) {
        value = nil
      }
      if ((($a = self['$realized?']()) !== nil && (!$a.$$is_boolean || $a == true))) {
        self.$raise($scope.get('ArgumentError'), "the promise has already been realized")};
      if ((($a = $scope.get('Promise')['$==='](value)) !== nil && (!$a.$$is_boolean || $a == true))) {
        return (value['$<<'](self.prev))['$^'](self)};
      try {
      if ((($a = block = ((($b = self.action['$[]']("failure")) !== false && $b !== nil) ? $b : self.action['$[]']("always"))) !== nil && (!$a.$$is_boolean || $a == true))) {
          value = block.$call(value)};
        if ((($a = self.action['$has_key?']("always")) !== nil && (!$a.$$is_boolean || $a == true))) {
          self['$resolve!'](value)
          } else {
          self['$reject!'](value)
        };
      } catch ($err) {if (Opal.rescue($err, [$scope.get('Exception')])) {e = $err;
        try {
          self['$exception!'](e)
        } finally {
          Opal.gvars["!"] = Opal.exceptions.pop() || Opal.nil;
        }
        }else { throw $err; }
      };
      return self;
    });

    Opal.defn(self, '$reject!', function(value) {
      var $a, self = this;

      self.realized = "reject";
      self.error = value;
      if ((($a = self.next) !== nil && (!$a.$$is_boolean || $a == true))) {
        return self.next.$reject(value)
        } else {
        return self.delayed = [value]
      };
    });

    Opal.defn(self, '$exception!', function(error) {
      var self = this;

      self.exception = true;
      return self['$reject!'](error);
    });

    Opal.defn(self, '$then', TMP_1 = function() {
      var $a, self = this, $iter = TMP_1.$$p, block = $iter || nil;

      TMP_1.$$p = null;
      if ((($a = self.next) !== nil && (!$a.$$is_boolean || $a == true))) {
        self.$raise($scope.get('ArgumentError'), "a promise has already been chained")};
      return self['$^']($scope.get('Promise').$new($hash2(["success"], {"success": block})));
    });

    Opal.alias(self, 'do', 'then');

    Opal.defn(self, '$fail', TMP_2 = function() {
      var $a, self = this, $iter = TMP_2.$$p, block = $iter || nil;

      TMP_2.$$p = null;
      if ((($a = self.next) !== nil && (!$a.$$is_boolean || $a == true))) {
        self.$raise($scope.get('ArgumentError'), "a promise has already been chained")};
      return self['$^']($scope.get('Promise').$new($hash2(["failure"], {"failure": block})));
    });

    Opal.alias(self, 'rescue', 'fail');

    Opal.alias(self, 'catch', 'fail');

    Opal.defn(self, '$always', TMP_3 = function() {
      var $a, self = this, $iter = TMP_3.$$p, block = $iter || nil;

      TMP_3.$$p = null;
      if ((($a = self.next) !== nil && (!$a.$$is_boolean || $a == true))) {
        self.$raise($scope.get('ArgumentError'), "a promise has already been chained")};
      return self['$^']($scope.get('Promise').$new($hash2(["always"], {"always": block})));
    });

    Opal.alias(self, 'finally', 'always');

    Opal.alias(self, 'ensure', 'always');

    Opal.defn(self, '$trace', TMP_4 = function(depth) {
      var $a, self = this, $iter = TMP_4.$$p, block = $iter || nil;

      if (depth == null) {
        depth = nil
      }
      TMP_4.$$p = null;
      if ((($a = self.next) !== nil && (!$a.$$is_boolean || $a == true))) {
        self.$raise($scope.get('ArgumentError'), "a promise has already been chained")};
      return self['$^']($scope.get('Trace').$new(depth, block));
    });

    Opal.defn(self, '$inspect', function() {
      var $a, self = this, result = nil;

      result = "#<" + (self.$class()) + "(" + (self.$object_id()) + ")";
      if ((($a = self.next) !== nil && (!$a.$$is_boolean || $a == true))) {
        result = $rb_plus(result, " >> " + (self.next.$inspect()))};
      if ((($a = self['$realized?']()) !== nil && (!$a.$$is_boolean || $a == true))) {
        result = $rb_plus(result, ": " + ((((($a = self.value) !== false && $a !== nil) ? $a : self.error)).$inspect()) + ">")
        } else {
        result = $rb_plus(result, ">")
      };
      return result;
    });

    (function($base, $super) {
      function $Trace(){};
      var self = $Trace = $klass($base, $super, 'Trace', $Trace);

      var def = self.$$proto, $scope = self.$$scope, TMP_6;

      Opal.defs(self, '$it', function(promise) {
        var $a, $b, self = this, current = nil, prev = nil;

        current = [];
        if ((($a = ((($b = promise['$act?']()) !== false && $b !== nil) ? $b : promise.$prev()['$nil?']())) !== nil && (!$a.$$is_boolean || $a == true))) {
          current.$push(promise.$value())};
        if ((($a = prev = promise.$prev()) !== nil && (!$a.$$is_boolean || $a == true))) {
          return current.$concat(self.$it(prev))
          } else {
          return current
        };
      });

      return (Opal.defn(self, '$initialize', TMP_6 = function(depth, block) {
        var $a, $b, TMP_5, self = this, $iter = TMP_6.$$p, $yield = $iter || nil;

        TMP_6.$$p = null;
        self.depth = depth;
        return Opal.find_super_dispatcher(self, 'initialize', TMP_6, null).apply(self, [$hash2(["success"], {"success": ($a = ($b = self).$lambda, $a.$$p = (TMP_5 = function(){var self = TMP_5.$$s || this, $a, $b, trace = nil;

        trace = $scope.get('Trace').$it(self).$reverse();
          trace.$pop();
          if ((($a = (($b = depth !== false && depth !== nil) ? $rb_le(depth, trace.$length()) : depth)) !== nil && (!$a.$$is_boolean || $a == true))) {
            trace.$shift($rb_minus(trace.$length(), depth))};
          return ($a = block).$call.apply($a, Opal.to_a(trace));}, TMP_5.$$s = self, TMP_5), $a).call($b)})]);
      }), nil) && 'initialize';
    })($scope.base, self);

    return (function($base, $super) {
      function $When(){};
      var self = $When = $klass($base, $super, 'When', $When);

      var def = self.$$proto, $scope = self.$$scope, TMP_7, TMP_9, TMP_11, TMP_13, TMP_17;

      def.wait = nil;
      Opal.defn(self, '$initialize', TMP_7 = function(promises) {
        var $a, $b, TMP_8, self = this, $iter = TMP_7.$$p, $yield = $iter || nil;

        if (promises == null) {
          promises = []
        }
        TMP_7.$$p = null;
        Opal.find_super_dispatcher(self, 'initialize', TMP_7, null).apply(self, []);
        self.wait = [];
        return ($a = ($b = promises).$each, $a.$$p = (TMP_8 = function(promise){var self = TMP_8.$$s || this;
if (promise == null) promise = nil;
        return self.$wait(promise)}, TMP_8.$$s = self, TMP_8), $a).call($b);
      });

      Opal.defn(self, '$each', TMP_9 = function() {
        var $a, $b, TMP_10, self = this, $iter = TMP_9.$$p, block = $iter || nil;

        TMP_9.$$p = null;
        if (block !== false && block !== nil) {
          } else {
          self.$raise($scope.get('ArgumentError'), "no block given")
        };
        return ($a = ($b = self).$then, $a.$$p = (TMP_10 = function(values){var self = TMP_10.$$s || this, $a, $b;
if (values == null) values = nil;
        return ($a = ($b = values).$each, $a.$$p = block.$to_proc(), $a).call($b)}, TMP_10.$$s = self, TMP_10), $a).call($b);
      });

      Opal.defn(self, '$collect', TMP_11 = function() {
        var $a, $b, TMP_12, self = this, $iter = TMP_11.$$p, block = $iter || nil;

        TMP_11.$$p = null;
        if (block !== false && block !== nil) {
          } else {
          self.$raise($scope.get('ArgumentError'), "no block given")
        };
        return ($a = ($b = self).$then, $a.$$p = (TMP_12 = function(values){var self = TMP_12.$$s || this, $a, $b;
if (values == null) values = nil;
        return $scope.get('When').$new(($a = ($b = values).$map, $a.$$p = block.$to_proc(), $a).call($b))}, TMP_12.$$s = self, TMP_12), $a).call($b);
      });

      Opal.defn(self, '$inject', TMP_13 = function() {
        var $a, $b, TMP_14, self = this, $iter = TMP_13.$$p, block = $iter || nil, $splat_index = nil;

        var array_size = arguments.length - 0;
        if(array_size < 0) array_size = 0;
        var args = new Array(array_size);
        for($splat_index = 0; $splat_index < array_size; $splat_index++) {
          args[$splat_index] = arguments[$splat_index + 0];
        }
        TMP_13.$$p = null;
        return ($a = ($b = self).$then, $a.$$p = (TMP_14 = function(values){var self = TMP_14.$$s || this, $a, $b;
if (values == null) values = nil;
        return ($a = ($b = values).$reduce, $a.$$p = block.$to_proc(), $a).apply($b, Opal.to_a(args))}, TMP_14.$$s = self, TMP_14), $a).call($b);
      });

      Opal.alias(self, 'map', 'collect');

      Opal.alias(self, 'reduce', 'inject');

      Opal.defn(self, '$wait', function(promise) {
        var $a, $b, TMP_15, self = this;

        if ((($a = $scope.get('Promise')['$==='](promise)) !== nil && (!$a.$$is_boolean || $a == true))) {
          } else {
          promise = $scope.get('Promise').$value(promise)
        };
        if ((($a = promise['$act?']()) !== nil && (!$a.$$is_boolean || $a == true))) {
          promise = promise.$then()};
        self.wait['$<<'](promise);
        ($a = ($b = promise).$always, $a.$$p = (TMP_15 = function(){var self = TMP_15.$$s || this, $a;
          if (self.next == null) self.next = nil;

        if ((($a = self.next) !== nil && (!$a.$$is_boolean || $a == true))) {
            return self.$try()
            } else {
            return nil
          }}, TMP_15.$$s = self, TMP_15), $a).call($b);
        return self;
      });

      Opal.alias(self, 'and', 'wait');

      Opal.defn(self, '$>>', TMP_17 = function() {
        var $a, $b, TMP_16, self = this, $iter = TMP_17.$$p, $yield = $iter || nil, $zuper = nil, $zuper_index = nil;

        TMP_17.$$p = null;
        $zuper = [];
        for($zuper_index = 0; $zuper_index < arguments.length; $zuper_index++) {
          $zuper[$zuper_index] = arguments[$zuper_index];
        }
        return ($a = ($b = Opal.find_super_dispatcher(self, '>>', TMP_17, $iter).apply(self, $zuper)).$tap, $a.$$p = (TMP_16 = function(){var self = TMP_16.$$s || this;

        return self.$try()}, TMP_16.$$s = self, TMP_16), $a).call($b);
      });

      return (Opal.defn(self, '$try', function() {
        var $a, $b, $c, $d, self = this, promise = nil;

        if ((($a = ($b = ($c = self.wait)['$all?'], $b.$$p = "realized?".$to_proc(), $b).call($c)) !== nil && (!$a.$$is_boolean || $a == true))) {
          if ((($a = promise = ($b = ($d = self.wait).$find, $b.$$p = "rejected?".$to_proc(), $b).call($d)) !== nil && (!$a.$$is_boolean || $a == true))) {
            return self.$reject(promise.$error())
            } else {
            return self.$resolve(($a = ($b = self.wait).$map, $a.$$p = "value".$to_proc(), $a).call($b))
          }
          } else {
          return nil
        };
      }), nil) && 'try';
    })($scope.base, self);
  })($scope.base, null)
};

/* Generated by Opal 0.9.0 */
Opal.modules["opal/jquery/http"] = function(Opal) {
  Opal.dynamic_require_severity = "error";
  var OPAL_CONFIG = { method_missing: true, arity_check: false, freezing: true, tainting: true };
  var self = Opal.top, $scope = Opal, nil = Opal.nil, $breaker = Opal.breaker, $slice = Opal.slice, $klass = Opal.klass, $hash2 = Opal.hash2;

  Opal.add_stubs(['$require', '$to_n', '$each', '$define_singleton_method', '$send', '$new', '$define_method', '$attr_reader', '$delete', '$update', '$upcase', '$succeed', '$fail', '$promise', '$parse', '$private', '$tap', '$proc', '$ok?', '$resolve', '$reject', '$from_object', '$call']);
  self.$require("json");
  self.$require("native");
  self.$require("promise");
  self.$require("opal/jquery/constants");
  return (function($base, $super) {
    function $HTTP(){};
    var self = $HTTP = $klass($base, $super, 'HTTP', $HTTP);

    var def = self.$$proto, $scope = self.$$scope, $a, $b, TMP_1;

    def.settings = def.payload = def.url = def.method = def.handler = def.json = def.body = def.ok = def.xhr = def.promise = def.status_code = nil;
    var $ = $scope.get('JQUERY_SELECTOR').$to_n();

    Opal.cdecl($scope, 'ACTIONS', ["get", "post", "put", "delete", "patch", "head"]);

    ($a = ($b = $scope.get('ACTIONS')).$each, $a.$$p = (TMP_1 = function(action){var self = TMP_1.$$s || this, $a, $b, TMP_2, $c, TMP_3;
if (action == null) action = nil;
    ($a = ($b = self).$define_singleton_method, $a.$$p = (TMP_2 = function(url, options){var self = TMP_2.$$s || this, block;
if (url == null) url = nil;if (options == null) options = $hash2([], {});
        block = TMP_2.$$p || nil, TMP_2.$$p = null;
      return self.$new().$send(action, url, options, block)}, TMP_2.$$s = self, TMP_2), $a).call($b, action);
      return ($a = ($c = self).$define_method, $a.$$p = (TMP_3 = function(url, options){var self = TMP_3.$$s || this, block;
if (url == null) url = nil;if (options == null) options = $hash2([], {});
        block = TMP_3.$$p || nil, TMP_3.$$p = null;
      return self.$send(action, url, options, block)}, TMP_3.$$s = self, TMP_3), $a).call($c, action);}, TMP_1.$$s = self, TMP_1), $a).call($b);

    Opal.defs(self, '$setup', function() {
      var self = this;

      return $scope.get('Hash').$new($.ajaxSetup());
    });

    Opal.defs(self, '$setup=', function(settings) {
      var self = this;

      return $.ajaxSetup(settings.$to_n());
    });

    self.$attr_reader("body", "error_message", "method", "status_code", "url", "xhr");

    Opal.defn(self, '$initialize', function() {
      var self = this;

      self.settings = $hash2([], {});
      return self.ok = true;
    });

    Opal.defn(self, '$send', function(method, url, options, block) {
      var $a, self = this, settings = nil, payload = nil;

      self.method = method;
      self.url = url;
      self.payload = options.$delete("payload");
      self.handler = block;
      self.settings.$update(options);
      $a = [self.settings.$to_n(), self.payload], settings = $a[0], payload = $a[1], $a;
      
      if (typeof(payload) === 'string') {
        settings.data = payload;
      }
      else if (payload != nil) {
        settings.data = payload.$to_json();
        settings.contentType = 'application/json';
      }

      settings.url  = self.url;
      settings.type = self.method.$upcase();

      settings.success = function(data, status, xhr) {
        return self.$succeed(data, status, xhr);
      };

      settings.error = function(xhr, status, error) {
        return self.$fail(xhr, status, error);
      };

      $.ajax(settings);
    ;
      if ((($a = self.handler) !== nil && (!$a.$$is_boolean || $a == true))) {
        return self
        } else {
        return self.$promise()
      };
    });

    Opal.defn(self, '$json', function() {
      var $a, self = this;

      return ((($a = self.json) !== false && $a !== nil) ? $a : self.json = $scope.get('JSON').$parse(self.body));
    });

    Opal.defn(self, '$ok?', function() {
      var self = this;

      return self.ok;
    });

    Opal.defn(self, '$get_header', function(key) {
      var self = this;

      return self.xhr.getResponseHeader(key);;
    });

    self.$private();

    Opal.defn(self, '$promise', function() {
      var $a, $b, TMP_4, self = this;

      if ((($a = self.promise) !== nil && (!$a.$$is_boolean || $a == true))) {
        return self.promise};
      return self.promise = ($a = ($b = $scope.get('Promise').$new()).$tap, $a.$$p = (TMP_4 = function(promise){var self = TMP_4.$$s || this, $a, $b, TMP_5;
if (promise == null) promise = nil;
      return self.handler = ($a = ($b = self).$proc, $a.$$p = (TMP_5 = function(res){var self = TMP_5.$$s || this, $a;
if (res == null) res = nil;
        if ((($a = res['$ok?']()) !== nil && (!$a.$$is_boolean || $a == true))) {
            return promise.$resolve(res)
            } else {
            return promise.$reject(res)
          }}, TMP_5.$$s = self, TMP_5), $a).call($b)}, TMP_4.$$s = self, TMP_4), $a).call($b);
    });

    Opal.defn(self, '$succeed', function(data, status, xhr) {
      var $a, self = this;

      
      self.body = data;
      self.xhr  = xhr;
      self.status_code = xhr.status;

      if (typeof(data) === 'object') {
        self.json = $scope.get('JSON').$from_object(data);
      }
    ;
      if ((($a = self.handler) !== nil && (!$a.$$is_boolean || $a == true))) {
        return self.handler.$call(self)
        } else {
        return nil
      };
    });

    return (Opal.defn(self, '$fail', function(xhr, status, error) {
      var $a, self = this;

      
      self.body = xhr.responseText;
      self.xhr = xhr;
      self.status_code = xhr.status;
    ;
      self.ok = false;
      if ((($a = self.handler) !== nil && (!$a.$$is_boolean || $a == true))) {
        return self.handler.$call(self)
        } else {
        return nil
      };
    }), nil) && 'fail';
  })($scope.base, null);
};

/* Generated by Opal 0.9.0 */
Opal.modules["opal/jquery/kernel"] = function(Opal) {
  Opal.dynamic_require_severity = "error";
  var OPAL_CONFIG = { method_missing: true, arity_check: false, freezing: true, tainting: true };
  var self = Opal.top, $scope = Opal, nil = Opal.nil, $breaker = Opal.breaker, $slice = Opal.slice, $module = Opal.module;

  return (function($base) {
    var $Kernel, self = $Kernel = $module($base, 'Kernel');

    var def = self.$$proto, $scope = self.$$scope;

    Opal.defn(self, '$alert', function(msg) {
      var self = this;

      alert(msg);
      return nil;
    })
  })($scope.base)
};

/* Generated by Opal 0.9.0 */
Opal.modules["opal/jquery"] = function(Opal) {
  Opal.dynamic_require_severity = "error";
  var OPAL_CONFIG = { method_missing: true, arity_check: false, freezing: true, tainting: true };
  var self = Opal.top, $scope = Opal, nil = Opal.nil, $breaker = Opal.breaker, $slice = Opal.slice;

  Opal.add_stubs(['$==', '$require']);
  if ($scope.get('RUBY_ENGINE')['$==']("opal")) {
    self.$require("opal/jquery/window");
    self.$require("opal/jquery/document");
    self.$require("opal/jquery/element");
    self.$require("opal/jquery/event");
    self.$require("opal/jquery/http");
    return self.$require("opal/jquery/kernel");}
};

/* Generated by Opal 0.9.0 */
Opal.modules["opal-jquery"] = function(Opal) {
  Opal.dynamic_require_severity = "error";
  var OPAL_CONFIG = { method_missing: true, arity_check: false, freezing: true, tainting: true };
  var self = Opal.top, $scope = Opal, nil = Opal.nil, $breaker = Opal.breaker, $slice = Opal.slice;

  Opal.add_stubs(['$require']);
  return self.$require("opal/jquery")
};

/* Generated by Opal 0.9.0 */
Opal.modules["vector2d/calculations"] = function(Opal) {
  Opal.dynamic_require_severity = "error";
  var OPAL_CONFIG = { method_missing: true, arity_check: false, freezing: true, tainting: true };
  function $rb_minus(lhs, rhs) {
    return (typeof(lhs) === 'number' && typeof(rhs) === 'number') ? lhs - rhs : lhs['$-'](rhs);
  }
  function $rb_times(lhs, rhs) {
    return (typeof(lhs) === 'number' && typeof(rhs) === 'number') ? lhs * rhs : lhs['$*'](rhs);
  }
  function $rb_plus(lhs, rhs) {
    return (typeof(lhs) === 'number' && typeof(rhs) === 'number') ? lhs + rhs : lhs['$+'](rhs);
  }
  var self = Opal.top, $scope = Opal, nil = Opal.nil, $breaker = Opal.breaker, $slice = Opal.slice, $klass = Opal.klass, $module = Opal.module;

  Opal.add_stubs(['$-', '$*', '$x', '$y', '$+', '$normalized?', '$normalize', '$acos', '$dot_product', '$calculate_each', '$sqrt', '$squared_distance', '$coerce', '$class', '$cross_product', '$angle_between', '$private', '$new', '$send']);
  return (function($base, $super) {
    function $Vector2d(){};
    var self = $Vector2d = $klass($base, $super, 'Vector2d', $Vector2d);

    var def = self.$$proto, $scope = self.$$scope;

    return (function($base) {
      var $Calculations, self = $Calculations = $module($base, 'Calculations');

      var def = self.$$proto, $scope = self.$$scope;

      (function($base) {
        var $ClassMethods, self = $ClassMethods = $module($base, 'ClassMethods');

        var def = self.$$proto, $scope = self.$$scope;

        Opal.defn(self, '$cross_product', function(vector1, vector2) {
          var self = this;

          return $rb_minus($rb_times(vector1.$x(), vector2.$y()), $rb_times(vector1.$y(), vector2.$x()));
        });

        Opal.defn(self, '$dot_product', function(vector1, vector2) {
          var self = this;

          return $rb_plus($rb_times(vector1.$x(), vector2.$x()), $rb_times(vector1.$y(), vector2.$y()));
        });

        Opal.defn(self, '$angle_between', function(vector1, vector2) {
          var $a, self = this, one = nil, two = nil;

          one = (function() {if ((($a = vector1['$normalized?']()) !== nil && (!$a.$$is_boolean || $a == true))) {
            return vector1
            } else {
            return vector1.$normalize()
          }; return nil; })();
          two = (function() {if ((($a = vector2['$normalized?']()) !== nil && (!$a.$$is_boolean || $a == true))) {
            return vector2
            } else {
            return vector2.$normalize()
          }; return nil; })();
          return $scope.get('Math').$acos(self.$dot_product(one, two));
        });
      })($scope.base);

      Opal.defn(self, '$*', function(other) {
        var self = this;

        return self.$calculate_each("*", other);
      });

      Opal.defn(self, '$/', function(other) {
        var self = this;

        return self.$calculate_each("/", other);
      });

      Opal.defn(self, '$+', function(other) {
        var self = this;

        return self.$calculate_each("+", other);
      });

      Opal.defn(self, '$-', function(other) {
        var self = this;

        return self.$calculate_each("-", other);
      });

      Opal.defn(self, '$distance', function(other) {
        var self = this;

        return $scope.get('Math').$sqrt(self.$squared_distance(other));
      });

      Opal.defn(self, '$squared_distance', function(other) {
        var $a, $b, self = this, v = nil, _ = nil, dx = nil, dy = nil;

        $b = self.$coerce(other), $a = Opal.to_ary($b), v = ($a[0] == null ? nil : $a[0]), _ = ($a[1] == null ? nil : $a[1]), $b;
        dx = $rb_minus(v.$x(), self.$x());
        dy = $rb_minus(v.$y(), self.$y());
        return $rb_plus($rb_times(dx, dx), $rb_times(dy, dy));
      });

      Opal.defn(self, '$dot_product', function(other) {
        var $a, $b, self = this, v = nil, _ = nil;

        $b = self.$coerce(other), $a = Opal.to_ary($b), v = ($a[0] == null ? nil : $a[0]), _ = ($a[1] == null ? nil : $a[1]), $b;
        return self.$class().$dot_product(self, v);
      });

      Opal.defn(self, '$cross_product', function(other) {
        var $a, $b, self = this, v = nil, _ = nil;

        $b = self.$coerce(other), $a = Opal.to_ary($b), v = ($a[0] == null ? nil : $a[0]), _ = ($a[1] == null ? nil : $a[1]), $b;
        return self.$class().$cross_product(self, v);
      });

      Opal.defn(self, '$angle_between', function(other) {
        var $a, $b, self = this, v = nil, _ = nil;

        $b = self.$coerce(other), $a = Opal.to_ary($b), v = ($a[0] == null ? nil : $a[0]), _ = ($a[1] == null ? nil : $a[1]), $b;
        return self.$class().$angle_between(self, v);
      });

      self.$private();

      Opal.defn(self, '$calculate_each', function(method, other) {
        var $a, $b, self = this, v = nil, _ = nil;

        $b = self.$coerce(other), $a = Opal.to_ary($b), v = ($a[0] == null ? nil : $a[0]), _ = ($a[1] == null ? nil : $a[1]), $b;
        return self.$class().$new(self.$x().$send(method, v.$x()), self.$y().$send(method, v.$y()));
      });
    })($scope.base)
  })($scope.base, null)
};

/* Generated by Opal 0.9.0 */
Opal.modules["vector2d/coercions"] = function(Opal) {
  Opal.dynamic_require_severity = "error";
  var OPAL_CONFIG = { method_missing: true, arity_check: false, freezing: true, tainting: true };
  var self = Opal.top, $scope = Opal, nil = Opal.nil, $breaker = Opal.breaker, $slice = Opal.slice, $klass = Opal.klass, $module = Opal.module, $hash2 = Opal.hash2;

  Opal.add_stubs(['$===', '$parse', '$raise', '$class', '$x', '$y', '$new', '$to_i', '$to_f']);
  return (function($base, $super) {
    function $Vector2d(){};
    var self = $Vector2d = $klass($base, $super, 'Vector2d', $Vector2d);

    var def = self.$$proto, $scope = self.$$scope;

    return (function($base) {
      var $Coercions, self = $Coercions = $module($base, 'Coercions');

      var def = self.$$proto, $scope = self.$$scope;

      Opal.defn(self, '$coerce', function(other) {
        var self = this, $case = nil;

        return (function() {$case = other;if ($scope.get('Vector2d')['$===']($case)) {return [other, self]}else if ($scope.get('Array')['$===']($case) || $scope.get('Numeric')['$===']($case) || $scope.get('String')['$===']($case) || $scope.get('Hash')['$===']($case)) {return [$scope.get('Vector2d').$parse(other), self]}else {return self.$raise($scope.get('TypeError'), "" + (self.$class()) + " can't be coerced into " + (other.$class()))}})();
      });

      Opal.defn(self, '$inspect', function() {
        var self = this;

        return "Vector2d(" + (self.$x()) + "," + (self.$y()) + ")";
      });

      Opal.defn(self, '$to_a', function() {
        var self = this;

        return [self.$x(), self.$y()];
      });

      Opal.defn(self, '$to_hash', function() {
        var self = this;

        return $hash2(["x", "y"], {"x": self.$x(), "y": self.$y()});
      });

      Opal.defn(self, '$to_i_vector', function() {
        var self = this;

        return self.$class().$new(self.$x().$to_i(), self.$y().$to_i());
      });

      Opal.defn(self, '$to_f_vector', function() {
        var self = this;

        return self.$class().$new(self.$x().$to_f(), self.$y().$to_f());
      });

      Opal.defn(self, '$to_s', function() {
        var self = this;

        return "" + (self.$x()) + "x" + (self.$y());
      });
    })($scope.base)
  })($scope.base, null)
};

/* Generated by Opal 0.9.0 */
Opal.modules["vector2d/fitting"] = function(Opal) {
  Opal.dynamic_require_severity = "error";
  var OPAL_CONFIG = { method_missing: true, arity_check: false, freezing: true, tainting: true };
  function $rb_gt(lhs, rhs) {
    return (typeof(lhs) === 'number' && typeof(rhs) === 'number') ? lhs > rhs : lhs['$>'](rhs);
  }
  function $rb_divide(lhs, rhs) {
    return (typeof(lhs) === 'number' && typeof(rhs) === 'number') ? lhs / rhs : lhs['$/'](rhs);
  }
  function $rb_times(lhs, rhs) {
    return (typeof(lhs) === 'number' && typeof(rhs) === 'number') ? lhs * rhs : lhs['$*'](rhs);
  }
  function $rb_lt(lhs, rhs) {
    return (typeof(lhs) === 'number' && typeof(rhs) === 'number') ? lhs < rhs : lhs['$<'](rhs);
  }
  var self = Opal.top, $scope = Opal, nil = Opal.nil, $breaker = Opal.breaker, $slice = Opal.slice, $klass = Opal.klass, $module = Opal.module;

  Opal.add_stubs(['$coerce', '$>', '$x', '$y', '$fit', '$/', '$to_f_vector', '$*', '$==', '$<', '$alias_method']);
  return (function($base, $super) {
    function $Vector2d(){};
    var self = $Vector2d = $klass($base, $super, 'Vector2d', $Vector2d);

    var def = self.$$proto, $scope = self.$$scope;

    return (function($base) {
      var $Fitting, self = $Fitting = $module($base, 'Fitting');

      var def = self.$$proto, $scope = self.$$scope;

      Opal.defn(self, '$contain', function(other) {
        var $a, $b, self = this, v = nil, _ = nil;

        $b = self.$coerce(other), $a = Opal.to_ary($b), v = ($a[0] == null ? nil : $a[0]), _ = ($a[1] == null ? nil : $a[1]), $b;
        if ((($a = (((($b = $rb_gt(v.$x(), self.$x())) !== false && $b !== nil) ? $b : $rb_gt(v.$y(), self.$y())))) !== nil && (!$a.$$is_boolean || $a == true))) {
          return other.$fit(self)
          } else {
          return other
        };
      });

      Opal.defn(self, '$fit', function(other) {
        var $a, $b, $c, self = this, v = nil, _ = nil, scale = nil;

        $b = self.$coerce(other), $a = Opal.to_ary($b), v = ($a[0] == null ? nil : $a[0]), _ = ($a[1] == null ? nil : $a[1]), $b;
        scale = $rb_divide(v.$to_f_vector(), self);
        return $rb_times(self, ((function() {if ((($a = (((($b = scale.$y()['$=='](0)) !== false && $b !== nil) ? $b : ((($c = $rb_gt(scale.$x(), 0)) ? $rb_lt(scale.$x(), scale.$y()) : $rb_gt(scale.$x(), 0)))))) !== nil && (!$a.$$is_boolean || $a == true))) {
          return scale.$x()
          } else {
          return scale.$y()
        }; return nil; })()));
      });

      self.$alias_method("constrain_both", "fit");

      Opal.defn(self, '$fit_either', function(other) {
        var $a, $b, self = this, v = nil, _ = nil, scale = nil;

        $b = self.$coerce(other), $a = Opal.to_ary($b), v = ($a[0] == null ? nil : $a[0]), _ = ($a[1] == null ? nil : $a[1]), $b;
        scale = $rb_divide(v.$to_f_vector(), self);
        if ((($a = ((($b = $rb_gt(scale.$x(), 0)) ? $rb_gt(scale.$y(), 0) : $rb_gt(scale.$x(), 0)))) !== nil && (!$a.$$is_boolean || $a == true))) {
          scale = (function() {if ((($a = ($rb_lt(scale.$x(), scale.$y()))) !== nil && (!$a.$$is_boolean || $a == true))) {
            return scale.$y()
            } else {
            return scale.$x()
          }; return nil; })();
          return $rb_times(self, scale);
          } else {
          return self.$fit(v)
        };
      });

      self.$alias_method("constrain_one", "fit_either");
    })($scope.base)
  })($scope.base, null)
};

/* Generated by Opal 0.9.0 */
Opal.modules["vector2d/properties"] = function(Opal) {
  Opal.dynamic_require_severity = "error";
  var OPAL_CONFIG = { method_missing: true, arity_check: false, freezing: true, tainting: true };
  function $rb_divide(lhs, rhs) {
    return (typeof(lhs) === 'number' && typeof(rhs) === 'number') ? lhs / rhs : lhs['$/'](rhs);
  }
  function $rb_plus(lhs, rhs) {
    return (typeof(lhs) === 'number' && typeof(rhs) === 'number') ? lhs + rhs : lhs['$+'](rhs);
  }
  function $rb_times(lhs, rhs) {
    return (typeof(lhs) === 'number' && typeof(rhs) === 'number') ? lhs * rhs : lhs['$*'](rhs);
  }
  var self = Opal.top, $scope = Opal, nil = Opal.nil, $breaker = Opal.breaker, $slice = Opal.slice, $klass = Opal.klass, $module = Opal.module;

  Opal.add_stubs(['$atan2', '$y', '$x', '$abs', '$/', '$to_f', '$sqrt', '$squared_length', '$+', '$*', '$==', '$length']);
  return (function($base, $super) {
    function $Vector2d(){};
    var self = $Vector2d = $klass($base, $super, 'Vector2d', $Vector2d);

    var def = self.$$proto, $scope = self.$$scope;

    return (function($base) {
      var $Properties, self = $Properties = $module($base, 'Properties');

      var def = self.$$proto, $scope = self.$$scope;

      Opal.defn(self, '$angle', function() {
        var self = this;

        return $scope.get('Math').$atan2(self.$y(), self.$x());
      });

      Opal.defn(self, '$aspect_ratio', function() {
        var self = this;

        return ($rb_divide(self.$x().$to_f(), self.$y().$to_f())).$abs();
      });

      Opal.defn(self, '$length', function() {
        var self = this;

        return $scope.get('Math').$sqrt(self.$squared_length());
      });

      Opal.defn(self, '$squared_length', function() {
        var self = this;

        return $rb_plus($rb_times(self.$x(), self.$x()), $rb_times(self.$y(), self.$y()));
      });

      Opal.defn(self, '$normalized?', function() {
        var self = this;

        return self.$length().$to_f()['$=='](1.0);
      });
    })($scope.base)
  })($scope.base, null)
};

/* Generated by Opal 0.9.0 */
Opal.modules["vector2d/transformations"] = function(Opal) {
  Opal.dynamic_require_severity = "error";
  var OPAL_CONFIG = { method_missing: true, arity_check: false, freezing: true, tainting: true };
  function $rb_times(lhs, rhs) {
    return (typeof(lhs) === 'number' && typeof(rhs) === 'number') ? lhs * rhs : lhs['$*'](rhs);
  }
  function $rb_divide(lhs, rhs) {
    return (typeof(lhs) === 'number' && typeof(rhs) === 'number') ? lhs / rhs : lhs['$/'](rhs);
  }
  function $rb_minus(lhs, rhs) {
    return (typeof(lhs) === 'number' && typeof(rhs) === 'number') ? lhs - rhs : lhs['$-'](rhs);
  }
  function $rb_plus(lhs, rhs) {
    return (typeof(lhs) === 'number' && typeof(rhs) === 'number') ? lhs + rhs : lhs['$+'](rhs);
  }
  var self = Opal.top, $scope = Opal, nil = Opal.nil, $breaker = Opal.breaker, $slice = Opal.slice, $klass = Opal.klass, $module = Opal.module;

  Opal.add_stubs(['$new', '$class', '$ceil', '$x', '$y', '$floor', '$resize', '$-@', '$*', '$/', '$length', '$-', '$cos', '$sin', '$+', '$round', '$min']);
  return (function($base, $super) {
    function $Vector2d(){};
    var self = $Vector2d = $klass($base, $super, 'Vector2d', $Vector2d);

    var def = self.$$proto, $scope = self.$$scope;

    return (function($base) {
      var $Transformations, self = $Transformations = $module($base, 'Transformations');

      var def = self.$$proto, $scope = self.$$scope;

      Opal.defn(self, '$ceil', function() {
        var self = this;

        return self.$class().$new(self.$x().$ceil(), self.$y().$ceil());
      });

      Opal.defn(self, '$floor', function() {
        var self = this;

        return self.$class().$new(self.$x().$floor(), self.$y().$floor());
      });

      Opal.defn(self, '$normalize', function() {
        var self = this;

        return self.$resize(1.0);
      });

      Opal.defn(self, '$perpendicular', function() {
        var self = this;

        return $scope.get('Vector2d').$new(self.$y()['$-@'](), self.$x());
      });

      Opal.defn(self, '$resize', function(new_length) {
        var self = this;

        return $rb_times(self, ($rb_divide(new_length, self.$length())));
      });

      Opal.defn(self, '$reverse', function() {
        var self = this;

        return self.$class().$new(self.$x()['$-@'](), self.$y()['$-@']());
      });

      Opal.defn(self, '$rotate', function(angle) {
        var self = this;

        return $scope.get('Vector2d').$new($rb_minus($rb_times(self.$x(), $scope.get('Math').$cos(angle)), $rb_times(self.$y(), $scope.get('Math').$sin(angle))), $rb_plus($rb_times(self.$x(), $scope.get('Math').$sin(angle)), $rb_times(self.$y(), $scope.get('Math').$cos(angle))));
      });

      Opal.defn(self, '$round', function(digits) {
        var self = this;

        if (digits == null) {
          digits = 0
        }
        return self.$class().$new(self.$x().$round(digits), self.$y().$round(digits));
      });

      Opal.defn(self, '$truncate', function(max) {
        var self = this;

        return self.$resize([max, self.$length()].$min());
      });
    })($scope.base)
  })($scope.base, null)
};

/* Generated by Opal 0.9.0 */
Opal.modules["vector2d/version"] = function(Opal) {
  Opal.dynamic_require_severity = "error";
  var OPAL_CONFIG = { method_missing: true, arity_check: false, freezing: true, tainting: true };
  var self = Opal.top, $scope = Opal, nil = Opal.nil, $breaker = Opal.breaker, $slice = Opal.slice, $klass = Opal.klass;

  return (function($base, $super) {
    function $Vector2d(){};
    var self = $Vector2d = $klass($base, $super, 'Vector2d', $Vector2d);

    var def = self.$$proto, $scope = self.$$scope;

    return Opal.cdecl($scope, 'VERSION', "2.1.0")
  })($scope.base, null)
};

/* Generated by Opal 0.9.0 */
Opal.modules["vector2d"] = function(Opal) {
  Opal.dynamic_require_severity = "error";
  var OPAL_CONFIG = { method_missing: true, arity_check: false, freezing: true, tainting: true };
  var self = Opal.top, $scope = Opal, nil = Opal.nil, $breaker = Opal.breaker, $slice = Opal.slice, $klass = Opal.klass;

  Opal.add_stubs(['$require', '$extend', '$include', '$nil?', '$parse_single_arg', '$new', '$private', '$===', '$parse', '$parse_str', '$parse_hash', '$dup', '$has_key?', '$[]', '$[]=', '$=~', '$map', '$to_proc', '$split', '$raise', '$attr_reader', '$x', '$y']);
  self.$require("vector2d/calculations");
  self.$require("vector2d/coercions");
  self.$require("vector2d/fitting");
  self.$require("vector2d/properties");
  self.$require("vector2d/transformations");
  self.$require("vector2d/version");
  (function($base, $super) {
    function $Vector2d(){};
    var self = $Vector2d = $klass($base, $super, 'Vector2d', $Vector2d);

    var def = self.$$proto, $scope = self.$$scope;

    self.$extend((((($scope.get('Vector2d')).$$scope.get('Calculations'))).$$scope.get('ClassMethods')));

    self.$include((($scope.get('Vector2d')).$$scope.get('Calculations')));

    self.$include((($scope.get('Vector2d')).$$scope.get('Coercions')));

    self.$include((($scope.get('Vector2d')).$$scope.get('Fitting')));

    self.$include((($scope.get('Vector2d')).$$scope.get('Properties')));

    self.$include((($scope.get('Vector2d')).$$scope.get('Transformations')));

    (function(self) {
      var $scope = self.$$scope, def = self.$$proto;

      Opal.defn(self, '$parse', function(arg, second_arg) {
        var $a, self = this;

        if (second_arg == null) {
          second_arg = nil
        }
        if ((($a = second_arg['$nil?']()) !== nil && (!$a.$$is_boolean || $a == true))) {
          return self.$parse_single_arg(arg)
          } else {
          return self.$new(arg, second_arg)
        };
      });
      self.$private();
      Opal.defn(self, '$parse_single_arg', function(arg) {
        var $a, self = this, $case = nil;

        return (function() {$case = arg;if ($scope.get('Vector2d')['$===']($case)) {return arg}else if ($scope.get('Array')['$===']($case)) {return ($a = self).$parse.apply($a, Opal.to_a(arg))}else if ($scope.get('String')['$===']($case)) {return self.$parse_str(arg)}else if ($scope.get('Hash')['$===']($case)) {return self.$parse_hash(arg.$dup())}else {return self.$new(arg, arg)}})();
      });
      Opal.defn(self, '$parse_hash', function(hash) {
        var $a, $b, $c, self = this;

        if ((($a = hash['$has_key?']("x")) !== nil && (!$a.$$is_boolean || $a == true))) {
          ($a = "x", $b = hash, ((($c = $b['$[]']($a)) !== false && $c !== nil) ? $c : $b['$[]=']($a, hash['$[]']("x"))))};
        if ((($a = hash['$has_key?']("y")) !== nil && (!$a.$$is_boolean || $a == true))) {
          ($a = "y", $b = hash, ((($c = $b['$[]']($a)) !== false && $c !== nil) ? $c : $b['$[]=']($a, hash['$[]']("y"))))};
        return self.$new(hash['$[]']("x"), hash['$[]']("y"));
      });
      return (Opal.defn(self, '$parse_str', function(str) {
        var $a, $b, $c, self = this;

        if ((($a = str['$=~'](/^[\s]*[\d\.]*[\s]*x[\s]*[\d\.]*[\s]*$/)) !== nil && (!$a.$$is_boolean || $a == true))) {
          return ($a = self).$new.apply($a, Opal.to_a(($b = ($c = str.$split("x")).$map, $b.$$p = "to_f".$to_proc(), $b).call($c)))
          } else {
          return self.$raise($scope.get('ArgumentError'), "not a valid string input")
        };
      }), nil) && 'parse_str';
    })(Opal.get_singleton_class(self));

    self.$attr_reader("x", "y");

    Opal.defn(self, '$initialize', function(x, y) {
      var $a, self = this;

      return $a = [x, y], self.x = $a[0], self.y = $a[1], $a;
    });

    return (Opal.defn(self, '$==', function(comp) {
      var $a, self = this;

      return ($a = comp.$x()['$==='](self.$x()), $a !== false && $a !== nil ?comp.$y()['$==='](self.$y()) : $a);
    }), nil) && '==';
  })($scope.base, null);
  return (Opal.defn(Opal.Object, '$Vector2d', function() {
    var $a, self = this, $splat_index = nil;

    var array_size = arguments.length - 0;
    if(array_size < 0) array_size = 0;
    var args = new Array(array_size);
    for($splat_index = 0; $splat_index < array_size; $splat_index++) {
      args[$splat_index] = arguments[$splat_index + 0];
    }
    return ($a = $scope.get('Vector2d')).$parse.apply($a, Opal.to_a(args));
  }), nil) && 'Vector2d';
};

/* Generated by Opal 0.9.0 */
Opal.modules["neatjson"] = function(Opal) {
  Opal.dynamic_require_severity = "error";
  var OPAL_CONFIG = { method_missing: true, arity_check: false, freezing: true, tainting: true };
  function $rb_times(lhs, rhs) {
    return (typeof(lhs) === 'number' && typeof(rhs) === 'number') ? lhs * rhs : lhs['$*'](rhs);
  }
  function $rb_plus(lhs, rhs) {
    return (typeof(lhs) === 'number' && typeof(rhs) === 'number') ? lhs + rhs : lhs['$+'](rhs);
  }
  function $rb_minus(lhs, rhs) {
    return (typeof(lhs) === 'number' && typeof(rhs) === 'number') ? lhs - rhs : lhs['$-'](rhs);
  }
  function $rb_le(lhs, rhs) {
    return (typeof(lhs) === 'number' && typeof(rhs) === 'number') ? lhs <= rhs : lhs['$<='](rhs);
  }
  function $rb_gt(lhs, rhs) {
    return (typeof(lhs) === 'number' && typeof(rhs) === 'number') ? lhs > rhs : lhs['$>'](rhs);
  }
  var self = Opal.top, $scope = Opal, nil = Opal.nil, $breaker = Opal.breaker, $slice = Opal.slice, $module = Opal.module, $hash2 = Opal.hash2;

  Opal.add_stubs(['$require', '$key?', '$[]=', '$==', '$[]', '$=~', '$raise', '$*', '$lambda', '$is_a?', '$first', '$last', '$keys', '$map', '$inspect', '$to_s', '$uniq', '$+', '$-', '$===', '$to_i', '$!~', '$%', '$join', '$empty?', '$!', '$<=', '$length', '$sub!', '$<<', '$sort_by', '$to_proc', '$sort', '$<=>', '$index', '$strip', '$max', '$map!', '$>', '$lstrip', '$to_json']);
  self.$require("json");
  return (function($base) {
    var $JSON, self = $JSON = $module($base, 'JSON');

    var def = self.$$proto, $scope = self.$$scope;

    Opal.defs(self, '$neat_generate', function(object, opts) {
      var $a, $b, $c, $d, $e, TMP_1, self = this, apad = nil, opad = nil, comma = nil, colon1 = nil, colonn = nil, memoizer = nil, build = nil;

      if (opts == null) {
        opts = $hash2([], {})
      }
      if ((($a = opts['$key?']("wrap")) !== nil && (!$a.$$is_boolean || $a == true))) {
        } else {
        opts['$[]=']("wrap", 80)
      };
      if (opts['$[]']("wrap")['$=='](true)) {
        opts['$[]=']("wrap", -1)};
      ($a = "indent", $b = opts, ((($c = $b['$[]']($a)) !== false && $c !== nil) ? $c : $b['$[]=']($a, "  ")));
      ($a = "array_padding", $b = opts, ((($c = $b['$[]']($a)) !== false && $c !== nil) ? $c : $b['$[]=']($a, ((($d = opts['$[]']("padding")) !== false && $d !== nil) ? $d : 0))));
      ($a = "object_padding", $b = opts, ((($c = $b['$[]']($a)) !== false && $c !== nil) ? $c : $b['$[]=']($a, ((($d = opts['$[]']("padding")) !== false && $d !== nil) ? $d : 0))));
      ($a = "after_comma", $b = opts, ((($c = $b['$[]']($a)) !== false && $c !== nil) ? $c : $b['$[]=']($a, ((($d = opts['$[]']("around_comma")) !== false && $d !== nil) ? $d : 0))));
      ($a = "before_comma", $b = opts, ((($c = $b['$[]']($a)) !== false && $c !== nil) ? $c : $b['$[]=']($a, ((($d = opts['$[]']("around_comma")) !== false && $d !== nil) ? $d : 0))));
      ($a = "before_colon", $b = opts, ((($c = $b['$[]']($a)) !== false && $c !== nil) ? $c : $b['$[]=']($a, ((($d = opts['$[]']("around_colon")) !== false && $d !== nil) ? $d : 0))));
      ($a = "after_colon", $b = opts, ((($c = $b['$[]']($a)) !== false && $c !== nil) ? $c : $b['$[]=']($a, ((($d = opts['$[]']("around_colon")) !== false && $d !== nil) ? $d : 0))));
      ($a = "before_colon_1", $b = opts, ((($c = $b['$[]']($a)) !== false && $c !== nil) ? $c : $b['$[]=']($a, ((($d = ((($e = opts['$[]']("around_colon_1")) !== false && $e !== nil) ? $e : opts['$[]']("before_colon"))) !== false && $d !== nil) ? $d : 0))));
      ($a = "after_colon_1", $b = opts, ((($c = $b['$[]']($a)) !== false && $c !== nil) ? $c : $b['$[]=']($a, ((($d = ((($e = opts['$[]']("around_colon_1")) !== false && $e !== nil) ? $e : opts['$[]']("after_colon"))) !== false && $d !== nil) ? $d : 0))));
      ($a = "before_colon_n", $b = opts, ((($c = $b['$[]']($a)) !== false && $c !== nil) ? $c : $b['$[]=']($a, ((($d = ((($e = opts['$[]']("around_colon_n")) !== false && $e !== nil) ? $e : opts['$[]']("before_colon"))) !== false && $d !== nil) ? $d : 0))));
      ($a = "after_colon_n", $b = opts, ((($c = $b['$[]']($a)) !== false && $c !== nil) ? $c : $b['$[]=']($a, ((($d = ((($e = opts['$[]']("around_colon_n")) !== false && $e !== nil) ? $e : opts['$[]']("after_colon"))) !== false && $d !== nil) ? $d : 0))));
      if ((($a = opts['$[]']("indent")['$=~'](/\S/)) !== nil && (!$a.$$is_boolean || $a == true))) {
        self.$raise(":indent option must only be whitespace")};
      apad = $rb_times(" ", opts['$[]']("array_padding"));
      opad = $rb_times(" ", opts['$[]']("object_padding"));
      comma = "" + ($rb_times(" ", opts['$[]']("before_comma"))) + "," + ($rb_times(" ", opts['$[]']("after_comma")));
      colon1 = "" + ($rb_times(" ", opts['$[]']("before_colon_1"))) + ":" + ($rb_times(" ", opts['$[]']("after_colon_1")));
      colonn = "" + ($rb_times(" ", opts['$[]']("before_colon_n"))) + ":" + ($rb_times(" ", opts['$[]']("after_colon_n")));
      memoizer = $hash2([], {});
      build = ($a = ($b = self).$lambda, $a.$$p = (TMP_1 = function(o, indent){var self = TMP_1.$$s || this, $a, $b, TMP_2, $c, $d, $e, $f, TMP_3, $g, TMP_4, $h, TMP_5, $i, TMP_6, $j, $k, TMP_7, $l, TMP_8, $m, TMP_9, $n, $o, TMP_10, $p, $q, $r, TMP_11, $s, TMP_12, $t, TMP_13, $u, $v, TMP_14, $w, $x, $y, TMP_15, $z, TMP_16, pre = nil, post = nil, okeys = nil, sortfields = nil, $case = nil, pieces = nil, one_line = nil, indent2 = nil, keyvals = nil, longest = nil;
if (o == null) o = nil;if (indent == null) indent = nil;
      if ((($a = ($b = opts['$[]']("explicit_sort"), $b !== false && $b !== nil ?o['$is_a?']($scope.get('Hash')) : $b)) !== nil && (!$a.$$is_boolean || $a == true))) {
          pre = opts['$[]']("explicit_sort").$first();
          post = opts['$[]']("explicit_sort").$last();
          okeys = o.$keys();
          sortfields = ($a = ($b = ($rb_plus($rb_plus(pre, ($rb_minus($rb_minus(okeys, pre), post))), post)).$uniq()).$map, $a.$$p = (TMP_2 = function(i){var self = TMP_2.$$s || this;
if (i == null) i = nil;
          return i.$to_s().$inspect()}, TMP_2.$$s = self, TMP_2), $a).call($b);};
        return ($a = [o, indent], $c = memoizer, ((($d = $c['$[]']($a)) !== false && $d !== nil) ? $d : $c['$[]=']($a, (function() {$case = o;if ($scope.get('String')['$===']($case) || $scope.get('Integer')['$===']($case)) {return "" + (indent) + (o.$inspect())}else if ($scope.get('Symbol')['$===']($case)) {return "" + (indent) + (o.$to_s().$inspect())}else if ($scope.get('TrueClass')['$===']($case) || $scope.get('FalseClass')['$===']($case)) {return "" + (indent) + (o)}else if ($scope.get('NilClass')['$===']($case)) {return "" + (indent) + "null"}else if ($scope.get('Float')['$===']($case)) {if ((($e = ($f = (o['$=='](o.$to_i())), $f !== false && $f !== nil ?(o.$to_s()['$!~'](/e/)) : $f)) !== nil && (!$e.$$is_boolean || $e == true))) {
          return build['$[]'](o.$to_i(), indent)
        } else if ((($e = opts['$[]']("decimals")) !== nil && (!$e.$$is_boolean || $e == true))) {
          return ((((("") + (indent)) + "%.") + (opts['$[]']("decimals"))) + "f")['$%'](o)
          } else {
          return "" + (indent) + (o)
        }}else if ($scope.get('Array')['$===']($case)) {pieces = ($e = ($f = o).$map, $e.$$p = (TMP_3 = function(v){var self = TMP_3.$$s || this;
if (v == null) v = nil;
        return build['$[]'](v, "")}, TMP_3.$$s = self, TMP_3), $e).call($f);
        one_line = "" + (indent) + "[" + (apad) + (pieces.$join(comma)) + (apad) + "]";
        if ((($e = o['$empty?']()) !== nil && (!$e.$$is_boolean || $e == true))) {
          return "" + (indent) + "[]"
        } else if ((($e = ((($g = opts['$[]']("wrap")['$!']()) !== false && $g !== nil) ? $g : ($rb_le(one_line.$length(), opts['$[]']("wrap"))))) !== nil && (!$e.$$is_boolean || $e == true))) {
          return one_line
        } else if ((($e = opts['$[]']("short")) !== nil && (!$e.$$is_boolean || $e == true))) {
          indent2 = "" + (indent) + " " + (apad);
          pieces = ($e = ($g = o).$map, $e.$$p = (TMP_4 = function(v){var self = TMP_4.$$s || this;
if (v == null) v = nil;
          return build['$[]'](v, indent2)}, TMP_4.$$s = self, TMP_4), $e).call($g);
          pieces['$[]'](0)['$sub!'](indent2, "" + (indent) + "[" + (apad));
          pieces.$last()['$<<'](apad)['$<<']("]");
          return pieces.$join(",\n");
          } else {
          indent2 = "" + (indent) + (opts['$[]']("indent"));
          return "" + (indent) + "[\n" + (($e = ($h = o).$map, $e.$$p = (TMP_5 = function(v){var self = TMP_5.$$s || this;
if (v == null) v = nil;
          return build['$[]'](v, indent2)}, TMP_5.$$s = self, TMP_5), $e).call($h).$join(",\n")) + "\n" + ((function() {if ((($e = opts['$[]']("indent_last")) !== nil && (!$e.$$is_boolean || $e == true))) {
            return indent2
            } else {
            return indent
          }; return nil; })()) + "]";
        };}else if ($scope.get('Hash')['$===']($case)) {keyvals = ($e = ($i = o).$map, $e.$$p = (TMP_6 = function(k, v){var self = TMP_6.$$s || this;
if (k == null) k = nil;if (v == null) v = nil;
        return [k.$to_s().$inspect(), build['$[]'](v, "")]}, TMP_6.$$s = self, TMP_6), $e).call($i);
        if ((($e = opts['$[]']("sorted")) !== nil && (!$e.$$is_boolean || $e == true))) {
          keyvals = ($e = ($j = keyvals).$sort_by, $e.$$p = "first".$to_proc(), $e).call($j)};
        if ((($e = opts['$[]']("explicit_sort")) !== nil && (!$e.$$is_boolean || $e == true))) {
          keyvals = ($e = ($k = keyvals).$sort, $e.$$p = (TMP_7 = function(a, b){var self = TMP_7.$$s || this;
if (a == null) a = nil;if (b == null) b = nil;
          return sortfields.$index(a.$first().$strip())['$<=>'](sortfields.$index(b.$first().$strip()))}, TMP_7.$$s = self, TMP_7), $e).call($k)};
        keyvals = ($e = ($l = keyvals).$map, $e.$$p = (TMP_8 = function(kv){var self = TMP_8.$$s || this;
if (kv == null) kv = nil;
        return kv.$join(colon1)}, TMP_8.$$s = self, TMP_8), $e).call($l).$join(comma);
        one_line = "" + (indent) + "{" + (opad) + (keyvals) + (opad) + "}";
        if ((($e = o['$empty?']()) !== nil && (!$e.$$is_boolean || $e == true))) {
          return "" + (indent) + "{}"
        } else if ((($e = ((($m = opts['$[]']("wrap")['$!']()) !== false && $m !== nil) ? $m : ($rb_le(one_line.$length(), opts['$[]']("wrap"))))) !== nil && (!$e.$$is_boolean || $e == true))) {
          return one_line
        } else if ((($e = opts['$[]']("short")) !== nil && (!$e.$$is_boolean || $e == true))) {
          keyvals = ($e = ($m = o).$map, $e.$$p = (TMP_9 = function(k, v){var self = TMP_9.$$s || this;
if (k == null) k = nil;if (v == null) v = nil;
          return ["" + (indent) + " " + (opad) + (k.$to_s().$inspect()), v]}, TMP_9.$$s = self, TMP_9), $e).call($m);
          if ((($e = opts['$[]']("sorted")) !== nil && (!$e.$$is_boolean || $e == true))) {
            keyvals = ($e = ($n = keyvals).$sort_by, $e.$$p = "first".$to_proc(), $e).call($n)};
          if ((($e = opts['$[]']("explicit_sort")) !== nil && (!$e.$$is_boolean || $e == true))) {
            keyvals = ($e = ($o = keyvals).$sort, $e.$$p = (TMP_10 = function(a, b){var self = TMP_10.$$s || this;
if (a == null) a = nil;if (b == null) b = nil;
            return sortfields.$index(a.$first().$strip())['$<=>'](sortfields.$index(b.$first().$strip()))}, TMP_10.$$s = self, TMP_10), $e).call($o)};
          keyvals['$[]'](0)['$[]'](0)['$sub!']("" + (indent) + " ", "" + (indent) + "{");
          if ((($e = opts['$[]']("aligned")) !== nil && (!$e.$$is_boolean || $e == true))) {
            longest = ($e = ($p = ($q = ($r = keyvals).$map, $q.$$p = "first".$to_proc(), $q).call($r)).$map, $e.$$p = "length".$to_proc(), $e).call($p).$max();
            keyvals = ($e = ($q = keyvals).$map, $e.$$p = (TMP_11 = function(k, v){var self = TMP_11.$$s || this;
if (k == null) k = nil;if (v == null) v = nil;
            return [((("%-") + (longest)) + "s")['$%'](k), v]}, TMP_11.$$s = self, TMP_11), $e).call($q);};
          ($e = ($s = keyvals)['$map!'], $e.$$p = (TMP_12 = function(k, v){var self = TMP_12.$$s || this, $a, $b, $c;
if (k == null) k = nil;if (v == null) v = nil;
          indent2 = $rb_times(" ", ((("") + (k)) + (colonn)).$length());
            one_line = "" + (k) + (colonn) + (build['$[]'](v, ""));
            if ((($a = ($b = ($c = opts['$[]']("wrap"), $c !== false && $c !== nil ?($rb_gt(one_line.$length(), opts['$[]']("wrap"))) : $c), $b !== false && $b !== nil ?(((($c = v['$is_a?']($scope.get('Array'))) !== false && $c !== nil) ? $c : v['$is_a?']($scope.get('Hash')))) : $b)) !== nil && (!$a.$$is_boolean || $a == true))) {
              return "" + (k) + (colonn) + (build['$[]'](v, indent2).$lstrip())
              } else {
              return one_line
            };}, TMP_12.$$s = self, TMP_12), $e).call($s);
          return keyvals.$join(",\n")['$<<'](opad)['$<<']("}");
          } else {
          keyvals = ($e = ($t = o).$map, $e.$$p = (TMP_13 = function(k, v){var self = TMP_13.$$s || this;
if (k == null) k = nil;if (v == null) v = nil;
          return ["" + (indent) + (opts['$[]']("indent")) + (k.$to_s().$inspect()), v]}, TMP_13.$$s = self, TMP_13), $e).call($t);
          if ((($e = opts['$[]']("sorted")) !== nil && (!$e.$$is_boolean || $e == true))) {
            keyvals = ($e = ($u = keyvals).$sort_by, $e.$$p = "first".$to_proc(), $e).call($u)};
          if ((($e = opts['$[]']("explicit_sort")) !== nil && (!$e.$$is_boolean || $e == true))) {
            keyvals = ($e = ($v = keyvals).$sort, $e.$$p = (TMP_14 = function(a, b){var self = TMP_14.$$s || this;
if (a == null) a = nil;if (b == null) b = nil;
            return sortfields.$index(a.$first().$strip())['$<=>'](sortfields.$index(b.$first().$strip()))}, TMP_14.$$s = self, TMP_14), $e).call($v)};
          if ((($e = opts['$[]']("aligned")) !== nil && (!$e.$$is_boolean || $e == true))) {
            longest = ($e = ($w = ($x = ($y = keyvals).$map, $x.$$p = "first".$to_proc(), $x).call($y)).$map, $e.$$p = "length".$to_proc(), $e).call($w).$max();
            keyvals = ($e = ($x = keyvals).$map, $e.$$p = (TMP_15 = function(k, v){var self = TMP_15.$$s || this;
if (k == null) k = nil;if (v == null) v = nil;
            return [((("%-") + (longest)) + "s")['$%'](k), v]}, TMP_15.$$s = self, TMP_15), $e).call($x);};
          indent2 = "" + (indent) + (opts['$[]']("indent"));
          ($e = ($z = keyvals)['$map!'], $e.$$p = (TMP_16 = function(k, v){var self = TMP_16.$$s || this, $a, $b, $c;
if (k == null) k = nil;if (v == null) v = nil;
          one_line = "" + (k) + (colonn) + (build['$[]'](v, ""));
            if ((($a = ($b = ($c = opts['$[]']("wrap"), $c !== false && $c !== nil ?($rb_gt(one_line.$length(), opts['$[]']("wrap"))) : $c), $b !== false && $b !== nil ?(((($c = v['$is_a?']($scope.get('Array'))) !== false && $c !== nil) ? $c : v['$is_a?']($scope.get('Hash')))) : $b)) !== nil && (!$a.$$is_boolean || $a == true))) {
              return "" + (k) + (colonn) + (build['$[]'](v, indent2).$lstrip())
              } else {
              return one_line
            };}, TMP_16.$$s = self, TMP_16), $e).call($z);
          return "" + (indent) + "{\n" + (keyvals.$join(",\n")) + "\n" + ((function() {if ((($e = opts['$[]']("indent_last")) !== nil && (!$e.$$is_boolean || $e == true))) {
            return indent2
            } else {
            return indent
          }; return nil; })()) + "}";
        };}else {return "" + (indent) + (o.$to_json(opts))}})())));}, TMP_1.$$s = self, TMP_1), $a).call($b);
      return build['$[]'](object, "");
    })
  })($scope.base);
};

/* Generated by Opal 0.9.0 */
Opal.modules["consolelogger"] = function(Opal) {
  Opal.dynamic_require_severity = "error";
  var OPAL_CONFIG = { method_missing: true, arity_check: false, freezing: true, tainting: true };
  function $rb_plus(lhs, rhs) {
    return (typeof(lhs) === 'number' && typeof(rhs) === 'number') ? lhs + rhs : lhs['$+'](rhs);
  }
  function $rb_le(lhs, rhs) {
    return (typeof(lhs) === 'number' && typeof(rhs) === 'number') ? lhs <= rhs : lhs['$<='](rhs);
  }
  var self = Opal.top, $scope = Opal, nil = Opal.nil, $breaker = Opal.breaker, $slice = Opal.slice, $klass = Opal.klass, $hash2 = Opal.hash2, $gvars = Opal.gvars;

  Opal.add_stubs(['$require', '$attr_reader', '$[]', '$clear_annotations', '$add_annotation', '$write', '$to_sym', '$message', '$invert', '$private', '$first', '$+', '$last', '$<<', '$<=', '$strftime', '$now', '$write_html', '$puts']);
  self.$require("opal-jquery");
  return (function($base, $super) {
    function $ConsoleLogger(){};
    var self = $ConsoleLogger = $klass($base, $super, 'ConsoleLogger', $ConsoleLogger);

    var def = self.$$proto, $scope = self.$$scope;

    def.loglevel = def.annotations = def.console = nil;
    Opal.cdecl($scope, 'LOGLEVELS', $hash2(["message", "error", "warning", "info", "debug"], {"message": 0, "error": 1, "warning": 2, "info": 3, "debug": 4}));

    Opal.cdecl($scope, 'LOGICONS', $hash2(["message", "error", "warning", "info", "debug"], {"message": "icon-info-circled", "error": "icon-error-alt", "warning": "icon-attention", "info": "icon-info-circled", "debug": "icon-minus-squared"}));

    self.$attr_reader("annotations");

    Opal.defn(self, '$initialize', function(element_id) {
      var self = this;

      self.console = element_id;
      self.loglevel = $scope.get('LOGLEVELS')['$[]']("info");
      return self.$clear_annotations();
    });

    Opal.defn(self, '$clear_annotations', function() {
      var self = this;

      return self.annotations = [];
    });

    Opal.defn(self, '$error', function(msg, start_pos, end_pos) {
      var self = this;

      if (start_pos == null) {
        start_pos = nil
      }
      if (end_pos == null) {
        end_pos = nil
      }
      self.$add_annotation(msg, start_pos, end_pos, "error");
      return self.$write("error", msg);
    });

    Opal.defn(self, '$warning', function(msg, start_pos, end_pos) {
      var self = this;

      if (start_pos == null) {
        start_pos = nil
      }
      if (end_pos == null) {
        end_pos = nil
      }
      self.$add_annotation(msg, start_pos, end_pos, "warning");
      return self.$write("warning", msg);
    });

    Opal.defn(self, '$info', function(msg, start_pos, end_pos) {
      var self = this;

      if (start_pos == null) {
        start_pos = nil
      }
      if (end_pos == null) {
        end_pos = nil
      }
      self.$add_annotation(msg, start_pos, end_pos, "info");
      return self.$write("info", msg);
    });

    Opal.defn(self, '$debug', function(msg, start_pos, end_pos) {
      var self = this;

      if (start_pos == null) {
        start_pos = nil
      }
      if (end_pos == null) {
        end_pos = nil
      }
      self.$add_annotation(msg, start_pos, end_pos, "debug");
      return self.$write("debug", msg);
    });

    Opal.defn(self, '$message', function(msg, start_pos, end_pos) {
      var self = this;

      if (start_pos == null) {
        start_pos = nil
      }
      if (end_pos == null) {
        end_pos = nil
      }
      self.$add_annotation(msg, start_pos, end_pos, "message");
      return self.$write("message", msg);
    });

    Opal.defn(self, '$loglevel=', function(level) {
      var $a, self = this;
      if ($gvars.log == null) $gvars.log = nil;

      self.loglevel = ((($a = $scope.get('LOGLEVELS')['$[]'](level.$to_sym())) !== false && $a !== nil) ? $a : $scope.get('LOGLEVELS')['$[]']("debug"));
      return $gvars.log.$message("logging messages up to " + ($scope.get('LOGLEVELS').$invert()['$[]'](self.loglevel)));
    });

    Opal.defn(self, '$loglevel', function() {
      var self = this;

      return $scope.get('LOGLEVELS').$invert()['$[]'](self.loglevel);
    });

    self.$private();

    Opal.defn(self, '$add_annotation', function(msg, start_pos, end_pos, type) {
      var $a, self = this, the_start = nil, the_end = nil;

      if (start_pos !== false && start_pos !== nil) {
        the_start = start_pos;
        the_end = ((($a = end_pos) !== false && $a !== nil) ? $a : [the_start.$first(), $rb_plus(the_start.$last(), 1)]);
        self.annotations['$<<']($hash2(["start_pos", "end_pos", "text", "type"], {"start_pos": the_start, "end_pos": the_end, "text": msg, "type": type}));};
      return nil;
    });

    return (Opal.defn(self, '$write', function(type, msg) {
      var $a, self = this, current_level = nil, time = nil;

      current_level = ((($a = $scope.get('LOGLEVELS')['$[]'](type)) !== false && $a !== nil) ? $a : $scope.get('LOGLEVELS')['$[]']("warning"));
      if ((($a = ($rb_le(current_level, self.loglevel))) !== nil && (!$a.$$is_boolean || $a == true))) {
        time = $scope.get('Time').$now().$strftime("%H:%M:%S");
        self.console.$write_html("<li class='" + (type) + "'><i class=\"" + ($scope.get('LOGICONS')['$[]'](type)) + "\"><span class='time'>" + (time) + "</span><span class='msg'>" + (msg) + "</span></li>");
        return self.$puts(msg);
        } else {
        return nil
      };
    }), nil) && 'write';
  })($scope.base, null);
};

/* Generated by Opal 0.9.0 */
Opal.modules["harpnotes"] = function(Opal) {
  Opal.dynamic_require_severity = "error";
  var OPAL_CONFIG = { method_missing: true, arity_check: false, freezing: true, tainting: true };
  function $rb_plus(lhs, rhs) {
    return (typeof(lhs) === 'number' && typeof(rhs) === 'number') ? lhs + rhs : lhs['$+'](rhs);
  }
  function $rb_minus(lhs, rhs) {
    return (typeof(lhs) === 'number' && typeof(rhs) === 'number') ? lhs - rhs : lhs['$-'](rhs);
  }
  function $rb_gt(lhs, rhs) {
    return (typeof(lhs) === 'number' && typeof(rhs) === 'number') ? lhs > rhs : lhs['$>'](rhs);
  }
  function $rb_divide(lhs, rhs) {
    return (typeof(lhs) === 'number' && typeof(rhs) === 'number') ? lhs / rhs : lhs['$/'](rhs);
  }
  function $rb_times(lhs, rhs) {
    return (typeof(lhs) === 'number' && typeof(rhs) === 'number') ? lhs * rhs : lhs['$*'](rhs);
  }
  function $rb_lt(lhs, rhs) {
    return (typeof(lhs) === 'number' && typeof(rhs) === 'number') ? lhs < rhs : lhs['$<'](rhs);
  }
  var self = Opal.top, $scope = Opal, nil = Opal.nil, $breaker = Opal.breaker, $slice = Opal.slice, $module = Opal.module, $klass = Opal.klass, $hash2 = Opal.hash2, $range = Opal.range, $hash = Opal.hash, $gvars = Opal.gvars;

  Opal.add_stubs(['$attr_accessor', '$first', '$last', '$to_json', '$[]', '$+', '$class', '$map', '$instance_variable_get', '$-', '$instance_variables', '$is_a?', '$raise', '$pitch', '$beat', '$duration', '$first_in_part', '$attr_reader', '$nil?', '$each', '$beat=', '$companion=', '$visible?', '$update_beats', '$<<', '$compact', '$flatten', '$proxy_note', '$>', '$length', '$select', '$new', '$expanded_beat_maps', '$empty?', '$reject', '$notes', '$max', '$keys', '$last_beat', '$private', '$/', '$inject', '$*', '$get', '$tuplet', '$floor', '$start_pos', '$error', '$[]=', '$index=', '$index', '$==', '$center', '$dotted', '$include', '$line_width=', '$%', '$layout_debug_grid', '$strict=', '$push', '$harpnote_options', '$warning', '$initialize', '$compute_beat_compression', '$values', '$<', '$min', '$call', '$include?', '$build_synch_points', '$layout_voice', '$each_with_index', '$voices', '$origin', '$make_sheetmark_path', '$meta_data', '$strftime', '$now', '$join', '$split', '$delete', '$to_i', '$pop', '$!', '$visible=', '$layout_playable', '$visible', '$companion', '$layout_newpart', '$reverse', '$first_in_part?', '$tuplet_start?', '$tuplet_end?', '$Vector2d', '$make_annotated_bezier_path', '$debug', '$tap', '$to_a', '$to_s', '$tie_end?', '$make_slur_path', '$tie_start?', '$slur_starts', '$slur_ends', '$policy', '$make_path_from_jumpline', '$from', '$to', '$position', '$text', '$beat_maps', '$duration_to_id', '$layout_note', '$layout_measure_start', '$layout_accord', '$layout_pause', '$check_duration', '$shift', '$-@', '$sort_by', '$x', '$y', '$normalize', '$+@', '$name', '$to_sym', '$===', '$start_pos_to_s', '$end_pos', '$rotate', '$angle', '$perpendicular']);
  return (function($base) {
    var $Harpnotes, self = $Harpnotes = $module($base, 'Harpnotes');

    var def = self.$$proto, $scope = self.$$scope;

    (function($base) {
      var $Music, self = $Music = $module($base, 'Music');

      var def = self.$$proto, $scope = self.$$scope;

      (function($base, $super) {
        function $MusicEntity(){};
        var self = $MusicEntity = $klass($base, $super, 'MusicEntity', $MusicEntity);

        var def = self.$$proto, $scope = self.$$scope;

        def.visible = def.start_pos = nil;
        self.$attr_accessor("origin", "beat", "visible", "start_pos", "end_pos");

        Opal.defn(self, '$initialize', function() {
          var self = this;

          self.visible = true;
          return self.origin = nil;
        });

        Opal.defn(self, '$visible?', function() {
          var self = this;

          return self.visible;
        });

        Opal.defn(self, '$start_pos_to_s', function() {
          var self = this;

          return "[" + (self.start_pos.$first()) + ":" + (self.start_pos.$last()) + "]";
        });

        return (Opal.defn(self, '$to_json', function() {
          var $a, $b, TMP_1, self = this;

          return $scope.get('Hash')['$[]']($rb_plus([["class", self.$class()]], ($a = ($b = ($rb_minus(self.$instance_variables(), ["@constructor", "@toString"]))).$map, $a.$$p = (TMP_1 = function(v){var self = TMP_1.$$s || this;
if (v == null) v = nil;
          return [v, self.$instance_variable_get(v)]}, TMP_1.$$s = self, TMP_1), $a).call($b))).$to_json();
        }), nil) && 'to_json';
      })($scope.base, null);

      (function($base, $super) {
        function $NonPlayable(){};
        var self = $NonPlayable = $klass($base, $super, 'NonPlayable', $NonPlayable);

        var def = self.$$proto, $scope = self.$$scope;

        def.companion = nil;
        self.$attr_accessor("companion");

        Opal.defn(self, '$companion=', function(companion) {
          var $a, self = this;

          if ((($a = companion['$is_a?']((((($scope.get('Harpnotes')).$$scope.get('Music'))).$$scope.get('Playable')))) !== nil && (!$a.$$is_boolean || $a == true))) {
            } else {
            self.$raise("Companion must be playable")
          };
          return self.companion = companion;
        });

        Opal.defn(self, '$pitch', function() {
          var self = this;

          try {return self.companion.$pitch() } catch ($err) { return nil };
        });

        Opal.defn(self, '$beat', function() {
          var self = this;

          try {return self.companion.$beat() } catch ($err) { return nil };
        });

        return (Opal.defn(self, '$duration', function() {
          var self = this;

          try {return self.companion.$duration() } catch ($err) { return nil };
        }), nil) && 'duration';
      })($scope.base, $scope.get('MusicEntity'));

      (function($base, $super) {
        function $Playable(){};
        var self = $Playable = $klass($base, $super, 'Playable', $Playable);

        var def = self.$$proto, $scope = self.$$scope, TMP_2;

        def.tie_end = def.tie_start = def.tuplet_start = def.tuplet_end = nil;
        self.$attr_accessor("first_in_part", "jump_starts", "jump_ends", "slur_starts", "slur_ends", "tie_start", "tie_end", "duration", "tuplet", "tuplet_start", "tuplet_end", "shift");

        Opal.defn(self, '$initialize', TMP_2 = function() {
          var self = this, $iter = TMP_2.$$p, $yield = $iter || nil, $zuper = nil, $zuper_index = nil;

          TMP_2.$$p = null;
          $zuper = [];
          for($zuper_index = 0; $zuper_index < arguments.length; $zuper_index++) {
            $zuper[$zuper_index] = arguments[$zuper_index];
          }
          Opal.find_super_dispatcher(self, 'initialize', TMP_2, $iter).apply(self, $zuper);
          self.slur_starts = [];
          self.slur_ends = [];
          self.tie_start = false;
          self.tie_end = false;
          self.tuplet = 1;
          self.tuplet_start = false;
          return self.tuplet_end = false;
        });

        Opal.defn(self, '$first_in_part?', function() {
          var self = this;

          return self.$first_in_part();
        });

        Opal.defn(self, '$tie_end?', function() {
          var self = this;

          return self.tie_end;
        });

        Opal.defn(self, '$tie_start?', function() {
          var self = this;

          return self.tie_start;
        });

        Opal.defn(self, '$tuplet_start?', function() {
          var self = this;

          return self.tuplet_start;
        });

        Opal.defn(self, '$tuplet_end?', function() {
          var self = this;

          return self.tuplet_end;
        });

        return (Opal.defn(self, '$proxy_note', function() {
          var self = this;

          return self;
        }), nil) && 'proxy_note';
      })($scope.base, $scope.get('MusicEntity'));

      (function($base, $super) {
        function $Note(){};
        var self = $Note = $klass($base, $super, 'Note', $Note);

        var def = self.$$proto, $scope = self.$$scope, TMP_3;

        self.$attr_reader("pitch", "duration");

        return (Opal.defn(self, '$initialize', TMP_3 = function(pitch, duration) {
          var $a, self = this, $iter = TMP_3.$$p, $yield = $iter || nil, $zuper = nil, $zuper_index = nil;

          TMP_3.$$p = null;
          $zuper = [];
          for($zuper_index = 0; $zuper_index < arguments.length; $zuper_index++) {
            $zuper[$zuper_index] = arguments[$zuper_index];
          }
          Opal.find_super_dispatcher(self, 'initialize', TMP_3, $iter).apply(self, $zuper);
          if ((($a = pitch['$nil?']()) !== nil && (!$a.$$is_boolean || $a == true))) {
            self.$raise("trying to create a note with undefined pitch")};
          self.pitch = pitch;
          return self.duration = duration;
        }), nil) && 'initialize';
      })($scope.base, $scope.get('Playable'));

      (function($base, $super) {
        function $SynchPoint(){};
        var self = $SynchPoint = $klass($base, $super, 'SynchPoint', $SynchPoint);

        var def = self.$$proto, $scope = self.$$scope, TMP_4;

        def.notes = nil;
        self.$attr_reader("notes");

        Opal.defn(self, '$initialize', TMP_4 = function(notes) {
          var $a, self = this, $iter = TMP_4.$$p, $yield = $iter || nil;

          TMP_4.$$p = null;
          Opal.find_super_dispatcher(self, 'initialize', TMP_4, null).apply(self, []);
          if ((($a = notes['$is_a?']($scope.get('Array'))) !== nil && (!$a.$$is_boolean || $a == true))) {
            } else {
            self.$raise("Notes must be an array")
          };
          return self.notes = notes;
        });

        Opal.defn(self, '$duration', function() {
          var self = this;

          return self.notes.$first().$duration();
        });

        Opal.defn(self, '$beat=', function(value) {
          var $a, $b, TMP_5, self = this;

          self.beat = value;
          return ($a = ($b = self.notes).$each, $a.$$p = (TMP_5 = function(n){var self = TMP_5.$$s || this, $a, $b;
if (n == null) n = nil;
          return (($a = [value]), $b = n, $b['$beat='].apply($b, $a), $a[$a.length-1])}, TMP_5.$$s = self, TMP_5), $a).call($b);
        });

        Opal.defn(self, '$pitch', function() {
          var self = this;

          return self.notes.$first().$pitch();
        });

        return (Opal.defn(self, '$proxy_note', function() {
          var self = this;

          return self.notes.$first();
        }), nil) && 'proxy_note';
      })($scope.base, $scope.get('Playable'));

      (function($base, $super) {
        function $Pause(){};
        var self = $Pause = $klass($base, $super, 'Pause', $Pause);

        var def = self.$$proto, $scope = self.$$scope, TMP_6;

        def.visible = nil;
        self.$attr_reader("duration", "pitch");

        Opal.defn(self, '$initialize', TMP_6 = function(pitch, duration) {
          var $a, self = this, $iter = TMP_6.$$p, $yield = $iter || nil;

          TMP_6.$$p = null;
          Opal.find_super_dispatcher(self, 'initialize', TMP_6, null).apply(self, []);
          if ((($a = pitch['$nil?']()) !== nil && (!$a.$$is_boolean || $a == true))) {
            self.$raise("trying to create a rest with undefined pitch")};
          self.pitch = pitch;
          return self.duration = duration;
        });

        Opal.defn(self, '$visible=', function(visible) {
          var self = this;

          return self.visible = visible;
        });

        return (Opal.defn(self, '$visible?', function() {
          var self = this;

          return self.visible;
        }), nil) && 'visible?';
      })($scope.base, $scope.get('Playable'));

      (function($base, $super) {
        function $MeasureStart(){};
        var self = $MeasureStart = $klass($base, $super, 'MeasureStart', $MeasureStart);

        var def = self.$$proto, $scope = self.$$scope, TMP_7;

        return (Opal.defn(self, '$initialize', TMP_7 = function(companion) {
          var $a, $b, self = this, $iter = TMP_7.$$p, $yield = $iter || nil;

          TMP_7.$$p = null;
          Opal.find_super_dispatcher(self, 'initialize', TMP_7, null).apply(self, []);
          (($a = [companion]), $b = self, $b['$companion='].apply($b, $a), $a[$a.length-1]);
          return self.visible = companion['$visible?']();
        }), nil) && 'initialize'
      })($scope.base, $scope.get('NonPlayable'));

      (function($base, $super) {
        function $NewPart(){};
        var self = $NewPart = $klass($base, $super, 'NewPart', $NewPart);

        var def = self.$$proto, $scope = self.$$scope, TMP_8;

        self.$attr_reader("name");

        return (Opal.defn(self, '$initialize', TMP_8 = function(title) {
          var self = this, $iter = TMP_8.$$p, $yield = $iter || nil;

          TMP_8.$$p = null;
          Opal.find_super_dispatcher(self, 'initialize', TMP_8, null).apply(self, []);
          return self.name = title;
        }), nil) && 'initialize';
      })($scope.base, $scope.get('NonPlayable'));

      (function($base, $super) {
        function $NoteBoundAnnotation(){};
        var self = $NoteBoundAnnotation = $klass($base, $super, 'NoteBoundAnnotation', $NoteBoundAnnotation);

        var def = self.$$proto, $scope = self.$$scope, TMP_9;

        def.annotations = nil;
        Opal.defn(self, '$initialize', TMP_9 = function(companion, annotation) {
          var $a, $b, self = this, $iter = TMP_9.$$p, $yield = $iter || nil;

          TMP_9.$$p = null;
          Opal.find_super_dispatcher(self, 'initialize', TMP_9, null).apply(self, []);
          (($a = [companion]), $b = self, $b['$companion='].apply($b, $a), $a[$a.length-1]);
          return self.annotations = annotation;
        });

        Opal.defn(self, '$text', function() {
          var self = this;

          return self.annotations['$[]']("text");
        });

        return (Opal.defn(self, '$position', function() {
          var self = this;

          return self.annotations['$[]']("pos");
        }), nil) && 'position';
      })($scope.base, $scope.get('NonPlayable'));

      (function($base, $super) {
        function $Goto(){};
        var self = $Goto = $klass($base, $super, 'Goto', $Goto);

        var def = self.$$proto, $scope = self.$$scope, TMP_10;

        self.$attr_reader("from", "to", "policy");

        return (Opal.defn(self, '$initialize', TMP_10 = function(from, to, policy) {
          var $a, self = this, $iter = TMP_10.$$p, $yield = $iter || nil, $zuper = nil, $zuper_index = nil;

          TMP_10.$$p = null;
          $zuper = [];
          for($zuper_index = 0; $zuper_index < arguments.length; $zuper_index++) {
            $zuper[$zuper_index] = arguments[$zuper_index];
          }
          Opal.find_super_dispatcher(self, 'initialize', TMP_10, $iter).apply(self, $zuper);
          if ((($a = from['$is_a?']((((($scope.get('Harpnotes')).$$scope.get('Music'))).$$scope.get('Playable')))) !== nil && (!$a.$$is_boolean || $a == true))) {
            } else {
            self.$raise("End point of Jump (" + (from.$class()) + ") must be a Playable")
          };
          if ((($a = to['$is_a?']((((($scope.get('Harpnotes')).$$scope.get('Music'))).$$scope.get('Playable')))) !== nil && (!$a.$$is_boolean || $a == true))) {
            } else {
            self.$raise("Start point of Jump (" + (to.$class()) + ") must be a Playable")
          };
          self.from = from;
          self.to = to;
          return self.policy = policy;
        }), nil) && 'initialize';
      })($scope.base, $scope.get('MusicEntity'));

      (function($base, $super) {
        function $BeatMap(){};
        var self = $BeatMap = $klass($base, $super, 'BeatMap', $BeatMap);

        var def = self.$$proto, $scope = self.$$scope;

        self.$attr_accessor("index");

        return (Opal.defn(self, '$initialize', function(index) {
          var self = this;

          return self.index = index;
        }), nil) && 'initialize';
      })($scope.base, $scope.get('Hash'));

      (function($base, $super) {
        function $Song(){};
        var self = $Song = $klass($base, $super, 'Song', $Song);

        var def = self.$$proto, $scope = self.$$scope;

        def.voices = def.beat_maps = def.meta_data = def.harpnote_options = nil;
        self.$attr_reader("voices", "beat_maps");

        self.$attr_accessor("meta_data", "harpnote_options");

        Opal.defn(self, '$to_json', function() {
          var self = this;

          return $hash2(["voices", "beat_maps", "meta_data", "harpnote_options"], {"voices": self.voices, "beat_maps": self.beat_maps, "meta_data": self.meta_data, "harpnote_options": self.harpnote_options}).$to_json();
        });

        Opal.defn(self, '$initialize', function(voices, note_length_in_beats, metadata) {
          var self = this;

          if (voices == null) {
            voices = []
          }
          if (note_length_in_beats == null) {
            note_length_in_beats = 8
          }
          if (metadata == null) {
            metadata = $hash2([], {})
          }
          self.voices = voices;
          self.note_length_in_beats = note_length_in_beats;
          self.meta_data = metadata;
          self.views = [];
          return self.$update_beats();
        });

        Opal.defn(self, '$<<', function(voice) {
          var self = this;

          self.voices['$<<'](voice);
          return self.$update_beats();
        });

        Opal.defn(self, '$build_synch_points', function(selector) {
          var $a, $b, TMP_11, $c, TMP_14, self = this, syncpoints = nil;

          if (selector == null) {
            selector = nil
          }
          syncpoints = ($a = ($b = self.$expanded_beat_maps()).$map, $a.$$p = (TMP_11 = function(playables){var self = TMP_11.$$s || this, $a, $b, TMP_12, $c, TMP_13, the_playables = nil, result = nil;
if (playables == null) playables = nil;
          if (selector !== false && selector !== nil) {
              playables = [playables['$[]'](selector.$first()), playables['$[]'](selector.$last())]};
            the_playables = ($a = ($b = playables).$map, $a.$$p = (TMP_12 = function(p){var self = TMP_12.$$s || this, $a, r = nil;
if (p == null) p = nil;
            if ((($a = p['$is_a?']($scope.get('Playable'))) !== nil && (!$a.$$is_boolean || $a == true))) {
                r = p.$proxy_note()};
              return r;}, TMP_12.$$s = self, TMP_12), $a).call($b);
            result = nil;
            if ($rb_gt(($a = ($c = the_playables.$compact()).$select, $a.$$p = (TMP_13 = function(p){var self = TMP_13.$$s || this;
if (p == null) p = nil;
            return p['$visible?']()}, TMP_13.$$s = self, TMP_13), $a).call($c).$length(), 1)) {
              result = $scope.get('SynchPoint').$new(the_playables)};
            return result;}, TMP_11.$$s = self, TMP_11), $a).call($b).$flatten().$compact();
          syncpoints = ($a = ($c = syncpoints).$select, $a.$$p = (TMP_14 = function(sp){var self = TMP_14.$$s || this, $a, $b, TMP_15;
if (sp == null) sp = nil;
          return ($a = ($b = sp.$notes()).$reject, $a.$$p = (TMP_15 = function(e){var self = TMP_15.$$s || this;
if (e == null) e = nil;
            return e['$is_a?']($scope.get('Playable'))}, TMP_15.$$s = self, TMP_15), $a).call($b)['$empty?']()}, TMP_14.$$s = self, TMP_14), $a).call($c);
          return syncpoints;
        });

        Opal.defn(self, '$last_beat', function() {
          var $a, $b, TMP_16, self = this, max_beat = nil;

          return max_beat = ($a = ($b = self.beat_maps).$map, $a.$$p = (TMP_16 = function(map){var self = TMP_16.$$s || this;
if (map == null) map = nil;
          return map.$keys().$max()}, TMP_16.$$s = self, TMP_16), $a).call($b).$max();
        });

        Opal.defn(self, '$expanded_beat_maps', function() {
          var $a, $b, TMP_17, self = this;

          return ($a = ($b = ($range(0, self.$last_beat(), false))).$map, $a.$$p = (TMP_17 = function(beat){var self = TMP_17.$$s || this, $a, $b, TMP_18;
            if (self.beat_maps == null) self.beat_maps = nil;
if (beat == null) beat = nil;
          return ($a = ($b = self.beat_maps).$map, $a.$$p = (TMP_18 = function(map){var self = TMP_18.$$s || this;
if (map == null) map = nil;
            return map['$[]'](beat)}, TMP_18.$$s = self, TMP_18), $a).call($b)}, TMP_17.$$s = self, TMP_17), $a).call($b);
        });

        self.$private();

        return (Opal.defn(self, '$update_beats', function() {
          var $a, $b, TMP_19, self = this, tupletmap = nil;

          tupletmap = $hash(1, 1, 2, $rb_divide(3, 2), 3, $rb_divide(2, 3), 4, $rb_divide(3, 4), 5, $rb_divide(2, 5), 6, $rb_divide(2, 6), 7, $rb_divide(2, 7), 8, $rb_divide(3, 8), 9, $rb_divide(2, 9));
          self.beat_maps = ($a = ($b = self.voices).$map, $a.$$p = (TMP_19 = function(voice){var self = TMP_19.$$s || this, $a, $b, TMP_20, $c, $d, TMP_21, current_beat = nil, voice_map = nil;
if (voice == null) voice = nil;
          current_beat = 0;
            voice_map = ($a = ($b = ($c = ($d = voice).$select, $c.$$p = (TMP_21 = function(e){var self = TMP_21.$$s || this;
if (e == null) e = nil;
            return e['$is_a?']($scope.get('Playable'))}, TMP_21.$$s = self, TMP_21), $c).call($d)).$inject, $a.$$p = (TMP_20 = function(map, playable){var self = TMP_20.$$s || this, $a, $b, beats = nil, beat_error = nil, pos = nil;
              if ($gvars.conf == null) $gvars.conf = nil;
              if ($gvars.log == null) $gvars.log = nil;
if (map == null) map = nil;if (playable == null) playable = nil;
            beats = $rb_times(playable.$duration(), $gvars.conf.$get("layout.BEAT_PER_DURATION"));
              beats = $rb_times(beats, tupletmap['$[]'](playable.$tuplet()));
              beat_error = $rb_minus(beats, beats.$floor(0));
              if ($rb_gt(beat_error, 0)) {
                pos = playable.$start_pos();
                $gvars.log.$error("unsupported tuplet " + (playable.$tuplet()) + " " + (beat_error), pos);
                beats = beats.$floor(0);};
              map['$[]='](current_beat, playable);
              (($a = [current_beat]), $b = playable, $b['$beat='].apply($b, $a), $a[$a.length-1]);
              current_beat = $rb_plus(current_beat, beats);
              (($a = [voice.$index()]), $b = map, $b['$index='].apply($b, $a), $a[$a.length-1]);
              return map;}, TMP_20.$$s = self, TMP_20), $a).call($b, $scope.get('BeatMap').$new(voice.$index()));
            return voice_map;}, TMP_19.$$s = self, TMP_19), $a).call($b);
          return nil;
        }), nil) && 'update_beats';
      })($scope.base, null);

      (function($base, $super) {
        function $Voice(){};
        var self = $Voice = $klass($base, $super, 'Voice', $Voice);

        var def = self.$$proto, $scope = self.$$scope, TMP_22;

        def.show_voice = def.show_flowline = def.show_jumpline = nil;
        self.$attr_accessor("index", "name", "show_voice", "show_flowline", "show_jumpline");

        Opal.defn(self, '$initialize', TMP_22 = function() {
          var self = this, $iter = TMP_22.$$p, $yield = $iter || nil, $zuper = nil, $zuper_index = nil;

          TMP_22.$$p = null;
          $zuper = [];
          for($zuper_index = 0; $zuper_index < arguments.length; $zuper_index++) {
            $zuper[$zuper_index] = arguments[$zuper_index];
          }
          self.show_voice = true;
          self.show_flowline = true;
          self.show_jumpline = true;
          return Opal.find_super_dispatcher(self, 'initialize', TMP_22, $iter).apply(self, $zuper);
        });

        Opal.defn(self, '$show_voice?', function() {
          var self = this;

          return self.show_voice['$=='](true);
        });

        Opal.defn(self, '$show_flowline?', function() {
          var self = this;

          return self.show_flowline['$=='](true);
        });

        return (Opal.defn(self, '$show_jumpline?', function() {
          var self = this;

          return self.show_jumpline['$=='](true);
        }), nil) && 'show_jumpline?';
      })($scope.base, $scope.get('Array'));
    })($scope.base);

    (function($base) {
      var $Drawing, self = $Drawing = $module($base, 'Drawing');

      var def = self.$$proto, $scope = self.$$scope;

      (function($base, $super) {
        function $Sheet(){};
        var self = $Sheet = $klass($base, $super, 'Sheet', $Sheet);

        var def = self.$$proto, $scope = self.$$scope;

        self.$attr_reader("children", "vertical_scale");

        return (Opal.defn(self, '$initialize', function(children, vertical_scale) {
          var self = this;

          if (children == null) {
            children = []
          }
          if (vertical_scale == null) {
            vertical_scale = 1.0
          }
          self.children = children;
          return self.vertical_scale = vertical_scale;
        }), nil) && 'initialize';
      })($scope.base, null);

      (function($base, $super) {
        function $Drawable(){};
        var self = $Drawable = $klass($base, $super, 'Drawable', $Drawable);

        var def = self.$$proto, $scope = self.$$scope;

        def.visible = def.line_width = def.color = nil;
        self.$attr_accessor("conf_key");

        Opal.defn(self, '$initialize', function() {
          var self = this;
          if ($gvars.conf == null) $gvars.conf = nil;

          self.visible = true;
          self.line_width = $gvars.conf.$get("layout.LINE_THIN");
          return self.conf_key = nil;
        });

        Opal.defn(self, '$center', function() {
          var self = this;

          return self.$raise("center not implemented for " + (self.$class()));
        });

        Opal.defn(self, '$visible?', function() {
          var self = this;

          return self.visible;
        });

        Opal.defn(self, '$visible=', function(v) {
          var self = this;

          return self.visible = v;
        });

        Opal.defn(self, '$line_width=', function(v) {
          var self = this;

          return self.line_width = v;
        });

        Opal.defn(self, '$line_width', function() {
          var self = this;

          return self.line_width;
        });

        Opal.defn(self, '$color=', function(v) {
          var self = this;

          return self.color = v;
        });

        return (Opal.defn(self, '$color', function() {
          var self = this;

          return self.color;
        }), nil) && 'color';
      })($scope.base, null);

      (function($base, $super) {
        function $Symbol(){};
        var self = $Symbol = $klass($base, $super, 'Symbol', $Symbol);

        var def = self.$$proto, $scope = self.$$scope, TMP_23;

        return (Opal.defn(self, '$iniitalize', TMP_23 = function() {
          var self = this, $iter = TMP_23.$$p, $yield = $iter || nil, $zuper = nil, $zuper_index = nil;

          TMP_23.$$p = null;
          $zuper = [];
          for($zuper_index = 0; $zuper_index < arguments.length; $zuper_index++) {
            $zuper[$zuper_index] = arguments[$zuper_index];
          }
          return Opal.find_super_dispatcher(self, 'iniitalize', TMP_23, $iter).apply(self, $zuper);
        }), nil) && 'iniitalize'
      })($scope.base, $scope.get('Drawable'));

      (function($base, $super) {
        function $FlowLine(){};
        var self = $FlowLine = $klass($base, $super, 'FlowLine', $FlowLine);

        var def = self.$$proto, $scope = self.$$scope, TMP_24;

        def.style = nil;
        self.$attr_reader("from", "to", "style", "origin", "center");

        Opal.defn(self, '$initialize', TMP_24 = function(from, to, style, origin, center) {
          var $a, self = this, $iter = TMP_24.$$p, $yield = $iter || nil, $zuper = nil, $zuper_index = nil;

          if (style == null) {
            style = "solid"
          }
          if (origin == null) {
            origin = nil
          }
          if (center == null) {
            center = nil
          }
          TMP_24.$$p = null;
          $zuper = [];
          for($zuper_index = 0; $zuper_index < arguments.length; $zuper_index++) {
            $zuper[$zuper_index] = arguments[$zuper_index];
          }
          Opal.find_super_dispatcher(self, 'initialize', TMP_24, $iter).apply(self, $zuper);
          self.from = from;
          self.to = to;
          self.style = style;
          self.origin = origin;
          return self.center = ((($a = center) !== false && $a !== nil) ? $a : to.$center());
        });

        Opal.defn(self, '$dashed?', function() {
          var self = this;

          return self.style['$==']("dashed");
        });

        return (Opal.defn(self, '$dotted?', function() {
          var self = this;

          return self.style['$==']("dotted");
        }), nil) && 'dotted?';
      })($scope.base, $scope.get('Drawable'));

      (function($base, $super) {
        function $Path(){};
        var self = $Path = $klass($base, $super, 'Path', $Path);

        var def = self.$$proto, $scope = self.$$scope, TMP_25;

        def.fill = nil;
        self.$attr_reader("path", "style");

        Opal.defn(self, '$initialize', TMP_25 = function(path, fill, origin) {
          var self = this, $iter = TMP_25.$$p, $yield = $iter || nil, $zuper = nil, $zuper_index = nil;

          if (fill == null) {
            fill = nil
          }
          if (origin == null) {
            origin = nil
          }
          TMP_25.$$p = null;
          $zuper = [];
          for($zuper_index = 0; $zuper_index < arguments.length; $zuper_index++) {
            $zuper[$zuper_index] = arguments[$zuper_index];
          }
          Opal.find_super_dispatcher(self, 'initialize', TMP_25, $iter).apply(self, $zuper);
          self.path = path;
          self.fill = fill;
          return self.origin = origin;
        });

        return (Opal.defn(self, '$filled?', function() {
          var self = this;

          return self.fill['$==']("filled");
        }), nil) && 'filled?';
      })($scope.base, $scope.get('Drawable'));

      (function($base, $super) {
        function $Ellipse(){};
        var self = $Ellipse = $klass($base, $super, 'Ellipse', $Ellipse);

        var def = self.$$proto, $scope = self.$$scope, TMP_26;

        def.size = def.fill = def.rect = nil;
        self.$attr_reader("center", "size", "fill", "dotted", "origin");

        Opal.defn(self, '$initialize', TMP_26 = function(center, radii, fill, dotted, origin, rect) {
          var self = this, $iter = TMP_26.$$p, $yield = $iter || nil, $zuper = nil, $zuper_index = nil;

          if (fill == null) {
            fill = "filled"
          }
          if (dotted == null) {
            dotted = false
          }
          if (origin == null) {
            origin = nil
          }
          if (rect == null) {
            rect = false
          }
          TMP_26.$$p = null;
          $zuper = [];
          for($zuper_index = 0; $zuper_index < arguments.length; $zuper_index++) {
            $zuper[$zuper_index] = arguments[$zuper_index];
          }
          Opal.find_super_dispatcher(self, 'initialize', TMP_26, $iter).apply(self, $zuper);
          self.center = center;
          self.size = radii;
          self.fill = fill;
          self.dotted = dotted;
          self.rect = rect;
          return self.origin = origin;
        });

        Opal.defn(self, '$height', function() {
          var self = this;

          return self.size.$last();
        });

        Opal.defn(self, '$dotted?', function() {
          var self = this;

          return self.$dotted();
        });

        Opal.defn(self, '$filled?', function() {
          var self = this;

          return self.fill['$==']("filled");
        });

        return (Opal.defn(self, '$rect?', function() {
          var self = this;

          return self.rect['$=='](true);
        }), nil) && 'rect?';
      })($scope.base, $scope.get('Symbol'));

      (function($base, $super) {
        function $Annotation(){};
        var self = $Annotation = $klass($base, $super, 'Annotation', $Annotation);

        var def = self.$$proto, $scope = self.$$scope, TMP_27;

        self.$attr_reader("center", "text", "style", "origin", "conf_key");

        return (Opal.defn(self, '$initialize', TMP_27 = function(center, text, style, origin, conf_key) {
          var self = this, $iter = TMP_27.$$p, $yield = $iter || nil;

          if (style == null) {
            style = "regular"
          }
          if (origin == null) {
            origin = nil
          }
          if (conf_key == null) {
            conf_key = nil
          }
          TMP_27.$$p = null;
          Opal.find_super_dispatcher(self, 'initialize', TMP_27, null).apply(self, []);
          self.center = center;
          self.text = text;
          self.style = style;
          self.origin = origin;
          return self.conf_key = conf_key;
        }), nil) && 'initialize';
      })($scope.base, $scope.get('Drawable'));

      (function($base, $super) {
        function $Glyph(){};
        var self = $Glyph = $klass($base, $super, 'Glyph', $Glyph);

        var def = self.$$proto, $scope = self.$$scope, TMP_28;

        def.size = def.fill = nil;
        self.$attr_reader("center", "size", "glyph", "dotted", "origin");

        Opal.cdecl($scope, 'GLYPHS', $hash2(["rest_0", "rest_1", "rest_4", "rest_8", "rest_16", "rest_32", "rest_64", "rest_128"], {"rest_0": $hash2(["d", "w", "h"], {"d": [["M", $rb_minus($rb_minus(0.06, 0.06), $rb_divide(11.25, 2)), $rb_minus(0.03, $rb_times(1.7, 4.68))], ["l", 0.09, -0.06], ["l", 5.46, 0], ["l", 5.49, 0], ["l", 0.09, 0.06], ["l", 0.06, 0.09], ["l", 0, 2.19], ["l", 0, 2.19], ["l", -0.06, 0.09], ["l", -0.09, 0.06], ["l", -5.49, 0], ["l", -5.46, 0], ["l", -0.09, -0.06], ["l", -0.06, -0.09], ["l", 0, -2.19], ["l", 0, -2.19], ["z"]], "w": $rb_times(2, 11.25), "h": $rb_times(2.2, 4.68)}), "rest_1": $hash2(["d", "w", "h"], {"d": [["M", -10, -5], ["l", 20, 0], ["l", 0, 10], ["l", -20, 0], ["l", 0, -10], ["z"]], "w": 20, "h": 10}), "rest_4": $hash2(["d", "w", "h"], {"d": [["M", 5.0, -15], ["c", 0.12, -0.06, 0.24, -0.06, 0.36, -0.03], ["c", 0.09, 0.06, 4.74, 5.58, 4.86, 5.82], ["c", 0.21, 0.39, 0.15, 0.78, -0.15, 1.26], ["c", -0.24, 0.33, -0.72, 0.81, -1.62, 1.56], ["c", -0.45, 0.36, -0.87, 0.75, -0.96, 0.84], ["c", -0.93, 0.99, -1.14, 2.49, -0.6, 3.63], ["c", 0.18, 0.39, 0.27, 0.48, 1.32, 1.68], ["c", 1.92, 2.25, 1.83, 2.16, 1.83, 2.34], ["c", 0, 0.18, -0.18, 0.36, -0.36, 0.39], ["c", -0.15, 0, -0.27, -0.06, -0.48, -0.27], ["c", -0.75, -0.75, -2.46, -1.29, -3.39, -1.08], ["c", -0.45, 0.09, -0.69, 0.27, -0.9, 0.69], ["c", -0.12, 0.3, -0.21, 0.66, -0.24, 1.14], ["c", -0.03, 0.66, 0.09, 1.35, 0.3, 2.01], ["c", 0.15, 0.42, 0.24, 0.66, 0.45, 0.96], ["c", 0.18, 0.24, 0.18, 0.33, 0.03, 0.42], ["c", -0.12, 0.06, -0.18, 0.03, -0.45, -0.3], ["c", -1.08, -1.38, -2.07, -3.36, -2.4, -4.83], ["c", -0.27, -1.05, -0.15, -1.77, 0.27, -2.07], ["c", 0.21, -0.12, 0.42, -0.15, 0.87, -0.15], ["c", 0.87, 0.06, 2.1, 0.39, 3.3, 0.9], ["l", 0.39, 0.18], ["l", -1.65, -1.95], ["c", -2.52, -2.97, -2.61, -3.09, -2.7, -3.27], ["c", -0.09, -0.24, -0.12, -0.48, -0.03, -0.75], ["c", 0.15, -0.48, 0.57, -0.96, 1.83, -2.01], ["c", 0.45, -0.36, 0.84, -0.72, 0.93, -0.78], ["c", 0.69, -0.75, 1.02, -1.8, 0.9, -2.79], ["c", -0.06, -0.33, -0.21, -0.84, -0.39, -1.11], ["c", -0.09, -0.15, -0.45, -0.6, -0.81, -1.05], ["c", -0.36, -0.42, -0.69, -0.81, -0.72, -0.87], ["c", -0.09, -0.18, 0, -0.42, 0.21, -0.51], ["z"]], "w": 7.9, "h": 25.5}), "rest_8": $hash2(["d", "w", "h"], {"d": [["M", 5, -20], ["c", 0.66, -0.09, 1.23, 0.09, 1.68, 0.51], ["c", 0.27, 0.3, 0.39, 0.54, 0.57, 1.26], ["c", 0.09, 0.33, 0.18, 0.66, 0.21, 0.72], ["c", 0.12, 0.27, 0.33, 0.45, 0.6, 0.48], ["c", 0.12, 0, 0.18, 0, 0.33, -0.09], ["c", 0.39, -0.18, 1.32, -1.29, 1.68, -1.98], ["c", 0.09, -0.21, 0.24, -0.3, 0.39, -0.3], ["c", 0.12, 0, 0.27, 0.09, 0.33, 0.18], ["c", 0.03, 0.06, -0.27, 1.11, -1.86, 6.42], ["c", -1.02, 3.48, -1.89, 6.39, -1.92, 6.42], ["c", 0, 0.03, -0.12, 0.12, -0.24, 0.15], ["c", -0.18, 0.09, -0.21, 0.09, -0.45, 0.09], ["c", -0.24, 0, -0.3, 0, -0.48, -0.06], ["c", -0.09, -0.06, -0.21, -0.12, -0.21, -0.15], ["c", -0.06, -0.03, 0.15, -0.57, 1.68, -4.92], ["c", 0.96, -2.67, 1.74, -4.89, 1.71, -4.89], ["l", -0.51, 0.15], ["c", -1.08, 0.36, -1.74, 0.48, -2.55, 0.48], ["c", -0.66, 0, -0.84, -0.03, -1.32, -0.27], ["c", -1.32, -0.63, -1.77, -2.16, -1.02, -3.3], ["c", 0.33, -0.45, 0.84, -0.81, 1.38, -0.9], ["z"]], "w": 7.9, "h": 25.5}), "rest_16": $hash2(["d", "w", "h"], {"d": [["M", 3.33, -6.12], ["c", 0.66, -0.09, 1.23, 0.09, 1.68, 0.51], ["c", 0.27, 0.3, 0.39, 0.54, 0.57, 1.26], ["c", 0.09, 0.33, 0.18, 0.66, 0.21, 0.72], ["c", 0.15, 0.39, 0.57, 0.57, 0.87, 0.42], ["c", 0.39, -0.18, 1.2, -1.23, 1.62, -2.07], ["c", 0.06, -0.15, 0.24, -0.24, 0.36, -0.24], ["c", 0.12, 0, 0.27, 0.09, 0.33, 0.18], ["c", 0.03, 0.06, -0.45, 1.86, -2.67, 10.17], ["c", -1.5, 5.55, -2.73, 10.14, -2.76, 10.17], ["c", -0.03, 0.03, -0.12, 0.12, -0.24, 0.15], ["c", -0.18, 0.09, -0.21, 0.09, -0.45, 0.09], ["c", -0.24, 0, -0.3, 0, -0.48, -0.06], ["c", -0.09, -0.06, -0.21, -0.12, -0.21, -0.15], ["c", -0.06, -0.03, 0.12, -0.57, 1.44, -4.92], ["c", 0.81, -2.67, 1.47, -4.86, 1.47, -4.89], ["c", -0.03, 0, -0.27, 0.06, -0.54, 0.15], ["c", -1.08, 0.36, -1.77, 0.48, -2.58, 0.48], ["c", -0.66, 0, -0.84, -0.03, -1.32, -0.27], ["c", -1.32, -0.63, -1.77, -2.16, -1.02, -3.3], ["c", 0.72, -1.05, 2.22, -1.23, 3.06, -0.42], ["c", 0.3, 0.33, 0.42, 0.6, 0.6, 1.38], ["c", 0.09, 0.45, 0.21, 0.78, 0.33, 0.9], ["c", 0.09, 0.09, 0.27, 0.18, 0.45, 0.21], ["c", 0.12, 0, 0.18, 0, 0.33, -0.09], ["c", 0.33, -0.15, 1.02, -0.93, 1.41, -1.59], ["c", 0.12, -0.21, 0.18, -0.39, 0.39, -1.08], ["c", 0.66, -2.1, 1.17, -3.84, 1.17, -3.87], ["c", 0, 0, -0.21, 0.06, -0.42, 0.15], ["c", -0.51, 0.15, -1.2, 0.33, -1.68, 0.42], ["c", -0.33, 0.06, -0.51, 0.06, -0.96, 0.06], ["c", -0.66, 0, -0.84, -0.03, -1.32, -0.27], ["c", -1.32, -0.63, -1.77, -2.16, -1.02, -3.3], ["c", 0.33, -0.45, 0.84, -0.81, 1.38, -0.9], ["z"]], "w": 9.724, "h": 21.383}), "rest_32": $hash2(["d", "w", "h"], {"d": [["M", 4.23, -13.62], ["c", 0.66, -0.09, 1.23, 0.09, 1.68, 0.51], ["c", 0.27, 0.3, 0.39, 0.54, 0.57, 1.26], ["c", 0.09, 0.33, 0.18, 0.66, 0.21, 0.72], ["c", 0.12, 0.27, 0.33, 0.45, 0.6, 0.48], ["c", 0.12, 0, 0.18, 0, 0.27, -0.06], ["c", 0.33, -0.21, 0.99, -1.11, 1.44, -1.98], ["c", 0.09, -0.24, 0.21, -0.33, 0.39, -0.33], ["c", 0.12, 0, 0.27, 0.09, 0.33, 0.18], ["c", 0.03, 0.06, -0.57, 2.67, -3.21, 13.89], ["c", -1.8, 7.62, -3.3, 13.89, -3.3, 13.92], ["c", -0.03, 0.06, -0.12, 0.12, -0.24, 0.18], ["c", -0.21, 0.09, -0.24, 0.09, -0.48, 0.09], ["c", -0.24, 0, -0.3, 0, -0.48, -0.06], ["c", -0.09, -0.06, -0.21, -0.12, -0.21, -0.15], ["c", -0.06, -0.03, 0.09, -0.57, 1.23, -4.92], ["c", 0.69, -2.67, 1.26, -4.86, 1.29, -4.89], ["c", 0, -0.03, -0.12, -0.03, -0.48, 0.12], ["c", -1.17, 0.39, -2.22, 0.57, -3, 0.54], ["c", -0.42, -0.03, -0.75, -0.12, -1.11, -0.3], ["c", -1.32, -0.63, -1.77, -2.16, -1.02, -3.3], ["c", 0.72, -1.05, 2.22, -1.23, 3.06, -0.42], ["c", 0.3, 0.33, 0.42, 0.6, 0.6, 1.38], ["c", 0.09, 0.45, 0.21, 0.78, 0.33, 0.9], ["c", 0.12, 0.09, 0.3, 0.18, 0.48, 0.21], ["c", 0.12, 0, 0.18, 0, 0.3, -0.09], ["c", 0.42, -0.21, 1.29, -1.29, 1.56, -1.89], ["c", 0.03, -0.12, 1.23, -4.59, 1.23, -4.65], ["c", 0, -0.03, -0.18, 0.03, -0.39, 0.12], ["c", -0.63, 0.18, -1.2, 0.36, -1.74, 0.45], ["c", -0.39, 0.06, -0.54, 0.06, -1.02, 0.06], ["c", -0.66, 0, -0.84, -0.03, -1.32, -0.27], ["c", -1.32, -0.63, -1.77, -2.16, -1.02, -3.3], ["c", 0.72, -1.05, 2.22, -1.23, 3.06, -0.42], ["c", 0.3, 0.33, 0.42, 0.6, 0.6, 1.38], ["c", 0.09, 0.45, 0.21, 0.78, 0.33, 0.9], ["c", 0.18, 0.18, 0.51, 0.27, 0.72, 0.15], ["c", 0.3, -0.12, 0.69, -0.57, 1.08, -1.17], ["c", 0.42, -0.6, 0.39, -0.51, 1.05, -3.03], ["c", 0.33, -1.26, 0.6, -2.31, 0.6, -2.34], ["c", 0, 0, -0.21, 0.03, -0.45, 0.12], ["c", -0.57, 0.18, -1.14, 0.33, -1.62, 0.42], ["c", -0.33, 0.06, -0.51, 0.06, -0.96, 0.06], ["c", -0.66, 0, -0.84, -0.03, -1.32, -0.27], ["c", -1.32, -0.63, -1.77, -2.16, -1.02, -3.3], ["c", 0.33, -0.45, 0.84, -0.81, 1.38, -0.9], ["z"]], "w": 11.373, "h": 28.883}), "rest_64": $hash2(["d", "w", "h"], {"d": [["M", 5.13, -13.62], ["c", 0.66, -0.09, 1.23, 0.09, 1.68, 0.51], ["c", 0.27, 0.3, 0.39, 0.54, 0.57, 1.26], ["c", 0.15, 0.63, 0.21, 0.81, 0.33, 0.96], ["c", 0.18, 0.21, 0.54, 0.3, 0.75, 0.18], ["c", 0.24, -0.12, 0.63, -0.66, 1.08, -1.56], ["c", 0.33, -0.66, 0.39, -0.72, 0.6, -0.72], ["c", 0.12, 0, 0.27, 0.09, 0.33, 0.18], ["c", 0.03, 0.06, -0.69, 3.66, -3.54, 17.64], ["c", -1.95, 9.66, -3.57, 17.61, -3.57, 17.64], ["c", -0.03, 0.06, -0.12, 0.12, -0.24, 0.18], ["c", -0.21, 0.09, -0.24, 0.09, -0.48, 0.09], ["c", -0.24, 0, -0.3, 0, -0.48, -0.06], ["c", -0.09, -0.06, -0.21, -0.12, -0.21, -0.15], ["c", -0.06, -0.03, 0.06, -0.57, 1.05, -4.95], ["c", 0.6, -2.7, 1.08, -4.89, 1.08, -4.92], ["c", 0, 0, -0.24, 0.06, -0.51, 0.15], ["c", -0.66, 0.24, -1.2, 0.36, -1.77, 0.48], ["c", -0.42, 0.06, -0.57, 0.06, -1.05, 0.06], ["c", -0.69, 0, -0.87, -0.03, -1.35, -0.27], ["c", -1.32, -0.63, -1.77, -2.16, -1.02, -3.3], ["c", 0.72, -1.05, 2.22, -1.23, 3.06, -0.42], ["c", 0.3, 0.33, 0.42, 0.6, 0.6, 1.38], ["c", 0.09, 0.45, 0.21, 0.78, 0.33, 0.9], ["c", 0.09, 0.09, 0.27, 0.18, 0.45, 0.21], ["c", 0.21, 0.03, 0.39, -0.09, 0.72, -0.42], ["c", 0.45, -0.45, 1.02, -1.26, 1.17, -1.65], ["c", 0.03, -0.09, 0.27, -1.14, 0.54, -2.34], ["c", 0.27, -1.2, 0.48, -2.19, 0.51, -2.22], ["c", 0, -0.03, -0.09, -0.03, -0.48, 0.12], ["c", -1.17, 0.39, -2.22, 0.57, -3, 0.54], ["c", -0.42, -0.03, -0.75, -0.12, -1.11, -0.3], ["c", -1.32, -0.63, -1.77, -2.16, -1.02, -3.3], ["c", 0.36, -0.54, 0.96, -0.87, 1.65, -0.93], ["c", 0.54, -0.03, 1.02, 0.15, 1.41, 0.54], ["c", 0.27, 0.3, 0.39, 0.54, 0.57, 1.26], ["c", 0.09, 0.33, 0.18, 0.66, 0.21, 0.72], ["c", 0.15, 0.39, 0.57, 0.57, 0.9, 0.42], ["c", 0.36, -0.18, 1.2, -1.26, 1.47, -1.89], ["c", 0.03, -0.09, 0.3, -1.2, 0.57, -2.43], ["l", 0.51, -2.28], ["l", -0.54, 0.18], ["c", -1.11, 0.36, -1.8, 0.48, -2.61, 0.48], ["c", -0.66, 0, -0.84, -0.03, -1.32, -0.27], ["c", -1.32, -0.63, -1.77, -2.16, -1.02, -3.3], ["c", 0.36, -0.54, 0.96, -0.87, 1.65, -0.93], ["c", 0.54, -0.03, 1.02, 0.15, 1.41, 0.54], ["c", 0.27, 0.3, 0.39, 0.54, 0.57, 1.26], ["c", 0.15, 0.63, 0.21, 0.81, 0.33, 0.96], ["c", 0.21, 0.21, 0.54, 0.3, 0.75, 0.18], ["c", 0.36, -0.18, 0.93, -0.93, 1.29, -1.68], ["c", 0.12, -0.24, 0.18, -0.48, 0.63, -2.55], ["l", 0.51, -2.31], ["c", 0, -0.03, -0.18, 0.03, -0.39, 0.12], ["c", -1.14, 0.36, -2.1, 0.54, -2.82, 0.51], ["c", -0.42, -0.03, -0.75, -0.12, -1.11, -0.3], ["c", -1.32, -0.63, -1.77, -2.16, -1.02, -3.3], ["c", 0.33, -0.45, 0.84, -0.81, 1.38, -0.9], ["z"]], "w": 12.453, "h": 36.383}), "rest_128": $hash2(["d", "w", "h"], {"d": [["M", 6.03, -21.12], ["c", 0.66, -0.09, 1.23, 0.09, 1.68, 0.51], ["c", 0.27, 0.3, 0.39, 0.54, 0.57, 1.26], ["c", 0.09, 0.33, 0.18, 0.66, 0.21, 0.72], ["c", 0.12, 0.27, 0.33, 0.45, 0.6, 0.48], ["c", 0.21, 0, 0.33, -0.06, 0.54, -0.36], ["c", 0.15, -0.21, 0.54, -0.93, 0.78, -1.47], ["c", 0.15, -0.33, 0.18, -0.39, 0.3, -0.48], ["c", 0.18, -0.09, 0.45, 0, 0.51, 0.15], ["c", 0.03, 0.09, -7.11, 42.75, -7.17, 42.84], ["c", -0.03, 0.03, -0.15, 0.09, -0.24, 0.15], ["c", -0.18, 0.06, -0.24, 0.06, -0.45, 0.06], ["c", -0.24, 0, -0.3, 0, -0.48, -0.06], ["c", -0.09, -0.06, -0.21, -0.12, -0.21, -0.15], ["c", -0.06, -0.03, 0.03, -0.57, 0.84, -4.98], ["c", 0.51, -2.7, 0.93, -4.92, 0.9, -4.92], ["c", 0, 0, -0.15, 0.06, -0.36, 0.12], ["c", -0.78, 0.27, -1.62, 0.48, -2.31, 0.57], ["c", -0.15, 0.03, -0.54, 0.03, -0.81, 0.03], ["c", -0.66, 0, -0.84, -0.03, -1.32, -0.27], ["c", -1.32, -0.63, -1.77, -2.16, -1.02, -3.3], ["c", 0.36, -0.54, 0.96, -0.87, 1.65, -0.93], ["c", 0.54, -0.03, 1.02, 0.15, 1.41, 0.54], ["c", 0.27, 0.3, 0.39, 0.54, 0.57, 1.26], ["c", 0.09, 0.33, 0.18, 0.66, 0.21, 0.72], ["c", 0.12, 0.27, 0.33, 0.45, 0.63, 0.48], ["c", 0.12, 0, 0.18, 0, 0.3, -0.09], ["c", 0.42, -0.21, 1.14, -1.11, 1.5, -1.83], ["c", 0.12, -0.27, 0.12, -0.27, 0.54, -2.52], ["c", 0.24, -1.23, 0.42, -2.25, 0.39, -2.25], ["c", 0, 0, -0.24, 0.06, -0.51, 0.18], ["c", -1.26, 0.39, -2.25, 0.57, -3.06, 0.54], ["c", -0.42, -0.03, -0.75, -0.12, -1.11, -0.3], ["c", -1.32, -0.63, -1.77, -2.16, -1.02, -3.3], ["c", 0.36, -0.54, 0.96, -0.87, 1.65, -0.93], ["c", 0.54, -0.03, 1.02, 0.15, 1.41, 0.54], ["c", 0.27, 0.3, 0.39, 0.54, 0.57, 1.26], ["c", 0.15, 0.63, 0.21, 0.81, 0.33, 0.96], ["c", 0.18, 0.21, 0.51, 0.3, 0.75, 0.18], ["c", 0.36, -0.15, 1.05, -0.99, 1.41, -1.77], ["l", 0.15, -0.3], ["l", 0.42, -2.25], ["c", 0.21, -1.26, 0.42, -2.28, 0.39, -2.28], ["l", -0.51, 0.15], ["c", -1.11, 0.39, -1.89, 0.51, -2.7, 0.51], ["c", -0.66, 0, -0.84, -0.03, -1.32, -0.27], ["c", -1.32, -0.63, -1.77, -2.16, -1.02, -3.3], ["c", 0.36, -0.54, 0.96, -0.87, 1.65, -0.93], ["c", 0.54, -0.03, 1.02, 0.15, 1.41, 0.54], ["c", 0.27, 0.3, 0.39, 0.54, 0.57, 1.26], ["c", 0.15, 0.63, 0.21, 0.81, 0.33, 0.96], ["c", 0.18, 0.18, 0.48, 0.27, 0.72, 0.21], ["c", 0.33, -0.12, 1.14, -1.26, 1.41, -1.95], ["c", 0, -0.09, 0.21, -1.11, 0.45, -2.34], ["c", 0.21, -1.2, 0.39, -2.22, 0.39, -2.28], ["c", 0.03, -0.03, 0, -0.03, -0.45, 0.12], ["c", -0.57, 0.18, -1.2, 0.33, -1.71, 0.42], ["c", -0.3, 0.06, -0.51, 0.06, -0.93, 0.06], ["c", -0.66, 0, -0.84, -0.03, -1.32, -0.27], ["c", -1.32, -0.63, -1.77, -2.16, -1.02, -3.3], ["c", 0.36, -0.54, 0.96, -0.87, 1.65, -0.93], ["c", 0.54, -0.03, 1.02, 0.15, 1.41, 0.54], ["c", 0.27, 0.3, 0.39, 0.54, 0.57, 1.26], ["c", 0.09, 0.33, 0.18, 0.66, 0.21, 0.72], ["c", 0.12, 0.27, 0.33, 0.45, 0.6, 0.48], ["c", 0.18, 0, 0.36, -0.09, 0.57, -0.33], ["c", 0.33, -0.36, 0.78, -1.14, 0.93, -1.56], ["c", 0.03, -0.12, 0.24, -1.2, 0.45, -2.4], ["c", 0.24, -1.2, 0.42, -2.22, 0.42, -2.28], ["c", 0.03, -0.03, 0, -0.03, -0.39, 0.09], ["c", -1.05, 0.36, -1.8, 0.48, -2.58, 0.48], ["c", -0.63, 0, -0.84, -0.03, -1.29, -0.27], ["c", -1.32, -0.63, -1.77, -2.16, -1.02, -3.3], ["c", 0.33, -0.45, 0.84, -0.81, 1.38, -0.9], ["z"]], "w": 12.992, "h": 43.883})}));

        Opal.defn(self, '$initialize', TMP_28 = function(center, size, glyph_name, dotted, origin) {
          var self = this, $iter = TMP_28.$$p, $yield = $iter || nil, $zuper = nil, $zuper_index = nil;

          if (dotted == null) {
            dotted = $scope.get('FALSE')
          }
          if (origin == null) {
            origin = nil
          }
          TMP_28.$$p = null;
          $zuper = [];
          for($zuper_index = 0; $zuper_index < arguments.length; $zuper_index++) {
            $zuper[$zuper_index] = arguments[$zuper_index];
          }
          Opal.find_super_dispatcher(self, 'initialize', TMP_28, $iter).apply(self, $zuper);
          self.center = center;
          self.glyph_name = glyph_name;
          self.glyph = $scope.get('GLYPHS')['$[]'](glyph_name);
          self.size = size;
          self.dotted = dotted;
          self.origin = origin;
          return self.filled = true;
        });

        Opal.defn(self, '$height', function() {
          var self = this;

          return self.size.$last();
        });

        Opal.defn(self, '$dotted?', function() {
          var self = this;

          return self.$dotted();
        });

        return (Opal.defn(self, '$filled?', function() {
          var self = this;

          return self.fill['$==']("filled");
        }), nil) && 'filled?';
      })($scope.base, $scope.get('Symbol'));
    })($scope.base);

    (function($base) {
      var $Layout, self = $Layout = $module($base, 'Layout');

      var def = self.$$proto, $scope = self.$$scope;

      self.$include((($scope.get('Harpnotes')).$$scope.get('Music')));

      self.$include((($scope.get('Harpnotes')).$$scope.get('Drawing')));

      (function($base, $super) {
        function $Default(){};
        var self = $Default = $klass($base, $super, 'Default', $Default);

        var def = self.$$proto, $scope = self.$$scope;

        def.y_offset = def.beat_spacing = def.slur_index = nil;
        Opal.cdecl($scope, 'MM_PER_POINT', 0.3);

        Opal.defn(self, '$initialize', function() {
          var self = this;
          if ($gvars.conf == null) $gvars.conf = nil;

          self.beat_spacing = $rb_divide($rb_times($gvars.conf.$get("layout.Y_SCALE"), 1.0), $gvars.conf.$get("layout.BEAT_RESOLUTION"));
          self.slur_index = $hash2([], {});
          return self.y_offset = 5;
        });

        Opal.defn(self, '$layout_debug_grid', function() {
          var $a, $b, TMP_29, $c, $d, TMP_30, TMP_31, $e, $f, TMP_32, self = this, the_options = nil, gridsize = nil, emphasis = nil, sheetsize = nil, result = nil;

          the_options = $hash2(["size", "emphasis", "line", "emphline"], {"size": 1, "emphasis": 10, "line": 0.02, "emphline": 0.2});
          gridsize = the_options['$[]']("size");
          emphasis = $rb_times(the_options['$[]']("emphasis"), gridsize);
          sheetsize = [440, 330];
          result = [];
          ($a = ($b = ($c = ($d = ($range(0, $rb_divide(sheetsize.$first(), gridsize), false))).$map, $c.$$p = (TMP_30 = function(g){var self = TMP_30.$$s || this;
if (g == null) g = nil;
          return $rb_times(g, gridsize)}, TMP_30.$$s = self, TMP_30), $c).call($d)).$each, $a.$$p = (TMP_29 = function(g){var self = TMP_29.$$s || this, $a, $b, e = nil;
if (g == null) g = nil;
          e = (((($scope.get('Harpnotes')).$$scope.get('Drawing'))).$$scope.get('Path')).$new([["M", g, 0], ["l", 0, sheetsize.$last()]]);
            (($a = [the_options['$[]']("line")]), $b = e, $b['$line_width='].apply($b, $a), $a[$a.length-1]);
            if ((g['$%'](emphasis))['$=='](0)) {
              (($a = [the_options['$[]']("emphline")]), $b = e, $b['$line_width='].apply($b, $a), $a[$a.length-1])};
            return result['$<<'](e);}, TMP_29.$$s = self, TMP_29), $a).call($b);
          ($a = ($c = ($e = ($f = ($range(0, $rb_divide(sheetsize.$last(), gridsize), false))).$map, $e.$$p = (TMP_32 = function(g){var self = TMP_32.$$s || this;
if (g == null) g = nil;
          return $rb_times(g, gridsize)}, TMP_32.$$s = self, TMP_32), $e).call($f)).$each, $a.$$p = (TMP_31 = function(g){var self = TMP_31.$$s || this, $a, $b, e = nil;
if (g == null) g = nil;
          e = (((($scope.get('Harpnotes')).$$scope.get('Drawing'))).$$scope.get('Path')).$new([["M", 0, g], ["l", sheetsize.$first(), 0]]);
            (($a = [the_options['$[]']("line")]), $b = e, $b['$line_width='].apply($b, $a), $a[$a.length-1]);
            if ((g['$%'](emphasis))['$=='](0)) {
              (($a = [the_options['$[]']("emphline")]), $b = e, $b['$line_width='].apply($b, $a), $a[$a.length-1])};
            return result['$<<'](e);}, TMP_31.$$s = self, TMP_31), $a).call($c);
          return result;
        });

        Opal.defn(self, '$layout', function(music, beat_layout, print_variant_nr) {
          var $a, $b, $c, TMP_33, TMP_34, $d, TMP_35, $e, TMP_36, $f, TMP_38, $g, TMP_39, $h, $i, TMP_40, TMP_41, $j, TMP_43, $k, TMP_44, $l, TMP_46, self = this, debug_grid = nil, print_options = nil, song_print_options = nil, layout_options = nil, beat_compression_map = nil, maximal_beat = nil, full_beat_spacing = nil, factor = nil, compressed_beat_layout_proc = nil, required_synchlines = nil, synched_notes = nil, voice_elements = nil, note_to_ellipse = nil, synch_lines = nil, sheet_marks = nil, annotations = nil, title = nil, filename = nil, meter = nil, key = nil, composer = nil, tempo = nil, print_variant_title = nil, title_pos = nil, legend_pos = nil, legend = nil, datestring = nil, lyrics = nil, lyric_text = nil, text = nil, verses = nil, sheet_elements = nil;
          if ($gvars.conf == null) $gvars.conf = nil;
          if ($gvars.log == null) $gvars.log = nil;

          if (beat_layout == null) {
            beat_layout = nil
          }
          if (print_variant_nr == null) {
            print_variant_nr = 0
          }
          debug_grid = [];
          if ((($a = $gvars.conf['$[]']("layout.grid")) !== nil && (!$a.$$is_boolean || $a == true))) {
            debug_grid = self.$layout_debug_grid()};
          print_options = $scope.get('Confstack').$new();
          (($a = [false]), $b = print_options, $b['$strict='].apply($b, $a), $a[$a.length-1]);
          print_options.$push($gvars.conf.$get("extract.0"));
          if ($gvars.conf.$get("location")['$==']("song")) {
            song_print_options = $gvars.conf.$get("extract." + (print_variant_nr))
            } else {
            song_print_options = music.$harpnote_options()['$[]']("print")['$[]'](print_variant_nr);
            song_print_options['$[]=']("legend", music.$harpnote_options()['$[]']("legend"));
            song_print_options['$[]=']("lyrics", music.$harpnote_options()['$[]']("lyrics"));
            song_print_options['$[]=']("notes", music.$harpnote_options()['$[]']("notes"));
          };
          if (song_print_options !== false && song_print_options !== nil) {
            print_options.$push(song_print_options)
            } else {
            $gvars.log.$warning("selected print variant [" + (print_variant_nr) + "] not available using [0]: '" + (print_options.$get("title")) + "'")
          };
          print_options = print_options.$get();
          layout_options = ((($a = print_options['$[]']("layout")) !== false && $a !== nil) ? $a : $hash2([], {}));
          $gvars.conf.$push($hash2(["layout"], {"layout": layout_options}));
          self.$initialize();
          self.y_offset = print_options['$[]']("startpos");
          beat_compression_map = self.$compute_beat_compression(music, print_options['$[]']("layoutlines"));
          maximal_beat = beat_compression_map.$values().$max();
          full_beat_spacing = $rb_divide(($rb_minus($gvars.conf.$get("layout.DRAWING_AREA_SIZE").$last(), self.y_offset)), maximal_beat);
          if ($rb_lt(full_beat_spacing, self.beat_spacing)) {
            factor = ($rb_divide(self.beat_spacing, full_beat_spacing));
            $gvars.log.$warning("note distance too small (factor " + (factor) + ")");};
          self.beat_spacing = [full_beat_spacing, $rb_times(2, self.beat_spacing)].$min();
          beat_layout = ((($a = beat_layout) !== false && $a !== nil) ? $a : ($b = ($c = $scope.get('Proc')).$new, $b.$$p = (TMP_33 = function(beat){var self = TMP_33.$$s || this;
            if (self.beat_spacing == null) self.beat_spacing = nil;
            if (self.y_offset == null) self.y_offset = nil;
if (beat == null) beat = nil;
          return beat * self.beat_spacing + self.y_offset;}, TMP_33.$$s = self, TMP_33), $b).call($c));
          compressed_beat_layout_proc = ($a = ($b = $scope.get('Proc')).$new, $a.$$p = (TMP_34 = function(beat){var self = TMP_34.$$s || this;
if (beat == null) beat = nil;
          return beat_layout.$call(beat_compression_map['$[]'](beat))}, TMP_34.$$s = self, TMP_34), $a).call($b);
          required_synchlines = ($a = ($d = print_options['$[]']("synchlines")).$select, $a.$$p = (TMP_35 = function(sl){var self = TMP_35.$$s || this, $a;
if (sl == null) sl = nil;
          return ($a = print_options['$[]']("voices")['$include?'](sl.$first()), $a !== false && $a !== nil ?print_options['$[]']("voices")['$include?'](sl.$last()) : $a)}, TMP_35.$$s = self, TMP_35), $a).call($d);
          synched_notes = ($a = ($e = required_synchlines).$map, $a.$$p = (TMP_36 = function(selector){var self = TMP_36.$$s || this, $a, $b, TMP_37, synch_points_to_show = nil;
if (selector == null) selector = nil;
          synch_points_to_show = music.$build_synch_points(selector);
            return ($a = ($b = synch_points_to_show).$map, $a.$$p = (TMP_37 = function(sp){var self = TMP_37.$$s || this;
if (sp == null) sp = nil;
            return [sp.$notes().$first(), sp.$notes().$last()]}, TMP_37.$$s = self, TMP_37), $a).call($b);}, TMP_36.$$s = self, TMP_36), $a).call($e).$flatten();
          voice_elements = ($a = ($f = music.$voices().$each_with_index()).$map, $a.$$p = (TMP_38 = function(v, index){var self = TMP_38.$$s || this, $a;
if (v == null) v = nil;if (index == null) index = nil;
          if ((($a = print_options['$[]']("voices")['$include?'](index)) !== nil && (!$a.$$is_boolean || $a == true))) {
              return self.$layout_voice(v, compressed_beat_layout_proc, $hash2(["nonflowrest", "flowline", "subflowline", "jumpline", "annotations", "synched_notes"], {"nonflowrest": print_options['$[]']("nonflowrest"), "flowline": print_options['$[]']("flowlines")['$include?'](index), "subflowline": print_options['$[]']("subflowlines")['$include?'](index), "jumpline": print_options['$[]']("jumplines")['$include?'](index), "annotations": music.$harpnote_options()['$[]']("annotations"), "synched_notes": synched_notes}))
              } else {
              return nil
            }}, TMP_38.$$s = self, TMP_38), $a).call($f).$flatten().$compact();
          note_to_ellipse = $scope.get('Hash')['$[]'](($a = ($g = ($h = ($i = voice_elements).$select, $h.$$p = (TMP_40 = function(e){var self = TMP_40.$$s || this;
if (e == null) e = nil;
          return e['$is_a?']($scope.get('Symbol'))}, TMP_40.$$s = self, TMP_40), $h).call($i)).$map, $a.$$p = (TMP_39 = function(e){var self = TMP_39.$$s || this;
if (e == null) e = nil;
          return [e.$origin(), e]}, TMP_39.$$s = self, TMP_39), $a).call($g));
          synch_lines = ($a = ($h = required_synchlines).$map, $a.$$p = (TMP_41 = function(selector){var self = TMP_41.$$s || this, $a, $b, TMP_42, synch_points_to_show = nil;
if (selector == null) selector = nil;
          synch_points_to_show = music.$build_synch_points(selector);
            return ($a = ($b = synch_points_to_show).$map, $a.$$p = (TMP_42 = function(sp){var self = TMP_42.$$s || this;
if (sp == null) sp = nil;
            return $scope.get('FlowLine').$new(note_to_ellipse['$[]'](sp.$notes().$first()), note_to_ellipse['$[]'](sp.$notes().$last()), "dashed", sp)}, TMP_42.$$s = self, TMP_42), $a).call($b);}, TMP_41.$$s = self, TMP_41), $a).call($h).$flatten();
          sheet_marks = ($a = ($j = [79, 55, 43]).$inject, $a.$$p = (TMP_43 = function(result, pitch){var self = TMP_43.$$s || this, markpath = nil;
            if ($gvars.conf == null) $gvars.conf = nil;
if (result == null) result = nil;if (pitch == null) pitch = nil;
          markpath = self.$make_sheetmark_path([$rb_plus($rb_times(($rb_plus($gvars.conf.$get("layout.PITCH_OFFSET"), pitch)), $gvars.conf.$get("layout.X_SPACING")), $gvars.conf.$get("layout.X_OFFSET")), 10]);
            result['$<<']((((($scope.get('Harpnotes')).$$scope.get('Drawing'))).$$scope.get('Path')).$new(markpath, "filled"));
            return result;}, TMP_43.$$s = self, TMP_43), $a).call($j, []);
          annotations = [];
          title = ((($a = music.$meta_data()['$[]']("title")) !== false && $a !== nil) ? $a : "untitled");
          filename = music.$meta_data()['$[]']("filename");
          meter = music.$meta_data()['$[]']("meter");
          key = music.$meta_data()['$[]']("key");
          composer = music.$meta_data()['$[]']("composer");
          tempo = music.$meta_data()['$[]']("tempo_display");
          print_variant_title = print_options['$[]']("title");
          title_pos = print_options['$[]']("legend")['$[]']("pos");
          legend_pos = print_options['$[]']("legend")['$[]']("spos");
          legend = "" + (print_variant_title) + "\n" + (composer) + "\nTakt: " + (meter) + " (" + (tempo) + ")\nTonart: " + (key);
          annotations['$<<']((((($scope.get('Harpnotes')).$$scope.get('Drawing'))).$$scope.get('Annotation')).$new(title_pos, title, "large", nil, "extract." + (print_variant_nr) + ".legend.pos"));
          annotations['$<<']((((($scope.get('Harpnotes')).$$scope.get('Drawing'))).$$scope.get('Annotation')).$new(legend_pos, legend, "regular", nil, "extract." + (print_variant_nr) + ".legend.spos"));
          datestring = $scope.get('Time').$now().$strftime("%Y-%m-%d %H:%M:%S %Z");
          annotations['$<<']((((($scope.get('Harpnotes')).$$scope.get('Drawing'))).$$scope.get('Annotation')).$new([150, 289], "" + (filename) + " - created " + (datestring) + " by Zupfnoter " + ($scope.get('VERSION')), "smaller"));
          annotations['$<<']((((($scope.get('Harpnotes')).$$scope.get('Drawing'))).$$scope.get('Annotation')).$new([285, 289], "Zupfnoter " + ($scope.get('COPYRIGHT')), "smaller"));
          lyrics = print_options['$[]']("lyrics");
          lyric_text = music.$harpnote_options()['$[]']("lyrics")['$[]']("text");
          if (lyric_text !== false && lyric_text !== nil) {
            text = lyric_text.$join("\n");
            if (lyrics !== false && lyrics !== nil) {
              verses = text.$split("\n\n");
              lyrics.$delete("versepos");
              ($a = ($k = lyrics).$each, $a.$$p = (TMP_44 = function(key, entry){var self = TMP_44.$$s || this, $a, $b, TMP_45, pos = nil, the_text = nil;
if (key == null) key = nil;if (entry == null) entry = nil;
              pos = entry['$[]']("pos");
                the_text = ($a = ($b = entry['$[]']("verses")).$map, $a.$$p = (TMP_45 = function(i){var self = TMP_45.$$s || this;
if (i == null) i = nil;
                return verses['$[]']($rb_minus(i.$to_i(), 1))}, TMP_45.$$s = self, TMP_45), $a).call($b).$join("\n\n");
                return annotations['$<<']((((($scope.get('Harpnotes')).$$scope.get('Drawing'))).$$scope.get('Annotation')).$new(pos, the_text, nil, nil, "extract." + (print_variant_nr) + ".lyrics." + (key) + ".pos"));}, TMP_44.$$s = self, TMP_44), $a).call($k);};};
          ($a = ($l = print_options['$[]']("notes")).$each, $a.$$p = (TMP_46 = function(k, note){var self = TMP_46.$$s || this;
if (k == null) k = nil;if (note == null) note = nil;
          return annotations['$<<']((((($scope.get('Harpnotes')).$$scope.get('Drawing'))).$$scope.get('Annotation')).$new(note['$[]']("pos"), note['$[]']("text"), note['$[]']("style"), nil, "extract." + (print_variant_nr) + ".notes." + (k) + ".pos"))}, TMP_46.$$s = self, TMP_46), $a).call($l);
          sheet_elements = $rb_plus($rb_plus($rb_plus($rb_plus(debug_grid, synch_lines), voice_elements), sheet_marks), annotations);
          $gvars.conf.$pop();
          return (((($scope.get('Harpnotes')).$$scope.get('Drawing'))).$$scope.get('Sheet')).$new(sheet_elements);
        });

        Opal.defn(self, '$layout_voice', function(voice, beat_layout, show_options) {
          var $a, $b, TMP_47, $c, TMP_48, $d, TMP_49, $e, TMP_50, $f, $g, TMP_51, TMP_52, $h, $i, TMP_53, TMP_54, $j, TMP_55, $k, $l, TMP_56, TMP_57, $m, $n, TMP_58, TMP_59, $o, TMP_61, $p, TMP_68, $q, $r, TMP_70, TMP_71, $s, $t, TMP_72, self = this, playables = nil, res_playables = nil, res_measures = nil, res_newparts = nil, lookuptable_drawing_by_playable = nil, previous_note = nil, res_flow = nil, res_sub_flow = nil, tuplet_start = nil, res_tuplets = nil, tie_start = nil, res_slurs = nil, res_gotos = nil, res_annotations = nil, retval = nil;

          playables = ($a = ($b = voice).$select, $a.$$p = (TMP_47 = function(c){var self = TMP_47.$$s || this;
if (c == null) c = nil;
          return c['$is_a?']($scope.get('Playable'))}, TMP_47.$$s = self, TMP_47), $a).call($b);
          if ((($a = show_options['$[]']("nonflowrest")) !== nil && (!$a.$$is_boolean || $a == true))) {
            } else {
            ($a = ($c = playables).$each, $a.$$p = (TMP_48 = function(c){var self = TMP_48.$$s || this, $a, $b;
if (c == null) c = nil;
            if ((($a = ($b = c['$is_a?']($scope.get('Pause')), $b !== false && $b !== nil ?show_options['$[]']("flowline")['$!']() : $b)) !== nil && (!$a.$$is_boolean || $a == true))) {
                return (($a = [false]), $b = c, $b['$visible='].apply($b, $a), $a[$a.length-1])
                } else {
                return nil
              }}, TMP_48.$$s = self, TMP_48), $a).call($c)
          };
          res_playables = ($a = ($d = playables).$map, $a.$$p = (TMP_49 = function(playable){var self = TMP_49.$$s || this;
if (playable == null) playable = nil;
          return self.$layout_playable(playable, beat_layout)}, TMP_49.$$s = self, TMP_49), $a).call($d).$flatten().$compact();
          res_measures = ($a = ($e = ($f = ($g = voice).$select, $f.$$p = (TMP_51 = function(c){var self = TMP_51.$$s || this, $a;
if (c == null) c = nil;
          return ($a = c['$is_a?']($scope.get('MeasureStart')), $a !== false && $a !== nil ?c.$companion().$visible() : $a)}, TMP_51.$$s = self, TMP_51), $f).call($g)).$map, $a.$$p = (TMP_50 = function(measure){var self = TMP_50.$$s || this;
if (measure == null) measure = nil;
          return self.$layout_playable(measure, beat_layout)}, TMP_50.$$s = self, TMP_50), $a).call($e);
          res_newparts = ($a = ($f = ($h = ($i = voice).$select, $h.$$p = (TMP_53 = function(c){var self = TMP_53.$$s || this;
if (c == null) c = nil;
          return c['$is_a?']($scope.get('NewPart'))}, TMP_53.$$s = self, TMP_53), $h).call($i)).$map, $a.$$p = (TMP_52 = function(newpart){var self = TMP_52.$$s || this;
if (newpart == null) newpart = nil;
          return self.$layout_newpart(newpart, beat_layout)}, TMP_52.$$s = self, TMP_52), $a).call($f);
          lookuptable_drawing_by_playable = $scope.get('Hash')['$[]'](($a = ($h = res_playables).$map, $a.$$p = (TMP_54 = function(e){var self = TMP_54.$$s || this;
if (e == null) e = nil;
          return [e.$origin(), e]}, TMP_54.$$s = self, TMP_54), $a).call($h).$reverse());
          previous_note = nil;
          res_flow = ($a = ($j = ($k = ($l = voice).$select, $k.$$p = (TMP_56 = function(c){var self = TMP_56.$$s || this;
if (c == null) c = nil;
          return c['$is_a?']($scope.get('Playable'))}, TMP_56.$$s = self, TMP_56), $k).call($l)).$map, $a.$$p = (TMP_55 = function(playable){var self = TMP_55.$$s || this, $a, $b, res = nil;
            if ($gvars.conf == null) $gvars.conf = nil;
if (playable == null) playable = nil;
          res = nil;
            if ((($a = previous_note['$nil?']()) !== nil && (!$a.$$is_boolean || $a == true))) {
              } else {
              res = $scope.get('FlowLine').$new(lookuptable_drawing_by_playable['$[]'](previous_note), lookuptable_drawing_by_playable['$[]'](playable));
              (($a = [$gvars.conf.$get("layout.LINE_MEDIUM")]), $b = res, $b['$line_width='].apply($b, $a), $a[$a.length-1]);
              if ((($a = previous_note['$visible?']()) !== nil && (!$a.$$is_boolean || $a == true))) {
                } else {
                res = nil
              };
            };
            if ((($a = playable['$first_in_part?']()) !== nil && (!$a.$$is_boolean || $a == true))) {
              res = nil};
            if ((($a = playable['$visible?']()) !== nil && (!$a.$$is_boolean || $a == true))) {
              } else {
              res = nil
            };
            previous_note = playable;
            return res;}, TMP_55.$$s = self, TMP_55), $a).call($j).$compact();
          previous_note = nil;
          res_sub_flow = ($a = ($k = ($m = ($n = voice).$select, $m.$$p = (TMP_58 = function(c){var self = TMP_58.$$s || this, $a;
if (c == null) c = nil;
          return ((($a = c['$is_a?']($scope.get('Playable'))) !== false && $a !== nil) ? $a : c['$is_a?']($scope.get('SynchPoint')))}, TMP_58.$$s = self, TMP_58), $m).call($n)).$map, $a.$$p = (TMP_57 = function(playable){var self = TMP_57.$$s || this, $a, res = nil;
if (playable == null) playable = nil;
          if ((($a = show_options['$[]']("synched_notes")['$include?'](playable.$proxy_note())) !== nil && (!$a.$$is_boolean || $a == true))) {
              } else {
              res = nil;
              if ((($a = previous_note['$nil?']()) !== nil && (!$a.$$is_boolean || $a == true))) {
                } else {
                res = $scope.get('FlowLine').$new(lookuptable_drawing_by_playable['$[]'](previous_note), lookuptable_drawing_by_playable['$[]'](playable), "dotted")
              };
              if ((($a = playable['$first_in_part?']()) !== nil && (!$a.$$is_boolean || $a == true))) {
                res = nil};
            };
            if ((($a = playable['$visible?']()) !== nil && (!$a.$$is_boolean || $a == true))) {
              } else {
              res = nil
            };
            previous_note = playable;
            return res;}, TMP_57.$$s = self, TMP_57), $a).call($k).$compact();
          if ((($a = show_options['$[]']("subflowline")) !== nil && (!$a.$$is_boolean || $a == true))) {
            } else {
            res_sub_flow = []
          };
          if ((($a = show_options['$[]']("flowline")) !== nil && (!$a.$$is_boolean || $a == true))) {
            } else {
            res_flow = []
          };
          tuplet_start = playables.$first();
          res_tuplets = ($a = ($m = playables).$inject, $a.$$p = (TMP_59 = function(result, playable){var self = TMP_59.$$s || this, $a, $b, TMP_60, p1 = nil, p2 = nil, tiepath = nil, anchor = nil;
            if ($gvars.log == null) $gvars.log = nil;
if (result == null) result = nil;if (playable == null) playable = nil;
          if ((($a = playable['$tuplet_start?']()) !== nil && (!$a.$$is_boolean || $a == true))) {
              tuplet_start = playable};
            if ((($a = playable['$tuplet_end?']()) !== nil && (!$a.$$is_boolean || $a == true))) {
              p1 = self.$Vector2d(lookuptable_drawing_by_playable['$[]'](tuplet_start).$center());
              p2 = self.$Vector2d(lookuptable_drawing_by_playable['$[]'](playable).$center());
              $b = self.$make_annotated_bezier_path([p1, p2]), $a = Opal.to_ary($b), tiepath = ($a[0] == null ? nil : $a[0]), anchor = ($a[1] == null ? nil : $a[1]), $b;
              $gvars.log.$debug("" + ([tiepath, anchor]) + " (" + ("harpnotes") + " " + (1319) + ")");
              result.$push(($a = ($b = (((($scope.get('Harpnotes')).$$scope.get('Drawing'))).$$scope.get('Path')).$new(tiepath)).$tap, $a.$$p = (TMP_60 = function(d){var self = TMP_60.$$s || this, $a, $b;
                if ($gvars.conf == null) $gvars.conf = nil;
if (d == null) d = nil;
              return (($a = [$gvars.conf.$get("layout.LINE_MEDIUM")]), $b = d, $b['$line_width='].apply($b, $a), $a[$a.length-1])}, TMP_60.$$s = self, TMP_60), $a).call($b));
              result.$push((((($scope.get('Harpnotes')).$$scope.get('Drawing'))).$$scope.get('Annotation')).$new(anchor.$to_a(), playable.$tuplet().$to_s(), "small"));};
            return result;}, TMP_59.$$s = self, TMP_59), $a).call($m, []);
          self.slur_index['$[]=']("first_playable", playables.$first());
          tie_start = playables.$first();
          res_slurs = ($a = ($o = playables).$inject, $a.$$p = (TMP_61 = function(result, playable){var self = TMP_61.$$s || this, $a, $b, TMP_62, $c, TMP_63, $d, TMP_65, $e, TMP_66, p1 = nil, p2 = nil, tiepath = nil;
            if (self.slur_index == null) self.slur_index = nil;
if (result == null) result = nil;if (playable == null) playable = nil;
          if ((($a = playable['$tie_end?']()) !== nil && (!$a.$$is_boolean || $a == true))) {
              p1 = $rb_plus(self.$Vector2d(lookuptable_drawing_by_playable['$[]'](tie_start).$center()), [3, 0]);
              p2 = $rb_plus(self.$Vector2d(lookuptable_drawing_by_playable['$[]'](playable).$center()), [3, 0]);
              tiepath = self.$make_slur_path(p1, p2);
              result.$push(($a = ($b = (((($scope.get('Harpnotes')).$$scope.get('Drawing'))).$$scope.get('Path')).$new(tiepath)).$tap, $a.$$p = (TMP_62 = function(d){var self = TMP_62.$$s || this, $a, $b;
                if ($gvars.conf == null) $gvars.conf = nil;
if (d == null) d = nil;
              return (($a = [$gvars.conf.$get("layout.LINE_MEDIUM")]), $b = d, $b['$line_width='].apply($b, $a), $a[$a.length-1])}, TMP_62.$$s = self, TMP_62), $a).call($b));
              if ((($a = playable['$is_a?']((((($scope.get('Harpnotes')).$$scope.get('Music'))).$$scope.get('SynchPoint')))) !== nil && (!$a.$$is_boolean || $a == true))) {
                ($a = ($c = playable.$notes()).$each_with_index, $a.$$p = (TMP_63 = function(n, index){var self = TMP_63.$$s || this, $a, $b, TMP_64, e = nil;
                  if ($gvars.log == null) $gvars.log = nil;
if (n == null) n = nil;if (index == null) index = nil;
                try {
                  p1 = tie_start.$notes()['$[]'](index);
                    p1 = $rb_plus(self.$Vector2d(lookuptable_drawing_by_playable['$[]'](p1).$center()), [3, 0]);
                    p2 = $rb_plus(self.$Vector2d(lookuptable_drawing_by_playable['$[]'](n).$center()), [3, 0]);
                    tiepath = self.$make_slur_path(p1, p2);
                    return result.$push(($a = ($b = (((($scope.get('Harpnotes')).$$scope.get('Drawing'))).$$scope.get('Path')).$new(tiepath)).$tap, $a.$$p = (TMP_64 = function(d){var self = TMP_64.$$s || this, $a, $b;
                      if ($gvars.conf == null) $gvars.conf = nil;
if (d == null) d = nil;
                    return (($a = [$gvars.conf.$get("layout.LINE_MEDIUM")]), $b = d, $b['$line_width='].apply($b, $a), $a[$a.length-1])}, TMP_64.$$s = self, TMP_64), $a).call($b));
                  } catch ($err) {if (Opal.rescue($err, [$scope.get('Exception')])) {e = $err;
                    try {
                      return $gvars.log.$error("tied chords which doesn't have same number of notes", n.$start_pos())
                    } finally {
                      Opal.gvars["!"] = Opal.exceptions.pop() || Opal.nil;
                    }
                    }else { throw $err; }
                  }}, TMP_63.$$s = self, TMP_63), $a).call($c)};};
            if ((($a = playable['$tie_start?']()) !== nil && (!$a.$$is_boolean || $a == true))) {
              tie_start = playable};
            ($a = ($d = playable.$slur_starts()).$each, $a.$$p = (TMP_65 = function(s){var self = TMP_65.$$s || this;
              if (self.slur_index == null) self.slur_index = nil;
if (s == null) s = nil;
            return self.slur_index['$[]='](s, playable)}, TMP_65.$$s = self, TMP_65), $a).call($d);
            self.slur_index['$[]='](playable.$slur_starts().$first(), playable);
            ($a = ($e = playable.$slur_ends()).$each, $a.$$p = (TMP_66 = function(id){var self = TMP_66.$$s || this, $a, $b, TMP_67, begin_slur = nil, slurpath = nil;
              if (self.slur_index == null) self.slur_index = nil;
              if ($gvars.conf == null) $gvars.conf = nil;
if (id == null) id = nil;
            begin_slur = ((($a = self.slur_index['$[]'](id)) !== false && $a !== nil) ? $a : self.slur_index['$[]']("first_playable"));
              p1 = $rb_plus(self.$Vector2d(lookuptable_drawing_by_playable['$[]'](begin_slur).$center()), [3, 0]);
              p2 = $rb_plus(self.$Vector2d(lookuptable_drawing_by_playable['$[]'](playable).$center()), [3, 0]);
              slurpath = self.$make_slur_path(p1, p2);
              if ((($a = $gvars.conf.$get("layout.SHOW_SLUR")) !== nil && (!$a.$$is_boolean || $a == true))) {
                return result.$push(($a = ($b = (((($scope.get('Harpnotes')).$$scope.get('Drawing'))).$$scope.get('Path')).$new(slurpath)).$tap, $a.$$p = (TMP_67 = function(d){var self = TMP_67.$$s || this, $a, $b;
                  if ($gvars.conf == null) $gvars.conf = nil;
if (d == null) d = nil;
                return (($a = [$gvars.conf.$get("layout.LINE_MEDIUM")]), $b = d, $b['$line_width='].apply($b, $a), $a[$a.length-1])}, TMP_67.$$s = self, TMP_67), $a).call($b))
                } else {
                return nil
              };}, TMP_66.$$s = self, TMP_66), $a).call($e);
            return result;}, TMP_61.$$s = self, TMP_61), $a).call($o, []);
          res_gotos = ($a = ($p = ($q = ($r = voice).$select, $q.$$p = (TMP_70 = function(c){var self = TMP_70.$$s || this;
if (c == null) c = nil;
          return c['$is_a?']($scope.get('Goto'))}, TMP_70.$$s = self, TMP_70), $q).call($r)).$map, $a.$$p = (TMP_68 = function(goto$){var self = TMP_68.$$s || this, $a, $b, TMP_69, distance = nil, vertical = nil, path = nil;
            if ($gvars.log == null) $gvars.log = nil;
            if ($gvars.conf == null) $gvars.conf = nil;
if (goto$ == null) goto$ = nil;
          distance = goto$.$policy()['$[]']("distance");
            $gvars.log.$debug("vertical line x offset: " + (distance) + " " + ("harpnotes") + ":" + (1375));
            if ($rb_gt(distance, 0)) {
              distance = $rb_minus(distance, 1)};
            if (distance !== false && distance !== nil) {
              vertical = $rb_times(($rb_plus(distance, 0.5)), $gvars.conf.$get("layout.X_SPACING"))
              } else {
              vertical = $rb_times(0.5, $gvars.conf.$get("layout.X_SPACING"))
            };
            path = self.$make_path_from_jumpline(self.$Vector2d(lookuptable_drawing_by_playable['$[]'](goto$.$from()).$center()), self.$Vector2d(lookuptable_drawing_by_playable['$[]'](goto$.$to()).$center()), self.$Vector2d(2.5, 2.5), vertical);
            return [($a = ($b = (((($scope.get('Harpnotes')).$$scope.get('Drawing'))).$$scope.get('Path')).$new(path['$[]'](0), nil, goto$.$from())).$tap, $a.$$p = (TMP_69 = function(s){var self = TMP_69.$$s || this, $a, $b;
              if ($gvars.conf == null) $gvars.conf = nil;
if (s == null) s = nil;
            return (($a = [$gvars.conf.$get("layout.LINE_THICK")]), $b = s, $b['$line_width='].apply($b, $a), $a[$a.length-1])}, TMP_69.$$s = self, TMP_69), $a).call($b), (((($scope.get('Harpnotes')).$$scope.get('Drawing'))).$$scope.get('Path')).$new(path['$[]'](1), "filled", goto$.$from())];}, TMP_68.$$s = self, TMP_68), $a).call($p).$flatten();
          if ((($a = show_options['$[]']("jumpline")) !== nil && (!$a.$$is_boolean || $a == true))) {
            } else {
            res_gotos = []
          };
          res_annotations = ($a = ($q = ($s = ($t = voice).$select, $s.$$p = (TMP_72 = function(c){var self = TMP_72.$$s || this;
if (c == null) c = nil;
          return c['$is_a?']($scope.get('NoteBoundAnnotation'))}, TMP_72.$$s = self, TMP_72), $s).call($t)).$map, $a.$$p = (TMP_71 = function(annotation){var self = TMP_71.$$s || this, position = nil;
if (annotation == null) annotation = nil;
          position = $rb_plus(self.$Vector2d(lookuptable_drawing_by_playable['$[]'](annotation.$companion()).$center()), annotation.$position());
            return (((($scope.get('Harpnotes')).$$scope.get('Drawing'))).$$scope.get('Annotation')).$new(position.$to_a(), annotation.$text(), nil, annotation.$companion().$origin(), nil);}, TMP_71.$$s = self, TMP_71), $a).call($q);
          return retval = ($rb_plus($rb_plus($rb_plus($rb_plus($rb_plus($rb_plus($rb_plus($rb_plus(res_flow, res_sub_flow), res_slurs), res_tuplets), res_playables), res_gotos), res_measures), res_newparts), res_annotations)).$compact();
        });

        self.$private();

        Opal.defn(self, '$compute_beat_compression', function(music, layout_lines) {
          var $a, $b, TMP_73, $c, TMP_74, $d, TMP_75, self = this, max_beat = nil, conf_beat_resolution = nil, current_beat = nil, last_size = nil, relevant_beat_maps = nil, duration_to_style = nil, result = nil;
          if ($gvars.conf == null) $gvars.conf = nil;

          max_beat = ($a = ($b = music.$beat_maps()).$map, $a.$$p = (TMP_73 = function(map){var self = TMP_73.$$s || this;
if (map == null) map = nil;
          return map.$keys().$max()}, TMP_73.$$s = self, TMP_73), $a).call($b).$max();
          conf_beat_resolution = $gvars.conf.$get("layout.BEAT_RESOLUTION");
          current_beat = 0;
          last_size = 0;
          relevant_beat_maps = ($a = ($c = layout_lines).$inject, $a.$$p = (TMP_74 = function(r, i){var self = TMP_74.$$s || this;
if (r == null) r = nil;if (i == null) i = nil;
          return r.$push(music.$beat_maps()['$[]'](i))}, TMP_74.$$s = self, TMP_74), $a).call($c, []).$compact();
          duration_to_style = $gvars.conf.$get("layout.DURATION_TO_STYLE");
          result = $scope.get('Hash')['$[]'](($a = ($d = ($range(0, max_beat, false))).$map, $a.$$p = (TMP_75 = function(beat){var self = TMP_75.$$s || this, $a, $b, TMP_76, $c, TMP_77, $d, TMP_78, notes_on_beat = nil, max_duration_on_beat = nil, has_no_notes_on_beat = nil, is_new_part = nil, size = nil, e = nil, increment = nil;
            if ($gvars.log == null) $gvars.log = nil;
if (beat == null) beat = nil;
          notes_on_beat = ($a = ($b = relevant_beat_maps).$map, $a.$$p = (TMP_76 = function(bm){var self = TMP_76.$$s || this;
if (bm == null) bm = nil;
            return bm['$[]'](beat)}, TMP_76.$$s = self, TMP_76), $a).call($b).$flatten().$compact();
            max_duration_on_beat = ($a = ($c = notes_on_beat).$map, $a.$$p = (TMP_77 = function(n){var self = TMP_77.$$s || this;
if (n == null) n = nil;
            return n.$duration()}, TMP_77.$$s = self, TMP_77), $a).call($c).$max();
            has_no_notes_on_beat = notes_on_beat['$empty?']();
            is_new_part = ($a = ($d = notes_on_beat).$select, $a.$$p = (TMP_78 = function(n){var self = TMP_78.$$s || this;
if (n == null) n = nil;
            return n['$first_in_part?']()}, TMP_78.$$s = self, TMP_78), $a).call($d);
            if (has_no_notes_on_beat !== false && has_no_notes_on_beat !== nil) {
              } else {
              try {
              size = conf_beat_resolution * duration_to_style['$[]'](self.$duration_to_id(max_duration_on_beat)).$first()
              } catch ($err) {if (Opal.rescue($err, [$scope.get('Exception')])) {e = $err;
                try {
                  $gvars.log.$error("BUG: unsupported duration: " + (max_duration_on_beat) + " on beat " + (beat) + ",  " + (notes_on_beat.$to_json()))
                } finally {
                  Opal.gvars["!"] = Opal.exceptions.pop() || Opal.nil;
                }
                }else { throw $err; }
              };
              increment = $rb_divide(($rb_plus(size, last_size)), 2);
              last_size = size;
              if ((($a = is_new_part['$empty?']()) !== nil && (!$a.$$is_boolean || $a == true))) {
                } else {
                increment = $rb_plus(increment, increment)
              };
              current_beat = $rb_plus(current_beat, increment);
            };
            return [beat, current_beat];}, TMP_75.$$s = self, TMP_75), $a).call($d));
          return result;
        });

        Opal.defn(self, '$layout_playable', function(root, beat_layout) {
          var $a, self = this;
          if ($gvars.log == null) $gvars.log = nil;

          if ((($a = root['$is_a?']($scope.get('Note'))) !== nil && (!$a.$$is_boolean || $a == true))) {
            return self.$layout_note(root, beat_layout)
          } else if ((($a = root['$is_a?']($scope.get('MeasureStart'))) !== nil && (!$a.$$is_boolean || $a == true))) {
            return self.$layout_measure_start(root, beat_layout)
          } else if ((($a = root['$is_a?']($scope.get('SynchPoint'))) !== nil && (!$a.$$is_boolean || $a == true))) {
            return self.$layout_accord(root, beat_layout)
          } else if ((($a = root['$is_a?']($scope.get('Pause'))) !== nil && (!$a.$$is_boolean || $a == true))) {
            return self.$layout_pause(root, beat_layout)
          } else if ((($a = root['$is_a?']($scope.get('NewPart'))) !== nil && (!$a.$$is_boolean || $a == true))) {
            return self.$layout_newpart(root, beat_layout)
            } else {
            return $gvars.log.$error("Missing Music -> Sheet transform: " + (root))
          };
        });

        Opal.defn(self, '$layout_note', function(root, beat_layout) {
          var $a, $b, TMP_79, $c, self = this, x_offset = nil, y_offset = nil, scale = nil, fill = nil, dotted = nil, size = nil, shift = nil, res = nil;
          if ($gvars.conf == null) $gvars.conf = nil;

          x_offset = $rb_plus($rb_times(($rb_plus($gvars.conf.$get("layout.PITCH_OFFSET"), root.$pitch())), $gvars.conf.$get("layout.X_SPACING")), $gvars.conf.$get("layout.X_OFFSET"));
          y_offset = beat_layout.$call(root.$beat());
          $b = $gvars.conf.$get("layout.DURATION_TO_STYLE")['$[]'](self.$check_duration(root)), $a = Opal.to_ary($b), scale = ($a[0] == null ? nil : $a[0]), fill = ($a[1] == null ? nil : $a[1]), dotted = ($a[2] == null ? nil : $a[2]), $b;
          size = ($a = ($b = $gvars.conf.$get("layout.ELLIPSE_SIZE")).$map, $a.$$p = (TMP_79 = function(e){var self = TMP_79.$$s || this;
if (e == null) e = nil;
          return $rb_times(e, scale)}, TMP_79.$$s = self, TMP_79), $a).call($b);
          shift = 0;
          if ((($a = root.$shift()) !== nil && (!$a.$$is_boolean || $a == true))) {
            if (root.$shift()['$[]']("dir")['$==']("left")) {
              shift = size.$first()['$-@']()
              } else {
              shift = size.$first()
            }};
          res = $scope.get('Ellipse').$new([$rb_plus(x_offset, shift), y_offset], size, fill, dotted, root);
          (($a = [$gvars.conf.$get("layout.LINE_THICK")]), $c = res, $c['$line_width='].apply($c, $a), $a[$a.length-1]);
          return res;
        });

        Opal.defn(self, '$layout_accord', function(root, beat_layout) {
          var $a, $b, TMP_80, $c, TMP_81, self = this, resnotes = nil, resnotes_sorted = nil, res = nil;

          resnotes = ($a = ($b = root.$notes()).$map, $a.$$p = (TMP_80 = function(c){var self = TMP_80.$$s || this;
if (c == null) c = nil;
          return self.$layout_note(c, beat_layout)}, TMP_80.$$s = self, TMP_80), $a).call($b);
          resnotes_sorted = ($a = ($c = resnotes).$sort_by, $a.$$p = (TMP_81 = function(n){var self = TMP_81.$$s || this;
if (n == null) n = nil;
          return n.$origin().$pitch()}, TMP_81.$$s = self, TMP_81), $a).call($c);
          res = [];
          res['$<<']($scope.get('FlowLine').$new(resnotes_sorted.$first(), resnotes_sorted.$last(), "dashed", root, resnotes.$first().$center()));
          res['$<<'](resnotes);
          return res;
        });

        Opal.defn(self, '$layout_pause', function(root, beat_layout) {
          var $a, $b, self = this, x_offset = nil, y_offset = nil, scale = nil, glyph = nil, dotted = nil, rest_size = nil, size = nil, shift = nil, res = nil;
          if ($gvars.conf == null) $gvars.conf = nil;

          x_offset = $rb_plus($rb_times(($rb_plus($gvars.conf.$get("layout.PITCH_OFFSET"), root.$pitch())), $gvars.conf.$get("layout.X_SPACING")), $gvars.conf.$get("layout.X_OFFSET"));
          y_offset = beat_layout.$call(root.$beat());
          self.$check_duration(root);
          $b = $gvars.conf.$get("layout.REST_TO_GLYPH")['$[]'](self.$check_duration(root)), $a = Opal.to_ary($b), scale = ($a[0] == null ? nil : $a[0]), glyph = ($a[1] == null ? nil : $a[1]), dotted = ($a[2] == null ? nil : $a[2]), $b;
          rest_size = $gvars.conf.$get("layout.REST_SIZE");
          size = [$rb_times(rest_size.$first(), scale.$first()), $rb_times(rest_size.$last(), scale.$last())];
          shift = 0;
          if ((($a = root.$shift()) !== nil && (!$a.$$is_boolean || $a == true))) {
            if (root.$shift()['$[]']("dir")['$==']("left")) {
              shift = size.$first()['$-@']()
              } else {
              shift = size.$first()
            }};
          res = nil;
          res = (((($scope.get('Harpnotes')).$$scope.get('Drawing'))).$$scope.get('Glyph')).$new([$rb_plus(x_offset, shift), y_offset], size, glyph, dotted, root);
          if ((($a = root['$visible?']()) !== nil && (!$a.$$is_boolean || $a == true))) {
            } else {
            (($a = [false]), $b = res, $b['$visible='].apply($b, $a), $a[$a.length-1])
          };
          return res;
        });

        Opal.defn(self, '$layout_measure_start', function(root, beat_layout) {
          var $a, $b, TMP_82, $c, self = this, x_offset = nil, y_offset = nil, scale = nil, fill = nil, dotted = nil, size = nil, res = nil;
          if ($gvars.conf == null) $gvars.conf = nil;

          x_offset = $rb_plus($rb_times(($rb_plus($gvars.conf.$get("layout.PITCH_OFFSET"), root.$pitch())), $gvars.conf.$get("layout.X_SPACING")), $gvars.conf.$get("layout.X_OFFSET"));
          y_offset = beat_layout.$call(root.$beat());
          $b = $gvars.conf.$get("layout.DURATION_TO_STYLE")['$[]'](self.$duration_to_id(root.$duration())), $a = Opal.to_ary($b), scale = ($a[0] == null ? nil : $a[0]), fill = ($a[1] == null ? nil : $a[1]), dotted = ($a[2] == null ? nil : $a[2]), $b;
          size = ($a = ($b = $gvars.conf.$get("layout.ELLIPSE_SIZE")).$map, $a.$$p = (TMP_82 = function(e){var self = TMP_82.$$s || this;
if (e == null) e = nil;
          return $rb_times($rb_times(e, scale), 1.2)}, TMP_82.$$s = self, TMP_82), $a).call($b);
          res = $scope.get('Ellipse').$new([x_offset, $rb_minus($rb_minus(y_offset, size.$last()), 0.5)], [size.$first(), 0.1], "filled", false, root, true);
          if ((($a = root['$visible?']()) !== nil && (!$a.$$is_boolean || $a == true))) {
            } else {
            (($a = [false]), $c = res, $c['$visible='].apply($c, $a), $a[$a.length-1])
          };
          return res;
        });

        Opal.defn(self, '$make_path_from_jumpline', function(from, to, north_east_offset, policy) {
          var self = this, start_of_vertical = nil, end_of_vertical = nil, start_orientation = nil, end_orientation = nil, start_offset = nil, end_offset = nil, end_offset_of_line = nil, start_of_jumpline = nil, end_of_jumpline = nil, p1 = nil, p2 = nil, p3 = nil, p4 = nil, p4_line = nil, a1 = nil, a2 = nil, a3 = nil, rp2 = nil, rp3 = nil, rp4 = nil, ra1 = nil, ra2 = nil, ra3 = nil, path = nil;

          start_of_vertical = self.$Vector2d($rb_plus(from.$x(), policy), from.$y());
          end_of_vertical = self.$Vector2d($rb_plus(from.$x(), policy), to.$y());
          start_orientation = self.$Vector2d((($rb_times(($rb_minus(start_of_vertical, from)), [1, 0])).$normalize()).$x(), 0);
          end_orientation = self.$Vector2d((($rb_times(($rb_minus(end_of_vertical, to)), [1, 0])).$normalize()).$x(), 0);
          start_offset = $rb_times(north_east_offset, [start_orientation.$x(), 1]);
          end_offset = $rb_times(north_east_offset, [end_orientation.$x(), -1]);
          end_offset_of_line = $rb_times($rb_times(north_east_offset, [end_orientation.$x(), -1]), [1.5, 1]);
          start_of_vertical = $rb_plus(start_of_vertical, $rb_times(start_offset, [0, 1]));
          end_of_vertical = $rb_plus(end_of_vertical, $rb_times(end_offset, [0, 1]));
          start_of_jumpline = $rb_plus(from, [$rb_times(start_offset.$x(), north_east_offset.$x()), north_east_offset.$y()['$+@']()]);
          end_of_jumpline = $rb_plus(to, [$rb_times(end_offset.$x(), north_east_offset.$x()), north_east_offset.$y()['$-@']()]);
          p1 = $rb_plus(from, start_offset);
          p2 = start_of_vertical;
          p3 = end_of_vertical;
          p4 = $rb_plus(to, end_offset);
          p4_line = $rb_plus(to, end_offset_of_line);
          a1 = $rb_plus($rb_plus(p4, $rb_times(end_orientation, 2.5)), [0, 1]);
          a2 = $rb_minus($rb_plus(p4, $rb_times(end_orientation, 2.5)), [0, 1]);
          a3 = p4;
          rp2 = $rb_minus(p2, p1);
          rp3 = $rb_minus(p3, p2);
          rp4 = $rb_minus(p4_line, p3);
          ra1 = $rb_minus(a1, p4);
          ra2 = $rb_minus(a2, a1);
          ra3 = $rb_minus(p4, a2);
          path = [[["M", p1.$x(), p1.$y()], ["l", rp2.$x(), rp2.$y()], ["l", rp3.$x(), rp3.$y()], ["l", rp4.$x(), rp4.$y()]], [["M", p4.$x(), p4.$y()], ["l", ra1.$x(), ra1.$y()], ["l", ra2.$x(), ra2.$y()], ["l", ra3.$x(), ra3.$y()], ["z"]]];
          return path;
        });

        Opal.defn(self, '$layout_newpart', function(root, beat_layout) {
          var $a, self = this, x_offset = nil, y_offset = nil, res = nil;
          if ($gvars.conf == null) $gvars.conf = nil;
          if ($gvars.log == null) $gvars.log = nil;

          if ((($a = root.$beat()) !== nil && (!$a.$$is_boolean || $a == true))) {
            x_offset = $rb_plus($rb_times(($rb_plus($rb_plus($gvars.conf.$get("layout.PITCH_OFFSET"), root.$pitch()), (-0.5))), $gvars.conf.$get("layout.X_SPACING")), $gvars.conf.$get("layout.X_OFFSET"));
            y_offset = $rb_minus(beat_layout.$call(root.$beat()), $rb_times($gvars.conf.$get("layout.FONT_STYLE_DEF")['$[]']("regular")['$[]']("font_size"), 0.5));
            res = $scope.get('Annotation').$new([x_offset, y_offset], root.$name(), "regular", nil);
            } else {
            $gvars.log.$error("BUG Part without content");
            res = nil;
          };
          return res;
        });

        Opal.defn(self, '$duration_to_id', function(duration) {
          var $a, self = this, result = nil;
          if ($gvars.conf == null) $gvars.conf = nil;

          result = (("d") + (duration)).$to_sym();
          if ((($a = $gvars.conf.$get("layout.DURATION_TO_STYLE")['$[]'](result)['$nil?']()) !== nil && (!$a.$$is_boolean || $a == true))) {
            result = "err"};
          return result;
        });

        Opal.defn(self, '$check_duration', function(root) {
          var $a, self = this, result = nil;
          if ($gvars.log == null) $gvars.log = nil;

          result = self.$duration_to_id(root.$duration());
          if ((($a = result['$===']("err")) !== nil && (!$a.$$is_boolean || $a == true))) {
            $gvars.log.$error("unsupported duration at " + (root.$start_pos_to_s()), root.$start_pos(), root.$end_pos())};
          return result;
        });

        Opal.defn(self, '$make_sheetmark_path', function(note) {
          var $a, $b, TMP_83, self = this, w = nil, h = nil, base = nil, vpath = nil, path = nil;

          w = 0.5;
          h = 5;
          base = $rb_minus(self.$Vector2d(note), [w, $rb_divide(h, 2)]);
          vpath = [self.$Vector2d(w, (w)['$-@']()), self.$Vector2d(w, $rb_times(2, w)), self.$Vector2d(0, h), self.$Vector2d((w)['$-@'](), $rb_times(2, w)), self.$Vector2d((w)['$-@'](), $rb_times(-2, (w))), self.$Vector2d(0, h['$-@']())];
          path = [["M", base.$x(), base.$y()]];
          ($a = ($b = vpath).$each, $a.$$p = (TMP_83 = function(p){var self = TMP_83.$$s || this;
if (p == null) p = nil;
          return path['$<<'](["l", p.$x(), p.$y()])}, TMP_83.$$s = self, TMP_83), $a).call($b);
          return path;
        });

        Opal.defn(self, '$make_slur_path', function(p1, p2) {
          var self = this, deltap = nil, cp_template = nil, cp1 = nil, cp2 = nil, slurpath = nil;

          deltap = $rb_minus(p2, p1);
          cp_template = self.$Vector2d($rb_times(0.3, deltap.$length()), 0).$rotate(deltap.$angle());
          cp1 = cp_template.$rotate(-0.4);
          cp2 = $rb_plus(deltap, cp_template.$reverse().$rotate(0.4));
          slurpath = [["M", p1.$x(), p1.$y()], ["c", cp1.$x(), cp1.$y(), cp2.$x(), cp2.$y(), deltap.$x(), deltap.$y()]];
          return slurpath;
        });

        return (Opal.defn(self, '$make_annotated_bezier_path', function(points) {
          var self = this, p1 = nil, p2 = nil, deltap = nil, cp_template = nil, rotate_by = nil, cp1 = nil, cp2 = nil, cpa1 = nil, cpa2 = nil, cpm1 = nil, cpm2 = nil, cpmm = nil, cpmm1 = nil, cpmm2 = nil, annotation_anchor = nil, slurpath = nil;

          p1 = points.$first();
          p2 = points.$last();
          deltap = $rb_minus(p2, p1);
          cp_template = self.$Vector2d(5, 0).$rotate(deltap.$angle());
          rotate_by = $rb_divide((($scope.get('Math')).$$scope.get('PI')), 2);
          cp1 = cp_template.$rotate(rotate_by['$-@']());
          cp2 = $rb_plus(deltap, cp_template.$reverse().$rotate(rotate_by));
          cpa1 = $rb_plus(p1, cp1);
          cpa2 = $rb_plus(p1, cp2);
          cpm1 = $rb_divide(($rb_plus(p1, cpa1)), 2);
          cpm2 = $rb_divide(($rb_plus(p2, cpa2)), 2);
          cpmm = $rb_divide(($rb_plus(cpa1, cpa2)), 2);
          cpmm1 = $rb_divide(($rb_plus(cpm1, cpmm)), 2);
          cpmm2 = $rb_divide(($rb_plus(cpm2, cpmm)), 2);
          annotation_anchor = $rb_plus($rb_divide(($rb_plus(cpmm1, cpmm2)), 2), $rb_times(($rb_minus(cpmm1, cpmm2)).$perpendicular().$normalize(), 2));
          annotation_anchor = $rb_plus(annotation_anchor, [0, -4]);
          slurpath = [["M", p1.$x(), p1.$y()], ["c", cp1.$x(), cp1.$y(), cp2.$x(), cp2.$y(), deltap.$x(), deltap.$y()]];
          return [slurpath, annotation_anchor];
        }), nil) && 'make_annotated_bezier_path';
      })($scope.base, null);
    })($scope.base);
  })($scope.base)
};

/* Generated by Opal 0.9.0 */
Opal.modules["abc_to_harpnotes_factory"] = function(Opal) {
  Opal.dynamic_require_severity = "error";
  var OPAL_CONFIG = { method_missing: true, arity_check: false, freezing: true, tainting: true };
  function $rb_plus(lhs, rhs) {
    return (typeof(lhs) === 'number' && typeof(rhs) === 'number') ? lhs + rhs : lhs['$+'](rhs);
  }
  var self = Opal.top, $scope = Opal, nil = Opal.nil, $breaker = Opal.breaker, $slice = Opal.slice, $module = Opal.module, $klass = Opal.klass, $gvars = Opal.gvars, $hash2 = Opal.hash2;

  Opal.add_stubs(['$===', '$new', '$error', '$split', '$[]', '$count', '$length', '$last', '$each', '$match', '$parse', '$[]=', '$first', '$<<', '$message', '$+', '$inject', '$get_metadata', '$map', '$select', '$nil?', '$join', '$compact', '$flatten']);
  return (function($base) {
    var $Harpnotes, self = $Harpnotes = $module($base, 'Harpnotes');

    var def = self.$$proto, $scope = self.$$scope;

    (function($base) {
      var $Input, self = $Input = $module($base, 'Input');

      var def = self.$$proto, $scope = self.$$scope;

      (function($base, $super) {
        function $ABCToHarpnotesFactory(){};
        var self = $ABCToHarpnotesFactory = $klass($base, $super, 'ABCToHarpnotesFactory', $ABCToHarpnotesFactory);

        var def = self.$$proto, $scope = self.$$scope;

        return (Opal.defs(self, '$create_engine', function(engine) {
          var self = this, $case = nil, result = nil;
          if ($gvars.log == null) $gvars.log = nil;

          $case = engine;if ("ABC2SVG"['$===']($case)) {result = (((($scope.get('Harpnotes')).$$scope.get('Input'))).$$scope.get('Abc2svgToHarpnotes')).$new()}else {$gvars.log.$error("Undefined abc parser: " + (engine));
          result = (((($scope.get('Harpnotes')).$$scope.get('Input'))).$$scope.get('Abc2svgToHarpnotes')).$new();};
          return result;
        }), nil) && 'create_engine'
      })($scope.base, null);

      (function($base, $super) {
        function $AbstractAbcToHarpnotes(){};
        var self = $AbstractAbcToHarpnotes = $klass($base, $super, 'AbstractAbcToHarpnotes', $AbstractAbcToHarpnotes);

        var def = self.$$proto, $scope = self.$$scope;

        def.abc_code = nil;
        Opal.defn(self, '$initialize', function() {
          var self = this;

          self.abc_code = nil;
          return self.previous_new_part = [];
        });

        Opal.defn(self, '$charpos_to_line_column', function(charpos) {
          var self = this, lines = nil, line_no = nil, char_pos = nil;

          lines = self.abc_code['$[]'](1, charpos).$split("\n");
          line_no = lines.$count();
          char_pos = lines.$last().$length();
          return [line_no, char_pos];
        });

        Opal.defn(self, '$parse_harpnote_config', function(abc_code) {
          var $a, $b, TMP_1, self = this, hn_config_from_song = nil, line_no = nil;

          hn_config_from_song = $hash2([], {});
          line_no = 1;
          ($a = ($b = abc_code.$split("\n")).$each, $a.$$p = (TMP_1 = function(line){var self = TMP_1.$$s || this, $a, $b, TMP_2, $c, $d, entry = nil, parsed_entry = nil, e = nil, message = nil;
            if ($gvars.log == null) $gvars.log = nil;
if (line == null) line = nil;
          entry = ($a = ($b = line).$match, $a.$$p = (TMP_2 = function(m){var self = TMP_2.$$s || this;
if (m == null) m = nil;
            return [m['$[]'](1), m['$[]'](2)]}, TMP_2.$$s = self, TMP_2), $a).call($b, /^%%%%hn\.(print|legend|note|annotation|lyrics) (.*)/);
            if (entry !== false && entry !== nil) {
              try {
              parsed_entry = $scope.get('JSON').$parse(entry.$last());
                parsed_entry['$[]=']("line_no", line_no);
                ($a = entry.$first(), $c = hn_config_from_song, ((($d = $c['$[]']($a)) !== false && $d !== nil) ? $d : $c['$[]=']($a, [])));
                hn_config_from_song['$[]'](entry.$first())['$<<'](parsed_entry);
              } catch ($err) {if (Opal.rescue($err, [$scope.get('Exception')])) {e = $err;
                try {
                  message = ("error in harpnote commands: " + (e.$message()) + " [" + (line_no) + ":" + (entry) + "]");
                  $gvars.log.$error(message, [line_no, 1], [line_no, 2]);
                } finally {
                  Opal.gvars["!"] = Opal.exceptions.pop() || Opal.nil;
                }
                }else { throw $err; }
              }};
            return line_no = $rb_plus(line_no, 1);}, TMP_1.$$s = self, TMP_1), $a).call($b);
          if ((($a = hn_config_from_song['$[]']("legend")) !== nil && (!$a.$$is_boolean || $a == true))) {
            hn_config_from_song['$[]=']("legend", hn_config_from_song['$[]']("legend").$first())};
          if ((($a = hn_config_from_song['$[]']("lyrics")) !== nil && (!$a.$$is_boolean || $a == true))) {
            hn_config_from_song['$[]=']("lyrics", hn_config_from_song['$[]']("lyrics").$first())};
          return hn_config_from_song;
        });

        Opal.defn(self, '$get_metadata', function(abc_code) {
          var $a, $b, TMP_3, self = this, retval = nil;

          retval = ($a = ($b = abc_code.$split("\n")).$inject, $a.$$p = (TMP_3 = function(result, line){var self = TMP_3.$$s || this, $a, $b, TMP_4, entry = nil, key = nil;
if (result == null) result = nil;if (line == null) line = nil;
          entry = ($a = ($b = line).$match, $a.$$p = (TMP_4 = function(m){var self = TMP_4.$$s || this;
if (m == null) m = nil;
            return [m['$[]'](1), m['$[]'](2)]}, TMP_4.$$s = self, TMP_4), $a).call($b, /^([A-Z]):\s*(.*)/);
            if (entry !== false && entry !== nil) {
              key = entry.$first();
              if ((($a = result['$[]'](key)) !== nil && (!$a.$$is_boolean || $a == true))) {
                result['$[]'](key)['$<<'](entry.$last())
                } else {
                result['$[]='](key, [entry.$last()])
              };};
            return result;}, TMP_3.$$s = self, TMP_3), $a).call($b, $hash2([], {}));
          return retval;
        });

        return (Opal.defn(self, '$add_metadata', function(abc_code, new_metadata) {
          var $a, $b, TMP_5, $c, $d, TMP_6, self = this, old_metadata = nil, more_metadata = nil;

          old_metadata = self.$get_metadata(abc_code);
          more_metadata = ($a = ($b = ($c = ($d = new_metadata).$select, $c.$$p = (TMP_6 = function(k, v){var self = TMP_6.$$s || this;
if (k == null) k = nil;if (v == null) v = nil;
          return old_metadata['$[]'](k)['$nil?']()}, TMP_6.$$s = self, TMP_6), $c).call($d)).$map, $a.$$p = (TMP_5 = function(k, v){var self = TMP_5.$$s || this;
if (k == null) k = nil;if (v == null) v = nil;
          return "" + (k) + ":" + (v)}, TMP_5.$$s = self, TMP_5), $a).call($b);
          return [more_metadata, abc_code].$flatten().$compact().$join("\n");
        }), nil) && 'add_metadata';
      })($scope.base, null);
    })($scope.base)
  })($scope.base)
};

/* Generated by Opal 0.9.0 */
Opal.modules["abc2svg_to_harpnotes"] = function(Opal) {
  Opal.dynamic_require_severity = "error";
  var OPAL_CONFIG = { method_missing: true, arity_check: false, freezing: true, tainting: true };
  function $rb_divide(lhs, rhs) {
    return (typeof(lhs) === 'number' && typeof(rhs) === 'number') ? lhs / rhs : lhs['$/'](rhs);
  }
  function $rb_plus(lhs, rhs) {
    return (typeof(lhs) === 'number' && typeof(rhs) === 'number') ? lhs + rhs : lhs['$+'](rhs);
  }
  function $rb_times(lhs, rhs) {
    return (typeof(lhs) === 'number' && typeof(rhs) === 'number') ? lhs * rhs : lhs['$*'](rhs);
  }
  function $rb_minus(lhs, rhs) {
    return (typeof(lhs) === 'number' && typeof(rhs) === 'number') ? lhs - rhs : lhs['$-'](rhs);
  }
  function $rb_lt(lhs, rhs) {
    return (typeof(lhs) === 'number' && typeof(rhs) === 'number') ? lhs < rhs : lhs['$<'](rhs);
  }
  function $rb_gt(lhs, rhs) {
    return (typeof(lhs) === 'number' && typeof(rhs) === 'number') ? lhs > rhs : lhs['$>'](rhs);
  }
  var self = Opal.top, $scope = Opal, nil = Opal.nil, $breaker = Opal.breaker, $slice = Opal.slice, $module = Opal.module, $klass = Opal.klass, $hash2 = Opal.hash2, $gvars = Opal.gvars, $hash = Opal.hash, $range = Opal.range;

  Opal.add_stubs(['$require', '$get', '$_reset_state', '$get_metadata', '$new', '$get_abcmodel', '$_transform_voices', '$meta_data=', '$_make_metadata', '$harpnote_options=', '$_make_harpnote_options', '$[]', '$[]=', '$compact', '$map', '$error', '$_get_key_by_accidentals', '$first', '$==', '$/', '$to_i', '$join', '$private', '$each', '$to_s', '$send', '$charpos_to_line_column', '$each_with_index', '$flatten', '$inject', '$<<', '$_make_jumplines', '$_make_notebound_annotations', '$+', '$count', '$unshift', '$=~', '$_transform_bar_repeat_end', '$_parse_origin', '$_parse_tuplet_info', '$_convert_duration', '$origin=', '$start_pos=', '$end_pos=', '$tuplet=', '$tuplet_start=', '$tuplet_end=', '$empty?', '$last', '$duration=', '$duration', '$origin', '$start_pos', '$end_pos', '$tuplet', '$tuplet_start', '$tuplet_end', '$tie_end=', '$!', '$nil?', '$tie_start=', '$slur_starts=', '$_push_slur', '$_parse_slur', '$slur_ends=', '$_pop_slur', '$_make_repeats_jumps_annotations', '$min', '$round', '$*', '$-', '$raise', '$visible=', '$length', '$pop', '$split', '$debug', '$_extract_chord_lines', '$is_a?', '$start_pos_to_s', '$select', '$match', '$===', '$shift=', '$to_f', '$companion=', '$first_in_part=', '$<', '$>', '$push', '$&', '$>>']);
  self.$require("native");
  return (function($base) {
    var $Harpnotes, self = $Harpnotes = $module($base, 'Harpnotes');

    var def = self.$$proto, $scope = self.$$scope;

    (function($base) {
      var $Input, self = $Input = $module($base, 'Input');

      var def = self.$$proto, $scope = self.$$scope;

      (function($base, $super) {
        function $Abc2svgToHarpnotes(){};
        var self = $Abc2svgToHarpnotes = $klass($base, $super, 'Abc2svgToHarpnotes', $Abc2svgToHarpnotes);

        var def = self.$$proto, $scope = self.$$scope, TMP_1;

        def.abc_code = def.info_fields = def.abc_model = def.next_note_marks = def.repetition_stack = def.tie_started = def._shortest_note = def.pitch_providers = def.previous_note = def.part_table = def.slurstack = def.tuplet_count = def.tuplet_down_count = nil;
        Opal.cdecl($scope, 'ABC2SVG_DURATION_FACTOR', 1536);

        Opal.defn(self, '$initialize', TMP_1 = function() {
          var self = this, $iter = TMP_1.$$p, $yield = $iter || nil, $zuper = nil, $zuper_index = nil;
          if ($gvars.conf == null) $gvars.conf = nil;

          TMP_1.$$p = null;
          $zuper = [];
          for($zuper_index = 0; $zuper_index < arguments.length; $zuper_index++) {
            $zuper[$zuper_index] = arguments[$zuper_index];
          }
          Opal.find_super_dispatcher(self, 'initialize', TMP_1, $iter).apply(self, $zuper);
          self.abc_code = nil;
          self.previous_new_part = [];
          self.part_table = $hash2([], {});
          self._shortest_note = $gvars.conf.$get("layout.SHORTEST_NOTE");
          return self.$_reset_state();
        });

        Opal.defn(self, '$transform', function(zupfnoter_abc) {
          var $a, $b, self = this, abc_parser = nil, result = nil;
          if ($gvars.conf == null) $gvars.conf = nil;

          self.abc_code = zupfnoter_abc;
          self.annotations = $gvars.conf.$get("annotations");
          self.info_fields = self.$get_metadata(self.abc_code);
          abc_parser = (($scope.get('ABC2SVG')).$$scope.get('Abc2Svg')).$new(nil, $hash2(["mode"], {"mode": "model"}));
          self.abc_model = abc_parser.$get_abcmodel(zupfnoter_abc);
          result = self.$_transform_voices();
          (($a = [self.$_make_metadata()]), $b = result, $b['$meta_data='].apply($b, $a), $a[$a.length-1]);
          (($a = [self.$_make_harpnote_options()]), $b = result, $b['$harpnote_options='].apply($b, $a), $a[$a.length-1]);
          return result;
        });

        Opal.defn(self, '$_make_harpnote_options', function() {
          var $a, $b, TMP_2, self = this, result = nil;
          if ($gvars.conf == null) $gvars.conf = nil;

          result = $hash2(["lyrics"], {"lyrics": $hash2(["text"], {"text": self.info_fields['$[]']("W")})});
          result['$[]=']("print", ($a = ($b = $gvars.conf.$get("produce")).$map, $a.$$p = (TMP_2 = function(i){var self = TMP_2.$$s || this, title = nil;
            if ($gvars.conf == null) $gvars.conf = nil;
            if ($gvars.log == null) $gvars.log = nil;
if (i == null) i = nil;
          title = $gvars.conf.$get("extract." + (i) + ".title");
            if (title !== false && title !== nil) {
              return $hash2(["title", "view_id"], {"title": title, "view_id": i})
              } else {
              $gvars.log.$error("could not find extract number " + (i), [1, 1], [1000, 1000]);
              return nil;
            };}, TMP_2.$$s = self, TMP_2), $a).call($b).$compact());
          return result;
        });

        Opal.defn(self, '$_get_key_by_accidentals', function(accidentals) {
          var self = this;

          return $hash(7, "C#", 6, "F#", 5, "B", 4, "E", 3, "A", 2, "D", 1, "G", 0, "C", -1, "F", -2, "Bb", -3, "Eb", -4, "Ab", -5, "Db", -6, "Gb", -7, "Cb")['$[]'](accidentals);
        });

        Opal.defn(self, '$_make_metadata', function() {
          var $a, $b, TMP_3, $c, TMP_4, $d, TMP_5, self = this, key = nil, o_key = nil, o_key_display = nil, tempo_id = nil, tempo_note = nil, duration = nil, duration_display = nil, bpm = nil, tempo = nil;

          key = self.$_get_key_by_accidentals(self.abc_model['$[]']("voices").$first()['$[]']("voice_properties")['$[]']("key")['$[]']("k_sf"));
          o_key = self.$_get_key_by_accidentals(self.abc_model['$[]']("voices").$first()['$[]']("voice_properties")['$[]']("okey")['$[]']("k_sf"));
          o_key_display = "";
          if (key['$=='](o_key)) {
            } else {
            o_key_display = "(Original in " + (o_key) + ")"
          };
          tempo_id = self.abc_model['$[]']("music_type_ids")['$[]']("tempo");
          tempo_note = (function() {try {return self.abc_model['$[]']("voices").$first()['$[]']("voice_properties")['$[]']("sym")['$[]']("extra")['$[]'](tempo_id) } catch ($err) { return nil }})();
          if (tempo_note !== false && tempo_note !== nil) {
            duration = ($a = ($b = tempo_note['$[]']("tempo_notes")).$map, $a.$$p = (TMP_3 = function(i){var self = TMP_3.$$s || this;
if (i == null) i = nil;
            return $rb_divide(i, $scope.get('ABC2SVG_DURATION_FACTOR'))}, TMP_3.$$s = self, TMP_3), $a).call($b);
            duration_display = ($a = ($c = duration).$map, $a.$$p = (TMP_4 = function(d){var self = TMP_4.$$s || this;
if (d == null) d = nil;
            return "1/" + ($rb_divide(1, d))}, TMP_4.$$s = self, TMP_4), $a).call($c);
            bpm = tempo_note['$[]']("tempo_value").$to_i();
            tempo = $hash2(["duration", "bpm"], {"duration": duration, "bpm": bpm});
            } else {
            duration = [0.25];
            duration_display = ($a = ($d = duration).$map, $a.$$p = (TMP_5 = function(d){var self = TMP_5.$$s || this;
if (d == null) d = nil;
            return "1/" + ($rb_divide(1, d))}, TMP_5.$$s = self, TMP_5), $a).call($d);
            bpm = 120;
            tempo = $hash2(["duration", "bpm"], {"duration": duration, "bpm": bpm});
          };
          return $hash2(["composer", "title", "filename", "tempo", "tempo_display", "meter", "key"], {"composer": (((($a = self.info_fields['$[]']("C")) !== false && $a !== nil) ? $a : [])).$join("\n"), "title": (((($a = self.info_fields['$[]']("T")) !== false && $a !== nil) ? $a : [])).$join("\n"), "filename": (((($a = self.info_fields['$[]']("F")) !== false && $a !== nil) ? $a : [])).$join("\n"), "tempo": $hash2(["duration", "bpm"], {"duration": duration, "bpm": bpm}), "tempo_display": [duration_display, "=", bpm].$join(" "), "meter": self.info_fields['$[]']("M"), "key": "" + (key) + " " + (o_key_display)});
        });

        self.$private();

        Opal.defn(self, '$_reset_state', function() {
          var self = this;

          self.jumptargets = $hash2([], {});
          self.next_note_marks = $hash2(["measure", "repeat_start", "variant_ending"], {"measure": false, "repeat_start": false, "variant_ending": nil});
          self.previous_new_part = [];
          self.previous_note = nil;
          self.repetition_stack = [];
          self.tie_started = false;
          self.slurstack = 0;
          self.tuplet_count = 1;
          self.tuplet_down_count = 1;
          return nil;
        });

        Opal.defn(self, '$_transform_voices', function() {
          var $a, $b, TMP_6, $c, TMP_7, self = this, part_id = nil, note_id = nil, hn_voices = nil;

          part_id = self.abc_model['$[]']("music_type_ids")['$[]']("part");
          note_id = self.abc_model['$[]']("music_type_ids")['$[]']("note");
          ($a = ($b = self.abc_model['$[]']("voices").$first()['$[]']("symbols")).$each, $a.$$p = (TMP_6 = function(voice_model_element){var self = TMP_6.$$s || this, $a, $b, part = nil;
            if (self.part_table == null) self.part_table = nil;
if (voice_model_element == null) voice_model_element = nil;
          part = (((($a = (((($b = voice_model_element['$[]']("extra")) !== false && $b !== nil) ? $b : $hash2([], {})))['$[]'](part_id)) !== false && $a !== nil) ? $a : $hash2([], {})))['$[]']("text");
            if (part !== false && part !== nil) {
              return self.part_table['$[]='](voice_model_element['$[]']("time").$to_s(), part)
              } else {
              return nil
            };}, TMP_6.$$s = self, TMP_6), $a).call($b);
          hn_voices = ($a = ($c = self.abc_model['$[]']("voices").$each_with_index()).$map, $a.$$p = (TMP_7 = function(voice_model, voice_index){var self = TMP_7.$$s || this, $a, $b, TMP_8, $c, TMP_9, $d, TMP_10, $e, TMP_11, result = nil, jumplines = nil, notebound_annotations = nil;
            if ($gvars.log == null) $gvars.log = nil;
if (voice_model == null) voice_model = nil;if (voice_index == null) voice_index = nil;
          self.$_reset_state();
            self.pitch_providers = ($a = ($b = voice_model['$[]']("symbols")).$map, $a.$$p = (TMP_8 = function(voice_model_element){var self = TMP_8.$$s || this;
if (voice_model_element == null) voice_model_element = nil;
            nil;
              if (voice_model_element['$[]']("type")['$=='](note_id)) {
                return voice_model_element
                } else {
                return nil
              };}, TMP_8.$$s = self, TMP_8), $a).call($b);
            result = ($a = ($c = voice_model['$[]']("symbols").$each_with_index()).$map, $a.$$p = (TMP_9 = function(voice_model_element, index){var self = TMP_9.$$s || this, type = nil, e = nil;
              if (self.abc_model == null) self.abc_model = nil;
              if ($gvars.log == null) $gvars.log = nil;
if (voice_model_element == null) voice_model_element = nil;if (index == null) index = nil;
            type = self.abc_model['$[]']("music_types")['$[]'](voice_model_element['$[]']("type"));
              try {
              result = self.$send("_transform_" + (type), voice_model_element, index)
              } catch ($err) {if (Opal.rescue($err, [$scope.get('Exception')])) {e = $err;
                try {
                  $gvars.log.$error("BUG: " + (e), self.$charpos_to_line_column(voice_model_element['$[]']("istart")));
                  nil;
                } finally {
                  Opal.gvars["!"] = Opal.exceptions.pop() || Opal.nil;
                }
                }else { throw $err; }
              };
              return result;}, TMP_9.$$s = self, TMP_9), $a).call($c);
            result = result.$flatten();
            jumplines = ($a = ($d = result).$inject, $a.$$p = (TMP_10 = function(jumplines, element){var self = TMP_10.$$s || this;
if (jumplines == null) jumplines = nil;if (element == null) element = nil;
            jumplines['$<<'](self.$_make_jumplines(element));
              return jumplines;}, TMP_10.$$s = self, TMP_10), $a).call($d, []);
            notebound_annotations = ($a = ($e = result).$inject, $a.$$p = (TMP_11 = function(notebound_annotations, element){var self = TMP_11.$$s || this;
if (notebound_annotations == null) notebound_annotations = nil;if (element == null) element = nil;
            return notebound_annotations['$<<'](self.$_make_notebound_annotations(element))}, TMP_11.$$s = self, TMP_11), $a).call($e, []);
            result = $rb_plus(result, ($rb_plus(jumplines, notebound_annotations)));
            result = result.$flatten().$compact();
            if ((($a = (result.$count()['$=='](0))) !== nil && (!$a.$$is_boolean || $a == true))) {
              $gvars.log.$error("Empty voice " + (voice_index));
              result = nil;};
            return result;}, TMP_7.$$s = self, TMP_7), $a).call($c).$compact();
          hn_voices.$unshift(hn_voices.$first());
          return (((($scope.get('Harpnotes')).$$scope.get('Music'))).$$scope.get('Song')).$new(hn_voices);
        });

        Opal.defn(self, '$_transform_bar', function(voice_element) {
          var $a, self = this, result = nil, type = nil;

          result = [];
          type = voice_element['$[]']("bar_type");
          self.next_note_marks['$[]=']("measure", true);
          self.next_note_marks['$[]=']("variant_ending", voice_element['$[]']("text"));
          if ((($a = type['$=~'](/^.*:$/)) !== nil && (!$a.$$is_boolean || $a == true))) {
            self.next_note_marks['$[]=']("repeat_start", true)};
          if ((($a = type['$=~'](/^:.*$/)) !== nil && (!$a.$$is_boolean || $a == true))) {
            return result['$<<'](self.$_transform_bar_repeat_end(voice_element))
            } else {
            return nil
          };
        });

        Opal.defn(self, '$_transform_note', function(voice_element) {
          var $a, $b, TMP_12, $c, $d, $e, TMP_13, $f, TMP_14, TMP_15, self = this, origin = nil, start_pos = nil, end_pos = nil, tuplet = nil, tuplet_end = nil, tuplet_start = nil, notes = nil, result = nil, synchpoint = nil, first_note = nil, amount_of_slur_ends = nil;

          origin = self.$_parse_origin(voice_element);
          $a = [origin['$[]']("startChar"), origin['$[]']("endChar")], start_pos = $a[0], end_pos = $a[1], $a;
          $b = self.$_parse_tuplet_info(voice_element), $a = Opal.to_ary($b), tuplet = ($a[0] == null ? nil : $a[0]), tuplet_end = ($a[1] == null ? nil : $a[1]), tuplet_start = ($a[2] == null ? nil : $a[2]), $b;
          notes = ($a = ($b = voice_element['$[]']("notes")).$map, $a.$$p = (TMP_12 = function(the_note){var self = TMP_12.$$s || this, $a, $b, duration = nil, result = nil;
if (the_note == null) the_note = nil;
          duration = self.$_convert_duration(the_note['$[]']("dur"));
            result = (((($scope.get('Harpnotes')).$$scope.get('Music'))).$$scope.get('Note')).$new(the_note['$[]']("midi"), duration);
            (($a = [origin]), $b = result, $b['$origin='].apply($b, $a), $a[$a.length-1]);
            (($a = [self.$charpos_to_line_column(start_pos)]), $b = result, $b['$start_pos='].apply($b, $a), $a[$a.length-1]);
            (($a = [self.$charpos_to_line_column(end_pos)]), $b = result, $b['$end_pos='].apply($b, $a), $a[$a.length-1]);
            (($a = [tuplet]), $b = result, $b['$tuplet='].apply($b, $a), $a[$a.length-1]);
            (($a = [tuplet_start]), $b = result, $b['$tuplet_start='].apply($b, $a), $a[$a.length-1]);
            (($a = [tuplet_end]), $b = result, $b['$tuplet_end='].apply($b, $a), $a[$a.length-1]);
            return result;}, TMP_12.$$s = self, TMP_12), $a).call($b);
          if ((($a = self.repetition_stack['$empty?']()) !== nil && (!$a.$$is_boolean || $a == true))) {
            self.repetition_stack['$<<'](notes.$last())};
          result = [];
          if (notes.$count()['$=='](1)) {
            result['$<<'](notes.$first())
            } else {
            synchpoint = (((($scope.get('Harpnotes')).$$scope.get('Music'))).$$scope.get('SynchPoint')).$new(notes);
            first_note = notes.$first();
            (($a = [first_note.$duration()]), $c = synchpoint, $c['$duration='].apply($c, $a), $a[$a.length-1]);
            (($a = [first_note.$origin()]), $c = synchpoint, $c['$origin='].apply($c, $a), $a[$a.length-1]);
            (($a = [first_note.$start_pos()]), $c = synchpoint, $c['$start_pos='].apply($c, $a), $a[$a.length-1]);
            (($a = [first_note.$end_pos()]), $c = synchpoint, $c['$end_pos='].apply($c, $a), $a[$a.length-1]);
            (($a = [first_note.$tuplet()]), $c = synchpoint, $c['$tuplet='].apply($c, $a), $a[$a.length-1]);
            (($a = [first_note.$tuplet_start()]), $c = synchpoint, $c['$tuplet_start='].apply($c, $a), $a[$a.length-1]);
            (($a = [first_note.$tuplet_end()]), $c = synchpoint, $c['$tuplet_end='].apply($c, $a), $a[$a.length-1]);
            result['$<<'](synchpoint);
          };
          (($a = [self.tie_started]), $c = result.$first(), $c['$tie_end='].apply($c, $a), $a[$a.length-1]);
          self.tie_started = voice_element['$[]']("ti1")['$nil?']()['$!']();
          (($a = [self.tie_started]), $c = result.$first(), $c['$tie_start='].apply($c, $a), $a[$a.length-1]);
          (($a = [($d = ($e = self.$_parse_slur(voice_element['$[]']("slur_start"))).$map, $d.$$p = (TMP_13 = function(i){var self = TMP_13.$$s || this;
if (i == null) i = nil;
          return self.$_push_slur()}, TMP_13.$$s = self, TMP_13), $d).call($e)]), $c = result.$first(), $c['$slur_starts='].apply($c, $a), $a[$a.length-1]);
          amount_of_slur_ends = (((($a = voice_element['$[]']("slur_end")) !== false && $a !== nil) ? $a : 0));
          (($a = [($d = ($f = ($range(1, amount_of_slur_ends, false))).$map, $d.$$p = (TMP_14 = function(){var self = TMP_14.$$s || this;

          return self.$_pop_slur()}, TMP_14.$$s = self, TMP_14), $d).call($f)]), $c = result.$first(), $c['$slur_ends='].apply($c, $a), $a[$a.length-1]);
          if ((($a = self.next_note_marks['$[]']("measure")) !== nil && (!$a.$$is_boolean || $a == true))) {
            ($a = ($c = notes).$each, $a.$$p = (TMP_15 = function(note){var self = TMP_15.$$s || this;
if (note == null) note = nil;
            return result['$<<']((((($scope.get('Harpnotes')).$$scope.get('Music'))).$$scope.get('MeasureStart')).$new(note))}, TMP_15.$$s = self, TMP_15), $a).call($c);
            self.next_note_marks['$[]=']("measure", false);};
          self.$_make_repeats_jumps_annotations(result, voice_element);
          return result;
        });

        Opal.defn(self, '$_convert_duration', function(raw_duration) {
          var self = this, duration = nil;

          return duration = [128, ($rb_times(($rb_divide(raw_duration, $scope.get('ABC2SVG_DURATION_FACTOR'))), self._shortest_note)).$round()].$min();
        });

        Opal.defn(self, '$_transform_rest', function(voice_element, index) {
          var $a, $b, self = this, origin = nil, start_pos = nil, end_pos = nil, pitch_note = nil, pitch = nil, the_note = nil, duration = nil, tuplet = nil, tuplet_end = nil, tuplet_start = nil, result = nil;

          origin = self.$_parse_origin(voice_element);
          $a = [origin['$[]']("startChar"), origin['$[]']("endChar")], start_pos = $a[0], end_pos = $a[1], $a;
          pitch_note = (((($a = self.pitch_providers['$[]']($range(index, -1, false)).$compact().$first()) !== false && $a !== nil) ? $a : self.pitch_providers['$[]']($range(0, $rb_minus(index, 1), false)).$compact().$last()));
          if (pitch_note !== false && pitch_note !== nil) {
            pitch = pitch_note['$[]']("notes").$first()['$[]']("midi")
            } else {
            pitch = 60
          };
          if ((($a = (pitch['$nil?']())) !== nil && (!$a.$$is_boolean || $a == true))) {
            self.$raise("undefined pitch");
            pitch = 60;};
          the_note = voice_element['$[]']("notes").$first();
          duration = self.$_convert_duration(the_note['$[]']("dur"));
          $b = self.$_parse_tuplet_info(voice_element), $a = Opal.to_ary($b), tuplet = ($a[0] == null ? nil : $a[0]), tuplet_end = ($a[1] == null ? nil : $a[1]), tuplet_start = ($a[2] == null ? nil : $a[2]), $b;
          result = (((($scope.get('Harpnotes')).$$scope.get('Music'))).$$scope.get('Pause')).$new(pitch, duration);
          (($a = [self.$_parse_origin(voice_element)]), $b = result, $b['$origin='].apply($b, $a), $a[$a.length-1]);
          (($a = [self.$charpos_to_line_column(start_pos)]), $b = result, $b['$start_pos='].apply($b, $a), $a[$a.length-1]);
          (($a = [self.$charpos_to_line_column(end_pos)]), $b = result, $b['$end_pos='].apply($b, $a), $a[$a.length-1]);
          (($a = [tuplet]), $b = result, $b['$tuplet='].apply($b, $a), $a[$a.length-1]);
          (($a = [tuplet_start]), $b = result, $b['$tuplet_start='].apply($b, $a), $a[$a.length-1]);
          (($a = [tuplet_end]), $b = result, $b['$tuplet_end='].apply($b, $a), $a[$a.length-1]);
          if ((($a = voice_element['$[]']("invisible")) !== nil && (!$a.$$is_boolean || $a == true))) {
            (($a = [false]), $b = result, $b['$visible='].apply($b, $a), $a[$a.length-1])};
          if ((($a = self.repetition_stack['$empty?']()) !== nil && (!$a.$$is_boolean || $a == true))) {
            self.repetition_stack['$<<'](result)};
          result = [result];
          if ((($a = self.next_note_marks['$[]']("measure")) !== nil && (!$a.$$is_boolean || $a == true))) {
            result['$<<']((((($scope.get('Harpnotes')).$$scope.get('Music'))).$$scope.get('MeasureStart')).$new(result.$first()));
            self.next_note_marks['$[]=']("measure", false);};
          self.$_make_repeats_jumps_annotations(result, voice_element);
          return result;
        });

        Opal.defn(self, '$_transform_yspace', function(voice_element, index) {
          var self = this;

          return nil;
        });

        Opal.defn(self, '$_transform_bar_repeat_end', function(bar) {
          var $a, $b, TMP_16, self = this, start = nil, distance = nil;

          if (self.repetition_stack.$length()['$=='](1)) {
            start = self.repetition_stack.$last()
            } else {
            start = self.repetition_stack.$pop()
          };
          distance = 2;
          ($a = ($b = self.$_extract_chord_lines(bar)).$each, $a.$$p = (TMP_16 = function(line){var self = TMP_16.$$s || this, $a, level = nil;
            if ($gvars.log == null) $gvars.log = nil;
if (line == null) line = nil;
          level = line.$split("@");
            if ((($a = level['$[]'](2)) !== nil && (!$a.$$is_boolean || $a == true))) {
              level = level['$[]'](2);
              $gvars.log.$debug("bar repeat level " + (level) + " " + ("abc2svg_to_harpnotes") + ":" + (360));
              if ((($a = level['$nil?']()) !== nil && (!$a.$$is_boolean || $a == true))) {
                return nil
                } else {
                return distance = level.$to_i()
              };
              } else {
              return nil
            };}, TMP_16.$$s = self, TMP_16), $a).call($b);
          return [(((($scope.get('Harpnotes')).$$scope.get('Music'))).$$scope.get('Goto')).$new(self.previous_note, start, $hash2(["distance"], {"distance": distance}))];
        });

        Opal.defn(self, '$_transform_format', function(voice_element) {
          var self = this;

          return nil;
        });

        Opal.defn(self, '$_transform_meter', function(voice_element) {
          var self = this;

          return nil;
        });

        Opal.defn(self, '$_transform_clef', function(voice_element) {
          var self = this;

          return nil;
        });

        Opal.defn(self, '$_make_jumplines', function(element) {
          var $a, $b, TMP_17, $c, $d, TMP_18, self = this, chords = nil;

          if ((($a = element['$is_a?']((((($scope.get('Harpnotes')).$$scope.get('Music'))).$$scope.get('Playable')))) !== nil && (!$a.$$is_boolean || $a == true))) {
            chords = self.$_extract_chord_lines(element.$origin()['$[]']("raw"));
            return ($a = ($b = ($c = ($d = chords).$select, $c.$$p = (TMP_18 = function(c){var self = TMP_18.$$s || this;
if (c == null) c = nil;
            return c['$[]'](0)['$==']("@")}, TMP_18.$$s = self, TMP_18), $c).call($d)).$inject, $a.$$p = (TMP_17 = function(result, chord){var self = TMP_17.$$s || this, $a, nameparts = nil, targetname = nil, target = nil, argument = nil;
              if (self.jumptargets == null) self.jumptargets = nil;
              if ($gvars.log == null) $gvars.log = nil;
if (result == null) result = nil;if (chord == null) chord = nil;
            nameparts = chord.$split("@");
              targetname = nameparts['$[]'](1);
              target = self.jumptargets['$[]'](targetname);
              argument = ((($a = nameparts['$[]'](2)) !== false && $a !== nil) ? $a : 1);
              argument = argument.$to_i();
              if ((($a = target['$nil?']()) !== nil && (!$a.$$is_boolean || $a == true))) {
                $gvars.log.$error("target '" + (targetname) + "' not found in voice at " + (element.$start_pos_to_s()), element.$start_pos(), element.$end_pos())
                } else {
                result['$<<']((((($scope.get('Harpnotes')).$$scope.get('Music'))).$$scope.get('Goto')).$new(element, target, $hash2(["distance"], {"distance": argument})))
              };
              return result;}, TMP_17.$$s = self, TMP_17), $a).call($b, []);
            } else {
            return nil
          };
        });

        Opal.defn(self, '$_make_notebound_annotations', function(entity) {
          var $a, $b, TMP_19, self = this, result = nil, chords = nil;

          result = [];
          if ((($a = entity['$is_a?']((((($scope.get('Harpnotes')).$$scope.get('Music'))).$$scope.get('Playable')))) !== nil && (!$a.$$is_boolean || $a == true))) {
            chords = self.$_extract_chord_lines(entity.$origin()['$[]']("raw"));
            ($a = ($b = chords).$each, $a.$$p = (TMP_19 = function(name){var self = TMP_19.$$s || this, $a, $b, TMP_20, $c, match = nil, semantic = nil, text = nil, pos_x = nil, pos_y = nil, $case = nil, annotation = nil, notepos = nil, position = nil;
              if (self.annotations == null) self.annotations = nil;
              if ($gvars.log == null) $gvars.log = nil;
if (name == null) name = nil;
            match = name.$match(/^([!#\<\>])([^\@]+)?(\@(\-?[0-9\.]+),(\-?[0-9\.]+))?$/);
              if (match !== false && match !== nil) {
                semantic = match['$[]'](1);
                text = match['$[]'](2);
                if ((($a = match['$[]'](4)) !== nil && (!$a.$$is_boolean || $a == true))) {
                  pos_x = match['$[]'](4)};
                if ((($a = match['$[]'](5)) !== nil && (!$a.$$is_boolean || $a == true))) {
                  pos_y = match['$[]'](5)};
                $case = semantic;if ("#"['$===']($case)) {annotation = self.annotations['$[]'](text);
                if (annotation !== false && annotation !== nil) {
                  } else {
                  $gvars.log.$error("could not find annotation " + (text), entity.$start_pos(), entity.$end_pos())
                };}else if ("!"['$===']($case)) {annotation = $hash2(["text"], {"text": text})}else if ("<"['$===']($case)) {(($a = [$hash2(["dir", "size"], {"dir": "left", "size": text})]), $b = entity, $b['$shift='].apply($b, $a), $a[$a.length-1])}else if (">"['$===']($case)) {(($a = [$hash2(["dir", "size"], {"dir": "right", "size": text})]), $b = entity, $b['$shift='].apply($b, $a), $a[$a.length-1])}else {annotation = nil};
                if (annotation !== false && annotation !== nil) {
                  if (pos_x !== false && pos_x !== nil) {
                    notepos = ($a = ($b = [pos_x, pos_y]).$map, $a.$$p = (TMP_20 = function(p){var self = TMP_20.$$s || this;
if (p == null) p = nil;
                    return p.$to_f()}, TMP_20.$$s = self, TMP_20), $a).call($b)};
                  position = ((($a = ((($c = notepos) !== false && $c !== nil) ? $c : annotation['$[]']("pos"))) !== false && $a !== nil) ? $a : [2, -5]);
                  return result['$<<']((((($scope.get('Harpnotes')).$$scope.get('Music'))).$$scope.get('NoteBoundAnnotation')).$new(entity, $hash2(["pos", "text"], {"pos": position, "text": annotation['$[]']("text")})));
                  } else {
                  return nil
                };
                } else {
                return nil
              };}, TMP_19.$$s = self, TMP_19), $a).call($b);};
          return result;
        });

        Opal.defn(self, '$_make_repeats_jumps_annotations', function(result, voice_element) {
          var $a, $b, TMP_21, $c, $d, TMP_23, self = this, part_label = nil, part = nil, chords = nil;

          self.previous_note = result.$first();
          if ((($a = part_label = self.part_table['$[]'](voice_element['$[]']("time").$to_s())) !== nil && (!$a.$$is_boolean || $a == true))) {
            part = (((($scope.get('Harpnotes')).$$scope.get('Music'))).$$scope.get('NewPart')).$new(part_label);
            (($a = [self.$_parse_origin(voice_element)]), $b = part, $b['$origin='].apply($b, $a), $a[$a.length-1]);
            (($a = [result.$first()]), $b = part, $b['$companion='].apply($b, $a), $a[$a.length-1]);
            (($a = [true]), $b = result.$first(), $b['$first_in_part='].apply($b, $a), $a[$a.length-1]);
            result['$<<'](part);};
          if ((($a = self.next_note_marks['$[]']("repeat_start")) !== nil && (!$a.$$is_boolean || $a == true))) {
            self.repetition_stack['$<<'](result.$first());
            self.next_note_marks['$[]=']("repeat_start", false);};
          if ((($a = self.next_note_marks['$[]']("variant_ending")) !== nil && (!$a.$$is_boolean || $a == true))) {
            result['$<<']((((($scope.get('Harpnotes')).$$scope.get('Music'))).$$scope.get('NoteBoundAnnotation')).$new(result.$first(), $hash2(["pos", "text"], {"pos": [4, -2], "text": self.next_note_marks['$[]']("variant_ending")})));
            self.next_note_marks['$[]=']("variant_ending", nil);};
          chords = self.$_extract_chord_lines(voice_element);
          return ($a = ($b = ($c = ($d = chords).$select, $c.$$p = (TMP_23 = function(chord){var self = TMP_23.$$s || this;
if (chord == null) chord = nil;
          return chord['$[]'](0)['$=='](":")}, TMP_23.$$s = self, TMP_23), $c).call($d)).$each, $a.$$p = (TMP_21 = function(name){var self = TMP_21.$$s || this, $a, $b, TMP_22;
            if (self.jumptargets == null) self.jumptargets = nil;
if (name == null) name = nil;
          return self.jumptargets['$[]='](name['$[]']($range(1, -1, false)), ($a = ($b = result).$select, $a.$$p = (TMP_22 = function(n){var self = TMP_22.$$s || this;
if (n == null) n = nil;
            return n['$is_a?']((((($scope.get('Harpnotes')).$$scope.get('Music'))).$$scope.get('Playable')))}, TMP_22.$$s = self, TMP_22), $a).call($b).$last())}, TMP_21.$$s = self, TMP_21), $a).call($b);
        });

        Opal.defn(self, '$_push_slur', function() {
          var self = this;

          return self.slurstack = $rb_plus(self.slurstack, 1);
        });

        Opal.defn(self, '$_pop_slur', function() {
          var self = this, result = nil;

          result = self.slurstack;
          self.slurstack = $rb_minus(self.slurstack, 1);
          if ($rb_lt(self.slurstack, 0)) {
            self.slurstack = 0};
          return result;
        });

        Opal.defn(self, '$_extract_chord_lines', function(voice_element) {
          var $a, $b, TMP_24, $c, $d, TMP_25, self = this, chords = nil, result = nil;

          chords = voice_element['$[]']("a_gch");
          if (chords !== false && chords !== nil) {
            result = ($a = ($b = ($c = ($d = chords).$select, $c.$$p = (TMP_25 = function(e){var self = TMP_25.$$s || this;
if (e == null) e = nil;
            return e['$[]=']("type", "^")}, TMP_25.$$s = self, TMP_25), $c).call($d)).$map, $a.$$p = (TMP_24 = function(e){var self = TMP_24.$$s || this;
if (e == null) e = nil;
            return e['$[]']("text")}, TMP_24.$$s = self, TMP_24), $a).call($b)
            } else {
            result = []
          };
          return result;
        });

        Opal.defn(self, '$_parse_origin', function(voice_element) {
          var self = this;

          return $hash2(["startChar", "endChar", "raw"], {"startChar": voice_element['$[]']("istart"), "endChar": voice_element['$[]']("iend"), "raw": voice_element});
        });

        Opal.defn(self, '$_parse_slur', function(slurstart) {
          var $a, self = this, startvalue = nil, result = nil;

          startvalue = slurstart;
          result = [];
          while ($rb_gt(startvalue, 0)) {
          result.$push(startvalue['$&'](15));
          startvalue = startvalue['$>>'](4);};
          return result;
        });

        return (Opal.defn(self, '$_parse_tuplet_info', function(voice_element) {
          var $a, $b, self = this, tuplet_start = nil, tuplet = nil, tuplet_end = nil;

          if ((($a = voice_element['$[]']("in_tuplet")) !== nil && (!$a.$$is_boolean || $a == true))) {
            if ((($a = ($b = voice_element['$[]']("extra"), $b !== false && $b !== nil ?voice_element['$[]']("extra")['$[]'](15) : $b)) !== nil && (!$a.$$is_boolean || $a == true))) {
              self.tuplet_count = (voice_element['$[]']("extra")['$[]'](15)['$[]']("tuplet_p"));
              self.tuplet_down_count = self.tuplet_count;
              tuplet_start = true;
              } else {
              tuplet_start = nil
            };
            tuplet = self.tuplet_count;
            if (self.tuplet_down_count['$=='](1)) {
              self.tuplet_count = 1;
              tuplet_end = true;
              } else {
              self.tuplet_down_count = $rb_minus(self.tuplet_down_count, 1);
              tuplet_end = nil;
            };
            } else {
            tuplet = 1;
            tuplet_start = nil;
            tuplet_end = nil;
          };
          return [tuplet, tuplet_end, tuplet_start];
        }), nil) && '_parse_tuplet_info';
      })($scope.base, $scope.get('AbstractAbcToHarpnotes'))
    })($scope.base)
  })($scope.base);
};

/* Generated by Opal 0.9.0 */
Opal.modules["opal-raphael"] = function(Opal) {
  Opal.dynamic_require_severity = "error";
  var OPAL_CONFIG = { method_missing: true, arity_check: false, freezing: true, tainting: true };
  function $rb_divide(lhs, rhs) {
    return (typeof(lhs) === 'number' && typeof(rhs) === 'number') ? lhs / rhs : lhs['$/'](rhs);
  }
  var self = Opal.top, $scope = Opal, nil = Opal.nil, $breaker = Opal.breaker, $slice = Opal.slice, $module = Opal.module, $klass = Opal.klass, $gvars = Opal.gvars, $hash2 = Opal.hash2;

  Opal.add_stubs(['$attr_accessor', '$[]=', '$Native', '$lambda', '$info', '$new', '$line_width=', '$/', '$last', '$path']);
  return (function($base) {
    var $Raphael, self = $Raphael = $module($base, 'Raphael');

    var def = self.$$proto, $scope = self.$$scope;

    (function($base, $super) {
      function $Element(){};
      var self = $Element = $klass($base, $super, 'Element', $Element);

      var def = self.$$proto, $scope = self.$$scope, TMP_1;

      self.$attr_accessor("unhighlight_color", "r", "conf_key", "startpos");

      Opal.defn(self, '$initialize', function(r) {
        var self = this;

        self.r = r;
        self.conf_key = "conf unknown";
        self.startpos = [0, 0];
        return self['$[]=']("stroke-width", 0.5);
      });

      Opal.defn(self, '$[]', function(name) {
        var self = this;

        return self.r.attr(name);
      });

      Opal.defn(self, '$[]=', function(name, value) {
        var self = this;

        return self.r.attr(name, value);
      });

      Opal.defn(self, '$line_width=', function(width) {
        var self = this;

        return self['$[]=']("stroke-width", width);
      });

      Opal.defn(self, '$translate', function(x, y) {
        var self = this;

        return self.r.translate(x, y);
      });

      Opal.defn(self, '$transform', function(cmd) {
        var self = this;

        return self.r.transform(cmd);
      });

      Opal.defn(self, '$get_bbox', function() {
        var self = this;

        return self.$Native(self.r.getBBox());
      });

      return (Opal.defn(self, '$on_click', TMP_1 = function() {
        var self = this, $iter = TMP_1.$$p, block = $iter || nil;

        TMP_1.$$p = null;
        
        var wrapper = function(evt) {
          return block.apply(null, arguments);
        };
        self.r.click(wrapper);
      
      }), nil) && 'on_click';
    })($scope.base, null);

    (function($base, $super) {
      function $Paper(){};
      var self = $Paper = $klass($base, $super, 'Paper', $Paper);

      var def = self.$$proto, $scope = self.$$scope, TMP_3;

      def.r = def.on_drop = def.line_width = def.canvas = nil;
      Opal.defn(self, '$initialize', function(element, width, height) {
        var $a, $b, TMP_2, self = this;

        self.on_drop = ($a = ($b = self).$lambda, $a.$$p = (TMP_2 = function(dropinfo){var self = TMP_2.$$s || this;
          if ($gvars.log == null) $gvars.log = nil;
if (dropinfo == null) dropinfo = nil;
        return $gvars.log.$info(dropinfo)}, TMP_2.$$s = self, TMP_2), $a).call($b);
        self.canvas = [width, height];
        self.scale = 1;
        self.r = Raphael(element, width, height);
        return self.line_width = 0.2;
      });

      Opal.defn(self, '$raw', function() {
        var self = this;

        return self.r;
      });

      Opal.defn(self, '$line_width=', function(width) {
        var self = this;

        return self.line_width = width;
      });

      Opal.defn(self, '$clear', function() {
        var self = this;

        return self.r.clear();
      });

      Opal.defn(self, '$on_annotation_drag_end', TMP_3 = function() {
        var self = this, $iter = TMP_3.$$p, block = $iter || nil;

        TMP_3.$$p = null;
        return self.on_drop = block;
      });

      Opal.defn(self, '$draggable', function(element) {
        var self = this;

        
         var otransform = element.r.transform();
         var me = element.r,
          lx = 0,
          ly = 0,
          ox = 0,
          oy = 0,
          moveFnc = function(dx, dy) {
            scale = this.paper._viewBox[3] / this.paper.canvas.clientHeight ;
            lx = Math.round(scale * dx) + ox;
            ly = Math.round(scale * dy) + oy;

            this.transform('t' + lx + ',' + ly + otransform);
          },
          startFnc = function() {},
          endFnc = function() {
            ox = lx;
            oy = ly;
            element.r.attr({fill: 'red'});
            self.on_drop({element: element.r, "config": element.conf_key, "origin": element.startpos, "delta":  [ox, oy]} );
          };

      element.r.drag(moveFnc, startFnc, endFnc);
      
      });

      Opal.defn(self, '$ellipse', function(x, y, rx, ry) {
        var $a, $b, self = this, result = nil;

        result = (($scope.get('Raphael')).$$scope.get('Element')).$new(self.r.ellipse(x, y, rx, ry));
        (($a = [self.line_width]), $b = result, $b['$line_width='].apply($b, $a), $a[$a.length-1]);
        return result;
      });

      Opal.defn(self, '$path', function(spec) {
        var $a, $b, self = this, result = nil;

        result = (($scope.get('Raphael')).$$scope.get('Element')).$new(self.r.path(spec));
        (($a = [self.line_width]), $b = result, $b['$line_width='].apply($b, $a), $a[$a.length-1]);
        return result;
      });

      Opal.defn(self, '$rect', function(x, y, rx, ry, radius) {
        var $a, $b, self = this, result = nil;

        if (radius == null) {
          radius = 0
        }
        result = (($scope.get('Raphael')).$$scope.get('Element')).$new(self.r.rect(x, y, rx, ry, radius));
        (($a = [self.line_width]), $b = result, $b['$line_width='].apply($b, $a), $a[$a.length-1]);
        return result;
      });

      Opal.defn(self, '$set_view_box', function(x, y, width, height, fit) {
        var self = this;

        self.scale = $rb_divide(self.canvas.$last(), height);
        return self.r.setViewBox(x, y, width, height, fit);
      });

      Opal.defn(self, '$line', function(x1, y1, x2, y2) {
        var self = this;

        return self.$path("M" + (x1) + "," + (y1) + "L" + (x2) + "," + (y2));
      });

      Opal.defn(self, '$text', function(x, y, text, attributes) {
        var self = this, result = nil;

        if (attributes == null) {
          attributes = $hash2([], {})
        }
        result = (($scope.get('Raphael')).$$scope.get('Element')).$new(self.r.text(x ,y, text));
        return result;
      });

      Opal.defn(self, '$size', function() {
        var self = this;

        return [self.r.canvas.offsetWidth, self.r.canvas.offsetHeight];
      });

      return (Opal.defn(self, '$enable_pan_zoom', function() {
        var $a, self = this;

        if ((($a = self.r.panzoom != undefined) !== nil && (!$a.$$is_boolean || $a == true))) {
          return self.r.panzoom().enable();
          } else {
          return nil
        };
      }), nil) && 'enable_pan_zoom';
    })($scope.base, null);
  })($scope.base)
};

/* Generated by Opal 0.9.0 */
Opal.modules["opal-jspdf"] = function(Opal) {
  Opal.dynamic_require_severity = "error";
  var OPAL_CONFIG = { method_missing: true, arity_check: false, freezing: true, tainting: true };
  function $rb_times(lhs, rhs) {
    return (typeof(lhs) === 'number' && typeof(rhs) === 'number') ? lhs * rhs : lhs['$*'](rhs);
  }
  function $rb_minus(lhs, rhs) {
    return (typeof(lhs) === 'number' && typeof(rhs) === 'number') ? lhs - rhs : lhs['$-'](rhs);
  }
  function $rb_plus(lhs, rhs) {
    return (typeof(lhs) === 'number' && typeof(rhs) === 'number') ? lhs + rhs : lhs['$+'](rhs);
  }
  function $rb_divide(lhs, rhs) {
    return (typeof(lhs) === 'number' && typeof(rhs) === 'number') ? lhs / rhs : lhs['$/'](rhs);
  }
  var self = Opal.top, $scope = Opal, nil = Opal.nil, $breaker = Opal.breaker, $slice = Opal.slice, $klass = Opal.klass;

  Opal.add_stubs(['$require', '$attr_accessor', '$apply_offset_to_point', '$[]', '$nil?', '$map', '$*', '$-', '$==', '$+', '$/', '$private', '$first', '$last']);
  self.$require("native");
  
jsPDF.API.setLineDash = function(dashArray, dashPhase) {
  if(dashArray == undefined) {
    this.internal.write('[] 0 d')
  } else {
    this.internal.write('[' + dashArray + '] ' + dashPhase + ' d')
  }

  return this;
};

  return (function($base, $super) {
    function $JsPDF(){};
    var self = $JsPDF = $klass($base, $super, 'JsPDF', $JsPDF);

    var def = self.$$proto, $scope = self.$$scope;

    def.native_jspdf = def.x_offset = def.y_offset = nil;
    self.$attr_accessor("x_offset", "y_offset");

    Opal.defn(self, '$initialize', function(orientation, unit, format) {
      var self = this;

      if (orientation == null) {
        orientation = "p"
      }
      if (unit == null) {
        unit = "mm"
      }
      if (format == null) {
        format = "a4"
      }
      self.x_offset = 0;
      self.y_offset = 0;
      return self.native_jspdf = new jsPDF(orientation, unit, format);
    });

    Opal.defn(self, '$line', function(from, to) {
      var self = this, nfrom = nil, nto = nil;

      nfrom = self.$apply_offset_to_point(from);
      nto = self.$apply_offset_to_point(to);
      return self.native_jspdf.lines([ [ nto[0] - nfrom[0], nto[1] - nfrom[1] ] ], nfrom[0], nfrom[1]);
    });

    Opal.defn(self, '$line_cap=', function(value) {
      var self = this;

      return self.native_jspdf.setLineCap(value);
    });

    Opal.defn(self, '$ellipse', function(center, radii, style) {
      var self = this, ncenter = nil;

      if (style == null) {
        style = undefined
      }
      ncenter = self.$apply_offset_to_point(center);
      return self.native_jspdf.ellipse(ncenter['$[]'](0), ncenter['$[]'](1), radii['$[]'](0), radii['$[]'](1), style);
    });

    Opal.defn(self, '$fill=', function(rgb) {
      var self = this;

      return self.native_jspdf.setFillColor(rgb[0], rgb[1], rgb[2]);
    });

    Opal.defn(self, '$stroke=', function(rgb) {
      var self = this;

      return self.native_jspdf.setDrawColor(rgb[0], rgb[1], rgb[2]);
    });

    Opal.defn(self, '$line_width=', function(width) {
      var self = this;

      return self.native_jspdf.setLineWidth(width);
    });

    Opal.defn(self, '$line_dash=', function(dist) {
      var $a, self = this;

      if (dist == null) {
        dist = 3
      }
      if ((($a = dist['$nil?']()) !== nil && (!$a.$$is_boolean || $a == true))) {
        dist = undefined};
      return self.native_jspdf.setLineDash(dist, dist);
    });

    Opal.defn(self, '$text_color=', function(rgb) {
      var self = this;

      return self.native_jspdf.setTextColor(rgb[0], rgb[1], rgb[2]);
    });

    Opal.defn(self, '$font_size=', function(size) {
      var self = this;

      return self.native_jspdf.setFontSize(size);
    });

    Opal.defn(self, '$font_style=', function(style) {
      var self = this;

      return self.native_jspdf.setFontStyle(style);
    });

    Opal.defn(self, '$use_solid_lines', function() {
      var self = this;

      return self.native_jspdf.setLineDash('', 0);
    });

    Opal.defn(self, '$text', function(x, y, text, flags) {
      var $a, $b, self = this, nx = nil, ny = nil;

      if (flags == null) {
        flags = nil
      }
      $b = self.$apply_offset_to_point([x, y]), $a = Opal.to_ary($b), nx = ($a[0] == null ? nil : $a[0]), ny = ($a[1] == null ? nil : $a[1]), $b;
      return self.native_jspdf.text(nx, ny, text, flags);
    });

    Opal.defn(self, '$rect_like_ellipse', function(center, radii, style) {
      var $a, $b, TMP_1, self = this, ncenter = nil, rsize = nil;

      if (style == null) {
        style = "undefined"
      }
      ncenter = self.$apply_offset_to_point(center);
      rsize = ($a = ($b = radii).$map, $a.$$p = (TMP_1 = function(s){var self = TMP_1.$$s || this;
if (s == null) s = nil;
      return $rb_times(2.0, s)}, TMP_1.$$s = self, TMP_1), $a).call($b);
      return self.native_jspdf.rect($rb_minus(ncenter['$[]'](0), radii['$[]'](0)), $rb_minus(ncenter['$[]'](1), radii['$[]'](1)), rsize['$[]'](0), rsize['$[]'](1), style);
    });

    Opal.defn(self, '$rect', function(x1, y1, x2, y2, style) {
      var $a, $b, self = this, nx1 = nil, ny1 = nil;

      if (style == null) {
        style = "undefined"
      }
      $b = self.$apply_offset_to_point([x1, y1]), $a = Opal.to_ary($b), nx1 = ($a[0] == null ? nil : $a[0]), ny1 = ($a[1] == null ? nil : $a[1]), $b;
      return self.native_jspdf.rect(nx1, ny1, x2, y2, style);
    });

    Opal.defn(self, '$lines', function(lines, x, y, scale, style, close) {
      var $a, $b, self = this, nx = nil, ny = nil;

      $b = self.$apply_offset_to_point([x, y]), $a = Opal.to_ary($b), nx = ($a[0] == null ? nil : $a[0]), ny = ($a[1] == null ? nil : $a[1]), $b;
      return self.native_jspdf.lines(lines, nx, ny, scale, style, close);
    });

    Opal.defn(self, '$output', function(type, options) {
      var $a, self = this;

      if (type == null) {
        type = "raw"
      }
      if (options == null) {
        options = nil
      }
      if (type['$==']("raw")) {
        type = undefined};
      if ((($a = options['$nil?']()) !== nil && (!$a.$$is_boolean || $a == true))) {
        options = undefined};
      return self.native_jspdf.output(type, options);
    });

    Opal.defn(self, '$left_arrowhead', function(x, y) {
      var $a, $b, self = this, delta = nil, nx = nil, ny = nil, x0 = nil, x1 = nil, y_top = nil, y_bottom = nil;

      delta = 1.0;
      $b = self.$apply_offset_to_point([x, y]), $a = Opal.to_ary($b), nx = ($a[0] == null ? nil : $a[0]), ny = ($a[1] == null ? nil : $a[1]), $b;
      x0 = nx;
      x1 = $rb_plus(nx, delta);
      y_top = $rb_plus(ny, $rb_divide(delta, 2.0));
      y_bottom = $rb_minus(ny, $rb_divide(delta, 2.0));
      return self.native_jspdf.triangle(x0, ny, x1, y_top, x1, y_bottom, x0, ny, 'FD');
    });

    Opal.defn(self, '$addPage', function() {
      var self = this;

      return self.native_jspdf.addPage();
    });

    self.$private();

    return (Opal.defn(self, '$apply_offset_to_point', function(point) {
      var self = this;

      return [$rb_plus(point.$first(), self.x_offset), $rb_plus(point.$last(), self.y_offset)];
    }), nil) && 'apply_offset_to_point';
  })($scope.base, null);
};

/* Generated by Opal 0.9.0 */
Opal.modules["opal-jszip"] = function(Opal) {
  Opal.dynamic_require_severity = "error";
  var OPAL_CONFIG = { method_missing: true, arity_check: false, freezing: true, tainting: true };
  var self = Opal.top, $scope = Opal, nil = Opal.nil, $breaker = Opal.breaker, $slice = Opal.slice, $module = Opal.module, $klass = Opal.klass;

  Opal.add_stubs(['$new', '$to_blob']);
  return (function($base) {
    var $JSZip, self = $JSZip = $module($base, 'JSZip');

    var def = self.$$proto, $scope = self.$$scope;

    (function($base, $super) {
      function $ZipFile(){};
      var self = $ZipFile = $klass($base, $super, 'ZipFile', $ZipFile);

      var def = self.$$proto, $scope = self.$$scope;

      Opal.defn(self, '$initialize', function() {
        var self = this;

        return self.zip = new JSZip();
      });

      Opal.defn(self, '$folder', function(name) {
        var self = this;

        return $scope.get('Folder').$new(self.zip.folder(name));
      });

      Opal.defn(self, '$file', function(name, content) {
        var self = this;

        return self.zip.file(name, content);
      });

      Opal.defn(self, '$to_blob', function() {
        var self = this;

        return self.zip.generate({type:"blob"});
      });

      Opal.defn(self, '$to_blob_url', function() {
        var self = this, blob = nil;

        blob = self.$to_blob();
        return window.URL.createObjectURL(blob);
      });

      return (Opal.defn(self, '$to_base64', function() {
        var self = this;

        return self.zip.generate();
      }), nil) && 'to_base64';
    })($scope.base, null);

    (function($base, $super) {
      function $Folder(){};
      var self = $Folder = $klass($base, $super, 'Folder', $Folder);

      var def = self.$$proto, $scope = self.$$scope;

      Opal.defn(self, '$initialize', function(folder) {
        var self = this;

        return self.folder = folder;
      });

      return (Opal.defn(self, '$file', function(name, content) {
        var self = this;

        return self.folder.file(name, content);
      }), nil) && 'file';
    })($scope.base, null);
  })($scope.base)
};

/* Generated by Opal 0.9.0 */
Opal.modules["opal-abcjs"] = function(Opal) {
  Opal.dynamic_require_severity = "error";
  var OPAL_CONFIG = { method_missing: true, arity_check: false, freezing: true, tainting: true };
  function $rb_plus(lhs, rhs) {
    return (typeof(lhs) === 'number' && typeof(rhs) === 'number') ? lhs + rhs : lhs['$+'](rhs);
  }
  var self = Opal.top, $scope = Opal, nil = Opal.nil, $breaker = Opal.breaker, $slice = Opal.slice, $module = Opal.module, $klass = Opal.klass, $hash2 = Opal.hash2;

  Opal.add_stubs(['$Native', '$find', '$css', '$+', '$[]', '$remove', '$new', '$raw', '$to_n']);
  return (function($base) {
    var $ABCJS, self = $ABCJS = $module($base, 'ABCJS');

    var def = self.$$proto, $scope = self.$$scope;

    (function($base) {
      var $Write, self = $Write = $module($base, 'Write');

      var def = self.$$proto, $scope = self.$$scope;

      (function($base, $super) {
        function $Printer(){};
        var self = $Printer = $klass($base, $super, 'Printer', $Printer);

        var def = self.$$proto, $scope = self.$$scope, TMP_1;

        def.parent = nil;
        Opal.defn(self, '$initialize', function(div, printerparams) {
          var self = this, paper = nil, pp = nil;

          if (printerparams == null) {
            printerparams = $hash2([], {})
          }
          self.parent = self.$Native($scope.get('Element').$find("#" + (div)));
          self.parent.$css("width", $rb_plus(printerparams['$[]']("staffwidth"), 50));
          self.parent.$find("svg").$remove();
          paper = (($scope.get('Raphael')).$$scope.get('Paper')).$new(div, printerparams['$[]']("staffwidth"), 900);
          self.paper = paper.$raw();
          pp = printerparams.$to_n();
          return self.printer = new ABCJS.write.Printer(self.paper, pp);
        });

        Opal.defn(self, '$on_select', TMP_1 = function() {
          var self = this, $iter = TMP_1.$$p, block = $iter || nil;

          TMP_1.$$p = null;
          
            self.printer.addSelectListener(
              {// anonymous object!
               highlight: function(abcelem){
                 block.apply(null , [abcelem]);
                }
              }
            )
          
        });

        Opal.defn(self, '$range_highlight', function(from, to) {
          var self = this;

          
        self.printer.rangeHighlight(from, to);
        
          return nil;
        });

        Opal.defn(self, '$range_highlight_more', function(from, to) {
          var self = this;

          
        self.printer.rangeHighlightMore(from, to);
        
          return nil;
        });

        Opal.defn(self, '$range_unhighlight_more', function(from, to) {
          var self = this;

          
        self.printer.rangeUnhighlightMore(from, to);
        
          return nil;
        });

        return (Opal.defn(self, '$draw', function(abc_code) {
          var self = this;

          

        var book = new ABCJS.TuneBook(abc_code);
        var parser = new ABCJS.parse.Parse();
        parser.parse(abc_code);
        var tune = parser.getTune();


        // memoize the some container properties
        // note that printABC changes the width of the surrounding div :-(
        var top = self.parent.scrollTop();
        var width = self.parent.width();

        self.paper.clear();
        self.printer.printABC(tune)

        // reset the aforehead mentioned container poperties
        self.parent.scrollTop(top);
        self.parent.width(width);
        
        }), nil) && 'draw';
      })($scope.base, null)
    })($scope.base)
  })($scope.base)
};

/* Generated by Opal 0.9.0 */
Opal.modules["opal-musicaljs"] = function(Opal) {
  Opal.dynamic_require_severity = "error";
  var OPAL_CONFIG = { method_missing: true, arity_check: false, freezing: true, tainting: true };
  var self = Opal.top, $scope = Opal, nil = Opal.nil, $breaker = Opal.breaker, $slice = Opal.slice, $module = Opal.module, $klass = Opal.klass;

  return (function($base) {
    var $Musicaljs, self = $Musicaljs = $module($base, 'Musicaljs');

    var def = self.$$proto, $scope = self.$$scope;

    (function($base, $super) {
      function $Instrument(){};
      var self = $Instrument = $klass($base, $super, 'Instrument', $Instrument);

      var def = self.$$proto, $scope = self.$$scope;

      Opal.defn(self, '$initialize', function(options) {
        var self = this;

        self.instrument = new Instrument(options);
        return self.isplaying = false;
      });

      Opal.defn(self, '$play', function(options, abc_text) {
        var self = this;

        return self.instrument.play(options, abc_text);
      });

      return (Opal.defn(self, '$tone', function(pitch, velocity, duration, delay, timbre) {
        var self = this;

        return self.instrument.tone(pitch, velocity, duration, delay, timbre);
      }), nil) && 'tone';
    })($scope.base, null)
  })($scope.base)
};

/* Generated by Opal 0.9.0 */
Opal.modules["raphael_engine"] = function(Opal) {
  Opal.dynamic_require_severity = "error";
  var OPAL_CONFIG = { method_missing: true, arity_check: false, freezing: true, tainting: true };
  function $rb_plus(lhs, rhs) {
    return (typeof(lhs) === 'number' && typeof(rhs) === 'number') ? lhs + rhs : lhs['$+'](rhs);
  }
  function $rb_gt(lhs, rhs) {
    return (typeof(lhs) === 'number' && typeof(rhs) === 'number') ? lhs > rhs : lhs['$>'](rhs);
  }
  function $rb_times(lhs, rhs) {
    return (typeof(lhs) === 'number' && typeof(rhs) === 'number') ? lhs * rhs : lhs['$*'](rhs);
  }
  function $rb_minus(lhs, rhs) {
    return (typeof(lhs) === 'number' && typeof(rhs) === 'number') ? lhs - rhs : lhs['$-'](rhs);
  }
  function $rb_divide(lhs, rhs) {
    return (typeof(lhs) === 'number' && typeof(rhs) === 'number') ? lhs / rhs : lhs['$/'](rhs);
  }
  var self = Opal.top, $scope = Opal, nil = Opal.nil, $breaker = Opal.breaker, $slice = Opal.slice, $module = Opal.module, $klass = Opal.klass, $hash2 = Opal.hash2, $gvars = Opal.gvars, $range = Opal.range;

  Opal.add_stubs(['$require', '$include', '$attr_reader', '$new', '$set_view_box', '$clear', '$rect', '$each', '$line_width=', '$line_width', '$is_a?', '$visible?', '$draw_ellipse', '$draw_flowline', '$draw_glyph', '$draw_annotation', '$draw_path', '$debug', '$class', '$children', '$on_annotation_drag_end', '$call', '$Native', '$unhighlight_element', '$highlight_element', '$get_elements_by_range', '$private', '$inject', '$+', '$first', '$join', '$[]', '$sort', '$each_key', '$origin', '$nil?', '$map', '$>', '$*', '$-', '$last', '$push', '$unhighlight_color=', '$[]=', '$include?', '$unhighlight_color', '$<<', '$rect?', '$center', '$size', '$ellipse', '$push_element', '$==', '$fill', '$dotted?', '$on_click', '$path_to_raphael', '$glyph', '$transform', '$/', '$path', '$get_bbox', '$-@', '$line', '$from', '$to', '$style', '$distance', '$level', '$translate', '$get', '$gsub', '$text', '$draggable', '$conf_key=', '$conf_key', '$startpos=', '$filled?']);
  self.$require("opal-raphael");
  self.$require("harpnotes");
  return (function($base) {
    var $Harpnotes, self = $Harpnotes = $module($base, 'Harpnotes');

    var def = self.$$proto, $scope = self.$$scope;

    (function($base, $super) {
      function $RaphaelEngine(){};
      var self = $RaphaelEngine = $klass($base, $super, 'RaphaelEngine', $RaphaelEngine);

      var def = self.$$proto, $scope = self.$$scope, TMP_2, TMP_3;

      def.paper = def.highlighted = def.elements = def.x = def.container_id = nil;
      self.$include((($scope.get('Harpnotes')).$$scope.get('Drawing')));

      self.$attr_reader("paper");

      Opal.cdecl($scope, 'PADDING', 5);

      Opal.cdecl($scope, 'ARROW_SIZE', 1.0);

      Opal.cdecl($scope, 'JUMPLINE_INDENT', 10);

      Opal.cdecl($scope, 'DOTTED_SIZE', 0.3);

      Opal.defn(self, '$initialize', function(element_id, width, height) {
        var self = this;

        self.container_id = element_id;
        self.paper = (($scope.get('Raphael')).$$scope.get('Paper')).$new(element_id, width, height);
        self.on_select = nil;
        self.elements = $hash2([], {});
        return self.highlighted = [];
      });

      Opal.defn(self, '$set_view_box', function(x, y, width, height) {
        var self = this;

        return self.paper.$set_view_box(x, y, width, height, true);
      });

      Opal.defn(self, '$draw', function(sheet) {
        var $a, $b, TMP_1, self = this;

        self.paper.$clear();
        self.elements = $hash2([], {});
        self.highlighted = [];
        self.paper.$rect(1.0, 1.0, 418, 295);
        self.paper.$rect(0.0, 0.0, 420.0, 297.0);
        return ($a = ($b = sheet.$children()).$each, $a.$$p = (TMP_1 = function(child){var self = TMP_1.$$s || this, $a, $b;
          if (self.paper == null) self.paper = nil;
          if ($gvars.log == null) $gvars.log = nil;
if (child == null) child = nil;
        (($a = [child.$line_width()]), $b = self.paper, $b['$line_width='].apply($b, $a), $a[$a.length-1]);
          if ((($a = child['$is_a?']($scope.get('Ellipse'))) !== nil && (!$a.$$is_boolean || $a == true))) {
            if ((($a = child['$visible?']()) !== nil && (!$a.$$is_boolean || $a == true))) {
              return self.$draw_ellipse(child)
              } else {
              return nil
            }
          } else if ((($a = child['$is_a?']($scope.get('FlowLine'))) !== nil && (!$a.$$is_boolean || $a == true))) {
            if ((($a = child['$visible?']()) !== nil && (!$a.$$is_boolean || $a == true))) {
              return self.$draw_flowline(child)
              } else {
              return nil
            }
          } else if ((($a = child['$is_a?']((((($scope.get('Harpnotes')).$$scope.get('Drawing'))).$$scope.get('Glyph')))) !== nil && (!$a.$$is_boolean || $a == true))) {
            if ((($a = child['$visible?']()) !== nil && (!$a.$$is_boolean || $a == true))) {
              return self.$draw_glyph(child)
              } else {
              return nil
            }
          } else if ((($a = child['$is_a?']((((($scope.get('Harpnotes')).$$scope.get('Drawing'))).$$scope.get('Annotation')))) !== nil && (!$a.$$is_boolean || $a == true))) {
            if ((($a = child['$visible?']()) !== nil && (!$a.$$is_boolean || $a == true))) {
              return self.$draw_annotation(child)
              } else {
              return nil
            }
          } else if ((($a = child['$is_a?']((((($scope.get('Harpnotes')).$$scope.get('Drawing'))).$$scope.get('Path')))) !== nil && (!$a.$$is_boolean || $a == true))) {
            if ((($a = child['$visible?']()) !== nil && (!$a.$$is_boolean || $a == true))) {
              return self.$draw_path(child)
              } else {
              return nil
            }
            } else {
            $gvars.log.$debug("BUG:don't know how to draw " + (child.$class()) + " (" + ("raphael_engine") + " " + (49) + ")");
            return nil;
          };}, TMP_1.$$s = self, TMP_1), $a).call($b);
      });

      Opal.defn(self, '$on_select', TMP_2 = function() {
        var self = this, $iter = TMP_2.$$p, block = $iter || nil;

        TMP_2.$$p = null;
        return self.on_select = block;
      });

      Opal.defn(self, '$on_annotation_drag_end', TMP_3 = function() {
        var $a, $b, TMP_4, self = this, $iter = TMP_3.$$p, block = $iter || nil;

        TMP_3.$$p = null;
        return ($a = ($b = self.paper).$on_annotation_drag_end, $a.$$p = (TMP_4 = function(info){var self = TMP_4.$$s || this;
if (info == null) info = nil;
        return block.$call(self.$Native(info))}, TMP_4.$$s = self, TMP_4), $a).call($b);
      });

      Opal.defn(self, '$unhighlight_all', function() {
        var $a, $b, TMP_5, self = this;

        return ($a = ($b = self.highlighted).$each, $a.$$p = (TMP_5 = function(e){var self = TMP_5.$$s || this;
if (e == null) e = nil;
        return self.$unhighlight_element(e)}, TMP_5.$$s = self, TMP_5), $a).call($b);
      });

      Opal.defn(self, '$range_highlight', function(from, to) {
        var $a, $b, TMP_6, self = this;

        return ($a = ($b = self.$get_elements_by_range(from, to)).$each, $a.$$p = (TMP_6 = function(element){var self = TMP_6.$$s || this;
if (element == null) element = nil;
        return self.$highlight_element(element)}, TMP_6.$$s = self, TMP_6), $a).call($b);
      });

      Opal.defn(self, '$range_unhighlight', function(from, to) {
        var $a, $b, TMP_7, self = this;

        return ($a = ($b = self.$get_elements_by_range(from, to)).$each, $a.$$p = (TMP_7 = function(element){var self = TMP_7.$$s || this;
if (element == null) element = nil;
        return self.$unhighlight_element(element)}, TMP_7.$$s = self, TMP_7), $a).call($b);
      });

      self.$private();

      Opal.defn(self, '$path_to_raphael', function(path) {
        var $a, $b, TMP_8, self = this, result = nil;

        result = ($a = ($b = path).$inject, $a.$$p = (TMP_8 = function(result, element){var self = TMP_8.$$s || this;
if (result == null) result = nil;if (element == null) element = nil;
        result = $rb_plus(result, element.$first());
          return result = $rb_plus(result, element['$[]']($range(1, -1, false)).$join(" "));}, TMP_8.$$s = self, TMP_8), $a).call($b, "");
        return result;
      });

      Opal.defn(self, '$get_elements_by_range', function(from, to) {
        var $a, $b, TMP_9, self = this, result = nil, range = nil;

        result = [];
        range = [from, to].$sort();
        ($a = ($b = self.elements).$each_key, $a.$$p = (TMP_9 = function(k){var self = TMP_9.$$s || this, $a, $b, TMP_10, $c, TMP_11, origin = nil, noterange = nil;
          if (self.elements == null) self.elements = nil;
if (k == null) k = nil;
        origin = self.$Native(k.$origin());
          if ((($a = origin['$nil?']()) !== nil && (!$a.$$is_boolean || $a == true))) {
            return nil
            } else {
            noterange = ($a = ($b = ["startChar", "endChar"]).$map, $a.$$p = (TMP_10 = function(c){var self = TMP_10.$$s || this;
if (c == null) c = nil;
            return self.$Native(k.$origin())['$[]'](c)}, TMP_10.$$s = self, TMP_10), $a).call($b).$sort();
            if ($rb_gt($rb_times(($rb_minus(range.$first(), noterange.$last())), ($rb_minus(noterange.$first(), range.$last()))), 0)) {
              return ($a = ($c = self.elements['$[]'](k)).$each, $a.$$p = (TMP_11 = function(e){var self = TMP_11.$$s || this;
if (e == null) e = nil;
              return result.$push(e)}, TMP_11.$$s = self, TMP_11), $a).call($c)
              } else {
              return nil
            };
          };}, TMP_9.$$s = self, TMP_9), $a).call($b);
        return result;
      });

      Opal.defn(self, '$highlight_element', function(element) {
        var $a, $b, self = this;

        self.$unhighlight_element(element);
        self.highlighted.$push(element);
        if ((($a = self.x) !== nil && (!$a.$$is_boolean || $a == true))) {
          } else {
          self.x = 0
        };
        
            var node = element.r;
            var bbox = node.getBBox();
            var top = bbox.y + bbox.y2;
            top = 100 * Math.floor(top/100);
            $("#"+self.container_id).get(0).scrollTop=top;
      ;
        (($a = [element['$[]']("fill")]), $b = element, $b['$unhighlight_color='].apply($b, $a), $a[$a.length-1]);
        element['$[]=']("fill", "#ff0000");
        element['$[]=']("stroke", "#ff0000");
        return nil;
      });

      Opal.defn(self, '$unhighlight_element', function(element) {
        var $a, self = this;

        if ((($a = self.highlighted['$include?'](element)) !== nil && (!$a.$$is_boolean || $a == true))) {
          self.highlighted = $rb_minus(self.highlighted, [element]);
          element['$[]=']("fill", element.$unhighlight_color());
          element['$[]=']("stroke", "#000000");};
        return nil;
      });

      Opal.defn(self, '$push_element', function(root, element) {
        var $a, $b, $c, self = this;

        ($a = root, $b = self.elements, ((($c = $b['$[]']($a)) !== false && $c !== nil) ? $c : $b['$[]=']($a, [])));
        return self.elements['$[]'](root)['$<<'](element);
      });

      Opal.defn(self, '$draw_ellipse', function(root) {
        var $a, $b, TMP_12, $c, TMP_13, self = this, e = nil, x = nil, y = nil, e_dot = nil;

        if ((($a = root['$rect?']()) !== nil && (!$a.$$is_boolean || $a == true))) {
          e = self.paper.$rect($rb_minus(root.$center().$first(), root.$size().$first()), $rb_minus(root.$center().$last(), root.$size().$last()), $rb_times(2, root.$size().$first()), $rb_times(2, root.$size().$last()))
          } else {
          e = self.paper.$ellipse(root.$center().$first(), root.$center().$last(), root.$size().$first(), root.$size().$last())
        };
        self.$push_element(root.$origin(), e);
        e['$[]=']("fill", (function() {if (root.$fill()['$==']("filled")) {
          return "black"
          } else {
          return "white"
        }; return nil; })());
        if ((($a = root['$dotted?']()) !== nil && (!$a.$$is_boolean || $a == true))) {
          x = $rb_plus(root.$center().$first(), ($rb_times(root.$size().$first(), 1.2)));
          y = $rb_plus(root.$center().$last(), ($rb_times(root.$size().$last(), 1.2)));
          e_dot = self.paper.$ellipse(x, y, $scope.get('DOTTED_SIZE'), $scope.get('DOTTED_SIZE'));
          e_dot['$[]=']("fill", "black");
          self.$push_element(root.$origin(), e_dot);
          ($a = ($b = e_dot).$on_click, $a.$$p = (TMP_12 = function(){var self = TMP_12.$$s || this, $a, $b, origin = nil;
            if (self.on_select == null) self.on_select = nil;

          origin = root.$origin();
            if ((($a = ((($b = origin['$nil?']()) !== false && $b !== nil) ? $b : self.on_select['$nil?']())) !== nil && (!$a.$$is_boolean || $a == true))) {
              return nil
              } else {
              return self.on_select.$call(origin)
            };}, TMP_12.$$s = self, TMP_12), $a).call($b);};
        return ($a = ($c = e).$on_click, $a.$$p = (TMP_13 = function(){var self = TMP_13.$$s || this, $a, $b, origin = nil;
          if (self.on_select == null) self.on_select = nil;

        origin = root.$origin();
          if ((($a = ((($b = origin['$nil?']()) !== false && $b !== nil) ? $b : self.on_select['$nil?']())) !== nil && (!$a.$$is_boolean || $a == true))) {
            return nil
            } else {
            return self.on_select.$call(origin)
          };}, TMP_13.$$s = self, TMP_13), $a).call($c);
      });

      Opal.defn(self, '$draw_glyph', function(root) {
        var $a, $b, TMP_14, $c, TMP_15, self = this, center = nil, size = nil, path_spec = nil, e = nil, bbox = nil, glyph_center = nil, scalefactor = nil, x = nil, y = nil, e_dot = nil;

        center = [root.$center().$first(), root.$center().$last()];
        size = [$rb_times(root.$size().$first(), 2), $rb_times(root.$size().$last(), 2)];
        path_spec = self.$path_to_raphael(root.$glyph()['$[]']("d"));
        e = self.paper.$rect(root.$center().$first(), $rb_minus(root.$center().$last(), size.$last()), size.$first(), size.$last());
        e['$[]=']("fill", "white");
        e['$[]=']("stroke", "white");
        e.$transform("t-" + ($rb_divide(size.$first(), 2)) + " " + ($rb_divide(size.$last(), 2)));
        e = self.paper.$path(path_spec);
        e['$[]=']("fill", "black");
        self.$push_element(root.$origin(), e);
        ($a = ($b = e).$on_click, $a.$$p = (TMP_14 = function(){var self = TMP_14.$$s || this, $a, $b, origin = nil;
          if (self.on_select == null) self.on_select = nil;

        origin = root.$origin();
          if ((($a = ((($b = origin['$nil?']()) !== false && $b !== nil) ? $b : self.on_select['$nil?']())) !== nil && (!$a.$$is_boolean || $a == true))) {
            return nil
            } else {
            return self.on_select.$call(origin)
          };}, TMP_14.$$s = self, TMP_14), $a).call($b);
        bbox = e.$get_bbox();
        glyph_center = [$rb_divide(($rb_plus(bbox['$[]']("x"), bbox['$[]']("x2"))), 2), $rb_divide(($rb_plus(bbox['$[]']("y"), bbox['$[]']("y2"))), 2)];
        scalefactor = $rb_divide(size.$last(), bbox['$[]']("height"));
        e.$transform("t" + ((center.$first())) + " " + ((center.$last())) + "t" + ((glyph_center.$first()['$-@']())) + " " + ((glyph_center.$last()['$-@']())) + "s" + (scalefactor));
        if ((($a = root['$dotted?']()) !== nil && (!$a.$$is_boolean || $a == true))) {
          bbox = e.$get_bbox();
          x = $rb_plus(bbox['$[]']("x2"), 0.5);
          y = $rb_plus(bbox['$[]']("y2"), 0.5);
          e_dot = self.paper.$ellipse(x, y, $scope.get('DOTTED_SIZE'), $scope.get('DOTTED_SIZE'));
          e_dot['$[]=']("fill", "black");
          self.$push_element(root.$origin(), e_dot);
          return ($a = ($c = e_dot).$on_click, $a.$$p = (TMP_15 = function(){var self = TMP_15.$$s || this, $a, $b, origin = nil;
            if (self.on_select == null) self.on_select = nil;

          origin = root.$origin();
            if ((($a = ((($b = origin['$nil?']()) !== false && $b !== nil) ? $b : self.on_select['$nil?']())) !== nil && (!$a.$$is_boolean || $a == true))) {
              return nil
              } else {
              return self.on_select.$call(origin)
            };}, TMP_15.$$s = self, TMP_15), $a).call($c);
          } else {
          return nil
        };
      });

      Opal.defn(self, '$draw_flowline', function(root) {
        var self = this, l = nil;

        l = self.paper.$line(root.$from().$center()['$[]'](0), root.$from().$center()['$[]'](1), root.$to().$center()['$[]'](0), root.$to().$center()['$[]'](1));
        if (root.$style()['$==']("dashed")) {
          l['$[]=']("stroke-dasharray", "-")};
        if (root.$style()['$==']("dotted")) {
          return l['$[]=']("stroke-dasharray", ". ")
          } else {
          return nil
        };
      });

      Opal.defn(self, '$draw_jumpline', function(root) {
        var $a, $b, self = this, startpoint = nil, endpoint = nil, distance = nil, depth = nil, path = nil, arrow = nil;

        startpoint = root.$from().$center();
        ($a = 0, $b = startpoint, $b['$[]=']($a, $rb_plus($b['$[]']($a), $scope.get('PADDING'))));
        endpoint = root.$to().$center();
        ($a = 0, $b = endpoint, $b['$[]=']($a, $rb_plus($b['$[]']($a), $scope.get('PADDING'))));
        distance = root.$distance();
        if ((($a = distance['$nil?']()) !== nil && (!$a.$$is_boolean || $a == true))) {
          depth = $rb_minus(420, ($rb_times(root.$level(), $scope.get('JUMPLINE_INDENT'))))
          } else {
          depth = $rb_plus(endpoint['$[]'](0), distance)
        };
        path = "M" + (endpoint['$[]'](0)) + "," + (endpoint['$[]'](1)) + "L" + (depth) + "," + (endpoint['$[]'](1)) + "L" + (depth) + "," + (startpoint['$[]'](1)) + "L" + (startpoint['$[]'](0)) + "," + (startpoint['$[]'](1));
        self.paper.$path(path);
        arrow = self.paper.$path("M0,0L" + ($scope.get('ARROW_SIZE')) + "," + ($rb_times(-0.5, $scope.get('ARROW_SIZE'))) + "L" + ($scope.get('ARROW_SIZE')) + "," + ($rb_times(0.5, $scope.get('ARROW_SIZE'))) + "L0,0");
        arrow['$[]=']("fill", "red");
        return arrow.$translate(startpoint['$[]'](0), startpoint['$[]'](1));
      });

      Opal.defn(self, '$draw_annotation', function(root) {
        var $a, $b, self = this, style = nil, mm_per_point = nil, text = nil, element = nil, scaley = nil, scalex = nil, bbox = nil, dx = nil, dy = nil, translation = nil;
        if ($gvars.conf == null) $gvars.conf = nil;

        style = ((($a = $gvars.conf.$get("layout.FONT_STYLE_DEF")['$[]'](root.$style())) !== false && $a !== nil) ? $a : $gvars.conf.$get("layout.FONT_STYLE_DEF")['$[]']("regular"));
        mm_per_point = $gvars.conf.$get("layout.MM_PER_POINT");
        text = root.$text().$gsub("\n\n", "\n \n");
        element = self.paper.$text(root.$center().$first(), root.$center().$last(), text);
        element['$[]=']("font-size", 1);
        element['$[]=']("font-weight", style['$[]']("font_style"));
        element['$[]=']("text-anchor", "start");
        scaley = $rb_divide(style['$[]']("font_size"), 3);
        scalex = $rb_divide($rb_times(scaley, 45), 42.5);
        element.$transform("s" + (scalex) + "," + (scaley));
        bbox = element.$get_bbox();
        dx = $rb_minus(root.$center().$first(), bbox['$[]']("x"));
        dy = $rb_minus(root.$center().$last(), bbox['$[]']("y"));
        translation = "s" + (scalex) + "," + (scaley) + "T" + (dx) + "," + (dy);
        element.$transform(translation);
        self.paper.$draggable(element);
        (($a = [root.$conf_key()]), $b = element, $b['$conf_key='].apply($b, $a), $a[$a.length-1]);
        (($a = [root.$center()]), $b = element, $b['$startpos='].apply($b, $a), $a[$a.length-1]);
        return element;
      });

      return (Opal.defn(self, '$draw_path', function(root) {
        var $a, self = this, path_spec = nil, e = nil;

        path_spec = self.$path_to_raphael(root.$path());
        e = self.paper.$path(path_spec);
        if ((($a = root['$filled?']()) !== nil && (!$a.$$is_boolean || $a == true))) {
          return e['$[]=']("fill", "#000000")
          } else {
          return nil
        };
      }), nil) && 'draw_path';
    })($scope.base, null)
  })($scope.base);
};

/* Generated by Opal 0.9.0 */
Opal.modules["pdf_engine"] = function(Opal) {
  Opal.dynamic_require_severity = "error";
  var OPAL_CONFIG = { method_missing: true, arity_check: false, freezing: true, tainting: true };
  function $rb_divide(lhs, rhs) {
    return (typeof(lhs) === 'number' && typeof(rhs) === 'number') ? lhs / rhs : lhs['$/'](rhs);
  }
  function $rb_times(lhs, rhs) {
    return (typeof(lhs) === 'number' && typeof(rhs) === 'number') ? lhs * rhs : lhs['$*'](rhs);
  }
  function $rb_plus(lhs, rhs) {
    return (typeof(lhs) === 'number' && typeof(rhs) === 'number') ? lhs + rhs : lhs['$+'](rhs);
  }
  function $rb_minus(lhs, rhs) {
    return (typeof(lhs) === 'number' && typeof(rhs) === 'number') ? lhs - rhs : lhs['$-'](rhs);
  }
  var self = Opal.top, $scope = Opal, nil = Opal.nil, $breaker = Opal.breaker, $slice = Opal.slice, $module = Opal.module, $klass = Opal.klass, $range = Opal.range, $gvars = Opal.gvars, $hash2 = Opal.hash2;

  Opal.add_stubs(['$require', '$include', '$attr_reader', '$/', '$new', '$x_offset=', '$*', '$y_offset=', '$each', '$draw_segment', '$+', '$rect', '$draw_cutmarks', '$line_width=', '$line_width', '$is_a?', '$visible?', '$draw_ellipse', '$draw_flowline', '$draw_glyph', '$draw_path', '$draw_annotation', '$debug', '$class', '$children', '$private', '$[]', '$get', '$style', '$text_color=', '$font_size=', '$font_style=', '$text', '$first', '$center', '$last', '$Vector2d', '$line', '$to_a', '$-@', '$filled?', '$fill=', '$map', '$rect?', '$rect_like_ellipse', '$size', '$ellipse', '$dotted?', '$zip', '$-', '$stroke=', '$glyph', '$===', '$empty?', '$lines', '$push', '$error', '$addPage', '$draw', '$dashed?', '$line_dash=', '$from', '$to', '$use_solid_lines', '$clone', '$[]=', '$distance', '$nil?', '$level', '$left_arrowhead', '$path']);
  self.$require("opal-jspdf");
  self.$require("harpnotes");
  return (function($base) {
    var $Harpnotes, self = $Harpnotes = $module($base, 'Harpnotes');

    var def = self.$$proto, $scope = self.$$scope;

    (function($base, $super) {
      function $PDFEngine(){};
      var self = $PDFEngine = $klass($base, $super, 'PDFEngine', $PDFEngine);

      var def = self.$$proto, $scope = self.$$scope;

      def.pdf = nil;
      self.$include((($scope.get('Harpnotes')).$$scope.get('Drawing')));

      self.$attr_reader("pdf");

      Opal.cdecl($scope, 'PADDING', 4.0);

      Opal.cdecl($scope, 'JUMPLINE_INDENT', 10.0);

      Opal.cdecl($scope, 'DOTTED_SIZE', 0.3);

      Opal.cdecl($scope, 'X_SPACING', $rb_divide(115.0, 10.0));

      Opal.defn(self, '$initialize', function() {
        var $a, $b, self = this;

        self.pdf = $scope.get('JsPDF').$new("l", "mm", "a3");
        return (($a = [0.0]), $b = self.pdf, $b['$x_offset='].apply($b, $a), $a[$a.length-1]);
      });

      Opal.defn(self, '$draw_in_segments', function(sheet) {
        var $a, $b, TMP_1, self = this, delta = nil, addpage = nil;

        delta = $rb_times(-12.0, $scope.get('X_SPACING'));
        self.pdf = $scope.get('JsPDF').$new("p", "mm", [210, 287]);
        (($a = [-5]), $b = self.pdf, $b['$y_offset='].apply($b, $a), $a[$a.length-1]);
        addpage = false;
        ($a = ($b = ($range(0, 2, false))).$each, $a.$$p = (TMP_1 = function(i){var self = TMP_1.$$s || this;
if (i == null) i = nil;
        self.$draw_segment($rb_plus(30, $rb_times(i, delta)), sheet, addpage);
          return addpage = true;}, TMP_1.$$s = self, TMP_1), $a).call($b);
        return self.pdf;
      });

      Opal.defn(self, '$draw', function(sheet) {
        var $a, $b, TMP_2, $c, TMP_4, self = this, delta = nil;

        self.pdf.$rect(1.0, 1.0, 418, 295);
        self.pdf.$rect(0.0, 0.0, 420.0, 297.0);
        delta = $rb_times(12.0, $scope.get('X_SPACING'));
        ($a = ($b = ($range(1, 2, false))).$each, $a.$$p = (TMP_2 = function(i){var self = TMP_2.$$s || this, $a, $b, TMP_3;
if (i == null) i = nil;
        return ($a = ($b = ["top", "bottom"]).$each, $a.$$p = (TMP_3 = function(border){var self = TMP_3.$$s || this;
if (border == null) border = nil;
          return self.$draw_cutmarks(i, delta, border)}, TMP_3.$$s = self, TMP_3), $a).call($b)}, TMP_2.$$s = self, TMP_2), $a).call($b);
        ($a = ($c = sheet.$children()).$each, $a.$$p = (TMP_4 = function(child){var self = TMP_4.$$s || this, $a, $b;
          if (self.pdf == null) self.pdf = nil;
          if ($gvars.log == null) $gvars.log = nil;
if (child == null) child = nil;
        (($a = [child.$line_width()]), $b = self.pdf, $b['$line_width='].apply($b, $a), $a[$a.length-1]);
          if ((($a = child['$is_a?']($scope.get('Ellipse'))) !== nil && (!$a.$$is_boolean || $a == true))) {
            if ((($a = child['$visible?']()) !== nil && (!$a.$$is_boolean || $a == true))) {
              return self.$draw_ellipse(child)
              } else {
              return nil
            }
          } else if ((($a = child['$is_a?']($scope.get('FlowLine'))) !== nil && (!$a.$$is_boolean || $a == true))) {
            if ((($a = child['$visible?']()) !== nil && (!$a.$$is_boolean || $a == true))) {
              return self.$draw_flowline(child)
              } else {
              return nil
            }
          } else if ((($a = child['$is_a?']((((($scope.get('Harpnotes')).$$scope.get('Drawing'))).$$scope.get('Glyph')))) !== nil && (!$a.$$is_boolean || $a == true))) {
            if ((($a = child['$visible?']()) !== nil && (!$a.$$is_boolean || $a == true))) {
              return self.$draw_glyph(child)
              } else {
              return nil
            }
          } else if ((($a = child['$is_a?']((((($scope.get('Harpnotes')).$$scope.get('Drawing'))).$$scope.get('Path')))) !== nil && (!$a.$$is_boolean || $a == true))) {
            if ((($a = child['$visible?']()) !== nil && (!$a.$$is_boolean || $a == true))) {
              return self.$draw_path(child)
              } else {
              return nil
            }
          } else if ((($a = child['$is_a?']((((($scope.get('Harpnotes')).$$scope.get('Drawing'))).$$scope.get('Annotation')))) !== nil && (!$a.$$is_boolean || $a == true))) {
            if ((($a = child['$visible?']()) !== nil && (!$a.$$is_boolean || $a == true))) {
              return self.$draw_annotation(child)
              } else {
              return nil
            }
            } else {
            $gvars.log.$debug("don't know how to draw " + (child.$class()) + " (" + ("pdf_engine") + " " + (58) + ")");
            return nil;
          };}, TMP_4.$$s = self, TMP_4), $a).call($c);
        return self.pdf;
      });

      self.$private();

      Opal.defn(self, '$draw_annotation', function(root) {
        var $a, $b, self = this, style = nil, mm_per_point = nil;
        if ($gvars.conf == null) $gvars.conf = nil;

        style = ((($a = $gvars.conf.$get("layout.FONT_STYLE_DEF")['$[]'](root.$style())) !== false && $a !== nil) ? $a : $gvars.conf.$get("layout.FONT_STYLE_DEF")['$[]']("regular"));
        mm_per_point = $gvars.conf.$get("layout.MM_PER_POINT");
        (($a = [style['$[]']("text_color")]), $b = self.pdf, $b['$text_color='].apply($b, $a), $a[$a.length-1]);
        (($a = [style['$[]']("font_size")]), $b = self.pdf, $b['$font_size='].apply($b, $a), $a[$a.length-1]);
        (($a = [style['$[]']("font_style")]), $b = self.pdf, $b['$font_style='].apply($b, $a), $a[$a.length-1]);
        return self.pdf.$text(root.$center().$first(), $rb_plus(root.$center().$last(), $rb_times(style['$[]']("font_size"), mm_per_point)), root.$text());
      });

      Opal.defn(self, '$draw_cutmarks', function(i, delta, border) {
        var self = this, vertical_pos = nil, hpos = nil, hdiff = nil, center = nil, size = nil;

        vertical_pos = $hash2(["top", "bottom"], {"top": 7, "bottom": 290})['$[]'](border);
        hpos = $rb_plus($rb_plus($rb_divide($scope.get('X_SPACING'), 2.0), $rb_times(delta, i)), 3);
        hdiff = $rb_divide($scope.get('X_SPACING'), 2.0);
        center = self.$Vector2d($rb_plus($rb_divide($scope.get('X_SPACING'), 2.0), $rb_times(delta, i)), vertical_pos);
        size = 1;
        self.pdf.$line(($rb_plus(center, [size['$-@'](), size['$-@']()])).$to_a(), ($rb_plus(center, [size, size])).$to_a());
        return self.pdf.$line(($rb_plus(center, [size['$-@'](), size])).$to_a(), ($rb_plus(center, [size, size['$-@']()])).$to_a());
      });

      Opal.defn(self, '$draw_ellipse', function(root) {
        var $a, $b, $c, $d, TMP_5, $e, TMP_6, TMP_7, self = this, style = nil;

        style = (function() {if ((($a = root['$filled?']()) !== nil && (!$a.$$is_boolean || $a == true))) {
          return "F"
          } else {
          return "FD"
        }; return nil; })();
        (($a = [($c = ($d = ($range(0, 3, true))).$map, $c.$$p = (TMP_5 = function(){var self = TMP_5.$$s || this, $a;

        if ((($a = root['$filled?']()) !== nil && (!$a.$$is_boolean || $a == true))) {
            return 0
            } else {
            return 255
          }}, TMP_5.$$s = self, TMP_5), $c).call($d)]), $b = self.pdf, $b['$fill='].apply($b, $a), $a[$a.length-1]);
        if ((($a = root['$rect?']()) !== nil && (!$a.$$is_boolean || $a == true))) {
          self.pdf.$rect_like_ellipse(root.$center(), root.$size(), style)
          } else {
          self.pdf.$ellipse(root.$center(), root.$size(), style)
        };
        if ((($a = root['$dotted?']()) !== nil && (!$a.$$is_boolean || $a == true))) {
          (($a = [($c = ($e = ($range(0, 3, true))).$map, $c.$$p = (TMP_6 = function(){var self = TMP_6.$$s || this;

          return 0}, TMP_6.$$s = self, TMP_6), $c).call($e)]), $b = self.pdf, $b['$fill='].apply($b, $a), $a[$a.length-1]);
          return self.pdf.$ellipse(($a = ($b = root.$center().$zip(root.$size())).$map, $a.$$p = (TMP_7 = function(s){var self = TMP_7.$$s || this, $a, $b, a = nil, b = nil;
if (s == null) s = nil;
          $b = s, $a = Opal.to_ary($b), a = ($a[0] == null ? nil : $a[0]), b = ($a[1] == null ? nil : $a[1]), $b;
            return $rb_plus($rb_plus(a, b), 0.7);}, TMP_7.$$s = self, TMP_7), $a).call($b), [$scope.get('DOTTED_SIZE'), $scope.get('DOTTED_SIZE')], "F");
          } else {
          return nil
        };
      });

      Opal.defn(self, '$draw_glyph', function(root) {
        var $a, $b, $c, $d, TMP_8, TMP_9, $e, $f, TMP_10, TMP_11, $g, $h, TMP_12, TMP_13, self = this, style = nil, center = nil, background_size = nil, scalefactor = nil, scale = nil, lines = nil, start = nil;

        style = root['$filled?']("FD", "FD");
        (($a = [($c = ($d = ($range(0, 3, true))).$map, $c.$$p = (TMP_8 = function(){var self = TMP_8.$$s || this, $a;

        if ((($a = root['$filled?']()) !== nil && (!$a.$$is_boolean || $a == true))) {
            return 0
            } else {
            return 255
          }}, TMP_8.$$s = self, TMP_8), $c).call($d)]), $b = self.pdf, $b['$fill='].apply($b, $a), $a[$a.length-1]);
        center = [$rb_minus(root.$center().$first(), root.$size().$first()), $rb_minus(root.$center().$last(), root.$size().$last())];
        background_size = ($a = ($b = root.$size()).$map, $a.$$p = (TMP_9 = function(s){var self = TMP_9.$$s || this;
if (s == null) s = nil;
        return $rb_times(2.0, s)}, TMP_9.$$s = self, TMP_9), $a).call($b);
        (($a = [($e = ($f = ($range(0, 3, true))).$map, $e.$$p = (TMP_10 = function(){var self = TMP_10.$$s || this, $a;

        if ((($a = root['$filled?']()) !== nil && (!$a.$$is_boolean || $a == true))) {
            return 0
            } else {
            return 255
          }}, TMP_10.$$s = self, TMP_10), $e).call($f)]), $c = self.pdf, $c['$fill='].apply($c, $a), $a[$a.length-1]);
        (($a = [[255, 255, 255]]), $c = self.pdf, $c['$fill='].apply($c, $a), $a[$a.length-1]);
        (($a = [[255, 255, 255]]), $c = self.pdf, $c['$stroke='].apply($c, $a), $a[$a.length-1]);
        self.pdf.$rect_like_ellipse(root.$center(), root.$size(), "FD");
        (($a = [[0, 0, 0]]), $c = self.pdf, $c['$fill='].apply($c, $a), $a[$a.length-1]);
        (($a = [[0, 0, 0]]), $c = self.pdf, $c['$stroke='].apply($c, $a), $a[$a.length-1]);
        scalefactor = $rb_divide(background_size.$last(), root.$glyph()['$[]']("h"));
        scale = [scalefactor, scalefactor];
        lines = [];
        start = [];
        ($a = ($c = root.$glyph()['$[]']("d")).$each, $a.$$p = (TMP_11 = function(element){var self = TMP_11.$$s || this, $a, $case = nil;
          if (self.pdf == null) self.pdf = nil;
          if ($gvars.log == null) $gvars.log = nil;
if (element == null) element = nil;
        return (function() {$case = element.$first();if ("M"['$===']($case)) {if ((($a = lines['$empty?']()) !== nil && (!$a.$$is_boolean || $a == true))) {
            } else {
            self.pdf.$lines(lines, start.$first(), start.$last(), scale, "FD", false)
          };
          lines = [];
          return start = [$rb_plus(center.$first(), $rb_times(($rb_plus(element['$[]'](1), $rb_divide(root.$glyph()['$[]']("w"), 2))), scale.$first())), $rb_minus(center.$last(), $rb_times(($rb_plus(element['$[]'](2), $rb_divide(root.$glyph()['$[]']("h"), 2))), scale.$last()))];}else if ("l"['$===']($case)) {return lines.$push(element['$[]']($range(1, -1, false)))}else if ("c"['$===']($case)) {return lines.$push(element['$[]']($range(1, -1, false)))}else if ("z"['$===']($case)) {if ((($a = lines['$empty?']()) !== nil && (!$a.$$is_boolean || $a == true))) {
            } else {
            self.pdf.$lines(lines, start.$first(), start.$last(), scale, "FD", true)
          };
          return lines = [];}else {return $gvars.log.$error("BUG: unsupported command '" + (element.$first()) + "' in glyph (" + ("pdf_engine") + " " + (152) + ")")}})()}, TMP_11.$$s = self, TMP_11), $a).call($c);
        (($a = [[0, 0, 0]]), $e = self.pdf, $e['$stroke='].apply($e, $a), $a[$a.length-1]);
        if ((($a = root['$dotted?']()) !== nil && (!$a.$$is_boolean || $a == true))) {
          (($a = [($g = ($h = ($range(0, 3, true))).$map, $g.$$p = (TMP_12 = function(){var self = TMP_12.$$s || this;

          return 0}, TMP_12.$$s = self, TMP_12), $g).call($h)]), $e = self.pdf, $e['$fill='].apply($e, $a), $a[$a.length-1]);
          return self.pdf.$ellipse(($a = ($e = root.$center().$zip(root.$size())).$map, $a.$$p = (TMP_13 = function(s){var self = TMP_13.$$s || this, $a, $b, a = nil, b = nil;
if (s == null) s = nil;
          $b = s, $a = Opal.to_ary($b), a = ($a[0] == null ? nil : $a[0]), b = ($a[1] == null ? nil : $a[1]), $b;
            return $rb_plus(a, $rb_times(b, 1.5));}, TMP_13.$$s = self, TMP_13), $a).call($e), [$scope.get('DOTTED_SIZE'), $scope.get('DOTTED_SIZE')], "F");
          } else {
          return nil
        };
      });

      Opal.defn(self, '$draw_segment', function(x_offset, sheet, newpage) {
        var $a, $b, self = this;

        if (newpage == null) {
          newpage = false
        }
        (($a = [x_offset]), $b = self.pdf, $b['$x_offset='].apply($b, $a), $a[$a.length-1]);
        if (newpage !== false && newpage !== nil) {
          self.pdf.$addPage()};
        return self.$draw(sheet);
      });

      Opal.defn(self, '$draw_flowline', function(root) {
        var $a, $b, self = this;

        if ((($a = root['$dashed?']()) !== nil && (!$a.$$is_boolean || $a == true))) {
          (($a = [3]), $b = self.pdf, $b['$line_dash='].apply($b, $a), $a[$a.length-1])};
        if ((($a = root['$dotted?']()) !== nil && (!$a.$$is_boolean || $a == true))) {
          (($a = [6]), $b = self.pdf, $b['$line_dash='].apply($b, $a), $a[$a.length-1])};
        self.pdf.$line(root.$from().$center(), root.$to().$center());
        return self.pdf.$use_solid_lines();
      });

      Opal.defn(self, '$draw_jumpline', function(root) {
        var $a, $b, $c, $d, TMP_14, self = this, startpoint = nil, endpoint = nil, distance = nil, depth = nil;

        startpoint = root.$from().$center().$clone();
        ($a = 0, $b = startpoint, $b['$[]=']($a, $rb_plus($b['$[]']($a), $scope.get('PADDING'))));
        ($a = 1, $b = startpoint, $b['$[]=']($a, $rb_minus($b['$[]']($a), $rb_divide($scope.get('PADDING'), 4.0))));
        endpoint = root.$to().$center().$clone();
        ($a = 0, $b = endpoint, $b['$[]=']($a, $rb_plus($b['$[]']($a), $scope.get('PADDING'))));
        ($a = 1, $b = endpoint, $b['$[]=']($a, $rb_plus($b['$[]']($a), $rb_divide($scope.get('PADDING'), 4.0))));
        distance = root.$distance();
        if ((($a = distance['$nil?']()) !== nil && (!$a.$$is_boolean || $a == true))) {
          depth = $rb_minus(418.0, ($rb_times(root.$level(), $scope.get('JUMPLINE_INDENT'))))
          } else {
          depth = $rb_plus(endpoint['$[]'](0), distance)
        };
        (($a = [($c = ($d = ($range(0, 3, true))).$map, $c.$$p = (TMP_14 = function(){var self = TMP_14.$$s || this;

        return 0}, TMP_14.$$s = self, TMP_14), $c).call($d)]), $b = self.pdf, $b['$stroke='].apply($b, $a), $a[$a.length-1]);
        self.pdf.$line(endpoint, [depth, endpoint['$[]'](1)]);
        self.pdf.$line([depth, endpoint['$[]'](1)], [depth, startpoint['$[]'](1)]);
        self.pdf.$line([depth, startpoint['$[]'](1)], startpoint);
        return self.pdf.$left_arrowhead(startpoint['$[]'](0), startpoint['$[]'](1));
      });

      return (Opal.defn(self, '$draw_path', function(root) {
        var $a, $b, $c, $d, TMP_15, TMP_16, self = this, lines = nil, scale = nil, start = nil, style = nil;

        lines = [];
        scale = [1, 1];
        start = [];
        style = (function() {if ((($a = root['$filled?']()) !== nil && (!$a.$$is_boolean || $a == true))) {
          return "FD"
          } else {
          return ""
        }; return nil; })();
        (($a = [($c = ($d = ($range(1, 3, false))).$map, $c.$$p = (TMP_15 = function(){var self = TMP_15.$$s || this, $a;

        if ((($a = root['$filled?']()) !== nil && (!$a.$$is_boolean || $a == true))) {
            return 0
            } else {
            return 255
          }}, TMP_15.$$s = self, TMP_15), $c).call($d)]), $b = self.pdf, $b['$fill='].apply($b, $a), $a[$a.length-1]);
        ($a = ($b = root.$path()).$each, $a.$$p = (TMP_16 = function(element){var self = TMP_16.$$s || this, $a, $case = nil;
          if (self.pdf == null) self.pdf = nil;
          if ($gvars.log == null) $gvars.log = nil;
if (element == null) element = nil;
        return (function() {$case = element.$first();if ("M"['$===']($case)) {if ((($a = lines['$empty?']()) !== nil && (!$a.$$is_boolean || $a == true))) {
            } else {
            self.pdf.$lines(lines, start.$first(), start.$last(), scale, style, false)
          };
          lines = [];
          return start = element['$[]']($range(1, 2, false));}else if ("L"['$===']($case)) {if ((($a = lines['$empty?']()) !== nil && (!$a.$$is_boolean || $a == true))) {
            } else {
            self.pdf.$lines(lines, start.$first(), start.$last(), scale, style, false)
          };
          lines = [];
          return start = element['$[]']($range(1, 2, false));}else if ("l"['$===']($case)) {return lines.$push(element['$[]']($range(1, -1, false)))}else if ("c"['$===']($case)) {return lines.$push(element['$[]']($range(1, -1, false)))}else if ("z"['$===']($case)) {if ((($a = lines['$empty?']()) !== nil && (!$a.$$is_boolean || $a == true))) {
            } else {
            self.pdf.$lines(lines, start.$first(), start.$last(), scale, "FD", true)
          };
          return lines = [];}else {return $gvars.log.$error("BUG: unsupported command '" + (element.$first()) + "' in glyph (" + ("pdf_engine") + " " + (247) + ")")}})()}, TMP_16.$$s = self, TMP_16), $a).call($b);
        if ((($a = lines['$empty?']()) !== nil && (!$a.$$is_boolean || $a == true))) {
          return nil
          } else {
          return self.pdf.$lines(lines, start.$first(), start.$last(), scale, style, false)
        };
      }), nil) && 'draw_path';
    })($scope.base, null)
  })($scope.base);
};

/* Generated by Opal 0.9.0 */
Opal.modules["command-controller"] = function(Opal) {
  Opal.dynamic_require_severity = "error";
  var OPAL_CONFIG = { method_missing: true, arity_check: false, freezing: true, tainting: true };
  var self = Opal.top, $scope = Opal, nil = Opal.nil, $breaker = Opal.breaker, $slice = Opal.slice, $module = Opal.module, $klass = Opal.klass, $hash2 = Opal.hash2, $range = Opal.range;

  Opal.add_stubs(['$attr_reader', '$call', '$lambda', '$raise', '$new', '$set_help', '$set_default', '$push', '$!', '$start_with?', '$to_s', '$each', '$[]=', '$name', '$[]', '$get_default', '$get_help', '$empty?', '$get_clean_argument_values', '$perform', '$undoable?', '$clear', '$can_undo?', '$pop', '$invert', '$can_redo?', '$map', '$first', '$select', '$nil?', '$scan', '$parse_string', '$gsub', '$to_sym', '$each_with_index', '$parameter_name', '$clone', '$unshift', '$join', '$parameters', '$is_public?', '$last', '$to_a']);
  return (function($base) {
    var $CommandController, self = $CommandController = $module($base, 'CommandController');

    var def = self.$$proto, $scope = self.$$scope;

    (function($base, $super) {
      function $Parameter(){};
      var self = $Parameter = $klass($base, $super, 'Parameter', $Parameter);

      var def = self.$$proto, $scope = self.$$scope, TMP_1, TMP_2;

      def.default_action = def.name = def.help_action = nil;
      self.$attr_reader("name");

      Opal.defn(self, '$initialize', function(name, type) {
        var self = this;

        self.name = name;
        return self.type = type;
      });

      Opal.defn(self, '$set_help', TMP_1 = function() {
        var self = this, $iter = TMP_1.$$p, block = $iter || nil;

        TMP_1.$$p = null;
        return self.help_action = block;
      });

      Opal.defn(self, '$set_default', TMP_2 = function() {
        var self = this, $iter = TMP_2.$$p, block = $iter || nil;

        TMP_2.$$p = null;
        return self.default_action = block;
      });

      Opal.defn(self, '$get_help', function() {
        var self = this, default$ = nil;

        default$ = self.default_action.$call();
        if (default$ !== false && default$ !== nil) {
          default$ = "[" + (default$) + "]"};
        return "{" + (self.name) + "}" + (default$) + " = " + (self.help_action.$call());
      });

      return (Opal.defn(self, '$get_default', function() {
        var self = this;

        return self.default_action.$call();
      }), nil) && 'get_default';
    })($scope.base, null);

    (function($base, $super) {
      function $Command(){};
      var self = $Command = $klass($base, $super, 'Command', $Command);

      var def = self.$$proto, $scope = self.$$scope, TMP_6, TMP_9, TMP_10, TMP_11;

      def.parameters = def.name = def.action = def.inverse_action = def.help_action = def.undoable = nil;
      self.$attr_reader("name", "parameters");

      Opal.defn(self, '$initialize', function(name) {
        var $a, $b, TMP_3, $c, TMP_4, $d, TMP_5, self = this;

        self.name = name;
        self.help_action = ($a = ($b = self).$lambda, $a.$$p = (TMP_3 = function(){var self = TMP_3.$$s || this;

        return "no help defined for " + (name)}, TMP_3.$$s = self, TMP_3), $a).call($b);
        self.parameters = [];
        self.action = ($a = ($c = self).$lambda, $a.$$p = (TMP_4 = function(p){var self = TMP_4.$$s || this;
if (p == null) p = nil;
        return self.$raise("No action defined for " + (name))}, TMP_4.$$s = self, TMP_4), $a).call($c);
        self.inverse_action = ($a = ($d = self).$lambda, $a.$$p = (TMP_5 = function(p){var self = TMP_5.$$s || this;
if (p == null) p = nil;
        return self.$raise("No  undo defined fore " + (name))}, TMP_5.$$s = self, TMP_5), $a).call($d);
        return self.undoable = true;
      });

      Opal.defn(self, '$add_parameter', TMP_6 = function(name, type, help, default$) {
        var $a, $b, TMP_7, $c, TMP_8, self = this, $iter = TMP_6.$$p, block = $iter || nil, parameter = nil;

        if (help == null) {
          help = nil
        }
        if (default$ == null) {
          default$ = nil
        }
        TMP_6.$$p = null;
        parameter = $scope.get('Parameter').$new(name, type);
        ($a = ($b = parameter).$set_help, $a.$$p = (TMP_7 = function(){var self = TMP_7.$$s || this;

        return help}, TMP_7.$$s = self, TMP_7), $a).call($b);
        ($a = ($c = parameter).$set_default, $a.$$p = (TMP_8 = function(){var self = TMP_8.$$s || this;

        return default$}, TMP_8.$$s = self, TMP_8), $a).call($c);
        if ((block !== nil)) {
          block.$call(parameter)};
        return self.parameters.$push(parameter);
      });

      Opal.defn(self, '$as_action', TMP_9 = function() {
        var self = this, $iter = TMP_9.$$p, block = $iter || nil;

        TMP_9.$$p = null;
        return self.action = block;
      });

      Opal.defn(self, '$as_inverse', TMP_10 = function() {
        var self = this, $iter = TMP_10.$$p, block = $iter || nil;

        TMP_10.$$p = null;
        return self.inverse_action = block;
      });

      Opal.defn(self, '$is_public?', function() {
        var self = this;

        return (self.name.$to_s()['$start_with?']("_"))['$!']();
      });

      Opal.defn(self, '$set_help', TMP_11 = function() {
        var self = this, $iter = TMP_11.$$p, block = $iter || nil;

        TMP_11.$$p = null;
        return self.help_action = block;
      });

      Opal.defn(self, '$perform', function(arguments$) {
        var self = this;

        return self.action.$call(arguments$);
      });

      Opal.defn(self, '$invert', function(arguments$) {
        var self = this;

        return self.inverse_action.$call(arguments$);
      });

      Opal.defn(self, '$get_clean_argument_values', function(arguments$) {
        var $a, $b, TMP_12, self = this, result = nil;

        result = $hash2([], {});
        ($a = ($b = self.parameters).$each, $a.$$p = (TMP_12 = function(p){var self = TMP_12.$$s || this, $a;
if (p == null) p = nil;
        return result['$[]='](p.$name(), ((($a = arguments$['$[]'](p.$name())) !== false && $a !== nil) ? $a : p.$get_default()))}, TMP_12.$$s = self, TMP_12), $a).call($b);
        return result;
      });

      Opal.defn(self, '$get_help', function() {
        var self = this;

        return self.help_action.$call();
      });

      Opal.defn(self, '$parameter_name', function(index) {
        var self = this;

        return self.parameters['$[]'](index).$name();
      });

      Opal.defn(self, '$parameter_help', function(index) {
        var self = this;

        return self.parameters['$[]'](index).$get_help();
      });

      Opal.defn(self, '$parameter_default', function(index) {
        var self = this;

        return self.parameters['$[]'](index).$get_default();
      });

      Opal.defn(self, '$undoable?', function() {
        var self = this;

        return self.undoable;
      });

      return (Opal.defn(self, '$undoable=', function(value) {
        var self = this;

        return self.undoable = value;
      }), nil) && 'undoable=';
    })($scope.base, null);

    (function($base, $super) {
      function $CommandStack(){};
      var self = $CommandStack = $klass($base, $super, 'CommandStack', $CommandStack);

      var def = self.$$proto, $scope = self.$$scope, TMP_13;

      def.commands = def.undo_stack = def.redo_stack = def.history_stack = nil;
      Opal.defn(self, '$initialize', function() {
        var self = this;

        self.commands = $hash2([], {});
        self.undo_stack = [];
        self.redo_stack = [];
        return self.history_stack = [];
      });

      Opal.defn(self, '$add_command', TMP_13 = function(name) {
        var self = this, $iter = TMP_13.$$p, block = $iter || nil, command = nil;

        TMP_13.$$p = null;
        command = $scope.get('Command').$new(name);
        block.$call(command);
        return self.commands['$[]='](command.$name(), command);
      });

      Opal.defn(self, '$can_undo?', function() {
        var self = this;

        return self.undo_stack['$empty?']()['$!']();
      });

      Opal.defn(self, '$can_redo?', function() {
        var self = this;

        return self.redo_stack['$empty?']()['$!']();
      });

      Opal.defn(self, '$perform', function(command, arguments$) {
        var $a, self = this, the_arguments = nil;

        the_arguments = command.$get_clean_argument_values(arguments$);
        command.$perform(the_arguments);
        if ((($a = command['$undoable?']()) !== nil && (!$a.$$is_boolean || $a == true))) {
          self.undo_stack.$push([command, the_arguments]);
          self.redo_stack.$clear();};
        return self.history_stack.$push(["  do", command, the_arguments]);
      });

      Opal.defn(self, '$undo', function() {
        var $a, $b, self = this, command = nil, arguments$ = nil;

        if ((($a = self['$can_undo?']()) !== nil && (!$a.$$is_boolean || $a == true))) {
          $b = self.undo_stack.$pop(), $a = Opal.to_ary($b), command = ($a[0] == null ? nil : $a[0]), arguments$ = ($a[1] == null ? nil : $a[1]), $b;
          self.redo_stack.$push([command, arguments$]);
          self.history_stack.$push(["undo", command, arguments$]);
          return command.$invert(arguments$);
          } else {
          return self.$raise("nothing to undo")
        };
      });

      Opal.defn(self, '$redo', function() {
        var $a, $b, self = this, command = nil, arguments$ = nil;

        if ((($a = self['$can_redo?']()) !== nil && (!$a.$$is_boolean || $a == true))) {
          $b = self.redo_stack.$pop(), $a = Opal.to_ary($b), command = ($a[0] == null ? nil : $a[0]), arguments$ = ($a[1] == null ? nil : $a[1]), $b;
          return self.$perform(command, arguments$);
          } else {
          return self.$raise("nothing to redo")
        };
      });

      Opal.cdecl($scope, 'STRING_COMMAND_REGEX', /([^ \\\^"{]+)|"(([^\\"]|\\["n\\])*)"|(\{.+\})/);

      Opal.defn(self, '$parse_string', function(command) {
        var $a, $b, TMP_14, self = this, r = nil;

        return r = ($a = ($b = command.$scan($scope.get('STRING_COMMAND_REGEX'))).$map, $a.$$p = (TMP_14 = function(s){var self = TMP_14.$$s || this, $a, $b, TMP_15;
if (s == null) s = nil;
        return ($a = ($b = s).$select, $a.$$p = (TMP_15 = function(x){var self = TMP_15.$$s || this;
if (x == null) x = nil;
          return x['$nil?']()['$!']()}, TMP_15.$$s = self, TMP_15), $a).call($b).$first()}, TMP_14.$$s = self, TMP_14), $a).call($b);
      });

      Opal.defn(self, '$run_string', function(command) {
        var $a, $b, TMP_16, $c, TMP_17, self = this, arguments$ = nil, parts = nil, the_command = nil;

        arguments$ = $hash2([], {});
        parts = self.$parse_string(command);
        if ((($a = parts['$empty?']()) !== nil && (!$a.$$is_boolean || $a == true))) {
          parts = ["help"]};
        parts = ($a = ($b = parts).$map, $a.$$p = (TMP_16 = function(p){var self = TMP_16.$$s || this;
if (p == null) p = nil;
        return p.$gsub("\\\\", "\\")}, TMP_16.$$s = self, TMP_16), $a).call($b);
        the_command = self.commands['$[]'](parts.$first().$to_sym());
        if (the_command !== false && the_command !== nil) {
          } else {
          self.$raise("wrong command: " + (command))
        };
        ($a = ($c = parts['$[]']($range(1, -1, false))).$each_with_index, $a.$$p = (TMP_17 = function(argument, index){var self = TMP_17.$$s || this;
if (argument == null) argument = nil;if (index == null) index = nil;
        try {
          return arguments$['$[]='](the_command.$parameter_name(index), argument)
          } catch ($err) {if (true) {
            try {
              return self.$raise("too many arguments in '" + (command) + "'")
            } finally {
              Opal.gvars["!"] = Opal.exceptions.pop() || Opal.nil;
            }
            }else { throw $err; }
          }}, TMP_17.$$s = self, TMP_17), $a).call($c);
        return self.$perform(the_command, arguments$);
      });

      Opal.defn(self, '$history', function() {
        var self = this;

        return self.history_stack.$clone();
      });

      Opal.defn(self, '$undostack', function() {
        var $a, $b, TMP_18, self = this;

        return ($a = ($b = self.undo_stack).$map, $a.$$p = (TMP_18 = function(c){var self = TMP_18.$$s || this;
if (c == null) c = nil;
        return c.$clone().$unshift("undo")}, TMP_18.$$s = self, TMP_18), $a).call($b);
      });

      Opal.defn(self, '$redostack', function() {
        var $a, $b, TMP_19, self = this;

        return ($a = ($b = self.redo_stack).$map, $a.$$p = (TMP_19 = function(c){var self = TMP_19.$$s || this;
if (c == null) c = nil;
        return c.$clone().$unshift("redo")}, TMP_19.$$s = self, TMP_19), $a).call($b);
      });

      return (Opal.defn(self, '$help_string_style', function() {
        var $a, $b, TMP_20, $c, $d, TMP_22, self = this;

        return ($a = ($b = ($c = ($d = self.commands.$to_a()).$select, $c.$$p = (TMP_22 = function(c){var self = TMP_22.$$s || this;
if (c == null) c = nil;
        return c.$last()['$is_public?']()}, TMP_22.$$s = self, TMP_22), $c).call($d)).$map, $a.$$p = (TMP_20 = function(k, c){var self = TMP_20.$$s || this, $a, $b, TMP_21, parameter_names = nil;
if (k == null) k = nil;if (c == null) c = nil;
        parameter_names = ($a = ($b = c.$parameters()).$map, $a.$$p = (TMP_21 = function(p){var self = TMP_21.$$s || this;
if (p == null) p = nil;
          return "{" + (p.$name()) + "}"}, TMP_21.$$s = self, TMP_21), $a).call($b).$join(" ");
          return "" + (c.$name()) + " " + (parameter_names) + " : " + (c.$get_help());}, TMP_20.$$s = self, TMP_20), $a).call($b);
      }), nil) && 'help_string_style';
    })($scope.base, null);
  })($scope.base)
};

/* Generated by Opal 0.9.0 */
Opal.modules["controller"] = function(Opal) {
  Opal.dynamic_require_severity = "error";
  var OPAL_CONFIG = { method_missing: true, arity_check: false, freezing: true, tainting: true };
  function $rb_plus(lhs, rhs) {
    return (typeof(lhs) === 'number' && typeof(rhs) === 'number') ? lhs + rhs : lhs['$+'](rhs);
  }
  function $rb_minus(lhs, rhs) {
    return (typeof(lhs) === 'number' && typeof(rhs) === 'number') ? lhs - rhs : lhs['$-'](rhs);
  }
  function $rb_gt(lhs, rhs) {
    return (typeof(lhs) === 'number' && typeof(rhs) === 'number') ? lhs > rhs : lhs['$>'](rhs);
  }
  var $a, $b, TMP_30, self = Opal.top, $scope = Opal, nil = Opal.nil, $breaker = Opal.breaker, $slice = Opal.slice, $klass = Opal.klass, $hash2 = Opal.hash2, $gvars = Opal.gvars;

  Opal.add_stubs(['$load_dir', '$save_dir', '$[]', '$warning', '$update', '$to_json', '$[]=', '$parse', '$clone', '$private', '$attr', '$lambda', '$to_n', '$html', '$find', '$new', '$load_from_loacalstorage', '$on_command', '$save_to_localstorage', '$handle_command', '$info', '$strict=', '$push', '$_init_conf', '$debug', '$get', '$each', '$send', '$select', '$=~', '$methods', '$setup_harpnote_preview', '$load_demo_tune', '$set_status', '$loglevel', '$render_previews', '$setup_nodewebkit', '$setup_ui_listener', '$run_string', '$error', '$message', '$caller', '$include?', '$get_text', '$Native', '$nil?', '$set_text', '$draw', '$layout_harpnotes', '$draw_in_segments', '$migrate_config_lyrics', '$migrate_notes', '$migrate_config_legend', '$migrate_config_cleanup', '$delete', '$inject', '$last', '$first', '$+', '$map', '$to_i', '$split', '$gsub', '$to_s', '$count', '$is_a?', '$is_playing?', '$stop', '$call', '$==', '$play_song', '$play_selection', '$play_from_selection', '$setup_tune_preview', '$get_abc_part', '$join', '$backtrace', '$set_inactive', '$set_annotations', '$annotations', '$now', '$load_song', '$-', '$send_remote_command', '$set_active', '$render_tunepreview_callback', '$file', '$output', '$render_a4', '$render_a3', '$to_blob', '$strftime', '$clear_annotations', '$get_config_part', '$migrate_config', '$set_config_part', '$get_config_position', '$>', '$keys', '$transform', '$create_engine', '$layout', '$pop', '$select_range_by_position', '$range_highlight_more', '$range_highlight', '$range_unhighlight_more', '$range_unhighlight', '$unhighlight_all', '$highlight_abc_object', '$merge!', '$loglevel=', '$set_view_box', '$on_select', '$select_abc_object', '$origin', '$on_annotation_drag_end', '$zip', '$patch_config_part', '$set_file_drop', '$on_change', '$request_refresh', '$on_selection_change', '$get_selection_positions', '$on_cursor_change', '$on_noteon', '$on_noteoff', '$unhighlight_abc_object', '$on_songoff', '$stop_play_abc', '$on', '$key', '$newValue', '$===', '$css', '$ready?']);
  (function($base, $super) {
    function $LocalStore(){};
    var self = $LocalStore = $klass($base, $super, 'LocalStore', $LocalStore);

    var def = self.$$proto, $scope = self.$$scope;

    def.directory = def.name = nil;
    Opal.defn(self, '$initialize', function(name) {
      var $a, self = this;

      self.name = name;
      self.$load_dir();
      if ((($a = self.directory) !== nil && (!$a.$$is_boolean || $a == true))) {
        return nil
        } else {
        self.directory = $hash2([], {});
        return self.$save_dir();
      };
    });

    Opal.defn(self, '$create', function(key, item, title) {
      var $a, self = this;
      if ($gvars.log == null) $gvars.log = nil;

      if (title == null) {
        title = nil
      }
      if ((($a = self.directory['$[]'](key)) !== nil && (!$a.$$is_boolean || $a == true))) {
        return $gvars.log.$warning("local storage: key '" + (key) + "' already exists")
        } else {
        return self.$update(key, item, title, true)
      };
    });

    Opal.defn(self, '$update', function(key, item, title, create) {
      var $a, $b, self = this, envelope = nil;
      if ($gvars.log == null) $gvars.log = nil;

      if (title == null) {
        title = nil
      }
      if (create == null) {
        create = false
      }
      envelope = $hash2(["p", "title"], {"p": item, "title": title}).$to_json();
      if ((($a = ((($b = self.directory['$[]'](key)) !== false && $b !== nil) ? $b : create)) !== nil && (!$a.$$is_boolean || $a == true))) {
        localStorage.setItem(self.$mangle_key(key), envelope);
        self.directory['$[]='](key, title);
        return self.$save_dir();
        } else {
        return $gvars.log.$warning("local storage update: key '" + (key) + "' does not exist")
      };
    });

    Opal.defn(self, '$retrieve', function(key) {
      var self = this, envelope = nil, result = nil;

      envelope = $scope.get('JSON').$parse(localStorage.getItem(self.$mangle_key(key)));
      if (envelope !== false && envelope !== nil) {
        result = envelope['$[]']("p")};
      return result;
    });

    Opal.defn(self, '$delete', function(key) {
      var $a, self = this;
      if ($gvars.log == null) $gvars.log = nil;

      if ((($a = self.directory['$[]'](key)) !== nil && (!$a.$$is_boolean || $a == true))) {
        return $gvars.log.$warning("local storage: key '" + (key) + "' does not exist")
        } else {
        localStorage.deleteItem(self.$mangle_key(key));
        self.directory['$[]='](key, nil);
        return self.$save_dir();
      };
    });

    Opal.defn(self, '$list', function() {
      var self = this;

      return self.directory.$clone();
    });

    self.$private();

    Opal.defn(self, '$mangle_key', function(key) {
      var self = this;

      return "" + (self.name) + "." + (key);
    });

    Opal.defn(self, '$load_dir', function() {
      var self = this, dirkey = nil;

      dirkey = "" + (self.name) + "__dir";
      return self.directory = $scope.get('JSON').$parse(localStorage.getItem(dirkey));
    });

    return (Opal.defn(self, '$save_dir', function() {
      var self = this, dir_json = nil, dirkey = nil;

      dir_json = self.directory.$to_json();
      dirkey = "" + (self.name) + "__dir";
      return localStorage.setItem(dirkey, dir_json);
    }), nil) && 'save_dir';
  })($scope.base, null);
  (function($base, $super) {
    function $Controller(){};
    var self = $Controller = $klass($base, $super, 'Controller', $Controller);

    var def = self.$$proto, $scope = self.$$scope;

    def.console = def.commands = def.systemstatus = def.editor = def.harpnote_player = def.update_sytemstatus_consumers = def.tune_preview_printer = def.music_model = def.harpnote_preview_printer = def.song_harpnotes = def.refresh_timer = nil;
    self.$attr("editor", "harpnote_preview_printer", "tune_preview_printer", "systemstatus");

    Opal.defn(self, '$initialize', function() {
      var $a, $b, TMP_1, $c, TMP_2, $d, TMP_3, $e, TMP_4, $f, TMP_5, $g, $h, TMP_6, self = this;
      if ($gvars.log == null) $gvars.log = nil;
      if ($gvars.conf == null) $gvars.conf = nil;

      init_w2ui();
      self.update_sytemstatus_consumers = $hash2(["systemstatus", "play_start", "play_stop"], {"systemstatus": [($a = ($b = self).$lambda, $a.$$p = (TMP_1 = function(){var self = TMP_1.$$s || this;
        if (self.systemstatus == null) self.systemstatus = nil;

      return update_sytemstatus_w2ui(self.systemstatus.$to_n());}, TMP_1.$$s = self, TMP_1), $a).call($b)], "play_start": [($a = ($c = self).$lambda, $a.$$p = (TMP_2 = function(){var self = TMP_2.$$s || this;

      return update_play_w2ui('start');}, TMP_2.$$s = self, TMP_2), $a).call($c)], "play_stop": [($a = ($d = self).$lambda, $a.$$p = (TMP_3 = function(){var self = TMP_3.$$s || this;

      return update_play_w2ui('stop');}, TMP_3.$$s = self, TMP_3), $a).call($d)]});
      $scope.get('Element').$find("#lbZupfnoter").$html("Zupfnoter " + ($scope.get('VERSION')));
      self.console = (($scope.get('JqConsole')).$$scope.get('JqConsole')).$new("commandconsole", "zupfnoter> ");
      self.console.$load_from_loacalstorage();
      ($a = ($e = self.console).$on_command, $a.$$p = (TMP_4 = function(cmd){var self = TMP_4.$$s || this;
        if (self.console == null) self.console = nil;
if (cmd == null) cmd = nil;
      self.console.$save_to_localstorage();
        return self.$handle_command(cmd);}, TMP_4.$$s = self, TMP_4), $a).call($e);
      self.dropped_abc = "T: nothing dropped yet";
      $gvars.log = $scope.get('ConsoleLogger').$new(self.console);
      $gvars.log.$info("Welcome to Zupfnoter " + ($scope.get('VERSION')));
      $gvars.conf = $scope.get('Confstack').$new();
      (($a = [false]), $f = $gvars.conf, $f['$strict='].apply($f, $a), $a[$a.length-1]);
      $gvars.conf.$push(self.$_init_conf());
      $gvars.log.$debug($gvars.conf.$get().$to_json());
      self.editor = (($scope.get('Harpnotes')).$$scope.get('TextPane')).$new("abcEditor");
      self.harpnote_player = (((($scope.get('Harpnotes')).$$scope.get('Music'))).$$scope.get('HarpnotePlayer')).$new();
      self.songbook = $scope.get('LocalStore').$new("songbook");
      self.abc_transformer = (((($scope.get('Harpnotes')).$$scope.get('Input'))).$$scope.get('Abc2svgToHarpnotes')).$new();
      self.dropboxclient = (((($scope.get('Opal')).$$scope.get('DropboxJs'))).$$scope.get('NilClient')).$new();
      self.systemstatus = $hash2([], {});
      self.commands = (($scope.get('CommandController')).$$scope.get('CommandStack')).$new();
      ($a = ($f = ($g = ($h = self.$methods()).$select, $g.$$p = (TMP_6 = function(n){var self = TMP_6.$$s || this;
if (n == null) n = nil;
      return n['$=~'](/__ic.*/)}, TMP_6.$$s = self, TMP_6), $g).call($h)).$each, $a.$$p = (TMP_5 = function(m){var self = TMP_5.$$s || this;
if (m == null) m = nil;
      return self.$send(m)}, TMP_5.$$s = self, TMP_5), $a).call($f);
      self.$setup_harpnote_preview();
      self.$load_demo_tune();
      self.$set_status($hash2(["dropbox", "music_model", "loglevel", "autorefresh", "view"], {"dropbox": "not connected", "music_model": "unchanged", "loglevel": $gvars.log.$loglevel(), "autorefresh": "off", "view": 0}));
      self.$load_from_loacalstorage();
      self.$render_previews();
      self.$setup_nodewebkit();
      return self.$setup_ui_listener();
    });

    Opal.defn(self, '$handle_command', function(command) {
      var self = this, e = nil;
      if ($gvars.log == null) $gvars.log = nil;

      try {
      return self.commands.$run_string(command)
      } catch ($err) {if (Opal.rescue($err, [$scope.get('Exception')])) {e = $err;
        try {
          return $gvars.log.$error("" + (e.$message()) + " in " + (command) + " " + (e.$caller()) + " " + ("controller") + ":" + (150))
        } finally {
          Opal.gvars["!"] = Opal.exceptions.pop() || Opal.nil;
        }
        }else { throw $err; }
      };
    });

    Opal.defn(self, '$save_to_localstorage', function() {
      var $a, $b, TMP_7, self = this, systemstatus = nil, abc = nil;

      systemstatus = ($a = ($b = self.systemstatus).$select, $a.$$p = (TMP_7 = function(key, _){var self = TMP_7.$$s || this;
if (key == null) key = nil;if (_ == null) _ = nil;
      return ["music_model", "view", "autorefresh", "loglevel", "nwworkingdir", "dropboxapp", "dropboxpath", "perspective", "zoom"]['$include?'](key)}, TMP_7.$$s = self, TMP_7), $a).call($b).$to_json();
      abc = localStorage.setItem('systemstatus', systemstatus);;
      abc = self.editor.$get_text();
      return abc = localStorage.setItem('abc_data', abc);;
    });

    Opal.defn(self, '$load_from_loacalstorage', function() {
      var $a, self = this, abc = nil, envelope = nil;

      abc = self.$Native(localStorage.getItem('abc_data'));
      if ((($a = abc['$nil?']()) !== nil && (!$a.$$is_boolean || $a == true))) {
        } else {
        self.editor.$set_text(abc)
      };
      envelope = $scope.get('JSON').$parse(localStorage.getItem('systemstatus'));
      if (envelope !== false && envelope !== nil) {
        self.$set_status(envelope)};
      if ((($a = self.systemstatus['$[]']("dropboxapp")) !== nil && (!$a.$$is_boolean || $a == true))) {
        self.$handle_command("dlogin " + (self.systemstatus['$[]']("dropboxapp")) + " " + (self.systemstatus['$[]']("dropboxpath")))};
      return nil;
    });

    Opal.defn(self, '$load_demo_tune', function() {
      var self = this, abc = nil;

      abc = "X:21\nF:21_Ich_steh_an_deiner_krippen_hier\nT:Ich steh an deiner Krippen hier\nC:Nr. 59 aus dem Weihnachtsoratorium\nC:Joh. Seb. Bach\nC:Kirchenchor Mattighofen\n%%score ( 1 2 ) ( 3 4 )\nL:1/4\nQ:1/4=80.00\nM:4/4\nI:linebreak $\nK:G\nV:1 treble nm=\"Sopran Alt\"\nV:2 treble\nV:3 bass nm=\"Tenor Bass\"\nV:4 bass\nV:1\nG | G/A/ B A G | A A !fermata!B G/A/ |\nB c d c/B/ | A/G/ A !fermata!G :| B | B A G F |\nG/A/ B !fermata!A A | G F G D | G A !fermata!B G/A/ |\nB c d c/B/ | A/G/ A !fermata!G z |]\nV:2\nD | E/F/ G G/F/ G | G F G E/F/ |\nG/ B A/4G/4 F G | G F D :| z | G3/2 F/ F/E/ E/^D/ |\nE D D D | D/C/ D D/C/ B, | B, E ^D B, |\nE E D/E/2 G | G F D z |]\nV:3\nB, | B, E E/D/ D | E/C/ A,/D/ !fermata!D E |\nD G,/A,/ B,/C/ D | D C/B,/ !fermata!B, :| D | D D/C/ B,/C/ F,/B,/ |\nB,/A,/ A,/G,/ !fermata!F, F, | G,/A,/ B,/C/ B,/A,/ G, | G, F,/E,/ !fermata!F, E,/F,/ |\nG,3/2 A,/ B,/C/ D | D C/B,/ !fermata!B, z |]\nV:4\nG,/F,/ | E,3/2 D,/ C,3/2 B,,/ | C,/A,,/ D, G,, C, |\nG,/F,/ E, B,/A,/ G, | D D, G, :| z | B,/C/ D/2-D/2 G,/A,/ B, |\nE,/F,/ G, D, D/C/ | B,3/2 A,/ G,3/2 F,/ | E,/D,/ C, B,, E,/-E,/ |\nE,/D,/ C, B,,/A,,/ G,, | D,2 G,, z |]\n\n%%%%zupfnoter.config\n{\n \"produce\":[1],\n \"annotations\": {\n                  \"refn\": {\"id\": \"refn\", \"text\": \"referenced note\", \"pos\": [20,10]}\n                },\n \"extract\": {\n  \"0\": {\n       \"voices\": [1,2,3,4],\n       \"flowlines\": [1,3],\n       \"layoutlines\": [1,2,3,4],\n       \"lyrics\": {\"versepos\": {\"1,2,3,4,5,6,7,8\" :[10,100]}},\n       \"legend\": {\"pos\": [310,175]},\n       \"notes\":[\n         {\"pos\": [340,10], \"text\": \"Ich steh an deiner Krippen hier\", \"style\": \"strong\"}\n         ]\n      }\n       }\n    }\n";
      return self.editor.$set_text(abc);
    });

    Opal.defn(self, '$render_a3', function(index) {
      var self = this, printer = nil;

      if (index == null) {
        index = self.systemstatus['$[]']("view")
      }
      printer = (($scope.get('Harpnotes')).$$scope.get('PDFEngine')).$new();
      return printer.$draw(self.$layout_harpnotes(index));
    });

    Opal.defn(self, '$render_a4', function(index) {
      var self = this;

      if (index == null) {
        index = self.systemstatus['$[]']("view")
      }
      return (($scope.get('Harpnotes')).$$scope.get('PDFEngine')).$new().$draw_in_segments(self.$layout_harpnotes(index));
    });

    Opal.defn(self, '$migrate_config', function(config) {
      var $a, $b, self = this, result = nil, new_lyrics = nil, sheetnotes = nil, new_legend = nil;

      result = $scope.get('Confstack').$new(false);
      (($a = [false]), $b = result, $b['$strict='].apply($b, $a), $a[$a.length-1]);
      result.$push(config);
      if ((($a = config['$[]']("extract")) !== nil && (!$a.$$is_boolean || $a == true))) {
        new_lyrics = self.$migrate_config_lyrics(result);
        result.$push(new_lyrics);
        sheetnotes = self.$migrate_notes(result);
        result.$push(sheetnotes);
        new_legend = self.$migrate_config_legend(result);
        result.$push(new_legend);};
      result['$[]=']("$schema", $scope.get('SCHEMA_VERSION'));
      result['$[]=']("$version", $scope.get('VERSION'));
      result;
      return self.$migrate_config_cleanup(result.$get());
    });

    Opal.defn(self, '$migrate_config_cleanup', function(config) {
      var $a, $b, TMP_8, self = this;

      if ((($a = config['$[]']("extract")) !== nil && (!$a.$$is_boolean || $a == true))) {
        ($a = ($b = config['$[]']("extract")).$each, $a.$$p = (TMP_8 = function(k, element){var self = TMP_8.$$s || this, lyrics = nil;
if (k == null) k = nil;if (element == null) element = nil;
        lyrics = element['$[]']("lyrics");
          if (lyrics !== false && lyrics !== nil) {
            return lyrics.$delete("versepos")
            } else {
            return nil
          };}, TMP_8.$$s = self, TMP_8), $a).call($b)};
      return config;
    });

    Opal.defn(self, '$migrate_config_legend', function(config) {
      var $a, $b, TMP_9, self = this, new_legend = nil;

      new_legend = ($a = ($b = config['$[]']("extract")).$inject, $a.$$p = (TMP_9 = function(r, element){var self = TMP_9.$$s || this, $a, legend = nil, opos = nil, result = nil;
if (r == null) r = nil;if (element == null) element = nil;
      legend = element.$last()['$[]']("legend");
        if (legend !== false && legend !== nil) {
          if ((($a = legend['$[]']("spos")) !== nil && (!$a.$$is_boolean || $a == true))) {
            } else {
            opos = legend['$[]']("pos");
            result = $hash2(["spos", "pos"], {"spos": [opos.$first(), $rb_plus(opos.$last(), 7)], "pos": opos});
            r['$[]='](element.$first(), $hash2(["legend"], {"legend": result}));
          }};
        return r;}, TMP_9.$$s = self, TMP_9), $a).call($b, $hash2([], {}));
      return $hash2(["extract"], {"extract": new_legend});
    });

    Opal.defn(self, '$migrate_config_lyrics', function(config) {
      var $a, $b, TMP_10, self = this, new_lyrics = nil;

      new_lyrics = ($a = ($b = config['$[]']("extract")).$inject, $a.$$p = (TMP_10 = function(r, element){var self = TMP_10.$$s || this, $a, $b, TMP_11, lyrics = nil, result = nil;
if (r == null) r = nil;if (element == null) element = nil;
      lyrics = element.$last()['$[]']("lyrics");
        if (lyrics !== false && lyrics !== nil) {
          lyrics = lyrics['$[]']("versepos")};
        if (lyrics !== false && lyrics !== nil) {
          result = ($a = ($b = lyrics).$inject, $a.$$p = (TMP_11 = function(ir, element){var self = TMP_11.$$s || this, $a, $b, TMP_12, verses = nil;
if (ir == null) ir = nil;if (element == null) element = nil;
          verses = ($a = ($b = element.$first().$gsub(",", " ").$split(" ")).$map, $a.$$p = (TMP_12 = function(f){var self = TMP_12.$$s || this;
if (f == null) f = nil;
            return f.$to_i()}, TMP_12.$$s = self, TMP_12), $a).call($b);
            ir['$[]='](($rb_plus(ir.$count(), 1)).$to_s(), $hash2(["verses", "pos"], {"verses": verses, "pos": element.$last()}));
            return ir;}, TMP_11.$$s = self, TMP_11), $a).call($b, $hash2([], {}));
          r['$[]='](element.$first(), $hash2(["lyrics"], {"lyrics": result}));};
        return r;}, TMP_10.$$s = self, TMP_10), $a).call($b, $hash2([], {}));
      return $hash2(["extract"], {"extract": new_lyrics});
    });

    Opal.defn(self, '$migrate_notes', function(config) {
      var $a, $b, TMP_13, self = this, sheetnotes = nil;

      sheetnotes = ($a = ($b = config['$[]']("extract")).$inject, $a.$$p = (TMP_13 = function(r, element){var self = TMP_13.$$s || this, $a, $b, TMP_14, notes = nil, result = nil;
if (r == null) r = nil;if (element == null) element = nil;
      notes = element.$last()['$[]']("notes");
        if ((($a = notes['$is_a?']($scope.get('Array'))) !== nil && (!$a.$$is_boolean || $a == true))) {
          result = ($a = ($b = notes).$inject, $a.$$p = (TMP_14 = function(ir, element){var self = TMP_14.$$s || this;
if (ir == null) ir = nil;if (element == null) element = nil;
          ir['$[]='](($rb_plus(ir.$count(), 1)).$to_s(), element);
            return ir;}, TMP_14.$$s = self, TMP_14), $a).call($b, $hash2([], {}));
          r['$[]='](element.$first(), $hash2(["notes"], {"notes": result}));};
        return r;}, TMP_13.$$s = self, TMP_13), $a).call($b, $hash2([], {}));
      return $hash2(["extract"], {"extract": sheetnotes});
    });

    Opal.defn(self, '$play_abc', function(mode) {
      var $a, $b, TMP_15, $c, TMP_16, self = this;

      if (mode == null) {
        mode = "music_model"
      }
      if ((($a = self.harpnote_player['$is_playing?']()) !== nil && (!$a.$$is_boolean || $a == true))) {
        self.harpnote_player.$stop();
        return ($a = ($b = self.update_sytemstatus_consumers['$[]']("play_stop")).$each, $a.$$p = (TMP_15 = function(i){var self = TMP_15.$$s || this;
if (i == null) i = nil;
        return i.$call()}, TMP_15.$$s = self, TMP_15), $a).call($b);
        } else {
        ($a = ($c = self.update_sytemstatus_consumers['$[]']("play_start")).$each, $a.$$p = (TMP_16 = function(i){var self = TMP_16.$$s || this;
if (i == null) i = nil;
        return i.$call()}, TMP_16.$$s = self, TMP_16), $a).call($c);
        if (mode['$==']("music_model")) {
          self.harpnote_player.$play_song()};
        if (mode['$==']("selection")) {
          self.harpnote_player.$play_selection()};
        if (mode['$==']("selection_ff")) {
          return self.harpnote_player.$play_from_selection()
          } else {
          return nil
        };
      };
    });

    Opal.defn(self, '$stop_play_abc', function() {
      var $a, $b, TMP_17, self = this;

      self.harpnote_player.$stop();
      return ($a = ($b = self.update_sytemstatus_consumers['$[]']("play_stop")).$each, $a.$$p = (TMP_17 = function(i){var self = TMP_17.$$s || this;
if (i == null) i = nil;
      return i.$call()}, TMP_17.$$s = self, TMP_17), $a).call($b);
    });

    Opal.defn(self, '$render_tunepreview_callback', function() {
      var self = this, abc_text = nil, e = nil;
      if ($gvars.log == null) $gvars.log = nil;

      self.$setup_tune_preview();
      try {
      abc_text = self.editor.$get_abc_part();
        self.tune_preview_printer.$draw(abc_text);
      } catch ($err) {if (Opal.rescue($err, [$scope.get('Exception')])) {e = $err;
        try {
          $gvars.log.$error(["Bug", e.$message(), e.$backtrace()].$join("\n"), [1, 1], [10, 1000])
        } finally {
          Opal.gvars["!"] = Opal.exceptions.pop() || Opal.nil;
        }
        }else { throw $err; }
      };
      $gvars.log.$debug("finished render tune " + ("controller") + " " + (365));
      self.$set_inactive("#tunePreview");
      self.editor.$set_annotations($gvars.log.$annotations());
      return nil;
    });

    Opal.defn(self, '$render_harpnotepreview_callback', function() {
      var self = this, s = nil, e = nil;
      if ($gvars.log == null) $gvars.log = nil;

      s = $scope.get('Time').$now();
      try {
      $gvars.log.$debug("viewid: " + (self.systemstatus['$[]']("view")) + " " + ("controller") + " " + (378));
        self.song_harpnotes = self.$layout_harpnotes(self.systemstatus['$[]']("view"));
        self.harpnote_player.$load_song(self.music_model);
        self.harpnote_preview_printer.$draw(self.song_harpnotes);
      } catch ($err) {if (Opal.rescue($err, [$scope.get('Exception')])) {e = $err;
        try {
          $gvars.log.$error(["Bug", e.$message(), e.$backtrace()].$join("\n"), [1, 1], [10, 1000])
        } finally {
          Opal.gvars["!"] = Opal.exceptions.pop() || Opal.nil;
        }
        }else { throw $err; }
      };
      self.$set_status($hash2(["refresh"], {"refresh": false}));
      $gvars.log.$debug("finished rendering Haprnotes inn " + ($rb_minus($scope.get('Time').$now(), s)) + " seconds " + ("controller") + " " + (390));
      self.$set_inactive("#harpPreview");
      self.editor.$set_annotations($gvars.log.$annotations());
      return nil;
    });

    Opal.defn(self, '$render_previews', function() {
      var self = this;
      if ($gvars.log == null) $gvars.log = nil;

      $gvars.log.$info("rendering");
      if (self.systemstatus['$[]']("autorefresh")['$==']("remote")) {
        } else {
        self.$save_to_localstorage();
        self.$send_remote_command("render");
      };
      self.$setup_tune_preview();
      self.$set_active("#tunePreview");
      setTimeout(function(){self.$render_tunepreview_callback()}, 0);
      self.$set_active("#harpPreview");
      return setTimeout(function(){self.$render_harpnotepreview_callback()}, 0);
    });

    Opal.defn(self, '$render_remote', function() {
      var self = this;

      self.$set_status($hash2(["refresh"], {"refresh": false}));
      self.$save_to_localstorage();
      self.$render_tunepreview_callback();
      return self.$send_remote_command("render");
    });

    Opal.defn(self, '$save_file', function() {
      var self = this, zip = nil, blob = nil, filename = nil;

      zip = (($scope.get('JSZip')).$$scope.get('ZipFile')).$new();
      zip.$file("song.abc", self.editor.$get_text());
      zip.$file("harpnotes_a4.pdf", self.$render_a4().$output("blob"));
      zip.$file("harpnotes_a3.pdf", self.$render_a3().$output("blob"));
      blob = zip.$to_blob();
      filename = "song" + ($scope.get('Time').$now().$strftime("%d%m%Y%H%M%S")) + ".zip";
      return window.saveAs(blob, filename);
    });

    Opal.defn(self, '$layout_harpnotes', function(print_variant) {
      var $a, $b, self = this, config_part = nil, config = nil, error = nil, line_col = nil, outdated_configs = nil, abc_parser = nil, start = nil, result = nil;
      if ($gvars.log == null) $gvars.log = nil;
      if ($gvars.conf == null) $gvars.conf = nil;

      if (print_variant == null) {
        print_variant = 0
      }
      $gvars.log.$clear_annotations();
      config_part = self.editor.$get_config_part();
      try {
      config = json_parse(config_part);
        config = $scope.get('JSON').$parse(config_part);
        config = self.$migrate_config(config);
        self.editor.$set_config_part(config);
      } catch ($err) {if (Opal.rescue($err, [$scope.get('Object')])) {error = $err;
        try {
          line_col = self.editor.$get_config_position(error.$last());
          $gvars.log.$error("" + (error.$first()) + " at " + (line_col), line_col);
          config = $hash2([], {});
        } finally {
          Opal.gvars["!"] = Opal.exceptions.pop() || Opal.nil;
        }
        }else { throw $err; }
      };
      outdated_configs = self.editor.$get_text().$split("%%%%hn.").$count();
      if ((($a = ((($b = $rb_gt(config.$keys().$count(), 0)) !== false && $b !== nil) ? $b : outdated_configs['$=='](1))) !== nil && (!$a.$$is_boolean || $a == true))) {
        config['$[]=']("location", "song")};
      $gvars.conf.$push(config);
      abc_parser = $gvars.conf.$get("abc_parser");
      start = $scope.get('Time').$now();
      self.music_model = (((($scope.get('Harpnotes')).$$scope.get('Input'))).$$scope.get('ABCToHarpnotesFactory')).$create_engine(abc_parser).$transform(self.editor.$get_abc_part());
      $gvars.log.$info("duration transform " + ($rb_minus($scope.get('Time').$now(), start)));
      result = (((($scope.get('Harpnotes')).$$scope.get('Layout'))).$$scope.get('Default')).$new().$layout(self.music_model, nil, print_variant);
      $gvars.log.$info("duration transform + layout " + ($rb_minus($scope.get('Time').$now(), start)));
      if ($gvars.log.$loglevel()['$==']("debug")) {
        $gvars.log.$debug(self.music_model.$to_json())};
      self.editor.$set_annotations($gvars.log.$annotations());
      $gvars.conf.$pop();
      return result;
    });

    Opal.defn(self, '$highlight_abc_object', function(abcelement) {
      var $a, self = this, a = nil, startchar = nil, endchar = nil;
      if ($gvars.log == null) $gvars.log = nil;

      a = self.$Native(abcelement);
      $gvars.log.$debug("select_abc_element " + (a['$[]']("startChar")) + " (" + ("controller") + " " + (474) + ")");
      startchar = a['$[]']("startChar");
      endchar = a['$[]']("endChar");
      if (endchar['$=='](startchar)) {
        endchar = $rb_minus(endchar, 5)};
      if ((($a = self.harpnote_player['$is_playing?']()) !== nil && (!$a.$$is_boolean || $a == true))) {
        } else {
        self.editor.$select_range_by_position(startchar, endchar)
      };
      self.tune_preview_printer.$range_highlight_more(a['$[]']("startChar"), a['$[]']("endChar"));
      return self.harpnote_preview_printer.$range_highlight(a['$[]']("startChar"), a['$[]']("endChar"));
    });

    Opal.defn(self, '$unhighlight_abc_object', function(abcelement) {
      var self = this, a = nil;

      a = self.$Native(abcelement);
      self.tune_preview_printer.$range_unhighlight_more(a['$[]']("startChar"), a['$[]']("endChar"));
      return self.harpnote_preview_printer.$range_unhighlight(a['$[]']("startChar"), a['$[]']("endChar"));
    });

    Opal.defn(self, '$select_abc_object', function(abcelement) {
      var self = this;

      self.harpnote_preview_printer.$unhighlight_all();
      return self.$highlight_abc_object(abcelement);
    });

    Opal.defn(self, '$set_status', function(status) {
      var $a, $b, TMP_18, self = this;
      if ($gvars.log == null) $gvars.log = nil;

      self.systemstatus['$merge!'](status);
      $gvars.log.$debug("" + (self.systemstatus.$to_s()) + " " + ("controller") + " " + (509));
      if (self.systemstatus['$[]']("loglevel")['$==']($gvars.log.$loglevel())) {
        } else {
        (($a = [(self.systemstatus['$[]']("loglevel"))]), $b = $gvars.log, $b['$loglevel='].apply($b, $a), $a[$a.length-1])
      };
      ($a = ($b = self.update_sytemstatus_consumers['$[]']("systemstatus")).$each, $a.$$p = (TMP_18 = function(c){var self = TMP_18.$$s || this;
        if (self.sytemstatus == null) self.sytemstatus = nil;
if (c == null) c = nil;
      return c.$call(self.sytemstatus)}, TMP_18.$$s = self, TMP_18), $a).call($b);
      return nil;
    });

    self.$private();

    Opal.defn(self, '$setup_harpnote_preview', function() {
      var $a, $b, TMP_19, $c, TMP_20, self = this;

      self.harpnote_preview_printer = (($scope.get('Harpnotes')).$$scope.get('RaphaelEngine')).$new("harpPreview", 2200, 1400);
      self.harpnote_preview_printer.$set_view_box(0, 0, 440, 297);
      ($a = ($b = self.harpnote_preview_printer).$on_select, $a.$$p = (TMP_19 = function(harpnote){var self = TMP_19.$$s || this;
if (harpnote == null) harpnote = nil;
      return self.$select_abc_object(harpnote.$origin())}, TMP_19.$$s = self, TMP_19), $a).call($b);
      return ($a = ($c = self.harpnote_preview_printer).$on_annotation_drag_end, $a.$$p = (TMP_20 = function(info){var self = TMP_20.$$s || this, $a, $b, TMP_21, newcoords = nil, report = nil;
        if (self.editor == null) self.editor = nil;
        if ($gvars.log == null) $gvars.log = nil;
if (info == null) info = nil;
      newcoords = ($a = ($b = info['$[]']("origin").$zip(info['$[]']("delta"))).$map, $a.$$p = (TMP_21 = function(i){var self = TMP_21.$$s || this;
if (i == null) i = nil;
        return $rb_plus(i.$first(), i.$last())}, TMP_21.$$s = self, TMP_21), $a).call($b);
        report = "" + (info['$[]']("config")) + ": " + (newcoords);
        if ((($a = info['$[]']("config")) !== nil && (!$a.$$is_boolean || $a == true))) {
          self.editor.$patch_config_part(info['$[]']("config"), newcoords)};
        $scope.get('Element').$find("#tbCoords").$html(report);
        return $gvars.log.$info(report);}, TMP_20.$$s = self, TMP_20), $a).call($c);
    });

    Opal.defn(self, '$setup_tune_preview', function() {
      var $a, $b, TMP_22, self = this;

      self.tune_preview_printer = (($scope.get('ABC2SVG')).$$scope.get('Abc2Svg')).$new($scope.get('Element').$find("#tunePreview"));
      return ($a = ($b = self.tune_preview_printer).$on_select, $a.$$p = (TMP_22 = function(abcelement){var self = TMP_22.$$s || this, a = nil;
if (abcelement == null) abcelement = nil;
      a = self.$Native(abcelement);
        return self.$select_abc_object(abcelement);}, TMP_22.$$s = self, TMP_22), $a).call($b);
    });

    Opal.defn(self, '$set_file_drop', function(dropzone) {
      var self = this;
      if ($gvars.log == null) $gvars.log = nil;

      

    function pasteXml(text){
                            try{
                              var xmldata = $.parseXML(text);
                             }
                            catch(ex){
                              $gvars.log.$error(ex.message)
                            }

                            var options = {
                                    'u': 0, 'b': 0, 'n': 0,    // unfold repeats (1), bars per line, chars per line
                            'c': 0, 'v': 0, 'd': 0,    // credit text filter level (0-6), no volta on higher voice numbers (1), denominator unit length (L:)
                            'm': 0, 'x': 0,           // with midi volume and panning (1), no line breaks (1)
                            'p': 'f'
                          };              // page format: scale (1.0), width, left- and right margin in cm

                          result = vertaal(xmldata, options);
                          $gvars.log.$info(result[1])
      self.dropped_abc = result[0]
      self.$handle_command("drop")
    }

    function pasteMxl(text){
       zip = new JSZip(text);
       text = zip.file(/^lg.*xml$/)[0].asText();
       pasteXml(text);
    }

    function pasteAbc(text){
       self.dropped_abc = text
      self.$handle_command("drop")
    }


    function initializeAbc2svg(element) {

               //xml2abc = new ZnXml2Abc();

               function handleDrop(event) {
                          event.stopPropagation();
                          event.preventDefault();
                          files = event.dataTransfer.files;
                          reader = new FileReader();
                          reader.onload = function (e) {
                             text = e.target.result;
                             if (text[0] == '<'){
                               pasteXml(text);
                               }
                             else if (files[0].name.endsWith(".mxl")) {
                                pasteMxl(text)
                               }
                             else
                               {
                                pasteAbc(text);
                               }
                            }

               reader.readAsBinaryString(files[0]);
             }

    function handleDragover(event) {
               event.stopPropagation();
               event.preventDefault();
               event.dataTransfer.dropEffect = 'copy'; // Explicitly show this is a copy.
             }

    a = document.getElementById(element);
    a.addEventListener('dragover', handleDragover, false);
    a.addEventListener('drop', handleDrop);
    }

    initializeAbc2svg(dropzone);
    
    });

    Opal.defn(self, '$setup_ui_listener', function() {
      var $a, $b, TMP_23, $c, TMP_24, $d, TMP_25, $e, TMP_26, $f, TMP_27, $g, TMP_28, $h, TMP_29, self = this;

      self.$set_file_drop("layout");
      ($a = ($b = self.editor).$on_change, $a.$$p = (TMP_23 = function(e){var self = TMP_23.$$s || this;
if (e == null) e = nil;
      self.$set_status($hash2(["music_model"], {"music_model": "changed"}));
        self.$request_refresh(true);
        return nil;}, TMP_23.$$s = self, TMP_23), $a).call($b);
      ($a = ($c = self.editor).$on_selection_change, $a.$$p = (TMP_24 = function(e){var self = TMP_24.$$s || this, $a, a = nil;
        if (self.editor == null) self.editor = nil;
        if (self.tune_preview_printer == null) self.tune_preview_printer = nil;
        if (self.harpnote_preview_printer == null) self.harpnote_preview_printer = nil;
        if (self.harpnote_player == null) self.harpnote_player = nil;
        if ($gvars.log == null) $gvars.log = nil;
if (e == null) e = nil;
      a = self.editor.$get_selection_positions();
        $gvars.log.$debug("editor selecton " + (a.$first()) + " to " + (a.$last()) + " (" + ("controller") + ":" + (654) + ")");
        if ((($a = self.tune_preview_printer.$range_highlight(a.$first(), a.$last())) !== nil && (!$a.$$is_boolean || $a == true))) {
          return nil
          } else {
          self.harpnote_preview_printer.$unhighlight_all();
          self.harpnote_preview_printer.$range_highlight(a.$first(), a.$last());
          return self.harpnote_player.$range_highlight(a.$first(), a.$last());
        };}, TMP_24.$$s = self, TMP_24), $a).call($c);
      ($a = ($d = self.editor).$on_cursor_change, $a.$$p = (TMP_25 = function(e){var self = TMP_25.$$s || this;
if (e == null) e = nil;
      return self.$request_refresh(false)}, TMP_25.$$s = self, TMP_25), $a).call($d);
      ($a = ($e = self.harpnote_player).$on_noteon, $a.$$p = (TMP_26 = function(e){var self = TMP_26.$$s || this;
        if ($gvars.log == null) $gvars.log = nil;
if (e == null) e = nil;
      $gvars.log.$debug("noteon " + (self.$Native(e)['$[]']("startChar")) + " (" + ("controller") + " " + (668) + ")");
        return self.$highlight_abc_object(e);}, TMP_26.$$s = self, TMP_26), $a).call($e);
      ($a = ($f = self.harpnote_player).$on_noteoff, $a.$$p = (TMP_27 = function(e){var self = TMP_27.$$s || this;
        if ($gvars.log == null) $gvars.log = nil;
if (e == null) e = nil;
      $gvars.log.$debug("noteoff " + (self.$Native(e)['$[]']("startChar")) + " (" + ("controller") + " " + (673) + ")");
        return self.$unhighlight_abc_object(e);}, TMP_27.$$s = self, TMP_27), $a).call($f);
      ($a = ($g = self.harpnote_player).$on_songoff, $a.$$p = (TMP_28 = function(){var self = TMP_28.$$s || this;

      return self.$stop_play_abc()}, TMP_28.$$s = self, TMP_28), $a).call($g);
      return ($a = ($h = $scope.get('Element').$find(window)).$on, $a.$$p = (TMP_29 = function(evt){var self = TMP_29.$$s || this, $a, $b, $c, key = nil, value = nil;
        if (self.systemstatus == null) self.systemstatus = nil;
        if ($gvars.log == null) $gvars.log = nil;
if (evt == null) evt = nil;
      key = self.$Native(evt['$[]']("originalEvent")).$key();
        value = self.$Native(evt['$[]']("originalEvent")).$newValue();
        $gvars.log.$debug("got storage event " + (key) + ": " + (value) + " (" + ("controller") + " " + (701) + ")");
        if ((($a = ($b = (($c = self.systemstatus['$[]']("autorefresh")['$==']("remote")) ? key['$==']("command") : self.systemstatus['$[]']("autorefresh")['$==']("remote")), $b !== false && $b !== nil ?value['$==']("render") : $b)) !== nil && (!$a.$$is_boolean || $a == true))) {
          return self.$load_from_loacalstorage()
          } else {
          return nil
        };}, TMP_29.$$s = self, TMP_29), $a).call($h, "storage");
    });

    Opal.defn(self, '$request_refresh', function(init) {
      var $a, self = this, $case = nil;
      if ($gvars.log == null) $gvars.log = nil;

      if (init !== false && init !== nil) {
        self.$set_status($hash2(["refresh"], {"refresh": true}))};
      $gvars.log.$debug("request refresh " + (self.systemstatus['$[]']("refresh")) + " " + (init) + " " + ("controller") + " " + (714));
      if ((($a = self.refresh_timer) !== nil && (!$a.$$is_boolean || $a == true))) {
        clearTimeout(self.refresh_timer);};
      if ((($a = self.systemstatus['$[]']("refresh")) !== nil && (!$a.$$is_boolean || $a == true))) {
        self.$handle_command("stop");
        return (function() {$case = self.systemstatus['$[]']("autorefresh");if ("on"['$===']($case)) {return self.refresh_timer = setTimeout(function(){self.$render_previews()}, 100)}else if ("off"['$===']($case)) {return self.refresh_timer = setTimeout(function(){self.$render_remote()}, 0)}else if ("remote"['$===']($case)) {return self.refresh_timer = setTimeout(function(){self.$render_previews()}, 500)}else { return nil }})();
        } else {
        return nil
      };
    });

    Opal.defn(self, '$send_remote_command', function(command) {
      var self = this;

      localStorage.setItem('command', '');
      return localStorage.setItem('command', command);;
    });

    Opal.defn(self, '$set_active', function(ui_element) {
      var self = this;

      return $scope.get('Element').$find(ui_element).$css("background-color", "red");
    });

    Opal.defn(self, '$set_inactive', function(ui_element) {
      var self = this;

      return $scope.get('Element').$find(ui_element).$css("background-color", "white");
    });

    self.$private();

    return (Opal.defn(self, '$_init_conf', function() {
      var self = this, result = nil;

      result = $hash2(["produce", "abc_parser", "wrap", "defaults", "annotations", "extract", "layout"], {"produce": [0], "abc_parser": "ABC2SVG", "wrap": 60, "defaults": $hash2(["note_length", "print", "legend", "lyrics", "annotation"], {"note_length": "1/4", "print": $hash2(["t", "v", "startpos", "s", "f", "sf", "j", "l"], {"t": "", "v": [1, 2, 3, 4], "startpos": 15, "s": [[1, 2], [2, 3]], "f": [1, 3], "sf": [2, 4], "j": [1, 3], "l": [1, 2, 3, 4]}), "legend": $hash2(["pos"], {"pos": [20, 20]}), "lyrics": $hash2(["pos"], {"pos": [20, 60]}), "annotation": $hash2(["pos"], {"pos": [2, -5]})}), "annotations": $hash2(["vt", "vr", "vl"], {"vt": $hash2(["text", "pos"], {"text": "v", "pos": [-1, -6]}), "vr": $hash2(["text", "pos"], {"text": "v", "pos": [2, -3]}), "vl": $hash2(["text", "pos"], {"text": "v", "pos": [-4, -3]})}), "extract": $hash2(["0", "1", "2"], {"0": $hash2(["title", "startpos", "voices", "synchlines", "flowlines", "subflowlines", "jumplines", "layoutlines", "legend", "lyrics", "nonflowrest", "notes"], {"title": "alle Stimmen", "startpos": 15, "voices": [1, 2, 3, 4], "synchlines": [[1, 2], [3, 4]], "flowlines": [1, 3], "subflowlines": [2, 4], "jumplines": [1, 3], "layoutlines": [1, 2, 3, 4], "legend": $hash2(["spos", "pos"], {"spos": [320, 27], "pos": [320, 20]}), "lyrics": $hash2(["1"], {"1": $hash2(["verses", "pos"], {"verses": [1], "pos": [350, 70]})}), "nonflowrest": false, "notes": $hash2(["1"], {"1": $hash2(["pos", "text", "style"], {"pos": [320, 0], "text": "", "style": "large"})})}), "1": $hash2(["title", "voices"], {"title": "Sopran, Alt", "voices": [1, 2]}), "2": $hash2(["title", "voices"], {"title": "Tenor, Bass", "voices": [3, 4]})}), "layout": $hash2(["grid", "SHOW_SLUR", "LINE_THIN", "LINE_MEDIUM", "LINE_THICK", "ELLIPSE_SIZE", "REST_SIZE", "X_SPACING", "X_OFFSET", "Y_SCALE", "DRAWING_AREA_SIZE", "BEAT_RESOLUTION", "SHORTEST_NOTE", "BEAT_PER_DURATION", "PITCH_OFFSET", "FONT_STYLE_DEF", "MM_PER_POINT", "DURATION_TO_STYLE", "REST_TO_GLYPH"], {"grid": false, "SHOW_SLUR": false, "LINE_THIN": 0.1, "LINE_MEDIUM": 0.3, "LINE_THICK": 0.5, "ELLIPSE_SIZE": [3.5, 1.7], "REST_SIZE": [4, 2], "X_SPACING": 11.5, "X_OFFSET": 2.8, "Y_SCALE": 4, "DRAWING_AREA_SIZE": [400, 282], "BEAT_RESOLUTION": 192, "SHORTEST_NOTE": 64, "BEAT_PER_DURATION": 3, "PITCH_OFFSET": -43, "FONT_STYLE_DEF": $hash2(["smaller", "small", "regular", "large"], {"smaller": $hash2(["text_color", "font_size", "font_style"], {"text_color": [0, 0, 0], "font_size": 6, "font_style": "normal"}), "small": $hash2(["text_color", "font_size", "font_style"], {"text_color": [0, 0, 0], "font_size": 9, "font_style": "normal"}), "regular": $hash2(["text_color", "font_size", "font_style"], {"text_color": [0, 0, 0], "font_size": 12, "font_style": "normal"}), "large": $hash2(["text_color", "font_size", "font_style"], {"text_color": [0, 0, 0], "font_size": 20, "font_style": "bold"})}), "MM_PER_POINT": 0.3, "DURATION_TO_STYLE": $hash2(["err", "d64", "d48", "d32", "d24", "d16", "d12", "d8", "d6", "d4", "d3", "d2", "d1"], {"err": [2, "filled", $scope.get('FALSE')], "d64": [1, "empty", $scope.get('FALSE')], "d48": [0.75, "empty", $scope.get('TRUE')], "d32": [0.75, "empty", $scope.get('FALSE')], "d24": [0.75, "filled", $scope.get('TRUE')], "d16": [0.75, "filled", $scope.get('FALSE')], "d12": [0.5, "filled", $scope.get('TRUE')], "d8": [0.5, "filled", $scope.get('FALSE')], "d6": [0.3, "filled", $scope.get('TRUE')], "d4": [0.3, "filled", $scope.get('FALSE')], "d3": [0.1, "filled", $scope.get('TRUE')], "d2": [0.1, "filled", $scope.get('FALSE')], "d1": [0.05, "filled", $scope.get('FALSE')]}), "REST_TO_GLYPH": $hash2(["err", "d64", "d48", "d32", "d24", "d16", "d12", "d8", "d6", "d4", "d3", "d2", "d1"], {"err": [[2, 2], "rest_1", $scope.get('FALSE')], "d64": [[0.5, 0.5], "rest_1", $scope.get('FALSE')], "d48": [[0.3, 0.3], "rest_1", $scope.get('TRUE')], "d32": [[0.3, 0.3], "rest_1", $scope.get('FALSE')], "d24": [[0.4, 1], "rest_4", $scope.get('TRUE')], "d16": [[0.4, 1], "rest_4", $scope.get('FALSE')], "d12": [[0.4, 1], "rest_8", $scope.get('TRUE')], "d8": [[0.4, 1], "rest_8", $scope.get('FALSE')], "d6": [[0.4, 1], "rest_16", $scope.get('TRUE')], "d4": [[0.3, 1], "rest_16", $scope.get('FALSE')], "d3": [[0.3, 0.5], "rest_32", $scope.get('TRUE')], "d2": [[0.3, 0.5], "rest_32", $scope.get('FALSE')], "d1": [[0.3, 0.5], "rest_64", $scope.get('FALSE')]})})});
      return result;
    }), nil) && '_init_conf';
  })($scope.base, null);
  return ($a = ($b = $scope.get('Document'))['$ready?'], $a.$$p = (TMP_30 = function(){var self = TMP_30.$$s || this, a = nil;

  a = $scope.get('Controller').$new();
    uicontroller = a;
    return nil;}, TMP_30.$$s = self, TMP_30), $a).call($b);
};

/* Generated by Opal 0.9.0 */
Opal.modules["controller-nw"] = function(Opal) {
  Opal.dynamic_require_severity = "error";
  var OPAL_CONFIG = { method_missing: true, arity_check: false, freezing: true, tainting: true };
  var self = Opal.top, $scope = Opal, nil = Opal.nil, $breaker = Opal.breaker, $slice = Opal.slice, $klass = Opal.klass, $gvars = Opal.gvars, $hash2 = Opal.hash2;

  Opal.add_stubs(['$info', '$gsub', '$handle_command', '$set_status', '$[]', '$render_previews', '$__ic_06_node_fs_commands', '$debug', '$message', '$join', '$backtrace', '$private', '$add_command', '$add_parameter', '$set_default', '$undoable=', '$set_help', '$parameter_help', '$as_action', '$get_text', '$get_metadata', '$first', '$split', '$raise', '$layout_harpnotes', '$harpnote_options', '$map', '$[]=', '$output', '$render_a3', '$render_a4', '$each_with_index', '$each', '$match', '$set_text', '$error', '$as_inverse']);
  return (function($base, $super) {
    function $Controller(){};
    var self = $Controller = $klass($base, $super, 'Controller', $Controller);

    var def = self.$$proto, $scope = self.$$scope;

    def.systemstatus = def.commands = nil;
    Opal.defn(self, '$setup_nodewebkit', function() {
      var self = this, arg = nil, f = nil, e = nil;
      if ($gvars.log == null) $gvars.log = nil;

      try {
      

        var gui = require('nw.gui');
        var fs = require('fs')

        var menu = new gui.Menu({ type: 'menubar', label: 'foobar'});

        menu.append(new gui.MenuItem({
          label: 'Zupfnoter',
          submenu: new gui.Menu()
        }));

        menu.items[0].submenu.append(new gui.MenuItem({
          label: 'About Zupfnoter',
          icon: 'public/menuicons/about-26.png',

          click: function () {
            alert("Zupfnoter version "+ $scope.get('VERSION') + "\n" + $scope.get('COPYRIGHT'));
          }
        }))

        menu.items[0].submenu.append(new gui.MenuItem({
          label: 'show arguments',
          click: function () {
            arg = nil
              arg = gui.App.argv;
            $gvars.log.$info(arg);
          }
        }))


        menu.items[0].submenu.append(new gui.MenuItem({
          label: 'Quit Zupfnoter',
          icon: 'public/menuicons/exit-26.png',

          click: function () {
            gui.App.quit();
          }
        }));

        menu.append(new gui.MenuItem({
          label: 'File',
          submenu: new gui.Menu()
        }));


        var chooser = $("#fileDialog");

        chooser.change(function(evt) {

          var filename = $(this).val();
          f = (filename).$gsub("\\", "\\\\")
        self.$handle_command("_fopen \"" + (f) + "\"")
          // Reset the selected value to empty ('')
          $(this).val('');
        });

        menu.items[1].submenu.append(new gui.MenuItem({
          label: 'open',
          icon: 'public/menuicons/view_file-26.png',
          click: function () {
            chooser.trigger('click');
          }
        }));


        var savechooser = $("#folderDialog");

        savechooser.change(function(evt) {
          var filename = $(this).val();

          f = filename
        self.$set_status($hash2(["nwworkingdir"], {"nwworkingdir": f}))
        f = f.$gsub("\\", "\\\\")
        self.$handle_command("_fsave \"" + (f) + "\"")

          // Reset the selected value to empty ('')
          $(this).val('');
        });

        menu.items[1].submenu.append(new gui.MenuItem({
          label: 'save',
          icon: 'public/menuicons/save-26.png',
          click: function () {
            savechooser.attr("nwworkingdir", self.systemstatus['$[]']("nwworkingdir"));
            savechooser.trigger('click');
          }
        }));

        menu.append(new gui.MenuItem({
          label: 'View',
          submenu: new gui.Menu()
        }));


        menu.items[2].submenu.append(new gui.MenuItem({
          label: 'refresh',
          icon: 'public/menuicons/refresh-26.png',

          click: function () {
            self.$render_previews()
          }
        }));

        menu.items[2].submenu.append(new gui.MenuItem({
          label: 'play all',
          icon: 'public/menuicons/play-26.png',

          click: function () {
            self.$handle_command("p all")
          }
        }));

        menu.items[2].submenu.append(new gui.MenuItem({
          label: 'play from here',
          icon: 'public/menuicons/last-26.png',
          click: function () {
            self.$handle_command("p ff")
          }
        }));

        menu.items[2].submenu.append(new gui.MenuItem({
          label: 'play selection',
          icon: 'public/menuicons/music_transcripts-26.png',
          click: function () {
            self.$handle_command("p sel")
          }
        }));


        menu.items[2].submenu.append(new gui.MenuItem({
          label: 'set extract 0',
          icon: 'public/menuicons/0-26.png',

          click: function () {
            self.$handle_command("view 0")
          }
        }));

        menu.items[2].submenu.append(new gui.MenuItem({
          label: 'set extract 1',
          icon: 'public/menuicons/1-26.png',
          click: function () {
            self.$handle_command("view 1")
          }
        }));

        menu.items[2].submenu.append(new gui.MenuItem({
          label: 'set extract 2',
          icon: 'public/menuicons/2-26.png',
          click: function () {
            self.$handle_command("view 2")
          }
        }));
        menu.items[2].submenu.append(new gui.MenuItem({
          label: 'set extract 3',
          icon: 'public/menuicons/3-26.png',
          click: function () {
            self.$handle_command("view 3")
          }
        }));



        gui.Window.get().menu = menu;
        gui.Window.title = "zupfnoter";
        gui.Window.get().show();

      ;
        return self.$__ic_06_node_fs_commands();
      } catch ($err) {if (Opal.rescue($err, [$scope.get('Exception')])) {e = $err;
        try {
          return $gvars.log.$debug("error with node webkit: " + (e.$message()) + " " + (e.$backtrace().$join("\\n")) + " (" + ("controller-nw") + " " + (188) + ")")
        } finally {
          Opal.gvars["!"] = Opal.exceptions.pop() || Opal.nil;
        }
        }else { throw $err; }
      };
    });

    self.$private();

    return (Opal.defn(self, '$__ic_06_node_fs_commands', function() {
      var $a, $b, TMP_1, $c, TMP_8, self = this;

      ($a = ($b = self.commands).$add_command, $a.$$p = (TMP_1 = function(command){var self = TMP_1.$$s || this, $a, $b, TMP_2, $c, TMP_4, $d, TMP_5;
if (command == null) command = nil;
      ($a = ($b = command).$add_parameter, $a.$$p = (TMP_2 = function(parameter){var self = TMP_2.$$s || this, $a, $b, TMP_3;
if (parameter == null) parameter = nil;
        return ($a = ($b = parameter).$set_default, $a.$$p = (TMP_3 = function(){var self = TMP_3.$$s || this;
            if (self.node_fs_path == null) self.node_fs_path = nil;

          return self.node_fs_path}, TMP_3.$$s = self, TMP_3), $a).call($b)}, TMP_2.$$s = self, TMP_2), $a).call($b, "path", "string");
        (($a = [false]), $c = command, $c['$undoable='].apply($c, $a), $a[$a.length-1]);
        ($a = ($c = command).$set_help, $a.$$p = (TMP_4 = function(){var self = TMP_4.$$s || this;

        return "save to local file system {" + (command.$parameter_help(0)) + "}"}, TMP_4.$$s = self, TMP_4), $a).call($c);
        return ($a = ($d = command).$as_action, $a.$$p = (TMP_5 = function(args){var self = TMP_5.$$s || this, $a, $b, TMP_6, $c, TMP_7, abc_code = nil, metadata = nil, filebase = nil, rootpath = nil, print_variants = nil, pdfs = nil, name = nil;
          if (self.editor == null) self.editor = nil;
          if (self.abc_transformer == null) self.abc_transformer = nil;
          if (self.music_model == null) self.music_model = nil;
          if ($gvars.log == null) $gvars.log = nil;
if (args == null) args = nil;
        abc_code = self.editor.$get_text();
          metadata = self.abc_transformer.$get_metadata(abc_code);
          filebase = metadata['$[]']("F").$first();
          if (filebase !== false && filebase !== nil) {
            filebase = filebase.$split("\n").$first()
            } else {
            self.$raise("Filename not specified in song add an F: instruction")
          };
          rootpath = args['$[]']("path");
          $gvars.log.$info("saving to " + (rootpath));
          self.$layout_harpnotes();
          self.$render_previews();
          print_variants = self.music_model.$harpnote_options()['$[]']("print");
          pdfs = $hash2([], {});
          ($a = ($b = print_variants.$each_with_index()).$map, $a.$$p = (TMP_6 = function(print_variant, index){var self = TMP_6.$$s || this, filename = nil;
if (print_variant == null) print_variant = nil;if (index == null) index = nil;
          filename = print_variant['$[]']("title").$gsub(/[^a-zA-Z0-9\-\_]/, "_");
            pdfs['$[]=']("" + (rootpath) + "/" + (filebase) + "_" + (print_variant['$[]']("title")) + "_a3.pdf", self.$render_a3(index).$output("blob"));
            return pdfs['$[]=']("" + (rootpath) + "/" + (filebase) + "_" + (print_variant['$[]']("title")) + "_a4.pdf", self.$render_a4(index).$output("blob"));}, TMP_6.$$s = self, TMP_6), $a).call($b);
          var fs = require('fs');
          ($a = ($c = pdfs).$each, $a.$$p = (TMP_7 = function(name, pdfdata){var self = TMP_7.$$s || this;
            if ($gvars.log == null) $gvars.log = nil;
if (name == null) name = nil;if (pdfdata == null) pdfdata = nil;
          
            fs.writeFileSync(name, pdfdata)
          ;
            return $gvars.log.$info(name);}, TMP_7.$$s = self, TMP_7), $a).call($c);
          name = "" + (rootpath) + "/" + (filebase) + ".abc";
          fs.writeFileSync(name, self.editor.$get_text())  ;
          return $gvars.log.$info("save abco to " + (name));}, TMP_5.$$s = self, TMP_5), $a).call($d);}, TMP_1.$$s = self, TMP_1), $a).call($b, "_fsave");
      return ($a = ($c = self.commands).$add_command, $a.$$p = (TMP_8 = function(command){var self = TMP_8.$$s || this, $a, $b, TMP_9, $c, TMP_11, $d, TMP_12, $e, TMP_13;
if (command == null) command = nil;
      ($a = ($b = command).$add_parameter, $a.$$p = (TMP_9 = function(p){var self = TMP_9.$$s || this, $a, $b, TMP_10;
if (p == null) p = nil;
        return ($a = ($b = p).$set_help, $a.$$p = (TMP_10 = function(){var self = TMP_10.$$s || this;

          return "path to open"}, TMP_10.$$s = self, TMP_10), $a).call($b)}, TMP_9.$$s = self, TMP_9), $a).call($b, "path", "string");
        ($a = ($c = command).$set_help, $a.$$p = (TMP_11 = function(){var self = TMP_11.$$s || this;

        return "read file from local filesystem " + (command.$parameter_help(0))}, TMP_11.$$s = self, TMP_11), $a).call($c);
        ($a = ($d = command).$as_action, $a.$$p = (TMP_12 = function(args){var self = TMP_12.$$s || this, rootpath = nil, text = nil, nwdir = nil, e = nil;
          if (self.editor == null) self.editor = nil;
          if (self.systemstatus == null) self.systemstatus = nil;
          if ($gvars.log == null) $gvars.log = nil;
if (args == null) args = nil;
        args['$[]=']("oldval", $hash2(["text", "nwworkdir"], {"text": self.editor.$get_text(), "nwworkdir": self.systemstatus['$[]']("nwworkingdir")}));
          rootpath = args['$[]']("path");
          try {
          var fs = require('fs');
            text = fs.readFileSync(rootpath).toString();;
            nwdir = rootpath.$match(/(.*)([\/\\].+)/)['$[]'](1);
            self.$set_status($hash2(["nwworkingdir"], {"nwworkingdir": nwdir}));
            $gvars.log.$info("opened " + (rootpath));
            return self.editor.$set_text(text);
          } catch ($err) {if (Opal.rescue($err, [$scope.get('Exception')])) {e = $err;
            try {
              return $gvars.log.$error(e.$message())
            } finally {
              Opal.gvars["!"] = Opal.exceptions.pop() || Opal.nil;
            }
            }else { throw $err; }
          };}, TMP_12.$$s = self, TMP_12), $a).call($d);
        return ($a = ($e = command).$as_inverse, $a.$$p = (TMP_13 = function(args){var self = TMP_13.$$s || this;
          if (self.editor == null) self.editor = nil;
if (args == null) args = nil;
        self.$set_status($hash2(["nwworkdir"], {"nwworkdir": args['$[]']("oldval")['$[]']("nwworkingdir")}));
          return self.editor.$set_text(args['$[]']("oldval")['$[]']("text"));}, TMP_13.$$s = self, TMP_13), $a).call($e);}, TMP_8.$$s = self, TMP_8), $a).call($c, "_fopen");
    }), nil) && '__ic_06_node_fs_commands';
  })($scope.base, null)
};

/* Generated by Opal 0.9.0 */
Opal.modules["controller_command_definitions"] = function(Opal) {
  Opal.dynamic_require_severity = "error";
  var OPAL_CONFIG = { method_missing: true, arity_check: false, freezing: true, tainting: true };
  function $rb_plus(lhs, rhs) {
    return (typeof(lhs) === 'number' && typeof(rhs) === 'number') ? lhs + rhs : lhs['$+'](rhs);
  }
  var self = Opal.top, $scope = Opal, nil = Opal.nil, $breaker = Opal.breaker, $slice = Opal.slice, $klass = Opal.klass, $gvars = Opal.gvars, $hash2 = Opal.hash2, $range = Opal.range;

  Opal.add_stubs(['$private', '$info', '$add_command', '$undoable=', '$set_help', '$as_action', '$message', '$join', '$help_string_style', '$add_parameter', '$set_default', '$[]', '$parameter_help', '$set_status', '$to_i', '$render_previews', '$loglevel=', '$loglevel', '$keys', '$error', '$undo', '$redo', '$map', '$first', '$name', '$last', '$history', '$undostack', '$redostack', '$===', '$play_abc', '$stop_play_abc', '$gsub', '$raise', '$[]=', '$get_text', '$set_text', '$as_inverse', '$load_demo_tune', '$include?', '$nil?', '$pop', '$lambda', '$call', '$patch_config_part', '$key', '$parse', '$get_metadata', '$update', '$+', '$list', '$retrieve', '$command_tokens', '$new', '$app_name=', '$app_id=', '$then', '$app_name', '$app_id', '$authenticate', '$to_s', '$select', '$=~', '$read_dir', '$to_a', '$match', '$handle_command', '$split', '$choose_file', '$debug', '$layout_harpnotes', '$harpnote_options', '$write_file', '$output', '$render_a3', '$render_a4', '$each', '$push', '$fail', '$when', '$add_metadata', '$read_file']);
  return (function($base, $super) {
    function $Controller(){};
    var self = $Controller = $klass($base, $super, 'Controller', $Controller);

    var def = self.$$proto, $scope = self.$$scope;

    def.commands = nil;
    self.$private();

    Opal.defn(self, '$__ic_01_internal_commands', function() {
      var $a, $b, TMP_1, $c, TMP_4, $d, TMP_10, $e, TMP_16, $f, TMP_22, $g, TMP_25, $h, TMP_28, $i, TMP_32, $j, TMP_36, self = this;
      if ($gvars.log == null) $gvars.log = nil;

      $gvars.log.$info("registering commands");
      ($a = ($b = self.commands).$add_command, $a.$$p = (TMP_1 = function(c){var self = TMP_1.$$s || this, $a, $b, TMP_2, $c, TMP_3;
if (c == null) c = nil;
      (($a = [false]), $b = c, $b['$undoable='].apply($b, $a), $a[$a.length-1]);
        ($a = ($b = c).$set_help, $a.$$p = (TMP_2 = function(){var self = TMP_2.$$s || this;

        return "this help"}, TMP_2.$$s = self, TMP_2), $a).call($b);
        return ($a = ($c = c).$as_action, $a.$$p = (TMP_3 = function(){var self = TMP_3.$$s || this;
          if (self.commands == null) self.commands = nil;
          if ($gvars.log == null) $gvars.log = nil;

        return $gvars.log.$message("<pre>" + (self.commands.$help_string_style().$join("\n")) + "</pre>")}, TMP_3.$$s = self, TMP_3), $a).call($c);}, TMP_1.$$s = self, TMP_1), $a).call($b, "help");
      ($a = ($c = self.commands).$add_command, $a.$$p = (TMP_4 = function(command){var self = TMP_4.$$s || this, $a, $b, TMP_5, $c, TMP_8, $d, TMP_9;
if (command == null) command = nil;
      ($a = ($b = command).$add_parameter, $a.$$p = (TMP_5 = function(p){var self = TMP_5.$$s || this, $a, $b, TMP_6, $c, TMP_7;
if (p == null) p = nil;
        ($a = ($b = p).$set_default, $a.$$p = (TMP_6 = function(){var self = TMP_6.$$s || this;
            if (self.systemstatus == null) self.systemstatus = nil;

          return self.systemstatus['$[]']("view")}, TMP_6.$$s = self, TMP_6), $a).call($b);
          return ($a = ($c = p).$set_help, $a.$$p = (TMP_7 = function(){var self = TMP_7.$$s || this;
            if (self.systemstatus == null) self.systemstatus = nil;

          return "id of the view to be used for preview [" + (self.systemstatus['$[]']("view")) + "]"}, TMP_7.$$s = self, TMP_7), $a).call($c);}, TMP_5.$$s = self, TMP_5), $a).call($b, "view", "integer");
        ($a = ($c = command).$set_help, $a.$$p = (TMP_8 = function(){var self = TMP_8.$$s || this;

        return "set current view  " + (command.$parameter_help(0)) + " and redisplay"}, TMP_8.$$s = self, TMP_8), $a).call($c);
        (($a = [false]), $d = command, $d['$undoable='].apply($d, $a), $a[$a.length-1]);
        return ($a = ($d = command).$as_action, $a.$$p = (TMP_9 = function(args){var self = TMP_9.$$s || this;
if (args == null) args = nil;
        self.$set_status($hash2(["view"], {"view": args['$[]']("view").$to_i()}));
          return self.$render_previews();}, TMP_9.$$s = self, TMP_9), $a).call($d);}, TMP_4.$$s = self, TMP_4), $a).call($c, "view");
      ($a = ($d = self.commands).$add_command, $a.$$p = (TMP_10 = function(c){var self = TMP_10.$$s || this, $a, $b, TMP_11, $c, TMP_12, $d, TMP_15;
if (c == null) c = nil;
      (($a = [false]), $b = c, $b['$undoable='].apply($b, $a), $a[$a.length-1]);
        ($a = ($b = c).$set_help, $a.$$p = (TMP_11 = function(){var self = TMP_11.$$s || this;

        return "set log level to " + (c.$parameter_help(0))}, TMP_11.$$s = self, TMP_11), $a).call($b);
        ($a = ($c = c).$add_parameter, $a.$$p = (TMP_12 = function(parameter){var self = TMP_12.$$s || this, $a, $b, TMP_13, $c, TMP_14;
if (parameter == null) parameter = nil;
        ($a = ($b = parameter).$set_default, $a.$$p = (TMP_13 = function(){var self = TMP_13.$$s || this;

          return "warning"}, TMP_13.$$s = self, TMP_13), $a).call($b);
          return ($a = ($c = parameter).$set_help, $a.$$p = (TMP_14 = function(){var self = TMP_14.$$s || this;

          return "error | warning | info | debug"}, TMP_14.$$s = self, TMP_14), $a).call($c);}, TMP_12.$$s = self, TMP_12), $a).call($c, "level", "string");
        return ($a = ($d = c).$as_action, $a.$$p = (TMP_15 = function(args){var self = TMP_15.$$s || this, $a, $b;
          if ($gvars.log == null) $gvars.log = nil;
if (args == null) args = nil;
        (($a = [args['$[]']("level")]), $b = $gvars.log, $b['$loglevel='].apply($b, $a), $a[$a.length-1]);
          return self.$set_status($hash2(["loglevel"], {"loglevel": $gvars.log.$loglevel()}));}, TMP_15.$$s = self, TMP_15), $a).call($d);}, TMP_10.$$s = self, TMP_10), $a).call($d, "loglevel");
      ($a = ($e = self.commands).$add_command, $a.$$p = (TMP_16 = function(c){var self = TMP_16.$$s || this, $a, $b, TMP_17, $c, TMP_18, $d, TMP_21, values = nil;
if (c == null) c = nil;
      (($a = [false]), $b = c, $b['$undoable='].apply($b, $a), $a[$a.length-1]);
        ($a = ($b = c).$set_help, $a.$$p = (TMP_17 = function(){var self = TMP_17.$$s || this;

        return "turnon autorefresh"}, TMP_17.$$s = self, TMP_17), $a).call($b);
        values = $hash2(["on", "off", "remote"], {"on": "on", "off": "off", "remote": "remote"});
        ($a = ($c = c).$add_parameter, $a.$$p = (TMP_18 = function(parameter){var self = TMP_18.$$s || this, $a, $b, TMP_19, $c, TMP_20;
if (parameter == null) parameter = nil;
        ($a = ($b = parameter).$set_default, $a.$$p = (TMP_19 = function(){var self = TMP_19.$$s || this;

          return "true"}, TMP_19.$$s = self, TMP_19), $a).call($b);
          return ($a = ($c = parameter).$set_help, $a.$$p = (TMP_20 = function(){var self = TMP_20.$$s || this;

          return "" + (values.$keys().$join(" | "))}, TMP_20.$$s = self, TMP_20), $a).call($c);}, TMP_18.$$s = self, TMP_18), $a).call($c, "value", "string");
        return ($a = ($d = c).$as_action, $a.$$p = (TMP_21 = function(args){var self = TMP_21.$$s || this, result = nil;
          if ($gvars.log == null) $gvars.log = nil;
if (args == null) args = nil;
        result = values['$[]'](args['$[]']("value"));
          if (result !== false && result !== nil) {
            return self.$set_status($hash2(["autorefresh"], {"autorefresh": result}))
            } else {
            return $gvars.log.$error("wrong parameter " + (args['$[]']("value")) + ", " + (c.$parameter_help(0)))
          };}, TMP_21.$$s = self, TMP_21), $a).call($d);}, TMP_16.$$s = self, TMP_16), $a).call($e, "autorefresh");
      ($a = ($f = self.commands).$add_command, $a.$$p = (TMP_22 = function(c){var self = TMP_22.$$s || this, $a, $b, TMP_23, $c, TMP_24;
if (c == null) c = nil;
      (($a = [false]), $b = c, $b['$undoable='].apply($b, $a), $a[$a.length-1]);
        ($a = ($b = c).$set_help, $a.$$p = (TMP_23 = function(){var self = TMP_23.$$s || this;

        return "undo last command"}, TMP_23.$$s = self, TMP_23), $a).call($b);
        return ($a = ($c = c).$as_action, $a.$$p = (TMP_24 = function(a){var self = TMP_24.$$s || this;
          if (self.commands == null) self.commands = nil;
if (a == null) a = nil;
        return self.commands.$undo()}, TMP_24.$$s = self, TMP_24), $a).call($c);}, TMP_22.$$s = self, TMP_22), $a).call($f, "undo");
      ($a = ($g = self.commands).$add_command, $a.$$p = (TMP_25 = function(c){var self = TMP_25.$$s || this, $a, $b, TMP_26, $c, TMP_27;
if (c == null) c = nil;
      (($a = [false]), $b = c, $b['$undoable='].apply($b, $a), $a[$a.length-1]);
        ($a = ($b = c).$set_help, $a.$$p = (TMP_26 = function(){var self = TMP_26.$$s || this;

        return "redo last command"}, TMP_26.$$s = self, TMP_26), $a).call($b);
        return ($a = ($c = c).$as_action, $a.$$p = (TMP_27 = function(a){var self = TMP_27.$$s || this;
          if (self.commands == null) self.commands = nil;
if (a == null) a = nil;
        return self.commands.$redo()}, TMP_27.$$s = self, TMP_27), $a).call($c);}, TMP_25.$$s = self, TMP_25), $a).call($g, "redo");
      ($a = ($h = self.commands).$add_command, $a.$$p = (TMP_28 = function(c){var self = TMP_28.$$s || this, $a, $b, TMP_29, $c, TMP_30;
if (c == null) c = nil;
      (($a = [false]), $b = c, $b['$undoable='].apply($b, $a), $a[$a.length-1]);
        ($a = ($b = c).$set_help, $a.$$p = (TMP_29 = function(){var self = TMP_29.$$s || this;

        return "show history"}, TMP_29.$$s = self, TMP_29), $a).call($b);
        return ($a = ($c = c).$as_action, $a.$$p = (TMP_30 = function(a){var self = TMP_30.$$s || this, $a, $b, TMP_31, history = nil;
          if (self.commands == null) self.commands = nil;
          if ($gvars.log == null) $gvars.log = nil;
if (a == null) a = nil;
        history = ($a = ($b = self.commands.$history()).$map, $a.$$p = (TMP_31 = function(c){var self = TMP_31.$$s || this;
if (c == null) c = nil;
          return "" + (c.$first()) + ": " + (c['$[]'](1).$name()) + "(" + (c.$last()) + ")"}, TMP_31.$$s = self, TMP_31), $a).call($b);
          return $gvars.log.$message("<pre>" + (history.$join("\n")) + "</pre>");}, TMP_30.$$s = self, TMP_30), $a).call($c);}, TMP_28.$$s = self, TMP_28), $a).call($h, "history");
      ($a = ($i = self.commands).$add_command, $a.$$p = (TMP_32 = function(c){var self = TMP_32.$$s || this, $a, $b, TMP_33, $c, TMP_34;
if (c == null) c = nil;
      (($a = [false]), $b = c, $b['$undoable='].apply($b, $a), $a[$a.length-1]);
        ($a = ($b = c).$set_help, $a.$$p = (TMP_33 = function(){var self = TMP_33.$$s || this;

        return "show undo stack"}, TMP_33.$$s = self, TMP_33), $a).call($b);
        return ($a = ($c = c).$as_action, $a.$$p = (TMP_34 = function(a){var self = TMP_34.$$s || this, $a, $b, TMP_35, history = nil;
          if (self.commands == null) self.commands = nil;
          if ($gvars.log == null) $gvars.log = nil;
if (a == null) a = nil;
        history = ($a = ($b = self.commands.$undostack()).$map, $a.$$p = (TMP_35 = function(c){var self = TMP_35.$$s || this;
if (c == null) c = nil;
          return "" + (c.$first()) + ": " + (c['$[]'](1).$name()) + "(" + (c.$last()) + ")"}, TMP_35.$$s = self, TMP_35), $a).call($b);
          return $gvars.log.$message("<pre>" + (history.$join("\n")) + "</pre>");}, TMP_34.$$s = self, TMP_34), $a).call($c);}, TMP_32.$$s = self, TMP_32), $a).call($i, "showundo");
      return ($a = ($j = self.commands).$add_command, $a.$$p = (TMP_36 = function(c){var self = TMP_36.$$s || this, $a, $b, TMP_37, $c, TMP_38;
if (c == null) c = nil;
      (($a = [false]), $b = c, $b['$undoable='].apply($b, $a), $a[$a.length-1]);
        ($a = ($b = c).$set_help, $a.$$p = (TMP_37 = function(){var self = TMP_37.$$s || this;

        return "show redo stack"}, TMP_37.$$s = self, TMP_37), $a).call($b);
        return ($a = ($c = c).$as_action, $a.$$p = (TMP_38 = function(a){var self = TMP_38.$$s || this, $a, $b, TMP_39, history = nil;
          if (self.commands == null) self.commands = nil;
          if ($gvars.log == null) $gvars.log = nil;
if (a == null) a = nil;
        history = ($a = ($b = self.commands.$redostack()).$map, $a.$$p = (TMP_39 = function(c){var self = TMP_39.$$s || this;
if (c == null) c = nil;
          return "" + (c.$first()) + ": " + (c['$[]'](1).$name()) + "(" + (c.$last()) + ")"}, TMP_39.$$s = self, TMP_39), $a).call($b);
          return $gvars.log.$message("<pre>" + (history.$join("\n")) + "</pre>");}, TMP_38.$$s = self, TMP_38), $a).call($c);}, TMP_36.$$s = self, TMP_36), $a).call($j, "showredo");
    });

    Opal.defn(self, '$__ic_02_play_commands', function() {
      var $a, $b, TMP_40, $c, TMP_46, $d, TMP_49, self = this;

      ($a = ($b = self.commands).$add_command, $a.$$p = (TMP_40 = function(c){var self = TMP_40.$$s || this, $a, $b, TMP_41, $c, TMP_42, $d, TMP_45;
if (c == null) c = nil;
      (($a = [false]), $b = c, $b['$undoable='].apply($b, $a), $a[$a.length-1]);
        ($a = ($b = c).$set_help, $a.$$p = (TMP_41 = function(){var self = TMP_41.$$s || this;

        return "play song " + (c.$parameter_help(0))}, TMP_41.$$s = self, TMP_41), $a).call($b);
        ($a = ($c = c).$add_parameter, $a.$$p = (TMP_42 = function(parameter){var self = TMP_42.$$s || this, $a, $b, TMP_43, $c, TMP_44;
if (parameter == null) parameter = nil;
        ($a = ($b = parameter).$set_default, $a.$$p = (TMP_43 = function(){var self = TMP_43.$$s || this;

          return "ff"}, TMP_43.$$s = self, TMP_43), $a).call($b);
          return ($a = ($c = parameter).$set_help, $a.$$p = (TMP_44 = function(){var self = TMP_44.$$s || this;

          return "r(all | ff | sel): range to play"}, TMP_44.$$s = self, TMP_44), $a).call($c);}, TMP_42.$$s = self, TMP_42), $a).call($c, "range", "string");
        return ($a = ($d = c).$as_action, $a.$$p = (TMP_45 = function(argument){var self = TMP_45.$$s || this, $case = nil;
          if ($gvars.log == null) $gvars.log = nil;
if (argument == null) argument = nil;
        return (function() {$case = argument['$[]']("range");if ("sel"['$===']($case)) {return self.$play_abc("selection")}else if ("ff"['$===']($case)) {return self.$play_abc("selection_ff")}else if ("all"['$===']($case)) {return self.$play_abc()}else {return $gvars.log.$error("wrong range to play")}})()}, TMP_45.$$s = self, TMP_45), $a).call($d);}, TMP_40.$$s = self, TMP_40), $a).call($b, "p");
      ($a = ($c = self.commands).$add_command, $a.$$p = (TMP_46 = function(c){var self = TMP_46.$$s || this, $a, $b, TMP_47, $c, TMP_48;
if (c == null) c = nil;
      (($a = [false]), $b = c, $b['$undoable='].apply($b, $a), $a[$a.length-1]);
        ($a = ($b = c).$set_help, $a.$$p = (TMP_47 = function(){var self = TMP_47.$$s || this;

        return "stop playing"}, TMP_47.$$s = self, TMP_47), $a).call($b);
        return ($a = ($c = c).$as_action, $a.$$p = (TMP_48 = function(a){var self = TMP_48.$$s || this;
if (a == null) a = nil;
        return self.$stop_play_abc()}, TMP_48.$$s = self, TMP_48), $a).call($c);}, TMP_46.$$s = self, TMP_46), $a).call($c, "stop");
      return ($a = ($d = self.commands).$add_command, $a.$$p = (TMP_49 = function(c){var self = TMP_49.$$s || this, $a, $b, TMP_50, $c, TMP_51;
if (c == null) c = nil;
      (($a = [false]), $b = c, $b['$undoable='].apply($b, $a), $a[$a.length-1]);
        ($a = ($b = c).$set_help, $a.$$p = (TMP_50 = function(){var self = TMP_50.$$s || this;

        return "refresh"}, TMP_50.$$s = self, TMP_50), $a).call($b);
        return ($a = ($c = c).$as_action, $a.$$p = (TMP_51 = function(a){var self = TMP_51.$$s || this;
if (a == null) a = nil;
        return self.$render_previews()}, TMP_51.$$s = self, TMP_51), $a).call($c);}, TMP_49.$$s = self, TMP_49), $a).call($d, "render");
    });

    Opal.defn(self, '$__ic_03_create_commands', function() {
      var $a, $b, TMP_52, $c, TMP_61, $d, TMP_65, $e, TMP_69, $f, TMP_77, $g, TMP_96, self = this;

      ($a = ($b = self.commands).$add_command, $a.$$p = (TMP_52 = function(c){var self = TMP_52.$$s || this, $a, $b, TMP_53, $c, TMP_54, $d, TMP_56, $e, TMP_59, $f, TMP_60;
if (c == null) c = nil;
      ($a = ($b = c).$set_help, $a.$$p = (TMP_53 = function(){var self = TMP_53.$$s || this;

        return "create song " + (c.$parameter_help(0)) + " " + (c.$parameter_help(1))}, TMP_53.$$s = self, TMP_53), $a).call($b);
        ($a = ($c = c).$add_parameter, $a.$$p = (TMP_54 = function(parameter){var self = TMP_54.$$s || this, $a, $b, TMP_55;
if (parameter == null) parameter = nil;
        return ($a = ($b = parameter).$set_help, $a.$$p = (TMP_55 = function(){var self = TMP_55.$$s || this;

          return "value for X: line, a unique id"}, TMP_55.$$s = self, TMP_55), $a).call($b)}, TMP_54.$$s = self, TMP_54), $a).call($c, "id", "string");
        ($a = ($d = c).$add_parameter, $a.$$p = (TMP_56 = function(parameter){var self = TMP_56.$$s || this, $a, $b, TMP_57, $c, TMP_58;
if (parameter == null) parameter = nil;
        ($a = ($b = parameter).$set_default, $a.$$p = (TMP_57 = function(){var self = TMP_57.$$s || this;

          return "untitled"}, TMP_57.$$s = self, TMP_57), $a).call($b);
          return ($a = ($c = parameter).$set_help, $a.$$p = (TMP_58 = function(){var self = TMP_58.$$s || this;

          return "Title of the song"}, TMP_58.$$s = self, TMP_58), $a).call($c);}, TMP_56.$$s = self, TMP_56), $a).call($d, "title", "string");
        ($a = ($e = c).$as_action, $a.$$p = (TMP_59 = function(args){var self = TMP_59.$$s || this, song_id = nil, song_title = nil, filename = nil, template = nil;
          if (self.editor == null) self.editor = nil;
if (args == null) args = nil;
        song_id = args['$[]']("id");
          song_title = args['$[]']("title");
          filename = song_title.$gsub(/[^a-zA-Z0-9\-\_]/, "_");
          if (song_id !== false && song_id !== nil) {
            } else {
            self.$raise("no id specified")
          };
          if (song_title !== false && song_title !== nil) {
            } else {
            self.$raise("no title specified")
          };
          template = "X:" + (song_id) + "\nF:" + (song_id) + "_" + (filename) + "\nT:" + (song_title) + "\nC:\nS:\nM:4/4\nL:1/4\nQ:1/4=120\nK:C\n%%score T1 T2  B1 B2\nV:T1 clef=treble-8 name=\"Sopran\" snm=\"S\"\nC\nV:T2 clef=treble-8  name=\"Alt\" snm=\"A\"\nC,\n\n%%%%zupfnoter.config\n\n{\n  \"produce\": [\"1\", \"2\"]\n}\n";
          args['$[]=']("oldval", self.editor.$get_text());
          self.editor.$set_text(template);
          return self.$set_status($hash2(["music_model"], {"music_model": "new"}));}, TMP_59.$$s = self, TMP_59), $a).call($e);
        return ($a = ($f = c).$as_inverse, $a.$$p = (TMP_60 = function(args){var self = TMP_60.$$s || this;
          if (self.editor == null) self.editor = nil;
if (args == null) args = nil;
        return self.editor.$set_text(args['$[]']("oldval"))}, TMP_60.$$s = self, TMP_60), $a).call($f);}, TMP_52.$$s = self, TMP_52), $a).call($b, "c");
      ($a = ($c = self.commands).$add_command, $a.$$p = (TMP_61 = function(command){var self = TMP_61.$$s || this, $a, $b, TMP_62, $c, TMP_63, $d, TMP_64;
if (command == null) command = nil;
      ($a = ($b = command).$set_help, $a.$$p = (TMP_62 = function(){var self = TMP_62.$$s || this;

        return "Load demo tune"}, TMP_62.$$s = self, TMP_62), $a).call($b);
        ($a = ($c = command).$as_action, $a.$$p = (TMP_63 = function(args){var self = TMP_63.$$s || this;
          if (self.editor == null) self.editor = nil;
if (args == null) args = nil;
        args['$[]=']("oldval", self.editor.$get_text());
          return self.$load_demo_tune();}, TMP_63.$$s = self, TMP_63), $a).call($c);
        return ($a = ($d = command).$as_inverse, $a.$$p = (TMP_64 = function(args){var self = TMP_64.$$s || this;
          if (self.editor == null) self.editor = nil;
if (args == null) args = nil;
        return self.editor.$set_text(args['$[]']("oldval"))}, TMP_64.$$s = self, TMP_64), $a).call($d);}, TMP_61.$$s = self, TMP_61), $a).call($c, "demo");
      ($a = ($d = self.commands).$add_command, $a.$$p = (TMP_65 = function(command){var self = TMP_65.$$s || this, $a, $b, TMP_66, $c, TMP_67, $d, TMP_68;
if (command == null) command = nil;
      ($a = ($b = command).$set_help, $a.$$p = (TMP_66 = function(){var self = TMP_66.$$s || this;

        return "Handle a dropped _abc"}, TMP_66.$$s = self, TMP_66), $a).call($b);
        ($a = ($c = command).$as_action, $a.$$p = (TMP_67 = function(args){var self = TMP_67.$$s || this;
          if (self.editor == null) self.editor = nil;
          if (self.dropped_abc == null) self.dropped_abc = nil;
if (args == null) args = nil;
        args['$[]=']("oldval", self.editor.$get_text());
          return self.editor.$set_text(self.dropped_abc);}, TMP_67.$$s = self, TMP_67), $a).call($c);
        return ($a = ($d = command).$as_inverse, $a.$$p = (TMP_68 = function(args){var self = TMP_68.$$s || this;
          if (self.editor == null) self.editor = nil;
if (args == null) args = nil;
        return self.editor.$set_text(args['$[]']("oldval"))}, TMP_68.$$s = self, TMP_68), $a).call($d);}, TMP_65.$$s = self, TMP_65), $a).call($d, "drop");
      ($a = ($e = self.commands).$add_command, $a.$$p = (TMP_69 = function(command){var self = TMP_69.$$s || this, $a, $b, TMP_70, $c, TMP_72, $d, TMP_74, $e, TMP_75, $f, TMP_76;
if (command == null) command = nil;
      (($a = [false]), $b = command, $b['$undoable='].apply($b, $a), $a[$a.length-1]);
        ($a = ($b = command).$add_parameter, $a.$$p = (TMP_70 = function(parameter){var self = TMP_70.$$s || this, $a, $b, TMP_71;
if (parameter == null) parameter = nil;
        return ($a = ($b = parameter).$set_help, $a.$$p = (TMP_71 = function(){var self = TMP_71.$$s || this;

          return "parameter key"}, TMP_71.$$s = self, TMP_71), $a).call($b)}, TMP_70.$$s = self, TMP_70), $a).call($b, "key", "string");
        ($a = ($c = command).$add_parameter, $a.$$p = (TMP_72 = function(parameter){var self = TMP_72.$$s || this, $a, $b, TMP_73;
if (parameter == null) parameter = nil;
        return ($a = ($b = parameter).$set_help, $a.$$p = (TMP_73 = function(){var self = TMP_73.$$s || this;

          return "parameter value (true | false"}, TMP_73.$$s = self, TMP_73), $a).call($b)}, TMP_72.$$s = self, TMP_72), $a).call($c, "value", "boolean");
        ($a = ($d = command).$set_help, $a.$$p = (TMP_74 = function(){var self = TMP_74.$$s || this;

        return "set configuration parameter true/false"}, TMP_74.$$s = self, TMP_74), $a).call($d);
        ($a = ($e = command).$as_action, $a.$$p = (TMP_75 = function(args){var self = TMP_75.$$s || this, $a, value = nil;
          if ($gvars.conf == null) $gvars.conf = nil;
if (args == null) args = nil;
        value = $hash2(["true", "false"], {"true": true, "false": false})['$[]'](args['$[]']("value"));
          if ((($a = $gvars.conf.$keys()['$include?'](args['$[]']("key"))) !== nil && (!$a.$$is_boolean || $a == true))) {
            } else {
            self.$raise("invalid key " + (args['$[]']("key")))
          };
          if ((($a = value['$nil?']()) !== nil && (!$a.$$is_boolean || $a == true))) {
            self.$raise("value must be true or false")};
          $gvars.conf['$[]='](args['$[]']("key"), value);
          return nil;}, TMP_75.$$s = self, TMP_75), $a).call($e);
        return ($a = ($f = command).$as_inverse, $a.$$p = (TMP_76 = function(args){var self = TMP_76.$$s || this;
          if ($gvars.conf == null) $gvars.conf = nil;
if (args == null) args = nil;
        return $gvars.conf.$pop()}, TMP_76.$$s = self, TMP_76), $a).call($f);}, TMP_69.$$s = self, TMP_69), $a).call($e, "conf");
      ($a = ($f = self.commands).$add_command, $a.$$p = (TMP_77 = function(command){var self = TMP_77.$$s || this, $a, $b, TMP_78, $c, TMP_80, $d, TMP_81;
if (command == null) command = nil;
      (($a = [false]), $b = command, $b['$undoable='].apply($b, $a), $a[$a.length-1]);
        ($a = ($b = command).$add_parameter, $a.$$p = (TMP_78 = function(parameter){var self = TMP_78.$$s || this, $a, $b, TMP_79;
if (parameter == null) parameter = nil;
        return ($a = ($b = parameter).$set_help, $a.$$p = (TMP_79 = function(){var self = TMP_79.$$s || this;

          return "parameter key"}, TMP_79.$$s = self, TMP_79), $a).call($b)}, TMP_78.$$s = self, TMP_78), $a).call($b, "key", "string");
        ($a = ($c = command).$set_help, $a.$$p = (TMP_80 = function(){var self = TMP_80.$$s || this;

        return "add configuration parameter"}, TMP_80.$$s = self, TMP_80), $a).call($c);
        return ($a = ($d = command).$as_action, $a.$$p = (TMP_81 = function(args){var self = TMP_81.$$s || this, $a, $b, TMP_82, $c, TMP_83, $d, TMP_84, $e, TMP_85, $f, TMP_86, $g, TMP_87, $h, TMP_88, $i, TMP_89, $j, TMP_90, $k, TMP_91, $l, TMP_92, $m, TMP_93, $n, TMP_94, $o, TMP_95, values = nil, value = nil;
          if (self.editor == null) self.editor = nil;
if (args == null) args = nil;
        values = $hash2(["title", "voices", "flowlines", "layoutlines", "jumplines", "synchlines", "legend", "notes", "lyrics", "nonflowrest", "startpos", "subflowlines", "produce", "xx"], {"title": ($a = ($b = self).$lambda, $a.$$p = (TMP_82 = function(){var self = TMP_82.$$s || this;
            if (self.systemstatus == null) self.systemstatus = nil;

          return $hash2(["key", "value"], {"key": "extract." + (self.systemstatus['$[]']("view")) + ".title", "value": "extract " + (self.systemstatus['$[]']("view"))})}, TMP_82.$$s = self, TMP_82), $a).call($b), "voices": ($a = ($c = self).$lambda, $a.$$p = (TMP_83 = function(){var self = TMP_83.$$s || this;
            if (self.systemstatus == null) self.systemstatus = nil;
            if ($gvars.conf == null) $gvars.conf = nil;

          return $hash2(["key", "value"], {"key": "extract." + (self.systemstatus['$[]']("view")) + ".voices", "value": $gvars.conf['$[]']("extract.0.voices")})}, TMP_83.$$s = self, TMP_83), $a).call($c), "flowlines": ($a = ($d = self).$lambda, $a.$$p = (TMP_84 = function(){var self = TMP_84.$$s || this;
            if (self.systemstatus == null) self.systemstatus = nil;
            if ($gvars.conf == null) $gvars.conf = nil;

          return $hash2(["key", "value"], {"key": "extract." + (self.systemstatus['$[]']("view")) + ".flowlines", "value": $gvars.conf['$[]']("extract.0.flowlines")})}, TMP_84.$$s = self, TMP_84), $a).call($d), "layoutlines": ($a = ($e = self).$lambda, $a.$$p = (TMP_85 = function(){var self = TMP_85.$$s || this;
            if (self.systemstatus == null) self.systemstatus = nil;
            if ($gvars.conf == null) $gvars.conf = nil;

          return $hash2(["key", "value"], {"key": "extract." + (self.systemstatus['$[]']("view")) + ".layoutlines", "value": $gvars.conf['$[]']("extract.0.layoutlines")})}, TMP_85.$$s = self, TMP_85), $a).call($e), "jumplines": ($a = ($f = self).$lambda, $a.$$p = (TMP_86 = function(){var self = TMP_86.$$s || this;
            if (self.systemstatus == null) self.systemstatus = nil;
            if ($gvars.conf == null) $gvars.conf = nil;

          return $hash2(["key", "value"], {"key": "extract." + (self.systemstatus['$[]']("view")) + ".jumplines", "value": $gvars.conf['$[]']("extract.0.jumplines")})}, TMP_86.$$s = self, TMP_86), $a).call($f), "synchlines": ($a = ($g = self).$lambda, $a.$$p = (TMP_87 = function(){var self = TMP_87.$$s || this;
            if (self.systemstatus == null) self.systemstatus = nil;
            if ($gvars.conf == null) $gvars.conf = nil;

          return $hash2(["key", "value"], {"key": "extract." + (self.systemstatus['$[]']("view")) + ".synchlines", "value": $gvars.conf['$[]']("extract.0.synchlines")})}, TMP_87.$$s = self, TMP_87), $a).call($g), "legend": ($a = ($h = self).$lambda, $a.$$p = (TMP_88 = function(){var self = TMP_88.$$s || this;
            if (self.systemstatus == null) self.systemstatus = nil;
            if ($gvars.conf == null) $gvars.conf = nil;

          return $hash2(["key", "value"], {"key": "extract." + (self.systemstatus['$[]']("view")) + ".legend", "value": $gvars.conf['$[]']("extract.0.legend")})}, TMP_88.$$s = self, TMP_88), $a).call($h), "notes": ($a = ($i = self).$lambda, $a.$$p = (TMP_89 = function(){var self = TMP_89.$$s || this;
            if (self.systemstatus == null) self.systemstatus = nil;
            if ($gvars.conf == null) $gvars.conf = nil;

          return $hash2(["key", "value"], {"key": "extract." + (self.systemstatus['$[]']("view")) + ".notes.x", "value": $gvars.conf['$[]']("extract.0.notes.1")})}, TMP_89.$$s = self, TMP_89), $a).call($i), "lyrics": ($a = ($j = self).$lambda, $a.$$p = (TMP_90 = function(){var self = TMP_90.$$s || this;
            if (self.systemstatus == null) self.systemstatus = nil;
            if ($gvars.conf == null) $gvars.conf = nil;

          return $hash2(["key", "value"], {"key": "extract." + (self.systemstatus['$[]']("view")) + ".lyrics.x", "value": $gvars.conf['$[]']("extract.0.lyrics.1")})}, TMP_90.$$s = self, TMP_90), $a).call($j), "nonflowrest": ($a = ($k = self).$lambda, $a.$$p = (TMP_91 = function(){var self = TMP_91.$$s || this;
            if (self.systemstatus == null) self.systemstatus = nil;
            if ($gvars.conf == null) $gvars.conf = nil;

          return $hash2(["key", "value"], {"key": "extract." + (self.systemstatus['$[]']("view")) + ".nonflowrest", "value": $gvars.conf['$[]']("extract.0.nonflowrest")})}, TMP_91.$$s = self, TMP_91), $a).call($k), "startpos": ($a = ($l = self).$lambda, $a.$$p = (TMP_92 = function(){var self = TMP_92.$$s || this;
            if (self.systemstatus == null) self.systemstatus = nil;
            if ($gvars.conf == null) $gvars.conf = nil;

          return $hash2(["key", "value"], {"key": "extract." + (self.systemstatus['$[]']("view")) + ".startpos", "value": $gvars.conf['$[]']("extract.0.startpos")})}, TMP_92.$$s = self, TMP_92), $a).call($l), "subflowlines": ($a = ($m = self).$lambda, $a.$$p = (TMP_93 = function(){var self = TMP_93.$$s || this;
            if (self.systemstatus == null) self.systemstatus = nil;
            if ($gvars.conf == null) $gvars.conf = nil;

          return $hash2(["key", "value"], {"key": "extract." + (self.systemstatus['$[]']("view")) + ".subflowlines", "value": $gvars.conf['$[]']("extract.0.subflowlines")})}, TMP_93.$$s = self, TMP_93), $a).call($m), "produce": ($a = ($n = self).$lambda, $a.$$p = (TMP_94 = function(){var self = TMP_94.$$s || this;
            if ($gvars.conf == null) $gvars.conf = nil;

          return $hash2(["key", "value"], {"key": "produce", "value": $gvars.conf['$[]']("produce")})}, TMP_94.$$s = self, TMP_94), $a).call($n), "xx": ($a = ($o = self).$lambda, $a.$$p = (TMP_95 = function(){var self = TMP_95.$$s || this;
            if ($gvars.conf == null) $gvars.conf = nil;

          return $hash2(["key", "value"], {"key": "xx", "value": $gvars.conf['$[]']()})}, TMP_95.$$s = self, TMP_95), $a).call($o)});
          value = values['$[]'](args['$[]']("key"));
          if (value !== false && value !== nil) {
            value = value.$call();
            return self.editor.$patch_config_part(value['$[]']("key"), value['$[]']("value"));
            } else {
            self.$raise("unknown configuration parameter " + (self.$key()));
            return nil;
          };}, TMP_81.$$s = self, TMP_81), $a).call($d);}, TMP_77.$$s = self, TMP_77), $a).call($f, "addconf");
      return ($a = ($g = self.commands).$add_command, $a.$$p = (TMP_96 = function(command){var self = TMP_96.$$s || this, $a, $b, TMP_97, $c, TMP_99, $d, TMP_101, $e, TMP_102, $f, TMP_103;
if (command == null) command = nil;
      (($a = [false]), $b = command, $b['$undoable='].apply($b, $a), $a[$a.length-1]);
        ($a = ($b = command).$add_parameter, $a.$$p = (TMP_97 = function(parameter){var self = TMP_97.$$s || this, $a, $b, TMP_98;
if (parameter == null) parameter = nil;
        return ($a = ($b = parameter).$set_help, $a.$$p = (TMP_98 = function(){var self = TMP_98.$$s || this;

          return "parameter key"}, TMP_98.$$s = self, TMP_98), $a).call($b)}, TMP_97.$$s = self, TMP_97), $a).call($b, "key", "string");
        ($a = ($c = command).$add_parameter, $a.$$p = (TMP_99 = function(parameter){var self = TMP_99.$$s || this, $a, $b, TMP_100;
if (parameter == null) parameter = nil;
        return ($a = ($b = parameter).$set_help, $a.$$p = (TMP_100 = function(){var self = TMP_100.$$s || this;

          return "parameter value as JSON"}, TMP_100.$$s = self, TMP_100), $a).call($b)}, TMP_99.$$s = self, TMP_99), $a).call($c, "value", "string");
        ($a = ($d = command).$set_help, $a.$$p = (TMP_101 = function(){var self = TMP_101.$$s || this;

        return "set configuration parameter"}, TMP_101.$$s = self, TMP_101), $a).call($d);
        ($a = ($e = command).$as_action, $a.$$p = (TMP_102 = function(args){var self = TMP_102.$$s || this, value = nil;
          if (self.editor == null) self.editor = nil;
if (args == null) args = nil;
        value = $scope.get('JSON').$parse(args['$[]']("value"));
          self.editor.$patch_config_part(args['$[]']("key"), value);
          return nil;}, TMP_102.$$s = self, TMP_102), $a).call($e);
        return ($a = ($f = command).$as_inverse, $a.$$p = (TMP_103 = function(args){var self = TMP_103.$$s || this;
          if ($gvars.conf == null) $gvars.conf = nil;
if (args == null) args = nil;
        return $gvars.conf.$pop()}, TMP_103.$$s = self, TMP_103), $a).call($f);}, TMP_96.$$s = self, TMP_96), $a).call($g, "cconf");
    });

    Opal.defn(self, '$__ic_04_localstore_commands', function() {
      var $a, $b, TMP_104, $c, TMP_107, $d, TMP_111, self = this;

      ($a = ($b = self.commands).$add_command, $a.$$p = (TMP_104 = function(c){var self = TMP_104.$$s || this, $a, $b, TMP_105, $c, TMP_106;
if (c == null) c = nil;
      (($a = [false]), $b = c, $b['$undoable='].apply($b, $a), $a[$a.length-1]);
        ($a = ($b = c).$set_help, $a.$$p = (TMP_105 = function(){var self = TMP_105.$$s || this;

        return "save to localstore"}, TMP_105.$$s = self, TMP_105), $a).call($b);
        return ($a = ($c = c).$as_action, $a.$$p = (TMP_106 = function(){var self = TMP_106.$$s || this, abc_code = nil, metadata = nil, filename = nil;
          if (self.editor == null) self.editor = nil;
          if (self.abc_transformer == null) self.abc_transformer = nil;
          if (self.songbook == null) self.songbook = nil;
          if ($gvars.log == null) $gvars.log = nil;

        abc_code = self.editor.$get_text();
          metadata = self.abc_transformer.$get_metadata(abc_code);
          filename = "" + (metadata['$[]']("X")) + "_" + (metadata['$[]']("T"));
          self.songbook.$update(metadata['$[]']("X"), abc_code, metadata['$[]']("T"), true);
          self.$set_status($hash2(["music_model"], {"music_model": "saved to localstore"}));
          return $gvars.log.$message("saved to '" + (filename) + "'");}, TMP_106.$$s = self, TMP_106), $a).call($c);}, TMP_104.$$s = self, TMP_104), $a).call($b, "lsave");
      ($a = ($c = self.commands).$add_command, $a.$$p = (TMP_107 = function(c){var self = TMP_107.$$s || this, $a, $b, TMP_108, $c, TMP_109;
if (c == null) c = nil;
      (($a = [false]), $b = c, $b['$undoable='].apply($b, $a), $a[$a.length-1]);
        ($a = ($b = c).$set_help, $a.$$p = (TMP_108 = function(){var self = TMP_108.$$s || this;

        return "list files in localstore"}, TMP_108.$$s = self, TMP_108), $a).call($b);
        return ($a = ($c = c).$as_action, $a.$$p = (TMP_109 = function(a){var self = TMP_109.$$s || this, $a, $b, TMP_110;
          if (self.songbook == null) self.songbook = nil;
          if ($gvars.log == null) $gvars.log = nil;
if (a == null) a = nil;
        return $gvars.log.$message($rb_plus($rb_plus("<pre>", ($a = ($b = self.songbook.$list()).$map, $a.$$p = (TMP_110 = function(k, v){var self = TMP_110.$$s || this;
if (k == null) k = nil;if (v == null) v = nil;
          return "" + (k) + "_" + (v)}, TMP_110.$$s = self, TMP_110), $a).call($b).$join("\n")), "</pre>"))}, TMP_109.$$s = self, TMP_109), $a).call($c);}, TMP_107.$$s = self, TMP_107), $a).call($c, "lls");
      return ($a = ($d = self.commands).$add_command, $a.$$p = (TMP_111 = function(c){var self = TMP_111.$$s || this, $a, $b, TMP_112, $c, TMP_114, $d, TMP_115, $e, TMP_116;
if (c == null) c = nil;
      (($a = [true]), $b = c, $b['$undoable='].apply($b, $a), $a[$a.length-1]);
        ($a = ($b = c).$add_parameter, $a.$$p = (TMP_112 = function(parameter){var self = TMP_112.$$s || this, $a, $b, TMP_113;
if (parameter == null) parameter = nil;
        return ($a = ($b = parameter).$set_help, $a.$$p = (TMP_113 = function(){var self = TMP_113.$$s || this;

          return "id of the song to be loaded"}, TMP_113.$$s = self, TMP_113), $a).call($b)}, TMP_112.$$s = self, TMP_112), $a).call($b, "id", "string");
        ($a = ($c = c).$set_help, $a.$$p = (TMP_114 = function(){var self = TMP_114.$$s || this;

        return "open song from local store  " + (c.$parameter_help(0))}, TMP_114.$$s = self, TMP_114), $a).call($c);
        ($a = ($d = c).$as_action, $a.$$p = (TMP_115 = function(args){var self = TMP_115.$$s || this, $a, payload = nil;
          if (self.songbook == null) self.songbook = nil;
          if (self.editor == null) self.editor = nil;
          if ($gvars.log == null) $gvars.log = nil;
if (args == null) args = nil;
        if ((($a = args['$[]']("id")) !== nil && (!$a.$$is_boolean || $a == true))) {
            payload = self.songbook.$retrieve(args['$[]']("id"));
            if (payload !== false && payload !== nil) {
              args['$[]=']("oldval", self.editor.$get_text());
              return self.editor.$set_text(payload);
              } else {
              return $gvars.log.$error("song " + (self.$command_tokens().$last()) + " not found")
            };
            } else {
            return $gvars.log.$error("plase add a song number")
          }}, TMP_115.$$s = self, TMP_115), $a).call($d);
        return ($a = ($e = c).$as_inverse, $a.$$p = (TMP_116 = function(args){var self = TMP_116.$$s || this;
          if (self.editor == null) self.editor = nil;
if (args == null) args = nil;
        return self.editor.$set_text(args['$[]']("oldval"))}, TMP_116.$$s = self, TMP_116), $a).call($e);}, TMP_111.$$s = self, TMP_111), $a).call($d, "lopen");
    });

    return (Opal.defn(self, '$__ic_05_dropbox_commands', function() {
      var $a, $b, TMP_117, $c, TMP_128, $d, TMP_137, $e, TMP_144, $f, TMP_147, $g, TMP_151, $h, TMP_162, self = this;

      ($a = ($b = self.commands).$add_command, $a.$$p = (TMP_117 = function(command){var self = TMP_117.$$s || this, $a, $b, TMP_118, $c, TMP_121, $d, TMP_124, $e, TMP_125, $f, TMP_127;
if (command == null) command = nil;
      ($a = ($b = command).$add_parameter, $a.$$p = (TMP_118 = function(parameter){var self = TMP_118.$$s || this, $a, $b, TMP_119, $c, TMP_120;
if (parameter == null) parameter = nil;
        ($a = ($b = parameter).$set_default, $a.$$p = (TMP_119 = function(){var self = TMP_119.$$s || this;

          return "app"}, TMP_119.$$s = self, TMP_119), $a).call($b);
          return ($a = ($c = parameter).$set_help, $a.$$p = (TMP_120 = function(){var self = TMP_120.$$s || this;

          return "(app | full) app: app only | full: full dropbox"}, TMP_120.$$s = self, TMP_120), $a).call($c);}, TMP_118.$$s = self, TMP_118), $a).call($b, "scope", "string");
        ($a = ($c = command).$add_parameter, $a.$$p = (TMP_121 = function(parameter){var self = TMP_121.$$s || this, $a, $b, TMP_122, $c, TMP_123;
if (parameter == null) parameter = nil;
        ($a = ($b = parameter).$set_default, $a.$$p = (TMP_122 = function(){var self = TMP_122.$$s || this;

          return "/"}, TMP_122.$$s = self, TMP_122), $a).call($b);
          return ($a = ($c = parameter).$set_help, $a.$$p = (TMP_123 = function(){var self = TMP_123.$$s || this;

          return "path to set in dropbox"}, TMP_123.$$s = self, TMP_123), $a).call($c);}, TMP_121.$$s = self, TMP_121), $a).call($c, "path", "string");
        ($a = ($d = command).$set_help, $a.$$p = (TMP_124 = function(){var self = TMP_124.$$s || this;

        return "dropbox login for " + (command.$parameter_help(0))}, TMP_124.$$s = self, TMP_124), $a).call($d);
        ($a = ($e = command).$as_action, $a.$$p = (TMP_125 = function(args){var self = TMP_125.$$s || this, $a, $b, TMP_126, $case = nil;
          if (self.dropboxclient == null) self.dropboxclient = nil;
          if ($gvars.log == null) $gvars.log = nil;
if (args == null) args = nil;
        $case = args['$[]']("scope");if ("full"['$===']($case)) {self.dropboxclient = (((($scope.get('Opal')).$$scope.get('DropboxJs'))).$$scope.get('Client')).$new("us2s6tq6bubk6xh");
          (($a = ["full Dropbox"]), $b = self.dropboxclient, $b['$app_name='].apply($b, $a), $a[$a.length-1]);
          (($a = ["full"]), $b = self.dropboxclient, $b['$app_id='].apply($b, $a), $a[$a.length-1]);
          self.dropboxpath = args['$[]']("path");}else if ("app"['$===']($case)) {self.dropboxclient = (((($scope.get('Opal')).$$scope.get('DropboxJs'))).$$scope.get('Client')).$new("xr3zna7wrp75zax");
          (($a = ["App folder only"]), $b = self.dropboxclient, $b['$app_name='].apply($b, $a), $a[$a.length-1]);
          (($a = ["app"]), $b = self.dropboxclient, $b['$app_id='].apply($b, $a), $a[$a.length-1]);
          self.dropboxpath = args['$[]']("path");}else {$gvars.log.$error("select app | full")};
          return ($a = ($b = self.dropboxclient.$authenticate()).$then, $a.$$p = (TMP_126 = function(){var self = TMP_126.$$s || this;
            if (self.dropboxclient == null) self.dropboxclient = nil;
            if (self.dropboxpath == null) self.dropboxpath = nil;
            if ($gvars.log == null) $gvars.log = nil;

          self.$set_status($hash2(["dropbox", "dropboxapp", "dropboxpath"], {"dropbox": "" + (self.dropboxclient.$app_name()) + ": " + (self.dropboxpath), "dropboxapp": self.dropboxclient.$app_id(), "dropboxpath": self.dropboxpath}));
            return $gvars.log.$message("logged in at dropbox with " + (args['$[]']("scope")) + " access");}, TMP_126.$$s = self, TMP_126), $a).call($b);}, TMP_125.$$s = self, TMP_125), $a).call($e);
        return ($a = ($f = command).$as_inverse, $a.$$p = (TMP_127 = function(args){var self = TMP_127.$$s || this;
          if ($gvars.log == null) $gvars.log = nil;
if (args == null) args = nil;
        self.$set_status($hash2(["dropbox"], {"dropbox": "logged out"}));
          $gvars.log.$message("logged out from dropbox");
          return self.dropboxclient = nil;}, TMP_127.$$s = self, TMP_127), $a).call($f);}, TMP_117.$$s = self, TMP_117), $a).call($b, "dlogin");
      ($a = ($c = self.commands).$add_command, $a.$$p = (TMP_128 = function(command){var self = TMP_128.$$s || this, $a, $b, TMP_129, $c, TMP_132, $d, TMP_133;
if (command == null) command = nil;
      (($a = [false]), $b = command, $b['$undoable='].apply($b, $a), $a[$a.length-1]);
        ($a = ($b = command).$add_parameter, $a.$$p = (TMP_129 = function(parameter){var self = TMP_129.$$s || this, $a, $b, TMP_130, $c, TMP_131;
if (parameter == null) parameter = nil;
        ($a = ($b = parameter).$set_default, $a.$$p = (TMP_130 = function(){var self = TMP_130.$$s || this, $a;
            if (self.dropboxpath == null) self.dropboxpath = nil;

          return ((($a = self.dropboxpath) !== false && $a !== nil) ? $a : "/")}, TMP_130.$$s = self, TMP_130), $a).call($b);
          return ($a = ($c = parameter).$set_help, $a.$$p = (TMP_131 = function(){var self = TMP_131.$$s || this;
            if (self.dropboxclient == null) self.dropboxclient = nil;

          return "path in dropbox " + (self.dropboxclient.$app_name())}, TMP_131.$$s = self, TMP_131), $a).call($c);}, TMP_129.$$s = self, TMP_129), $a).call($b, "path", "string");
        ($a = ($c = command).$set_help, $a.$$p = (TMP_132 = function(){var self = TMP_132.$$s || this;

        return "list files in " + (command.$parameter_help(0))}, TMP_132.$$s = self, TMP_132), $a).call($c);
        return ($a = ($d = command).$as_action, $a.$$p = (TMP_133 = function(args){var self = TMP_133.$$s || this, $a, $b, TMP_134, $c, $d, TMP_136, rootpath = nil;
          if (self.dropboxclient == null) self.dropboxclient = nil;
          if ($gvars.log == null) $gvars.log = nil;
if (args == null) args = nil;
        rootpath = args['$[]']("path");
          $gvars.log.$message("" + (self.dropboxclient.$app_name()) + ": " + (args['$[]']("path")) + ":");
          return ($a = ($b = ($c = ($d = self.dropboxclient.$authenticate()).$then, $c.$$p = (TMP_136 = function(){var self = TMP_136.$$s || this;
            if (self.dropboxclient == null) self.dropboxclient = nil;

          return self.dropboxclient.$read_dir(rootpath)}, TMP_136.$$s = self, TMP_136), $c).call($d)).$then, $a.$$p = (TMP_134 = function(entries){var self = TMP_134.$$s || this, $a, $b, TMP_135;
            if ($gvars.log == null) $gvars.log = nil;
if (entries == null) entries = nil;
          return $gvars.log.$message($rb_plus($rb_plus("<pre>", ($a = ($b = entries).$select, $a.$$p = (TMP_135 = function(entry){var self = TMP_135.$$s || this;
if (entry == null) entry = nil;
            return entry['$=~'](/\.abc$/)}, TMP_135.$$s = self, TMP_135), $a).call($b).$join("\n").$to_s()), "</pre>"))}, TMP_134.$$s = self, TMP_134), $a).call($b);}, TMP_133.$$s = self, TMP_133), $a).call($d);}, TMP_128.$$s = self, TMP_128), $a).call($c, "dls");
      ($a = ($d = self.commands).$add_command, $a.$$p = (TMP_137 = function(command){var self = TMP_137.$$s || this, $a, $b, TMP_138, $c, TMP_141, $d, TMP_142, $e, TMP_143;
if (command == null) command = nil;
      ($a = ($b = command).$add_parameter, $a.$$p = (TMP_138 = function(parameter){var self = TMP_138.$$s || this, $a, $b, TMP_139, $c, TMP_140;
if (parameter == null) parameter = nil;
        ($a = ($b = parameter).$set_default, $a.$$p = (TMP_139 = function(){var self = TMP_139.$$s || this;
            if (self.dropboxpath == null) self.dropboxpath = nil;

          return self.dropboxpath}, TMP_139.$$s = self, TMP_139), $a).call($b);
          return ($a = ($c = parameter).$set_help, $a.$$p = (TMP_140 = function(){var self = TMP_140.$$s || this;
            if (self.dropboxclient == null) self.dropboxclient = nil;

          return "path in dropbox " + (self.dropboxclient.$app_name())}, TMP_140.$$s = self, TMP_140), $a).call($c);}, TMP_138.$$s = self, TMP_138), $a).call($b, "path", "string");
        ($a = ($c = command).$set_help, $a.$$p = (TMP_141 = function(){var self = TMP_141.$$s || this;

        return "dropbox change dir to " + (command.$parameter_help(0))}, TMP_141.$$s = self, TMP_141), $a).call($c);
        ($a = ($d = command).$as_action, $a.$$p = (TMP_142 = function(args){var self = TMP_142.$$s || this, rootpath = nil;
          if (self.dropboxpath == null) self.dropboxpath = nil;
          if (self.dropboxclient == null) self.dropboxclient = nil;
          if ($gvars.log == null) $gvars.log = nil;
if (args == null) args = nil;
        rootpath = args['$[]']("path");
          args['$[]=']("oldval", self.dropboxpath);
          self.dropboxpath = rootpath;
          self.$set_status($hash2(["dropbox"], {"dropbox": "" + (self.dropboxclient.$app_name()) + ": " + (self.dropboxpath)}));
          return $gvars.log.$message("dropbox path changed to " + (self.dropboxpath));}, TMP_142.$$s = self, TMP_142), $a).call($d);
        return ($a = ($e = command).$as_inverse, $a.$$p = (TMP_143 = function(args){var self = TMP_143.$$s || this;
          if (self.dropboxclient == null) self.dropboxclient = nil;
          if (self.dropboxpath == null) self.dropboxpath = nil;
          if ($gvars.log == null) $gvars.log = nil;
if (args == null) args = nil;
        self.dropboxpath = args['$[]']("oldval");
          self.$set_status($hash2(["dropbox"], {"dropbox": "" + (self.dropboxclient.$app_name()) + ": " + (self.dropboxpath)}));
          return $gvars.log.$message("dropbox path changed back to " + (self.dropboxpath));}, TMP_143.$$s = self, TMP_143), $a).call($e);}, TMP_137.$$s = self, TMP_137), $a).call($d, "dcd");
      ($a = ($e = self.commands).$add_command, $a.$$p = (TMP_144 = function(command){var self = TMP_144.$$s || this, $a, $b, TMP_145, $c, TMP_146;
if (command == null) command = nil;
      (($a = [false]), $b = command, $b['$undoable='].apply($b, $a), $a[$a.length-1]);
        ($a = ($b = command).$set_help, $a.$$p = (TMP_145 = function(){var self = TMP_145.$$s || this;

        return "show drobox path"}, TMP_145.$$s = self, TMP_145), $a).call($b);
        return ($a = ($c = command).$as_action, $a.$$p = (TMP_146 = function(args){var self = TMP_146.$$s || this;
          if (self.dropboxclient == null) self.dropboxclient = nil;
          if (self.dropboxpath == null) self.dropboxpath = nil;
          if ($gvars.log == null) $gvars.log = nil;
if (args == null) args = nil;
        return $gvars.log.$message("" + (self.dropboxclient.$app_name()) + ": " + (self.dropboxpath))}, TMP_146.$$s = self, TMP_146), $a).call($c);}, TMP_144.$$s = self, TMP_144), $a).call($e, "dpwd");
      ($a = ($f = self.commands).$add_command, $a.$$p = (TMP_147 = function(command){var self = TMP_147.$$s || this, $a, $b, TMP_148, $c, TMP_149;
if (command == null) command = nil;
      (($a = [false]), $b = command, $b['$undoable='].apply($b, $a), $a[$a.length-1]);
        ($a = ($b = command).$set_help, $a.$$p = (TMP_148 = function(){var self = TMP_148.$$s || this;

        return "choose File from Dropbox"}, TMP_148.$$s = self, TMP_148), $a).call($b);
        return ($a = ($c = command).$as_action, $a.$$p = (TMP_149 = function(args){var self = TMP_149.$$s || this, $a, $b, TMP_150;
          if (self.dropboxclient == null) self.dropboxclient = nil;
if (args == null) args = nil;
        return ($a = ($b = self.dropboxclient.$choose_file($hash2([], {}))).$then, $a.$$p = (TMP_150 = function(files){var self = TMP_150.$$s || this, chosenfile = nil, fileparts = nil, path = nil, filename = nil;
            if ($gvars.log == null) $gvars.log = nil;
if (files == null) files = nil;
          chosenfile = files.$first()['$[]']("link");
            fileparts = chosenfile.$match(/.*\/view\/[^\/]*\/(.*)\/(.*)/).$to_a();
            path = fileparts['$[]'](1);
            filename = fileparts['$[]'](2);
            self.$handle_command("dlogin full /" + (path) + "/");
            $gvars.log.$message("found " + (path) + " / " + (filename));
            self.$handle_command("dopen " + (filename.$split("_").$first()));
            return $gvars.log.$message("opened " + (path) + " / " + (filename));}, TMP_150.$$s = self, TMP_150), $a).call($b)}, TMP_149.$$s = self, TMP_149), $a).call($c);}, TMP_147.$$s = self, TMP_147), $a).call($f, "dchoose");
      ($a = ($g = self.commands).$add_command, $a.$$p = (TMP_151 = function(command){var self = TMP_151.$$s || this, $a, $b, TMP_152, $c, TMP_155, $d, TMP_156;
if (command == null) command = nil;
      ($a = ($b = command).$add_parameter, $a.$$p = (TMP_152 = function(parameter){var self = TMP_152.$$s || this, $a, $b, TMP_153, $c, TMP_154;
if (parameter == null) parameter = nil;
        ($a = ($b = parameter).$set_default, $a.$$p = (TMP_153 = function(){var self = TMP_153.$$s || this;
            if (self.dropboxpath == null) self.dropboxpath = nil;

          return self.dropboxpath}, TMP_153.$$s = self, TMP_153), $a).call($b);
          return ($a = ($c = parameter).$set_help, $a.$$p = (TMP_154 = function(){var self = TMP_154.$$s || this;
            if (self.dropboxclient == null) self.dropboxclient = nil;

          return "path to save in " + (self.dropboxclient.$app_name())}, TMP_154.$$s = self, TMP_154), $a).call($c);}, TMP_152.$$s = self, TMP_152), $a).call($b, "path", "string");
        (($a = [false]), $c = command, $c['$undoable='].apply($c, $a), $a[$a.length-1]);
        ($a = ($c = command).$set_help, $a.$$p = (TMP_155 = function(){var self = TMP_155.$$s || this;

        return "save to dropbox {" + (command.$parameter_help(0)) + "}"}, TMP_155.$$s = self, TMP_155), $a).call($c);
        return ($a = ($d = command).$as_action, $a.$$p = (TMP_156 = function(args){var self = TMP_156.$$s || this, $a, $b, TMP_157, $c, TMP_160, $d, $e, TMP_161, $f, abc_code = nil, metadata = nil, filebase = nil, print_variants = nil, rootpath = nil, save_promises = nil;
          if (self.editor == null) self.editor = nil;
          if (self.abc_transformer == null) self.abc_transformer = nil;
          if (self.music_model == null) self.music_model = nil;
          if (self.dropboxclient == null) self.dropboxclient = nil;
          if ($gvars.log == null) $gvars.log = nil;
if (args == null) args = nil;
        abc_code = self.editor.$get_text();
          metadata = self.abc_transformer.$get_metadata(abc_code);
          filebase = metadata['$[]']("F").$first();
          $gvars.log.$debug("" + (metadata.$to_s()) + " (" + ("controller_command_definitions") + " " + (538) + ")");
          if (filebase !== false && filebase !== nil) {
            filebase = filebase.$split("\n").$first()
            } else {
            self.$raise("Filename not specified in song add an F: instruction")
          };
          self.$layout_harpnotes();
          self.$render_previews();
          print_variants = self.music_model.$harpnote_options()['$[]']("print");
          rootpath = args['$[]']("path");
          save_promises = [];
          ($a = ($b = self.dropboxclient.$authenticate()).$then, $a.$$p = (TMP_157 = function(){var self = TMP_157.$$s || this, $a, $b, TMP_158, $c, TMP_159, pdfs = nil;
            if (self.dropboxclient == null) self.dropboxclient = nil;
            if (self.editor == null) self.editor = nil;

          save_promises = [self.dropboxclient.$write_file("" + (rootpath) + (filebase) + ".abc", self.editor.$get_text())];
            pdfs = $hash2([], {});
            ($a = ($b = print_variants).$map, $a.$$p = (TMP_158 = function(print_variant){var self = TMP_158.$$s || this, index = nil, filename = nil;
if (print_variant == null) print_variant = nil;
            index = print_variant['$[]']("view_id");
              filename = print_variant['$[]']("title").$gsub(/[^a-zA-Z0-9\-\_]/, "_");
              pdfs['$[]=']("" + (rootpath) + (filebase) + "_" + (print_variant['$[]']("title")) + "_a3.pdf", self.$render_a3(index).$output("blob"));
              pdfs['$[]=']("" + (rootpath) + (filebase) + "_" + (print_variant['$[]']("title")) + "_a4.pdf", self.$render_a4(index).$output("blob"));
              return nil;}, TMP_158.$$s = self, TMP_158), $a).call($b);
            return ($a = ($c = pdfs).$each, $a.$$p = (TMP_159 = function(name, pdfdata){var self = TMP_159.$$s || this;
              if (self.dropboxclient == null) self.dropboxclient = nil;
if (name == null) name = nil;if (pdfdata == null) pdfdata = nil;
            return save_promises.$push(self.dropboxclient.$write_file(name, pdfdata))}, TMP_159.$$s = self, TMP_159), $a).call($c);}, TMP_157.$$s = self, TMP_157), $a).call($b);
          return ($a = ($c = ($d = ($e = ($f = $scope.get('Promise')).$when.apply($f, Opal.to_a(save_promises))).$then, $d.$$p = (TMP_161 = function(){var self = TMP_161.$$s || this;
            if ($gvars.log == null) $gvars.log = nil;

          self.$set_status($hash2(["music_model"], {"music_model": "saved to dropbox"}));
            return $gvars.log.$message("all files saved");}, TMP_161.$$s = self, TMP_161), $d).call($e)).$fail, $a.$$p = (TMP_160 = function(err){var self = TMP_160.$$s || this;
            if ($gvars.log == null) $gvars.log = nil;
if (err == null) err = nil;
          return $gvars.log.$error("there was an error saving files " + (err))}, TMP_160.$$s = self, TMP_160), $a).call($c);}, TMP_156.$$s = self, TMP_156), $a).call($d);}, TMP_151.$$s = self, TMP_151), $a).call($g, "dsave");
      return ($a = ($h = self.commands).$add_command, $a.$$p = (TMP_162 = function(command){var self = TMP_162.$$s || this, $a, $b, TMP_163, $c, TMP_166, $d, TMP_167, $e, TMP_173;
if (command == null) command = nil;
      command.$add_parameter("fileid", "string", "file id");
        ($a = ($b = command).$add_parameter, $a.$$p = (TMP_163 = function(p){var self = TMP_163.$$s || this, $a, $b, TMP_164, $c, TMP_165;
if (p == null) p = nil;
        ($a = ($b = p).$set_default, $a.$$p = (TMP_164 = function(){var self = TMP_164.$$s || this;
            if (self.dropboxpath == null) self.dropboxpath = nil;

          return self.dropboxpath}, TMP_164.$$s = self, TMP_164), $a).call($b);
          return ($a = ($c = p).$set_help, $a.$$p = (TMP_165 = function(){var self = TMP_165.$$s || this;
            if (self.dropboxclient == null) self.dropboxclient = nil;

          return "path to save in " + (self.dropboxclient.$app_name())}, TMP_165.$$s = self, TMP_165), $a).call($c);}, TMP_163.$$s = self, TMP_163), $a).call($b, "path", "string");
        ($a = ($c = command).$set_help, $a.$$p = (TMP_166 = function(){var self = TMP_166.$$s || this;

        return "read file with " + (command.$parameter_help(0)) + ", from dropbox " + (command.$parameter_help(1))}, TMP_166.$$s = self, TMP_166), $a).call($c);
        ($a = ($d = command).$as_action, $a.$$p = (TMP_167 = function(args){var self = TMP_167.$$s || this, $a, $b, TMP_168, $c, $d, TMP_169, $e, $f, TMP_170, $g, $h, TMP_172, fileid = nil, rootpath = nil;
          if (self.editor == null) self.editor = nil;
          if (self.dropboxclient == null) self.dropboxclient = nil;
          if ($gvars.log == null) $gvars.log = nil;
if (args == null) args = nil;
        args['$[]=']("oldval", self.editor.$get_text());
          fileid = args['$[]']("fileid");
          rootpath = args['$[]']("path");
          $gvars.log.$message("get from Dropbox path " + (rootpath) + (fileid) + "_ ...:");
          return ($a = ($b = ($c = ($d = ($e = ($f = ($g = ($h = self.dropboxclient.$authenticate()).$then, $g.$$p = (TMP_172 = function(error, data){var self = TMP_172.$$s || this;
            if (self.dropboxclient == null) self.dropboxclient = nil;
if (error == null) error = nil;if (data == null) data = nil;
          return self.dropboxclient.$read_dir(rootpath)}, TMP_172.$$s = self, TMP_172), $g).call($h)).$then, $e.$$p = (TMP_170 = function(entries){var self = TMP_170.$$s || this, $a, $b, TMP_171;
            if (self.dropboxclient == null) self.dropboxclient = nil;
            if ($gvars.log == null) $gvars.log = nil;
if (entries == null) entries = nil;
          $gvars.log.$debug("" + (entries) + " (" + ("controller_command_definitions") + " " + (597) + ")");
            fileid = ($a = ($b = entries).$select, $a.$$p = (TMP_171 = function(entry){var self = TMP_171.$$s || this;
if (entry == null) entry = nil;
            return entry['$=~']((new RegExp("^" + fileid + "_.*\\.abc$")))}, TMP_171.$$s = self, TMP_171), $a).call($b).$first();
            return self.dropboxclient.$read_file("" + (rootpath) + (fileid));}, TMP_170.$$s = self, TMP_170), $e).call($f)).$then, $c.$$p = (TMP_169 = function(abc_text){var self = TMP_169.$$s || this, filebase = nil;
            if (self.abc_transformer == null) self.abc_transformer = nil;
            if (self.editor == null) self.editor = nil;
            if ($gvars.log == null) $gvars.log = nil;
if (abc_text == null) abc_text = nil;
          $gvars.log.$debug("loaded " + (fileid) + " (" + ("controller_command_definitions") + " " + (601) + ")");
            filebase = fileid.$split(".abc")['$[]']($range(0, -1, false)).$join(".abc");
            abc_text = self.abc_transformer.$add_metadata(abc_text, $hash2(["F"], {"F": filebase}));
            self.editor.$set_text(abc_text);
            self.$set_status($hash2(["music_model"], {"music_model": "loaded"}));
            return self.$handle_command("render");}, TMP_169.$$s = self, TMP_169), $c).call($d)).$fail, $a.$$p = (TMP_168 = function(err){var self = TMP_168.$$s || this;
            if ($gvars.log == null) $gvars.log = nil;
if (err == null) err = nil;
          return $gvars.log.$error("could not load file " + (err))}, TMP_168.$$s = self, TMP_168), $a).call($b);}, TMP_167.$$s = self, TMP_167), $a).call($d);
        return ($a = ($e = command).$as_inverse, $a.$$p = (TMP_173 = function(args){var self = TMP_173.$$s || this;
          if (self.editor == null) self.editor = nil;
if (args == null) args = nil;
        return self.editor.$set_text(args['$[]']("oldval"))}, TMP_173.$$s = self, TMP_173), $a).call($e);}, TMP_162.$$s = self, TMP_162), $a).call($h, "dopen");
    }), nil) && '__ic_05_dropbox_commands';
  })($scope.base, null)
};

/* Generated by Opal 0.9.0 */
Opal.modules["harpnote_player"] = function(Opal) {
  Opal.dynamic_require_severity = "error";
  var OPAL_CONFIG = { method_missing: true, arity_check: false, freezing: true, tainting: true };
  function $rb_ge(lhs, rhs) {
    return (typeof(lhs) === 'number' && typeof(rhs) === 'number') ? lhs >= rhs : lhs['$>='](rhs);
  }
  function $rb_times(lhs, rhs) {
    return (typeof(lhs) === 'number' && typeof(rhs) === 'number') ? lhs * rhs : lhs['$*'](rhs);
  }
  function $rb_plus(lhs, rhs) {
    return (typeof(lhs) === 'number' && typeof(rhs) === 'number') ? lhs + rhs : lhs['$+'](rhs);
  }
  function $rb_minus(lhs, rhs) {
    return (typeof(lhs) === 'number' && typeof(rhs) === 'number') ? lhs - rhs : lhs['$-'](rhs);
  }
  function $rb_gt(lhs, rhs) {
    return (typeof(lhs) === 'number' && typeof(rhs) === 'number') ? lhs > rhs : lhs['$>'](rhs);
  }
  function $rb_lt(lhs, rhs) {
    return (typeof(lhs) === 'number' && typeof(rhs) === 'number') ? lhs < rhs : lhs['$<'](rhs);
  }
  function $rb_divide(lhs, rhs) {
    return (typeof(lhs) === 'number' && typeof(rhs) === 'number') ? lhs / rhs : lhs['$/'](rhs);
  }
  var self = Opal.top, $scope = Opal, nil = Opal.nil, $breaker = Opal.breaker, $slice = Opal.slice, $module = Opal.module, $klass = Opal.klass, $gvars = Opal.gvars, $hash2 = Opal.hash2;

  Opal.add_stubs(['$[]', '$debug', '$[]=', '$on', '$Native', '$call', '$to_s', '$first', '$select', '$>=', '$error', '$play_notes', '$stop', '$empty?', '$warning', '$last', '$*', '$+', '$-', '$get', '$each', '$clone', '$to_n', '$create_inst', '$each_with_index', '$info', '$backtrace', '$nil?', '$class', '$>', '$<', '$===', '$push', '$sort', '$<=>', '$reduce', '$meta_data', '$/', '$compact', '$flatten', '$map', '$is_a?', '$mk_to_play', '$pitch', '$notes', '$tie_end?', '$==', '$tie_start?', '$voices', '$beat', '$-@', '$duration', '$origin']);
  return (function($base) {
    var $Harpnotes, self = $Harpnotes = $module($base, 'Harpnotes');

    var def = self.$$proto, $scope = self.$$scope;

    (function($base) {
      var $Music, self = $Music = $module($base, 'Music');

      var def = self.$$proto, $scope = self.$$scope;

      (function($base, $super) {
        function $HarpnotePlayer(){};
        var self = $HarpnotePlayer = $klass($base, $super, 'HarpnotePlayer', $HarpnotePlayer);

        var def = self.$$proto, $scope = self.$$scope, TMP_3, TMP_4, TMP_5;

        def.isplaying = def.inst = def.selection = def.voice_elements = def.song_off_timer = def.duration_timefactor = def.songoff_callback = def.beat_timefactor = nil;
        Opal.defn(self, '$initialize', function() {
          var self = this;

          self.inst = [];
          self.isplaying = false;
          return self.selection = [];
        });

        Opal.defn(self, '$is_playing?', function() {
          var self = this;

          return self.isplaying;
        });

        Opal.defn(self, '$create_inst', function(instrument_id) {
          var $a, $b, TMP_1, $c, TMP_2, self = this;
          if ($gvars.log == null) $gvars.log = nil;

          if ((($a = self.inst['$[]'](instrument_id)) !== nil && (!$a.$$is_boolean || $a == true))) {
            } else {
            $gvars.log.$debug("creating instrument " + (instrument_id) + " ");
            self.inst['$[]='](instrument_id, new Instrument("piano"));
            ($a = ($b = self.$Native(self.inst['$[]'](instrument_id))).$on, $a.$$p = (TMP_1 = function(element){var self = TMP_1.$$s || this, abc_element = nil;
              if (self.noteon_callback == null) self.noteon_callback = nil;
if (element == null) element = nil;
            abc_element = self.$Native(element)['$[]']("origin");
              self.noteon_callback.$call(abc_element);
              return nil;}, TMP_1.$$s = self, TMP_1), $a).call($b, "noteon");
            ($a = ($c = self.$Native(self.inst['$[]'](instrument_id))).$on, $a.$$p = (TMP_2 = function(element){var self = TMP_2.$$s || this, abc_element = nil;
              if (self.noteoff_callback == null) self.noteoff_callback = nil;
if (element == null) element = nil;
            abc_element = self.$Native(element)['$[]']("origin");
              self.noteoff_callback.$call(abc_element);
              return nil;}, TMP_2.$$s = self, TMP_2), $a).call($c, "noteoff");
          };
          return self.inst['$[]'](instrument_id);
        });

        Opal.defn(self, '$on_noteon', TMP_3 = function() {
          var self = this, $iter = TMP_3.$$p, block = $iter || nil;

          TMP_3.$$p = null;
          return self.noteon_callback = block;
        });

        Opal.defn(self, '$on_noteoff', TMP_4 = function() {
          var self = this, $iter = TMP_4.$$p, block = $iter || nil;

          TMP_4.$$p = null;
          return self.noteoff_callback = block;
        });

        Opal.defn(self, '$on_songoff', TMP_5 = function() {
          var self = this, $iter = TMP_5.$$p, block = $iter || nil;

          TMP_5.$$p = null;
          return self.songoff_callback = block;
        });

        Opal.defn(self, '$play_from_selection', function() {
          var $a, $b, TMP_6, self = this, notes_to_play = nil;
          if ($gvars.log == null) $gvars.log = nil;

          $gvars.log.$debug("" + (self.selection.$to_s()) + " (" + ("harpnote_player") + " " + (55) + ")");
          if ((($a = self.selection.$first()) !== nil && (!$a.$$is_boolean || $a == true))) {
            notes_to_play = ($a = ($b = self.voice_elements).$select, $a.$$p = (TMP_6 = function(n){var self = TMP_6.$$s || this;
              if (self.selection == null) self.selection = nil;
if (n == null) n = nil;
            return $rb_ge(n['$[]']("delay"), self.selection.$first()['$[]']("delay"))}, TMP_6.$$s = self, TMP_6), $a).call($b)
            } else {
            $gvars.log.$error("please select at least one note");
            notes_to_play = self.voice_elements;
          };
          return self.$play_notes(notes_to_play);
        });

        Opal.defn(self, '$play_selection', function() {
          var self = this;

          return self.$play_notes(self.selection);
        });

        Opal.defn(self, '$play_song', function() {
          var self = this;

          return self.$play_notes(self.voice_elements);
        });

        Opal.defn(self, '$play_notes', function(the_notes) {
          var $a, $b, TMP_7, self = this, firstnote = nil, lastnote = nil, stop_time = nil, idx = nil;
          if ($gvars.log == null) $gvars.log = nil;
          if ($gvars.conf == null) $gvars.conf = nil;

          self.$stop();
          if ((($a = the_notes['$empty?']()) !== nil && (!$a.$$is_boolean || $a == true))) {
            return $gvars.log.$warning("nothing selected to play")
            } else {
            if ((($a = self.song_off_timer) !== nil && (!$a.$$is_boolean || $a == true))) {
              clearTimeout(self.song_off_timer);};
            firstnote = the_notes.$first();
            lastnote = the_notes.$last();
            stop_time = $rb_times(($rb_plus($rb_minus(lastnote['$[]']("delay"), firstnote['$[]']("delay")), $rb_times($gvars.conf.$get("layout.SHORTEST_NOTE"), self.duration_timefactor))), 1000);
            self.song_off_timer = setTimeout(function(){self.songoff_callback.$call()}, stop_time );
            idx = 0;
            ($a = ($b = the_notes).$each, $a.$$p = (TMP_7 = function(the_note){var self = TMP_7.$$s || this, $a, $b, the_note_to_play = nil, note = nil, index = nil, inst = nil;
if (the_note == null) the_note = nil;
            the_note_to_play = the_note.$clone();
              ($a = "delay", $b = the_note_to_play, $b['$[]=']($a, $rb_minus($b['$[]']($a), firstnote['$[]']("delay"))));
              note = the_note_to_play.$to_n();
              index = the_note_to_play['$[]']("index");
              inst = self.$create_inst(index);
              
            inst.tone(note);
            inst.schedule(note.delay + note.duration, function(){inst._trigger("noteoff", note);});
           ;}, TMP_7.$$s = self, TMP_7), $a).call($b);
            return self.isplaying = true;
          };
        });

        Opal.defn(self, '$stop', function() {
          var $a, $b, TMP_8, self = this;

          ($a = ($b = self.inst).$each_with_index, $a.$$p = (TMP_8 = function(inst, index){var self = TMP_8.$$s || this, e = nil;
            if ($gvars.log == null) $gvars.log = nil;
if (inst == null) inst = nil;if (index == null) index = nil;
          try {
            return inst.silence();
            } catch ($err) {if (Opal.rescue($err, [$scope.get('Exception')])) {e = $err;
              try {
                return $gvars.log.$info(e.$backtrace())
              } finally {
                Opal.gvars["!"] = Opal.exceptions.pop() || Opal.nil;
              }
              }else { throw $err; }
            }}, TMP_8.$$s = self, TMP_8), $a).call($b);
          return self.isplaying = false;
        });

        Opal.defn(self, '$unhighlight_all', function() {
          var self = this;

          return self.selection = [];
        });

        Opal.defn(self, '$range_highlight', function(from, to) {
          var $a, $b, TMP_9, $c, $d, TMP_10, self = this;

          self.selection = [];
          return ($a = ($b = ($c = ($d = self.voice_elements).$sort, $c.$$p = (TMP_10 = function(a, b){var self = TMP_10.$$s || this;
if (a == null) a = nil;if (b == null) b = nil;
          return a['$[]']("delay")['$<=>'](b['$[]']("delay"))}, TMP_10.$$s = self, TMP_10), $c).call($d)).$each, $a.$$p = (TMP_9 = function(element){var self = TMP_9.$$s || this, $a, $b, $c, origin = nil, el_start = nil, el_end = nil;
            if (self.selection == null) self.selection = nil;
            if ($gvars.log == null) $gvars.log = nil;
if (element == null) element = nil;
          origin = self.$Native(element['$[]']("origin"));
            if ((($a = origin['$nil?']()) !== nil && (!$a.$$is_boolean || $a == true))) {
              return $gvars.log.$error("BUG: note without origin " + (element.$class()))
              } else {
              el_start = origin['$[]']("startChar");
              el_end = origin['$[]']("endChar");
              if ((($a = (((($b = ((($c = $rb_gt(to, el_start)) ? $rb_lt(from, el_end) : $rb_gt(to, el_start)))) !== false && $b !== nil) ? $b : (($c = (to['$==='](from)), $c !== false && $c !== nil ?to['$==='](el_end) : $c))))) !== nil && (!$a.$$is_boolean || $a == true))) {
                return self.selection.$push(element)
                } else {
                return nil
              };
            };}, TMP_9.$$s = self, TMP_9), $a).call($b);
        });

        Opal.defn(self, '$load_song', function(music) {
          var $a, $b, TMP_11, self = this, specduration = nil, specbpm = nil, spectf = nil, tf = nil;
          if ($gvars.conf == null) $gvars.conf = nil;
          if ($gvars.log == null) $gvars.log = nil;

          specduration = music.$meta_data()['$[]']("tempo")['$[]']("duration").$reduce("+");
          specbpm = music.$meta_data()['$[]']("tempo")['$[]']("bpm");
          spectf = ($rb_times(specduration, specbpm));
          tf = $rb_times(spectf, ($rb_divide(128, 120)));
          self.duration_timefactor = $rb_divide(1, tf);
          self.beat_timefactor = $rb_divide(1, ($rb_times(tf, $gvars.conf.$get("layout.BEAT_PER_DURATION"))));
          $gvars.log.$debug("playing with tempo: " + (tf) + " ticks per quarter " + ("harpnote_player") + " " + (160));
          return self.voice_elements = ($a = ($b = music.$voices().$each_with_index()).$map, $a.$$p = (TMP_11 = function(voice, index){var self = TMP_11.$$s || this, $a, $b, TMP_12, $c, $d, TMP_15, tie_start = nil;
if (voice == null) voice = nil;if (index == null) index = nil;
          tie_start = $hash2([], {});
            return ($a = ($b = ($c = ($d = voice).$select, $c.$$p = (TMP_15 = function(c){var self = TMP_15.$$s || this;
if (c == null) c = nil;
            return c['$is_a?']($scope.get('Playable'))}, TMP_15.$$s = self, TMP_15), $c).call($d)).$map, $a.$$p = (TMP_12 = function(root){var self = TMP_12.$$s || this, $a, $b, TMP_13, $c, TMP_14, velocity = nil, to_play = nil, more_to_play = nil;
              if ($gvars.log == null) $gvars.log = nil;
if (root == null) root = nil;
            velocity = 0.5;
              if ((($a = root['$is_a?']($scope.get('Pause'))) !== nil && (!$a.$$is_boolean || $a == true))) {
                velocity = 1.1e-05};
              to_play = self.$mk_to_play(root, velocity, index);
              more_to_play = [];
              if ((($a = root['$is_a?']($scope.get('SynchPoint'))) !== nil && (!$a.$$is_boolean || $a == true))) {
                more_to_play = ($a = ($b = root.$notes().$each()).$map, $a.$$p = (TMP_13 = function(note){var self = TMP_13.$$s || this, $a;
if (note == null) note = nil;
                if ((($a = note.$pitch()['$==='](root.$pitch())) !== nil && (!$a.$$is_boolean || $a == true))) {
                    return nil
                    } else {
                    return self.$mk_to_play(note, velocity, index)
                  }}, TMP_13.$$s = self, TMP_13), $a).call($b).$compact()};
              if ((($a = root['$tie_end?']()) !== nil && (!$a.$$is_boolean || $a == true))) {
                if (tie_start['$[]']("pitch")['$=='](to_play['$[]']("pitch"))) {
                  ($a = "duration", $c = to_play, $c['$[]=']($a, $rb_plus($c['$[]']($a), tie_start['$[]']("duration"))));
                  to_play['$[]=']("delay", tie_start['$[]']("delay"));
                  $gvars.log.$debug("" + (more_to_play) + " " + ("harpnote_player") + " " + (184));
                  ($a = ($c = more_to_play).$each, $a.$$p = (TMP_14 = function(p){var self = TMP_14.$$s || this, $a, $b;
if (p == null) p = nil;
                  ($a = "duration", $b = p, $b['$[]=']($a, $rb_plus($b['$[]']($a), tie_start['$[]']("duration"))));
                    return p['$[]=']("delay", tie_start['$[]']("delay"));}, TMP_14.$$s = self, TMP_14), $a).call($c);}};
              if ((($a = root['$tie_start?']()) !== nil && (!$a.$$is_boolean || $a == true))) {
                tie_start = to_play};
              return $rb_plus([to_play], [more_to_play]);}, TMP_12.$$s = self, TMP_12), $a).call($b);}, TMP_11.$$s = self, TMP_11), $a).call($b).$flatten().$compact();
        });

        return (Opal.defn(self, '$mk_to_play', function(note, velocity, index) {
          var self = this;

          return $hash2(["delay", "pitch", "duration", "velocity", "origin", "index"], {"delay": $rb_times(note.$beat(), self.beat_timefactor), "pitch": note.$pitch()['$-@'](), "duration": $rb_times(note.$duration(), self.duration_timefactor), "velocity": velocity, "origin": note.$origin(), "index": index});
        }), nil) && 'mk_to_play';
      })($scope.base, null)
    })($scope.base)
  })($scope.base)
};

/* Generated by Opal 0.9.0 */
Opal.modules["text_pane"] = function(Opal) {
  Opal.dynamic_require_severity = "error";
  var OPAL_CONFIG = { method_missing: true, arity_check: false, freezing: true, tainting: true };
  function $rb_minus(lhs, rhs) {
    return (typeof(lhs) === 'number' && typeof(rhs) === 'number') ? lhs - rhs : lhs['$-'](rhs);
  }
  function $rb_plus(lhs, rhs) {
    return (typeof(lhs) === 'number' && typeof(rhs) === 'number') ? lhs + rhs : lhs['$+'](rhs);
  }
  var self = Opal.top, $scope = Opal, nil = Opal.nil, $breaker = Opal.breaker, $slice = Opal.slice, $module = Opal.module, $klass = Opal.klass, $gvars = Opal.gvars, $hash2 = Opal.hash2;

  Opal.add_stubs(['$attr_accessor', '$on', '$clear_markers', '$call', '$Native', '$getSession', '$[]', '$debug', '$map', '$-', '$first', '$set_markers', '$to_n', '$each', '$add_marker', '$last', '$<<', '$clear', '$split', '$get_text', '$get_selection_positions', '$neat_generate', '$append_text', '$get_config_part', '$==', '$strip', '$replace_text', '$+', '$select_range_by_position', '$new', '$parse', '$push', '$[]=', '$set_config_part', '$get', '$get_config_position', '$error', '$set_annotations', '$annotations', '$length', '$get_abc_part', '$count']);
  return (function($base) {
    var $Harpnotes, self = $Harpnotes = $module($base, 'Harpnotes');

    var def = self.$$proto, $scope = self.$$scope;

    (function($base, $super) {
      function $TextPane(){};
      var self = $TextPane = $klass($base, $super, 'TextPane', $TextPane);

      var def = self.$$proto, $scope = self.$$scope, TMP_1, TMP_3, TMP_5;

      def.editor = def.range = def.markers = nil;
      self.$attr_accessor("editor");

      Opal.defn(self, '$initialize', function(div) {
        var self = this;

        
        // see http://stackoverflow.com/questions/13545433/autocompletion-in-ace-editor
        //     http://stackoverflow.com/questions/26991288/ace-editor-autocompletion-remove-local-variables
        var langTools = ace.require("ace/ext/language_tools");
        langTools.setCompleters([langTools.snippetCompleter])

        var editor = ace.edit(div);
        editor.$blockScrolling = Infinity;

        editor.setTheme("ace/theme/abc");
        editor.getSession().setMode("ace/mode/abc");

        editor.setTheme("ace/theme/abc");

        editor.setOptions({
          highlightActiveLine: true,
          enableBasicAutocompletion: true,
          enableSnippets: true,
          enableLiveAutocompletion: false        });

        // todo: refine autocompletion according to http://plnkr.co/edit/6MVntVmXYUbjR0DI82Cr?p=preview
        //                                          https://github.com/ajaxorg/ace/wiki/How-to-enable-Autocomplete-in-the-Ace-editor

      
        self.editor = editor;
        self.range = ace.require('ace/range').Range;
        self.inhibit_callbacks = false;
        return self.markers = [];
      });

      Opal.defn(self, '$on_change', TMP_1 = function() {
        var $a, $b, TMP_2, self = this, $iter = TMP_1.$$p, block = $iter || nil;

        TMP_1.$$p = null;
        return ($a = ($b = self.$Native(self.$Native(self.editor).$getSession())).$on, $a.$$p = (TMP_2 = function(e){var self = TMP_2.$$s || this, $a;
          if (self.inhibit_callbacks == null) self.inhibit_callbacks = nil;
if (e == null) e = nil;
        self.$clear_markers();
          if ((($a = self.inhibit_callbacks) !== nil && (!$a.$$is_boolean || $a == true))) {
            return nil
            } else {
            return block.$call(e)
          };}, TMP_2.$$s = self, TMP_2), $a).call($b, "change");
      });

      Opal.defn(self, '$on_selection_change', TMP_3 = function() {
        var $a, $b, TMP_4, self = this, $iter = TMP_3.$$p, block = $iter || nil;

        TMP_3.$$p = null;
        return ($a = ($b = self.$Native(self.$Native(self.editor)['$[]']("selection"))).$on, $a.$$p = (TMP_4 = function(e){var self = TMP_4.$$s || this, $a;
          if (self.inhibit_callbacks == null) self.inhibit_callbacks = nil;
if (e == null) e = nil;
        if ((($a = self.inhibit_callbacks) !== nil && (!$a.$$is_boolean || $a == true))) {
            return nil
            } else {
            return block.$call(e)
          }}, TMP_4.$$s = self, TMP_4), $a).call($b, "changeSelection");
      });

      Opal.defn(self, '$on_cursor_change', TMP_5 = function() {
        var $a, $b, TMP_6, self = this, $iter = TMP_5.$$p, block = $iter || nil;

        TMP_5.$$p = null;
        return ($a = ($b = self.$Native(self.$Native(self.editor)['$[]']("selection"))).$on, $a.$$p = (TMP_6 = function(e){var self = TMP_6.$$s || this, $a;
          if (self.inhibit_callbacks == null) self.inhibit_callbacks = nil;
if (e == null) e = nil;
        if ((($a = self.inhibit_callbacks) !== nil && (!$a.$$is_boolean || $a == true))) {
            return nil
            } else {
            return block.$call(e)
          }}, TMP_6.$$s = self, TMP_6), $a).call($b, "changeCursor");
      });

      Opal.defn(self, '$clear_selection', function() {
        var self = this;

        
      self.editor.selection.clearSelection()
      ;
      });

      Opal.defn(self, '$get_selection_positions', function() {
        var self = this;

        
        doc = self.editor.selection.doc;
        range = self.editor.selection.getRange();
        range_start = doc.positionToIndex(range.start, 0);
        range_end = doc.positionToIndex(range.end, 0);
      
        return [range_start, range_end];
      });

      Opal.defn(self, '$select_range_by_position', function(selection_start, selection_end) {
        var self = this;
        if ($gvars.log == null) $gvars.log = nil;

        $gvars.log.$debug("set editor selection to " + (selection_start) + ", " + (selection_end) + " (" + ("text_pane") + " " + (110) + ") ");
        
        doc = self.editor.selection.doc
        startrange = doc.indexToPosition(selection_start);
        endrange = doc.indexToPosition(selection_end);
        range = new Range(startrange.row, startrange.column, endrange.row, endrange.column);
        myrange = {start:startrange, end:endrange}
        self.editor.focus();
        self.editor.selection.setSelectionRange(myrange, false);
      
      });

      Opal.defn(self, '$get_text', function() {
        var self = this;

        return self.editor.getSession().getValue();
      });

      Opal.defn(self, '$set_text', function(text) {
        var self = this;

        
         self.editor.getSession().setValue(text);
      
      });

      Opal.defn(self, '$replace_text', function(oldtext, newtext) {
        var self = this;

        return self.editor.replace(newtext, {needle: oldtext}) ;
      });

      Opal.defn(self, '$set_annotations', function(annotations) {
        var $a, $b, TMP_7, self = this, editor_annotations = nil;

        editor_annotations = ($a = ($b = annotations).$map, $a.$$p = (TMP_7 = function(annotation){var self = TMP_7.$$s || this;
if (annotation == null) annotation = nil;
        return $hash2(["row", "text", "type"], {"row": $rb_minus(annotation['$[]']("start_pos").$first(), 1), "text": annotation['$[]']("text"), "type": annotation['$[]']("type")})}, TMP_7.$$s = self, TMP_7), $a).call($b);
        self.$set_markers(annotations);
        return self.editor.getSession().setAnnotations(editor_annotations.$to_n());
      });

      Opal.defn(self, '$set_markers', function(annotations) {
        var $a, $b, TMP_8, self = this;

        self.$clear_markers();
        return ($a = ($b = annotations).$each, $a.$$p = (TMP_8 = function(annotation){var self = TMP_8.$$s || this;
if (annotation == null) annotation = nil;
        return self.$add_marker(annotation)}, TMP_8.$$s = self, TMP_8), $a).call($b);
      });

      Opal.defn(self, '$append_text', function(text) {
        var self = this;

        
      self.editor.selection.moveCursorFileEnd();
      self.editor.insert(text);
      ;
      });

      Opal.defn(self, '$add_marker', function(annotation) {
        var self = this, marker_start = nil, marker_end = nil, id = nil;

        marker_start = $hash2(["row", "col"], {"row": annotation['$[]']("start_pos").$first(), "col": annotation['$[]']("start_pos").$last()});
        marker_end = $hash2(["row", "col"], {"row": annotation['$[]']("end_pos").$first(), "col": annotation['$[]']("end_pos").$last()});
        id = self.editor.getSession().addMarker(new self.range($rb_minus(marker_start['$[]']("row"), 1), $rb_minus(marker_start['$[]']("col"), 1),
                                                              $rb_minus(marker_end['$[]']("row"), 1), $rb_minus(marker_end['$[]']("col"), 1)),
                                               "marked", "line", true);
        self.markers['$<<']($hash2(["from", "to", "id"], {"from": [marker_start['$[]']("row"), marker_start['$[]']("col")], "to": [marker_end['$[]']("row"), marker_end['$[]']("col")], "id": id}));
        return nil;
      });

      Opal.defn(self, '$clear_markers', function() {
        var $a, $b, TMP_9, self = this;

        ($a = ($b = self.markers).$each, $a.$$p = (TMP_9 = function(marker){var self = TMP_9.$$s || this;
          if (self.editor == null) self.editor = nil;
if (marker == null) marker = nil;
        return self.editor.session.removeMarker(marker['$[]']("id"));}, TMP_9.$$s = self, TMP_9), $a).call($b);
        return self.markers.$clear();
      });

      Opal.cdecl($scope, 'CONFIG_SEPARATOR', "%%%%zupfnoter.config");

      Opal.defn(self, '$get_abc_part', function() {
        var self = this;

        return self.$get_text().$split($scope.get('CONFIG_SEPARATOR')).$first();
      });

      Opal.defn(self, '$get_config_part', function() {
        var $a, self = this;

        return ((($a = self.$get_text().$split($scope.get('CONFIG_SEPARATOR'))['$[]'](1)) !== false && $a !== nil) ? $a : "{}");
      });

      Opal.defn(self, '$resize', function() {
        var self = this;

        return self.editor.resize();
      });

      Opal.defn(self, '$set_config_part', function(object) {
        var $a, self = this, the_selection = nil, options = nil, configjson = nil, oldconfigpart = nil;
        if ($gvars.conf == null) $gvars.conf = nil;

        the_selection = self.$get_selection_positions();
        options = $hash2(["wrap", "aligned", "after_comma", "after_colon_1", "after_colon_n", "before_colon_n", "sort", "explicit_sort"], {"wrap": ((($a = object['$[]']("wrap")) !== false && $a !== nil) ? $a : $gvars.conf['$[]']("wrap")), "aligned": true, "after_comma": 1, "after_colon_1": 1, "after_colon_n": 1, "before_colon_n": 1, "sort": true, "explicit_sort": [["produce", "layout", "annotations", "extract", "title", "voices", "flowlines", "subflowlines", "synchlines", "jumplines", "layoutlines", "legend", "notes", "lyrics", "nonflowrest", "0", "1", "2", "3", "4", "5", "6", "verses", "pos", "text", "style"], []]});
        configjson = $scope.get('JSON').$neat_generate(object, options);
        if ((($a = self.$get_text().$split($scope.get('CONFIG_SEPARATOR'))['$[]'](1)) !== nil && (!$a.$$is_boolean || $a == true))) {
          } else {
          self.$append_text("\n\n" + ($scope.get('CONFIG_SEPARATOR')) + "\n\n{}")
        };
        oldconfigpart = self.$get_config_part();
        self.inhibit_callbacks = true;
        if (oldconfigpart.$strip()['$=='](configjson.$strip())) {
          } else {
          self.$replace_text($rb_plus($scope.get('CONFIG_SEPARATOR'), oldconfigpart), "" + ($scope.get('CONFIG_SEPARATOR')) + "\n\n" + (configjson));
          self.$select_range_by_position(the_selection.$first(), the_selection.$last());
        };
        return self.inhibit_callbacks = false;
      });

      Opal.defn(self, '$patch_config_part', function(key, object) {
        var self = this, pconfig = nil, config_part = nil, config = nil, error = nil, line_col = nil;
        if ($gvars.log == null) $gvars.log = nil;

        pconfig = (($scope.get('Confstack')).$$scope.get('Confstack')).$new(false);
        config_part = self.$get_config_part();
        try {
        config = json_parse(config_part);
          config = $scope.get('JSON').$parse(config_part);
          pconfig.$push(config);
          pconfig['$[]='](key, object);
          return self.$set_config_part(pconfig.$get());
        } catch ($err) {if (Opal.rescue($err, [$scope.get('Object')])) {error = $err;
          try {
            line_col = self.$get_config_position(error.$last());
            $gvars.log.$error("" + (error.$first()) + " at " + (line_col), line_col);
            return self.$set_annotations($gvars.log.$annotations());
          } finally {
            Opal.gvars["!"] = Opal.exceptions.pop() || Opal.nil;
          }
          }else { throw $err; }
        };
      });

      return (Opal.defn(self, '$get_config_position', function(charpos) {
        var self = this, cp = nil, lines = nil, line_no = nil, char_pos = nil;

        cp = $rb_plus(charpos, ($rb_plus(self.$get_abc_part(), $scope.get('CONFIG_SEPARATOR'))).$length());
        lines = self.$get_text()['$[]'](0, cp).$split("\n");
        line_no = lines.$count();
        char_pos = lines.$last().$length();
        return [line_no, char_pos];
      }), nil) && 'get_config_position';
    })($scope.base, null)
  })($scope.base)
};

/* Generated by Opal 0.9.0 */
Opal.modules["opal-dropboxjs"] = function(Opal) {
  Opal.dynamic_require_severity = "error";
  var OPAL_CONFIG = { method_missing: true, arity_check: false, freezing: true, tainting: true };
  function $rb_minus(lhs, rhs) {
    return (typeof(lhs) === 'number' && typeof(rhs) === 'number') ? lhs - rhs : lhs['$-'](rhs);
  }
  function $rb_ge(lhs, rhs) {
    return (typeof(lhs) === 'number' && typeof(rhs) === 'number') ? lhs >= rhs : lhs['$>='](rhs);
  }
  var self = Opal.top, $scope = Opal, nil = Opal.nil, $breaker = Opal.breaker, $slice = Opal.slice, $module = Opal.module, $klass = Opal.klass, $gvars = Opal.gvars;

  Opal.add_stubs(['$require', '$attr_accessor', '$raise', '$lambda', '$error', '$tap', '$call', '$reject', '$resolve', '$new', '$Native', '$-', '$>=', '$info', '$with_promise', '$debug', '$with_promise_retry', '$with_promise_chooser']);
  self.$require("promise");
  return (function($base) {
    var $Opal, self = $Opal = $module($base, 'Opal');

    var def = self.$$proto, $scope = self.$$scope;

    (function($base) {
      var $DropboxJs, self = $DropboxJs = $module($base, 'DropboxJs');

      var def = self.$$proto, $scope = self.$$scope;

      (function($base, $super) {
        function $NilClient(){};
        var self = $NilClient = $klass($base, $super, 'NilClient', $NilClient);

        var def = self.$$proto, $scope = self.$$scope;

        self.$attr_accessor("root_in_dropbox", "app_name");

        return (Opal.defn(self, '$authenticate', function() {
          var self = this;

          return self.$raise("not logged in to dropbox");
        }), nil) && 'authenticate';
      })($scope.base, null);

      (function($base, $super) {
        function $Client(){};
        var self = $Client = $klass($base, $super, 'Client', $Client);

        var def = self.$$proto, $scope = self.$$scope, TMP_2, TMP_5, TMP_8;

        self.$attr_accessor("root_in_dropbox", "app_name", "app_id");

        Opal.defn(self, '$initialize', function(key) {
          var $a, $b, TMP_1, self = this;

          self.errorlogger = ($a = ($b = self).$lambda, $a.$$p = (TMP_1 = function(error){var self = TMP_1.$$s || this;
            if ($gvars.log == null) $gvars.log = nil;
if (error == null) error = nil;
          return $gvars.log.$error(error)}, TMP_1.$$s = self, TMP_1), $a).call($b);
          return self.root = new Dropbox.Client({ key: key });;
        });

        Opal.defn(self, '$with_promise', TMP_2 = function() {
          var $a, $b, TMP_3, self = this, $iter = TMP_2.$$p, block = $iter || nil;

          TMP_2.$$p = null;
          return ($a = ($b = $scope.get('Promise').$new()).$tap, $a.$$p = (TMP_3 = function(promise){var self = TMP_3.$$s || this, $a, $b, TMP_4;
if (promise == null) promise = nil;
          return block.$call(($a = ($b = self).$lambda, $a.$$p = (TMP_4 = function(error, data){var self = TMP_4.$$s || this;
if (error == null) error = nil;if (data == null) data = nil;
            if (error !== false && error !== nil) {
                return promise.$reject(error)
                } else {
                return promise.$resolve(data)
              }}, TMP_4.$$s = self, TMP_4), $a).call($b))}, TMP_3.$$s = self, TMP_3), $a).call($b);
        });

        Opal.defn(self, '$with_promise_chooser', TMP_5 = function() {
          var $a, $b, TMP_6, self = this, $iter = TMP_5.$$p, block = $iter || nil;

          TMP_5.$$p = null;
          return ($a = ($b = $scope.get('Promise').$new()).$tap, $a.$$p = (TMP_6 = function(promise){var self = TMP_6.$$s || this, $a, $b, TMP_7;
if (promise == null) promise = nil;
          return block.$call(($a = ($b = self).$lambda, $a.$$p = (TMP_7 = function(data){var self = TMP_7.$$s || this, $a;
if (data == null) data = nil;
            if ((($a = false) !== nil && (!$a.$$is_boolean || $a == true))) {
                return promise.$reject(self.$error())
                } else {
                return promise.$resolve(self.$Native(data))
              }}, TMP_7.$$s = self, TMP_7), $a).call($b))}, TMP_6.$$s = self, TMP_6), $a).call($b);
        });

        Opal.defn(self, '$with_promise_retry', TMP_8 = function(info, retries) {
          var $a, $b, TMP_9, self = this, $iter = TMP_8.$$p, block = $iter || nil;

          if (info == null) {
            info = ""
          }
          if (retries == null) {
            retries = 2
          }
          TMP_8.$$p = null;
          return ($a = ($b = $scope.get('Promise').$new()).$tap, $a.$$p = (TMP_9 = function(promise){var self = TMP_9.$$s || this, $a, $b, TMP_10, remaining = nil, handler = nil;
if (promise == null) promise = nil;
          remaining = retries;
            handler = ($a = ($b = self).$lambda, $a.$$p = (TMP_10 = function(error, data){var self = TMP_10.$$s || this;
              if ($gvars.log == null) $gvars.log = nil;
if (error == null) error = nil;if (data == null) data = nil;
            if (error !== false && error !== nil) {
                remaining = $rb_minus(remaining, 1);
                if ($rb_ge(remaining, 0)) {
                  $gvars.log.$info("" + (remaining) + " remaining retries " + (info));
                  return block.$call(handler);
                  } else {
                  $gvars.log.$error(error);
                  return promise.$reject(error);
                };
                } else {
                $gvars.log.$info("successs " + (info));
                return promise.$resolve(data);
              }}, TMP_10.$$s = self, TMP_10), $a).call($b);
            return block.$call(handler);}, TMP_9.$$s = self, TMP_9), $a).call($b);
        });

        Opal.defn(self, '$authenticate', function() {
          var $a, $b, TMP_11, self = this;

          return ($a = ($b = self).$with_promise, $a.$$p = (TMP_11 = function(iblock){var self = TMP_11.$$s || this;
            if (self.root == null) self.root = nil;
if (iblock == null) iblock = nil;
          return self.root.authenticate(iblock);}, TMP_11.$$s = self, TMP_11), $a).call($b);
        });

        Opal.defn(self, '$get_account_info', function() {
          var $a, $b, TMP_12, self = this;

          return ($a = ($b = self).$with_promise, $a.$$p = (TMP_12 = function(iblock){var self = TMP_12.$$s || this;
            if (self.root == null) self.root = nil;
if (iblock == null) iblock = nil;
          return self.root.getAccountInfo(iblock);}, TMP_12.$$s = self, TMP_12), $a).call($b);
        });

        Opal.defn(self, '$write_file', function(filename, data) {
          var $a, $b, TMP_13, self = this;
          if ($gvars.log == null) $gvars.log = nil;

          $gvars.log.$debug("waiting");
          return ($a = ($b = self).$with_promise_retry, $a.$$p = (TMP_13 = function(iblock){var self = TMP_13.$$s || this;
            if (self.root == null) self.root = nil;
if (iblock == null) iblock = nil;
          return self.root.writeFile(filename, data, iblock);}, TMP_13.$$s = self, TMP_13), $a).call($b, filename, 2);
        });

        Opal.defn(self, '$read_file', function(filename) {
          var $a, $b, TMP_14, self = this;

          return ($a = ($b = self).$with_promise, $a.$$p = (TMP_14 = function(iblock){var self = TMP_14.$$s || this;
            if (self.root == null) self.root = nil;
if (iblock == null) iblock = nil;
          return self.root.readFile(filename, iblock);}, TMP_14.$$s = self, TMP_14), $a).call($b);
        });

        Opal.defn(self, '$read_dir', function(dirname) {
          var $a, $b, TMP_15, self = this;

          if (dirname == null) {
            dirname = "/"
          }
          return ($a = ($b = self).$with_promise, $a.$$p = (TMP_15 = function(iblock){var self = TMP_15.$$s || this;
            if (self.root == null) self.root = nil;
if (iblock == null) iblock = nil;
          self.root.readdir(dirname, iblock);
            return nil;}, TMP_15.$$s = self, TMP_15), $a).call($b);
        });

        return (Opal.defn(self, '$choose_file', function(options) {
          var $a, $b, TMP_16, self = this;

          return ($a = ($b = self).$with_promise_chooser, $a.$$p = (TMP_16 = function(iblock){var self = TMP_16.$$s || this;
if (iblock == null) iblock = nil;
          
              dropbox_options = {

                  // Required. Called when a user selects an item in the Chooser.
                      success: iblock,

                  // Optional. Called when the user closes the dialog without selecting a file
                  // and does not include any parameters.
                          cancel: function() {

                                  },

                       linkType: "direct", // or "direct"

                  // Optional. This is a list of file extensions. If specified, the user will
                  // only be able to select files with these extensions. You may also specify
                  // file types, such as "video" or "images" in the list. For more information,
                  // see File types below. By default, all extensions are allowed.
                      extensions: ['.abc'],
                  };

                  Dropbox.choose(dropbox_options);
          ;}, TMP_16.$$s = self, TMP_16), $a).call($b);
        }), nil) && 'choose_file';
      })($scope.base, null);
    })($scope.base)
  })($scope.base);
};

/* Generated by Opal 0.9.0 */
Opal.modules["opal-jqconsole"] = function(Opal) {
  Opal.dynamic_require_severity = "error";
  var OPAL_CONFIG = { method_missing: true, arity_check: false, freezing: true, tainting: true };
  function $rb_plus(lhs, rhs) {
    return (typeof(lhs) === 'number' && typeof(rhs) === 'number') ? lhs + rhs : lhs['$+'](rhs);
  }
  var self = Opal.top, $scope = Opal, nil = Opal.nil, $breaker = Opal.breaker, $slice = Opal.slice, $module = Opal.module, $klass = Opal.klass, $range = Opal.range;

  Opal.add_stubs(['$expose', '$Native', '$jqconsole', '$find', '$on_command', '$Write', '$handler', '$Focus', '$to_json', '$last', '$GetHistory', '$nil?', '$parse', '$SetHistory', '$[]', '$private', '$call', '$+', '$message', '$Prompt', '$lambda']);
  return (function($base) {
    var $JqConsole, self = $JqConsole = $module($base, 'JqConsole');

    var def = self.$$proto, $scope = self.$$scope;

    (function($base, $super) {
      function $JqConsole(){};
      var self = $JqConsole = $klass($base, $super, 'JqConsole', $JqConsole);

      var def = self.$$proto, $scope = self.$$scope, TMP_2;

      def.jqconsole = def.handler = nil;
      Opal.defn(self, '$initialize', function(container, prompt, message) {
        var $a, $b, TMP_1, self = this;

        if (message == null) {
          message = ""
        }
        $scope.get('Element').$expose("jqconsole");
        self.jqconsole = self.$Native($scope.get('Element').$find("#" + (container)).$jqconsole(message, prompt));
        ($a = ($b = self).$on_command, $a.$$p = (TMP_1 = function(cmd){var self = TMP_1.$$s || this;
          if (self.jqconsole == null) self.jqconsole = nil;
if (cmd == null) cmd = nil;
        return self.jqconsole.$Write("no handler installed; -> " + (cmd) + "\n")}, TMP_1.$$s = self, TMP_1), $a).call($b);
        self.$handler();
        return self.jqconsole;
      });

      Opal.defn(self, '$on_command', TMP_2 = function() {
        var self = this, $iter = TMP_2.$$p, block = $iter || nil;

        TMP_2.$$p = null;
        return self.handler = block;
      });

      Opal.defn(self, '$write', function() {
        var $a, self = this, $splat_index = nil;

        var array_size = arguments.length - 0;
        if(array_size < 0) array_size = 0;
        var stuff = new Array(array_size);
        for($splat_index = 0; $splat_index < array_size; $splat_index++) {
          stuff[$splat_index] = arguments[$splat_index + 0];
        }
        return ($a = self.jqconsole).$Write.apply($a, Opal.to_a(stuff));
      });

      Opal.defn(self, '$write_html', function(html_str) {
        var self = this;

        return self.jqconsole.$Write(html_str, "unescaped", false);
      });

      Opal.defn(self, '$set_focus', function() {
        var self = this;

        return self.jqconsole.$Focus();
      });

      Opal.defn(self, '$save_to_localstorage', function() {
        var self = this, history = nil;

        history = self.$Native(self.jqconsole.$GetHistory()).$last(15).$to_json();
        localStorage.setItem('console_history', history);
      });

      Opal.defn(self, '$load_from_loacalstorage', function() {
        var $a, self = this, history = nil;

        history = self.$Native(localStorage.getItem('console_history'));
        if ((($a = history['$nil?']()) !== nil && (!$a.$$is_boolean || $a == true))) {
          return nil
          } else {
          history = $scope.get('JSON').$parse(history);
          if ((($a = history['$nil?']()) !== nil && (!$a.$$is_boolean || $a == true))) {
            return nil
            } else {
            return self.jqconsole.$SetHistory(history)
          };
        };
      });

      Opal.defn(self, '$get_history', function() {
        var self = this;

        return self.jqconsole.$GetHistory();
      });

      Opal.defn(self, '$set_history', function(array) {
        var self = this;

        return self.jqconsole.$SetHistory(array['$[]']($range(0, 4, false)));
      });

      self.$private();

      return (Opal.defn(self, '$handler', function(cmd) {
        var $a, $b, TMP_3, $c, TMP_4, self = this, e = nil;

        if ((($a = (($b = cmd !== false && cmd !== nil) ? cmd != undefined : cmd)) !== nil && (!$a.$$is_boolean || $a == true))) {
          try {
          self.handler.$call(cmd)
          } catch ($err) {if (Opal.rescue($err, [$scope.get('Exception')])) {e = $err;
            try {
              self.jqconsole.$Write($rb_plus($rb_plus("Error: ", e.$message()), "\n"))
            } finally {
              Opal.gvars["!"] = Opal.exceptions.pop() || Opal.nil;
            }
            }else { throw $err; }
          }};
        return self.jqconsole.$Prompt(true, ($a = ($b = self).$lambda, $a.$$p = (TMP_3 = function(c){var self = TMP_3.$$s || this;
if (c == null) c = nil;
        return self.$handler(c)}, TMP_3.$$s = self, TMP_3), $a).call($b), ($a = ($c = self).$lambda, $a.$$p = (TMP_4 = function(c){var self = TMP_4.$$s || this;
if (c == null) c = nil;
        return false}, TMP_4.$$s = self, TMP_4), $a).call($c));
      }), nil) && 'handler';
    })($scope.base, null)
  })($scope.base)
};

/* Generated by Opal 0.9.0 */
Opal.modules["confstack"] = function(Opal) {
  Opal.dynamic_require_severity = "error";
  var OPAL_CONFIG = { method_missing: true, arity_check: false, freezing: true, tainting: true };
  function $rb_plus(lhs, rhs) {
    return (typeof(lhs) === 'number' && typeof(rhs) === 'number') ? lhs + rhs : lhs['$+'](rhs);
  }
  var self = Opal.top, $scope = Opal, nil = Opal.nil, $breaker = Opal.breaker, $slice = Opal.slice, $klass = Opal.klass, $hash2 = Opal.hash2, $range = Opal.range;

  Opal.add_stubs(['$require', '$attr_accessor', '$+', '$push', '$push_from_env', '$is_a?', '$first', '$caller', '$_flatten', '$count', '$new', '$get_from_env', '$[]', '$to_json', '$[]=', '$push_to_env', '$include?', '$keys', '$nil?', '$parse', '$pop', '$get', '$open', '$puts', '$to_yaml', '$_get_one', '$!', '$raise', '$==', '$_resolve_dependencies', '$each', '$call', '$uniq', '$flatten', '$compact', '$map', '$_get_keys', '$each_with_index', '$last', '$select', '$to_i', '$split', '$_update_hash', '$private', '$_keys', '$inject', '$_get', '$_add_hash', '$empty?', '$join', '$<<', '$has_key?', '$index', '$class', '$_resolve_value_dependency', '$_resolve_hash_dependency', '$_resolve_array_dependency']);
  self.$require("json");
  return (function($base, $super) {
    function $Confstack(){};
    var self = $Confstack = $klass($base, $super, 'Confstack', $Confstack);

    var def = self.$$proto, $scope = self.$$scope, TMP_3;

    def.confstack_name = def.sourcestack = def.confstack = def.confresult_flat = def.strict = def.confkeys_flat = def.confresult_cache = def.callstack = nil;
    self.$attr_accessor("strict");

    Opal.defn(self, '$initialize', function(name) {
      var self = this;

      if (name == null) {
        name = "default"
      }
      self.callstack = [];
      self.confstack = [$hash2([], {})];
      self.confresult_flat = $hash2([], {});
      self.confresult_cache = $hash2([], {});
      self.sourcestack = [];
      self.strict = true;
      if (name !== false && name !== nil) {
        self.confstack_name = $rb_plus("confstack__", name);
        self.$push($hash2(["confstack"], {"confstack": $hash2(["env"], {"env": self.confstack_name})}));
        self.$push_from_env();};
      return self;
    });

    Opal.defn(self, '$push', function(hash) {
      var $a, self = this;

      if (hash == null) {
        hash = $hash2([], {})
      }
      self.confresult_cache = $hash2([], {});
      if ((($a = hash['$is_a?']($scope.get('Hash'))) !== nil && (!$a.$$is_boolean || $a == true))) {
        self.sourcestack.$push(self.$caller().$first());
        self.confstack.$push(hash);
        self.$_flatten();
        } else {
        self
      };
      return self.confstack.$count();
    });

    Opal.defn(self, '$push_to_env', function(hash) {
      var self = this, localstack = nil, newpush = nil, hash_json = nil;

      if (hash == null) {
        hash = $hash2([], {})
      }
      localstack = $scope.get('Confstack').$new(false);
      localstack.$push(self.$get_from_env());
      localstack.$push(hash);
      newpush = localstack['$[]']();
      hash_json = newpush.$to_json();
      $scope.get('ENV')['$[]='](self['$[]']("confstack.env"), hash_json);
      return self.$push(hash);
    });

    Opal.defn(self, '$push_from_env', function() {
      var self = this;

      return self.$push_to_env(self.$get_from_env());
    });

    Opal.defn(self, '$get_from_env', function() {
      var $a, self = this, result = nil, fromenv = nil;

      result = $hash2([], {});
      if ((($a = self.$keys()['$include?']("confstack.env")) !== nil && (!$a.$$is_boolean || $a == true))) {
        fromenv = $scope.get('ENV')['$[]'](self['$[]']("confstack.env"));
        if ((($a = fromenv['$nil?']()) !== nil && (!$a.$$is_boolean || $a == true))) {
          } else {
          result = $scope.get('JSON').$parse(fromenv)
        };};
      return result;
    });

    Opal.defn(self, '$pop', function() {
      var self = this;

      self.confresult_cache = $hash2([], {});
      self.sourcestack.$pop();
      self.confstack.$pop();
      self.$_flatten();
      return self.confstack.$count();
    });

    Opal.defn(self, '$save', function(name) {
      var $a, $b, TMP_1, $c, TMP_2, self = this, result = nil;

      result = self.$get();
      ($a = ($b = $scope.get('File')).$open, $a.$$p = (TMP_1 = function(f){var self = TMP_1.$$s || this;
if (f == null) f = nil;
      return f.$puts(result.$to_yaml())}, TMP_1.$$s = self, TMP_1), $a).call($b, "" + (name) + ".yaml", "w");
      return ($a = ($c = $scope.get('File')).$open, $a.$$p = (TMP_2 = function(f){var self = TMP_2.$$s || this;
if (f == null) f = nil;
      return f.$puts(result.$to_json())}, TMP_2.$$s = self, TMP_2), $a).call($c, "" + (name) + ".json", "w");
    });

    Opal.defn(self, '$get', function(key, options) {
      var $a, $b, $c, self = this, result = nil;

      if (key == null) {
        key = nil
      }
      if (options == null) {
        options = $hash2(["resolve"], {"resolve": true})
      }
      if ((($a = key['$nil?']()) !== nil && (!$a.$$is_boolean || $a == true))) {
        result = self.confresult_flat
        } else {
        result = self.$_get_one(self.confresult_flat, key);
        if (result !== false && result !== nil) {
        } else if ((($a = ($b = ($c = result['$nil?'](), $c !== false && $c !== nil ?self.$keys()['$include?'](key)['$!']() : $c), $b !== false && $b !== nil ?self.strict : $b)) !== nil && (!$a.$$is_boolean || $a == true))) {
          self.$raise("confstack: key not available: " + (key))};
      };
      if (options['$[]']("resolve")['$=='](true)) {
        result = self.$_resolve_dependencies(key, result)};
      return result;
    });

    Opal.alias(self, '[]', 'get');

    Opal.defn(self, '$each', TMP_3 = function() {
      var $a, $b, TMP_4, self = this, $iter = TMP_3.$$p, block = $iter || nil;

      TMP_3.$$p = null;
      return ($a = ($b = self.$keys()).$each, $a.$$p = (TMP_4 = function(k){var self = TMP_4.$$s || this;
if (k == null) k = nil;
      return block.$call(k, self['$[]'](k))}, TMP_4.$$s = self, TMP_4), $a).call($b);
    });

    Opal.defn(self, '$keys', function() {
      var self = this;

      return self.confkeys_flat;
    });

    Opal.defn(self, '$_keys', function() {
      var $a, $b, TMP_5, self = this;

      return ($a = ($b = self.confstack).$map, $a.$$p = (TMP_5 = function(s){var self = TMP_5.$$s || this;
if (s == null) s = nil;
      return self.$_get_keys(s)}, TMP_5.$$s = self, TMP_5), $a).call($b).$compact().$flatten().$uniq();
    });

    Opal.defn(self, '$get_source', function(key) {
      var $a, $b, TMP_6, $c, TMP_7, $d, TMP_8, self = this, stack = nil, result = nil;

      stack = ($a = ($b = self.confstack.$each_with_index()).$map, $a.$$p = (TMP_6 = function(s, index){var self = TMP_6.$$s || this;
if (s == null) s = nil;if (index == null) index = nil;
      return [self.$_get_one(s, key), index]}, TMP_6.$$s = self, TMP_6), $a).call($b);
      result = ($a = ($c = stack).$select, $a.$$p = (TMP_7 = function(value){var self = TMP_7.$$s || this;
if (value == null) value = nil;
      return (value.$first()['$nil?']())['$!']()}, TMP_7.$$s = self, TMP_7), $a).call($c).$last();
      ($a = ($d = self.confstack).$map, $a.$$p = (TMP_8 = function(s, index){var self = TMP_8.$$s || this;
if (s == null) s = nil;if (index == null) index = nil;
      return self.$_get_one(s, key)}, TMP_8.$$s = self, TMP_8), $a).call($d).$compact().$last();
      return self.sourcestack['$[]'](result.$last().$to_i());
    });

    Opal.defn(self, '$[]=', function(key, value) {
      var self = this, keys = nil;

      keys = key.$split(".");
      self.$_update_hash(self.confstack.$last(), keys, value);
      return self.$_flatten();
    });

    self.$private();

    Opal.defn(self, '$_flatten', function() {
      var $a, $b, TMP_9, self = this;

      self.confkeys_flat = self.$_keys();
      self.confresult_flat = ($a = ($b = self.confkeys_flat).$inject, $a.$$p = (TMP_9 = function(result, element){var self = TMP_9.$$s || this;
if (result == null) result = nil;if (element == null) element = nil;
      result['$[]='](element, self.$_get(element));
        self.$_add_hash(result, element.$split("."), self.$_get(element), nil);
        return result;}, TMP_9.$$s = self, TMP_9), $a).call($b, $hash2([], {}));
      self.confresult_flat = self.confresult_flat['$[]'](nil);
      if ((($a = self.confresult_flat['$nil?']()) !== nil && (!$a.$$is_boolean || $a == true))) {
        return self.confresult_flat = $hash2([], {})
        } else {
        return nil
      };
    });

    Opal.defn(self, '$_add_hash', function(hash, keys, value, current_key) {
      var $a, self = this;

      if ((($a = hash['$nil?']()) !== nil && (!$a.$$is_boolean || $a == true))) {
        hash = $hash2([], {})};
      if ((($a = keys['$empty?']()) !== nil && (!$a.$$is_boolean || $a == true))) {
        hash['$[]='](current_key, value)
        } else {
        if ((($a = hash['$[]'](current_key)['$is_a?']($scope.get('Hash'))) !== nil && (!$a.$$is_boolean || $a == true))) {
          } else {
          hash['$[]='](current_key, $hash2([], {}))
        };
        self.$_add_hash(hash['$[]'](current_key), keys['$[]']($range(1, -1, false)), value, keys.$first());
      };
      return hash;
    });

    Opal.defn(self, '$_update_hash', function(hash, keys, value) {
      var $a, self = this;

      if ((($a = hash['$nil?']()) !== nil && (!$a.$$is_boolean || $a == true))) {
        hash = $hash2([], {})};
      if ((($a = keys['$empty?']()) !== nil && (!$a.$$is_boolean || $a == true))) {
        return self.$raise("empty keys in _add_hash")
        } else {
        if (keys.$count()['$=='](1)) {
          hash['$[]='](keys.$first(), value)
          } else {
          if ((($a = hash['$[]'](keys.$first())['$is_a?']($scope.get('Hash'))) !== nil && (!$a.$$is_boolean || $a == true))) {
            } else {
            hash['$[]='](keys.$first(), $hash2([], {}))
          };
          self.$_update_hash(hash['$[]'](keys.$first()), keys['$[]']($range(1, -1, false)), value);
        };
        return hash;
      };
    });

    Opal.defn(self, '$_get', function(key) {
      var $a, $b, TMP_10, self = this;

      return ($a = ($b = self.confstack).$map, $a.$$p = (TMP_10 = function(s, index){var self = TMP_10.$$s || this;
if (s == null) s = nil;if (index == null) index = nil;
      return self.$_get_one(s, key)}, TMP_10.$$s = self, TMP_10), $a).call($b).$compact().$last();
    });

    Opal.defn(self, '$_get_one', function(hash, key) {
      var $a, $b, TMP_11, self = this, keys = nil, retval = nil;

      keys = key.$split(".");
      retval = ($a = ($b = keys).$inject, $a.$$p = (TMP_11 = function(result, element){var self = TMP_11.$$s || this, $a;
if (result == null) result = nil;if (element == null) element = nil;
      if ((($a = result['$is_a?']($scope.get('Hash'))) !== nil && (!$a.$$is_boolean || $a == true))) {
          } else {
          return ($breaker.$v = nil, $breaker)
        };
        return result['$[]'](element);}, TMP_11.$$s = self, TMP_11), $a).call($b, hash);
      return retval;
    });

    Opal.defn(self, '$_get_keys', function(hash, path) {
      var $a, $b, TMP_12, self = this, retval = nil;

      if (path == null) {
        path = nil
      }
      retval = ($a = ($b = hash.$keys()).$inject, $a.$$p = (TMP_12 = function(result, key){var self = TMP_12.$$s || this, $a, newpath = nil, newhash = nil;
if (result == null) result = nil;if (key == null) key = nil;
      newpath = [path, key].$compact().$join(".");
        newhash = hash['$[]'](key);
        result['$<<'](newpath);
        if ((($a = newhash['$is_a?']($scope.get('Hash'))) !== nil && (!$a.$$is_boolean || $a == true))) {
          result['$<<'](self.$_get_keys(newhash, newpath))};
        return result;}, TMP_12.$$s = self, TMP_12), $a).call($b, []);
      return retval.$compact().$flatten();
    });

    Opal.defn(self, '$_resolve_value_dependency', function(key, value) {
      var $a, $b, TMP_13, self = this, result = nil, loop = nil;

      if ((($a = self.confresult_cache['$has_key?'](value)) !== nil && (!$a.$$is_boolean || $a == true))) {
        result = self.confresult_cache['$[]'](value)
        } else {
        if ((($a = self.callstack['$include?'](key)) !== nil && (!$a.$$is_boolean || $a == true))) {
          loop = self.callstack['$[]']($range(($a = ($b = self.callstack).$index, $a.$$p = (TMP_13 = function(x){var self = TMP_13.$$s || this;
if (x == null) x = nil;
          return x['$=='](key)}, TMP_13.$$s = self, TMP_13), $a).call($b), -1, false));
          self.callstack = [];
          self.$raise("circular conf dependency: " + ($rb_plus(loop, ["" + (key) + " ..."])));};
        self.callstack.$push(key);
        result = value.$call();
        self.callstack.$pop();
        self.confresult_cache['$[]='](key, result);
      };
      return result;
    });

    Opal.defn(self, '$_resolve_array_dependency', function(key, array) {
      var $a, $b, TMP_14, self = this;

      return ($a = ($b = array).$map, $a.$$p = (TMP_14 = function(f){var self = TMP_14.$$s || this;
if (f == null) f = nil;
      return self.$_resolve_dependencies(nil, f)}, TMP_14.$$s = self, TMP_14), $a).call($b);
    });

    Opal.defn(self, '$_resolve_hash_dependency', function(key, hash) {
      var $a, $b, TMP_15, self = this, result = nil;

      result = ($a = ($b = hash).$inject, $a.$$p = (TMP_15 = function(r, v){var self = TMP_15.$$s || this;
if (r == null) r = nil;if (v == null) v = nil;
      r['$[]='](v.$first(), self.$_resolve_dependencies([key, v.$first()], v.$last()));
        return r;}, TMP_15.$$s = self, TMP_15), $a).call($b, $hash2([], {}));
      return result;
    });

    return (Opal.defn(self, '$_resolve_dependencies', function(key, result) {
      var self = this;

      if (result.$class()['$==']($scope.get('Proc'))) {
        result = self.$_resolve_value_dependency(key, result)};
      if (result.$class()['$==']($scope.get('Hash'))) {
        result = self.$_resolve_hash_dependency(key, result)};
      if (result.$class()['$==']($scope.get('Array'))) {
        result = self.$_resolve_array_dependency(key, result)};
      return result;
    }), nil) && '_resolve_dependencies';
  })($scope.base, null);
};

/* Generated by Opal 0.9.0 */
Opal.modules["opal-abc2svg"] = function(Opal) {
  Opal.dynamic_require_severity = "error";
  var OPAL_CONFIG = { method_missing: true, arity_check: false, freezing: true, tainting: true };
  function $rb_plus(lhs, rhs) {
    return (typeof(lhs) === 'number' && typeof(rhs) === 'number') ? lhs + rhs : lhs['$+'](rhs);
  }
  function $rb_gt(lhs, rhs) {
    return (typeof(lhs) === 'number' && typeof(rhs) === 'number') ? lhs > rhs : lhs['$>'](rhs);
  }
  function $rb_times(lhs, rhs) {
    return (typeof(lhs) === 'number' && typeof(rhs) === 'number') ? lhs * rhs : lhs['$*'](rhs);
  }
  function $rb_minus(lhs, rhs) {
    return (typeof(lhs) === 'number' && typeof(rhs) === 'number') ? lhs - rhs : lhs['$-'](rhs);
  }
  var self = Opal.top, $scope = Opal, nil = Opal.nil, $breaker = Opal.breaker, $slice = Opal.slice, $module = Opal.module, $klass = Opal.klass, $hash2 = Opal.hash2, $gvars = Opal.gvars, $range = Opal.range;

  Opal.add_stubs(['$lambda', '$set_callback', '$error', '$+', '$[]', '$===', '$_anno_start', '$_anno_stop', '$push', '$_get_abcmodel', '$to_n', '$unhighlight_all', '$range_highlight_more', '$each', '$find', '$join', '$uniq', '$flatten', '$split', '$attr', '$get_elements_by_range', '$gsub', '$_set_on_select', '$_translate', '$html', '$get_svg', '$[]=', '$sort', '$map', '$>', '$*', '$-', '$first', '$last', '$private', '$to_s', '$class', '$inject', '$_clone_abc2svg_object', '$Native', '$_get_extra', '$clone', '$<<', '$to_a', '$each_with_index', '$==', '$loglevel', '$debug', '$to_json', '$length', '$_mk_id', '$on', '$stop_propagation', '$call', '$id', '$current_target']);
  return (function($base) {
    var $ABC2SVG, self = $ABC2SVG = $module($base, 'ABC2SVG');

    var def = self.$$proto, $scope = self.$$scope;

    (function($base, $super) {
      function $Abc2Svg(){};
      var self = $Abc2Svg = $klass($base, $super, 'Abc2Svg', $Abc2Svg);

      var def = self.$$proto, $scope = self.$$scope, TMP_10, TMP_11;

      def.user = def.printer = def.root = def.abc_model = def.svgbuf = def.element_to_position = def.object_map = def.abc_source = nil;
      Opal.defn(self, '$initialize', function(div, options) {
        var $a, $b, TMP_1, $c, TMP_2, $d, TMP_3, $e, TMP_4, $f, TMP_5, $g, TMP_6, $h, TMP_7, self = this, $case = nil;
        if ($gvars.log == null) $gvars.log = nil;

        if (options == null) {
          options = $hash2(["mode"], {"mode": "svg"})
        }
        self.on_select = ($a = ($b = self).$lambda, $a.$$p = (TMP_1 = function(element){var self = TMP_1.$$s || this;
if (element == null) element = nil;
        return nil}, TMP_1.$$s = self, TMP_1), $a).call($b);
        self.printer = div;
        self.svgbuf = [];
        self.abc_source = "";
        self.element_to_position = $hash2([], {});
        self.abc_model = nil;
        self.object_map = $hash2([], {});
        self.user = $hash2(["img_out", "errmsg", "read_file", "annotate", "page_format"], {"img_out": nil, "errmsg": nil, "read_file": nil, "annotate": true, "page_format": true});
        ($a = ($c = self).$set_callback, $a.$$p = (TMP_2 = function(message, line_number, column_number){var self = TMP_2.$$s || this;
          if ($gvars.log == null) $gvars.log = nil;
if (message == null) message = nil;if (line_number == null) line_number = nil;if (column_number == null) column_number = nil;
        return $gvars.log.$error(message, [$rb_plus(line_number, 1), $rb_plus(column_number, 1)])}, TMP_2.$$s = self, TMP_2), $a).call($c, "errmsg");
        $case = options['$[]']("mode");if ("svg"['$===']($case)) {($a = ($d = self).$set_callback, $a.$$p = (TMP_3 = function(type, start, stop, x, y, w, h){var self = TMP_3.$$s || this;
if (type == null) type = nil;if (start == null) start = nil;if (stop == null) stop = nil;if (x == null) x = nil;if (y == null) y = nil;if (w == null) w = nil;if (h == null) h = nil;
        return self.$_anno_start(type, start, stop, x, y, w, h)}, TMP_3.$$s = self, TMP_3), $a).call($d, "anno_start");
        ($a = ($e = self).$set_callback, $a.$$p = (TMP_4 = function(type, start, stop, x, y, w, h){var self = TMP_4.$$s || this;
if (type == null) type = nil;if (start == null) start = nil;if (stop == null) stop = nil;if (x == null) x = nil;if (y == null) y = nil;if (w == null) w = nil;if (h == null) h = nil;
        return self.$_anno_stop(type, start, stop, x, y, w, h)}, TMP_4.$$s = self, TMP_4), $a).call($e, "anno_stop");
        ($a = ($f = self).$set_callback, $a.$$p = (TMP_5 = function(svg){var self = TMP_5.$$s || this;
          if (self.svgbuf == null) self.svgbuf = nil;
if (svg == null) svg = nil;
        return self.svgbuf.$push(svg)}, TMP_5.$$s = self, TMP_5), $a).call($f, "img_out");
        ($a = ($g = self).$set_callback, $a.$$p = (TMP_6 = function(tsfirst, voice_tb, anno_type){var self = TMP_6.$$s || this;
if (tsfirst == null) tsfirst = nil;if (voice_tb == null) voice_tb = nil;if (anno_type == null) anno_type = nil;
        return nil}, TMP_6.$$s = self, TMP_6), $a).call($g, "get_abcmodel");}else if ("model"['$===']($case)) {($a = ($h = self).$set_callback, $a.$$p = (TMP_7 = function(tsfirst, voice_tb, anno_type, info){var self = TMP_7.$$s || this;
if (tsfirst == null) tsfirst = nil;if (voice_tb == null) voice_tb = nil;if (anno_type == null) anno_type = nil;if (info == null) info = nil;
        return self.$_get_abcmodel(tsfirst, voice_tb, anno_type, info)}, TMP_7.$$s = self, TMP_7), $a).call($h, "get_abcmodel")}else {$gvars.log.$error("BUG: unsupported mode for abc2svg")};
        return self.root = new Abc(self.user.$to_n());
      });

      Opal.defn(self, '$range_highlight', function(from, to) {
        var self = this;

        self.$unhighlight_all();
        self.$range_highlight_more(from, to);
        return nil;
      });

      Opal.defn(self, '$range_highlight_more', function(from, to) {
        var $a, $b, TMP_8, self = this;

        ($a = ($b = self.$get_elements_by_range(from, to)).$each, $a.$$p = (TMP_8 = function(id){var self = TMP_8.$$s || this, element = nil, classes = nil;
if (id == null) id = nil;
        element = $scope.get('Element').$find("#" + (id));
          element.parents('svg').get(0).scrollIntoView();
          classes = [element.$attr("class").$split(" "), "highlight"].$flatten().$uniq().$join(" ");
          return element.$attr("class", classes);}, TMP_8.$$s = self, TMP_8), $a).call($b);
        return nil;
      });

      Opal.defn(self, '$range_unhighlight_more', function(from, to) {
        var $a, $b, TMP_9, self = this;

        return ($a = ($b = self.$get_elements_by_range(from, to)).$each, $a.$$p = (TMP_9 = function(id){var self = TMP_9.$$s || this, foo = nil, classes = nil;
if (id == null) id = nil;
        foo = $scope.get('Element').$find("#" + (id));
          classes = foo.$attr("class").$gsub("highlight", "");
          return foo.$attr("class", classes);}, TMP_9.$$s = self, TMP_9), $a).call($b);
      });

      Opal.defn(self, '$unhighlight_all', function() {
        var self = this;

        return $scope.get('Element').$find(".highlight").$attr("class", "abcref");
      });

      Opal.defn(self, '$on_select', TMP_10 = function() {
        var self = this, $iter = TMP_10.$$p, block = $iter || nil;

        TMP_10.$$p = null;
        self.on_select = block;
        return self.$_set_on_select();
      });

      Opal.defn(self, '$draw', function(abc_code) {
        var self = this;

        self.$_translate("abc", abc_code);
        self.printer.$html(self.$get_svg());
        self.$_set_on_select();
        return nil;
      });

      Opal.defn(self, '$get_abcmodel', function(abc_code) {
        var self = this;

        self.root.tosvg("abc", abc_code);
        return self.abc_model;
      });

      Opal.defn(self, '$get_svg', function() {
        var self = this;

        return self.svgbuf.$join("\n");
      });

      Opal.defn(self, '$set_callback', TMP_11 = function(event) {
        var self = this, $iter = TMP_11.$$p, block = $iter || nil;

        TMP_11.$$p = null;
        return self.user['$[]='](event, block);
      });

      Opal.defn(self, '$get_elements_by_range', function(from, to) {
        var $a, $b, TMP_12, self = this, range = nil, result = nil;

        range = [from, to].$sort();
        result = [];
        ($a = ($b = self.element_to_position).$each, $a.$$p = (TMP_12 = function(k, value){var self = TMP_12.$$s || this, $a, $b, TMP_13, noterange = nil;
if (k == null) k = nil;if (value == null) value = nil;
        noterange = ($a = ($b = ["startChar", "endChar"]).$map, $a.$$p = (TMP_13 = function(c){var self = TMP_13.$$s || this;
if (c == null) c = nil;
          return value['$[]'](c)}, TMP_13.$$s = self, TMP_13), $a).call($b).$sort();
          if ($rb_gt($rb_times(($rb_minus(range.$first(), noterange.$last())), ($rb_minus(noterange.$first(), range.$last()))), 0)) {
            return result.$push(k)
            } else {
            return nil
          };}, TMP_12.$$s = self, TMP_12), $a).call($b);
        return result;
      });

      self.$private();

      Opal.defn(self, '$_clone_abc2svg_object', function(object) {
        var $a, $b, TMP_14, $c, TMP_15, self = this, dropkeys = nil, $case = nil, keys = nil, result = nil;

        dropkeys = ["next", "prev", "ts_next", "ts_prev", "extra"];
        $case = object.$class().$to_s();if ("Native::Object"['$===']($case)) {keys = $rb_minus(Object.keys(object.$to_n()), dropkeys);
        result = ($a = ($b = keys).$inject, $a.$$p = (TMP_14 = function(r1, key){var self = TMP_14.$$s || this;
if (r1 == null) r1 = nil;if (key == null) key = nil;
        r1['$[]='](key, self.$_clone_abc2svg_object(self.$Native(object['$[]'](key))));
          return r1;}, TMP_14.$$s = self, TMP_14), $a).call($b, $hash2([], {}));
        if ((($a = object['$[]']("extra")) !== nil && (!$a.$$is_boolean || $a == true))) {
          result['$[]=']("extra", self.$_get_extra(object))};
        self.object_map['$[]='](object['$[]']("__id__"), result);}else if ("Array"['$===']($case)) {result = ($a = ($c = object).$map, $a.$$p = (TMP_15 = function(element){var self = TMP_15.$$s || this;
if (element == null) element = nil;
        return self.$_clone_abc2svg_object(self.$Native(element))}, TMP_15.$$s = self, TMP_15), $a).call($c)}else {result = object};
        return result;
      });

      Opal.defn(self, '$_get_extra', function(object) {
        var $a, self = this, result = nil, next_object = nil, cloned_extra = nil;

        result = $hash2([], {});
        next_object = object['$[]']("extra");
        while (next_object !== false && next_object !== nil) {
        cloned_extra = self.$_clone_abc2svg_object(next_object);
        result['$[]='](cloned_extra['$[]']("type"), cloned_extra);
        next_object = next_object['$[]']("next");};
        return result;
      });

      Opal.defn(self, '$_get_abcmodel', function(tsfirst, voice_tb, music_types, info) {
        var $a, $b, TMP_16, self = this, tune = nil, info_clone = nil;
        if ($gvars.log == null) $gvars.log = nil;

        
          abcmidi = new AbcMIDI();
          abcmidi.add(tsfirst, voice_tb[0].key)
      ;
        tune = $hash2([], {});
        tune['$[]=']("music_types", self.$Native(music_types).$clone());
        tune['$[]=']("voices", ($a = ($b = self.$Native(voice_tb)).$map, $a.$$p = (TMP_16 = function(v){var self = TMP_16.$$s || this, $a, result = nil, curnote = nil, nextnote = nil, cloned_note = nil;
if (v == null) v = nil;
        result = $hash2(["voice_properties", "symbols"], {"voice_properties": self.$_clone_abc2svg_object(self.$Native(v)), "symbols": []});
          curnote = self.$Native(self.$Native(v)['$[]']("sym"));
          while (curnote !== false && curnote !== nil) {
          nextnote = curnote['$[]']("next");
          cloned_note = self.$_clone_abc2svg_object(curnote);
          result['$[]']("symbols")['$<<'](cloned_note);
          curnote = nextnote;};
          return result;}, TMP_16.$$s = self, TMP_16), $a).call($b));
        info_clone = self.$_clone_abc2svg_object(self.$Native(info));
        self.abc_model = $hash2(["music_types", "music_type_ids", "info", "voices"], {"music_types": music_types, "music_type_ids": $scope.get('Hash')['$[]'](music_types.$each_with_index().$to_a()), "info": info_clone, "voices": tune['$[]']("voices")});
        if ($gvars.log.$loglevel()['$==']("debug")) {
          $gvars.log.$debug(self.abc_model.$to_json())};
        return self.abc_model;
      });

      Opal.defn(self, '$_get_charpos', function(abc_source, line, column) {
        var $a, $b, TMP_17, self = this, lines = nil, result = nil;

        lines = self.abc_source.$split("\n");
        result = ($a = ($b = lines['$[]']($range(0, line, false))).$inject, $a.$$p = (TMP_17 = function(r, v){var self = TMP_17.$$s || this;
if (r == null) r = nil;if (v == null) v = nil;
        return r = $rb_plus(r, v.$length())}, TMP_17.$$s = self, TMP_17), $a).call($b, 0);
        return $rb_plus(result, column);
      });

      Opal.defn(self, '$_anno_start', function(music_type, start_offset, stop_offset, x, y, w, h) {
        var self = this, id = nil;

        id = self.$_mk_id(music_type, start_offset, stop_offset);
        
      self.root.out_svg('<g class="' + id +'">\n')
      ;
      });

      Opal.defn(self, '$_anno_stop', function(music_type, start_offset, stop_offset, x, y, w, h) {
        var self = this, id = nil;

        id = self.$_mk_id(music_type, start_offset, stop_offset);
        
          // close the container
          self.root.out_svg('</g>\n');
          // create a rectangle
          self.root.out_svg('<rect class="abcref" id="' + id +'" x="');
          self.root.out_sxsy(x, '" y="', y);
          self.root.out_svg('" width="' + w.toFixed(2) +
            '" height="' + h.toFixed(2) + '"/>\n')
        ;
        return self.element_to_position['$[]='](id, $hash2(["startChar", "endChar"], {"startChar": start_offset, "endChar": stop_offset}));
      });

      Opal.defn(self, '$_mk_id', function(music_type, start_offset, end_offset) {
        var self = this;

        return "_" + (music_type) + "_" + (start_offset) + "_" + (end_offset) + "_";
      });

      Opal.defn(self, '$_set_on_select', function() {
        var $a, $b, TMP_18, self = this;

        return ($a = ($b = $scope.get('Element').$find(".abcref")).$on, $a.$$p = (TMP_18 = function(evt){var self = TMP_18.$$s || this;
          if (self.on_select == null) self.on_select = nil;
          if (self.element_to_position == null) self.element_to_position = nil;
if (evt == null) evt = nil;
        evt.$stop_propagation();
          self.on_select.$call(self.element_to_position['$[]'](evt.$current_target().$id()));
          return nil;}, TMP_18.$$s = self, TMP_18), $a).call($b, "click");
      });

      return (Opal.defn(self, '$_translate', function(file_name, abc_source) {
        var self = this;

        self.abc_source = abc_source;
        self.element_to_position = $hash2([], {});
        self.svgbuf = [];
        
      self.root.tosvg(file_name, self.abc_source);
      ;
      }), nil) && '_translate';
    })($scope.base, null)
  })($scope.base)
};

/* Generated by Opal 0.9.0 */
Opal.modules["version"] = function(Opal) {
  Opal.dynamic_require_severity = "error";
  var OPAL_CONFIG = { method_missing: true, arity_check: false, freezing: true, tainting: true };
  var self = Opal.top, $scope = Opal, nil = Opal.nil, $breaker = Opal.breaker, $slice = Opal.slice;

  Opal.add_stubs(['$year', '$now']);
  Opal.cdecl($scope, 'VERSION', "1.0.0");
  Opal.cdecl($scope, 'SCHEMA_VERSION', "https://zupfnoter.weichel21.de/schema/zupfnoter-config_1.0.json");
  return Opal.cdecl($scope, 'COPYRIGHT', "©" + ($scope.get('Time').$now().$year()) + " http://www.bernhard-weichel.de/zupfnoterpage");
};

/* Generated by Opal 0.9.0 */
(function(Opal) {
  Opal.dynamic_require_severity = "error";
  var OPAL_CONFIG = { method_missing: true, arity_check: false, freezing: true, tainting: true };
  var self = Opal.top, $scope = Opal, nil = Opal.nil, $breaker = Opal.breaker, $slice = Opal.slice;

  Opal.add_stubs(['$require', '$puts']);
  self.$require("opal");
  self.$require("opal-jquery");
  self.$require("vector2d");
  self.$require("neatjson");
  self.$require("consolelogger");
  self.$require("harpnotes");
  self.$require("abc_to_harpnotes_factory");
  self.$require("abc2svg_to_harpnotes");
  self.$require("opal-raphael");
  self.$require("opal-jspdf");
  self.$require("opal-jszip");
  self.$require("opal-abcjs");
  self.$require("opal-musicaljs");
  self.$require("raphael_engine");
  self.$require("pdf_engine");
  self.$require("command-controller");
  self.$require("controller");
  self.$require("controller-nw");
  self.$require("controller_command_definitions");
  self.$require("harpnote_player");
  self.$require("text_pane");
  self.$require("opal-dropboxjs");
  self.$require("opal-jqconsole");
  self.$require("confstack");
  self.$require("opal-abc2svg");
  self.$require("version");
  self.$puts("now starting zupfnoter");
  return self.$puts("zupfnoter is now running");
})(Opal);
